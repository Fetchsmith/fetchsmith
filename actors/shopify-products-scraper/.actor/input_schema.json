{
  "title": "Shopify Products Scraper input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "storeUrls": {
      "title": "Store URLs",
      "type": "array",
      "description": "Shopify store homepages, collection URLs or product URLs, e.g. https://www.allbirds.com or https://www.allbirds.com/collections/mens.",
      "editor": "stringList",
      "prefill": [
        "https://www.allbirds.com"
      ],
      "default": ["https://www.allbirds.com"]
    },
    "maxProductsPerStore": {
      "title": "Max products per store",
      "type": "integer",
      "default": 500,
      "minimum": 1,
      "maximum": 100000,
      "description": "Stop after this many products for each store URL."
    },
    "includeDescription": {
      "title": "Include description",
      "type": "boolean",
      "description": "Include the product description as plain text.",
      "default": true
    },
    "includeVariants": {
      "title": "Include variants",
      "type": "boolean",
      "description": "Include the full variants array (SKU, price, options, availability).",
      "default": true
    },
    "onlyAvailable": {
      "title": "Only available products",
      "type": "boolean",
      "description": "Skip products where no variant is available.",
      "default": false
    },
    "maxResults": {
      "title": "Max results (total)",
      "type": "integer",
      "default": 5000,
      "minimum": 1,
      "maximum": 200000,
      "description": "Overall cap on the number of products across all stores."
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "editor": "proxy",
      "description": "Route storefront requests through Apify Proxy. Recommended: some stores rate-limit or geo-gate products.json by IP, and residential/country-specific proxies also return that country's prices and currency. Leave as-is to use automatic Apify Proxy; runs on accounts without proxy access fall back to a direct connection.",
      "default": { "useApifyProxy": true }
    },
    "detailLevel": {
      "title": "Detail level",
      "type": "string",
      "editor": "select",
      "description": "\"Basic\" (default) uses only the store's products.json/product.json feed — one request per page of products. \"Full\" additionally fetches each product's live page to read its SEO title/description and star-rating summary, which are not present in products.json. This is one extra HTTP request per product and is charged separately.",
      "enum": ["basic", "full"],
      "enumTitles": ["Basic (fast, feed only)", "Full (+ SEO title/description + rating, 1 extra request/product)"],
      "default": "basic"
    }
  },
  "required": [
    "storeUrls"
  ]
}