{
  "title": "Steam Reviews Scraper input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "dataType": {
      "title": "What to scrape",
      "type": "string",
      "description": "reviews = player reviews for the given games; games = the store record for each game (price, genres, developer, review score summary, optional live player count).",
      "default": "reviews",
      "enum": [
        "reviews",
        "games"
      ],
      "enumTitles": [
        "Player reviews",
        "Game / store details"
      ],
      "editor": "select"
    },
    "apps": {
      "title": "Games",
      "type": "array",
      "description": "Steam store URLs or numeric App IDs, e.g. https://store.steampowered.com/app/1145360/Hades/ or 1145360. Leading/trailing spaces are ignored.",
      "editor": "stringList",
      "prefill": [
        "https://store.steampowered.com/app/1145360/Hades/"
      ],
      "default": [
        "https://store.steampowered.com/app/1145360/Hades/"
      ]
    },
    "searchTerms": {
      "title": "Search terms",
      "type": "array",
      "description": "Find games by name instead of (or as well as) giving URLs. Each match is then scraped according to 'What to scrape'.",
      "editor": "stringList",
      "prefill": []
    },
    "maxReviewsPerApp": {
      "title": "Max reviews per game",
      "type": "integer",
      "description": "Reviews only: how many reviews to SCAN per game before moving to the next one (max 5000). This counts reviews looked at, before the keyword/minPlaytimeHours filter is applied — a low cap combined with a narrow filter can return far fewer matches than actually exist deeper in Steam's feed. Raise it if the log warns the cap was hit while filtering.",
      "default": 200,
      "minimum": 1,
      "maximum": 5000
    },
    "maxResults": {
      "title": "Max results (total)",
      "type": "integer",
      "description": "Hard cap on rows pushed to the dataset across all games — also caps what you are charged.",
      "default": 2000,
      "minimum": 1,
      "maximum": 50000
    },
    "language": {
      "title": "Review language",
      "type": "string",
      "description": "Steam language code for the reviews to return: english, schinese, russian, german, french, spanish, japanese, brazilian, ... or \"all\" for every language.",
      "default": "english",
      "editor": "textfield"
    },
    "reviewType": {
      "title": "Recommended / not recommended",
      "type": "string",
      "description": "positive = thumbs-up reviews only, negative = thumbs-down only.",
      "default": "all",
      "enum": [
        "all",
        "positive",
        "negative"
      ],
      "enumTitles": [
        "All reviews",
        "Positive only",
        "Negative only"
      ],
      "editor": "select"
    },
    "purchaseType": {
      "title": "Purchase type",
      "type": "string",
      "description": "steam = reviews from players who bought the game on Steam (excludes key activations and free weekends).",
      "default": "all",
      "enum": [
        "all",
        "steam",
        "non_steam_purchase"
      ],
      "enumTitles": [
        "All",
        "Steam purchases only",
        "Non-Steam purchases only"
      ],
      "editor": "select"
    },
    "includeOffTopic": {
      "title": "Include off-topic review bombs",
      "type": "boolean",
      "description": "Steam hides reviews from periods Valve flagged as off-topic review-bomb activity (a controversy rather than the game itself). Turn this on to get the unfiltered set — e.g. Total War: ROME II returns 88,334 reviews filtered vs 94,228 unfiltered. Also applies to the review totals on Game details rows.",
      "default": false
    },
    "sortBy": {
      "title": "Sort / selection",
      "type": "string",
      "description": "recent = newest first, updated = most recently edited first, all = Steam's helpfulness ranking, funny = Steam's \"Funny\" tab, the reviews with the most funny votes (always all-time). Note: Steam itself defaults 'all' to only the last 30 days unless you also set 'Last N days' below.",
      "default": "recent",
      "enum": [
        "recent",
        "updated",
        "all",
        "funny"
      ],
      "enumTitles": [
        "Most recent",
        "Recently updated",
        "Most helpful",
        "Funniest (all-time)"
      ],
      "editor": "select"
    },
    "dayRange": {
      "title": "Last N days (Most helpful only)",
      "type": "integer",
      "description": "With Sort = Most helpful, restrict to reviews from the last N days (1-365). Steam applies a 30-day window by default in that mode if you leave this blank, so set it to 365 for a true all-time 'most helpful' pull. Steam honours a day range in NO other sort mode, so this is ignored (with a warning) for Most recent / Recently updated / Funniest - use 'Reviews after/before' for a date window instead.",
      "minimum": 1,
      "maximum": 365,
      "editor": "number"
    },
    "reviewsAfter": {
      "title": "Reviews after (ISO date)",
      "type": "string",
      "description": "e.g. 2024-01-01. Keep only reviews created on or after this date. Unlike 'Last N days', this reaches any point in a game's history, not just the last year. Forces Sort to 'Most recent' so the date window can be applied correctly.",
      "editor": "textfield"
    },
    "reviewsBefore": {
      "title": "Reviews before (ISO date)",
      "type": "string",
      "description": "e.g. 2024-06-30. Keep only reviews created before this date. Combine with 'Reviews after' for an exact historical window (e.g. reviews from a specific patch or controversy). Forces Sort to 'Most recent'.",
      "editor": "textfield"
    },
    "minPlaytimeHours": {
      "title": "Minimum playtime (hours)",
      "type": "integer",
      "description": "Keep only reviews from players with at least this many hours in the game — a cheap way to drop drive-by reviews.",
      "minimum": 0,
      "editor": "number"
    },
    "keyword": {
      "title": "Keyword filter",
      "type": "string",
      "description": "Keep only reviews whose text contains this word or phrase (case-insensitive), e.g. \"crash\", \"controller\", \"price\". Leading/trailing spaces are ignored. Accented letters are Unicode-normalized before matching, so \"très\" matches regardless of which Unicode form (composed or decomposed) you typed it in.",
      "editor": "textfield"
    },
    "country": {
      "title": "Country / currency",
      "type": "string",
      "description": "Two-letter country code used for store prices and availability (us, gb, de, ...).",
      "default": "us",
      "editor": "textfield"
    },
    "includeGameInfo": {
      "title": "Attach game info to every review",
      "type": "boolean",
      "description": "Adds gameName, developers, publishers, genres, releaseDate and storeUrl to each review row (one extra request per game).",
      "default": true
    },
    "includePlayerCount": {
      "title": "Include live player count",
      "type": "boolean",
      "description": "Game details only: also fetch the current concurrent player count for each game (one extra request per game).",
      "default": false
    },
    "includeOwnerEstimates": {
      "title": "Include owner estimates & tags",
      "type": "boolean",
      "description": "Game details only: adds estimated owner range (ownersMin/ownersMax), peak concurrent players yesterday and Steam's crowd-voted tags with vote counts, from the free public SteamSpy API (one extra request per game, paced to 1/second). Steam's own store API exposes none of these.",
      "default": false
    },
    "searchLimit": {
      "title": "Games per search term",
      "type": "integer",
      "description": "How many games to take from each search term (max 50).",
      "default": 10,
      "minimum": 1,
      "maximum": 50
    },
    "watchLabel": {
      "title": "Watch label (alert mode)",
      "type": "string",
      "description": "Set a name (e.g. \"my-game-reviews\") to turn this run into a watch: the FIRST run for a given label + filter set records which reviews already exist and returns nothing (0 charged). Every run after that returns only reviews posted since the last run under the same label, so you can schedule this Actor and get alerted on new reviews without re-paying for ones you already have. Changing any filter (apps/searchTerms/searchLimit/country/language/reviewType/purchaseType/includeOffTopic/sortBy/dayRange/keyword/minPlaytimeHours/reviewsAfter/reviewsBefore) starts a fresh baseline. Reviews only — ignored when dataType is \"games\". Leave empty for normal one-off runs.",
      "editor": "textfield"
    },
    "watchEvents": {
      "title": "Watch events to report",
      "type": "array",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": [
          "new",
          "recommendationChanged"
        ],
        "enumTitles": [
          "New review",
          "Thumbs-up/thumbs-down edited (reviewer changed their own recommendation)"
        ]
      },
      "description": "Optional. Only report these kinds of change in watch mode — e.g. pick \"Thumbs-up/thumbs-down edited\" alone to be alerted only when an existing reviewer flips their recommendation, and never pay for anything else. Leave empty for both. Steam keeps a review's id stable when its author edits it in place, including flipping their own thumbs-up/thumbs-down — without this event an already-delivered review that later flips is invisible to a watch forever. Steam reviews have no developer-response feature, so that is not a watchable event here. The change event compares against the recommendation recorded the last time the review was delivered, so it only starts firing on the second run after a baseline created before this feature existed. Ignored when \"Watch label\" is empty.",
      "default": []
    },
    "webhookUrl": {
      "title": "Webhook URL (notify on completion)",
      "type": "string",
      "description": "Optional. An http(s) URL to POST a small JSON summary to when the run finishes — results pushed, watch-label new count if Watch label is set, and the run's dataset ID so you can fetch the results. A convenience for callers who want a completion ping without setting up an Apify platform webhook (which needs separate Console/API configuration per Task, not per run). Best-effort: a failed or slow webhook is logged as a warning and never fails the run or affects charging — it fires after every result has already been pushed and charged. Leave empty to skip.",
      "editor": "textfield"
    }
  }
}
