{
  "title": "Google News Scraper input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "queries": {
      "title": "Search queries",
      "type": "array",
      "description": "One or more search terms. Supports Google News operators like \"exact phrase\", site:bbc.com, when:7d, before:2026-01-01.",
      "editor": "stringList",
      "default": [
        "artificial intelligence"
      ],
      "prefill": [
        "artificial intelligence"
      ]
    },
    "rssUrls": {
      "title": "Custom Google News RSS URLs",
      "type": "array",
      "description": "Optional: topic/section/publication feeds from news.google.com/rss/... (added to the queries).",
      "editor": "stringList"
    },
    "topics": {
      "title": "Browse by topic",
      "type": "array",
      "description": "Optional: Google News' built-in topic sections (added to the queries), no RSS URL needed. One or more of: WORLD, NATION, BUSINESS, TECHNOLOGY, ENTERTAINMENT, SCIENCE, SPORTS, HEALTH.",
      "editor": "stringList"
    },
    "excludeWords": {
      "title": "Exclude words",
      "type": "array",
      "description": "Words or phrases to exclude, applied to every query (e.g. \"iphone\" drops articles mentioning it). Equivalent to typing -word yourself, just easier to manage as a list. Does not apply to rssUrls, since those are fixed feed URLs, not search terms.",
      "editor": "stringList"
    },
    "timePeriod": {
      "title": "Time period",
      "type": "string",
      "description": "Only return articles published within this window, filtered by Google itself (no extra requests). Applies to search queries only, not to topics or custom RSS URLs. Leave empty for no time limit. Ignored if you set an explicit From/To date below.",
      "editor": "select",
      "enum": [
        "",
        "1h",
        "6h",
        "12h",
        "1d",
        "7d",
        "30d",
        "90d",
        "1y"
      ],
      "enumTitles": [
        "No limit",
        "Last hour",
        "Last 6 hours",
        "Last 12 hours",
        "Last 24 hours",
        "Last 7 days",
        "Last 30 days",
        "Last 90 days",
        "Last year"
      ],
      "default": ""
    },
    "publishedAfter": {
      "title": "Published after (YYYY-MM-DD)",
      "type": "string",
      "description": "Only return articles published on or after this date, e.g. 2026-09-01. Applies to search queries only. Overrides Time period.",
      "editor": "textfield"
    },
    "publishedBefore": {
      "title": "Published before (YYYY-MM-DD)",
      "type": "string",
      "description": "Only return articles published before this date, e.g. 2026-09-10. Applies to search queries only. Overrides Time period.",
      "editor": "textfield"
    },
    "language": {
      "title": "Language (hl)",
      "type": "string",
      "description": "e.g. en-US, en-GB, de, fr, es, pt-BR, ar, ja",
      "default": "en-US",
      "editor": "textfield"
    },
    "country": {
      "title": "Country (gl)",
      "type": "string",
      "description": "Two-letter country code, e.g. US, GB, DE, IN, EG",
      "default": "US",
      "editor": "textfield"
    },
    "maxItemsPerQuery": {
      "title": "Max articles per query",
      "type": "integer",
      "description": "Google returns up to 100 per feed. Default is 50 — decoding the real publisher URL for each article is a sequential 2-request round trip, so 100 articles alone can take 4-5 minutes; raise it if you don't need every article back quickly.",
      "default": 50,
      "minimum": 1,
      "maximum": 100
    },
    "decodeUrls": {
      "title": "Resolve real article URLs",
      "type": "boolean",
      "description": "Google News links are encoded redirects. When on, each link is decoded to the publisher's URL (one extra request per article).",
      "default": true
    },
    "fetchArticleBody": {
      "title": "Extract full article text",
      "type": "boolean",
      "description": "Open each publisher page and extract the full article body, author, image, keywords and section (JSON-LD first, HTML paragraphs as fallback). Adds one request per article and is slower; paywalled/blocked articles are still returned with articleFetchStatus explaining why the text is missing.",
      "default": false
    },
    "articleBodyMaxChars": {
      "title": "Max article body length",
      "type": "integer",
      "description": "Truncate articleBody to this many characters (articleBodyTruncated tells you when it happened). Only used when 'Extract full article text' is on.",
      "default": 20000,
      "minimum": 500,
      "maximum": 200000
    },
    "maxResults": {
      "title": "Max results (total)",
      "type": "integer",
      "description": "Overall cap across all queries.",
      "default": 500,
      "minimum": 1,
      "maximum": 5000
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "editor": "proxy",
      "description": "Route all Google/publisher requests through Apify Proxy. Recommended: Google rate-limits the URL-decoding endpoint per source IP once you decode a lot in a short window, and rotating proxy IPs is the actual fix for it. Leave as-is to use automatic Apify Proxy; runs on accounts without proxy access fall back to a direct connection.",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [
    "queries"
  ]
}
