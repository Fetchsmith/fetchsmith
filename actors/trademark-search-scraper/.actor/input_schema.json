{
  "title": "Trademark Search Scraper input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchTerm": {
      "title": "Trademark term",
      "type": "string",
      "description": "Word or brand to search for. Matches trademarks whose name contains this term.",
      "editor": "textfield",
      "default": "coffee"
    },
    "offices": {
      "title": "Trademark offices",
      "type": "array",
      "description": "Two-letter office codes to search, e.g. US (USPTO), EM (EUIPO), GB, DE, FR, JP, CN, WO (WIPO). Leave empty to search all 70+ offices in TMview.",
      "editor": "stringList",
      "default": ["US", "EM"]
    },
    "niceClasses": {
      "title": "Nice classes",
      "type": "array",
      "description": "Restrict to these Nice classification classes, e.g. \"25\" (clothing), \"9\" (software). Leave empty for all classes.",
      "editor": "stringList",
      "default": []
    },
    "statuses": {
      "title": "Trademark status",
      "type": "array",
      "description": "Restrict to these statuses, e.g. Registered, Filed, Expired, Ended, Withdrawn. Leave empty for all statuses.",
      "editor": "stringList",
      "default": []
    },
    "maxResults": {
      "title": "Max results",
      "type": "integer",
      "description": "Stop after this many trademarks.",
      "default": 50,
      "minimum": 1,
      "maximum": 5000
    },
    "watchLabel": {
      "title": "Watch label",
      "type": "string",
      "description": "Optional label that turns on watch mode: the first run under a label seeds a baseline and returns nothing (charged nothing); every run after that returns and charges only marks not already delivered under that label. Leave unset for a plain, repeatable search.",
      "editor": "textfield",
      "default": ""
    },
    "webhookUrl": {
      "title": "Webhook URL (notify on completion)",
      "type": "string",
      "description": "Optional. An http(s) URL to POST a small JSON summary to when the run finishes — marks pushed, marks scanned, and (when Watch label is set) whether this was a baseline run plus the new/skipped counts, along with the run's dataset ID so you can fetch the results. A convenience for callers who want a completion ping without setting up an Apify platform webhook (which needs separate Console/API configuration per Task, not per run). Best-effort: a failed or slow webhook is logged as a warning and never fails the run or affects charging — it fires after every mark has already been pushed and charged. Leave empty to skip.",
      "editor": "textfield"
    }
  },
  "required": []
}
