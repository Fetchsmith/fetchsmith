{
  "title": "Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sources": {
      "title": "Portals",
      "type": "array",
      "description": "Which official UK procurement portals to search. 'cf' = Contracts Finder (sub-threshold contracts, the high-volume feed: ~18 tender-stage and 100+ award notices a day). 'fts' = Find a Tender (above-threshold notices only, a thin feed of ~7-8 tender-stage notices a day). Both are searched by default and merged into one deduplicated result set with a 'source' field on every row.",
      "editor": "select",
      "items": { "type": "string", "enum": ["fts", "cf"], "enumTitles": ["Find a Tender (above threshold)", "Contracts Finder (sub threshold)"] },
      "default": ["fts", "cf"]
    },
    "stages": {
      "title": "Stages",
      "type": "array",
      "description": "Which notice stages to return. 'tender' = live/closed opportunities to bid on (the default, and what most buyers want). 'award' = contract award notices, including the winning supplier. 'planning' = early pipeline notices. Leave as ['tender'] unless you want awards.",
      "editor": "select",
      "items": { "type": "string", "enum": ["planning", "tender", "award"], "enumTitles": ["Planning (pipeline)", "Tender (opportunities)", "Award (winners)"] },
      "default": ["tender"]
    },
    "updatedWithinDays": {
      "title": "Updated within (days)",
      "type": "integer",
      "description": "Only return notices published or updated in the last N days. Notices come back newest first.",
      "default": 7,
      "minimum": 1,
      "maximum": 365
    },
    "dateFrom": {
      "title": "Published/updated from (absolute date)",
      "type": "string",
      "description": "Optional absolute start of the date window, e.g. 2026-08-01 (or a full ISO datetime). Setting either this or 'dateTo' OVERRIDES 'Updated within (days)'. Enforced server-side by both portals, so it is a real filter and does not waste results.",
      "editor": "textfield"
    },
    "dateTo": {
      "title": "Published/updated to (absolute date)",
      "type": "string",
      "description": "Optional absolute end of the date window, e.g. 2026-08-31 (or a full ISO datetime). Defaults to the run start time. Note a bare date means midnight, so 2026-08-31 excludes the 31st itself — use 2026-09-01 to include it.",
      "editor": "textfield"
    },
    "cpvCodes": {
      "title": "CPV codes",
      "type": "array",
      "description": "Common Procurement Vocabulary codes to filter by, e.g. 72000000 (IT services), 45000000 (construction), 80000000 (education). A code matches its whole CPV subtree and nothing outside it: 72000000 matches every 72xxxxxx code, 72267000 also matches its children 72267100/72267200, and 80000000 matches 80xxxxxx only — never 85xxxxxx health. Matched against every CPV on the notice (headline plus all lot and item codes), not just the headline one, so a notice qualifies if any one of its codes is in the subtree. Leave empty for all.",
      "editor": "stringList",
      "default": []
    },
    "searchQuery": {
      "title": "Search text",
      "type": "string",
      "description": "Free-text filter. Every word must appear somewhere in the title, description, buyer name, CPV description or lot titles (case-insensitive, Unicode-normalized so accented words match regardless of composed/decomposed input form). E.g. \"software\" or \"nhs cleaning\".",
      "editor": "textfield"
    },
    "keywordsAny": {
      "title": "Any of these phrases",
      "type": "array",
      "description": "OR-matched alternative to 'Search text': a notice matches if it contains ANY of these phrases (each phrase can be multiple words, matched as a whole substring) in its title, description, buyer name, CPV description or lot titles. Use this to cover several unrelated opportunity types in one run, e.g. [\"software\", \"IT support\", \"cyber\"] — 'Search text' can only express AND-of-words within a single phrase, so it can't do this. Combine both if you want ANY-of-these-phrases AND all-of-those-words at once.",
      "editor": "stringList",
      "default": []
    },
    "regions": {
      "title": "Delivery region contains",
      "type": "array",
      "description": "OR-matched filter on where the contract is delivered, e.g. [\"London\", \"Scotland\", \"North West\"]. Matches against the notice's own delivery region/location text (case-insensitive substring). Leave empty for all regions. Notices with no delivery-region data are excluded when this is set.",
      "editor": "stringList",
      "default": []
    },
    "buyerName": {
      "title": "Buyer name contains",
      "type": "string",
      "description": "Case-insensitive substring match against the buying authority's name only, e.g. 'NHS', 'Ministry of Defence', 'Kent County Council'. Narrower than 'Search text', which also matches the title, description, CPV description and lot titles — so use this when you want a buyer's own notices and not every notice that merely mentions them.",
      "editor": "textfield"
    },
    "minValueGbp": {
      "title": "Minimum contract value (GBP)",
      "type": "integer",
      "description": "Only return notices whose published contract value is at least this much. Notices without a published value are excluded when this is set.",
      "editor": "number"
    },
    "maxValueGbp": {
      "title": "Maximum contract value (GBP)",
      "type": "integer",
      "description": "Only return notices whose published contract value is at most this much. Notices without a published value are excluded when this is set.",
      "editor": "number"
    },
    "openOnly": {
      "title": "Only still-open tenders",
      "type": "boolean",
      "description": "Only return notices whose submission deadline is still in the future. Use with stages = ['tender'] — award notices have no future deadline and will all be filtered out.",
      "default": false
    },
    "maxResults": {
      "title": "Max results",
      "type": "integer",
      "description": "Stop after this many notices.",
      "default": 100,
      "minimum": 1,
      "maximum": 5000
    },
    "maxPagesScanned": {
      "title": "Max API pages to scan",
      "type": "integer",
      "description": "Safety cap on how many 100-notice API pages to read while looking for matches. Raise it if you use a narrow filter over a long date range.",
      "default": 50,
      "minimum": 1,
      "maximum": 500
    },
    "includeRawOcds": {
      "title": "Include raw OCDS release",
      "type": "boolean",
      "description": "Attach the complete, unmodified OCDS 1.1 release JSON for each notice as a 'rawOcds' field, in addition to the normalized fields. Off by default — turn it on if your pipeline wants the full original government data (nested parties, all documents, amendment history) rather than just the flattened columns.",
      "default": false
    },
    "watchLabel": {
      "title": "Watch mode: label",
      "type": "string",
      "description": "Set a name (e.g. \"my-cpv-watch\") to turn this run into an alert: the FIRST run with a given label + filter combination records every currently-matching notice and returns nothing (you pay nothing). Every run after that returns and charges only for notices NOT already delivered under that label — ideal for a scheduled run that should only tell you what's new. Leave empty for a normal one-off search.",
      "editor": "textfield"
    },
    "webhookUrl": {
      "title": "Webhook URL (notify on completion)",
      "type": "string",
      "description": "Optional. An http(s) URL to POST a small JSON summary to when the run finishes — notices pushed, releases scanned, how many were filtered out, pages scanned, the watch-label new count if Watch label is set, and the run's dataset ID so you can fetch the results. A convenience for callers who want a completion ping without setting up an Apify platform webhook (which needs separate Console/API configuration per Task, not per run). Best-effort: a failed or slow webhook is logged as a warning and never fails the run or affects charging — it fires after every notice has already been pushed and charged. Leave empty to skip.",
      "editor": "textfield"
    }
  },
  "required": []
}
