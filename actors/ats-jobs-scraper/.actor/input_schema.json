{
  "title": "Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "companies": {
      "title": "Companies",
      "type": "array",
      "description": "List of {\"ats\": \"greenhouse|ashby|lever|recruitee|workable|smartrecruiters|workday\", \"slug\": \"<company-slug-from-their-career-board-url>\"}. Example: careers.airbnb.com/... uses Greenhouse slug \"airbnb\"; jobs.ashbyhq.com/ramp uses Ashby slug \"ramp\"; apply.workable.com/getresponse uses Workable slug \"getresponse\"; jobs.smartrecruiters.com/BMWDealerCareers uses SmartRecruiters slug \"BMWDealerCareers\" (case-sensitive); Workday uses \"<host>/<site>\" from the board's own URL, e.g. \"okgov.wd1.myworkdayjobs.com/okgovjobs\".",
      "editor": "json",
      "default": [
        {
          "ats": "greenhouse",
          "slug": "airbnb"
        },
        {
          "ats": "ashby",
          "slug": "ramp"
        },
        {
          "ats": "lever",
          "slug": "leverdemo"
        },
        {
          "ats": "recruitee",
          "slug": "vandebron"
        },
        {
          "ats": "workable",
          "slug": "getresponse"
        },
        {
          "ats": "smartrecruiters",
          "slug": "ElasticBandCompany"
        },
        {
          "ats": "workday",
          "slug": "okgov.wd1.myworkdayjobs.com/okgovjobs"
        }
      ]
    },
    "titleKeyword": {
      "title": "Title keyword filter",
      "type": "string",
      "description": "Only keep postings whose title contains this text (case-insensitive). Leave empty for all titles.",
      "editor": "textfield"
    },
    "titleExcludeKeyword": {
      "title": "Title exclude keyword",
      "type": "string",
      "description": "Drop postings whose title contains this text (case-insensitive), e.g. \"Senior\" to filter out senior roles. Leave empty for no exclusion.",
      "editor": "textfield"
    },
    "locationKeyword": {
      "title": "Location keyword filter",
      "type": "string",
      "description": "Only keep postings whose location/city/country contains this text (case-insensitive). Leave empty for all locations.",
      "editor": "textfield"
    },
    "locationExcludeKeyword": {
      "title": "Location exclude keyword",
      "type": "string",
      "description": "Drop postings whose location/city/country/secondary locations contains this text (case-insensitive), e.g. a country you don't hire in. Leave empty for no exclusion.",
      "editor": "textfield"
    },
    "employmentTypeKeyword": {
      "title": "Employment type filter",
      "type": "string",
      "description": "Only keep postings whose normalized employmentType field contains this text (case-insensitive), e.g. \"full\" for full-time or \"contract\". Leave empty for all employment types. Not every ATS publishes this field; postings where the ATS never exposes it are dropped when this filter is set.",
      "editor": "textfield"
    },
    "departmentKeyword": {
      "title": "Department filter",
      "type": "string",
      "description": "Only keep postings whose department/team contains this text (case-insensitive), e.g. \"Engineering\" or \"Sales\". Leave empty for all departments. Not every ATS publishes a department; postings where it is missing are dropped when this filter is set.",
      "editor": "textfield"
    },
    "descriptionKeyword": {
      "title": "Description keyword filter",
      "type": "string",
      "description": "Only keep postings whose full job description text contains this text (case-insensitive), e.g. \"Kubernetes\" or \"visa sponsorship\" — the things a title never says. Matched against the plain text, not the HTML. Leave empty for no description filter.",
      "editor": "textfield"
    },
    "descriptionExcludeKeyword": {
      "title": "Description exclude keyword",
      "type": "string",
      "description": "Drop postings whose job description text contains this text (case-insensitive), e.g. \"security clearance\". Leave empty for no exclusion.",
      "editor": "textfield"
    },
    "hasSalary": {
      "title": "Only postings with salary",
      "type": "boolean",
      "description": "Only keep postings where the ATS itself published a salaryMin or salaryMax (Ashby, Lever, Recruitee). Postings from ATSes that never carry a compensation field (Greenhouse, Workable, SmartRecruiters, Workday) are always dropped when this is on.",
      "default": false
    },
    "minSalary": {
      "title": "Min salary",
      "type": "integer",
      "description": "Only keep postings whose salary range overlaps this floor or higher, e.g. 100000. Compared against the number as the ATS posted it — no currency conversion and no pay-interval normalization (salaryInterval varies: year, month, week, day, hour — check salaryCurrency/salaryInterval on the output row). Postings with no salary at all never match. Leave empty for no floor.",
      "minimum": 0
    },
    "maxSalary": {
      "title": "Max salary",
      "type": "integer",
      "description": "Only keep postings whose salary range overlaps this ceiling or lower. Same currency/interval caveat as Min salary. Leave empty for no ceiling.",
      "minimum": 0
    },
    "remoteOnly": {
      "title": "Remote only",
      "type": "boolean",
      "description": "Only keep postings the ATS itself marks as remote.",
      "default": false
    },
    "postedAfter": {
      "title": "Posted after (ISO date)",
      "type": "string",
      "description": "Only keep postings published on or after this date, e.g. 2026-08-01. Leave empty for no date filter.",
      "editor": "textfield"
    },
    "postedBefore": {
      "title": "Posted before (ISO date)",
      "type": "string",
      "description": "Only keep postings published on or before this date, e.g. 2026-09-01. Pair with \"Posted after\" for a date window, e.g. everything a company opened during one quarter. Leave empty for no upper bound.",
      "editor": "textfield"
    },
    "includeDescriptions": {
      "title": "Include job descriptions",
      "type": "boolean",
      "description": "Include the full HTML and plain-text job description. Turn off for a smaller/faster dataset. The description keyword filters still work with this off — the text is fetched, matched, then dropped from the rows.",
      "default": true
    },
    "maxJobsPerCompany": {
      "title": "Max jobs per company",
      "type": "integer",
      "description": "Cap on how many postings that pass your filters are kept per company (the filters are applied first, then this cap). Kept low by default so a run with no input at all finishes fast and cheap on a first try; raise it explicitly for bulk pulls.",
      "default": 50,
      "minimum": 1,
      "maximum": 5000
    },
    "maxResults": {
      "title": "Max results (total)",
      "type": "integer",
      "description": "Stop the whole run after this many postings are pushed, across all companies. Kept low by default so a run with no input at all finishes fast and cheap on a first try; raise it explicitly for bulk pulls.",
      "default": 300,
      "minimum": 1,
      "maximum": 100000
    },
    "watchLabel": {
      "title": "Watch label (only new postings since last run)",
      "type": "string",
      "description": "Optional. Name a saved job alert (e.g. \"backend-remote-eu\") and this run returns ONLY postings that were not delivered under that same label and filter set before, instead of the whole open-roles list every time. The first run for a label is a free baseline: it records which postings are already open (up to 5000) and returns zero rows, charging nothing. Run it again later -- on a schedule, typically -- to get only the roles that opened since then. The baseline lives in your own Apify account (a named key-value store, \"fetchsmith-ats-watch\"), keyed by the label plus a fingerprint of the company list and every filter, so changing any of those starts a fresh baseline instead of dumping previously-excluded postings as \"new\".",
      "editor": "textfield"
    },
    "webhookUrl": {
      "title": "Webhook URL (notify on completion)",
      "type": "string",
      "description": "Optional. An http(s) URL to POST a small JSON summary to when the run finishes — job postings pushed, companies scanned and how many boards errored, plus watch-label new/skipped counts if Watch label is set, and the run's dataset ID so you can fetch the results. A convenience for callers who want a completion ping without setting up an Apify platform webhook (which needs separate Console/API configuration per Task, not per run). Best-effort: a failed or slow webhook is logged as a warning and never fails the run or affects charging — it fires after every posting has already been pushed and charged. Leave empty to skip.",
      "editor": "textfield"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Route requests through Apify Proxy (recommended, on by default).",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [
    "companies"
  ]
}
