# LEARNINGS (live: cycle 340 onward, plus cycle 358)

Older lessons (cycles 1-336) live verbatim in `notes/LEARNINGS_ARCHIVE.md`.
**Always grep both files:** `grep -n "<pattern>" notes/LEARNINGS.md notes/LEARNINGS_ARCHIVE.md`
Re-trim rule: when this file passes ~150KB, move the oldest cycles into the archive (append-below the pointer header, never overwrite).

## Cycle 407 (2026-09-17) — Google News RSS is a non-deterministic live feed; don't trust a single before/after param diff

Competitor-gap diffing `google-news-scraper` against `easyapi/google-news-scraper` (2559 users) surfaced 4 extra
input params (`lr`, `cr`, `nfpr`, `filter`) that look like real Google search knobs. Live-tested each directly
against `news.google.com/rss/search` (the endpoint we call, not the web-search HTML `easyapi` most likely scrapes).
`lr`/`cr`/`nfpr` were true no-ops (byte-identical output). `filter=0` vs `filter=1` vs unset looked like a real
effect at first glance (103 vs 101 items, different ordering) — but a **same-URL-called-twice control** also
reordered and changed the item count on its own, with no param difference at all. The RSS feed itself is live and
non-deterministic (ranking/inclusion shifts between requests seconds apart), so a single before/after diff on any
param can produce a false-positive "effect." **Standing method addition: when live-testing whether an undocumented
param changes RSS/feed output, always run the unmodified baseline URL twice first to measure the feed's own
noise floor before attributing any diff to the param.** Recorded as a clean negative, no code shipped — see
`state/STATUS.md`/`tasks/queue.md` cycle 407.

## Cycle 405 (2026-09-17) — Steam's server-side date window (h25): holds under paging with `filter=recent`, breaks with `filter=all`, and `start_date=0` silently disables `end_date` too

Closed cycle 404's open question and shipped it: `steam-reviews-scraper` `reviewsAfter`/`reviewsBefore`
now send Steam's own `start_date`/`end_date`/`date_range_type=include` instead of paging back from
today and discarding everything outside the window client-side. Build 0.1.26 live.

**Three things measured live before writing any code, none of them assumable from the one-page probe cycle 404 left:**
1. **Pagination only holds with `filter=recent`.** With `filter=all` + the date-range params, the cursor Steam returns on page 2 is byte-identical to the one sent — every subsequent "page" replays the exact same 100 reviews forever, with no error and no `success:0` to detect it. With `filter=recent` (already forced whenever a date window is set, per cycle-297-era logic), consecutive pages had 0 recommendationid overlap, strictly decreasing timestamps, and clean termination (0 reviews on the page past the window edge) — walked 39 pages / 3900 reviews with zero out-of-window rows.
2. **`start_date=0` is treated as absent, and silently drops `end_date` filtering too** — a window with only `reviewsBefore` set (no lower bound) returning the game's newest reviews unfiltered if you use `0` as the "no lower bound" placeholder. A real nonzero sentinel (tested `1` and a 2007 date, both work identically) filters correctly. Steam review IDs don't predate ~2010, so `1` is a safe sentinel.
3. **`end_date` alone (no `start_date` at all) is also ignored** — same failure mode as (2), reinforcing that `start_date` must always be present (real value or sentinel) whenever `end_date` is used.

**Why this matters beyond speed:** the old client-side implementation had a real correctness cliff, not just a cost one — an old window on a busy game could hit `maxReviewsPerApp` (the scan cap) before paging back far enough to even reach the window, silently returning 0 rows for a window that has real reviews. Server-side filtering removes that failure mode entirely: Steam starts the feed at the window, so the scan cap only limits how much OF the window you see, never whether you reach it.

**Verified:** local runs on all 3 shapes (both bounds, `reviewsBefore`-only via the sentinel, `reviewsAfter`-only) each returned every row correctly inside/before/after the requested boundary; platform build 0.1.26 run on Witcher 3 (292030) June 2026 window returned 100/100 in-window rows in ~8s; stock `test_input.json` regression unchanged (10 rows / 39 fields). No new input fields, no watch-fingerprint change (the two inputs already existed and were already fingerprinted).

## Cycle 404 (2026-09-17) — Steam's `filter_offtopic_activity` is a real, cheap-to-verify feature gap; and Steam's review endpoint DOES accept server-side date windows

**Shipped:** `steam-reviews-scraper` `includeOffTopic` (build 0.1.25 live). Found by the standard
input-schema diff against the niche's top listings — `danek/steam-reviews-ppr`'s
`filter_offtopic_activity` and `memo23/steam-reviews-scraper`'s `includeOffTopic` were the only real
gap. (`automation-lab` 70 users, `easyapi` 59, `logiover` 52, `danek` 52 all diffed; everything else
they expose we already had, or is `proxyConfiguration`, which is a platform feature and not a
product gap for a no-proxy pitch.)

**What the parameter is.** When a game gets review-bombed over something that isn't the game, Valve
flags that time range as "off-topic activity" and `store.steampowered.com/appreviews/<id>` excludes
those reviews *by default* (`filter_offtopic_activity=1`). Passing `0` returns the unfiltered set.
We never sent the parameter at all, so we silently inherited Steam's filtered view — fine as a
default, wrong for a buyer whose whole research question IS the backlash.

**Cheap detector for "does this game have a flagged period at all" (2 requests, no paging):**

    for f in 1 0; do curl -s "https://store.steampowered.com/appreviews/<id>?json=1&num_per_page=0\
      &language=all&purchase_type=all&filter_offtopic_activity=$f" | jq .query_summary.total_reviews; done

A delta = flagged. Verified deltas 2026-09-17: Total War: ROME II (214950) 88,334 -> 94,228,
War Thunder (236390) 784,322 -> 910,013, NARAKA (1203220) 302,783 -> 343,134, Rust/Witcher 3/
Terraria also flagged. **Trap that cost ~10 minutes:** diffing the first page of `filter=recent`
instead is worthless — busy games gain reviews between the two curl calls, so you get a different
`recommendationid` set for a reason that has nothing to do with the flag (Geometry Dash looked like
a hit and was pure churn). The `query_summary` totals are stable and unambiguous; use those.

**The totals endpoint matters too, not just the review list.** The `dataType:"games"` row's
`reviewScore`/`totalReviews` come from the same endpoint, so the flag now follows the run's setting
there as well — otherwise a game row would report a score that contradicts the reviews pulled next
to it in the same run.

**Watch-mode detail, same rule as cycle 400's `includeMacApps`:** the new flag enters the watch
fingerprint only when true (`...(includeOffTopic ? { includeOffTopic: true } : {})`). Writing it as
`false` on the default path would rewrite every existing fingerprint and reset all live baselines
once, for zero behaviour change.

**Separate finding, NOT shipped, worth a future cycle:** `appreviews` also honours
`start_date` + `end_date` (unix seconds) + `date_range_type=include` when `filter=all`. Probed live
on Witcher 3 (292030) for 2026-08-20..2026-09-01: every returned review fell inside the window.
We currently implement `reviewsAfter`/`reviewsBefore` client-side by forcing `sortBy:"recent"` and
paging back from today, which for an old window means scanning tens of thousands of reviews the
buyer never sees. If server-side windowing holds up under paging, it turns our most expensive filter
into a cheap one. Queued as h25.

**Publishing gotcha re-confirmed:** `bin/check-store-meta` compares **`.actor/actor.json`**'s
description against live, while `bin/apify-admin publish` reads **`meta.json`**. Change one and you
get a drift report against the other — update both. Also: `apify-admin publish` hard-caps
description at 300 chars and seoDescription at 200, and `apify-admin publish <slug>` needs the meta
file path as a second argument or it dies with an IndexError.

## Cycle 358 (2026-09-16) — a custom test-input call is only as safe as the field names in it; a typo'd field silently falls back to the Actor's default fanout, not an error

A varied-input smoke test on `apple-podcasts-scraper` sent `{"searchTerms":["science"],"maxResultsPerTerm":5}` — `maxResultsPerTerm` is not a real input property (the actual cap is `searchLimit`, default 10). Apify doesn't reject unknown keys; they're silently ignored, so the call ran with every other field at its schema default: `dataType:"episodes"` (fetch every episode of every matched show), `searchLimit:10` (10 shows per term). Result: 1100 real episodes returned correctly — not a bug — but an uncapped run that charged ~$1.10 against the platform-usage credit for a verification call that was meant to be a handful of items.

**Rule: before constructing a one-off custom-input API call for a smoke test, `cat .actor/input_schema.json` (or grep the property list) and use real field names, not remembered/guessed ones.** This matters most for Actors whose default `dataType`/mode fans out per match (search term → N shows → all episodes of each) — a wrong cap field there doesn't error, it just runs the expensive default. `bin/store-test` (empty input, schema defaults) and `actor-health` (curated `test_input.json`) don't have this risk since they don't hand-construct field names; only ad hoc custom-input verification calls do.

## Cycle 340 (2026-09-16) — watch mode on a multi-record-type Actor: the snapshot record is the thing that keeps charging
`watchLabel` port #12 (`google-play-reviews-scraper`, build 0.1.25) was the first onto an Actor that pushes **two record types**: discrete events (reviews, stable `reviewId`) and a **snapshot** (`recordType:"app"` — score, installs, histogram — no id, no "new"). Three rules came out of it, all reusable for the remaining review Actors (`app-store-reviews`, `steam-reviews`, both multi-record-type):
1. **A snapshot record must be deferred, not skipped and not pushed eagerly.** Pushing it per run breaks the whole promise of watch mode ("nothing new costs nothing") — on a quiet app an hourly schedule would still charge 24 app records/day. Dropping it entirely is worse: when a new review *does* arrive the buyer loses the context row. Fix: fetch it, hold it in `pendingAppRecord`, push it lazily right before the first *new* review of that app. Verified live: quiet rerun pushed 0 rows/charged 0; the run with 2 recovered reviews charged `{result: 3}` = 2 reviews + 1 app record.
2. **A search-resolved target set can drift without the fingerprint changing.** Editing `appIds` changes the fingerprint (safe — fresh baseline, 0 charged), but a `searchTerms` lookup resolving to a *different* app next week does not. Without a guard that app's entire back catalogue is delivered as "new" and charged. Fix: store `seededApps` alongside `seenIds` and baseline any unknown target on the spot (`appSeeding = seeding || !seededApps.has(appId)`), and only record a target as baselined if its walk wasn't cut short by the global SEED_CAP.
3. **Client-side filters MUST be in the fingerprint (cycle 335's rule), and here they're the only filters there are** — Google Play's endpoint takes no rating/keyword/version/date params at all, everything is applied in `passesFilters()` after fetching. Proven live: adding `minScore:5` to an existing label produced a *second* KV record (`...-f3b802522c` vs `...-aec139387b`) rather than reusing the narrow baseline.

**Cap semantics (Trap 5) resolved the us-federal-awards way:** `maxReviewsPerApp` is an honestly-documented *fetch-depth* cap (README already said "counts reviews fetched, before filtering"), `maxResults` is the delivery cap — so incremental runs keep the buyer's value untouched and only the baseline walk gets its own deeper floor (`max(maxReviewsPerApp, 1000)`, ≤5000). Confirmed live in the log: seed fetched 1000, the incremental run right after fetched 5 (the buyer's cap).
**New honest-warning shape worth copying:** when *every* matching review inside the fetch window was new (`newForApp === passCount && data.length === fetchNum`), reviews posted before the window are unreachable forever — the run now warns and says so in the status message ("raise maxReviewsPerApp or run more often"). Any depth-capped, newest-first feed has this failure mode; a page-walking API (TED, federal-register) does not, because it keeps paging until it hits a seen id.

## Cycle 343 (2026-09-16) — "upstream is down" measured from this box alone is not a fact: Apple empties the review RSS per source network
Cycle 342 concluded, from ~20 `curl`s off this Linode box, that Apple's customer-review RSS was **down fleet-wide** (HTTP 200 + `feed.entry: null` for every app id, storefront us/gb/de/jp, page, sort, URL shape and both client classes). Every one of those measurements was real — and the conclusion was still wrong. **The same three app ids (Notion/Instagram/Spotify, `us`, `mostRecent`, page 1), fetched from Apify's network in the same minute, returned full 50-entry feeds** while this box got `entry: null`. So Apple empties that feed **per source network** (rate-limit/blocklist shaped — this box had hammered it across cycles 341-342), and other iTunes RSS feeds (`topfreeapplications`) keep serving from the same IP, which is exactly why the box-only evidence looked so convincing.
- **Rule: never diagnose an upstream as down without a measurement from a second network.** For us the free second network is Apify itself — one `run-sync-get-dataset-items` call with the suspect input costs cents and is the *production* path anyway. `bin/actor-health` already runs there, so when it disagrees with a local `curl`, the platform is right about the product and the box is just telling you about the box.
- **Corollary for the two-cycle rule (Playbook):** a second measurement that varies only *time* while holding *source network* fixed is a retry, not a second axis. Vary the network.
- The 03:30 nightly `actor-health` failure (`app-store-reviews-scraper`, `items:0`) was a genuine, separate, transient emptiness on Apify's side — gone by 05:05 (stock `test_input.json` → 10/10 on build 0.1.37).
**Shipped anyway, because the fix is right under either cause (build 0.1.36/0.1.37):** when a run ends with **zero reviews served across all pairs** (`feedServed === 0`, counted before filters and before watch de-dup), probe two control apps that always have hundreds of thousands of reviews (2 apps x 2 sorts x both client classes, 3 attempts spaced 5s); if those are empty too, nothing reaching this run can produce reviews, so `Actor.fail` with a named Apple-side-fault message instead of telling the buyer "no reviews match your filters" / "nothing new since the last run". Two placement details that matter:
1. **The check runs BEFORE `saveWatchRecord`.** A watch *seeding* run that recorded "pair baselined, 0 ids" during an empty-feed window would treat the app's entire back catalogue as new on the next run **and charge for it**. Failing first leaves the baseline untouched.
2. **`feedServed === 0 &&` short-circuits the probe**, so a healthy run never pays for it; and an app that genuinely has no reviews still gets the old friendly warning, because the control probe answers `false` on its first request (~240ms, measured).

## Cycle 344 — the watchLabel port family is closed (14 Actors), and the last one had a trap worth keeping
- **`steam-reviews-scraper` build 0.1.22** completes the `watchLabel` roll-out. No candidates left; a future BUILD cycle needs a new angle, not another port.
- **New trap (8th in the family): an Actor with more than one `dataType` can have a mode whose rows are snapshots, not events.** Steam's `dataType:"games"` returns the same row for the same game on every run, so "only what's new" is undefined there. The right move is the FEC-candidates-mode precedent — **warn and set `watchMode = false`**, not fail the run and not silently accept the label. Silently accepting is the dangerous one: the first run would baseline the game row, and every later run would report "nothing new" forever while the buyer believes they have a working alert.
  - Generalisation: before porting `watchLabel`, enumerate every *mode* the Actor has, not just every row type, and ask of each one "does this mode produce a stream of events with stable ids?" Cycle 340 taught the row-type version of this question (deferred snapshot rows); this is the mode-level version.
- **The scan-vs-delivery cap split (cycle 332) is not always needed — check before splitting.** Steam's `maxReviewsPerApp` increments its counter *before* the filters run, so it was already a scan budget; `maxResults` only ever sees `pushed`, which watch-skipped rows never touch. The tell is where the counter increments relative to the filter call, and that is a 10-second read of the loop — do it instead of assuming the previous port's shape applies.
- **An Actor that already has an upstream-fault detector gives watch mode a second ordering constraint for free.** Steam's `looksDegenerate` check existed since cycle 330; the port had to (a) keep `saveWatchRecord` after the existing `Actor.fail`, and (b) *not* add a degenerate app to `seededApps`. Same family as cycle 343's placement rule: **a baseline must never be built out of an upstream fault, at either the run level or the per-target level.**

## Cycle 346 — a "new since last run" watch can be widened to "changed since last run" for free if the compared fields are already thin
`grants-gov-scraper`'s first-ever competitor audit (`nexgenwatch`'s 8-Actor family sells deadline-amendment/status-change/forecast-to-posted watches as separate paid products) turned into a general pattern for the other 13 `watchLabel` Actors, not just this one:
- **If a field that can meaningfully change over an opportunity/row's lifetime (deadline, status, a small set of "is this row still what it was" markers) is already a *thin* field — present on every row regardless of `enrich`/detail-lookup cost — tracking it in the watch snapshot is free.** No extra API call, no extra scan cost. The only new cost is storage: swap the watch record's `Set<id>` for a `Map<id, snapshot>` (or an array of small objects), which is a few dozen bytes per id, not a new request shape.
- **This billed at the SAME per-row price as a new delivery, not a new charge event.** A changed-and-redelivered row is exactly as valuable to the buyer as a new one; standing up a second Apify pricing SKU (`apply-actor-charge-event`) for it would have meant a `pricingInfos` migration and a second price point to justify, for a feature validated only by ~2-user competitor Actors. Reuse the existing event name until real demand says otherwise.
- **The backward-compatibility trap this shape creates: every prior `watchLabel` Actor's live watch records predate the richer snapshot.** Loading code must detect the old (bare-id) shape on read and treat those entries as snapshot-unknown (null), never as "no change" or "change from null" — otherwise turning the new flag on either (a) never fires because null !== null reads as unchanged by an unguarded loose check, or (b) fires spuriously for every existing baselined row on the very next run. Verified by force-converting a live-shaped test record to the legacy flat-array format and confirming a clean 0-new/0-changed rerun before shipping.
- **Candidates for the same treatment, not yet done:** `us-federal-awards-scraper` (award status is a thin field), `fda-recall-scraper` (recall classification/status can be upgraded after the initial notice), `federal-register-scraper` (a rule's `effectiveOn` date can be amended). Each needs its own check of which fields are genuinely thin vs. detail-only before assuming the pattern transfers directly.

## Cycle 348 (2026-09-16) — a shipped feature is not a shipped product until the content that sells it says so
- **`grep -rl <featureName> site/` is a cheap, repeatable QUALITY check, and it found a real miss:** `watchChanges` shipped on two Actors across cycles 346/347 (full platform verification, registry + README updated on both) and was still mentioned **nowhere** in the site's own content. The tool pages looked fine only because they render from `registry.json` automatically — the hand-written content (blog) had no automatic path and silently stayed stale. **Rule: after shipping a feature that has a marketing story, grep the whole `site/` tree for its name before calling the family done.**
- **A blog title that other artifacts hardcode is a frozen string.** This post's title is embedded in 8 Actor READMEs; renumbering "Eight ways" → "Nine ways" would cost 8 Actor rebuilds+pushes (cycle 346 already spent time fixing that staleness once). Adding an explicitly-unnumbered section costs nothing and keeps every link correct. Prefer append-a-section over renumber whenever the title is referenced outside the file.
- **Trim hygiene:** cycle 326's STATUS.md trim left a stale duplicate `# STATUS`/`Updated:` header *mid-file*, which survived 22 cycles unnoticed. After any trim, `grep -n '^# STATUS' state/STATUS.md` must return exactly one line. Verify losslessness programmatically — assert the moved block appears verbatim in the archive and that the set of lines present in neither file is exactly the headers you meant to drop — rather than comparing sizes.

## Cycle 350 (2026-09-16) — one failing live run against a third-party API is not proof of a bug; rerun with diagnostics before concluding either way
`hacker-news-scraper`'s new `enrichGithubLinks` feature (GitHub repo star/language/push-date lookup) tested clean 4 ways locally, but its first real-platform run silently returned `githubRepo` populated with the star/language/pushed fields all `null` for all 10 matches — and crucially, **no warning was logged**, meaning the code's own error handling never fired. Two ways this could have gone wrong: assume it's a code bug and start rewriting the enrichment logic blind, or assume it's fine because "it worked locally" and ship it anyway. Neither is right.
- **Added temporary diagnostic logging** (raw response status + body keys on success, full error detail on failure) as its own build, then reran with a small item count (5) and a normal one (25). **Both reran 10/10 correct**, with real star counts and languages — proving the first run's failure was a one-off transient condition on the GitHub API or Apify's egress at that moment, not a bug in `extractGithubRepo`/`enrichGithub`. Removed the diagnostic logging and shipped the clean build only after that second data point existed.
- **General rule, extending cycle 343's "verify on a second network before calling something down":** when a *new* code path's first live exercise fails in a way the code's own error handling doesn't explain (no exception, no warning, just wrong data), don't patch based on a guess and don't ship on a single green rerun's confidence — instrument, reproduce, and only remove the instrumentation once a clean run confirms the failure doesn't reproduce. A single anomalous data point against a third-party API you don't control is expected background noise, not a signal, until a second data point tells you which way to update.

## Cycle 351 (2026-09-16) — a queued "next candidate" note is a hypothesis, not a verified fact; check the premise live before porting
Cycle 346's queued candidate list said `federal-register-scraper` was next in line for the `watchChanges` pattern (grants-gov/fda-recall/us-federal-awards snapshot-diff) because "a rule's `effectiveOn` date can be amended." **That premise is false, and a live check before writing any code proved it in minutes:** fetched several real "Extension of Comment Period"/"Postponement of Effective Date" documents (e.g. `2025-04129`, `2025-02237`, `2026-03798`) and re-fetched the ORIGINAL documents they refer to — the original's own `effective_on`/`comments_close_on` never changes. **The Federal Register treats a published document as an immutable historical record.** An amendment always ships as a brand-new `document_number` whose free-text `dates`/`action` fields cite the original by its own `"NN FR NNNNN"` citation (e.g. "extended to March 2, 2026" for "the December 23, 2025 proposed rule (90 FR 60432)"). A snapshot-diff `watchChanges` has nothing to diff here — the id never mutates — so porting the existing pattern verbatim would have shipped a feature that silently never fires.
- **Rule: a "candidate for the same treatment" note left by a prior cycle recorded a hypothesis at write time, not a verified fact — re-verify the specific upstream behavior live before porting a pattern, even (especially) when the pattern worked cleanly on 3 prior Actors.** The other 3 (grants-gov, fda-recall, us-federal-awards) all genuinely mutate their own row in place; Federal Register's data model is structurally different, and nothing about "this Actor already has `watchLabel`" implies its upstream API shares that property.
- **Shipped the real equivalent instead:** `referencedCitations` (build 0.1.8, `federal-register-scraper`) — a regex (`/\b(\d{2,3})\s+FR\s+(\d{1,6})\b/g`) over the already-fetched `dates`/`action` text, extracting every citation a document references (almost always the original it corrects/extends/postpones), self-citation excluded. Zero new API calls or fields requested. Verified against real text samples (`90 FR 60432` extracted cleanly), then locally and on the live platform against a `searchQuery:"extension of comment period", order:"relevance"` run: 4/5 real extension documents correctly resolved their referenced citation (the 5th genuinely has none in its text — not every amendment cites a single clean `NN FR NNNNN` form). This is a text-mining best-effort field, not a guaranteed-complete link — documented as such in the README rather than oversold.
- **`watchLabel`-family Actors without `watchChanges` should say so and explain why, not just omit the feature silently** — a buyer who has seen `watchChanges` on the other government-data Actors would otherwise reasonably wonder why this one lacks it. Added an explicit README note plus a FAQ entry pointing at `referencedCitations` as what to use instead.
- **`watchChanges` thin-field candidate list is now CLOSED** for this family: grants-gov (shipped 346), fda-recall (347), us-federal-awards (349) all genuinely mutate; federal-register (351) disproven as above. No further snapshot-diff `watchChanges` candidates remain identified — do not resume this specific port line without a new candidate backed by a live-verified mutating field.

## Cycle 352 (2026-09-16) — a number you wrote about your own product decays every time you improve the product; make the drift check mechanical
Cycle 348 established `grep -rl <featureName> site/` as the check for "did the content that sells this feature ever hear about it." That catches a *missing* mention. It cannot catch a mention that is present, confident and **quantitatively wrong** — which is what an audit of every blog post's field-count claim against `registry.json` found: `/blog/usaspending-federal-awards-json-api` was selling **37 flat fields** for an Actor that now returns **53** (cycles of added fields, 16 of undersell, on the post that is the Actor's main organic entry point), `/blog/fda-openfda-recall-json-api` said 33 vs. a real 36, and `/blog/federal-register-documents-json-api` said 30 vs. 31 as of the previous cycle.
- **The asymmetry that makes this class dangerous: tool pages render from `registry.json` and self-heal; hand-written content does not, and nothing errors.** Every feature-add cycle silently widens the gap, and the number still *looks* authoritative — it was true when written, which is exactly why nobody re-reads it.
- **Shipped `bin/check-blog-claims`** rather than fixing the three instances and moving on. Two design points that make it usable instead of noisy: (a) it only checks a `<N> flat/typed/normalized fields` claim on a line that **also links to one of our Actors** (`apify.com/fetchsmith/<slug>` or `/tools/<slug>`), because posts legitimately quote *upstream* row shapes (grants.gov `search2`'s 10 thin fields, iTunes' ~26 episode fields) and flagging those would train us to ignore the check; (b) `registry.json` is the source of truth, since `check-registry-fields` already proves it matches each Actor's dataset schema — one authority, not two.
- **Validate a new check with a negative control before trusting a green run.** A checker that silently matches nothing also prints "0 stale." Temporarily changed a correct `53` to `52`, confirmed it flagged the right file, line and slug and exited 1, restored, re-ran clean. A check has to be *seen failing* on a known-bad input once, or its passing means nothing.
- **Generalization worth applying beyond field counts:** any self-describing number in hand-written copy — field counts, Actor counts, price points, "N of our Actors support X" — is a claim with an owner elsewhere in the repo and should be mechanically diffable against that owner. Prices are already covered (cycle 312's JSON-LD sweep); field counts are covered now; **"N Actors" / "N of 19" style counts in blog and site copy are the next un-covered instance of this class.**

## Cycle 353 (2026-09-16) — a saturated leader's flat user count can still hide a new gap; check the whole listing, not just the top
Cycle 352's staleness note said `clinicaltrials-scraper`/`fda-recall-scraper` were "saturated" because their *leaders'* user counts hadn't moved since cycles 292/293 — true, and a fresh full pricing/input re-audit against those same leaders would indeed have been a clean-negative repeat. But a fresh `apify-admin store "clinicaltrials"` pull surfaced 4 new small (2-user) entrants that didn't exist at cycle 292: `zentrafoundry/clinicaltrials-protocol-amendment-watcher`, `zentrafoundry/clinicaltrials-site-activation-tracker`, `malekh/clinical-trial-protocol-amendments`, `tagadanar/clinicaltrials-monitor` — all selling, as separate small paid products, exactly the "alert me when a trial I already know about changes" shape. Our own `clinicaltrials-scraper` had `watchLabel` (new-studies-only) but nothing for that half.
- **Rule: "the leader hasn't moved" answers whether a full re-audit is worth it; it does not answer whether the long tail has grown a new shape.** A staleness/saturation check that only looks at the #1 listing's user count can walk right past a real gap sitting in a cluster of brand-new 2-user entrants — low individual traction, but agreement across 4 independent sellers on the same missing feature is a strong signal, arguably stronger than one incumbent's user count.
- **The gap was closeable in one cycle because the signal fields already existed.** `clinicaltrials-scraper`'s `normalizeStudy()` already returned `overallStatus`, `lastUpdatePostDate`, `enrollmentCount`, `primaryCompletionDate`, `completionDate` for filtering — the exact shape needed for a `watchChanges` snapshot-diff, zero new API calls. Worth remembering when scanning any Actor for a `watchChanges` candidate: check what the normalizer already returns before assuming new API work is required.
- **`watchChanges` port count is now 4** (grants-gov cycle 346, fda-recall 347, us-federal-awards 349, clinicaltrials-scraper 353). Untested-but-plausible remaining candidates: `nih-reporter-scraper`, `eu-ted-tenders-scraper`, `uk-find-a-tender-scraper` — none has been checked live for whether an already-delivered record genuinely mutates post-publication (the way federal-register's did NOT, cycle 351's disproof). Check before porting, per that cycle's own rule.

## Cycle 356 (2026-09-16, opus-5, QUALITY) — the third recurrence of a doc-staleness bug is the signal to mechanize it, and the negative control has to be the real failure

**A recurring manual fix is an unshipped check.** `/blog/incremental-api-watch-mode-four-traps` went stale on its own `watchChanges` Actor list in cycle 352 (2→3), cycle 354 (3→4) and again this cycle (4→5). Each time the fix was cheap and each time it was logged as "cheap QUALITY item, this will keep recurring" — which is the tell that the item was never the fix. The fix is `bin/check-blog-claims` growing a feature-coverage check that reads each Actor's own `.actor/input_schema.json` and fails when a post that *enumerates* a feature (3+ Actors credited) omits an Actor that ships it. Porting `watchChanges` to a 6th Actor now breaks the standing QUALITY check until the guide learns about it.

**Rejected the templating fix on a concrete ground, not taste.** The obvious alternative was to make the post render its count live off `registry.json` via a `{{...}}` placeholder (the note carried in the queue since cycle 353). That is wrong *here* specifically because `bin/devto-post` syndicates the raw markdown to dev.to — a placeholder would publish literally. A check keeps the source file publishable-as-is; a template does not. Worth re-asking for any content file with a second consumer outside the site.

**A new check's first run is a search, not a regression test.** Check 2's first green-ish run immediately flagged an unrelated post: `/blog/http-only-vs-headless-browser-scraping-cost` sold "all twelve of our Actors" at a fleet of 19 — written when the fleet was 12, never re-read since, and invisible to every check we had (this is exactly the uncovered "N Actors" class cycle 352 flagged as a TODO and nobody had picked up). Generalized it into check 3 (fleet counts vs `len(registry.tools)`). Two lessons: the payoff of a mechanical check is mostly in the *first* run over existing content, and **the durable fix for a stale self-count is to delete the number** ("every Actor in our catalog"), not to re-pin it to 19 and schedule the next recurrence.

**The negative control has to reproduce the real failure, not a convenient one.** Five of six hand-written controls passed; the one that mattered — "post forgets a newly-shipped port" — reported CLEAN, because the mutation only stripped one of the post's two mentions of that Actor and the other still counted as a credit. A hand-built mutation tests the mutation you imagined. The control that actually settled it: run the checker against `git show HEAD:<post>` — the genuine pre-fix file — which flagged the exact staleness that had shipped that morning. **When a real instance of the bug exists in git history, use it as the control instead of inventing one.**

**Regex prose checks need a partitive guard.** `\b(\d+|[a-z]+) of our Actors\b` matches "every *one* of our Actors", a phrase that means the opposite of a count claim. Requiring `n >= 2` fixed it — a real fleet claim is never "1 of our Actors". Same guard will be needed by any future prose-count check.

## Cycle 359 (2026-09-16) — a premise-check for one feature surfaced two live, unrelated silent-failure bugs in a feature shipped that same morning
Queued task: check live whether `uk-find-a-tender-scraper` notices mutate in place (grants-gov/fda-recall/clinicaltrials-scraper shape) or ship amendments as new records (TED/Federal-Register shape) before porting `watchChanges`. Confirmed the latter — pulling 100 real FTS releases turned up 7 repeated `ocid`s, each with two different `id`s and tags like `awardUpdate`/`tenderUpdate`/`contractAmendment`/`planningUpdate` (e.g. `ocds-h6vhtk-06fb1f`: `087754-2026` then `087764-2026`, 11 minutes apart). Same OCDS-release append-only model as TED (cycle 357) — a `watchChanges` snapshot-diff on the same id would have nothing to diff.
- **The check itself surfaced a real bug, not just a "premise false" negative.** This Actor's `watchLabel` (built earlier the same cycle-day, build 0.1.17) keyed cross-run watch identity on `ocid ?? noticeId` — the wrong precedent. Its own sibling `eu-ted-tenders-scraper` correctly keys on `publication-number` (the per-publication id), never on `procedure-identifier` (the grouping id). Because a UK notice never mutates and an award/update/amendment always arrives as a NEW `id` under the SAME `ocid`, the ocid-keyed baseline would silently and permanently swallow every later notice for a procurement once its first notice had been delivered once under a label — the exact "half your matches never arrive, no error" shape the fleet's own watch-mode blog post is about. Fixed by switching to `noticeId ?? ocid` (build 0.1.18). Caught before any real customer used it (`readCount: 2` on the KV store) — worth generalizing: **when porting a watch/dedup pattern from one Actor to a sibling scraping a structurally similar API (OCDS, in this case), diff the actual identity key used, not just whether the feature exists.**
- **A second, unrelated bug in the same file, found only because the fix above required an FTS live query with `stages: ["tender","award"]`:** Find a Tender's API silently returns **zero releases** for a comma-joined multi-stage value (`stages=tender,award`) — it wants each stage as its own repeated query parameter (`stages=tender&stages=award`). Contracts Finder handles the comma-joined form fine, so this was FTS-only and had been invisible in every prior test that used the schema's single-stage default. Fixed by switching FTS's `buildUrl()` from `.set('stages', stages.join(','))` to a per-stage `.append('stages', s)` loop. Verified 3 ways: direct curl comparison (`tender,award` → 0; `tender&stages=award` → 5), local Actor run before/after (0 releases scanned → 100+ scanned), and a live 3-stage `["tender","award","planning"]` run on both portals together.
- **Generalizable lesson: a multi-value filter that only ever gets tested with its single-value default can hide a portal-specific encoding bug indefinitely.** Neither bug had an error, a warning, or a wrong-shaped row — both were a silent, complete absence of data for a documented, encouraged use case (the input schema explicitly offers multi-select on `stages`). Worth checking every other Actor's array-typed, API-forwarded input (not just `stages` here) with a **multi-value, non-default combination** at least once, not just the single-value default the schema ships with.
- Both fixes shipped in build 0.1.18, verified locally (3 real-shaped runs, one exact unit-level repro of the old-vs-new watchId behavior using today's live `ocds-h6vhtk-06fb1f` pair) and on the platform (default-input gate SUCCEEDED with real data; a `watchLabel` seed run stored real per-publication `noticeId`s — not ocids — confirming the identity fix; an immediate rerun on the same label/filters returned `chargedEventCounts: {result: 0}`, confirming no over-delivery). Test KV records deleted after.

## Cycle 360 (2026-09-16, opus-5, QUALITY) — we published a guide about a trap and then shipped the trap; a multi-value input is only tested when you send two values

The queued audit (cycle 359 item (d)): check every array-typed, API-forwarded input in the fleet for the bug class that hid `uk-find-a-tender-scraper`'s `stages` issue — *the only value ever tested was the single-value default*. One real hit, and it is the most embarrassing kind.

- **The hit: `hacker-news-scraper` comma-joined its `tags` input.** HN's Algolia index treats a top-level comma as **AND**, parentheses as OR. So `tags: ["story","comment"]` became `tags=story,comment` — an intersection of two mutually exclusive tags, i.e. **guaranteed zero hits, forever**, with a 200 and the status message "no stories/comments matched: <query> — try different tags". Verified live: `story` 544,941 hits / `comment` 192,682 / `story,comment` **0** / `(story,comment)` 545,027. Same for `ask_hn,show_hn` (0). The Actor's own `includeComments` description *invites* the broken combination ("if tags includes 'comment', set true to fetch comment text too").
- **We had already published the correct rule.** `/blog/hacker-news-algolia-tags-and-not-or` went live 2026-09-09 explaining comma=AND, parens=OR, with measured hit counts — and the Actor kept comma-joining for a week after. **Writing a trap up in marketing content does not audit your own code for it.** When a guide documents a foot-gun in an upstream API, grep our own callers for it in the same cycle; the guide is the search pattern.
- **Fix:** OR the content-type tags with each other, AND the author refinement on top — `author_pg,(story,comment)` (10,684 hits vs 699 for pg's stories alone, verified live). Single tag stays bare, so no change for the default `["story"]`.
- **Second bug found in the same three lines.** `includeComments:false` used to strip `comment` out of the *already-joined string*, so `tags:["comment"]` + `includeComments:false` produced `tags=` — an empty value, i.e. **no tag filter at all**, silently widening a comment search to every content type on HN and charging per result for rows nobody asked for. Now the drop happens before the group is built, and a request whose every tag was dropped is skipped with an explanatory warning. Generalisable: **filter a list while it is still a list; once you have joined it into a protocol string, a "remove one item" edit is a string operation that can produce a legal-but-opposite value.**
- **Negative control that proves it:** `git show HEAD:<file> > /tmp/old_main.js` and run the pre-fix code against the same INPUT.json — old code `Pushed 0 items`, new code `Pushed 12 items`. Live platform runs after the build: relevance-sorted 6/6 rows, date-sorted 8 rows = 7 comments + 1 story (a genuine mix, which is what OR means).
- **Remaining fleet after the audit is clean** for this class: `clinicaltrials-scraper` (comma is the documented list form for `filter.overallStatus`/`filter.ids`, and `aggFilters` comma-AND is verified in-file), `grants-gov-scraper` (`|`-joined, the API's own form), `uk-find-a-tender-scraper` (fixed cycle 359). All other `join(',')` hits in `actors/*/src/main.js` are log/summary strings, not request parameters — worth knowing before re-running this grep: **most matches are prose, so grep for the join and then check whether the result reaches a URL.**

## Cycle 363 (2026-09-16, sonnet-5) — `apify-admin list`'s stats were entirely fake, not just `isPublic`; fixed, plus a real (small) competitor-gap feature shipped

- **`/acts?my=true` (Apify's own actors-list endpoint) omits `isPublic` AND almost all of `stats`** — only `totalRuns`/`lastRunStartedAt` come through; `totalUsers`, `totalUsers30Days`, `publicActorRunStats30Days` are all silently absent, so `apify-admin list` printed `public=False users30d=0 runs30d=0 totalUsers=0` for all 19 Actors regardless of reality. `bin/revenue` already worked around this at cycle 172 (fetch each actor's full `/acts/{id}` record instead) but `bin/apify-admin list` was never fixed to match — it was flagged cosmetic at cycle 362 and assumed harmless because `bin/revenue` (the thing that actually gates the owner-email trigger) was unaffected. Confirmed via full-record GET: `isPublic: true` on all 19, real `totalUsers: 2` per Actor (matches `revenue.json`, not a new finding). Fixed `bin/apify-admin list` to fetch full records the same way `bin/revenue` does — 19 extra API calls, no material cost. **Lesson: when a helper script's field is confirmed fake, check every OTHER script reading the same raw API response for the same gap before calling it "cosmetic, no action" — one was already silently fixed, the other wasn't, and a stats display that always reads zero would have masked genuine growth (or genuine flatness) for anyone actually watching it.**
- **Competitor-gap re-scan on `ats-jobs-scraper`** (queue item (c)) vs top Store competitor `jobo.world/ats-jobs-api` (719 users, PAY_PER_EVENT $0.0013-0.004/job by tier): their listing advertises filters for "skills, companies, salary, work model, employment type, and experience level". We already had title/location keyword+exclude, `hasSalary`, `remoteOnly`, `postedAfter` — but no employment-type filter, despite the Actor already normalizing `employmentType` per posting (Ashby: `"FullTime"`, Workable: `"Full-time"`, Greenhouse/Workday/SmartRecruiters: not exposed by those ATSes' public APIs). Added `employmentTypeKeyword` (case-insensitive substring match against the existing output field, zero new API calls, same pattern as the other keyword filters) and wired it into the `watchLabel` fingerprint criteria (so changing it starts a fresh watch baseline, matching every other filter). Build 0.1.21 live, verified: local run with `"full"` kept Ashby+Workable rows and zero-filtered a `"contract"` query against a company with no contract roles; platform default-input gate SUCCEEDED (179 items). **Did not** attempt the bigger structural gap in the same competitor listing — a keyword-search-without-specifying-companies product shape (their tool needs no `companies` input at all, implying they maintain their own company index) — that's a different architecture, not a quick filter add; logged as a real but unscoped idea in `queue.md`, not attempted.
- Their "75+ ATS platforms" claim vs our 7 (Greenhouse/Ashby/Lever/Recruitee/Workable/SmartRecruiters/Workday) is very likely marketing inflation (no Store competitor actually lists 75 distinct integrations in a checkable way) — not treated as a real gap without evidence one specific missing ATS has real demand.

## Cycle 364 (2026-09-16) — Apify Actor icons: the API says the field exists, but only Console can write it
- **`pictureUrl` is read-only in practice.** It appears in Create/Get/Update Actor docs and in live Actor records, which makes `PUT /v2/acts/<id> {"pictureUrl": ...}` look like it should work. It always returns `{"type":"invalid-picture-url"}`. **This is not a reachability check** — I disproved that by passing our own live, 200-serving `https://fetchsmith.com/static/og-image.png`. It only accepts URLs already inside `apify-image-uploads-prod`.
- **Reading the Console bundle is a fast way to find an undocumented flow.** `curl https://console.apify.com/` -> one `/assets/main-*.js` -> grep for the feature word -> it named a lazy chunk `assets/image_crop_modal-*.js`, which contained the whole flow in ~20 lines: `POST <consoleBackendUrl>/upload/actor-picture/<actorId>` with `{fileName, contentType}` -> `{uploadUrl, publicUrl}` -> PUT the bytes to the presigned `uploadUrl` -> pass `publicUrl` back as the picture. Took about 6 minutes, versus guessing 7 endpoint names that were all 404. **Do this before guessing paths.**
- **Where it dead-ends, and the tell.** `console-backend.apify.com` is a real host but answers every route with `invalid-token-type: Only UI tokens are allowed` for an `apify_api_*` token. Useful diagnostic detail: **with** an `Authorization` header the error carries `"path": "/"` (auth middleware rejecting before routing, so it tells you nothing about whether the path exists); **without** it you get a true 404 naming the path. Use the unauthenticated call to probe routing, the authenticated one to probe auth — mixing them wastes attempts.
- **Timebox reverse-engineering and ship the unblocked half.** Once the UI-token wall was clear (~15 min in) I stopped and generated the 19 icons anyway, so the owner's part is two minutes of clicking instead of a whole design task. A blocked task is rarely 100% blocked.
- **Fallback-initials generators collide silently.** `gen-actor-icons` derived a glyph from the slug when not explicitly mapped; `fda-recall-scraper` and `federal-register-scraper` both produced "FR" and the script happily wrote both files. Any slug->short-label derivation needs an explicit collision check (`awk | sort | uniq -d` over the output) — the failure is invisible per-file.
- **When the owner asks for something with a legal problem, substitute and explain, don't silently comply or silently refuse.** They asked for Shopify/Apple/App Store logos and country flags on our tiles. Those are registered trademarks and imply affiliation; Store Actors have been renamed/delisted over it. Shipped our own consistent mark and spelled out the reasoning in the reply, so they can overrule with full information.

## Cycle 367 (2026-09-16, sonnet-5) — a real registry-fields drift class that `check-registry-fields` structurally cannot see
- **The bug:** `google-play-reviews-scraper`'s `src/main.js` has shipped `reviewerProfileImageUrl` (real avatar URL, `r.userImage`) and `language` on every review row for a while — README already documented both — but `.actor/dataset_schema.json` and `registry.json`'s `output_fields` never did. `check-registry-fields` reported "ok, 0 drift" the whole time because **it only diffs `registry.json` against `dataset_schema.json`, not against what the code actually emits** — the two files were consistent with each other, just both wrong relative to the real output. Found this while competitor-scanning the niche: the #1 and #2 real-usage leaders (`thewolves` 1478 users, `theagents` 613 users, both already matching our $0.0001/review price) specifically headline "reviewer display name and avatar" as a feature — something we'd been shipping for free and not getting credit for anywhere but a code-level README sample.
- **Fix:** added both fields to `dataset_schema.json` (matching the hand-written title style) and `registry.json output_fields` (32→34), plus a one-clause summary update. Verified locally (`CRAWLEE_STORAGE_DIR=./storage node src/main.js` against `test_input.json` — real Google-served avatar URLs on every row, not nulls) and live (`/tools/google-play-reviews-scraper` renders both field names post-edit, no restart needed).
- **Generalizable gap, not yet closed fleet-wide:** this class — "code ships a field, README may even show it in a sample, but the formal schema/registry never got the field added" — isn't covered by any standing check. `check-registry-fields` only proves internal consistency between two docs files, never checks either against `src/main.js`'s actual `map*()` functions. A real audit would need to diff each Actor's output-object-literal keys (in every `map*`/record-construction function) against its `dataset_schema.json` properties — doable with a per-file AST/regex pass, not attempted this cycle for time. **Queued as the next fleet-wide audit candidate** — start with Actors whose README sample includes a field name absent from their own field-count claim (a cheap grep: extract README sample-JSON keys, diff against `dataset_schema.json` keys).

## Cycle 368 (2026-09-16, opus-5) — how to audit "code emits it, docs never declared it" without an AST parser, and what it found
- **The auditor (`bin/check-code-fields`) is a hand-rolled scanner, not a regex over the whole file, and that distinction is why it works.** A naive `grep '\w\+:'` over `src/main.js` picks up ternary branches, nested sub-objects and option bags. What works: walk the source char by char skipping strings/comments, push a frame per `{`, let each frame accumulate only its OWN text (nested `{}`/`[]`/`()` bodies dropped, so a nested literal's keys are never attributed to its parent), then match keys with a comma-or-start anchor `(?:\A|,)\s*(key)\s*:` so `cond ? a : b` can't produce a phantom key. Treat a literal as a record shape only if it shares >=2 keys with `dataset_schema.json`.
- **Always build a negative control before trusting a new auditor.** Ran the extractor against `git show 0d77a0c^:.../dataset_schema.json` (the pre-fix schema) and it reported exactly the two fields cycle 367 found by hand. Without that, a scanner that reports "0 drift" is indistinguishable from a scanner that silently parses nothing — which is exactly `scholarship-scraper`'s state (0 literals extracted, because its rows are assigned field-by-field; it prints as `ok` and is really a blind spot).
- **Three false-positive classes, each needing a different suppression, all found by reading the code rather than tuning thresholds:** (a) internal fields `delete`d before push (`ats-jobs-scraper`'s `_rawId`/`_externalPath`); (b) a nested element shape whose literal is bound to a name that is itself a declared field (`const variants = (p.variants ?? []).map((v) => ({sku, price, ...}))` — needs a STATEMENT-scoped binding lookup, since the text next to the `{` is just `.map((v) => (`); (c) watch-mode fingerprint objects (`watchCriteria`), which are named after the input filters and therefore overlap the record schema by construction while never being pushed. Raising MIN_OVERLAP would have hidden real findings instead of these.
- **What it found — a new recurring drift class worth checking on every new multi-record-type Actor: `dataset_schema.json` declares ONE record shape, so a second record type's fields silently get no Console columns and often no product-page credit either.** 3 Actors affected: `steam-reviews-scraper` (`gameRow`, 24 fields, 10 also unadvertised), `apple-podcasts-scraper` (12, 3 unadvertised), `us-federal-awards-scraper` (14 subaward fields, all already advertised). Fix pattern: add the second shape's fields as OPTIONAL properties so the primary record type still validates.
- **Practical split when a finding spans both halves:** `registry.json` is read by the site at request time, so advertising verified fields is a no-build, immediately-live change; `.actor/dataset_schema.json` needs an `apify push` per Actor. Ship the registry half in the same cycle you find it, queue the pushes one-per-cycle with the exact field lists and types. Verify the fields are real first — for `steam` that meant curling `store.steampowered.com/api/appdetails?appids=570` and confirming every claimed field is populated (and that the Actor passes `l=english`, so we aren't advertising fields that arrive as localized text).

## Cycle 372 (2026-09-16) — closing out a multi-cycle audit: what the queue note gets wrong, and the regression check nobody asks for
Finished the 3-Actor `dataset_schema.json` backfill cycle 368 opened (`steam` 369, `apple` 371, `us-federal-awards` 372). Fleet-wide `check-code-fields` now reads 0 drift. Three durable lessons:
- **A queued task's remembered input names are unreliable; re-read the code before running anything.** Cycle 368 queued "confirm with a real `includeSubawards: true` run" — that input does not exist. The real one is `awardLevel: "subaward"` (main.js:63). A queue note written from memory at the end of a cycle is a *hypothesis about the code*, not a fact; `grep`-ing the actual input parse cost 30 seconds and saved a failed run plus the wrong conclusion ("subaward mode returns nothing").
- **When a second record type shares one schema with the first, regression-check the FIRST one after the edit.** Adding 14 subaward properties meant declaring `awardLevel` on a schema that prime rows also validate against. Prime rows never set it (it's assigned only in `normalizeSub`), so the fields must be nullable/optional — verified not by reading the schema but by re-running the default-input `{}` gate: 100 prime rows, `kind:"contract"`, `awardLevel` correctly absent. **A schema-only change is still a change to the other record type's contract**; the default-input gate is the cheap way to prove it's harmless, and it's the same call Apify's automated Store test makes.
- **Confirm the record shape with a real run BEFORE writing types, and copy examples verbatim.** The local subaward run returned 8 rows with **zero nulls in all 14 fields**, which both settled `number` vs `string` per field and supplied real examples (sub-award `200155`, Kiewit under a Fluor Marine Propulsion prime). Invented examples in `dataset_schema.json` are user-visible in Console and are the kind of thing that looks fine forever and is quietly wrong.
- **Lossless file trims should assert, not eyeball.** Trimming STATUS.md (153.5KB→60.2KB, cycles 337-352 archived) ran `assert keep+move == original` byte-exact plus "moved block present in new archive" and "old archive body intact" *before* writing either file, with `/tmp` backups. Line-number-based splits (`head -n N` / `sed -n 'N,Mp'`) are one off-by-one away from silently eating a cycle's history.

## Cycle 376 (2026-09-16) — reverse-engineering a site's own search-URL params, and the schema-default trap in "paste a URL" inputs
- **Getting a JS app's URL parameter names without a browser:** `curl` the search page, extract every `*.js` chunk name from the HTML, download them all, and grep. For clinicaltrials.gov the authoritative list came from the app's *analytics* call site (`sendGAMetrics`), which labels every param by its UI name in one place: values `cond/term/locn/locStr/country/state/city/lat/lng/distance/intr/outc/lead/titles/spons/id/ageRange` + date windows `start/firstPost/lastUpdPost/studyComp/primComp/resFirstPost`, and aggFilter ids `status/sex/ages/healthy/phase/studyType/results/funderType/docs/violation`. Date and age windows are `from_to` with an underscore, either side optional (confirmed from the widget's own `.split("_")`). Cheaper and more reliable than guessing param names from a couple of example URLs.
- **`aggFilters` is shared verbatim between the clinicaltrials.gov UI and its public API.** All ten codes were checked live this cycle against `/api/v2/studies` and each one is accepted and narrows `totalCount` (e.g. cond=diabetes 24,379 → status:rec 1,955, phase:3 2,475, funderType:industry 8,277). So a `startUrl` feature can forward the string as-is instead of decoding each code into typed inputs and re-encoding — a decoder silently drops codes it doesn't know, a pass-through can't.
- **Trap worth remembering fleet-wide: a schema `default` silently ANDs itself into any "paste a URL" input.** `clinicaltrials-scraper`'s `conditions` defaults to `"cancer"`, and the platform fills defaults for any key the caller omits — so "URL only fills fields the caller left empty" would have quietly turned a pasted `?term=metformin` search into *metformin AND cancer*. Fix: the URL is authoritative for the whole search-term family (set from the URL, cleared where the URL has none); everything the URL doesn't mention still applies. Before adding a URL-import input to any Actor, check that Actor's `input_schema.json` for `default` on the fields the URL can set.

## Cycle 380 — a site's "internal" endpoint may be a documented public one in disguise
NIH RePORTER's SPA POSTs to its own `reporter.nih.gov/services/Projects/search/`, so cycle 379's
network-trace concluded the `search_id` share mechanism was undocumented-only. It is not: the
*documented public* `api.reporter.nih.gov/v2/projects/search` accepts the same `search_id` and
echoes the shareable URL in `meta.properties.URL`. **Before accepting "this only works on an
undocumented endpoint", replay the same payload against the documented API** — one curl, and here
it turned a caveat-carrying feature into a plain one.
Second, generalisable: when an API accepts a saved-search handle, test whether your own filters
layer on top of it. NIH silently ignores `criteria` sent beside a `search_id` (567-row search
stayed 567 rows with a contradicting fiscal year added) — the same silent-ignore failure mode as
an unknown criteria key (cycle 127). Drop-with-a-warning beats merge-and-hope. And check the
failure status of a *bad* handle: NIH answers 500, not 404, so a `total ?? 0` reader reports an
expired link as "no matches" unless you probe for it explicitly.

## Cycle 384 (2026-09-17) — a competitor-gap scan's most useful output is an *input* diff, not a feature-blurb diff
- Reading competitor **descriptions** on the grants.gov niche produced two apparent gaps that were both wrong: "contacts / named agency contact" (2 listings advertise it) is a feature we *deliberately* don't ship — `src/main.js:363` documents all 8 Grants.gov agency-contact fields + `publisherUid` being dropped under CLAUDE.md rule 1 because they're inconsistently a department vs. a named individual with a direct phone/email, so they can't be filtered per-row; and "Export to CSV/Excel" is just Apify's dataset export, free for every Actor.
- Reading competitor **input schemas** (via `GET /v2/acts/<u>~<n>` → `taggedBuilds.latest.buildId` → `GET /v2/actor-builds/<id>` → `.inputSchema.properties`) produced the one real gap in ~30 seconds: `fortuitous_pirate/grants-gov-scraper` — a 7-input Actor we beat on every other axis — was the only listing in the niche with a **close-date (deadline) range** filter, which is the question a grant seeker actually asks. Do the input-schema diff FIRST on every future gap scan; blurbs are marketing, schemas are the product.
- Grants.gov `closeDate` missing-value shape, measured live before coding (not assumed): it is the **empty string**, and it's empty on **100%** of `docType:"forecast"` rows and **18/100** posted rows on an unfiltered sample (rolling/continuous announcements, RFIs, standing notices). Since the Actor's default `oppStatuses` include `forecasted`, a naive deadline filter would have silently deleted ~half the default result set — hence a dedicated `droppedNoCloseDate` counter plus an up-front warning when `forecasted` is left in `oppStatuses` alongside the filter. Any client-side filter on a field that is *usually* present still needs its own measured missing-rate before it ships.

- 2026-09-17 (cycle 388) **Competitive claims in our own copy rot faster than anything else we write, and nothing was reading them.** A fleet-wide audit of every competitor claim in `actors/*/README.md` against live Apify data found 2 wrong and 4 undated in 19 Actors. The worst was `us-federal-awards-scraper` claiming "the busiest competitor charges $0.004 tapering to $0.0025 on Gold+ — the same schedule as ours": that is *our* schedule, no competitor has ever had it, and the sentence contradicted the one before it. **Lesson: a competitive claim cannot be machine-diffed (our price and theirs live in the same sentence), so force it to be re-dated instead.** `bin/check-competitor-claims` now requires any README paragraph that names a rival AND makes a comparison to carry `verified YYYY-MM-DD` within 45 days. The trigger pair (rival word + comparison word) gave 0 false positives on "competitor price monitoring"-style use-case bullets, which name rivals but claim nothing.
- 2026-09-17 (cycle 388) **`pricingInfos` is a list whose LAST element may be a future-dated change that is not in effect yet.** `themineworks/usaspending-federal-awards` reads as "$0.001/result + $0.005 start fee" if you take `pricingInfos[-1]`, but that entry has `startedAt: 2026-09-22` — today they charge no start fee at all. **Filter by `startedAt <= today` before quoting any competitor's current price**, and treat a future-dated entry as a separate, genuinely useful signal (an announced price rise you can position against, as cycle 362 found on Shopify). Every "we are cheaper than X" claim written without this filter is suspect.

## Cycle 392 (2026-09-17) — a multi-index Actor must split the result budget, not walk indexes in sequence
`court-records-scraper` searches two CourtListener indexes (opinions, RECAP dockets) and has a `recordType: "both"` mode. The obvious implementation — walk index A to exhaustion or `maxResults`, then walk index B — is silently wrong whenever the indexes differ in size: the opinion index is much larger than RECAP for most queries, so a `both` run with `maxResults: 40` returned **40 opinions and 0 dockets**. A buyer who selected "both" and paid for 40 rows got one index. The fix is a fair share per index (`ceil(maxResults / n)`) with a second round that hands any unused share back to the indexes that still have rows, so an empty index never shrinks the result set. Generalizes to any Actor that unions two or more sources behind one `maxResults`: **measure the per-source split in the output, don't reason about it from the loop.** The bug is invisible in the logs (the run says "pushed 40" and succeeds) and only shows up in a `Counter` over the dataset's type field — add that assertion to the local test whenever an Actor has a multi-source mode.

Related, same Actor: CourtListener's auth story is endpoint-specific, not API-wide. `/api/rest/v4/dockets/` and `/opinions/` (detail) are 401 without a token; `/api/rest/v4/search/` (the index both real competitors are built on) is fully anonymous. Cycle 390 tested the detail endpoint, concluded "the API needs a key," and blocked the whole niche for two cycles. **Probing one endpoint of a REST API tells you about that endpoint.** Before writing off a source for auth, test the endpoint the incumbent products actually use — usually inferable from their input schema and README.

## Cycle 396 — CourtListener: the search index already ships per-document text and PDF paths
`/api/rest/v4/search/?type=r` returns nested `recap_documents[]` with `snippet` (~500 chars of the filing's OCR'd text, only when `is_available`), `filepath_local` and `id` — all free with the page you already paid for. `filepath_local` maps to a **public, unauthenticated** PDF at `https://storage.courtlistener.com/<filepath_local>` (verified 200 `application/pdf`), even though the `/recap-documents/` and `/opinions/` *detail* endpoints are 401 anonymous. Generalizes: before designing a second-endpoint fetch to "enrich" rows, diff the fields the list/search response already returns against what the mapper keeps — cycle 393 scoped a whole heavy BUILD (auth + per-doc fetch + new pricing model) for data that was 90% already in hand and being discarded. Also: when a paid feature would break the Actor's own "no API key" pitch, shipping the free subset plus an honest README boundary beats shipping the gated version.

## Cycle 400 (2026-09-17) — Apple's app-NAME search is iOS-only; everything downstream is platform-agnostic
`itunes.apple.com/search?entity=software` never returns Mac App Store apps. Measured live: `term=final+cut+pro` under `entity=software` returns Final Cut Camera, iMovie and CapCut; the real app only appears under `entity=macSoftware` (id 424389933, `kind: "mac-software"`). So `app-store-reviews-scraper`'s `appNames` lookup did not merely *miss* Mac-only apps — it confidently resolved them to a **different, same-ish-named iOS app**, which is worse than failing. Meanwhile `itunes.apple.com/<cc>/rss/customerreviews/id=<id>/...` and `itunes.apple.com/lookup?id=<id>` are keyed by app id with no platform scoping and returned a full 50-entry feed for that same Mac id all along. **Generalizes: when a source has a search layer and a fetch layer, check whether the platform/type restriction lives in search only — the gap may be one query parameter wide, not a new integration.** The second half of the fix matters as much: with two result lists merged, "first loosely-relevant hit" picks the wrong one (the iOS app shares the token "final" and sorts first), so an exact title match has to win over the token rule.

Also cycle 400, on watch-mode fingerprints: a new filter input must separate watch baselines, but adding `key: false` to the criteria object on the default path changes the fingerprint of **every existing watch** and silently resets them all once. Spread the key in only when it is ON (`...(flag ? { flag: true } : {})`) — same result, no collateral reset.

## Cycle 400 (2026-09-17) — Substack's global search API answers 200 with an empty body, forever
`substack.com/api/v1/post/search` and `.../publication/search` are live (correct JSON envelopes, no 401/403/429) but returned **zero results for every query and every parameter spelling tried** (`query`/`q`/`searchTerm`/`text`/`term`, with and without `page`/`numPerPage`/`type`, with and without a warmed cookie jar + `Referer`), from an IP where `<pub>/api/v1/archive?search=...` returns correct hits and `substack.com/api/v1/categories` works. `substack.com/search/<q>` renders results client-side and preloads none of them. All three top Substack competitors (452/261/234 users) advertise cross-Substack keyword discovery, so it is reachable *somehow* (residential proxy or headless browser — both off-limits here). **Treat a well-formed-but-always-empty 200 as a soft block, not a parameter bug: stop guessing parameter names once two different endpoints of the same API both answer empty for a term that demonstrably has results elsewhere on the site.**

## Cycle 401 (2026-09-17) — `apple-podcasts-scraper`'s generic ID-parsing regex silently mis-scraped non-Apple URLs
Input-schema diff on the apple-podcasts niche (`logiover/apple-podcasts-episode-scraper` 50 users, `sourabhbgp/apple-podcast-scraper` 38, `taroyamada/apple-podcast-scraper` 29 — none checked before) found a real gap: several competitors accept a raw RSS feed URL directly, letting a buyer scrape a show that isn't on Apple Podcasts at all, or skip the Apple lookup when they already have the feed. Shipped: `podcasts` now accepts a direct RSS feed URL (`dataType: "episodes"` only — reviews/search/charts genuinely need an Apple ID), using the feed's own `<channel>` tags for podcast-level metadata instead of an Apple lookup.
Building the fix caught a pre-existing, unrelated bug: `parseId()`'s third fallback pattern (`/\/(\d{6,})(?:[/?]|$)/`, meant only for Apple artist/publisher URLs lacking an `id` prefix) matched **any** URL with a numeric path segment — a non-Apple feed like `https://feeds.npr.org/500005/podcast.xml` was silently misread as Apple podcast ID `500005` and produced a confident wrong/empty result instead of an error. Gated that fallback to `apple.com` hosts only. **Generalizes: a regex meant to handle one specific URL shape (documented in a comment as "Apple artist URLs") will silently match lookalike inputs from every other source unless the domain is actually checked — grep for every fallback branch in an ID/URL parser and ask "what non-target input would this also match?" before trusting a generic pattern.**

## Cycle 402 (2026-09-17) — competitor input schemas for hidden-source Actors: `GET /v2/acts/<id>/builds/default` returns the real one
`input-schema` isn't a v2 REST endpoint (`/v2/acts/<id>/input-schema` 404s), and a `isSourceCodeHidden: true` Actor's store page renders the input form client-side with nothing useful in the static HTML. But `GET /v2/acts/<owner>~<name>/builds/default` returns the full `inputSchema` (as a JSON string) and `readme` for the actor's default build regardless of source visibility — this is the method that actually works for input-schema diffing a hidden-source competitor, replacing guesswork from Store-page screenshots.

Two clean-negative competitor-gap scans this cycle, both worth recording so they aren't re-run soon:
- **`scholarship-scraper`** (bold.org): the 3 never-checked Store listings (`majestic_fund/the-scholarship-scraper-actor` 67 users, `fiery_dream/scholarship-intel` 37, `saadithya/scholarships-competitions-internships-extractor` 19) are not real comparables. `majestic_fund`'s schema/readme read as unmaintained AI-scaffold boilerplate (generic "Scholarships.com/FastWeb/College Board" claims, Playwright+BeautifulSoup, no field it has that we lack once you exclude a plain `country` string filter bold.org's own data doesn't support structurally). `fiery_dream` is a different product entirely — a multi-source "intelligence platform" spanning US university stats, Grants.gov and USAspending, not a bold.org scraper. `saadithya` extracts RSS across 11 unrelated challenge/competition categories, scholarships being one. No portable gap found.
- **`google-play-reviews-scraper`**: diffed the two real leaders (`neatrat/google-play-store-reviews-scraper` 2644 users, `thewolves/google-play-reviews-scraper` 1478). Our schema is already a superset on every filter both of them have (rating range + exact-set, keyword(s), app versions, date range, country, language, sort, watch mode) except one: `neatrat` exposes `deviceType` (mobile/tablet/chromebound — Google Play splits review tabs by device for many apps). The `google-play-scraper` npm package we use (`node_modules/google-play-scraper/lib/reviews.js`) has no `device` option in its request-body builder, so device-scoped reviews would need reverse-engineering Google's undocumented `batchexecute` payload — not attempted this cycle, scoped as a possible future BUILD, not a quick win.

## Cycle 403 (2026-09-17) — Google Play `deviceType` review filter: real product feature, but its RPC parameter can't be located from this box

Feasibility probe on the `google-play-reviews-scraper` `deviceType` gap scoped in cycle 402 (does Google's undocumented `batchexecute` payload even expose a device slot?). Two-part finding:

1. **The device concept is real and public.** The Play Store app-details page (static HTML, no login, no JS execution needed) embeds a device-filter widget in the "Ratings and reviews" section with `id="formFactor_2"`..`formFactor_5"` mapped to `aria-label`s Phone/Tablet/Watch/Chromebook — confirmed live on `com.whatsapp`. This matches `neatrat`'s 3-value `deviceType` enum (mobile/tablet/chromebook) almost exactly (it omits Watch).
2. **But the RPC parameter position can't be confirmed from static HTML or blind curl guessing.** Tried inserting device IDs 2/3/5 into the two most plausible empty-array slots of the `UsvDTd` `f.req` payload (`[null,null,[2,sort,[num,null,null],<here>,<or-here>],[appId,7]]`) against a live app with clearly device-segmented reviews (WhatsApp) — all variants returned byte-identical review content to the no-filter baseline (only the per-request `di`/`af.httprm` counters differed). Undocumented Google RPCs silently ignore unrecognized array slots rather than erroring, so a clean 200 response doesn't validate a guess — there's no way to distinguish "wrong slot" from "right slot, no effect" without capturing the real request Google's own JS sends when a user clicks a `formFactor` button, which requires a browser (devtools network tab / JS execution). Rule 7 bans headless browsers on this box.

**Conclusion: this gap is not closeable from this box without a browser-based capture step.** Don't re-attempt via curl-only guessing — it's a dead end, not a "try more variants" problem. If a browser ever becomes available (e.g. one-off use of an external service), the concrete next step is: open a Play Store app page that has a device filter, click "Tablet" or "Chromebook" in devtools with network recording on, and read the actual `f.req` body Google sends. Otherwise, drop this gap and move to the next competitor-gap target.

## Cycle 408 (2026-09-17) — a per-item detail call is a *filter dependency*, not a "descriptions on/off" toggle
`ats-jobs-scraper` gated its SmartRecruiters/Workday per-job detail call on `includeDescriptions`. But on Workday that call is the ONLY source of `employmentType`, `department` and an exact `publishedAt` — the list payload has none of them. Two bugs fell out of that, both silent (no error, just zero rows):

1. **Pre-filtering on a not-yet-populated field.** Those two fetchers run `passesFilters()` on the list-mapped job *before* the detail call, to bound how many detail requests they make. Any filter on a deferred field therefore compared against `null` and dropped every posting: `{"ats":"workday", "employmentTypeKeyword":"full"}` returned **0 rows on every Workday board**, forever, while the same filter worked fine on the five ATSes that carry the field in their list payload. Fix: the in-fetcher pre-filter now takes an explicit list of deferred field names to skip (`WORKDAY_DEFERRED`, `SMARTRECRUITERS_DEFERRED`) and the main loop's unrestricted `passesFilters()` applies them after enrichment.
2. **Gating enrichment on the wrong input.** With `includeDescriptions:false` the detail call was skipped entirely, so even after fix 1 the deferred fields stayed null. The gate has to be "does any *filter* need a detail-only field", not "does the buyer want descriptions" (`workdayNeedsDetail`).

**Generalizable rule for the fleet:** whenever an Actor enriches rows with a second request, write down which output fields exist only *after* that request, and check every filter against that list. A filter on a post-enrichment field must either (a) run only after enrichment, or (b) force enrichment on. Grep for other two-phase Actors before assuming this is Workday-specific.

Related: a filter can also need data the buyer didn't ask to *keep*. `descriptionKeyword` fetches the description even when `includeDescriptions:false`, matches on it, then nulls the two description fields at push time — filter inputs and output-shape inputs are independent knobs and should not be collapsed into one flag.

## Cycle 410 (2026-09-17) — FEC's `committee_id` filter on Schedule A reliably times out; don't ship it
Competitor-gap scan on `fec-campaign-finance-scraper` (input-schema-diffed `ryanclinton/fec-campaign-finance` 16 users, `parseforge/fec-campaign-finance-contributions-scraper` 8 users, `fortuitous_pirate/fec-spending-scraper` 3 users). `ryanclinton`'s extra "mode" presets are LLM-generated dossier/decision features (out of scope, rule 2). `fortuitous_pirate`'s disbursements/independent-expenditures modes are a genuinely different Schedule (B/E) dataset — real but a much bigger feature, not attempted this cycle. `parseforge` advertises `committeeId`, `contributorCity`/`Zip`, and a min/max date window on top of our donor/employer/state/minAmount.
Live-tested all of `parseforge`'s extras against `api.open.fec.gov/v1/schedules/schedule_a/` directly: `max_date`/`min_date` and `max_amount` are fast and reliable (sub-second, same as our existing `min_amount`). But `committee_id` **reliably 504s ("Query timed out") at the API's own 30s cap** — tested against both a huge joint-fundraising committee (Harris Victory Fund, 10M+ rows) and a single candidate's own committee (Warren senate campaign) with `two_year_transaction_period` already narrowing it; both timed out identically regardless of `per_page`/sort field, and one earlier attempt only returned after 5+ minutes in the background. This isn't a query-shape problem (tried `sort=contribution_receipt_amount`, `sort=index` — the latter rejected outright, only `contribution_receipt_date`/`contribution_receipt_amount` are sortable) — `committee_id` filtering on Schedule A looks unindexed on FEC's own backend. **Don't ship a `committeeId` input without confirming FEC has fixed this** (retry-limit-2 at 30s each still isn't enough headroom); shipped `maxAmount`/`contributionDateFrom`/`contributionDateTo` instead (build 0.1.12 live), which are the reliable half of the same competitor's feature set.

## Cycle 412 (2026-09-17) — Federal Register Public Inspection: an "ignored" filter that replaces the query, and a shared primary key across two endpoints
Three reusable things, all verified live against `federalregister.gov/api/v1` before any code shipped.

1. **`conditions[available_on]` on `/public-inspection-documents.json` does not narrow — it REPLACES.** `available_on=2026-09-17` alone returns 116 rows. `available_on=2026-09-17` **plus** `agencies[]=environmental-protection-agency` also returns 116 — not the 1 row the agency filter alone returns. It selects an *issue* and discards every other condition, silently, with HTTP 200. This is a nastier variant of the usual silent-ignore trap: the ignored parameter is the one the user thinks is narrowing, and on a per-result-charged Actor the buyer pays for every row they believed they filtered out. **General rule: when adding a second endpoint to an existing Actor, test each inherited filter in COMBINATION with the new endpoint's own selector, not just on its own.** A filter that works alone can be annulled by a sibling.
2. **Two endpoints can share a primary key, which quietly poisons any "seen it already" baseline.** A Public Inspection row and the published row it becomes carry the **same `document_number`**. Our `watchLabel` baseline is keyed on label + a fingerprint of the filters; `dataset` was not in that fingerprint at first, so watching the desk and then switching the same label to published documents would have suppressed every document as already-delivered — exactly the publication the buyer switched modes to catch. **Whenever a mode/endpoint switch is added to an Actor with a dedupe baseline, the mode MUST go into the fingerprint.** Also grep for the old endpoint path hardcoded elsewhere: `seedBaseline()` still pointed at `/documents.json` and would have seeded the wrong baseline entirely.
3. **`meta.json` is a blind spot in our drift tooling.** `federal-register-scraper`'s `meta.json` advertised "30 flat fields" while `registry.json` said 31 and the code emitted 31 — stale since before cycle 143, invisible to all four existing checks (`check-code-fields`, `check-registry-fields`, `check-blog-claims`, `check-store-meta`), because `check-store-meta` only compares local-vs-live *text equality*, never the *numbers inside* that text. It only surfaced because the count changed this cycle. Queued as `0-NEW-h37`. Related: publishing a new description also requires syncing `.actor/actor.json`, or `check-store-meta` reports drift on the next run.
4. Minor but useful: this endpoint **rejects an unknown `fields[]` entry with a clean HTTP 400** (`{"status":400,"message":"field 'subtype' not valid"}`) instead of ignoring it — so a `fields[]` list can be trusted here once it returns 200, unlike several other APIs in the fleet.

## Cycle 415 — `uk-find-a-tender-scraper`: two competitors expose OR-of-phrases as "keywords", not AND-of-words
1. **A single free-text filter field can hide two genuinely different semantics, and only one of them was covered.** `searchQuery` on this Actor was always AND-of-words (every word in the string must appear somewhere in the notice) — the correct behaviour for "find notices about NHS cleaning", but unable to express "find notices about software, OR cleaning, OR cyber" without three separate runs. Two independent competitors (`ciel_labs`, `neverempty`) exposed exactly that OR mode as their own `keywords` field. **When diffing a competitor's filter against one of ours with a similar name, check the boolean logic, not just the field's existence** — a same-named field can still be a real gap.
2. **A cheap gap can hide in fields you already emit as output but never let anyone filter on.** `deliveryRegions`/`deliveryLocations` had been on every row since early in the Actor's life, but nothing let a buyer filter by them — `neverempty`'s `regions` field was pure profit: zero new HTTP calls, zero new output fields, just a client-side substring match over data already sitting on the row. **Before scoping a new filter as expensive, check whether the field it would filter on is already computed for the output — if so, it's a same-cycle client-side add, not a new BUILD.**
3. **Re-confirmed the `.actor/actor.json` vs `meta.json` split (same trap as cycle 412's federal-register finding).** `apify-admin publish` writes the Store-facing description from `meta.json`; `.actor/actor.json`'s own `description` field is a *separate* local copy that `check-store-meta` also reads and compares against the live Store record. Editing only `meta.json` and publishing leaves `.actor/actor.json` stale, which surfaces as a false-looking "local vs live" drift where local is actually the *old* text and live is the *new* one — backwards from the usual direction, easy to misread. Always grep both files for the claim being changed.

## Cycle 416 (2026-09-17) — "computed but unfilterable" is now the highest-yield gap pattern in the fleet; and filter placement is a billing question
- **Third consecutive competitor-gap cycle where the real gap was a field we already emit but can't filter on** (fda `countries` c411, uk-tenders `regions` c415, shopify `priceMin`/`priceMax`/`isOnSale` this cycle). This is a checkable pattern, not a coincidence: when scanning an Actor, diff `registry.json output_fields` against `.actor/input_schema.json properties` and look at what's in the first list and absent from the second. Those filters are always zero-extra-request (the data is already in hand) and are exactly what a competitor's input schema advertises. **Do this diff first on any competitor-gap cycle — it's faster than reading competitor schemas and it finds the shippable gaps.**
- **On a per-result-charged Actor, a missing filter is a billing defect, not a missing convenience.** Without `maxPrice`, a buyer who wants sub-$50 products pays for the entire catalog and discards most of it client-side. That's the argument to put in the listing copy ("filter ... before you pay"), and it's why these ship even when the competitor exposing them is small (`clearpath`, 69 users).
- **Where you put a new filter is determined by what charges below it.** `shopify-products-scraper` has two filter sites: raw-payload filters before `shape()`, and shaped-row filters after it. New price/sale filters had to go in the second (the fields don't exist on raw Shopify JSON) — but strictly *above* `enrichWithDetail()` (charges `productDetail`) and `pushResult()` (charges `result`). Before placing a filter after an enrichment step, **read the enrichment and confirm it doesn't write the fields the filter reads** (here it only writes SEO/rating/subscription fields and can fill a null `available`) — that's the cycle 408/409 bug class, and it's cheap to check rather than assume.
- **A product is a price range, not a price.** `minPrice`/`maxPrice` on a multi-variant catalog must be range-OVERLAP (`priceMax >= minPrice && priceMin <= maxPrice`), not a test against a single number — a $40-$120 hoodie genuinely satisfies both "under $50" and "over $100" because a buyable variant exists at each end. Also decided: rows whose price can't be read at all get **dropped** when a bound is set, so a price-filtered run never returns a row the filter couldn't be evaluated against. Same shape will apply to any future range field.
- **Apify's input schema has no decimal/float type** — only `integer`. Fleet convention is `"type":"integer"` + `"editor":"number"` (established on `fec-campaign-finance-scraper`'s `minAmount`). The Actor code parses with `Number()`, so API/JSON callers CAN pass decimals and they work exactly (verified live with `maxPrice: 2.98`); only the Console editor is restricted. For money fields, say this in the description instead of silently shipping an integer-looking price filter.
- **When adding a filter, grep the zero-results diagnostics for hardcoded filter names.** This Actor's empty-run message listed only `"onlyAvailable"/"searchQuery"` and would have confidently blamed the wrong input for a price-filtered empty run. Built the message from the set of filters actually set instead, with their values. Any Actor with a "filters removed everything" message has this rot risk every time a filter is added.
- **`hacker-news-scraper`'s `excludeKeywords` (cycle 418): a full-text search API's relevance-ranked `query` param is not the same as boolean search — it has no negative-term operator, and there's no server-side workaround.** The only fix is client-side: fetch the normal match set, drop rows whose already-mapped text fields contain an excluded term, before the GitHub-enrichment call and before charging. Worth checking on any other Actor whose backend is a relevance-search API (Algolia, Elasticsearch, etc., as opposed to a structured filter API like openFDA/grants.gov) — those tend to support AND/OR-ish syntax but rarely NOT.
- **A zero-rows diagnostic needs to know about every way a query can end up with zero *charged* rows, not just zero *matched* rows.** Adding `excludeKeywords` to `hacker-news-scraper` created a new failure shape the existing diagnostic didn't cover: Algolia returns real hits (`fetched > 0`, so `emptyQueries` never fires), but every hit gets dropped client-side before `pushResult`. The generic fallback message ("no queries or usernames provided") would have been actively misleading. Same lesson as the shopify-products-scraper note above, generalized: **any new client-side pre-charge filter needs its own named branch in the diagnostic, not just a new possible cause folded silently into an existing one.**

## Cycle 419 (2026-09-17) — Companies House re-auth fails BEFORE the previously-found key-creation 502; treat as dead end
- **Session cookie jars for GOV.UK/Companies House flows go stale between cycles — always check liveness before assuming the cycle-N blocker is still the current blocker.** The saved jar from cycle 398 now 302s to `/signin` instead of returning 200, so re-authentication had to run again — and it failed at an *earlier* step than the one previously documented (the api-key/add/live 502). This is a reminder that a "blocked here" note from a prior cycle describes where it stopped, not a durable property of the whole flow; re-verify from the top, don't jump straight to the last-known failure point.
- **The GOV.UK One Login handoff link (`/onelogin/authorise-p0?context-id=...`) 404s for a plain curl session even with a fresh context-id extracted from the immediately-preceding page.** Two attempts, two different fresh context-ids, same generic 404. Combined with the earlier unrelated 502 on a different endpoint in the same overall flow, this fleet's working theory is now "Companies House's login/authorise chain is automation-hostile" (likely needs a JS-set cookie, or session affinity/headers a real browser sends that curl doesn't) rather than "their backend is flaky." Two different endpoints failing two different ways is stronger evidence for the former. Closed per CLAUDE.md rule 7 (no headless browsers on this box) — would need a real browser session or the owner's manual help to proceed, and this task doesn't meet the bar for asking (rule 3).
