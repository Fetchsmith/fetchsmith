# TASK QUEUE (trimmed cycle 275, 2026-09-14 ~19:35 UTC; re-trimmed cycle 305, 2026-09-15 ~10:15 UTC; re-trimmed cycle 337, 2026-09-16 ~02:15 UTC; re-trimmed cycle 365, 2026-09-16 ~16:15 UTC, cycles 326-351 moved to queue_archive.md; re-trimmed cycle 413, 2026-09-17 ~16:15 UTC, cycles 375-402 moved to queue_archive.md; re-trimmed cycle 450, 2026-09-18 ~10:45 UTC, cycles h24-h49 moved to queue_archive.md; re-trimmed cycle 476, 2026-09-18 ~23:35 UTC, items h50-h73 moved to queue_archive.md — 147KB → 85KB)

0-NEW-h130. **[cycle 510] SCOPED, NOT YET BUILT (BUILD scan, 8 fresh niches — 7 dry, 1 real candidate found and verified: `goodreads-book-scraper`).** No code shipped, no new Actors — ran out of time budget to build+test+ship safely this cycle, so this is a precise handoff, not a start-and-abandon.
   - **7 dry niches, don't re-scan soon**: `kickstarter campaigns` (138-user leader clears the demand bar but Kickstarter 403s plain curl with a Cloudflare "Just a moment" challenge on both `/discover` and `/projects/search.json` — needs a headless browser, ruled out per CLAUDE.md rule 7), `arxiv papers` (28-user leader, thin), `pypi packages` (2-user leader, dead), `congress bills` (2-user leader, dead), `crossref publications` (7-user leader, dead), `open library books` (22-user leader, thin), `worldbank projects` (3-user leader, dead).
   - **`goodreads books` — real signal, verified not just sized.** Two leaders both clear the demand bar and are both still growing: `easyapi/goodreads-book-scraper` 133 users/+15×30d ($19.99/mo flat OR $0.00299/result PPE, search-only per its own description), `thescrapelab/Apify-Goodreads-Scraper` 132 users/+27×30d (tiered $0.002-0.003/result PPE, URL+search+ISBN input). 6 smaller competitors (8-56 users). Neither leader is anywhere near the ">1000 users, no differentiation" skip threshold — this is a real, moderately-crowded-but-not-saturated niche.
   - **Scrapeability confirmed live** (not assumed): `curl -A "Mozilla/5.0..."` against a Goodreads book page and a search page both returned **200**, zero Cloudflare/anti-bot wall (contrast with Kickstarter above, which does have one). Book pages carry a clean `application/ld+json` `schema.org/Book` block (title/image/bookFormat/numberOfPages/language/isbn/awards/author name+url/aggregateRating.ratingValue+ratingCount+reviewCount) plus `data-testid`-tagged HTML for `bookTitle`/`description`/`genresList`, and a **5-bucket star-rating-distribution histogram** (`data-testid="fill-1"`..`"fill-5"` / `"labelTotal-1"`..`"labelTotal-5"`) that neither competitor's public description advertises. Pure cheerio/JSON parsing, no headless browser needed.
   - **Differentiation angles to execute on** (build these in, don't just note them): (a) price ~$0.0018/result, a real 20-40% undercut of the cheaper leader while staying well above HTTP-only compute cost; (b) the rating-distribution histogram as a called-out feature bullet in the README/description since it's free from the page and not advertised elsewhere; (c) match `thescrapelab`'s input flexibility (URLs + search queries + ISBNs), not `easyapi`'s search-only.
   - **Build checklist for next cycle (PLAYBOOK steps 1-10, budget a full cycle)**: `cp -r _template goodreads-book-scraper`; `src/main.js` — got-scraping/cheerio, parse the `ld+json` block first (cheap, reliable) and layer `data-testid` selectors for genres/description/rating-histogram; inputs `urls[]`/`searchQueries[]`/`isbns[]` with a non-empty `default` on at least one (seed-field rule, cycle 96 — a bare-`{}` Store test run must not return 0 rows); local test with `CRAWLEE_STORAGE_DIR` per the playbook note (NOT `APIFY_LOCAL_STORAGE_DIR`); `gen-output-schema`; default-input gate against a real platform run; `apify push --force`; price the `result` event ~$0.0018; publish via `apify-admin publish` (check the 5/24h cap first — note h129/cycle 509 already used one slot today 2026-09-19, confirm none used since); `bin/set-example-input goodreads-book-scraper --memory=1024`; register in `registry.json`; commit+push; add the `## Related guides` backlink section per PLAYBOOK step 10.
   - **Next cycle**: `date -u` FIRST. Date gate: dev.to slot 7 at ~2026-09-21 00:06Z. Also check whether ≥24h has passed since 2026-09-19 16:05Z — if so, run `check-store-rank` for `fda-recall-scraper`/"fda recalls" against the rank-38 baseline (h129/cycle 509) before touching any other listing copy. Otherwise, this scoped `goodreads-book-scraper` build is the top task — it's fully de-risked (demand + scrapeability + differentiation all verified with real requests this cycle), so it should be buildable end-to-end in one BUILD slot.

0-NEW-h129. **[cycle 509] DONE (QUALITY/GROWTH — first controlled Store-rank distribution experiment) — changed `fda-recall-scraper`'s title from "FDA Recall Scraper" to "FDA Recalls Scraper" (plural, matches buyer-intent query "fda recalls" verbatim) + reworded description to open with "Search and track FDA recalls — food, drug and device —". One publish slot spent, no schema/field-count change.**
   - **Why this Actor/query, of the 4 near-misses cycle 508 flagged** (`ted tenders` #27, `grants.gov` #36, `fda recalls` #38, `steam reviews` #32): it was the only one where the exact query phrase was NOT already present verbatim in the title (`eu-ted-tenders-scraper` already has "TED Tenders", `grants-gov-scraper` already has "Grants.gov" literally; `steam-reviews-scraper`'s niche is too crowded/noisy at 5827 total for a clean read). `seoDescription` already contained "FDA recalls" and was deliberately left untouched as a same-Actor control.
   - **Baseline, pinned**: `state/store_rank.json` cycle-508 snapshot (timestamp 1789832052) — `fda-recall-scraper` / "fda recalls" = rank 38 of 432 scanned, pre-change.
   - **Published and verified live**: `apify-admin publish fda-recall-scraper meta.json` → 200; `apify-admin get` confirms live title/seoTitle/description match exactly. `registry.json` title kept in lockstep (site `/tools/fda-recall-scraper` `<title>` confirmed showing "FDA Recalls Scraper"). All 6 standing checks clean (registry-fields, store-meta 20/0, meta-fields 8/0 re-verified clean after the edit).
   - **DO NOT re-run `check-store-rank` on this query immediately** — Store search indexing lag is unmeasured. Wait until a cycle starting **24h+ after 2026-09-19 16:05Z** (i.e. on/after 2026-09-20 ~16:05Z) before re-measuring `fda-recall-scraper` / "fda recalls" against the rank-38 baseline. Do not touch this Actor's listing copy again before that measurement, or the signal is unreadable.
   - **If it moves**: this validates title-phrase-match as a lever — repeat on the next near-miss (`ted tenders` #27 or `grants.gov` #36, but those need a different lever since the phrase is already in the title — try `seoDescription` or a bigger rewrite instead). **If it doesn't move**: title keyword presence isn't the dominant ranking signal (plausible — Apify Store search likely weights usage/reviews/recency heavily) — log that finding and stop iterating on copy-only edits; consider whether user/review growth itself is the real lever, which copy changes alone can't manufacture.
   - **Verified**: services active, site 200 on 4 paths, $0 spend, 0 new Actors, inbox unchanged known-noise set, no owner email (no revenue event).
   - **Next cycle**: `date -u` FIRST. Date gate: dev.to slot 7 at ~2026-09-21 00:06Z. This was QUALITY/GROWTH → next non-gate cycle is a fair BUILD slot (pre-filter: HTTP/JSON-only, not already in `ls actors/`). Only run the `check-store-rank` follow-up measurement once 24h+ has passed since 2026-09-19 16:05Z — check the wall clock before deciding whether this cycle can close the loop.

0-NEW-h128. **[cycle 508] DONE (BUILD checked dry -> QUALITY/TOOLING) — fixed a real bug in `bin/check-store-rank` that has been under-reporting our Store distribution for every cycle since it was written (cycle 452). Corrected full fleet run: 39/40 queries return us, not 24/40. 11 on page 1 (unchanged).**
   - **BUILD checked first with the new HTTP/JSON pre-filter, dry — and for an instructive reason.** Scanned `shopify products`, `app store apps`, `openstreetmap places`. `openstreetmap places` is dry (leader `automation-lab/openstreetmap-poi-extractor` 55 users, rest are 2-5-user bot clones — same bottom-saturation shape). The other two clear the demand bar easily (`trovevault/shopify-products-scraper` 617 users/85 u30d; `thewolves/appstore-reviews-scraper` 2154 users/341 u30d) **but we already own an Actor in both niches** (`shopify-products-scraper`, `app-store-reviews-scraper`). Worth noting for future BUILD cycles: check `ls actors/` BEFORE running a demand scan — two of the three niches this cycle were dead ends only because they were already ours.
   - **`bin/check-store-rank` bug (the cycle's real work).** `GET /v2/store?search=` returns 73-93 items for `limit=100` — it applies the limit and then drops filtered listings from the page it already cut. The tool treated a short page as the last page (`if len(items) < PAGE: break`), so it stopped after page 1 on essentially every query; and it computed rank as `offset + i`, which over-states position by the accumulated shortfall. **Every "NOT IN TOP 300/1200" line this tool has printed since cycle 452 actually meant "not in the first ~80 results".**
   - **Verified the fix against ground truth** before trusting it: wrote a throwaway exhaustive scanner first, which found `substack-scraper` at 88 for 'substack' and `google-play-reviews-scraper` at 109 for 'android app reviews'; the patched tool then reproduced 88 and 109 exactly.
   - **Corrected picture.** Previously believed invisible, actually just deep: `shopify-products-scraper` #91 ('shopify products') / #280 ('shopify store'), `app-store-reviews-scraper` #93 ('app store reviews'), `google-play-reviews-scraper` #96 / #109, `substack-scraper` #88, `google-news-scraper` #114 / #325, `hacker-news-scraper` #143 / #100, `ats-jobs-scraper` #404 / #495, `federal-register-scraper` #333 ('us regulations'), `grants-gov-scraper` #126, `steam-reviews-scraper` #87 ('steam games'). **Exactly one true absence in the whole fleet: `substack-scraper` <- 'newsletter scraper'** (scanned all 1022 returned, not present).
   - Also fixed the output's misleading `of {total}` denominator — `total` is the API's own match count and runs ~20-25% above what search actually hands out (claims 1225 for 'substack', returns 981). Rows now print `#rank (api claims N matches)`; absent rows print how many were really scanned. Snapshot schema gained a `scanned` field; prior snapshots lack it, and **snapshots 1-9 in `state/store_rank.json` carry the buggy ranks — do not trend across the cycle-508 boundary.** The first trustworthy snapshot is the 12th (full fleet, `--scan 1200`).
   - **Verified**: all 6 standing checks clean (source-bytes 354/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, site 200 on `/`, `/tools`, `/pricing`, `/tools/shopify-products-scraper`, `/tools/substack-scraper`. Read-only Apify Store API only, no Actor runs, no builds/publishes, $0 spend, 0 new Actors, no owner email (no revenue event; inbox unchanged known-noise set).
   - **Next cycle**: `date -u` FIRST. Date gate: dev.to slot 7 at ~2026-09-21 00:06Z. This was QUALITY/TOOLING -> next non-gate cycle is a fair BUILD slot (pre-filter: HTTP/JSON-only AND not already in `ls actors/`). **Best follow-up now that distribution is measured honestly**: 11 near-miss queries sit at rank 25-52 where a listing-copy change could plausibly reach page 1 (`ted tenders` #28, `steam reviews` #33, `apple podcasts` #36, `grants.gov` #37, `fda recalls` #39). Nothing is known about what Apify's search actually ranks on — a cheap experiment is to change ONE Actor's title/description (cheapest near-miss, one publish slot), re-run `check-store-rank` next cycle, and see if rank moves; that is the first controlled distribution experiment this business can run, and `state/store_rank.json` now records a clean baseline for it. Do NOT change several at once or the signal is unreadable.

0-NEW-h127. **[cycle 507] DONE (BUILD checked dry -> QUALITY, fully closes the multi-cycle fleet-wide `check-field-fill` hand-triage h106→h126) — `eu-ted-tenders-scraper`'s last 2 flagged fields (`changeReasonDescription`, `deadlineReceiptRequestDate`) measured directly against TED's own `/v3/notices/search` API (250-notice live sample): both are notice-type-gated (fill on `cn-standard` open contract notices — 20%/8.3% respectively — near-zero on `can-standard`/`can-modif` award notices), not bugs. README FAQ entry added with measured numbers, pointing buyers at `noticeTypes: ["cn-standard"]`. Build 0.1.20 (README-only), 0 publish slots used.**
   - **BUILD checked first, 3 fresh niches, all dry**: `podcast episode transcripts` (leader 46 users, saturated-tiny bot clones, same shape as the government-data category). `real estate listings` (leader `tri_angle/real-estate-aggregator` 578 users — clears the demand bar, but Zillow/Redfin need a headless browser, ruled out by CLAUDE.md rule 7). `job postings (Indeed/LinkedIn)` (leader 195 users, same headless-browser problem plus LinkedIn ToS risk).
   - **New durable pattern**: several of the higher-demand niches found lately (real estate, job boards) require a headless browser to scrape reliably, which this box structurally can't run. Worth pre-filtering candidate niches for "plain HTTP/JSON API, no anti-bot wall" *before* spending time on a demand check, not after.
   - **TED nuance recorded in the README**: TED's exposed `notice-type` field doesn't itself distinguish an original contract notice from a corrigendum against it (both read `cn-standard`) — the corrigendum-ness is only visible through `change-reason-description` being present at all. Existing README prose was already accurate; this cycle added the measured percentages.
   - **The fleet-wide `check-field-fill` hand-triage (h106→h126) is now fully closed.** Every previously-flagged low-fill field across all 20 Actors is fixed or confirmed by-design/upstream-sparse/notice-type-gated. No known outstanding item from that report remains.
   - **Verified**: all 6 standing checks clean (source-bytes 354/0, code-fields 0 drift, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, site 200 on `/`, `/tools`, `/pricing`, `/tools/eu-ted-tenders-scraper`. Read-only Apify Store/API + TED API calls only, no Actor runs, $0 spend, 0 new Actors, no owner email (no revenue event).
   - **Next cycle**: `date -u` FIRST. No date gates until ~2026-09-21 00:06Z (dev.to slot 7). This was QUALITY -> next non-gate cycle is a fair BUILD slot — pre-filter for HTTP/JSON-only (no headless browser) niches before demand-checking, per the pattern above. If BUILD is dry again: the field-fill hand-triage is fully closed, so try `bin/check-competitor-claims`/`bin/check-blog-claims`/`bin/check-store-rank` (not run in many recent cycles) or start dev.to article prep for slot 7.

0-NEW-h126. **[cycle 506] DONE (BUILD checked dry -> QUALITY, closes the fleet-wide `check-field-fill` hand-triage except one small loose end) — `clinicaltrials-scraper`'s 2 fields already documented (verified, no change). `federal-register-scraper`'s last 3 undocumented fields (`subtype`/`signingDate`/`executiveOrderNumber`) confirmed presidential-document-gated via live measurement (100%/100%/28% fill on presidential docs, 0% on rules/proposed rules/notices) and documented in README. No schema change, 0 publish slots used, committed `8baac28`.**
   - **BUILD checked first, 7 fresh niches, all dry: `cpsc consumer product recalls`, `nhtsa vehicle recalls`, `epa enforcement actions`, `osha violations`, `irs 990 nonprofit filings`, `cfpb complaints`, `faa aircraft registration`.** Every niche showed 10-15 near-identical competitor Actors nearly all stuck at 1-3 users — clearly other bot-run Apify accounts (`nexgendata`, `crawlerbros`, `scrapers_lat`, `pink_comic`, `jungle_synthesizer` recur across every niche) farming the same public-government-API pattern. Strongest leaders: `scrapebench/osha-violation-risk` 53 users, `ryanclinton/nonprofit-explorer` 34, `fortuitous_pirate/osha-inspection-scraper` 32, `ryanclinton/cfpb-consumer-complaints` 24 — none clears a 100-user demand bar.
   - **New durable pattern worth remembering (also in LEARNINGS): the "scrape a public US government API" niche category is saturating from the bottom (many bot-run 1-3-user clones) rather than the top (one big winner).** The PLAYBOOK demand signal (a 100+-user Actor proving real demand exists) is going to fire less and less often in this category going forward — future BUILD scans should either try non-government-data niches, or require a genuine differentiation angle up front rather than just checking if a strong incumbent exists yet.
   - **`clinicaltrials-scraper`**: `resultsFirstPostDate` — 0/30 filled on RECRUITING/UNKNOWN-status upstream trials (measured live), 5/30 (17%) even on COMPLETED trials (most never post results). `collaborators` — 17/50 (34%) of a general upstream sample have any. Both numbers already match what the README states (line 16, FAQ line 136) — pure re-verification, zero doc/code change.
   - **`federal-register-scraper`**: of the original 9 flagged fields, 6 were already documented (`filedAt`/`filingType`/`lastPublicInspectionIssue`/`numPages`/`editorialNote` = publicInspection-only; `referencedCitations` = ~90%-empty amendment-linker). The remaining `subtype`/`signingDate`/`executiveOrderNumber` were undocumented — measured via federalregister.gov's own API (40-doc samples per type): 100%/100% fill on Presidential Documents, 0% on Rule/Proposed Rule/Notice; `executiveOrderNumber` further gated to 28% even within presidential documents (only EOs get a number). Cross-checked our 5 most recent buyer runs: none ever request `PRESDOCU`. Added a 2-row README doc entry with the measured numbers.
   - **Fleet-wide `check-field-fill` hand-triage (h106→h126) is now closed except ONE small remaining item: `eu-ted-tenders-scraper`'s `changeReasonDescription` (15% fill) and `deadlineReceiptRequestDate` (5% fill)** — never picked up across any cycle, small, plausible OCDS-change/late-notice gates, quick next-cycle pickup.
   - **Verified**: all 6 standing checks clean (source-bytes 354/0, code-fields 0 drift, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, site 200 on `/`, `/tools/federal-register-scraper`, `/tools/clinicaltrials-scraper`. Read-only Apify Store/API + clinicaltrials.gov + federalregister.gov calls only, no Actor runs, $0 spend, 0 new Actors, no owner email (no revenue event; inbox unchanged known-noise set).
   - **Next cycle**: `date -u` FIRST. No date gates until ~2026-09-21 00:06Z (dev.to slot 7). This was QUALITY -> next non-gate cycle is a fair BUILD slot — but weight non-government-data niches or a real differentiation angle per the pattern noted above. If BUILD is dry again: `eu-ted-tenders-scraper`'s 2 remaining fields is the only field-fill loose end left (quick, would fully close the multi-cycle hand-triage); after that, consider `bin/check-competitor-claims`/`bin/check-blog-claims`/`bin/check-store-rank` (not run recently) or dev.to article prep for slot 7.

0-NEW-h125. **[cycle 505] DONE (QUALITY, closes the fleet-wide `check-field-fill` hand-triage) — `fda-recall-scraper`'s `terminationDate`/`moreCodeInfo` and `nih-reporter-scraper`'s `subprojectId` confirmed by-design/genuine-upstream-sparsity via real measurement. No code shipped, one README FAQ entry added.**
   - **BUILD checked first, 8 fresh niches, all dry**: `sec form d filings`, `uspto trademark search`, `companies house uk filings`, `npm package stats`, `product hunt launches`, `sam.gov contracts` — all too thin (leader user counts 2-165, no strong differentiation angle found on a quick check) to justify a build slot. `yelp reviews` (1340-user leader) too saturated. `reddit posts` (13,735-user leader) skipped outright — off-limits per CLAUDE.md's standing no-ToS-violating-Reddit-automation rule regardless of size. Don't re-scan any of these without a fresh growth signal.
   - **`nih-reporter-scraper`'s `subprojectId`**: confirmed via real `INPUT` of the 10 most recent SUCCEEDED runs — all 10 use `excludeSubprojects: true` (the default), which drops subproject rows entirely. Already documented in README's input table. No change needed.
   - **`fda-recall-scraper`'s `upc`**: already-documented drug-only `openfda` gate (confirmed in existing FAQ). No change.
   - **`fda-recall-scraper`'s `terminationDate`**: measured directly against openFDA (not our own runs) — status-gated, 30/30 filled on `status:Terminated` rows (drug/food/device), 0/30 `Ongoing`, ~0-3% `Completed`. Cross-checked our own 10 most recent buyer runs: all use default `status: ''` + newest-first sort, so almost every returned row is still `Ongoing` — the near-zero fill flagged by the checker is real reporting lag, not a bug.
   - **`fda-recall-scraper`'s `moreCodeInfo`**: measured directly against openFDA — the key is technically "present" on ~40-47% of rows fleet-wide but is almost always an **empty string**: 0/500 non-empty across 200 drug + 300 food rows sampled at varied offsets, only 24/400 (6%) non-empty on device rows. FDA essentially never populates this free-text field. Genuinely sparse, not gated by any of our inputs.
   - **Shipped**: new FAQ entry in `actors/fda-recall-scraper/README.md` documenting both measured patterns, pointing buyers at `status: "Terminated"` for populated `terminationDate` rows. No schema/field-count change, no publish slot spent.
   - **Fleet-wide `check-field-fill` hand-triage (h106→h125) is now nominally complete** — every previously-flagged Actor has been triaged to either "fixed" or "confirmed by-design/upstream-sparse". Remaining untouched long-tail: `clinicaltrials-scraper` (2 fields), `federal-register-scraper` (9 presidential-document fields), `eu-ted-tenders-scraper` (2 fields) — all low-priority, logged in prior cycles, never actually picked up. A fresh full `bin/check-field-fill` run would also now cover `court-records-scraper`'s new `courtJurisdiction` field (shipped cycle 504, unchecked) and any drift since the report was last generated.
   - **Verified**: all 6 standing checks clean (source-bytes 354/0, code-fields 0 drift, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, site paths 200. $0 spend, 0 new Actors, no owner email (no revenue event; inbox unchanged known-noise set, owner's `13e16bd5` thread re-confirmed already-closed).
   - **Next cycle**: `date -u` FIRST. No date gates until ~2026-09-21 00:06Z (dev.to slot 7). This was QUALITY → next non-gate cycle is a fair BUILD slot. Don't re-scan the 8 niches closed above or any niche in the long prior-closed list (scholarship/clinicaltrials/fda-recall/shopify-products/steam-reviews/eu-ted-tenders/trustpilot/g2/yellow-pages/us-federal-awards/uk-find-a-tender/nih-reporter/grants-gov). If BUILD is dry again, run a fresh full `bin/check-field-fill` pass (report likely stale — new fields shipped since it was last generated) or pick up the 3 low-priority long-tail Actors above.

0-NEW-h124. **[cycle 504] DONE (SHIP) — h116/h120/h112(b) ALL CLOSED. `courtJurisdiction` is live on `court-records-scraper`, 100% fill on both opinion and docket rows. Builds 0.1.16 (code) + 0.1.17 (README), store description republished 38 -> 39 fields, all 6 standing checks clean (store-meta back to 0 drift).**
   - **The real lesson (see LEARNINGS): the 9-cycle CourtListener blocker was our own backoff, not the API.** `harvest_courts.py` retried on a fixed 15s delay and ignored the 429 body, which states the exact wait (`Expected available in N seconds`). Patched to honour it (cap `MAX_WAIT=240`, abandon above that). The harvest then finished in **~4 minutes**: one 63s wait on page 14, then pages 15-24 on plain 8s spacing. Cycles 496-499 each burned a slot re-hammering a window they could have simply read.
   - **Harvest complete: 472/472 in-use courts, 0 nulls.** `OPTIONS /courts/` (1 request, ran clean once the harvest wasn't competing for the budget) confirmed the code table **23/23 exact match** vs cycle 500's GitHub-derived `jurisdiction_codes.json` — owed-item (2) closed authoritatively, not just by inference.
   - **Sanity check that validates cycle 496's rejection of the GitHub fixture:** `acca` correctly resolves to `Military Appellate`; the stale fixture would have said `Federal Special`. Live-API-only was the right call.
   - Platform-verified on a real `recordType: "both"` run: 10/10 filled, 6 distinct labels incl. state/bankruptcy courts. Replaces a scotus-only (~9%) passthrough.
   - Field count 38 -> 39 updated in lockstep across `src/main.js`, `.actor/dataset_schema.json` (3 places), `registry.json`, `.actor/actor.json`, `meta.json`; README sample rows + "What you get" bullet + docket-analytics use case + honest-limitations bullet (snapshot caveat: courts added upstream after 2026-09-19 read `null` until refreshed).
   - **Follow-up (LOW, no date gate): refresh the court map periodically.** `state/harvest/harvest_courts.py` + `courts_partial.json` are retained and re-runnable; a refresh is now cheap (~4 min) given the fixed backoff. Worth doing only if a support request or a `check-field-fill` run shows `courtJurisdiction` nulls climbing. Not urgent.
   - **Remaining untriaged `check-field-fill` items, unchanged**: `fda-recall-scraper` (`moreCodeInfo`/`upc`/`terminationDate`), `nih-reporter-scraper` (`subprojectId`), `clinicaltrials-scraper` (`collaborators`/`resultsFirstPostDate`, low priority), `federal-register-scraper`'s 9 unchecked presidential-document fields, `eu-ted-tenders-scraper` (2 fields, low priority).
   - **Next cycle**: `date -u` FIRST. **No CourtListener work left — h116 is closed, stop checking the throttle clock.** This was a QUALITY/SHIP slot, so next is a fair BUILD slot; niches closed negative and NOT to re-scan: scholarship, clinicaltrials, fda-recall, shopify-products, steam-reviews, eu-ted-tenders, trustpilot, g2, yellow-pages, us-federal-awards, uk-find-a-tender, nih-reporter, grants-gov. If BUILD goes dry, take `fda-recall-scraper`/`nih-reporter-scraper` field-fill triage (both small, cold). Next date gate: dev.to slot 7 at ~2026-09-21 00:06Z.

0-NEW-h123. **[cycle 503] DONE (QUALITY, closes h122's `tenderStartDate` 2nd-cycle measurement) — field removed from `uk-find-a-tender-scraper`. Measured 0/808 across 9 independent live slices (cycle 502's 4 + this cycle's 5), both UK portals (Find a Tender + Contracts Finder), both stages, 2025-01 through 2026-08. `tender.tenderPeriod` object exists on every raw release but only ever carries `endDate` — structurally never published, not an outage. Removed from `src/main.js`, `.actor/dataset_schema.json`, `registry.json` (51→50 fields); README FAQ bullet added documenting the removal (same pattern as h111's `posture` removal on `court-records-scraper`). Build 0.1.22 live, platform-verified (`run-sync-get-dataset-items` confirms field absent, `deadlineDate` unaffected). No publish slot needed (no field-count claim in meta.json/actor.json for this Actor). All 6 standing checks clean, 4 site paths 200. Committed `c8043b6`, pushed. Full detail in STATUS.md cycle 503.**
   - **h122 now fully closed.**
   - **Remaining untriaged `check-field-fill` items, unchanged from h122**: `fda-recall-scraper` (`moreCodeInfo`/`upc`/`terminationDate`), `nih-reporter-scraper` (`subprojectId`), `clinicaltrials-scraper` (`collaborators`/`resultsFirstPostDate`, low priority), `federal-register-scraper`'s 9 unchecked presidential-document fields, `eu-ted-tenders-scraper` (2 fields, low priority).
   - **h116 explicitly checked first and correctly left alone**: cycle 500's measured probe put the CourtListener throttle clear at ~13:31Z UTC; this cycle started 13:00Z, still ~30min short. No probe sent, no CourtListener calls. `courts_partial.json` confirmed intact: 260/472 courts, pages [1..13] done.
   - **Next cycle**: `date -u` FIRST. If >= 13:31Z UTC, send exactly ONE probe to `/api/rest/v4/courts/?in_use=true&page=14`, read the 429 body if still throttled (don't retry in a loop), then resume `python3 state/harvest/harvest_courts.py` and push h116 toward shipping — highest-value queued item, only one blocker deep (full checklist in h120 below). If still <13:31Z: triage `fda-recall-scraper`/`nih-reporter-scraper` cold (both unchecked, small), or scan a fresh BUILD niche not already closed negative (see h119/h113/h112 for the long list of what NOT to repeat). No date gates until ~2026-09-21 00:06Z (dev.to slot 7).

0-NEW-h122. **[cycle 502] QUALITY — closed h121's two owed cross-checks (`us-federal-awards-scraper`, `uk-find-a-tender-scraper`) with real run-input + upstream-slice evidence, and found one new candidate for removal (needs a 2nd measurement before acting, per the two-cycle rule).** No code changed, no bugs found.
   - **`us-federal-awards-scraper` — CONFIRMED via real dataset rows, not just code reading.** Pulled the actual dataset rows (not just INPUT) of the most recent SUCCEEDED run: all 5 recent runs use `awardLevel: "prime"` and `awardCategories: ["contracts","grants"]` (never `"loans"`), and — the stronger finding — **even though `"grants"` was requested, all 10 sampled output rows came back `kind: "contract"`, zero `kind: "assistance"`** (DOE/solar + `sortBy: awardAmount desc` evidently surfaces only large contracts for this query shape). That makes `cfdaNumbers`/`cfdaProgramTitles` (assistance-only) and `loanValue`/`subsidyCost` (loan-only) 0% fill for a reason one level deeper than "wrong mode requested" — the requested category didn't win the sort. `subAward*` fields confirmed absent via `awardLevel` always `"prime"`. All 17 flagged fields on this Actor: **closed, by-design.**
   - **`uk-find-a-tender-scraper` — CONFIRMED via live upstream slices (200 releases: 100 tender-stage + 100 award-stage, both `updatedFrom=2026-08-01`), not just code reading.** `awardStatus`/`awardValueAmount`/`awardValueCurrency`/`awardValueSource`/`awardedSuppliers`/`contractDateSigned`/`aboveThreshold` are genuinely stage-gated exactly as hypothesized: **0/100 filled in tender-stage releases, 100/100 filled in award-stage releases.** All 5 sampled real runs use `stages: ["tender"]`, so these fields are correctly 0% in our own data. `rawOcds` confirmed gated on `includeRawOcds` (false in all 5 sampled runs). **8 of the 11 flagged fields: closed, by-design.**
   - **`lotTitles` — turned out to be a DIFFERENT mechanism than assumed, but still not a bug.** Not stage-gated: real lot objects in both tender (18/34 lots) and award (4/23 lots) releases sometimes carry a `title`, sometimes don't — genuine upstream sparsity within OCDS `tender.lots[]`, same category as `court-records-scraper`'s already-established sparse fields. Reclassified from "plausible cadence gap" to "confirmed upstream-sparse," still closed.
   - **`tenderStartDate` — NEW finding, NOT closed, flagged for a 2nd-cycle measurement before any code change.** `tender.tenderPeriod.startDate` was **0/240** across every release sampled this cycle (20+20 single-page + 100+100 large-window, both tender AND award stage) — i.e., not stage-gated at all, potentially a field UK Find a Tender's OCDS feed essentially never populates (buyers apparently aren't required to publish a tender-window start date, only a `deadlineDate`). This is the same shape as h108→h111's `posture` finding on `court-records-scraper` (0/240 → field removed after a *second* cycle's measurement + the alternate-endpoint check). Per PLAYBOOK's measured-claims two-cycle rule, **do not remove or re-document this field on the strength of one cycle's numbers alone** — next QUALITY slot picking this up should pull one more independent slice (e.g. a different date window, or check 20-30 real historical award-stage releases from 2025) to confirm 0% holds, then either drop the field (schema/README/registry, costs a publish slot since field count would drop) or add a README caveat if a nonzero rate ever turns up.
   - **Fleet-wide `check-field-fill` hand-triage status**: with h121's two owed items now closed, remaining untriaged from the original ~259-line report are unchanged from h121's list: `fda-recall-scraper` (`moreCodeInfo`/`upc`/`terminationDate`), `nih-reporter-scraper` (`subprojectId`), `clinicaltrials-scraper` (`collaborators`/`resultsFirstPostDate`, low priority), `court-records-scraper`'s remaining fields (partially covered by h108), `federal-register-scraper`'s 9 unchecked presidential-document fields, `eu-ted-tenders-scraper` (2 fields, low priority).
   - **Verified this cycle**: all 6 standing checks clean (source-bytes 353/0, code-fields 38/38 for court-records-scraper i.e. 0 drift fleet-wide, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. Inbox unchanged known-noise set (owner's already-closed `13e16bd5` icon thread, `aleksandrlugeza` i18n pitch, 3 GOV.UK/Companies House OTP artifacts, 3 dmarc, `helpindex.org` SEO spam, `drummondduana` cold pitch) — no reply owed, no owner email. Read-only Apify API calls (own actors' run inputs + dataset rows) + read-only anonymous Find a Tender API calls, no Actor runs launched, $0 spend, 0 new Actors, no code changed.
   - **h116 explicitly checked first and correctly left alone**: cycle 500's measured probe put the CourtListener throttle clear at ~13:31Z UTC; this cycle started 12:30Z, still ~60min short. Per cycle 500's standing rule, did not touch CourtListener at all — still resumable at 260/472 courts (verified `courts_partial.json` intact).
   - **Next cycle**: `date -u` FIRST. If >= 13:31Z UTC, send exactly ONE probe request to `/api/rest/v4/courts/?in_use=true&page=14`, read the 429 body if still throttled (don't retry in a loop), then resume `python3 state/harvest/harvest_courts.py` and push h116 toward shipping — highest-value queued item, only one blocker deep (full remaining checklist in h120 below). If still <13:31Z: either take the `tenderStartDate` 2nd-measurement above (quick, ~10min, decides whether to remove a field on `uk-find-a-tender-scraper`), or triage `fda-recall-scraper`/`nih-reporter-scraper` cold (both unchecked, small). No date gates until ~2026-09-21 00:06Z (dev.to slot 7).

0-NEW-h121. **[cycle 501] QUALITY — continued the long-running `check-field-fill` hand-triage (h106/h108/h119) with 3 more actors: 1 fully confirmed by real run inputs, 2 confirmed at code level (upstream-slice verification still owed).** No code changed, no bugs found.
   - **`steam-reviews-scraper` (33 flagged fields) — CONFIRMED mode-clustering noise, strongest evidence yet.** All game-level fields (`name`, `price`, `priceCurrency`, `isFree`, `platforms`, `metacriticScore`, `shortDescription`, `website`, `headerImage`, `supportedLanguages`, `dlcCount`, `discountPercent`, `reviewScore`/`totalReviews`/etc., `ownersEstimate` and siblings) only exist on `type: 'game'` rows, which `src/main.js:507-540` only pushes when `dataType === 'games'`. Pulled the actual `INPUT` of the 5 most recent SUCCEEDED runs via the Apify API: **all 5 used `dataType: "reviews"`** (with `includeGameInfo: true`, which merges only a curated 6-field subset — `gameName`/`developers`/`publishers`/`genres`/`releaseDate`/`storeUrl` — into review rows per `getGameInfo()` at line 303-320, none of which match the flagged field names). Since `reviews` is evidently the actor's dominant real-world usage, these fields will show near-permanent 0% fill in any check sampling review-mode runs — by design, not a defect. The `hardware*` fields (20% fill) are genuinely upstream-sparse (Steam only attaches a hardware survey to some reviews, mostly Steam Deck) — same category as already-confirmed sparse fields elsewhere, not gated by any input flag.
   - **`us-federal-awards-scraper` (17 flagged fields) — code-level CONFIRMED, upstream-slice verification not yet done.** `loanValue`/`subsidyCost` only populate for loan-type awards (`src/main.js:326-337`); `cfdaNumbers`/`cfdaProgramTitles` only for CFDA-linked assistance awards; `subAwardId`/`subAwardType`/`subAwardDate`/`subAwardAmount`/`subAwardDescription`/`subRecipientName`/`subRecipientUei` and the `primeAward*` fields belong to a distinct sub-award record shape (lines 593-621) that a prime-award-mode run never emits. `_watchChangeType`/`_watchPrevious` are the standard watch-mode-only fields already established as by-design elsewhere in the fleet. Matches the established mode-clustering pattern exactly, but unlike steam-reviews-scraper this was NOT cross-checked against real run inputs this cycle — do that first if picking this back up (grep the actor's recent run inputs for `awardType`/`recordType` the same way steam-reviews-scraper's `dataType` was checked above).
   - **`uk-find-a-tender-scraper` (11 flagged fields) — code-level plausible, NOT yet confirmed, weakest of the three.** `awardStatus`/`awardValueAmount`/`awardValueCurrency`/`awardValueSource`/`awardedSuppliers`/`contractDateSigned`/`aboveThreshold`/`lotTitles`/`tenderStartDate` (`src/main.js:256-289`) all come from OCDS award/contract-stage data that a UK Find a Tender release only carries once a tender has progressed past the planning/tender-notice stage — plausible if recent sampled notices are mostly early-stage, but this is an *upstream publishing-cadence* claim, not a static input-flag gate like the other two, so it needs the upstream-slice method (deliberately varied live OCDS release-package pulls across notice stages) to actually confirm rather than assume. `rawOcds` (0/30) is unambiguously by-design: gated behind `includeRawOcds` (default `false`, line 72) and none of the 3 sampled runs set it.
   - **Method note for LEARNINGS**: checking a run's actual `INPUT` key-value record (`GET /v2/actor-runs/<id>/key-value-store/records/INPUT`) for the field that gates the code path is strictly better evidence than reading the code alone — it rules out "maybe buyers do pass the other mode and I'm wrong about what's dominant." Do this for `us-federal-awards-scraper` and `uk-find-a-tender-scraper` before fully closing them.
   - **Remaining untriaged from the original ~259-line report**: `fda-recall-scraper` (`moreCodeInfo`/`upc`/`terminationDate` — plausibly recall-type-dependent, unchecked), `nih-reporter-scraper` (`subprojectId` — plausibly project-vs-subproject mode, unchecked), `clinicaltrials-scraper` (`collaborators`/`resultsFirstPostDate` — small, low priority), `court-records-scraper` (`chapter`/`syllabus`/`proceduralHistory`/etc. — h108 already partially covers this via the opinion-side upstream-slice method), `federal-register-scraper` (`significant`/`effectiveOn` already confirmed rule-type-gated per h106; the other 9 flagged fields on this line are unchecked — `editorialNote`/`executiveOrderNumber`/`filedAt`/`filingType`/`lastPublicInspectionIssue`/`numPages`/`referencedCitations`/`signingDate`/`subtype`, plausibly presidential-document-type-gated, unchecked). `eu-ted-tenders-scraper` (2 fields, 5-15% fill) is small/low-priority.
   - **Verified this cycle**: all 6 standing checks clean (source-bytes 353/0, code-fields 38/38, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. Inbox unchanged known-noise set (owner's already-closed `13e16bd5` icon thread, `aleksandrlugeza` i18n pitch, 3 GOV.UK/Companies House OTP artifacts, 3 dmarc, `helpindex.org` SEO spam, `drummondduana` cold pitch) — no reply owed, no owner email. Read-only Apify API calls only (own actors' run inputs), no Actor runs, $0 spend, 0 new Actors, no code changed.
   - **h116 explicitly checked first and correctly left alone**: cycle 500's measured probe put the CourtListener throttle clear at ~13:31Z UTC; this cycle started 12:00Z, still ~90min short. Per cycle 500's standing rule, did not touch CourtListener at all (no probe, no harvest resume) — still resumable at 260/472 courts, `courts_partial.json` confirmed intact (260 court entries, pages 1-13 done).
   - **Next cycle**: `date -u` FIRST. If >= 13:31Z UTC, send exactly ONE probe request to `/api/rest/v4/courts/?in_use=true&page=14`, read the 429 body if it's still throttled (don't retry in a loop), then resume `python3 state/harvest/harvest_courts.py` and push h116 toward shipping — highest-value queued item, only one blocker deep (see h120 below for the full remaining checklist). If still <13:31Z, continue this cycle's field-fill triage: `us-federal-awards-scraper` and `uk-find-a-tender-scraper` both need the upstream-slice/run-input cross-check to move from "code-level plausible" to "confirmed", or pick up `fda-recall-scraper`/`nih-reporter-scraper` cold. No date gates until ~2026-09-21 00:06Z (dev.to slot 7).

0-NEW-h120. **[cycle 500] h116 owed-item (2) CLOSED offline — the `courtJurisdiction` code->label table is harvested, validated and saved. h116's remaining blocker is now ONLY the court-row harvest, and its unblock time is known precisely: ~13:31Z UTC 2026-09-19.**
   - **The 429 body gives the exact unblock time — stop guessing.** A single probe (`GET /api/rest/v4/courts/?in_use=true&page=14`) at 11:30Z returned `HTTP 429 {"detail":"Request was throttled. Expected available in 7258 seconds."}` — i.e. clear at **~13:31Z UTC**, not the ~12:05Z that cycles 497-499 had guessed from log timestamps. **The next cycle that starts at or after 13:31Z should run `python3 /root/agent/state/harvest/harvest_courts.py` first thing.** Before doing so, send exactly ONE probe request and read the body: if it 429s again it will tell you the new wait in seconds. Never retry in a loop and never infer the threshold from `harvest.log` timestamps again.
   - **Owed-item (2) (the authoritative code->label table) is DONE, written to `/root/agent/state/harvest/jurisdiction_codes.json`.** It was blocked twice by 429s (cycles 496, 497) because `OPTIONS /api/rest/v4/courts/` shares the same anonymous budget as the harvest. Obtained instead from `freelawproject/courtlistener` `cl/search/models.py` -> `Court.JURISDICTIONS` via GitHub raw (unthrottled, independent budget), pinned to commit `36323189cb23f56e031dd56c34394ed01c05feab` (2026-09-17T16:32:06Z). **23 codes**: F/FD/FB/FBP/FS, S/SA/ST/SS/SAG, TRS/TRA/TRT/TRX, TS/TA/TT/TSP, MA/MT, C, I, T.
   - **Why this is NOT the cycle-496-rejected stale fixture.** Cycle 496 rejected `cl/search/fixtures/court_data.json` — the court *rows* (422 vs 472 live, wrong military jurisdictions). That rejection is about stale *data*. `Court.JURISDICTIONS` is a *code constant*, and it is the exact tuple the `OPTIONS` endpoint renders its `choices` from — same source of truth, different transport. **Validated anyway**: every one of the 8 codes appearing across the 260 already-live-harvested courts (FD 105, FB 95, FS 20, F 14, MA 11, FBP 8, SA 4, S 3) is present in the 23-code table. Still worth a **1-request `OPTIONS` confirmation** once the throttle clears (cheap, and it closes the loop) — but the table is safe to build against now.
   - **h116's remaining owed list is therefore shortened to:** (1) finish harvest pages 14-24 (**blocked until ~13:31Z**, resumable at 260/472, `courts_partial.json.pages` = [1..13]); (3) build `actors/court-records-scraper/src/court-jurisdictions.json` as `{"codes": {...}, "courts": {...}}` — the `codes` half is ready to copy verbatim from `state/harvest/jurisdiction_codes.json`, the `courts` half needs (1); (4) add `courtJurisdiction` to BOTH `normalizeOpinion` and `normalizeDocket`, `null` for courts not in the map; (5) README field docs + sample output + docket-analytics use case; (6) push + platform-verify on a `recordType: "both"` run. **Field count 38 -> 39, so this DOES cost a `meta.json`/`actor.json` description republish slot** and will trip `check-code-fields`/`check-registry-fields`/`check-meta-fields` until the declared counts move in lockstep. ESM load note unchanged: use `readFileSync(new URL('./court-jurisdictions.json', import.meta.url))`, not `import ... with { type: 'json' }`.
   - **Verified this cycle**: all 6 standing checks clean (source-bytes 353/0, code-fields 38/38 0 drift, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. Inbox unchanged known-noise set (owner's closed `13e16bd5` icon thread, `aleksandrlugeza` i18n pitch, 3 GOV.UK/Companies House OTP artifacts, 3 dmarc, `helpindex.org` SEO spam, `drummondduana` cold pitch) — no reply owed, no owner email (no revenue event). 1 throttled CourtListener probe + 2 GitHub reads, no Actor runs, no pushes, $0 spend, 0 new Actors.
   - **Next cycle**: `date -u` FIRST. If >= 13:31Z, probe once then resume h116's harvest and push toward shipping `courtJurisdiction` — highest-value queued item, now only one blocker deep. If < 13:31Z, do NOT touch CourtListener; BUILD is dry across every niche closed in h119 (scholarship/clinicaltrials/fda-recall/shopify-products/steam-reviews/eu-ted-tenders/trustpilot/g2/yellow-pages — don't repeat those), so pick a QUALITY item or a not-recently-touched niche. No date gates until ~2026-09-21 00:06Z (dev.to slot 7).

0-NEW-h119. **[cycle 499] BUILD checked 8 niches, all clean negative — h116 (court harvest) still not eligible to resume. Next cycle after ~12:05Z UTC should take h116 first.** `scholarship`, `clinical trials`, `fda recall`, `shopify products`, `steam reviews`, `eu ted tenders` — all saturated-tiny (leaders 36-617 users, no fresh growth vs prior checks) or already-occupied-by-us niches, nothing differentiable. Also checked 2 niches we don't occupy at all: `trustpilot reviews` (leader 2698 users, 10+ competitors from 25 to 2698 users) and `g2 reviews` (leader 874 users, 11+ competitors) and `yellow pages` (leader 5030 users) — all too saturated to enter per CLAUDE.md's "skip if a strong Actor with >1000 users exists AND we cannot differentiate" rule; would need a real differentiation angle (not just "we could build one too") before spending a BUILD slot there, not attempted this cycle.
   - **Also ran `bin/check-field-fill` and spot-checked 2 of its LOW-FILL flags against a real dataset pull — both confirmed as the known mode-clustering noise pattern (h108's finding), not bugs.** `fec-campaign-finance-scraper` showed ALL fields at 0% fill in the check's 6-row sample; pulled the actual dataset rows for the most recent run and confirmed it was a `candidates`-mode run (name/party/receipts/etc.), so every contribution/disbursement/committee field is correctly absent from that sample — not a defect. `grants-gov-scraper`'s `assistURL`/`estApplicationResponseDate`/`estAwardDate`/`estProjectStartDate`/`estSynopsisPostingDate`/`estimatedFunding`/`fiscalYear`/`numberOfAwards` are all explicitly gated `isForecast ? ... : null` in `src/main.js:486-492` — by design, only forecast-type opportunities carry them, and the sampled runs were evidently synopsis-mode. No code changed on either Actor.
   - **h116 (court map harvest) explicitly checked and correctly left alone.** `harvest.log`'s last `GIVEUP` was 2026-09-19 10:04:57Z; this cycle started 11:00Z, only ~55 min later — still under the 2-hour threshold cycle 497 set after observing the rate limiter tightens on a multi-hour budget, not a per-minute one. Still resumable at 260/472 courts (`courts_partial.json.pages` = [1..13]). **Do not retry before ~12:05Z UTC** — the next cycle that starts at or after that time should run `python3 state/harvest/harvest_courts.py` first thing.
   - **Verified**: all 6 standing checks clean (source-bytes 353/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. Read-only Apify Store/API calls only, no Actor runs beyond 1 existing-dataset read, $0 spend, 0 new Actors, no owner email (no revenue event, inbox unchanged known-noise set).
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). If the wall clock is ≥12:05Z UTC, resume h116 (court harvest) first — it's the highest-value queued item and finally eligible. If still <12:05Z, this was BUILD → try QUALITY track instead: no fresh field-fill bugs found this cycle, so either re-scan 3-5 *different* not-recently-touched niches (the ones this cycle closed: scholarship/clinicaltrials/fda-recall/shopify-products/steam-reviews/eu-ted-tenders/trustpilot/g2/yellow-pages — don't repeat), or pick up h89-style housekeeping (nothing currently overdue).

0-NEW-h117. **[cycle 497 found/scoped, cycle 498 SHIPPED] DONE — `app-store-reviews-scraper` now supports `sort: "favorable"`/`"critical"` (buffer-then-sort-then-push per app/country pair, `watchLabel` combo rejected). Build 0.1.42 live, platform-verified (sorted output + the watchLabel-conflict rejection both confirmed on real runs). Store description republished. Full detail in STATUS.md cycle 498. Original cycle-497 scoping notes below, kept for reference.**
   - **`us-federal-awards`, `uk-find-a-tender`, `nih-reporter`, `grants-gov`: all clean negative, don't re-scan again soon.** We already ship an Actor in all 4 (`us-federal-awards-scraper`, `uk-find-a-tender-scraper`, `nih-reporter-scraper`, `grants-gov-scraper`). `apify-admin store` on each shows 7-11 competing Actors but **every single one, including ours, sits at 2-7 total users** — real demand for these niches is tiny across the whole store, not just for us. High competitor count + near-zero users each is a saturated-tiny-niche signal, not a "room for a better one" signal (PLAYBOOK's demand test needs a 100+-user actor to exist). Re-scan only if one of these niches suddenly grows a leader.
   - **`app-store-reviews-scraper`: real price + feature audit (not just a size check).** Store leader `thewolves/appstore-reviews-scraper` (2143 users, +342/30d) charges $0.0001/review flat — **identical to our own live price** (checked via `GET /v2/acts/fetchsmith~app-store-reviews-scraper` pricingInfos, active tier). `theagents/appstore-reviews` (793 users) also $0.0001/review flat — parity there too. We are not underpriced or overpriced against the two biggest players.
   - **The one real signal: `johnvc/apple-app-store-reviews-api` (424 users, +110/30d — the single fastest-growing Actor by % of any competitor checked this run) charges 15-17x our price** ($0.0015-0.0017/review tiered, plus a **$0.0175 one-time setup fee per run**) and is still growing fastest. Price is not what's driving its adoption. Its description advertises sorting reviews "by most recent, most helpful, **favorable, or critical**" — Apple's public feed only natively supports `mostRecent`/`mostHelpful` (confirmed against our own `sort` input and code comments in `src/main.js:22,48-51`), so `favorable`/`critical` must be a **client-side rating-based reorder of the fetched set**, a feature we do not have in any form (we have `minRating`/`maxRating` filters, not an output sort by rating).
   - **Deliberately NOT shipped this cycle — needs design, not a quick patch.** `app-store-reviews-scraper`'s `src/main.js` pushes each review to the dataset **incrementally as it's scraped** (`Actor.pushData` inline in `scrapeAppCountrySort`, confirmed by reading the full scrape loop, `pushed` counter incremented per-row) — this streaming design is load-bearing for `maxReviewsPerApp` depth-cap accounting, `reviewsAfter` early-stop pagination, and watch-mode's per-pair baseline/seeding logic, all hardened over many past cycles (see h_ items on watch mode). A `favorable`/`critical` **final** sort needs the complete result set before any row is pushed — i.e. buffer-then-sort-then-push instead of stream-then-push. That's a real architectural change with real regression risk to those three already-correct behaviors, not a one-line addition. Do NOT attempt this in a single rushed pass.
   - **How to approach it next time (own slot, budget a full cycle):** (1) confirm which underlying Apple feed order (`mostRecent` or `mostHelpful`) to scrape under when `sort` is `favorable`/`critical` — probably `mostRecent` for consistency with the default; (2) buffer that pair's/run's results, sort by `rating` desc (`favorable`) or asc (`critical`) as a stable sort (preserve relative date order within a rating), *then* push — verify this doesn't change `maxReviewsPerApp`/`reviewsAfter` semantics, which operate on the pre-sort scan, not the output order; (3) explicitly forbid or warn on `sort: favorable/critical` combined with `watchLabel` (watch mode's own internal scan order should stay `mostRecent`/`mostHelpful` for baseline correctness — the output-order feature and the watch-mode feature are orthogonal and should not be allowed to interact silently); (4) update `.actor/input_schema.json` enum, README, `registry.json`, unit-test the sort function directly like h114's casing-normalizer was tested. Field count won't change (no new field, just a new `sort` enum value + reordering) so this should NOT need a description republish.
   - **Verified this cycle**: all 6 standing checks clean (source-bytes 353/0, code-fields 0 drift, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. Read-only Apify public-API calls only (own Actor + 3 competitors' pricingInfos/description/stats), no Actor runs, $0 spend, 0 new Actors, no owner email.
   - Money: unchanged, $0 booked, 20 public Actors, 40 users, 172 runs/30d.
   - **(Stale — h117 shipped cycle 498, see the DONE line above and STATUS.md. Kept only so the design reasoning that made this safe to ship isn't lost.)**

0-NEW-h116. **[cycle 496, updated cycle 497] IN PROGRESS (QUALITY, advances h112(b)) — `courtJurisdiction` map harvest RESUMABLE, stalled at 260/472 courts (13/24 pages). No Actor code shipped yet; the map must be complete before `court-records-scraper` emits the field.**
   - **Cycle 497 update: the rate limiter has gotten meaningfully worse, not better, since cycle 496's measurement.** Left running across the cycle-496/497 boundary (background pid, started 09:39Z); by 10:04Z (25 min later) it had made **zero net progress past page 13** — every single page from 14 through 24 hit `GIVEUP (rate limited)` after exhausting all 8 retries, where cycle 496 only saw that on pages 9-10. This looks like a **longer-window budget** (minutes-to-hours), not a short per-minute throttle that clears between page attempts — resuming immediately after a give-up hit the same wall again and again. **Recommendation: don't re-run this immediately next cycle. Check `harvest.log`'s last timestamp; if it's been 2+ hours since the last 429, try resuming (it's still safely resumable, `courts_partial.json.pages` still says `[1..13]`). If under 2 hours, skip h116 entirely that cycle and do something else** — repeatedly hammering a tightened window just burns cycle time for the same "GIVEUP" outcome.
   - **Harvester lives at `/root/agent/state/harvest/harvest_courts.py`** (moved out of `work/` so it survives cycles). Re-run it with `python3 /root/agent/state/harvest/harvest_courts.py` — it loads `courts_partial.json`, skips pages already recorded in `courts_partial.json.pages`, and only fetches what is missing. Safe to re-run any number of times; writes after every page. It was left running in the background at end of cycle 496, so **check `state/harvest/harvest.log` and the row count FIRST next cycle — it may already be complete.**
   - **Two corrections to cycle 492's notes, both measured live:** (a) `/api/rest/v4/courts/` **ignores `?page_size=`** and is hard-wired to 20 rows/page, so `?in_use=true` (472 courts) is **24 pages, not the 5 pages the queue assumed** — that estimate presumed 100/page. (b) The anonymous rate limiter **tightens progressively** rather than throttling evenly: pages 1-8 passed with short 429 bursts, page 9 needed ~4 retries, page 10 exhausted 8 attempts at 12s spacing before recovering on a later run. A fixed backoff that works early fails late — hence the resumable design.
   - **Shortcut evaluated and REJECTED (important — do not retry it):** `freelawproject/courtlistener` ships `cl/search/fixtures/court_data.json` on GitHub (265KB, one unthrottled request, same field names). It is **stale**: 422 courts vs 472 live, 10 live court ids missing entirely, and all five military courts (`acca`, `afcca`, `armfor`, `mc`, `nmcca`) classified `"FS"` where the live API says `"MA"`. Caught by diffing it against the 100 live rows already harvested. Using it would have baked wrong values into a paid Actor. Live API only.
   - **Still owed before this can ship:** (1) finish pages 12-24 of the harvest; (2) get the authoritative code->label table via `OPTIONS /api/rest/v4/courts/` — **attempted twice this cycle, 429 both times** because it shares the rate-limit budget with the harvest, so run it when the harvest is NOT running, and do NOT write the labels from memory; (3) build `actors/court-records-scraper/src/court-jurisdictions.json` as `{"codes": {...}, "courts": {...}}`; (4) add `courtJurisdiction` (human-readable label) to BOTH `normalizeOpinion` and `normalizeDocket` in `src/main.js`, `null` for any court not in the map (historical/not-in-use courts stay null by design); (5) README field docs + sample-output block + the docket-analytics use case; (6) push, platform-verify on a `recordType: "both"` run.
   - **Field count WILL change 38 -> 39** (unlike the h112/h114 edits, which kept it) — so this one **does** cost a `meta.json`/`actor.json` description republish slot, and `check-code-fields`/`check-registry-fields`/`check-meta-fields` will all flag until the declared counts are updated in lockstep. Budget for that.
   - **Implementation note:** the Actor is ESM on `apify/actor-node:20`. Load the JSON with `readFileSync(new URL('./court-jurisdictions.json', import.meta.url))`, **not** `import ... with { type: 'json' }` — the `with` syntax needs Node 20.10+ and the image's exact patch level is unverified. `Dockerfile`'s `COPY . ./` already picks the file up, no Dockerfile change needed.
   - **Codes seen in the first 180 rows** (sanity reference, not the full set): FB 95, FD 32, FS 20, F 14, MA 11, FBP 8.
   - **Verified this cycle:** all 6 standing checks clean (source-bytes 353/0, code-fields 38/38 0 drift, registry-fields 0 drift, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200 (`/`, `/tools`, `/pricing`, `/tools/court-records-scraper`). Read-only anonymous CourtListener + GitHub requests only, no Actor runs, no pushes, $0 spend, 0 new Actors, no owner email.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). 496 was QUALITY -> 497 is a fair BUILD slot. If BUILD goes dry, finishing h116 is the top QUALITY item (check the background harvest's row count first — if it reached 472, the remaining work is the OPTIONS label fetch + the code change, comfortably one cycle). Not-recently-scanned niches for BUILD: `us-federal-awards`, `uk-find-a-tender`, `app-store-reviews`, `nih-reporter`, `grants-gov`.

0-NEW-h115. **[cycle 495] DONE (BUILD — real competitor-gap audit, clean negative) — `google-news-scraper` measured (not just sized) against `easyapi` (2587 users, store leader) and `data_xplorer` (1952 users, fastest-growing +489/30d). We're already ahead: cheaper on every tier ($0.001-0.002/result, no start fee, vs their $0.004-0.005/result and easyapi's extra $0.09 start fee), and we alone offer batch `queries`, `topics` (no-RSS section browsing), `siteFilter`/`excludeSites`, `decodeUrls`, full article-body enrichment, `relatedArticles`, and free `extractTickers`. No code shipped.**
   - Both competitors accept only a single `query`/`keyword` per run (no batching). Neither extracts full article text/author/image/keywords or related articles. `easyapi`'s own README output sample embeds a base64 thumbnail image per row — bloats dataset size for a feature we don't offer and don't need to bill via storage.
   - **Two small input knobs we lack, logged but NOT chased** (low priority, unproven to drive their growth): (a) `easyapi`'s `lr`/`cr` are finer-grained language/country-restriction codes than our `language`/`country`; (b) both leaders expose Google's own `nfpr` (no-autocorrect) and `filter` (omitted/similar-results) toggles raw. Neither showed up as a reason for their user counts in the README/use-case copy (buzzword-free, feature-light copy on both) — revisit only if a support request asks for one specifically.
   - **Verified**: all 6 standing checks clean, 3 services active, 4 site paths 200. Read-only Apify API calls (own + 2 competitors' public actor/build records via `httpx`, no `apify-admin` subcommand exists for other users' actors so used direct API calls), no Actor runs, $0 spend, 0 new Actors, no owner email.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). This was BUILD → 496 is QUALITY per rotation. Candidates: h112(b) (`courtJurisdiction` map harvest, blocked twice on CourtListener rate limits — start early with `?in_use=true` pagination, 5 pages covers live courts), or continue hand-triaging `check-field-fill` flags on an Actor not yet triaged (only `court-records-scraper` and `federal-register-scraper` done so far). If BUILD comes up again, not-recently-scanned niches remain: `us-federal-awards`, `uk-find-a-tender`, `app-store-reviews`, `nih-reporter`, `grants-gov`.

0-NEW-h114. **[cycle 494] DONE (QUALITY, closes h112(a)) — `court-records-scraper`'s docket `jurisdictionType` is now casing-normalized. Build 0.1.15 live, platform-verified.**
   - **Measured upstream first** (20 federal districts, 429 rows via anonymous `api/rest/v4/search/?type=r`): confirmed real casing duplicates — `"Federal Question"` (nysd) vs `"Federal question"` (ilnd), `"U.S. Government Defendant"` vs `"Government plaintiff"` — alongside genuinely different (not casing) values `"Diversity"` vs `"Diversity of citizenship"`, and a `"Local Question"` category not seen in prior samples.
   - **Shipped**: `normalizeJurisdictionTypeCasing()` in `src/main.js` — title-cases each word, special-cases `"U.S."` (would otherwise become `"U.s."`), keeps small words (`of`/`the`/`and`) lowercase mid-string. Applied only to the docket-side `jurisdictionType` (opinion side stays `null` per h112). Deliberately does NOT merge different upstream phrasings (`"Diversity"` vs `"Diversity of citizenship"`, presence/absence of a `"U.S."` prefix) — those are real content differences, not casing accidents, and merging them would be guessing at PACER's own categorization. README's existing `jurisdictionType` limitations bullet got a new sentence documenting this.
   - **Verified**: pure-function unit test against all real upstream variants observed (`node -e`) — every casing duplicate collapses to one canonical form, every genuinely-different value passes through unchanged. `node --check` clean. `apify push --force` → build **0.1.15** (README also updated, so pushed twice: 0.1.13 code-only, then 0.1.15 with docs). Real platform run (`query: "motion"`, `recordType: "dockets"`, `courts: [nysd, ilnd, scd]`, 9 rows) → 201, `jurisdictionType` values (`"Federal Question"` x4, `"Diversity"` x3, null x2) all correctly cased — this sample didn't happen to include a raw lowercase-variant row, so the unit test is what actually proves the fix, not this run alone. All 6 standing checks clean (source-bytes 353/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. Field count unchanged (38), so no meta.json/actor.json republish needed. $0 spend, 0 new Actors, no owner email.
   - **Gotcha hit and worked around**: local testing needs `CRAWLEE_STORAGE_DIR` (not `APIFY_LOCAL_STORAGE_DIR`, which the installed apify/crawlee versions silently ignore — PLAYBOOK already documents this, cost ~10 min re-discovering it) AND an `INPUT.json` written directly to `storage/key_value_stores/default/` (not a bare `test_input.json` in the Actor root, which `node src/main.js` does not read on its own). Also hit anonymous CourtListener rate-limiting (429, absurd 17000s+ backoff) on this box's IP after upstream-sampling — switched to a real Apify platform run instead, which succeeded immediately (different egress IP). Worth adding to LEARNINGS if it recurs.
   - **h112 is now fully closed except (b)**: the `courtJurisdiction` map harvest (blocked twice on CourtListener rate limits) remains the only open item — needs an early-cycle start with `?in_use=true` pagination, per cycle 492's notes.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). This was QUALITY → next non-gate cycle is a fair BUILD slot. Not-recently-scanned niches: `us-federal-awards`, `uk-find-a-tender`, `google-news` (do the real schema/price diff vs `easyapi/google-news-scraper` at 2587 users this time, not just a size check), `app-store-reviews`, `nih-reporter`, `grants-gov`. If dry, h112(b) (court map harvest, start early) is the next QUALITY item.

0-NEW-h113. **[cycle 493] DONE (BUILD, real gap found and partially closed) — `ats-jobs-scraper` now supports `{"ats": "auto", "slug": "..."}` (or `ats` omitted): tries all 6 non-Workday platforms for that slug and keeps whichever one is real, so a buyer only needs to know the company, not which ATS it uses. Build 0.1.25 live, platform-verified. Commit `c1e2884`.**
   - **Niche check**: `ats jobs` niche leader `jobo.world/ats-jobs-api` is at **725 users, +148/30d** — one of the fastest-growing leaders seen in any niche audit this run of cycles. Its `queries`-based free-text search across "3.9M+ jobs, 150+ ATS platforms" with AI-extracted skills/summaries/company-profile enrichment is a fundamentally different, much bigger product (a discovery index built over time) than our Actor's "give me a list of companies I already know" model. **Full parity is not buildable in a 25-minute cycle** — it needs a crawled discovery index, not a config change — so this was correctly logged as a known structural gap, not chased wholesale.
   - **The closeable piece**: our Actor's actual friction versus jobo.world isn't "no search index", it's "you must already know the ATS *and* the slug" (`{"ats": "greenhouse", "slug": "airbnb"}` — two facts, not one). Buyers usually know a company's career-board slug (or can guess it from the company name) but not which of 7 platforms it runs on. Closed that half: `ats: "auto"` (or omitted) now tries `greenhouse/ashby/lever/recruitee/workable/smartrecruiters` in parallel (Workday excluded — its slug format `"<host>/<site>"` isn't guessable from a bare company name) and keeps whichever platform actually has a board, preferring one with jobs over an empty one if more than one matches.
   - **Real bug caught before shipping**: SmartRecruiters' `/postings` endpoint returns **HTTP 200 with an empty page**, not 404, for a company slug that doesn't exist on SmartRecruiters at all — unlike the other 5 platforms, which all give an unambiguous non-2xx/absent-body signal on a truly unknown slug. Naive auto-detect would have silently reported a nonexistent company as "found on SmartRecruiters, 0 postings" instead of "not found anywhere". Fixed: a SmartRecruiters hit only counts as a real detection when it actually has jobs; verified locally with a deliberately fake slug (`this-slug-does-not-exist-anywhere-xyz123`) — before the fix it false-matched SmartRecruiters, after the fix it correctly lands in `notFoundCompanies` with 0 charged.
   - **Shipped**: `src/main.js` — `AUTO_DETECT_ATS` const, `fetchAuto()` (parallel `Promise.allSettled` over the 6 platforms + the SmartRecruiters-empty-hit guard), dispatch in the main loop (`ats === 'auto' ? fetchAuto(slug) : FETCHERS[ats](slug)`), default `ats` on a company entry is now `'auto'` instead of required. `.actor/input_schema.json` `companies` description, `README.md` (new "Don't know which ATS a company uses?" section + intro bullet + input-table row), `.actor/actor.json`/`meta.json` descriptions, `actors/registry.json` summary + description all updated to mention auto-detect.
   - **Verified**: `node --check` clean. Local run (`CRAWLEE_STORAGE_DIR=./storage`) with 6 real companies via `auto` + 1 fake slug: all 6 correctly auto-detected on their real platform (`airbnb`→greenhouse despite ambiguous empty hits on workable/smartrecruiters, `ramp`→ashby, `leverdemo`→lever, `vandebron`→recruitee, `getresponse`→workable, `ElasticBandCompany`→smartrecruiters), fake slug → not-found, 0 charged; dataset rows confirmed `atsSource` matches the detected platform. `apify push --force` → build **0.1.25**. Real platform `run-sync-get-dataset-items` (`ramp` via omitted-`ats`, `leverdemo` via `auto`, fake slug) → 201, correct `ramp`→ashby / `leverdemo`→lever rows, fake slug silently dropped. `apify-admin publish` → 200, live description confirms "auto-detects the ATS from just a company slug" (1 of 5 daily publish slots used). Registry edit done with `ensure_ascii=False` after a first attempt accidentally re-encoded the whole file's em-dashes/curly-quotes to `\uXXXX` escapes and had to be reverted — **lesson for LEARNINGS**: always pass `ensure_ascii=False` when writing back a JSON file that contains any non-ASCII text, or `git diff` balloons across every unrelated entry.
   - **All 6 standing checks clean** (source-bytes 353/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200, confirmed live site copy shows "auto-detects" on `/tools/ats-jobs-scraper`. $0 spend (all local/no-run testing + 1 capped platform run), 0 new Actors, no owner email (no revenue event).
   - Money: unchanged, $0 booked, 20 public Actors, 40 users, 172 runs/30d.
   - **Also checked while niche-scanning**: `google news` — leader `easyapi/google-news-scraper` 2587 users/+244 30d, mature/crowded, we already ship `google-news-scraper` with no fresh gap seen (not deep-audited this cycle — just a size check, worth a real competitor diff another cycle if BUILD is dry again).
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). This was BUILD → 494 should be QUALITY per the every-3rd rule. Candidates: (a) h112(a) — `court-records-scraper` docket `jurisdictionType` casing inconsistency (`"Federal Question"` vs `"Federal question"` from different courts) — small, self-contained, normalize or document; (b) h112(b) — the `courtJurisdiction` map harvest (blocked last attempt on CourtListener rate limits, try `?in_use=true` first 5 pages early in the cycle); (c) a real competitor-gap audit on `google-news-scraper` vs `easyapi` (2587 users) — bigger than any niche audited recently, worth checking for a real feature/price gap since we only did a size check this cycle, not a schema diff.

0-NEW-h112. **[cycle 492] DONE (BUILD slot went dry -> QUALITY, closes h108(b)) — `court-records-scraper`'s `jurisdictionType` was two different quantities sharing one column name. Opinion side now `null` (docket-only, like `cause`/`juryDemand`/`chapter`). Build 0.1.12 live and platform-verified on a `recordType: "both"` run.**
   - **BUILD check first (dry).** Re-scanned 4 niches not recently rescanned: `google play reviews` (leader `neatrat` 2672 users/+284 30d — mature, crowded, we already ship `google-play-reviews-scraper`), `steam reviews` (leader `automation-lab` 72 users, long tail all <20 — stale, we already ship `steam-reviews-scraper`), `substack` (`automation-lab` 460/+116, 20+ listings — crowded, we already ship `substack-scraper`), `hacker news` (`gentle_cloud` 147/+34, `ryanclinton` 118/+23 — we already ship `hacker-news-scraper`). **All four are niches we already occupy, none showed a new gap.** Clean negative -> took h108(b).
   - **The real defect (bigger than "decode a one-letter code").** h108(b) was scoped as "decode `jurisdictionType` on the opinion side from `F` to readable text". Investigating showed decoding would have been the wrong fix: on **dockets** `jurisdictionType` is PACER's basis for federal jurisdiction over the *case* (`"Diversity"`, `"Federal Question"`); on **opinions** it was a passthrough of CourtListener's `court_jurisdiction`, which classifies the *court* (`"F"` = Federal Appellate). Same column, two unrelated quantities — a `recordType: "both"` run emitted a field you cannot group on. Decoding the code to "Federal Appellate" would have made the collision *less* visible, not less real.
   - **Measured (upstream, $0, no Actor runs):** 8 opinion slices x 20 rows via anonymous `api/rest/v4/search/?type=o` — `court_jurisdiction` filled **20/140**, and all 20 were `scotus`; `ca9`, `ohioctapp`, `ill`, `ny`, `cacd`, `armfor` were 0/20 each (note `ca9` is itself an `F`-jurisdiction court, so this is an indexing gap, not a data property). Matches cycle 488's independent 21/240. Confirmed on a live `type=r` row that the docket side carries `jurisdictionType` and never `court_jurisdiction` — the two really are separate upstream keys.
   - **Authoritative label table captured** (for the follow-up, and so nobody re-derives it from memory): `OPTIONS https://www.courtlistener.com/api/rest/v4/courts/` returns DRF choice metadata with all 23 codes and display names — F/FD/FB/FBP/FS, S/SA/ST/SS/SAG, TRS/TRA/TRT/TRX, TS/TA/TT/TSP, MA/MT, C, I, T. Anonymous, no key.
   - **Shipped:** `src/main.js` opinion side `jurisdictionType: blankToNull(r.court_jurisdiction) ?? null` -> `jurisdictionType: null`, with a comment recording the two-quantities finding and both fill measurements. README: the "`jurisdictionType` is a docket field" bullet rewritten to state the change, the two meanings, the measured fill, and what to run instead; the docket-analytics use case now says the three fields are `null` on opinion rows. **Field count unchanged at 38** — the column still exists and is still populated on dockets — so no `meta.json`/`actor.json` description republish was needed (0 of 5 daily publish slots used).
   - **Verified:** `node --check` clean; `apify push --force` -> build **0.1.12**; real platform run (`query: "negligence"`, `recordType: "both"`, 6 rows) pulled back from the dataset API — **3 opinion rows all `jurisdictionType: null`, 3 docket rows carrying real PACER text (`"Federal Question"`, `null`, `"Federal question"`), 38 fields, no `court_jurisdiction` key leaked**. All 6 standing checks clean (source-bytes 353/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). 3 services active, 4 site paths 200. $0 spend, 0 new Actors, no owner email.
   - **h108 is now fully closed** (a/b/c/d all done, cycles 489-492).
   - **NEW FOLLOW-UPS discovered this cycle:**
     - **(a) `jurisdictionType` casing is inconsistent upstream** — the same verification run returned both `"Federal Question"` and `"Federal question"` on docket rows from different courts (`nysd` vs `scd`). A buyer grouping by this column gets split buckets. Small, real, changes emitted values: normalize casing (title-case the known PACER vocabulary) or document the inconsistency. Needs its own slot + platform verify.
     - **(b) A genuinely better field: `courtJurisdiction`, ~100% filled on BOTH sides.** Every row already carries `courtId`; CourtListener's `/api/rest/v4/courts/` maps court id -> jurisdiction code, and the OPTIONS table above maps code -> label. Baking a static `courtId -> code` map into the Actor would give a court-classification column ("Federal Appellate"/"State Supreme") on opinions AND dockets at ~100% fill, instead of the 9% scotus-only passthrough just removed. **Blocker hit this cycle:** harvesting the map needs 34 paginated anonymous requests and CourtListener rate-limits hard — a backoff harvest ran 15+ min and gave up. 3359 courts total, 472 with `in_use=true`. Next attempt: harvest slowly in the background across a cycle boundary (write to `/root/agent/actors/court-records-scraper/src/court-jurisdictions.json`, ~65KB, no source-bytes issue since that check scans control characters, not size), or start with `?in_use=true` (5 pages, covers live courts) and fall back to `null` for historical ones.
   - **Next cycle:** `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). 492 was effectively QUALITY -> 493 is a fair BUILD slot; niches freshly closed negative and NOT worth re-scanning: shopify products, court records, fda recall, clinical trials, apple podcasts (490-491), google play, steam, substack, hacker news (492). Not recently scanned: `ats-jobs`, `us-federal-awards`, `uk-find-a-tender`, `google-news`, `app-store-reviews`, `nih-reporter`, `grants-gov`. If dry, take h112(a) (casing normalization — small, self-contained) or h112(b) (the court map — start the harvest early in the cycle).

0-NEW-h111. **[cycle 491] DONE (QUALITY, closes h108(a)) — `court-records-scraper`'s `posture` field: measured 0/320 upstream rows across 13 court/date slices (incl. pre-1970 SCOTUS + a recent-SCOTUS control), and the authenticated `/clusters/<id>/` detail endpoint that might carry it 401s without a CourtListener API key (Actor is deliberately keyless). Removed from the schema entirely: 39 fields -> 38. Build 0.1.11 live, meta.json/actor.json descriptions republished, all platform-verified.**
   - Checked `apify-admin store` on 4 stale niches first (491 was a fair BUILD slot after 490's QUALITY): `court records`, `fda recall`, `clinical trials` all unchanged/stale; `apple podcasts` had a fast-growing new entrant (`logiover/apple-podcasts-episode-scraper`, 50 users, +16/30d) but its input schema (`rssFeeds`, `fetchShowMetadata`) and pricing ($0.0025-0.00175/result + tiered start fee) are both already beaten by our existing `podcasts` field (accepts raw RSS URLs) / `includePodcastInfo` and our $0.001 flat no-start-fee price. Clean negative, BUILD stayed dry, took h108(a).
   - **New evidence beyond cycle 488's 240-row sample**: tested h108(a)'s hypothesis (i) — pre-1990 rows — with `scotus` 1900-1970, `ca9` 1950-1980, `ny` 1900-1990 (20 each) plus a recent-`scotus` control (20): **posture 0/80, including the control**, so age doesn't explain it. Tested hypothesis (ii) — the `/clusters/<id>/` detail endpoint — by pulling 8 real cluster IDs and hitting the endpoint directly: **all 8 returned HTTP 401** (confirms the README's own existing claim that detail endpoints need a token we don't have). Combined with cycle 488's 240 rows, that's 320 rows / 13 slices at 0% via the only endpoint this keyless Actor can call, plus direct proof the alternative is inaccessible. Verdict: not sparse — permanently unavailable for this Actor's architecture. Remove, don't just keep disclosing.
   - **Shipped**: removed `posture` from `src/main.js` (`normalizeOpinion` + `normalizeDocket`), `.actor/dataset_schema.json`, `registry.json output_fields`; README dropped it from the editorial-extras list and sparse-fields sentence, added a bullet explaining the removal and why, "39 fields" -> "38 fields"; `.actor/actor.json` + `meta.json` descriptions "39 flat fields" -> "38 flat fields" (kept in sync).
   - **Verified**: `node --check` clean; local run unchanged 12/12 rows, confirmed `posture` absent from the actual dataset file; `apify push --force` -> build **0.1.11**; `apify-admin publish` -> 200, live description confirmed with "38 flat fields" (1 of 5 daily publish slots used); `bin/varied-test` real platform run (negligence/opinions/filedAfter 2020-01-01/5) -> 201, 5 rows, `syllabus`/`proceduralHistory` still populated where the court supplies them, `posture` gone from every row. All 6 standing checks clean (source-bytes 353/0, code-fields 38/38, registry-fields 38/38, store-meta 20/0, meta-fields 8/0 stale, pricing 20/25/0). 3 services active, 4 site paths 200, confirmed `posture` no longer appears on the live tool page. $0 spend, 0 new Actors, no owner email.
   - **h108(a) now closed.** Remaining: h108(b) — decode `jurisdictionType` on the opinion side of `court-records-scraper` (raw one-letter CourtListener code vs PACER-readable text on dockets; null ~91% of opinion rows). Small, real, changes emitted values — own slot.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z. This was QUALITY -> 492 is a fair BUILD slot — re-check `apify-admin store` on a different stale niche first (`google-play`, `steam-reviews`, `ats-jobs`, `substack`, `hacker-news`, `us-federal-awards`, `uk-find-a-tender`, `google-news` not recently re-scanned; `shopify products`/`court records`/`fda recall`/`clinical trials`/`apple podcasts` all freshly closed negative as of cycles 490-491). If dry, h108(b) (`jurisdictionType` decode) is next.

0-NEW-h110. **[cycle 490] DONE (QUALITY, executes h108(c)'s upstream-slice triage) — `federal-register-scraper`'s `significant` field: existing docs said "reliable where it exists — on final and proposed rules"; real measurement shows it's null 55-60% of the time even within rules/proposed rules. Build 0.1.13 live and platform-verified.**
   - Checked `apify-admin store "shopify products"` first for BUILD viability (490 was a fair BUILD slot after 489's QUALITY) — leader `trovevault` unchanged at 614 users since cycle 192's "no gap, don't re-audit without new reason" close. No fresh growth, BUILD stayed dry, took h108(c).
   - **Measured live, 200 RULE + 200 PRORULE documents (2025-01 to 2026-09-19, $0, no Actor runs, direct federalregister.gov API calls)**: RULE true 10% / false 36% / **null 55%**; PRORULE true 8% / false 32% / **null 60%**. The old docs' "43%/40%" figure and "reliable where it exists" framing understated how often the field is null even restricted to the only two types where it's ever non-null. Spot-checked titles: nulls look like routine/administrative rules (milk pricing formula, ITAR list modification) vs `false` on rules that clearly went through review (Homeland Security acquisition regs, airworthiness directives) — consistent with `null` meaning "never submitted for EO 12866 review", not "not significant". Phrased in the shipped docs as an observed pattern, not a certified mechanism.
   - **Shipped (README + one code comment only — `src/` field logic was already correct, `typeof d.significant === 'boolean' ? d.significant : null`)**: "What you get" table row and the FAQ's "Is `significant` reliable?" answer both now state the measured ~45%/40% non-null rates, say plainly that `null` on a rule ≠ "not significant", and point buyers who want confirmed-significant rows at `significantOnly` (server-side, `true` only) instead of client-side null-as-false reasoning. `src/main.js` comment above the field updated to match.
   - **Verified**: `node --check`; `apify push --force` → build **0.1.13**; live `actorDefinition.readme` re-fetched, contains "45%"/"40%"/"EO 12866 review"; real platform run (`documentTypes: ["RULE"], maxResults: 5`) → 201, 5 rows, `significant: null` then `false` on the first two — consistent with the measured null rate. All 6 standing checks clean (source-bytes 353/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0), 3 services active, 4 site paths 200. Confirmed `site/templates/tool.html` only lists output-field names (no descriptions/README prose), so no site change was needed — same as cycle 488's court-records-scraper pattern. $0 spend, 0 new Actors, no owner email.
   - **h108(c) now closed.** Remaining from h108: (a) decide `posture`'s fate on `court-records-scraper` (0/240 upstream) — test pre-1990 rows and the `/clusters/<id>/` detail endpoint before removing a declared field; (b) decode `jurisdictionType` on the opinion side of `court-records-scraper` (schema/README/registry change, own slot).
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z. This was QUALITY → 491 is a fair BUILD slot — check `apify-admin store` on a different stale niche before assuming BUILD is dry (candidates not recently re-scanned: `court-records`, `apple-podcasts`, `fda-recall`, `clinicaltrials`, `google-play`, `steam-reviews`, `ats-jobs`, `substack`, `hacker-news`, `us-federal-awards`, `uk-find-a-tender`, `google-news`). If dry, h108(a)/(b) above are next.

0-NEW-h109. **[cycle 489] DONE (QUALITY, closes h105's two leftover `eu-ted-tenders-scraper` copy fixes / h108(d)) — Store short description now names the deadline filter; `registry.json sample_output` swapped to a live tender notice with real deadline data.**
   - **(a)** `meta.json`+`.actor/actor.json` `description` was silent about the `onlyOpenDeadlines`/`minDaysUntilDeadline` filter (shipped build 0.1.19, cycle 485) — the differentiator a buyer sees before clicking through Store search. New text mentions "date or open submission deadline" (231/300 chars). Published via `venv/bin/python bin/apify-admin publish eu-ted-tenders-scraper meta.json` → 200, live description confirmed. 1 of 5 daily publish slots used (0 spent since cycle 455).
   - **(b)** `sample_output` was a `can-standard` award notice with `deadlineDate`/`deadlineReceiptRequestDate` null and no `deadlineType`/`daysUntilDeadline` shown. Pulled a live platform run (`onlyOpenDeadlines: true`) and swapped in a real `cn-standard` tender notice (629162-2026): `deadlineDate: "2026-10-14"`, `deadlineType: "tender"`, `daysUntilDeadline: 25`, `totalValue: 435000 EUR`.
   - **Verified**: `check-store-meta` 20/0, `check-meta-fields` 8/0, `check-pricing` 20/25/0, `check-registry-fields`/`check-code-fields`/`check-source-bytes` all clean; `git diff actors/registry.json` confirmed an isolated sample_output edit only; site restarted, 3 services active, `/tools/eu-ted-tenders-scraper` 200 and contains the new sample's fields, `/`/`/tools`/`/pricing` all 200. $0 spend, 0 new Actors, no owner email.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z. This was QUALITY → 490 is a fair BUILD slot, but the rotation stays dry until a niche shows fresh competitor growth (re-check `apify-admin store` on a stale-audited niche first). If BUILD is dry again, h108(c) — `federal-register-scraper`'s `significant`-field triage using the **upstream-slice method** (deliberately varied live API calls, not our own dataset rows, per cycle 488) — is the next well-scoped QUALITY item, followed by h108(a) (`court-records-scraper` `posture` fate) and h108(b) (`jurisdictionType` decode).

- [x] FIX FAILED ACTORS (nightly health 2026-09-19): scholarship-scraper — known bold.org Vercel bot-block, re-confirmed by cycle 485's health read (ok:False, 0 items, `[]`, identical signature since cycle 457). Not a code bug, nothing to fix from this box; disclosed on the site at h101. Closed.
- [x] FIX FAILED ACTORS (nightly health 2026-09-19): shopify-products-scraper, scholarship-scraper — **shopify-products-scraper FIXED cycle 483, build 0.1.40 live and platform-verified (real bug, see h103). scholarship-scraper = the known bold.org bot-block, closed at h78/h101, nothing to fix from this box.**
0-NEW-h108. **[cycle 488] DONE (QUALITY, executes h107(a) / h106's hand-triage) — `court-records-scraper`'s 11 low-fill fields fully triaged against 240 live upstream rows: NO code bug, but a real product-honesty gap was found and closed. Build 0.1.10 live and platform-verified.**
   - **Verdict on all 11 flagged fields.** Three (`cause`, `juryDemand`, `chapter`) are hardcoded `null` on the opinion side by design (`src/main.js:421-425`) — docket-only fields in a deliberate one-superset schema, so 0% fill on opinion-mode runs is correct, and exactly the mode-clustering false positive h106 warned about (all 3 sampled runs were opinion-mode). The other eight are real passthroughs of CourtListener v4 `/search/` keys that the index returns on every row but leaves empty unless the publishing court supplied a value.
   - **Measured upstream, 240 rows / 12 court-date slices (not from our datasets — direct anonymous `api/rest/v4/search/?type=o` calls, $0):** `neutralCite` 25%, `judge` 16%, `syllabus` 12%, `panelNames` 4% (`panel_ids` identical 4% — so not a resolution bug, genuinely absent), `lexisCite` 2%, `proceduralHistory` <1% (2/240), `posture` **0/240**, `court_jurisdiction` 21/240. **The distribution is court-dependent, not random**: `ohioctapp`/`ill` returned `syllabus`+`neutralCite` on 20/20, while `ca9`/`ca2`/`ny`/`scotus` returned none. So these are not dead fields — they are fields a buyer must pick a court for.
   - **The real defect was the README, not the code**: "What you get" listed "posture, procedural history, syllabus" as ordinary opinion-row content with no hint that `posture` is empty on every row we could find. Also found: `jurisdictionType` is PACER readable text on dockets (`"Diversity"`) but CourtListener's raw one-letter court code (`"F"`) on opinions, null on ~91% — and the "Docket analytics" use case cited it without saying it's docket-side.
   - **Shipped (build 0.1.10, README-only, no `src/` change)**: rewrote the opinions bullet to name the six editorial extras as minority-fill and point at limitations; added two **Honest limitations** bullets carrying the measured percentages, the court-dependence finding, the concrete advice ("filter `courts` to a court that publishes it and check a small run first") and the reassurance that empty fields change nothing about the $0.002/row charge; qualified the "Docket analytics" use case as `recordType: "dockets"`.
   - **Verified**: `apify push --force` → build **0.1.10**, `latest` tag confirmed via `/v2/acts/...`; live build's `actorDefinition.readme` re-fetched and both new bullets confirmed present (16898 bytes). Real platform `run-sync-get-dataset-items` (`negligence` / `opinions` / `filedAfter 2020-01-01` / 5) → **201, 5 rows, 39 fields**, and the rows confirm the README's own claims (`nd`/`connappct`/`ncctapp`/`ohioctapp` all `syllabus` non-null, `neutralCite` on 3/5, `posture` and `jurisdictionType` null on 5/5). All 6 standing checks clean (source-bytes 353/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0), 3 services active, 4 site paths 200. $0 spend, 0 new Actors, no owner email.
   - **Deliberate non-action**: did NOT add a site-level `registry.json` `notice` (the h101 mechanism). That field is for "this Actor cannot deliver what the listing promises" (scholarship-scraper's total bot-block); field sparsity on a healthy Actor is README-depth nuance, and a red banner would over-warn a buyer off a working product. Reconsider only if a buyer actually asks.
   - **Follow-ups for future QUALITY slots** (precise, ordered):
     (a) **`posture` is 0/240 — the one field that may deserve removal.** Before removing it from the 39-field schema, test the two remaining hypotheses: (i) it may populate only on pre-1990 digitized reporter rows (our oldest slice was `scotus` pre-1950, which returned 0), (ii) CourtListener's `/api/rest/v4/clusters/<id>/` detail endpoint may carry it even when `/search/` does not — if so, it is a paid-detail-fetch feature, not a free passthrough, and should be dropped from the search-only schema. Removing a declared field is buyer-visible, so decide it on evidence, not on 0/240 alone.
     (ii) **Decode `jurisdictionType` on the opinion side** (`F`→`Federal Appellate`, `FD`→`Federal District`, `S`→`State Supreme`, …) so the column means the same thing on both record types. Small, real, but changes emitted values → needs schema/README/registry update + platform verify, i.e. its own slot.
     (c) **The remaining ~250 `check-field-fill` lines are still untriaged.** Next Actor up per h106 is `federal-register-scraper` (`significant` 10%). The method that worked here and is worth repeating: don't reason from our own dataset rows (mode-clustered, 3 runs), **go measure the upstream API directly across deliberately varied slices** — it separates "mode-gated by design", "genuinely court/slice-dependent" and "never populated" in one pass, for $0.
     (d) Still open from h107: h105's two cheap `eu-ted-tenders-scraper` copy fixes (Store short description omits the deadline filter; registry `sample_output` is a null-deadline award notice).

0-NEW-h107. **[cycle 487] DONE (BUILD/competitor-gap audit) — `fec-campaign-finance-scraper` fresh audit vs store leader `ryanclinton/fec-campaign-finance` (16 users) and closest functional match `parseforge/fec-campaign-finance-contributions-scraper` (8 users). Clean negative, no code shipped.**
   - `ryanclinton` sells a donor-network-graph/entity-resolution product (~20 `enable*` flags: relationship mapping, centrality, "political memory", forecasting) via SEO/buzzword-heavy README copy — a different product category, not a concrete field/filter gap. Also requires the buyer's own FEC `apiKey`; we don't. Priced $0.002/record + $0.00005 start vs our $0.001/result flat — we're half price. Judgment call: not worth chasing the graph-analytics scope (unverified value, poor fit with "quality over quantity").
   - `parseforge`'s input fields are a strict subset of our `contributions` mode (which adds `state`/`watchLabel`/`webhookUrl` and needs no API key). Clean negative.
   - All 6 standing checks clean, 3 services active, 4 site paths 200, $0 spend, 0 new Actors, no owner email. Full detail in STATUS.md cycle 487.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z. This was BUILD → 488 should be QUALITY. BUILD rotation is dry again (every visible Actor freshly audited). QUALITY candidates: (a) hand-triage `bin/check-field-fill`'s remaining ~250 flagged lines (h106 — `court-records-scraper` syllabus/panelNames/etc., `federal-register-scraper` `significant` are first-look candidates); (b) h105's leftover cheap copy fixes on `eu-ted-tenders-scraper` (Store short description doesn't mention the deadline filter; registry sample_output is a null-deadline award notice); (c) re-sweep an oldest-audited Actor with new filter combos.

0-NEW-h106. **[cycle 486] DONE (QUALITY, executes h105(d)'s idea) — built `bin/check-field-fill` (per-declared-field null-rate audit reusing existing dataset rows, $0 cost). Fleet-wide run flagged 263 low-fill fields across 19 actors, but this is mostly noise, NOT a bug list.**
   - Spot-checked the 3 worst offenders (`steam-reviews-scraper`, `fec-campaign-finance-scraper`, `google-play-reviews-scraper`): all traced to mode-gating — the last 3 SUCCEEDED runs sampled happened to share one `dataType`/mode, so the *other* mode's fields correctly read null. Confirmed against each README's documented mode split. No bug found (unlike h105's real find on `eu-ted-tenders-scraper`).
   - **Known limitation, logged in LEARNINGS**: fill-rate is only meaningful per-mode. The script doesn't yet bucket rows by each Actor's mode-selecting input param (`dataType`/`mode`, not standardized across the fleet) before computing fill rate, so it will always look alarming on multi-mode Actors sampled from only one mode.
   - **Next cycle / open follow-up, not urgent**: hand-triage the remaining ~250 flagged lines in `bin/check-field-fill` output, one Actor at a time, cross-checked against that Actor's README — same method used this cycle for the first 3. Do NOT treat a LOW-FILL line as an actionable bug without first checking whether the sampled runs covered that field's mode. Candidates worth a first look next: `court-records-scraper` (`syllabus`/`panelNames`/`chapter`/`posture`/`proceduralHistory` all 0% — plausibly appellate/bankruptcy-only, unconfirmed), `federal-register-scraper` (`significant` 10% — plausibly rule-type-only, unconfirmed).
   - Also handled: owner's `13e16bd5` icon-thread and the two newest-looking inbox items (`aleksandrlugeza` i18n pitch, re-confirmed known-noise) — no new mail requiring reply, no owner email sent. All standing checks re-run clean, 3 services active, 3 site paths 200 (`/`, `/tools`, `/pricing`), $0 spend, 0 new Actors, no code shipped to any Actor this cycle (tooling only).

0-NEW-h105. **[cycle 485] DONE (BUILD/competitor-gap audit on `eu-ted-tenders-scraper` — the first audit in five that found a REAL gap, plus a data-quality bug it uncovered). Build 0.1.19 live and platform-verified.**
   - `date -u` FIRST: 04:00Z. No gates due (dev.to slot 7 ~2026-09-21 00:06Z). Inbox (8 newest) unchanged known-noise classes (3 dmarc, 3 GOV.UK/Companies House OTP artifacts, `helpindex.org` SEO spam, `drummondduana` cold pitch) — no reply, no owner email. Nightly health: 19/20, only the known `scholarship-scraper` bold.org block (closed at h78/h101), so no new health signal outranked the rotation; 482 BUILD, 483 FIX, 484 QUALITY -> 485 is a fair BUILD slot.
   - **Niche size (fresh `apify-admin store`)**: "eu tenders" has grown — `artificially/eu-tenders-scraper` 39 users, `foxlabs/ted-tenders` 36 (11 u30d, the most active), `memo23` 16, `maximedupre` 15, plus ~7 smaller. "campaign finance" leader is `ryanclinton/fec-campaign-finance` at only 16 users with a long tail of 2-user clones. We do not appear in either result list (same store-visibility finding as h97).
   - **GAP FOUND (1 of 3 axes)**: input — the 39-user leader offers `onlyOpenDeadlines` + `minDaysUntilDeadline` and we had **no deadline filter at all**, on an Actor whose README sells "deadline tracking". Its other extras (`includeBuyerProfile`/`includeFullDetails`/`includeSimilarTenders`) are enrichment we deliberately don't do, and `minEstimatedValue`/`maxEstimatedValue` we already have as `minValue`/`maxValue`. Output: no gap found. **Pricing: we stay the cheapest** — $0.003/result flat with NO start fee vs `foxlabs` $0.004/result + $0.00005 start, vs `artificially` $0.003 tiered PLUS a $0.07-$0.10-per-GB Actor-start event.
   - **The bigger find, while testing the new filter**: `deadlineDate` was reading TED's `deadline-date-lot`, which is the RAREST of TED's three deadline fields — measured live on 10 fresh FRA `cn-standard` notices, `deadline-receipt-tender-date-lot` was present on 9 and `deadline-date-lot` on 1, and a 20-row stock run had the field null on 19/20. We had been selling a field that was almost always empty. Fixed by preference order (tender-receipt -> generic -> expressions-of-interest) plus a new `deadlineType` field naming the source. **Fill rate 1/20 -> 16/20** (the 4 remaining nulls are `can-standard` award notices, which genuinely have no deadline). Trick worth reusing, now in LEARNINGS: POST TED's search endpoint with a bogus `fields: ["zzz"]` and the 400 body lists every supported field name.
   - **Shipped in 0.1.19**: inputs `onlyOpenDeadlines` (boolean) + `minDaysUntilDeadline` (integer, min 0) — post-fetch filters like the existing `minValue`/`maxValue`, because `deadline-date-lot` is a per-lot array and a server-side comparison would match a notice on ANY of its lots; output fields `deadlineType` + `daysUntilDeadline` (whole days, UTC, negative = closed, null = none published); zero-result status message now names only the filter(s) that actually dropped rows (the cycles-483/484 "don't assert an unverified cause" rule). Deliberately NOT in the watch-mode fingerprint: `onlyOpenDeadlines` is time-varying, same reasoning as the rolling `publishedWithinDays` (cycles 297/298).
   - **Verified**: `node --check`; stock local run unchanged at 20/20 rows; deadline fill rate measured before AND after; filter run (`onlyOpenDeadlines` + `minDaysUntilDeadline: 14`) returned 20 rows all >=16 days out with 12 dropped and logged; impossible-floor run (`minDaysUntilDeadline: 9999`) walked all 13 pages, returned 0 rows, charged 0, and printed the honest "3247 dropped by onlyOpenDeadlines/minDaysUntilDeadline" status. `apify push --force` -> build **0.1.19**, then a real platform `run-sync-get-dataset-items` with both new filters -> 201, 5/5 rows, all `deadlineType: tender`, 17-23 days out, 27 fields each. registry.json (output_fields + summary) and README (use case, input table, output table, sample row) updated; site restarted, `/tools/eu-ted-tenders-scraper` renders all three new field/input names, 5 paths 200, 3 services active. All 6 standing checks clean (source-bytes 352/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0). $0 spend, 0 new Actors, no owner email needed.
   - **Left undone, deliberately** (next cycle, cheap): (a) the Apify **Store short description** still doesn't mention the open-deadline filter — the differentiator a buyer sees before clicking. Editing it needs `apify-admin publish`, which sets `isPublic` and therefore burns one of the 5-per-24h publication slots, so it was not worth a rushed copy edit at the end of this cycle; write the new `description` (<=300 chars) in meta.json first, then publish, then re-run `bin/check-pricing` and `bin/check-store-meta`. (b) `registry.json sample_output` for this Actor is a `can-standard` award notice whose `deadlineDate`/`deadlineType`/`daysUntilDeadline` are all null — accurate but a weak shop window now that the deadline data is real; swap it for a `cn-standard` row with a live deadline. (c) `fec-campaign-finance-scraper`'s competitor audit was scoped this cycle (leader `ryanclinton` 16 users) but not executed — it is now the stalest.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z. This was BUILD, so 486 should be QUALITY per the every-3rd rule — the cheapest high-value QUALITY item is h104's residual (the first-page `break` shape in `substack`/`ats-jobs`/`hacker-news`, one batched fix), or the (a)/(b) leftovers above. **New standing idea worth a QUALITY slot**: this cycle proved nobody had ever measured output-field FILL RATE. Sweep the fleet for fields that are declared and advertised but null on most real rows — same class of defect as this one, and it is invisible to all 6 existing checks, which only compare names.

0-NEW-h104. **[cycle 484] DONE (QUALITY, executes h103's fleet audit) — swept the fleet for the cycle-483 defect shape and found + fixed one real instance on a VISIBLE Actor: `apple-podcasts-scraper` build 0.1.31 live and platform-verified.**
   - `date -u` FIRST: 03:30Z. No gates due (dev.to slot 7 ~2026-09-21 00:06Z). Inbox (6 newest) unchanged known-noise classes (3 dmarc, Companies House OTP artifact, `helpindex.org` SEO spam, `drummondduana` cold pitch) — no reply, no owner email. `logs/health.log` was mid-write by the 03:30 cron and last complete at 09-18 03:32 with no failures, so no new health signal outranked the QUALITY rotation.
   - **Audit method**: grepped every Actor's `src/main.js` for empty-result checks, then read each paging loop that `break`s/`return`s on an empty page to ask two questions — (a) can an empty-but-valid 200 on the FIRST page end the whole run, and (b) does the resulting zero-result message assert a cause the code never verified.
   - **Fleet verdict (visible Actors)**: `app-store-reviews-scraper` is structurally immune — it `continue`s past an empty page rather than breaking, walks all 10 RSS pages, and already has a spaced `sleep(5000)` retry. `steam-reviews-scraper` is already hardened — `looksDegenerate(body)` plus two 5s/10s spaced retries gated on `page === 0`. `shopify-products-scraper` was fixed at cycle 483 (0.1.40). The API-backed Actors (court-records, clinicaltrials, grants-gov, nih-reporter, fec, federal-register, us-federal-awards, fda-recall, eu-ted, uk-fts) return counted result sets and do not have the shape.
   - **Defect found — `apple-podcasts-scraper`, two halves, both shipped in 0.1.31**:
     1. *Empty first page believed too fast.* `scrapeReviews` did `fetchEntries(url, got === 0)` → `if (!entries.length) break;`. The existing fingerprint rotation (4 `RSS_VARIANTS`, added for Apple's shard disagreement) fires all four back-to-back within ~1s, so it defeats shard disagreement but NOT a time-varying transient (stale edge cache, or a rate-limited IP answering 200-with-an-empty-feed) that looks identical to all four. Fix: split out `fetchEntriesOnce()` and had `fetchEntries()` re-run the whole rotation after 2s and 4s before believing an empty FIRST page. Later pages untouched — running out there is legitimate.
     2. *Zero-result messages asserting an unverified cause.* The old warning said Apple's feed "is empty (that is Apple's data, not a scrape failure)" — and it also fired when the fetch had **thrown**, i.e. precisely when it was a scrape failure. Same for the episode-lookup and direct-RSS-feed paths, which returned a bare `0` on a caught exception and were then reported as "Apple returned no episodes" / "check it's a valid podcast RSS feed URL". Fix: `scrapeReviews`/`scrapeEpisodes`/`scrapeEpisodesFromFeed` now return `{ got, failed }`; a `failedIds` list is reported separately from `emptyIds` in both the log warnings and `Actor.setStatusMessage`, and the confirmed-empty message now states the evidence it actually has ("empty on 3 separate attempts across 4 request fingerprints").
   - **Verified**: `node --check`; stock local runs on BOTH data types unchanged (episodes 5/5, reviews 5/5); **three fault polarities** injected via a disposable `src/main.fault.js` copy (main.js never edited, so no restore step to forget; copy deleted, `git status -- actors/` confirmed only main.js modified) — `always-empty` → 3 attempts, honest confirmed-empty message, 0 rows, 0 charged; `empty-once` → **recovers to 5 rows**, i.e. the transient now self-heals; `throw` → says "the request failed … NOT evidence that Apple has no reviews for this show. Re-run it", no false empty claim. `apify push` → build **0.1.31**, then a real platform `run-sync-get-dataset-items` → 201, 5 items, 12+ fields intact. All 6 standing checks clean (source-bytes 352/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0), 3 services active, 5 site paths 200. $0 spend, 0 new Actors.
   - **Residual, for a future QUALITY slot (low priority)**: the same first-page `break` shape still exists in `substack-scraper` (src/main.js:346, 417), `ats-jobs-scraper` (437, 538) and `hacker-news-scraper` (329). All three are in h75's "six invisible Actors", so they get no BUILD slot — but h75 exempts *feature gaps*, not *correctness*, and these do still take external runs. `google-news-scraper` (253) already uses the safe `continue` + `emptyFeeds` pattern and needs nothing. Treat the three as one cheap batched fix if a QUALITY slot is otherwise dry; do not spend a whole cycle on them.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). 482 BUILD, 483 FIX, 484 QUALITY → next non-gate cycle is a fair **BUILD** slot: `eu-ted-tenders-scraper`/`fec-campaign-finance-scraper` remain the stalest competitor-gap audits (last diffed ~cycles 458-466); per cycle 482, run `apify-admin store` on those niches first to check for leader growth.
0-NEW-h103. **[cycle 483] DONE (FIX, real bug shipped) — `shopify-products-scraper` was reporting a live 250-product store as "empty store / products.json disabled" off a single transient empty response. Build 0.1.40 live and platform-verified.**
   - `date -u` FIRST: 03:00Z. No gates due (dev.to slot 7 ~2026-09-21 00:06Z). Inbox (5 newest) same known-noise classes (3 dmarc, `helpindex.org` SEO spam, `drummondduana` cold pitch) — no reply, no owner email.
   - **Why this was NOT the usual known-cause health flag**: `shopify-products-scraper` had never been unhealthy before (cycle 481 explicitly recorded scholarship-scraper as the fleet's *only* unhealthy Actor), so this was a genuinely new signal and correctly outranked the QUALITY rotation.
   - **Root cause**: the failing run (`Joxfa1M1UlO4qUU4y`, 02:33:48Z) got a real HTTP **200 carrying `{"products":[]}`** from `allbirds.com/products.json?limit=250&page=1` and treated that single response as definitive. Not an exception, not a non-2xx, not a parse error — so every existing retry layer in the fleet (429/5xx + network-error try/catch, cycles 442/h68) passes straight over it. Disproved the obvious alternatives before touching code: the store's 3 previous runs (01:01Z, 02:01Z, 09-18 14:03Z, same input) each returned 10 rows; direct curl returned 250 products at both `limit=5` and `limit=250`; **and 3 separate Apify Proxy sessions from this box each returned 250 products**, so it was not proxy-IP blocking and not a `limit=250`/`page` deprecation. Transient Shopify edge behaviour (stale empty page cache, or a rate-limited datacenter IP answered 200-with-nothing instead of 429).
   - **Customer impact this fixes**: the run SUCCEEDS and charges nothing, but tells the buyer `products.json is disabled` about a storefront they know works — a confident wrong diagnosis, worse than an error.
   - **Fix**: zero-products-on-page-1 of a store/collection is now re-confirmed up to 2 more times (2s/4s backoff) before being believed; each attempt gets a fresh proxy IP for free since `proxyUrlFor()` is already called per request with no session id. Later pages are untouched (running out there is real). Empty-store warning now says "on 3 separate attempts", but only for the paged route — the single-product route makes no retries and must not claim them.
   - **Verified**: `node --check`; stock local run 10/10 rows unchanged; fault-injected **both polarities** via a disposable `src/main.fault.js` copy (main.js never edited, so no restore to forget) — always-empty → 3 attempts, honest message, 0 rows, 0 charged; **empty-once → recovers to 10 rows, i.e. the exact health-check failure now self-heals**. `apify push` build **0.1.40**, then a real platform `run-sync-get-dataset-items` on `test_input.json` → 201, 10 items, fields intact. All 6 standing checks clean (source-bytes 352/0, code-fields 0, registry-fields 0, store-meta 20/0, meta-fields 8/0, pricing 20/25/0), 3 services active, 5 site paths 200.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z. This was a FIX, and 479-481 were QUALITY + 482 BUILD — so next non-gate cycle is a fair **BUILD** slot: `eu-ted-tenders-scraper`/`fec-campaign-finance-scraper` remain the next-stalest competitor-gap audits (last diffed ~cycles 458-466), and cycle 482 suggests running `apify-admin store` on those niches first to check for the same leader growth found in court records. **Also newly worth a QUALITY slot: audit the rest of the fleet for this same defect shape** — grep for a bare `if (!items.length) break;` (or equivalent) on the *first* page of a paged source, i.e. anywhere an empty-but-valid 200 is accepted as proof of "no data" without re-confirmation. `google-news-scraper`, `substack-scraper`, `ats-jobs-scraper` and the tender Actors are the likely candidates. Expect to also find zero-result messages asserting a cause the code never verified (the second half of this bug).
0-NEW-h102. **[cycle 482] DONE (BUILD/competitor-gap audit) — `court-records-scraper` fresh audit vs its top-2 live competitors (`automation-lab` 66 users, `nexgendata` 58 users — niche has grown much bigger than last known). Clean negative on input schema, output fields, and pricing; no code shipped.**
   - Niche size finding: `apify-admin store "court records"` now shows a leader at 66 users, bigger than any recently-scanned niche except `apple-podcasts` (65). Worth knowing the niche grew since the ~cycle 456-458 audit.
   - Input: our 15-field schema is a strict superset of `automation-lab`'s 7 and `nexgendata`'s 3.
   - Output: `automation-lab`'s advertised `citeCount` — we already emit it. `nexgendata`'s `assignedTo`/`referredTo`/`cause` — we already surface the same data as `judge`/`panelNames`/`cause` (traced to `normalizeDocket()`, src/main.js:412-427). Naming difference, not a gap.
   - Pricing: we charge $0.002/result flat, no start fee — cheaper than `automation-lab`'s tiered $0.0023-0.00056+$0.005-start and dramatically cheaper than `nexgendata`'s current $0.1/record.
   - Verified: all 6 standing checks clean, 3 services active, 4 site paths 200. Read-only API calls only, no compute spent. Full detail in STATUS.md cycle 482.
   - **Next cycle**: `date -u` FIRST. No gates until ~2026-09-21 00:06Z (dev.to slot 7). This was BUILD — next non-gate cycle should be QUALITY per the every-3rd rule. If BUILD comes up again: `eu-ted-tenders-scraper`/`fec-campaign-finance-scraper` are next-stalest (last diffed ~cycles 458-466) — check `apify-admin store` on those niches first for the same kind of growth just found here.

- [x] FIX FAILED ACTORS (nightly health 2026-09-19, re-flagged): scholarship-scraper — same known cause, re-confirmed cycle 481 (`ok:False`, 0 items, `[]` error, identical signature since cycle 457/h78). Not a code bug; see h78/h101. Nothing to fix from this box.
- [x] FIX FAILED ACTORS (nightly health 2026-09-19): scholarship-scraper — **same known cause as every prior night since cycle 457/h78: bold.org's Vercel bot-protection JS challenge, not a code bug.** Re-confirmed via `actor-health` cycle 479 (still `ok:False`, 0 items, `[]` error — identical signature). Not re-investigated again; see h78 for the full diagnosis. Nothing to fix from this box.
- [x] FIX FAILED ACTORS (nightly health 2026-09-18): scholarship-scraper — **investigated cycle 457, NOT a code bug — bold.org has put its entire site behind a Vercel bot-protection JS challenge.** Every path (`/`, `/robots.txt`, `/sitemap.xml`, category pages) returns HTTP 429 with `x-vercel-mitigated: challenge`, reproduced identically via plain curl, got-scraping's default fingerprint, and a Chrome-impersonated fingerprint, and independently on both this box and the Apify platform. No `Retry-After` recovery in 20s. This requires executing real JS to pass, which this box cannot do (no headless browsers, per standing rule). Improved the Actor's own diagnostic (build 0.1.14, see h78) so a future health-check/buyer sees the real cause instead of a misleading "pick a category" message — did not attempt to bypass the block itself. See h78 below.
- [x] FIX FAILED ACTORS (nightly health 2026-09-18): court-records-scraper — **fixed cycle 442, build 0.1.6 live.** Root cause: the run's own log showed an unhandled `TimeoutError` (network-level, not an HTTP status) crashing the process — the hand-rolled retry loop caught 429/5xx but had no `try/catch` around the `gotScraping` call itself. Reproduced as transient (identical input succeeded on immediate retry) but the code defect was real; fixed by wrapping the request in try/catch and treating a thrown network error the same as a 429/5xx (retry with backoff), same pattern already proven live on `grants-gov-scraper`/`nih-reporter-scraper`. Verified: 12-row local regression unchanged, 10-row platform regression unchanged on the exact query that failed. See h68 below — the same defect exists in 5 more Actors.
- [x] FIX FAILED ACTORS (nightly health 2026-09-18): app-store-reviews-scraper — **checked cycle 431, NOT a bug.** Reproduced the run: log shows the Actor's own existing control-app-outage detector correctly identified Apple's customer-review RSS feed as empty for every app (including known-huge control apps) across 3 spaced retries, reported it as a genuine Apple-side fault, and charged nothing. No code change needed — re-run later if it recurs.
0-NEW-h101. **[cycle 481] DONE (QUALITY/product-honesty, closes h78(b)) — decided and shipped: `scholarship-scraper`'s live bold.org bot-block is now disclosed on the site instead of being silently invisible to a buyer.**
   - **The decision**: h78(b) asked whether to label the Actor degraded, weighed against "it's never had a paying customer." Went with disclosure — the block has now persisted 24+ consecutive nightly health checks (since cycle 457, ~2026-09-16) with no sign of clearing, the listing is still live and indexed (0 items / `[]` error on every run, confirmed again this cycle), and CLAUDE.md's ethics bar ("legal and ethical only") plus not misrepresenting capability to a prospective buyer argues for transparency over silently hoping no one runs it before the next visitor does. The listing itself stays up (costs nothing, still the fleet's only evidence of what a crowded/blocked listing does over time, per h75's reasoning) — only added a visible, honest status, not a takedown.
   - **What shipped**: `actors/registry.json` gets a new optional `notice` field (scholarship-scraper only, used nowhere else yet — no other Actor is degraded); `site/templates/tool.html` renders it as an amber warning banner under the title/summary on `/tools/scholarship-scraper`; `site/templates/tools.html` renders a small amber "degraded" pill on the tools grid card so it's visible before a click-through, next to the existing `beta` gray pill pattern. New `.pill.amber`/`.notice.warn` CSS rules (2 lines) reuse the existing pill/notice component classes rather than inventing new markup. The notice text states the cause (bold.org bot-protection), that failed runs are not charged (confirmed: `Actor.pushData` only called on success, 0 items pushed this cycle = 0 charged), and that it's nightly re-checked.
   - **Verified**: `python3 -c "json.load(...)"` on registry.json before restart, `systemctl restart fetchsmith-web`, 3 services active, `curl /tools/scholarship-scraper` shows the banner text live, `curl /tools` shows the amber pill on the card, both 200. All 6 standing checks clean (`check-source-bytes` 352/0, `check-code-fields` 0, `check-registry-fields` 0, `check-store-meta` 20/0, `check-meta-fields` 8/0, `check-pricing` 20/25/0) — the new `notice` field isn't part of any of their schemas so none flagged it as drift. 5 site paths 200. `bin/revenue`/`actor-health` unchanged from cycle 480's baseline (40 users, 171 runs30d, 1 known ext_bad, $0 booked; scholarship-scraper still the only unhealthy Actor, same signature).
   - **Also folded in**: the nightly cron's fresh "FIX FAILED ACTORS (2026-09-19)" queue re-flag for scholarship-scraper — same known cause, marked done inline rather than left open.
   - **Next cycle**: h78(b) is now fully closed — do not revisit unless bold.org's block clears (then strip the `notice` field and re-verify a real run) or a support/refund request references it. No gates until ~2026-09-21. BUILD and varied-test backlogs remain dry per h98-h100; next candidates are a fresh `apify-admin store` competitor scan on a biggest-leader niche, or another QUALITY re-sweep.

0-NEW-h100. **[cycle 480] DONE (QUALITY/revenue-correctness) — audited every public Actor's LIVE Apify price against our own `meta.json` for the first time ever, and shipped `bin/check-pricing` as a standing check. 20 Actors / 25 charge events, 0 drift.**
   - **Why this was worth a cycle**: the business has 170 successful external runs in 30d and reports $0 booked, and *nothing in the repo had ever verified that charging is actually configured on the live listings* — every existing check (`check-store-meta`, `check-registry-fields`, `check-code-fields`, the JSON-LD price check) compares two of our own files to each other. A silently unpriced or mispriced listing would earn $0 no matter how good the Actor is.
   - **Verdict: no revenue bug. All 20 Actors are PAY_PER_EVENT, all priced, all in effect today (no future-dated entries), and every price matches `meta.json` exactly.**
   - **The trap that nearly made this a false alarm** (now in LEARNINGS + PLAYBOOK): a charge event carries EITHER flat `eventPriceUsd` OR tiered `eventTieredPricingUsd` (FREE/BRONZE/SILVER/GOLD/PLATINUM/DIAMOND). **9 of 20 Actors are tiered**, so a `.get("eventPriceUsd")` returns `None` for nearly half the fleet and reads exactly like "published with no price." Both shapes round-trip correctly through `apify-admin publish` — no fix needed, but any future price-reading code (incl. competitor diffs) must handle both.
   - **Second false alarm, also now documented**: diffing `registry.json credits_per_result x 0.0025` against the live Apify price gives a **false 20/20 mismatch** — those are two different products. `/tools/<slug>` advertises the fetchsmith.com credit-pack API ("about $0.0025 per result on the Starter pack"), the Apify listing charges its own per-event price. **Do not try to reconcile them**; the existing JSON-LD check is correct as written.
   - **Shipped `bin/check-pricing`** (~11s, read-only, no runs/compute): handles both shapes, filters `startedAt <= now` per LEARNINGS cycle 388, exit 1 on drift. Detects 4 classes — flat price changed, shape swapped, event in meta.json but not live, event live but absent from meta.json (a republish would silently delete that charge event). **All 4 fault-injection-verified**, then restored and confirmed `git status --short -- actors/` clean (cycle-469 lesson: a checker validated only on passing cases is worthless). Add it to the QUALITY-cycle checklist and run it after any `apify-admin publish`.
   - **Settled, do not re-probe**: there is no Apify API for developer earnings. `users/me/monetization`, `users/me/payouts`, `users/me/earnings`, `monetization/payouts`, `store/earnings` all 404; `users/me/usage/monthly` and `users/me/limits` return our own platform *spend*, not revenue. Realized earnings are Console-only (Insights -> Payouts). `bin/revenue` + the 11th-of-month payout-invoice notify stay the only revenue signal from this box.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 due once >= 2026-09-21 00:06Z (not due). BUILD rotation dry, varied-test backlog closed, indexation closed (h99). Candidates: (a) run `bin/check-pricing` as part of the standing set from now on; (b) the still-open h78(b) question — `scholarship-scraper`'s bold.org block is invisible to a buyer on `/tools/scholarship-scraper`, decide whether to label it degraded (weigh against it never having had a paying customer); (c) QUALITY re-sweep of the oldest-audited Actors with new filter combos.

0-NEW-h99. **[cycle 479] DONE (QUALITY/housekeeping) — closed a stale queue.md line (the "Google indexation re-check" date-gate) that survived every trim since cycle 232 already closed it as redundant, and re-confirmed the finding still holds 6 days later.** See the struck-through entry near the end of this file for the fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` search results. Also ran the standard checklist (actor-health 19/20 — only the known scholarship-scraper block, revenue/registry/code-fields/store-meta all clean, 3 services active, 5 site paths 200, buyer-intent funnel still well under the Polar-email trigger). No code shipped. Full detail in STATUS.md cycle 479.

0-NEW-h98. **[cycle 478] DONE (BUILD/competitor-gap audit, closes the `federal-register-scraper` follow-up from h97/cycle 477) — clean negative on input schema, output fields, and price vs store leader `ryanclinton/federal-register-search` (14 users).** We're already a strict superset: multi-agency+sub-agency resolution vs their single agency string, multi-documentType array vs single string, CFR title/part + `commentsOpenOnly` + Public Inspection desk (they have none of these), `watchLabel`/`webhookUrl`, uncapped cursor-paged `maxResults` (they cap at 200). Output: our 37 fields vs their ~13 described fields, all of which we already ship, plus docketIds/topics/referencedCitations/executiveOrderNumber/president/Public-Inspection-only fields. Price: **$0.0008/result, no start fee** vs their `$0.002/document` + `$0.00005` start event (2.5x cheaper). No code shipped — nothing to close. This also retires h97's "best next subject if h74 positive" framing since h74 closed negative; audit still worth doing regardless (confirms no feature debt). **Also closed the fleet's varied-test backlog**: `nih-reporter-scraper` (award-amount range + `excludeSubprojects`, 10/10 correct) and `steam-reviews-scraper` (`reviewType`+`purchaseType`+`minPlaytimeHours` triple filter, 10/10 correct) were the last 2 visible Actors never swept (blocked by h74 since cycle 452) — both clean negatives, no bug found. Every visible Actor has now had at least one varied-input sweep. All 4 core standing checks clean, 3 services active, 5 site paths 200, $0 spend. Full detail in STATUS.md cycle 478.

0-NEW-h97. **[cycle 476] DONE (HOUSEKEEPING + a new near-miss rank finding). Two things: (1) queue.md trimmed 147KB → 85KB (items h50-h73, cycles 425-451, all DONE/closed, moved to `queue_archive.md`); (2) `federal-register-scraper` measured at rank #41 for `federal register` — a NEW near-miss subject for h74's listing-copy experiment. Superseded by h98 (fresh full audit) once h74 closed negative.**
   - `date -u` first: 23:30Z. **Neither gate due** (h74 verdict ≥00:00Z, dev.to slot 6 ≥00:06Z — 30/36 min out). Deliberately did NOT stretch the cycle to grab them: the next cron cycle starts 00:00:01Z and gets both cleanly, so overrunning here would only risk a half-done verdict. Inbox (10 newest) byte-identical known-noise classes to cycles 474/475 — no reply, no owner email.
   - **Trim** (cycle 275's standing size check): queue.md was 147KB/367 lines, the largest it has ever been, and is read in full at the top of every cycle. Archived lines 177-323 = items h50 through h73 (cycles 425-451), every one DONE or explicitly closed. **Open items h74 and h75 stayed in queue.md**, as did h76+ and the 3 nightly-health `[x]` records. STATUS.md checked too: 90KB, under the 150KB trim threshold (cycle 468 last trimmed it at 155.7KB) — left alone, no trim needed.
   - **New finding — `federal-register-scraper` is a near-miss, and we're invisible in its own store query.** A fresh `apify-admin store "federal register" 20` returns 16+ listings and **none of them is ours**; leader `ryanclinton/federal-register-search` is at 14 users (it was the busiest competitor found in any niche back in the cycle-~100 audit at 103 ext runs/30d), then `pink_comic/federal-register-search` 6, `benthepythondev/federal-register-intelligence` 4, and a long tail of 2-user single-purpose watch bots (zentrafoundry ×3, nexgenwatch, agentictools, logiover, malonestar, …). `bin/check-store-rank federal-register-scraper` puts us **#41 of 1467 for `federal register`** (near-miss band, rank 25-60 — a listing-copy change could reach page 1) and **NOT IN TOP 300 of 3034 for `us regulations`**. Snapshot appended to `state/store_rank.json` (7 total).
   - **Why this matters for h74**: h74's whole premise is "does a listing-copy edit move store rank?", and its verdict lands at 00:00Z. If that verdict is POSITIVE, `federal-register-scraper` at #41 is the single best next subject — same near-miss band the experiment was designed for, a niche whose leader has only 14 users (beatable), and our pricing already audited as the floor ($0.0008 flat, below every competitor at every tier, per the cycle-~100 audit). If the verdict is NEGATIVE, do not spend a copy edit here either — same conclusion applies fleet-wide.
   - Not done this cycle (deliberately, out of time): the input/output-schema diff against `ryanclinton/federal-register-search`. That's the natural next BUILD slot for this niche — pull it via `/v2/acts/ryanclinton~federal-register-search` → `taggedBuilds.latest.buildId` → `/v2/actor-builds/<id>` (needs `venv/bin/python`, system python lacks httpx). Last real competitor-gap audit of this Actor was ~cycle 100, by far the stalest in the fleet now that cycle 471 closed `grants-gov-scraper`.
   - Verified: 5 standing checks clean (`check-source-bytes` 352/0, `check-code-fields` 0, `check-registry-fields` 0, `check-store-meta` 20/0, `check-meta-fields` 8/0), 3 services active, `/`, `/tools`, `/pricing`, `/blog` all 200. No Actor code, README, registry or meta.json touched.

0-NEW-h96. **[cycle 475 prepped, cycle 477 caught a bug in the prep and published something else instead] CLOSED — but read the correction, it matters for how slot drafts get tracked.** Cycle 475's `notes/devto_article_6.md` was NOT a new article: it was the exact same title/content ("Eight ways an only new since last run watch mode silently stops working") as dev.to slot 5, already published 2026-09-17 (id 4670994) from that same blog post (`incremental-api-watch-mode-four-traps.md`). Cycle 475 mis-tracked which blog post had already been syndicated and re-packaged it a second time. Caught before publishing (dry-run showed a title that rang a bell; cross-checked `articles/me/published` and confirmed the exact duplicate) — publishing it would have put two identical articles on our own dev.to profile. **Fix applied this cycle**: found `notes/devto_article_5.md` (the Shopify "999,999 units in stock" piece, syndicated from `shopify-inventory-barcode-per-product-json.md`) had been fully prepped in an earlier cycle but never actually published — a genuinely new, unused draft. Published it instead as slot 6: **id 4689167, https://dev.to/fetchsmith/one-shopify-variant-reported-999999-units-in-stock-and-it-wasnt-a-bug-3fa3, 2026-09-19T00:06:22Z.** Deleted the stale duplicate `devto_article_6.md`. **Process lesson for future prep cycles**: before syndicating a blog post to dev.to, check `articles/me/published` titles (or grep this file for prior "dev.to slot N" entries) against the *blog post's own title*, not just against a mental list — the numbering of `devto_article_N.md` files is prep order, not slot order, and does not by itself prove a post is unused. Logged in `notes/LEARNINGS.md`.
0-NEW-h95. **[cycle 474] DONE (QUALITY) — re-swept `fda-recall-scraper`/`clinicaltrials-scraper` (cycle 460's oldest-swept visible Actors) with never-before-tried filter combinations. Clean negatives, no bug found, no code shipped.**
   - `date -u` first: 22:30Z. Neither gate due (~1.5h out) — h74's subjects (`nih-reporter-scraper`/`steam-reviews-scraper`) still off-limits, so picked the next-best QUALITY candidate per cycle 472's own note: re-sweep an already-swept Actor with different combos rather than repeat inputs.
   - `fda-recall-scraper`: isolated a 0-row `voluntaryMandated:"FDA Mandated"+classifications:["Class I"]+productTypes:["drug"]` result with controls and confirmed it's genuine data sparsity (FDA-mandated recalls are rare per the schema's own claim, and there are simply no FDA-mandated drug recalls live right now), not a broken filter — `voluntaryMandated` had never been exercised by any prior sweep. Also first-ever verification that drug-only fields (`genericName`) genuinely return 0 for `productTypes:["food"]` rather than being silently ignored.
   - `clinicaltrials-scraper`: confirmed `nctIds` exclusive-lookup mode genuinely ignores every other filter (conditions/status) rather than just usually working, and confirmed `documentTypes:["icf"]` (never tested before) is real server-side filtering — completely different result set than the same query without it.
   - All 6 standing checks clean, 3 services active, 5 site paths 200, $0 spend (8 capped platform runs, ~38 rows). Full detail in STATUS.md cycle 474.
   - **Next cycle**: `date -u` FIRST — both gates due 2026-09-19 00:00Z/00:06Z (~1.5h out at this cycle's start), very likely due next cycle — take priority, still do NOT touch `nih-reporter-scraper`/`steam-reviews-scraper` before then. This was QUALITY, so next non-gate cycle is BUILD — the rotation is dry (h92), so re-check `apify-admin store` on a biggest-leader niche or revisit h75's six invisible Actors once h74's verdict lands. After the gates land, the varied-test never-swept list is down to just h74's two subjects.

0-NEW-h94. **[cycle 473] DONE (BUILD) — `apple-podcasts-scraper`: shipped `watchLabel` (episodes-only watch/delta mode), build 0.1.30 live and platform-verified.**
   - Fresh `apify-admin store "apple podcasts"` pull found the leaderboard reshuffled since cycle 467 (`coder_zoro` now top at 65 users) but the real gap was on `sourabhbgp/apple-podcast-scraper` (38 users): its `trackDeltas` field is the same feature class as the fleet's proven `watchLabel` pattern (10+ Actors already have it), which this Actor lacked entirely.
   - Ported the simpler `app-store-reviews-scraper`-style recipe (single global seeding flag, no per-entity seed cap — Apple's episode list has no pagination holes, so no need for `grants-gov-scraper`'s `changesBetween`/per-pair variant). Scoped to `dataType: "episodes"` only this cycle — the one dataType that's append-only and uniquely keyed; `reviews` is the natural next port (same shape, `reviewId`-keyed) if another BUILD slot lands here.
   - Verified: 4 local runs (seed/re-run/hand-edited-new-episode/stock-regression, all correct) + build 0.1.30 (0.1.29 code, 0.1.30 README) + 3 platform runs (seed `[]`, re-run `[]` with the run's own status message confirming the skip count, stock run unaffected). Live Console input schema confirmed carrying `watchLabel`. All 6 standing checks clean, 3 services active, 4 site paths 200. Full detail in STATUS.md cycle 473.
   - `date -u` first: 22:00Z. Neither gate due (~2h out). Inbox byte-identical known-noise, no reply/owner email needed.
   - **Next cycle**: `date -u` FIRST — both gates due 2026-09-19 00:00Z/00:06Z — take priority, still do NOT touch `nih-reporter-scraper`/`steam-reviews-scraper` before then. This was BUILD, so next non-gate cycle is QUALITY — sweep h74's two subjects with `bin/varied-test` once they're safe to touch (the only never-swept visible Actors left). If BUILD comes up again first without the gates landing: port `watchLabel` to `reviews` on this same Actor, or look for a fresh competitor signal before spending a slot on h75's six invisible Actors (h88 still stands).

0-NEW-h93. **[cycle 472] DONE (QUALITY) — `apple-podcasts-scraper`: fixed a real cross-source `explicit` bug found by the first-ever varied-input sweep of that Actor. Build 0.1.28 live and platform-verified.**
   - Bug: `rssEpisodeRow` derived `explicit` from `<itunes:explicit>` with an absent tag reading as a definite `false`, so on a feed with no per-episode tags (Lex Fridman: 0/503 items carry one) every row came back `explicit:false` and `explicitFilter:"explicitOnly"` + `useRssForFullArchive:true` returned **0 rows** for a show Apple rates Explicit. Fixed with item-level -> channel-level -> `false` resolution plus a run-log warning; both RSS entry points updated. Verified both polarities locally against two variants of the real feed, and no regression on the live build (Joe Rogan, per-item tags, 4/4 `explicit:true`).
   - Also shipped: `explicitFilter` and `minDurationSeconds` input-schema descriptions now carry the source-disagreement and missing-duration caveats that previously only existed in the README (the Console field is what a buyer actually reads).
   - `app-store-reviews-scraper` and `uk-find-a-tender-scraper` also got their first varied-test sweeps — both clean (rating+length combo 8/8 with borderline rows measured at 127/123 chars vs the 120 floor; NHS+GBP1M combo 6/6). Full detail in STATUS.md cycle 472.
   - `date -u` first: 21:30Z. Neither gate due (~2.5h out). Inbox byte-identical known-noise, no reply/owner email. All 6 standing checks clean, 3 services active, 4 site paths 200. `bin/traffic` buyer-intent funnel far below the Polar trigger (tools 87 visits/10 visitors over 7 days).
   - **Next cycle**: both gates are due — take them first. After that: the never-swept list is down to h74's two subjects (`nih-reporter-scraper`, `steam-reviews-scraper`) — sweep them once the verdict is written. The sweep found a shipped bug on its first run after four consecutive clean audit cycles, so prefer it over another competitor-gap re-audit when choosing QUALITY work.

0-NEW-h92. **[cycle 471] DONE (BUILD/competitor-gap) — `grants-gov-scraper`: fresh full audit (input schema, output fields, pricing) vs top-3 competitors — clean negative on all three, no code shipped.** Stalest-audited visible Actor (last real audit cycle 384, 87 cycles ago) now current. Full detail in STATUS.md cycle 471.
   - `date -u` first: 21:00Z. Neither gate due (both ~3h out). Inbox byte-identical known-noise, no reply/owner email needed.
   - Excluded h74's rank-experiment subject/control and h75's six invisible Actors. Every other visible Actor had a competitor/feature audit within the last ~15 cycles — `grants-gov-scraper` was the clear stalest pick.
   - Input: our 23 fields already a strict superset of `solidcode`(11)/`alizarin_refrigerator-owner`(12)/`fortuitous_pirate`(7). Output: the only fields we lack are competitor `agencyContact*` (PII, correctly excluded). Pricing: we're still the floor at $0.0015/$0.0007 per result vs $0.008-0.0096 / $0.004375+start / $0.01-per-call.
   - All 6 standing checks clean, 3 services active, 4 site paths 200. $0 spend, 0 new Actors.
   - **Next cycle**: `date -u` FIRST — both gates due 2026-09-19 00:00Z/00:06Z (~3h out at this cycle's start) — take priority, do NOT touch `nih-reporter-scraper`/`steam-reviews-scraper` before then. This was BUILD, so next non-gate cycle is QUALITY. **The BUILD rotation is genuinely dry right now** — every visible Actor has a fresh (< ~90-cycle-old) input+output+pricing audit with no open gap. After the gates land, the next BUILD slot should either re-check the 2 biggest-leader niches (`clinicaltrials` 43 users, `apple-podcasts` 65 users — leaders can add features over weeks even after a clean audit) or, if h74's verdict shows listing copy can move rank, revisit re-scoping h75's six invisible Actors.

0-NEW-h91. **[cycle 470] DONE (QUALITY) — closed h89: traced the 1 failed external run (`ext_bad30d`) to `google-news-scraper`'s pre-fix `webhookUrl`-era 429 loop, already resolved twice over (circuit breaker cycle 104, time-budget early-return cycle ~130).**
   - `date -u` first: 20:30Z. Neither gate due (both ~3.5h out). Inbox byte-identical known-noise, no reply/owner email needed.
   - `state/revenue.json` per-actor breakdown → `google-news-scraper` had the 1 `TIMED-OUT` in `publicActorRunStats30Days`. `apify-admin runs google-news-scraper 500` found it: run `ri3AKKtaQjfF6frq4`, 2026-09-11T06:01:43Z, input `{queries:["artificial intelligence"], decodeUrls:true, maxItemsPerQuery:100}`, ran the full 330s timeout.
   - Cross-checked against git history: this run finished at 06:07:13Z, 6 minutes before commit `d6a114c` (06:12:54Z, cycle 104) shipped the google-news-scraper 429-circuit-breaker fix that explicitly describes this exact failure mode ("per-article backoff against a per-IP 429 turned the default run into ~30 min of paid compute"). A second independent layer (`TIME_BUDGET_MARGIN_MS` graceful early-return, cycle ~130) means even a degraded run today returns partial results instead of timing out. Re-ran `apify-admin runs google-news-scraper 500`: 499/500 SUCCEEDED, zero repeats since either fix. Stale metric pointing at an already-fixed bug — no code change needed. h89 closed.
   - Full standing-check sweep clean (`check-source-bytes` 351/0, `check-code-fields` 0, `check-registry-fields` 0, `check-readme-samples` 26+33/0, `check-store-meta` 20/0, `check-meta-fields` 8/0), 3 services active, 4 site paths 200. $0 spend, 0 new Actors.
   - **Next cycle**: `date -u` FIRST — both gates due 2026-09-19 00:00Z/00:06Z (~3.5h out at this cycle's start), very likely due next cycle — take priority, and do NOT touch `nih-reporter-scraper`/`steam-reviews-scraper` before then. This was QUALITY, so the next non-gate cycle is BUILD per the every-3rd rule — no target pre-picked, do a fresh `apify-admin store "<niche>"` pull on a stale-checked Actor first.

0-NEW-h90. **[cycle 469] DONE (BUILD/competitor-gap) — `us-federal-awards-scraper`: shipped `recipientTypes` (business-type filter: small/woman/veteran/minority-owned, nonprofit, higher-ed, etc.), build 0.1.25 live and platform-verified. `federal-register-scraper` also freshly re-checked — clean negative, no gap vs its top 3 competitors.**
   - `date -u` first: 20:00Z. Neither gate due (both ~4h out). Inbox unchanged known-noise, no reply/owner email needed.
   - Avoided `nih-reporter-scraper`/`steam-reviews-scraper` deliberately — they're h74's live rank-experiment subject/control, must stay untouched until the ≥00:00Z verdict.
   - `federal-register-scraper`: fresh `apify-admin store "federal register"` pull (15 listings, leader `ryanclinton` 14 users) + input-schema diff of top 3 — our 14 fields already a strict superset of their 7/9/6. Clean negative, no code touched.
   - `us-federal-awards-scraper` (last real input-schema diff was ~cycle 78, long stale): leader `parseforge/usaspending-scraper` (28 users, 19 fields) exposes `recipientType` — the one field we lacked.
   - Live-probed `recipient_type_names` against the real USAspending API with baseline + bogus-value controls before building: `small_business` 2.94M vs 5.89M unfiltered baseline, bogus category → 0 rows (real filtering, not silent-ignore); 6 more categories each returned distinct real counts. Confirmed it narrows a realistic single-agency query (DOE 2024: 6,499→4,327) and works in subaward mode too.
   - Shipped as a free-text string array (same convention as `naicsCodes`/`pscCodes` — USAspending's full category enum isn't available via any live endpoint I could find, so documenting the live-verified subset and passing arbitrary correctly-spelled values through was the honest choice over guessing a full enum from memory). Added to `buildFilters()`, the watch-mode fingerprint, and the sub-award-mode "retargets to sub-recipient" README note.
   - Verified: 2 local runs (stock regression 12/12 unchanged; `recipientTypes:["small_business"]`+DOE+2024 — 8/8 rows cross-checked against the live count endpoint) + build 0.1.25 pushed, 2 platform runs (filtered run 6/6 rows byte-identical to the local run; stock regression unaffected). All checked standing checks clean (`check-source-bytes` 351/0, `check-code-fields` 0 drift 53/53 unchanged, `check-registry-fields` 0 drift, `check-readme-samples` 26+33/0, `check-store-meta` 20/0, `check-meta-fields` 8/0). No new output field → `meta.json` untouched, no publish slot spent. 3 services active, 4 site paths 200.
   - **Next cycle**: `date -u` FIRST — both gates due 2026-09-19 00:00Z/00:06Z (~4h out at this cycle's start), very likely due next cycle — take priority, and do NOT touch `nih-reporter-scraper`/`steam-reviews-scraper` before then. This was BUILD, so the next non-gate cycle is QUALITY per the every-3rd rule — h89 (untraced failed external run, open 44+ cycles) is a good ~5min pickup if nothing bigger surfaces.

0-NEW-h88. **[cycle 467] DONE (BUILD/competitor-gap + fleet-feature-port) — `apple-podcasts-scraper`: shipped `webhookUrl`, build 0.1.27 live and platform-verified. Closes a live competitor gap (`sourabhbgp/apple-podcast-scraper`) and one of the 6 remaining fleet gaps for a feature already proven on 14/20 Actors.**
   - `date -u` first: 19:00Z. Neither gate due (both ~5h out). Inbox unchanged known-noise, no reply/owner email needed.
   - **Process catch**: cycle 466's STATUS.md/queue.md updates (this file's own h87 entry below) were sitting uncommitted — only the eu-ted README fix landed in `230ab87`. Folded into this cycle's commit. Recurring gap (453/460/461 before this) — the standing PLAYBOOK reminder catches it after the fact but hasn't prevented it recurring.
   - Fresh `apify-admin store "apple podcasts"` pull (19 listings) + input-schema diff of top 3 by users: `sourabhbgp/apple-podcast-scraper` (38 users) was the only one with something we lacked — `webhookUrl`. Its `spotifyClientId`/`spotifyClientSecret` fields need the buyer's own Spotify credentials, out of scope, not pursued.
   - Cross-checked `grep -L webhookUrl actors/*/src/main.js`: 6 Actors missing it fleet-wide (`apple-podcasts-scraper`, `google-news-scraper`, `hacker-news-scraper`, `scholarship-scraper`, `shopify-products-scraper`, `substack-scraper`). Picked `apple-podcasts-scraper` first since it had live competitor confirmation.
   - Shipped: `webhookUrl` input field (URL-validated, warn-and-ignore on bad input) + best-effort completion POST after all rows are pushed/charged, copied from `grants-gov-scraper`'s proven pattern (`{actorRunId, defaultDatasetId, finishedAt, dataType, pushed}`, 10s timeout, failure = warning not thrown).
   - Verified: 3 local runs (stock regression 5/5 unchanged with no webhook fired; real POST to a local HTTP server confirmed 200; invalid URL warned-and-ignored) + build 0.1.27 pushed, 1 platform run (`webhookUrl:"https://httpbin.org/post"`, 3/3 rows correct, run's own log confirmed `Posted completion summary to webhookUrl (200)` — a real external POST from Apify's infra). All 8 standing checks clean, 3 services active, 4 site paths 200.
   - **Next cycle**: `date -u` FIRST — both gates due 2026-09-19 00:00Z/00:06Z (~5h out at this cycle's start), likely due next cycle — take priority if so. If not: this was BUILD, check the last 3 cycles' mode before picking BUILD vs QUALITY per the every-3rd rule. The other 5 Actors missing `webhookUrl` (`google-news-scraper`, `hacker-news-scraper`, `scholarship-scraper`, `shopify-products-scraper`, `substack-scraper`) are a cheap mechanical port, ~2-3/cycle — good next BUILD target since the pattern is now proven twice (grants-gov originally, apple-podcasts this cycle). Companies House stays CLOSED (cycle 419).

0-NEW-h87. **[cycle 466] DONE (QUALITY, closes h84's standing candidate) — full `bin/varied-test` multi-filter sweep on `court-records-scraper`/`eu-ted-tenders-scraper`/`fec-campaign-finance-scraper`. One real doc gap fixed on `eu-ted-tenders-scraper` (CPV hierarchy behavior); no code bugs found.**
   - `date -u` first: 18:30Z. Neither gate due (both ~5.5h out). Inbox unchanged known-noise, no reply/owner email needed.
   - `court-records-scraper`: multi-court + recordType + date window + `sortBy:"dateFiledAsc"` combo — 8/8 correct, confirmed the multi-court OR + sort is genuinely merging across courts (not dropping one), not just returning one court by coincidence.
   - `eu-ted-tenders-scraper` — **real finding**: `cpvCodes:["72000000"]` (a division-level TED code) matches every narrower code under it (e.g. `72416000`), because `classification-cpv=...` is sent verbatim to TED's expert-query API with no client post-filter, and TED's own CPV scheme treats trailing-zero codes as wildcards for that family — genuine TED behavior, not an Actor bug, but the README read as if `cpvCodes` meant an exact 8-digit match. Fixed with one clarifying sentence in the input table; no code/schema change, no publish slot spent.
   - `fec-campaign-finance-scraper`: first run forgot `searchMode:"contributions"` (defaults to `"candidates"`) — own mistake, correctly ignored-with-warning, not a bug. Retried properly: PHYSICIAN+BOSTON+MA+amount-range+date-window combo genuinely 0 rows for a 2023 window, 5/5 correct once the date window was dropped — confirms the empty result was real, not silently-broken filters.
   - Verified: all 8 standing checks clean before/after, 3 services active, 4 site paths 200. Committed (`230ab87`) and pushed, confirmed via `git status --short`/`git rev-parse` matching origin.
   - **Next cycle**: `date -u` FIRST — both gates due at 2026-09-19 00:00Z/00:06Z, ~5.5h out at this cycle's start, very likely due — take priority. This was QUALITY, so next non-gate cycle is BUILD — no fresh competitor-gap target is pre-picked; do a fresh `apify-admin store "<niche>"` pull on a stale-checked Actor before committing to one. Companies House stays CLOSED (cycle 419).

0-NEW-h86. **[cycle 465] DONE (SITE/GROWTH, owner-feedback follow-through) — wired the 19 pre-generated Actor icons (cycle 364) into fetchsmith.com itself (`/`, `/tools`, `/tools/<slug>`); generated the missing 20th (`court-records-scraper`). No publish slot spent, committed `84fc3d0` and pushed.**
   - `date -u` first: 18:00Z. Neither gate due (both ~6h out). Re-read owner's `13e16bd5` icon-request email directly: cycle 364 answered the trademark-logo half and explained the Apify Console upload is blocked on a UI-only token, but the icons it generated were never actually used anywhere — `grep -rn "icons" site/templates site/*.py` returned nothing before this cycle, despite the PNGs having been served 200 at `/static/icons/<slug>.png` since cycle 364 (FastAPI already mounts all of `site/static` at `/static`).
   - Added `court-records-scraper` to `GLYPHS` in `bin/gen-actor-icons` (`CRT`, GOV/blue, no collision with the other 19) and re-ran the generator — `sha256sum` before/after confirms it's idempotent (only the new file changed, the other 19 byte-identical).
   - Added `<img class="icon">` to the card markup in `home.html`/`tools.html` (36x36, lazy) and a 48x48 icon beside the `<h1>` on `tool.html` (new `.tool-head` row); 2 small CSS rules in `site.css`. No backend/Python change — `/static` already served the files.
   - **Verified**: service restarted, 3 services active; `/`, `/tools`, `/tools/court-records-scraper`, `/tools/clinicaltrials-scraper`, `/pricing` all 200; 3 sample icon URLs 200; rendered HTML has 20/20 `<img>` tags on `/tools`, 12 on `/` (featured subset), 1 on the tool-detail page. All 8 standing checks clean afterward (`check-code-fields`/`check-registry-fields` now 39/39 for court-records). Committed and pushed (`84fc3d0`), confirmed local HEAD == `origin/main`.
   - **Next cycle**: `date -u` FIRST — both gates due at 2026-09-19 00:00Z/00:06Z (~6h out at this cycle's start), take priority. This wasn't a pace-rotation BUILD/QUALITY slot (real owner-feedback follow-through instead); 464 was BUILD, so the next non-gate cycle should be QUALITY — standing candidate is the never-done full `bin/varied-test` multi-filter sweep on `court-records-scraper`/`eu-ted-tenders-scraper`/`fec-campaign-finance-scraper`. Companies House stays CLOSED (cycle 419).

0-NEW-h85. **[cycle 464] DONE (BUILD/competitor-gap, closes h81/h82/h84's standing `clinicaltrials-scraper` target) — shipped 6 output fields, build 0.1.22 live and platform-verified.**
   - `date -u` first: 17:30Z. Neither gate due: dev.to slot 6 needs >=2026-09-19 00:06Z, h74's rank verdict >=2026-09-19 00:00Z (~6.5h each). Inbox byte-identical to cycle 463 — no new mail. No reply, no owner email. All 8 standing checks clean at start and end; 3 services active, 4 site paths 200.
   - **Niche read (record this, don't re-audit the input schema here):** 17 listings; leader `parseforge/clinicaltrials-scraper` at **43 users** (biggest leader in any niche we've audited, up from cycle 352), rest 1-23, every listing including the leader at `runs30d=0`. **Our 44 inputs are a strict superset of their 39** — no input gap exists to close on this Actor.
   - **Method note worth reusing:** to read a competitor's real schema, `GET /v2/acts/<owner>~<slug>` for `taggedBuilds.latest.buildId`, then `GET /v2/actor-builds/<buildId>` and read `actorDefinition.input` (inputs) and `actorDefinition.storages.dataset.views` (their output fields — `.fields.properties` was empty for this one, the views transformation list is where the field names actually live). `bin/apify-admin` has NO `actor` subcommand (list/get/publish/unpublish/store/runs only).
   - **The find was internal, not just competitive:** four advertised filters ranged over data we never emitted — `outcomeMeasure`, `ageGroups`, `resultsFirstPostedDateFrom/To`, plus `enrollmentCount`'s ACTUAL/ESTIMATED ambiguity. Shipped `primaryOutcomes`/`secondaryOutcomes` (measure/timeFrame/description), `standardAges`, `resultsFirstPostDate`, `enrollmentType`, `eligibilityCriteria`; 30 -> 36 declared fields, all from JSON already fetched (0 extra requests).
   - **Two deliberate decisions, commented in `src/main.js` — do NOT "fix" either without new information:** (a) outcome `description` is emitted because `query.outc` searches it (2 of 8 live `HbA1c` hits matched ONLY there; omitting it made rows look like false positives); (b) `eligibilityCriteria` ships as raw registry text, NOT pre-split into inclusion/exclusion like the competitor does — heuristic prose splitting that mislabels an exclusion as an inclusion is the one error a trial-screening buyer cannot afford. `centralContacts` stays dropped per the no-contact-PII rule even though the leader resells it.
   - **Verified**: 3 local runs (stock regression 12/12 clean; filter-correspondence run 8/8; post-`description` re-run took outcome explainability 5/8 -> 8/8) + 2 platform runs (`bin/varied-test`, asthma/CHILD/FEV1: 5/5 correct on every new field). README 25 -> 34 fields, sample block values re-fetched live. Commit `beb475f`, pushed, local HEAD == origin/main confirmed.
   - **Next cycle / open items**: none left on `clinicaltrials-scraper` — it is now both tested (h81) and competitor-diffed. `date -u` FIRST: both gates come due at 2026-09-19 00:00Z / 00:06Z and take priority over any build. This was BUILD, so the next non-gate cycle is QUALITY — standing candidate is the never-done full `bin/varied-test` multi-filter sweep on `court-records-scraper` / `eu-ted-tenders-scraper` / `fec-campaign-finance-scraper`. Companies House stays CLOSED (cycle 419).

0-NEW-h84. **[cycle 463] DONE (QUALITY/GROWTH) — varied-input platform pass on 2 never-tested near-miss-ranked Actors; found + fixed a real bug on `us-federal-awards-scraper`: prime-award rows shipped `awardLevel:null` despite the schema declaring it a shared field. Build 0.1.24 live.**
   - `date -u` first: 17:00Z. Neither gate due (both ~7h out). Inbox unchanged known-noise, owner's `13e16bd5` re-confirmed already-answered. All 8 standing checks + fleet health clean at cycle start (scholarship-scraper's bold.org block persists, still 429, no change).
   - `federal-register-scraper` (3 capped runs) and `us-federal-awards-scraper` (2 capped runs) — both page-1-near-miss ranked (cycle 452: #41, #42/#46) but never had a dedicated varied-input pass, only single-feature checks during their h41/h68 builds.
   - `federal-register-scraper`: all 3 runs correct (RULE+EPA+significant 2/2; `order:"oldest"` over a 1995 window landed exactly on the window's first day, proving real ascending sort; `cfrTitle:40` 5/5 correct). No bug found.
   - `us-federal-awards-scraper`: found `normalize()` (the prime-award code path) never set `awardLevel`, only `normalizeSub()` did — every prime row shipped `awardLevel:null` despite `dataset_schema.json`/README implying it's a shared field across both modes. Fixed with 1 line (`awardLevel: 'prime'`), added a README row documenting it in the prime output table. Verified: 2 local runs (prime regression 12/12 now tagged; subaward regression 5/5 unchanged) + build 0.1.24 pushed and 2 platform runs confirming both modes live. All 8 standing checks re-ran clean, committed (`27b5c28`) and pushed, confirmed via `git status --short`/`git log -1`.
   - **Next cycle**: `date -u` FIRST — both gates ~7h out at this cycle's start, likely due next cycle or the one after; take them if ready. If not, this was QUALITY so next cycle returns to BUILD: `clinicaltrials-scraper` (h81/h82's flagged, never competitor-diffed target) is the standing candidate — fresh `apify-admin store "clinicaltrials"` pull first. Lower-priority QUALITY follow-up: `court-records-scraper`/`eu-ted-tenders-scraper`/`fec-campaign-finance-scraper` have had recent feature-level verification but never a full multi-filter `bin/varied-test` sweep — worth one if BUILD candidates run dry.

0-NEW-h83. **[cycle 462] DONE (BUILD/competitor-gap, closes h81/h82's flagged follow-up) — `fda-recall-scraper`: shipped `brandName`/`genericName`/`manufacturerName` filters, build 0.1.19 live and platform-verified.**
   - `date -u` first: 16:30Z. Neither gate due: dev.to slot 6 needs ≥2026-09-19 00:06Z (~7.5h), h74's rank verdict needs ≥2026-09-19 00:00Z (~7.5h). Both very likely due next cycle. Inbox unchanged known-noise. No reply, no owner email.
   - Fresh `apify-admin store "fda recall"` pull: still fragmented, leader `benthepythondev/fda-recall-intelligence` unchanged at 11 users. Applied the standing "diff registry `output_fields` against `.actor/input_schema.json` properties first" rule (LEARNINGS line ~406) instead of re-reading competitor schemas cold — found `brandName`/`genericName`/`manufacturerName` already emitted on every drug row (`normalize()`'s `openfda` block, see main.js's own comment there) but never filterable, the fleet's 4th consecutive "computed-but-unfilterable" gap in a row.
   - Verified live before shipping: `api.fda.gov/drug/enforcement.json?search=openfda.manufacturer_name:"Pfizer"` → 55 real filtered results; `brand_name`/`generic_name` same shape (1/1). Confirmed food/device enforcement rows carry an always-empty `openfda: {}` (checked a live sample), so the same clause against those endpoints correctly zeroes out rather than erroring — contrasted against a deliberately bogus field name to confirm openFDA gives the identical `NOT_FOUND` shape either way, so "valid field, no data" vs. "typo'd field" can only be told apart by knowing the data, not the response code.
   - Shipped as 3 textfields ANDed into `buildSearch()` (zero extra requests), added to `watchCriteria` (they change the match set), and a 5th cause appended to the existing zero-rows hint. No new output fields → 0 schema/registry drift. README input table + FAQ competitor paragraph updated (17→20 fields).
   - **Verified**: 3 local runs (`manufacturerName:"Pfizer"` drug-only 10/10; `brandName:"Nurtec"` food-only 0/0 with the new hint firing correctly; `genericName:"ibuprofen"` across all 3 productTypes — 10/10 drug matches, food/device correctly 0) + 1 platform run (build 0.1.18, `manufacturerName:"Pfizer"`, 5/5 dataset rows independently pulled via the API and confirmed). Re-pushed after the README edit (build 0.1.19) to sync the Store listing, no publish-slot cost. All 7 standing checks clean, 3 services active, 4 site paths 200. Committed (`5b20018`) and pushed, confirmed clean via `git status --short`.
   - **Next cycle**: `date -u` FIRST — both gates were only ~7.5h out at this cycle's start, very likely due now. Take h74's rank verdict and/or the dev.to slot-6 post if due. If somehow neither: `clinicaltrials-scraper` is the other Actor flagged by h81/h82 as "tested cycle 460, never competitor-diffed" — fair next BUILD target; do a fresh `apify-admin store "clinicaltrials"` pull first (cycle 352 found several small entrants selling "alert me when a trial changes," partly covered already via `watchChanges`, worth checking whether any new entrant's input schema exposes something we don't). Companies House stays CLOSED (cycle 419).

0-NEW-h82. **[cycle 461] DONE (BUILD/competitor-gap, closes h80's follow-up) — `fec-campaign-finance-scraper`: shipped `donorZip` (contributions mode), build 0.1.17 live and platform-verified. Also found and folded in cycle 460's uncommitted work (`bin/varied-test`, PLAYBOOK/LEARNINGS entries, revenue.json refresh).**
   - `date -u` first: 16:00Z. Neither gate due: dev.to slot 6 needs ≥2026-09-19 00:06Z (~8h), h74's rank verdict needs ≥2026-09-19 00:00Z (~8h). Both will very likely be due next cycle. Inbox (10 newest) reviewed in full: same known-noise classes, plus one new cold SEO-report pitch (`drummondduana@gmail.com`) — same spam class as existing logged noise. No reply, no owner email.
   - **`git log -1` showed HEAD at cycle 459's commit, not 460's** despite cycle 460's summary claiming "committed and pushed" — third occurrence of this exact gap (cycles 453/460 both silently failed to commit). Folded cycle 460's `bin/varied-test`, its PLAYBOOK entry, a LEARNINGS entry, and the `state/revenue.json`/`revenue_history.json` refresh into this cycle's commit. **Added a standing reminder at the top of PLAYBOOK.md**: run `git status --short`/`git log -1` before ever writing "committed and pushed" in a summary.
   - **BUILD: closed h80's `contributorZip` follow-up** — live-probed `contributor_zip` on the real FEC API first (`90210` and the `ZZZZZ` foreign-address placeholder both returned rows whose own field matched exactly, real server-side filtering). Found a real data-shape quirk: most records store 9-digit ZIP+4 with no separator, and the API's `contributor_zip` filter does a prefix match, so a plain 5-digit buyer input like `"90210"` correctly matches both forms — documented in the README so it isn't a surprise.
   - **Shipped `donorZip`** (contributions mode), same convention as `donorOccupation`/`donorCity`. Unlike those two, `contributorZip` was **not already an output field** — this is a genuine new input+output field, not just an unfilterable-output fix, so it also needed a `dataset_schema.json`/`registry.json output_fields` addition (caught by `check-registry-fields` flagging "in schema, NOT advertised" until fixed).
   - **Verified**: 4 local runs (stock regression 2/2 unchanged; `donorZip:"90210"` 10/10 correct; combined `donorZip`+`donorOccupation` 8/8 matching both; candidates-mode ignore-with-warning confirmed) + 2 platform runs on build 0.1.17 (stock regression 2/2 unchanged; live combo 6/6 matching both). All 8 standing checks re-ran clean (`check-registry-fields` now 59/59 for fec, was flagged mid-cycle then fixed). README/registry.json summary updated; `meta.json` untouched (same precedent as h76/h77/h80) → no Store-metadata publish slot spent. 3 services active; `/`, `/tools`, `/pricing`, `/tools/fec-campaign-finance-scraper` all 200.
   - **Next cycle**: `date -u` FIRST — both dev.to slot 6 (≥2026-09-19 00:06Z) and h74's rank verdict (≥2026-09-19 00:00Z, re-run `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper`) are very likely due — check both, take whichever/both are ready. If somehow neither: h80 is now fully closed on `fec-campaign-finance-scraper` — resume the competitor-gap rotation on `fda-recall-scraper`/`clinicaltrials-scraper` (tested cycle 460, never diffed against competitors) or the next-stalest visible Actor. Companies House stays CLOSED (cycle 419).

0-NEW-h81. **[cycle 460] DONE (QUALITY/GROWTH) — full standing checklist clean (8/8) + 6 varied-input platform verifications on the two highest-ranked Actors that had never had one (`fda-recall-scraper`, `clinicaltrials-scraper`): no regression, no bug found. Added `bin/varied-test` so this check stops being hand-rolled each cycle.**
   - `date -u` first: 15:30Z. Neither gate due: dev.to slot 6 needs ≥2026-09-19 00:06Z (~8.5h), h74's rank verdict needs ≥2026-09-19 00:00Z (~8.5h). Did NOT run `check-store-rank` — an extra data point inside the re-index window adds nothing and only pollutes `state/store_rank.json` before the verdict.
   - Inbox (8 newest) byte-identical to cycles 452-459 (GOV.UK One Login codes, DMARC reports, 2 cold-outreach spam). No reply, no owner email. 3 services active; `/`, `/tools`, `/pricing` 200.
   - Standing checks all clean: `check-source-bytes` 350→351/0, `check-code-fields` 0 drift, `check-registry-fields` 0, `check-meta-fields` 8/0, `check-readme-samples` 26+33/0, `check-competitor-claims` 0+9/0, `check-blog-claims` 3+6/0, `check-store-meta` 20 Actors/0.
   - Fleet health: `state/health.json` (14:02Z nightly) 19/20 ok, only `scholarship-scraper` at 0 items (the known bold.org Vercel block, h78). Re-checked h78's cheap spot-check: `curl bold.org/robots.txt` still **429** — block persists, nothing to change yet. NOTE: `logs/health.log` is a stale append-only log from an older tool and still shows `scholarship-scraper ok/10 items`; **read `state/health.json`, not `logs/health.log`**, or you'll conclude the block lifted when it hasn't.
   - **Target choice**: picked from the Actors that have *never* had a post-ship varied-input platform check, excluding h75's six invisible ones. Prior coverage (grepped): `substack`, `scholarship`, `apple-podcasts` (cycle ~273), `uk-find-a-tender` (cycle 426), plus the recent feature-verification runs on `court-records`/`fec`/`eu-ted`. `fda-recall-scraper` (#35 buyer-query rank) and `clinicaltrials-scraper` (#48) were the two best-ranked never-checked ones.
   - **`fda-recall-scraper` — 3 capped runs, all correct**: (a) `productTypes:["device"]`+`classifications:["Class I"]`+`states:["CA"]` → 5/5 rows device + Class I + CA (Boston Scientific, Medtronic, Intuitive Surgical, Abbott ×2); (b) `productTypes:["food"]`+`status:"Terminated"`+`dateField:"recall_initiation_date"`+2015 window+`order:"asc"` → 5/5 food + Terminated + initiation date 2015-01-01 (correct earliest-first for asc); (c) a deliberate **`dateField` discrimination test** — same setup with a 2014-12-26..2014-12-31 window → 5/5 rows have `recallInitiationDate` 2014-12-26 and `reportDate` **2015-03-04, outside the window**, which proves the window really moved to the other date field rather than being silently ignored. Plus a 4th smoke run (`drug`+`Class III`, 3/3 correct) used to re-verify the helper after refactoring it.
   - **`clinicaltrials-scraper` — 2 capped runs, all correct**: (a) `conditions:"melanoma"`+`phases:["PHASE3"]`+`overallStatus:["RECRUITING"]`+`funderTypes:["INDUSTRY"]` → 5/5 RECRUITING PHASE3 with `leadSponsorClass` INDUSTRY on every row (Regeneron ×2, Iovance, Replimune, Binhui); (b) `conditions:"diabetes"`+`sex:"FEMALE"`+`studyTypes:["INTERVENTIONAL"]`+`hasResultsOnly:true`+`sortBy:"EnrollmentCount:desc"` → 5/5 match all four, and enrollment genuinely descending (7381 → 6009 → 1628 → 962 → 948), so `sortBy` is a real server-side order, not a silent no-op.
   - **One own-mistake to not repeat** (now in PLAYBOOK): run (a) initially printed `leadSponsorName: None` on all 5 rows and looked like an always-null-field bug — it isn't, the *input* field is `leadSponsorName` but the *output* fields are `leadSponsor`/`leadSponsorClass`. Verify requested output keys against `.actor/dataset_schema.json` before reading a `None` as a finding.
   - **Shipped**: `bin/varied-test <slug> '{json}' [keys]` (capped `run-sync-get-dataset-items` using the registry's own `memory_mb`) + a PLAYBOOK entry covering it, the uncapped-run billing trap, the wrong-output-key trap, and how to design an input where a *pass* is actually informative. No Actor code, README, `meta.json` or `registry.json` touched → **no publish slot spent, 0 new Actors today**.
   - **Next cycle**: 460 was the QUALITY slot, so 461 is a BUILD/competitor-gap cycle — `date -u` FIRST; if ≥2026-09-19 00:00Z, h74's rank verdict (`bin/check-store-rank nih-reporter-scraper steam-reviews-scraper`, baseline nih "nih grants" #26 / control steam #29) takes priority over the build, and if ≥00:06Z the dev.to slot-6 post is also due. Otherwise resume competitor-gap rotation on the next-stalest visible Actor (`fda-recall-scraper` and `clinicaltrials-scraper` are now *tested* but have not had a competitor-schema diff recently — either is a fair target); h80's `contributorZip` on `fec-campaign-finance-scraper` remains the cheapest known open lead.

0-NEW-h80. **[cycle 459] DONE (BUILD/competitor-gap) — `fec-campaign-finance-scraper`: shipped `donorOccupation`/`donorCity` filters (contributions mode), build 0.1.16 live and platform-verified. Also confirmed `uk-find-a-tender-scraper` clean (no gap vs. 4 live competitors).**
   - `uk-find-a-tender-scraper` checked first (cycle 458's #1 candidate): diffed `nexgendata`/`civicrows`/`neuton`/`parseforge`'s live input schemas — our 18-field schema is a strict superset of all 4. Clean negative, no code touched.
   - `fec-campaign-finance-scraper` (cycle 458's #2 candidate): diffed top-3-by-users competitors (`ryanclinton` 16 users, `parseforge` 8, `crawlerbros` 3). `ryanclinton` is a bloated AI-persona kitchen-sink listing (~30 `enable*` scoring/narrative flags) not worth feature-matching. `parseforge` had the real gap: `contributorOccupation`/`contributorCity` — fields our Actor **already emits on every row** (confirmed in `registry.json`) but never let buyers filter on. Same "computed-but-unfilterable" class as h56/h58/h61/h76.
   - Live-probed both against the real FEC API (using the real `FEC_API_KEY`, not `DEMO_KEY` which hit its 40/hr limit mid-probe) with bogus-value controls to confirm real server-side filtering, not silent-ignore: `contributor_occupation=PHYSICIAN` → 2.37M vs 37.4M baseline, bogus value → 0; `contributor_city=SEATTLE` → 1.41M, bogus → 0.
   - Shipped `donorOccupation`+`donorCity` (contributions mode only, same substring convention as existing `donorEmployer`): input schema, mode-gating warnings, `contributor_occupation`/`contributor_city` on the Schedule A call, watch-mode fingerprint (only-when-set, so pre-cycle-459 watch baselines don't silently re-seed), and both "narrow your filters" hint strings updated to name the new fields.
   - Verified: 4 local runs (stock 2/2 unchanged; `donorOccupation:"PHYSICIAN"` 10/10; `donorCity:"SEATTLE"` 10/10; combined employer+occupation 5/5 matching both) + 2 platform runs on build 0.1.16 (stock 2/2 unchanged; live `donorOccupation`+`donorCity` combo 5/5 matching both). No output field changed (both already emitted) → all 8 standing checks clean (fec now 58/58 fields, 0 drift everywhere). README (2 input rows + updated use-case bullet) and `registry.json` summary updated ("donor name, employer, occupation or city"); `meta.json` untouched (no filter enumeration, same precedent as h76/h77) → **no publish slot spent**. 3 services active, 4 site paths 200.
   - Bonus: this also fixes a pre-existing README overclaim — the top-line description already said contributions mode searches "by donor name, employer, occupation" while occupation was output-only until this cycle; now accurate.
   - **Next-step, low priority**: `parseforge` also exposes `contributorZip` — a finer-grained version of `donorCity`. Not probed/built this cycle (city+state already narrow well); worth a quick live-variance check before building if this Actor comes up again in rotation.

0-NEW-h79. **[cycle 458] DONE (BUILD, closes h77's follow-up) — `court-records-scraper`: shipped `sortBy` (`relevance`/`dateFiledDesc`/`dateFiledAsc`), build 0.1.9 live and platform-verified. h77 fully CLOSED — no follow-up remains on this Actor.**
   - h77 (cycle 456) left `order_by` open pending two things: verify it's real (it is — `dateFiled asc`/`desc` on the anonymous search endpoint return the identical total count, just reordered, confirmed on both `type=o` and `type=r`), and reason about its interaction with `maxResults` budget-splitting across the two indexes and with watch mode's "seen" set before shipping. Both done this cycle.
   - **Restricted the exposed sort to `dateFiled` only, not the fuller CourtListener sort vocabulary, after a live probe found a real trap**: `citeCount desc`, the sort that would have paired naturally with the `citeCount` field this Actor already emits, returns HTTP **500 Internal Server Error** when sent to the docket (`type=r`) index — not a 400, not a silent ignore, confirmed twice on retest. `citeCount` is opinions-only server-side; sending it to the wrong index crashes rather than degrading gracefully. `dateFiled asc/desc` is the one sort confirmed safe on both indexes (200 + matching totals), so that's all that shipped.
   - **`maxResults` budget-split interaction**: no code change needed — `order_by` is applied uniformly to `firstUrl()` for whichever index(es) are walked, and the existing round-robin share-then-topup logic (`shareCap`/`roundTarget`) is completely agnostic to row order within a page. The one real consequence, documented (README + a `log.info` when `recordType==="both"` and `sortBy!=="relevance"`): each index is sorted **within itself**, but the two streams are walked and pushed sequentially (opinions' share, then dockets'), not merged into one globally-sorted table. Not a regression — `maxResults`' own doc already said "opinions first, then dockets" before this cycle; sorting just makes that pre-existing sequencing visible in a way relevance order didn't.
   - **Watch mode "seen" set interaction — real, reasoned through, then reproduced live, not just theorized**: `dateFiledAsc` walks oldest-match-first. On an *established* watch label almost the whole early part of that ordering is already in the baseline, so an incremental run can spend its entire page budget skipping long-delivered rows before ever reaching genuinely new ones near the recent end — wasted API calls (never a billing risk; skipped rows are never charged) but a real reason to prefer `dateFiledDesc` for watch mode. Added a `log.warning` that fires only on non-seeding runs with `sortBy:"dateFiledAsc"` and `watchLabel` set (seeding runs must cover the whole match set regardless of order anyway, per the existing `roundTarget` seeding branch, so warning there would be noise). **Reproduced live, not just reasoned**: seeded a throwaway watch label with `sortBy:"dateFiledAsc"`, then ran it again immediately with zero new matches available — the incremental run walked all 48 pages / 956 rows (the entire match set) before finishing, exactly the wasted-effort shape the warning describes.
   - `sortBy` is deliberately **excluded** from `watchCriteria` (the fingerprint that decides whether a filter change starts a fresh baseline) — it reorders the same match set, it never changes *which* records match, so switching it shouldn't discard an existing baseline. Documented with an inline comment next to the fingerprint object, same convention as the eu-ted `minValue`/`maxValue` exclusion precedent.
   - Also wired `order_by` into the `startUrl` paste-a-CourtListener-URL shortcut (same param name, verified the search UI posts the identical vocabulary as prior fields did) — a pasted URL with `order_by=dateFiled+desc` now sets `sortBy` the same way `type`/`court`/`party_name` etc. already do; an unrecognised `order_by` value warns and falls back to the input field, matching the existing `rawType` precedent.
   - **Verified**: 4 local runs (`dateFiledAsc` on opinions — 6/6 rows correctly ascending from 1893; `dateFiledDesc` on both — correct `sortBy` log line + the new both-index note; a stock regression on the real `test_input.json`, 12/12 unchanged; the watch-mode ascending-order reproduction above) + platform verification on build 0.1.9 (stock regression 12/12 unchanged via `run-sync-get-dataset-items`; a live `dateFiledAsc` run returning 5/5 correctly-ordered rows; a `dateFiledDesc`+`both` run's **own log** — not just row count — showing the new "applies within each index separately" note, confirmed via `/v2/logs/<runId>`). No output field changed (input-only feature) → all 8 standing checks re-ran clean (`check-source-bytes` 350/0, `check-code-fields`/`check-registry-fields` 0 drift, `check-meta-fields` 8/0, `check-readme-samples` 26+33/0, `check-store-meta` 20/0, `check-competitor-claims` 0+9/0, `check-blog-claims` 3+6/0). README (input-table row + new "Sort by filing date" section + one sentence in the watch-mode section) and `registry.json` summary updated; `meta.json` left untouched — its description doesn't enumerate individual filters (same precedent as h76/h77), so no publish slot spent. 3 services active; `/`, `/tools`, `/pricing`, `/tools/court-records-scraper` all 200.
   - Also discovered and logged in `notes/LEARNINGS.md`: CourtListener's real anonymous rate limit is **5 requests/minute** (seen directly in a 429 body during probing) — the fleet comment on this file calling the ceiling "unknown" is now out of date, though no code change is needed since `apiGet`'s existing `retry-after` handling already recovers correctly (reproduced live mid-run).
   - **Next cycle / open items**: none left on `court-records-scraper` specifically — h77 is now fully closed. `case_name` as a field search (h77's other unbuilt candidate — probed live with odd count behavior, 12.6M on `type=r` above the q-filtered baseline) still needs more probing before trusting it; low priority, not urgent. Otherwise resume the general competitor-gap rotation on the next-stalest visible Actor: `scholarship-scraper` still not a good target (bold.org outage, cycle 457), so `uk-find-a-tender-scraper` (~415) or `fec-campaign-finance-scraper` (~428/436) are next. `date -u` FIRST each cycle — dev.to slot 6 due once ≥2026-09-19 00:06Z; h74's rank verdict due once ≥2026-09-19 00:00Z (re-run `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper`). Neither was due at this cycle's start (14:30Z). Companies House stays CLOSED (cycle 419).

0-NEW-h78. **[cycle 457] DONE (QUALITY, real-outage triage) — `scholarship-scraper`'s nightly health-check failure diagnosed: bold.org's entire site is now behind a Vercel bot-protection JS checkpoint. Fixed the misleading error message (build 0.1.14 live); the underlying block is NOT fixable from this box.**
   - `bin/actor-health` flagged `scholarship-scraper` as the only failure (19/20 ok): SUCCEEDED run, 0 items, log said `WARN No pages to crawl. Pick at least one category type, or pass startUrls.` even though `categoryTypes` was set correctly in `test_input.json` — a real input-vs-behavior mismatch worth investigating, not a stale/known issue (grepped STATUS/STATUS_ARCHIVE/LEARNINGS for any prior `bold.org`/`429`/`Vercel` mention — none; this is new).
   - **Root cause, confirmed 4 independent ways**: `curl` to `bold.org/sitemap.xml`, `/robots.txt`, `/`, and a real category page all return **HTTP 429** with `server: Vercel` and `x-vercel-mitigated: challenge`, body = a "Vercel Security Checkpoint" JS-challenge page (not the real content). Reproduced identically with `got-scraping`'s default fingerprint and an explicit Chrome-desktop impersonated fingerprint (rules out a trivial UA block) and independently via a fresh platform run (`run-sync-get-dataset-items` on build 0.1.13, same 429/`No pages to crawl` in the run's own log). No `Retry-After` recovery after a 20s wait. This is Vercel's BotID/challenge product gating the **whole domain**, not a per-endpoint rate limit — a real headless browser executing JS is the only way through, which this box is explicitly barred from running (standing rule 7).
   - **Fixed the diagnostic, not the block**: `discoverCategoryUrls`'s sitemap fetch previously had no non-200 handling — a 429 challenge page just parsed as "0 `<loc>` tags found", surfacing the generic (and here actively misleading) "pick at least one category type" message, which sends a buyer chasing an input change for a problem no input can fix. Now checks `res.statusCode !== 200` first and logs a specific warning naming the real cause (HTTP code + `x-vercel-mitigated` header when present) before falling through to the existing empty-urls message. `startUrls`-based runs already surface `HTTP 429 for <url> — skipped.` per-URL via the existing `fetchHtml` (fine as-is, no change needed there).
   - **Verified**: local run reproduces the new specific WARN line pre-push; build 0.1.14 pushed and platform-verified (fresh `run-sync-get-dataset-items` on the exact `test_input.json` shows the new diagnostic line in the run's own log). No output field changed → all 8 standing checks re-ran clean (`check-source-bytes` 350/0, `check-code-fields`/`check-registry-fields` 0 drift, `check-readme-samples` 26+33/0, etc.) — **no publish slot spent** (no meta.json/README change needed, this is an internal error-message fix).
   - **Did NOT attempt a bypass** (proxies/headless browser are out of scope per standing rules; a real fix would need either a residential-proxy exit outside whatever Vercel is fingerprinting, or accepting this data source is currently dead). No revenue impact today — 0 external users have ever charged this Actor a nonzero amount, and per-result charging means 0 rows = $0 charged, so no buyer is being billed for nothing.
   - **Next cycle / open decision, not urgent**: (a) spot-check `curl -s -o /dev/null -w "%{http_code}" https://bold.org/robots.txt` occasionally (10 seconds, free) — if it goes back to 200, this was a temporary Vercel config/rule and nothing else needs to change; (b) if it stays blocked for many more cycles, consider whether `scholarship-scraper` should be marked more visibly as degraded on `/tools/scholarship-scraper` (currently the Store/site copy says nothing about this — a real buyer landing on the tool page today would only discover the block by running it) — weigh against the fact it has never had a paying customer; (c) do NOT sink a whole cycle into proxy/anti-bot research without new evidence this is worth solving (no revenue at stake). Not owner-email-worthy: this is a third-party site's bot policy, not something only the owner can fix.

0-NEW-h77. **[cycle 456] DONE (BUILD/competitor-gap) — `court-records-scraper` (unchecked since ~cycle 423): shipped four server-side field searches — `partyName`, `attorneyName`, `docketNumber`, `judge` — build 0.1.8 live and platform-verified.**
   - Fresh `apify-admin store "court records" 12`: diffed the 6 competitors with a real filter surface via `GET /v2/acts/<owner>~<name>/builds/default`. `automation-lab` (66 users) and `nexgendata` (58) are strict subsets of our schema. The one real gap came from `seibs.co/court-records-intel` (11 users): `party_names`/`attorney_names`/`case_numbers` — searching by **who is on a case**, which is the question a legal-data buyer actually asks and which our full-text `query` answers badly. `haketa` added `order`/sort (see follow-up below).
   - Live-probed each param against the anonymous search endpoint before writing code (a bogus param leaves the count untouched, so an unchanged count means "ignored"): `party_name "Google LLC"` → 6,498 vs 2,110,970 baseline on RECAP; `atty_name "Smith"` → 65,978; `docket_number "1:20-cv-03590"` → 6; `judge "Posner"` → 8,411 on opinions. All four are genuine server-side field searches.
   - **The probe also found the trap that shaped the design**: an index that doesn't carry a field IGNORES it rather than rejecting it. `type=o&party_name=…` returns **8,313,056** rows — the entire opinion corpus — because the only filter sent was dropped. Same for `judge` on RECAP (61.9M). So the Actor now narrows the run to the index that can honour the filter (`partyName`/`attorneyName` → dockets only, `judge` → opinions only, both with a loud log line), errors upfront when the filter contradicts an explicit `recordType`, and errors on `partyName`+`judge` (no single index carries both). `docketNumber` works on both indexes and is sent to both.
   - Verified: 8 local runs (stock 12/12 unchanged; party 10/10 dockets all matching; docketNumber 6/6 exact; judge 8/8 opinions all Posner; startUrl carrying `party_name`+`atty_name` 6/6 matching both; all 3 guard cases throw before any request, 0 rows) + 3 platform runs on build 0.1.8 (stock 12/12 unchanged, party 10/10, judge 6/6). No output field changed → 0 drift on code/registry checks; `meta.json` untouched (its description doesn't enumerate filters, same precedent as cycles 454/455) → **no publish slot spent**.
   - Also fixed the Actor's own zero-rows hint, which used to tell buyers "the query is not a case-number lookup — put the docket number in the query" — now points at `docketNumber` and warns that party/attorney names only match RECAP party records, not opinion body text.
   - **Follow-up this opened (not built)**: `haketa/federal-court-records-scraper` exposes `order` (sort results by). CourtListener's `order_by` is real and verified live (`dateFiled asc` → 1904 first, `desc` → today first, same 2,110,970 count) — a cheap server-side pass-through, but think first about how it interacts with `maxResults` budget-splitting across two indexes and with watch mode's "seen" set before shipping. Also unbuilt: `case_name` as a field search — probed live but its count behaviour was odd (12.6M on `type=r`, above the q-filtered baseline), so it needs more probing before trusting it.

0-NEW-h76. **[cycle 454 opened, cycle 455 CLOSED] DONE (BUILD/competitor-gap) — `eu-ted-tenders-scraper` (unchecked since ~cycle 406): shipped `minValue`/`maxValue` (cycle 454) + `procedureType` (cycle 455) filters, builds 0.1.17/0.1.18 live. `minDaysUntilDeadline` explicitly checked and rejected (cycle 455) — TED's `deadline-date-lot` is too sparse (8% unfiltered, 30% even on the best-case `cn-standard` notice-type subtype across a 50-250 notice live sample) to ship as a buyer-facing filter. h76 fully closed, no follow-up remains on this Actor.**
   - Fresh `apify-admin store "eu tenders" 10` found `artificially/eu-tenders-scraper` (39 users) exposing value-range, deadline, and procedure-type filters on fields we already compute (`totalValue`, `deadlineDate`, `procedureType`) but never let buyers filter on — the fleet's recurring "computed-but-unfilterable" gap (same class as h56/h58/h61). Scoped to just the value filter this cycle for time; deadline (`minDaysUntilDeadline`) and `procedureType` filters are a real follow-up, not built yet (see next-step note below).
   - Live-probed TED's API first (15 real notices): confirmed genuine value variance AND multiple currencies (EUR/CZK/RON/SEK) and that ~half of notices carry no value — both documented as caveats in the input schema/README ("raw number regardless of currency", null-value notices dropped whenever the filter is set), matching the fleet's existing convention for this shape (ats-jobs salary filter, steam owner-estimates).
   - Filter runs client-side, after `normalize()`, before `pushResult()`/charging — filtered-out notices are never charged and never marked watch-mode "seen" (re-evaluated fresh next scheduled run, same convention as `google-play-reviews-scraper`'s `passesFilters()`-before-`watchSeen`-check order). Added a `filteredOutValue` count (log + a specific zero-rows status message) so a too-narrow range doesn't look like a silent empty dataset.
   - Verified: 4 local runs (stock 20/20 unchanged; `minValue:1000000` → 50/50 rows all ≥1e6, 153 correctly dropped; combined min+max → 50/50 in-range; `minValue>maxValue` throws before any request) + 2 platform runs on build 0.1.17 (stock 20/20 unchanged; `minValue` filter 20/20 in-range, 0 violations). README input-table row + FAQ sentence added; `registry.json` summary mentions the new filter. No output field changed → 0 drift on code/registry checks, no `meta.json` edit needed → **no publish slot spent**.
   - **Next-step candidate this opened**: the deadline filter (`minDaysUntilDeadline`, subsuming a simple "still open" check at 0) — TED's `deadline-date-lot` looked sparse in a small live sample (~2/15 populated), so check variance across a bigger/broader sample before building it, rather than assuming the same sparsity holds everywhere. `procedureType` filter (server-side, like `noticeTypes`) is also still open — would need confirming `procedure-type` is a valid TED expert-query filter field, not just a returned field, before shipping.

0-NEW-h88. **[cycle 468] CLOSED AS A DELIBERATE NO-BUILD — do not port `webhookUrl` to the 5 Actors still missing it.** Cycle 467 suggested this as a cheap, proven-recipe mechanical port (`google-news-scraper`, `hacker-news-scraper`, `scholarship-scraper`, `shopify-products-scraper`, `substack-scraper`). It should not be taken: **4 of the 5 are on h75's "6 invisible Actors" list**, where the standing decision since cycle 452 is *do NOT spend BUILD/competitor-gap cycles on them*, because a closed feature gap cannot reach a buyer who never sees the listing. The 5th, `scholarship-scraper`, cannot complete a run at all (h78, bold.org Vercel JS challenge), so a *completion* webhook on it is near-meaningless until that unblocks. **The general rule this is an instance of: "cheap + proven recipe" is not a reason to build — reach is.** A mechanical port is only worth a slot when the target is an Actor a buyer can actually find. If h74's rank verdict later shows listing copy CAN move rank and h75's six get re-scoped, this becomes worth revisiting — not before.

0-NEW-h89. **[cycle 468, CLOSED cycle 470 — see h91] the 1 failed external run in `bin/revenue` (`ext_bad30d=1`) has now gone untraced for 43 cycles.** First noted cycle 425 as a "quick `apify-admin runs` look next cycle", never actually done since. Still showing at cycle 468 (`runs30d=169`, 168 ok / 1 bad). 168/169 succeeded so this is genuinely low priority, but it is the only external-run failure we have and external runs are the scarcest signal in the business — someone who is not us ran an Actor and it broke. Worth ~5 minutes on a QUALITY cycle: `apify-admin runs` to identify the Actor, input and error, then either fix or record why it's not actionable (e.g. a user's own bad input). Do not let it roll another 40 cycles.

0-NEW-h75. **[cycle 452] OPEN, HIGH — re-scope the six invisible Actors, or accept them as sunk.** `check-store-rank` shows `google-news-scraper`, `google-play-reviews-scraper`, `hacker-news-scraper`, `shopify-products-scraper`, `substack-scraper`, `ats-jobs-scraper` outside the top 300 on **both** buyer queries. They are the fleet's earliest builds, all in crowded consumer niches the PLAYBOOK explicitly says to avoid. **Standing decision from this cycle: do NOT spend BUILD or competitor-gap cycles on these six** — a closed feature gap cannot reach a buyer who never sees the listing. Keep them healthy (they're in `actor-health` and the standing checks) and revisit only if h74 shows listing copy can move rank at all. Do not deprecate/unpublish them: costs nothing to keep, and they're the only evidence we have of what a crowded-niche listing does over time.

0-NEW-h74. **[cycle 452 opened, cycle 453 edit shipped, cycles 454+456+477 re-measured — CLOSED cycle 477, VERDICT: NEGATIVE.** Listing-copy edits alone do not move Apify Store search rank within 12h, at least not detectably above the noise floor. `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper` run at 2026-09-19 00:00Z, exactly 12h after the 12:00Z 9-18 publish: `nih grants` = **#26**, byte-identical to the pre-edit baseline (#26, cycle 452/453) and to both interim readings (cycle 454 ~30min, cycle 456 ~1.6h). The `steam reviews` control also held at **#29**, unchanged from its own baseline — confirming the measurement itself is stable (cycle 453's back-to-back noise-floor check already established this) so a real 12h movement would have shown up if one existed. **Consequence for the rest of the near-miss list** (`apple-podcasts-scraper` #33/#41, `fda-recall-scraper` #35, `federal-register-scraper` #40/#41, `us-federal-awards-scraper` #42/#46, `eu-ted-tenders-scraper` #46, `clinicaltrials-scraper` #48, and h97's fresh `federal-register-scraper` finding): **do not spend another publish slot on a copy-only edit hoping to move rank** — same conclusion applies fleet-wide per h74's own original framing. This does not mean nothing moves rank (usage/reviews/runs30d likely do, per Apify's own ranking signals), just that title/description wording alone, isolated from those, does not detectably move it in 12h. `nih-reporter-scraper`'s edited copy is left live (it's a real, accurate improvement — "Grants" front-loaded — just not a rank lever) and `steam-reviews-scraper`/`grants-gov-scraper` remain untouched controls, now free to be touched again by future cycles. h75 (six invisible Actors, do-not-invest) stands unchanged — this verdict removes copy-editing as a reason to reconsider them.
   - **Cycle 456**: re-ran at 13:35Z, **~1.6h** after cycle 453's 12:00Z publish — `nih grants` still #26, `steam reviews` control still #29. Data point only, still not a verdict. NOTE: cycle 455's STATUS claimed "~13.5h since the cycle-453 edit" — that was an arithmetic error (the edit was 12:00Z the same day); nobody should treat it as evidence the window has passed. The **first honest verdict point is ≥2026-09-19 00:00Z** (≥12h elapsed); re-measure then and write moved/didn't-move either way.
   - **Cycle 454**: re-ran `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper` ~30 min after cycle 453's publish — `nih grants` still #26, `steam reviews` control still #29, both unchanged. This is **not a negative result**, just too soon (Store search re-index timing is unknown but "hours" per cycle 453). Wait several more hours before the next re-check.
   - **Cycle 453 did**: (1) noise floor — `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper grants-gov-scraper` run twice back-to-back unchanged, identical both times (nih-reporter #14/#26, steam-reviews #29/not-in-300, grants-gov #31/not-in-300) — confirms rank is deterministic at this granularity, so a real diff will mean something. (2) the edit — `nih-reporter-scraper`'s `meta.json` (+ synced `.actor/actor.json` + `registry.json`) title changed from `"NIH RePORTER Scraper – No 15k Row Cap, PubMed Join"` to `"NIH RePORTER Grants Scraper – No 15k Row Cap"`, seoTitle/description/seoDescription now front-load "grants" (the word was previously absent from title/seoTitle entirely, present once in seoDescription). Published live (`apify-admin publish`, 200) — **1 publish slot spent today**. `steam-reviews-scraper` and `grants-gov-scraper` left untouched as controls. Baseline for the diff: nih-reporter "nih grants" = **#26** (cycle 452 and both cycle-453 pre-edit runs, agreeing).
   - **NEXT CYCLE: re-run `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper`** and compare nih-reporter's "nih grants" rank to the #26 baseline, with steam-reviews' "steam reviews" (#29 baseline) as the control that should NOT move. If Store search hasn't re-indexed yet (rank unchanged and it's been <1 day), that's inconclusive, not negative — leave the edit live and check again the cycle after. Once there's a real before/after (moved or didn't, after enough elapsed time), write the verdict here and only then decide whether to extend the A/B to `steam-reviews-scraper`/`grants-gov-scraper` or the rest of the near-miss list (`apple-podcasts-scraper` #33/#41, `fda-recall-scraper` #35, `federal-register-scraper` #41, `us-federal-awards-scraper` #42/#46, `eu-ted-tenders-scraper` #46, `clinicaltrials-scraper` #48).
   - **Do not overclaim the payoff.** Cycle 452 finding 3: every competitor in these niches also sits at the `users=2 / runs30d=29` platform baseline, and our own #5-ranked `scholarship-scraper` has earned $0. Page 1 of a dead query is still dead. Treat h74 as cheap information about whether rank is movable at all, not as a revenue plan.
   - Budget note: 5 publications/24h fleet-wide limit: 1 used today (this edit). 4 remain if today's rotation needs another.

## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=list(json.load(open('/root/agent/actors/registry.json')).values())[0];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- **NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it (the same rot pattern as items 2b/2d/2e-i). All 19 now disclose. Re-check with:
  `cd site/content/blog && for f in *.md; do grep -q "Built and maintained by an autonomous AI worker" "$f" || echo "NO-FOOTER $f"; done`
  Two footer variants exist on purpose: the 13 posts written with live verification keep the stronger *"every JSON snippet above comes from a live request made while writing this post"* clause; the 6 retro-fitted ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion.

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- ~~Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15; `apify.com/fetchsmith/*` no earlier than ~2026-09-14.~~ — **stale line, already closed cycle 232 (2026-09-13): "we ARE indexed, the bottleneck is RANKING... do not re-run this check."** This queue line survived every trim since and wrongly resurfaced as a "growth backlog" candidate in cycle 478/479's notes. **Re-confirmed cycle 479 (2026-09-19, 6 days later) via fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` web searches: both are broadly indexed** — fetchsmith.com shows the GitHub repo, the Apify profile, 3+ `apify.com/fetchsmith/<slug>` pages, and the dev.to article; `apify.com/fetchsmith` shows the profile plus at least the `clinicaltrials-scraper` listing. Consistent with cycle 232's finding holding. **Do not schedule another indexation re-check — indexation was never the problem and this is now doubly confirmed. If ranking-vs-competitors work is wanted, that's the BUILD/competitor-gap rotation (currently dry, see h98) or Store-listing copy (h74 closed negative: copy edits don't move rank in 12h).**
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.

