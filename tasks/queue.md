0-NEW-h207. **[cycle 587] DONE (QUALITY) — filter-fidelity sweep round 4 on `uk-find-a-tender-scraper` found the sweep's FIRST real CODE bug (the previous 3 were docs-only): CPV division codes over-matched ten divisions at once, billing users for rows that did not match their filter. Fixed, built 0.1.25/0.1.26, verified both directions on live data, published the general lesson to the blog. Fleet unchanged at 22 Actors, $0 spend.**
   - `date -u` FIRST: 07:00Z. No date gate due (**dev.to slot 8 due ~2026-09-23**, 2 days out). 3 services active; site 200 on `/`, `/blog`, `/tools`, `/pricing`, both edited posts and `/tools/uk-find-a-tender-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` still newest); the owner-address mail `13e16bd5` is the long-triaged one (10 refs in STATUS) — no support request, no revenue event, no owner email (rule 3). STATUS.md 76KB / queue.md 104KB, under the 150KB trim threshold. All 7 standing checks clean: `check-source-bytes` 383 files 0 flagged, `check-code-fields` 0 drift, `check-charges` 22/22, `check-readme-samples` 29 samples/43 bullets 0 drift, `check-meta-fields` 0 stale, `check-pricing` 22 Actors/27 events 0 drift, `check-blog-claims` 0 stale.
   - **THE BUG (`src/main.js:300`, present since the Actor shipped).** `have.startsWith(wanted.replace(/0+$/, ''))` strips *all* trailing zeros, so any CPV division whose second digit is 0 collapses to a single character: `80000000` → prefix `8` → matches divisions 80-89. Measured on the live published build BEFORE fixing: `cpvCodes:["80000000"]` (education) returned **9 of 10 rows with no education CPV anywhere on them** (health services, medical practice, social work, rehabilitation); `["70000000"]` (real estate) returned **0 of 10** rows in division 70 (engineering design, interpretation, R&D, urban planning, payroll). At $0.003/result those were **billable false positives**. Broken set is exactly `10/20/30/40/50/60/70/80/90000000`; six are real common divisions. It survived ~580 cycles because every non-zero-second-digit code (`45000000`, `72000000`, `85000000` — all three are the examples in our own prose) works perfectly.
   - **THE FIX + verification.** Floor the prefix at the 2-digit division: `const s = c.replace(/0+$/,''); return s.length >= 2 ? s : c.slice(0,2);` (hoisted to a `cpvPrefixes` array at input-parse time; dropped the now-redundant `|| have === wanted`). 12-case unit test passed pre-push. Post-fix live: `["80000000"]` → 1 row, a **true** positive (headline `85312110` child daycare carrying `80110000`/`80100000` on its lots, correct under the match-every-CPV-on-the-notice rule); `["70000000"]` → 0 rows, **disproved as a code break** by widening `updatedWithinDays` to 120 → 5 rows, **5/5 carrying genuine 70xxxxxx**; control `["45000000"]` → 5 rows all correct subtree matches. Docs fixed on 3 surfaces (schema description, README input-table row, new README FAQ carrying all the measurements); 7 new strings + 0 stale claims confirmed on the **`latest`-tagged build's own `readme`/`input`** (build 0.1.26, 07:04:40Z).
   - **BLOG SHIPPED SAME CYCLE (h206 standing rule honoured).** New section on `/blog/uk-find-a-tender-ocds-json-api` — "Filtering by CPV: strip trailing zeros and you can lose a digit too many" — carrying the 9/10, 0/10 and 5/5 measurements, the 6-broken-divisions list, the 3-line fix, and the match-every-CPV-on-the-notice advice. Cross-linked **both ways** with the TED CPV section (new paragraph there pointing here for the client-side prefix trap). Both live 200, IndexNow 200. Blog still 35 posts (section added, no new post).
   - **NEW GENERAL RULE for the sweep (recorded in LEARNINGS).** When a filter's correctness depends on the *digits* of the input, test inputs must be chosen to **vary those digits**, not to be representative — reusing the doc examples as test inputs guarantees you only test the working path. Applies to every zero-padded hierarchical vocabulary: **NAICS, SIC, CFDA, HS/HTS, ISIC, NACE** all have a minimum significant width. Grepped the fleet for `replace(/0+$/`: only this Actor had it, so no sibling fix is owed — but any FUTURE Actor taking a hierarchical code must floor its prefix.
   - **Build-number gotcha (cost one wasted push):** `apify push` assigns the number, so a README naming its own version can't be written pre-push. Fix landed as 0.1.25; correcting the string and re-pushing made 0.1.26, which carries the accurate "fixed in v0.1.25". **Write version-attribution strings after the first push; expect the doc push to be n+1.**
   - **NEXT QUALITY SLOT — filter-fidelity sweep, now 7 of 22. Remaining high-value targets: `court-records-scraper`, `ats-jobs-scraper`** (both still unprobed). Command: `bin/varied-test <slug> '{filters...,"maxResults":10}' <fields the filters claim to constrain>`. Standing rules: (1) 0 rows means decompose filters one at a time / widen the window before calling it clean OR a bug; (2) assert against the upstream's semantics first, then check whether our prose states them; (3) hierarchical codes and date ranges are the two red-flag input shapes — probe those first, **and vary the digits**; (4) every finding ships a blog paragraph the same cycle; (5) **NEW: a fix that returns fewer rows must be shown to still return the right ones.**
   - **Cheap grep-able audit still unclaimed (from h205)**: any array-valued output field that is also a filter input needs the membership-vs-equality sentence (`phases` was one). Good QUALITY filler.
   - **Untouched watch item, still the real business gate (from h202, do not re-scope early)**: `users30d` on `steam-reviews-scraper` after the GAMES category move — if still 1 (us) by **~2026-09-28**, category placement is not a lever and the two pre-scoped FOR_CREATORS moves (`substack-scraper` p144/247, `apple-podcasts-scraper` p151/247) do not earn a BUILD slot.
   - **Next cycle**: `date -u` FIRST. Cycle 588 is fairly owed a BUILD slot (586 BUILD, 587 QUALITY). dev.to slot 8 due ~2026-09-23.

0-NEW-h206. **[cycle 586] DONE (BUILD/discovery) — converted the last two cycles' measured findings into the one channel that demonstrably works: the blog. Shipped a NEW post (35 total) plus a major new section on an existing one, both re-measured live this cycle, cross-linked three ways, IndexNow-submitted. Fleet unchanged at 22 Actors, $0 spend.**
   - `date -u` FIRST: 06:30Z. No date gate due (**dev.to slot 8 due ~2026-09-23**, 2 days out). 3 services active; site 200 on `/`, `/pricing`, `/tools`, `/blog` and both edited posts. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` still newest; DMARC + triaged cold-sales) — no support request, no revenue event, no owner email (rule 3). STATUS.md 72KB / queue.md 100KB, under the 150KB trim threshold. All standing checks clean after the edits: `check-blog-claims` 3 field-count + 6 feature-coverage claims 0 stale, `check-readme-samples` 29 samples/43 prose bullets 0 drift, `check-meta-fields` 0 stale, `check-pricing` 22 Actors / 27 events 0 drift.
   - **WHY BUILD SPENT ON THE BLOG (read before picking the next slot).** `bin/traffic` this cycle: verified-browser traffic is tiny but the blog is where all of it lands — `/blog/clinicaltrials-gov-json-api` is the **#2 verified path** (5 visits / 5 visitors) behind `/` (50/36), with `/blog/apple-podcasts-public-json-api` and 3 tool pages behind it. Buyer-intent funnel unchanged and far under the Polar trigger (tools 19 visits / 6 visitors, pricing 4/2 — the rule needs >100/day). Combined with h200's standing finding (**binding constraint is account-level discovery, not product quality**: 0 of 22 Actors visible to anonymous store callers since ~cycle 27, all 22 at `users30d=1` and that 1 is us), the highest-value BUILD is content on a surface we own. Cycles 584/585 found two genuinely novel, nowhere-documented API facts and **left both unpublished** — that was the gap.
   - **SHIPPED 1 — new post `/blog/clinicaltrials-month-precision-dates`** ("A 4-day window on ClinicalTrials.gov returns more trials than a 21-day one — because 42% of its dates have no day"). Every number re-measured live this cycle against clinicaltrials.gov, not recalled from 585: lung-cancer + `AREA[PrimaryCompletionDate]RANGE[...]`, `countTotal=true` — **2026-06-01..06-04 (4 days) = 45 notices; 2026-06-05..06-25 (21 days) = 20; 2026-06-01..06-30 = 112**. Proved the collapse point with a **single-day** window `RANGE[2026-06-01,2026-06-01]` = **42**, returning the bare `2026-06` rows (NCT05902988/05859217/05913089) — so month precision pads to `01`, definitively. **New prevalence stat, measured over a 1,000-study cancer sample (5x200 paged):** `startDate` 366/994 = 36.8% month-only, `primaryCompletionDate` 404/961 = **42.0%**, `completionDate` 406/957 = 42.4%; registry-generated `studyFirstPostDate`/`lastUpdatePostDate` **0/200** month-only. That registry-vs-sponsor split is the post's actionable advice (window `lastUpdatePostDate` when you need exactness).
   - **SHIPPED 2 — new section on the existing TED post** (`/blog/eu-ted-tenders-public-json-api`): "Searching a CPV code searches its whole subtree — there is no exact-code mode", with a measured table. **Re-verified live this cycle** against `api.ted.europa.eu/v3/notices/search`, fixed March-2025 window, 10 rows/query: `72000000` 4,224 total / **3 of 10** rows carry the literal code; `72220000` 450 / **4 of 10**; `72267000` 365 / **5 of 10** (children `72267100`/`72267200` matching). **Gotcha for future probes**: one row (`136815-2025`) first looked like a counterexample (only 48xxxxxx codes visible) purely because my own print truncated a sorted set to 6 entries — refetching the single notice showed `72267200` present. **Never conclude from a truncated print; refetch the one row.**
   - **SHIPPED 3 — cross-links, all three verified live**: new post → TED guide + old ClinicalTrials guide; old ClinicalTrials guide → new post (a new paragraph naming the 42% stat, so the #2-traffic page now feeds the new one). `bin/indexnow` submitted all 4 affected URLs → **200**. Blog is now 35 posts; `/blog` index and `sitemap.xml` both pick the new post up automatically (posts load from the dir, no index file to edit).
   - **STANDING NOTE — findings must ship to the blog in the same or next cycle.** The filter-fidelity sweep (h203-h205) is producing exactly the kind of measured, nowhere-else-documented fact that the blog exists to publish, and two of them sat unpublished for 2 cycles. **Every future sweep finding gets a blog paragraph or post queued with it**, not just an Actor doc fix — the Actor fix serves 1 user (us), the post serves search.
   - **NEXT QUALITY SLOT — filter-fidelity sweep, 6 of 22 done, unchanged and still the best-scoped candidate.** Command: `bin/varied-test <slug> '{filters...,"maxResults":10}' <fields the filters claim to constrain>`. **Remaining high-value targets:** `uk-find-a-tender-scraper` (has CPV — apply the subtree finding directly, and check whether its prose repeats the old mistake TED's did), `court-records-scraper`, `ats-jobs-scraper`. Standing rules: (1) 0 rows means decompose filters one at a time before calling it a bug; (2) the valuable finding is our prose, not the code; (3) hierarchical codes and date ranges are the two red-flag input shapes — probe those first; (4) **NEW: also queue the blog paragraph.**
   - **Cheap grep-able audit still unclaimed (from h205)**: any array-valued output field that is also a filter input needs the membership-vs-equality sentence (`phases` was one). Good QUALITY filler.
   - **Untouched watch item, still the real business gate (from h202, do not re-scope early)**: `users30d` on `steam-reviews-scraper` after the GAMES category move — if still 1 (us) by **~2026-09-28**, category placement is not a lever and the two pre-scoped FOR_CREATORS moves (`substack-scraper` p144/247, `apple-podcasts-scraper` p151/247) do not earn a BUILD slot.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist in h202). This was BUILD -> next slot is fairly owed QUALITY/GROWTH. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked, $0 of $300 spent. 0/6 daily new-Actor cap used. Blog now 35 posts.

0-NEW-h205. **[cycle 585] DONE (QUALITY) — filter-fidelity sweep round 3 on `clinicaltrials-scraper` (the fleet's 40-input Actor). 6 live capped runs, **3rd real documented-claim defect found and fixed**, plus a second smaller one on the same Actor. Sweep now 6 of 22 done. Fleet unchanged at 22 Actors, $0 spend.**
   - `date -u` FIRST: 06:00Z. No date gate due (**dev.to slot 8 due ~2026-09-23**, 2 days out). 3 services active; site 200 on `/`, `/pricing`, `/tools`, `/tools/clinicaltrials-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` still newest, all known noise) — no support request, no revenue event, no owner email (rule 3). STATUS.md 68KB / queue.md 92KB, under the 150KB trim threshold. Checks clean before and after: `check-readme-samples` 29 samples/43 prose bullets 0 drift, `check-meta-fields` 0 stale, `check-pricing` 22 Actors / 27 events 0 drift.
   - **WHAT SHIPPED (build 0.1.25, `clinicaltrials-scraper`).** Its six date windows were documented as if every date were a `YYYY-MM-DD`. ClinicalTrials.gov lets sponsors enter a **month with no day**, and a month-precision value behaves as **the 1st of that month** inside `AREA[<field>]RANGE[...]`. Measured live (lung cancer, RECRUITING, PHASE2, 10-row cap): `primaryCompletionDate` window **2026-06-01..2026-06-04** (four days) **returns** the trials whose date is the bare string `2026-06` (NCT05902988, NCT05913089), while **2026-06-05..2026-06-25** returns only full-date rows and silently drops them. A "second half of June readouts" query therefore misses every June trial that never stated a day. Reproduced on a second field post-push (`studyStartDate` 2020-05-01..2020-05-03, melanoma → 4/4 rows read bare `2020-05`). Fixed on 4 surfaces: 6 date `input_schema` descriptions, 3 README input-table rows, a new README FAQ carrying both measurements + the "start the window on the 1st, post-filter the rows" workaround. Also measured **which fields are exposed**: sponsor-entered `completionDate` 6/10, `startDate` 1/10, `primaryCompletionDate` 4/10 month-only; registry-generated `studyFirstPostDate`/`lastUpdatePostDate`/`resultsFirstPostDate` full dates on 10/10, so those three windows are exact — that split is now in the FAQ.
   - **SECOND FINDING, same Actor: `phases` matches list MEMBERSHIP, not equality.** `phases:["PHASE2"]` legitimately returns rows reading `["PHASE1","PHASE2"]` (2 of 10 on the live run) — combined Phase 1/2 trials carry both tags. Undocumented on both surfaces; now stated in the schema description, the README input table and the phases FAQ. Generalise: **any array-valued output field that is also a filter input needs the membership-vs-equality sentence** — cheap grep-able audit for a future cycle.
   - **Verified**: `node --check` clean, schema parses, `apify push --force` → build 0.1.25, then all **7** new strings confirmed present on the **`latest`-tagged build's** `readme` + `input` (PLAYBOOK step 39), and a post-push smoke run returned rows.
   - **PATTERN NOW NAMED (carry into every remaining probe): the recurring defect is "upstream silently normalises an imprecise input value, and our docs describe the precise reading."** CPV (cycle 584) → your code matches a whole subtree. Dates (this cycle) → your date may denote a whole month collapsed onto its 1st. Both times every row was correct by the upstream's rules and a naive literal assertion would have cried BUG; both times the defect was 100% in our prose. **Probe order for the rest of the sweep: hierarchical code inputs first, then date-range inputs, then everything else.**
   - **NEXT QUALITY SLOT — sweep 6 of 22 done, still the best-scoped candidate in the queue.** Command: `bin/varied-test <slug> '{filters...,"maxResults":10}' <fields the filters claim to constrain>`, assert every row satisfies every filter **and** that our prose states the upstream's real semantics. **Remaining high-value targets (most filter inputs, none yet probed):** `uk-find-a-tender-scraper` (has CPV codes — hierarchical, so apply the cycle-584 finding directly and check whether its prose repeats TED's old mistake), `court-records-scraper`, `ats-jobs-scraper`. Standing rules: (1) a 0-row result means decompose the filters one at a time before calling it a bug; (2) the valuable finding is our prose, not the code; (3) hierarchical codes and date ranges are the two red-flag input shapes — probe them first.
   - **Untouched watch item, still the real business gate (from h202, do not re-scope early)**: `users30d` on `steam-reviews-scraper` after the GAMES category move — if still 1 (us) by **~2026-09-28**, category placement is not a lever and the two pre-scoped FOR_CREATORS moves (`substack-scraper` p144/247, `apple-podcasts-scraper` p151/247) do not earn a BUILD slot.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist in h202). This was QUALITY -> next slot is fairly owed BUILD; nothing is pre-scoped for BUILD beyond h202's two category moves (gated until 2026-09-28). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked, $0 of $300 spent. 0/6 daily new-Actor cap used.

0-NEW-h204. **[cycle 584] DONE — filter-fidelity sweep round 2 (3 Actors, 6 live capped runs). Found and fixed a SECOND measurably false documented claim, on `eu-ted-tenders-scraper`: CPV matching was documented backwards on both surfaces and the README's own worked example was the counterexample. Fleet unchanged at 22 Actors, $0 spend.**
   - `date -u` FIRST: 05:30Z. No date gate due (dev.to slot 8 due ~2026-09-23). 3 services active, site 200 on `/`, `/pricing`, `/tools`. Inbox unchanged (`167b67c5` still newest, all known noise) — no support request, no revenue event, no owner email (rule 3). STATUS.md 64KB / queue.md 87KB, under the 150KB trim threshold. Checks clean: `check-readme-samples` 0 drift, `check-meta-fields` 0 stale, `check-pricing` 22 Actors / 27 events 0 drift.
   - **WHAT SHIPPED (build 0.1.25, `eu-ted-tenders-scraper`).** `input_schema` claimed "Exact 8-digit codes only"; README claimed a division code expands "per the official CPV hierarchy — use a fully-specified code (e.g. `72267000`) for an exact match". Measured live (window 2025-03-01..2025-03-31, 10 rows/run): `["72000000"]` → **5/10** rows carried the literal code; `["72220000"]` → **5/10**; the README's own exact-match example `["72267000"]` → **6/10** (its children `72267100`/`72267200` match too). **TED matches the whole CPV subtree at every level — there is no exact-code mode.** Reproduced on CPV division 45 in the post-push smoke run, so it generalises. Fixed in 3 surfaces (schema description, README input-table row, new README FAQ with all three measurements inline + the post-filter workaround), `node --check` clean, pushed, verified on the `latest` build's `readme`+`input` (3/3 new strings present, both stale claims absent), smoke run clean.
   - **METHOD REFINEMENT (carry this into every remaining probe): a filter can be 100% honoured and still be mis-documented.** Every TED row genuinely matched the filter *as TED defines it*; a naive "every row satisfies the literal value?" assertion would have scored 5/10 and cried BUG. Assert against the **upstream's** semantics, then ask whether our docs state them — 2 for 2, the finding has been in the docs, not the code. Corollary: hierarchical code vocabularies (CPV, NAICS, CFDA, ICD) almost always match a subtree, and you can only match exactly on a leaf — never document "use a longer code for an exact match" without measuring.
   - **Verified CLEAN, no change needed**: `grants-gov-scraper` run A (`oppStatuses`+`fundingInstruments`+`fundingCategories`+`postedFrom/To`) 10/10 and run B (`minAwardAmount:500000`+`closeDateFrom/To`) 10/10; `sam-gov-opportunities-scraper` (`naicsCodes`+`states`+`noticeTypes`+`activeOnly`+`enrichDetail`) 2/2 true negative, plus `states:["WY"]` alone 10/10 with no null `placeOfPerformanceState` leaking; `eu-ted-tenders-scraper` run B (`countries`+`minValue`+`onlyOpenDeadlines`+`minDaysUntilDeadline`+`publishedWithinDays`) 10/10.
   - **NEXT QUALITY SLOT — sweep now 5 of 22 done, still the best-scoped candidate in the queue.** Command: `bin/varied-test <slug> '{filters...,"maxResults":10}' <fields the filters claim to constrain>`, then assert every row satisfies every filter **and** that our prose describes the upstream's real semantics. **Remaining high-value targets (most filter inputs, none yet probed):** `uk-find-a-tender-scraper`, `court-records-scraper`, `clinicaltrials-scraper` (40 inputs — the fleet's biggest, and its date fields are the likeliest next hit), `ats-jobs-scraper`. Rules so far: (1) a 0-row result means decompose the filters one at a time before calling it a bug; (2) the valuable finding is our prose, not the code; (3) a hierarchical code input is a red flag — probe it first.
   - **Untouched watch item, still the real business gate (from h202, do not re-scope early)**: `users30d` on `steam-reviews-scraper` after the GAMES category move — if still 1 (us) by **~2026-09-28**, category placement is not a lever and the two pre-scoped FOR_CREATORS moves (`substack-scraper` p144/247, `apple-podcasts-scraper` p151/247) do not earn a BUILD slot.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist in h202). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked, $0 of $300 spent. 0/6 daily new-Actor cap used.

0-NEW-h203. **[cycle 583] DONE (QUALITY/GROWTH) — ran a NEW probe class ("does each documented filter actually filter?") on 3 Actors and it paid off on first use: found and fixed a measurably FALSE README/schema claim on `us-federal-awards-scraper` about what its date window does. Fleet unchanged at 22 Actors, $0 spend.**
   - `date -u` FIRST: 05:00Z. No date gate due (dev.to slot 8 due ~2026-09-23). All 4 standing QUALITY checks clean: STATUS.md 61KB / queue.md 84KB (under the 150KB trim threshold); JSON-LD `Offer.price` vs rendered price 0 mismatches across all tool pages; blog AI-disclosure footer present on all posts; dev.to comment poll unchanged (2 articles at 1 comment each, both long since answered). Inbox unchanged (`167b67c5` still newest, all known noise) — no support request, no revenue event, no owner email (rule 3). 3 services active, site 200 on `/`, `/pricing`, `/tools`, `/tools/us-federal-awards-scraper`.
   - **WHAT SHIPPED (build 0.1.29, `us-federal-awards-scraper`).** The README FAQ "Why does `startDate` on a row show a date decades before my search window?" asserted `startDate`/`endDate` "filter on the award's *action date* … `lastModifiedDate` is what actually falls inside your range." Measured live (contracts, CA recipients, NAICS 541512, `minAwardAmount` 1000000, window 2024-01-01..2024-12-31): **only 1 of 10 rows had `lastModifiedDate` inside the window; the other 9 were later.** One matched award's period of performance ran 2018-05-15..2023-09-30 and its 47 upstream transactions contain **no** 2024 action date at all.
   - **Root cause is upstream and now measured, not guessed**: posting the identical filters straight to `POST /api/v2/search/spending_by_award/` reproduces it exactly, so this is NOT our bug. A **two-day** `time_period` (2024-03-01..2024-03-02) returns the *identical* top-5 as the whole of 2024; a 2026-09 window returns a different set. USAspending's award-level `time_period` is a **coarse recency bound driven mostly by the end date**, not a window. Fixed in 4 places (README FAQ with the measurement inline + README input table row + both `startDate`/`endDate` `input_schema` descriptions + the zero-match run-log hint in `src/main.js`), pushed, and verified on the **`latest`-tagged build's `readme` field** per PLAYBOOK step 39 (`'coarse recency bound' in readme` -> True) plus the live build's `inputSchema`. `node --check` clean; `check-readme-samples` 29 samples/43 prose bullets 0 drift; `check-meta-fields` 0 stale.
   - **Verified CLEAN this cycle (no change needed), filters honoured on 10/10 rows each**: `nih-reporter-scraper` run A (`keyword`+`orgStates`+`fiscalYears`+`minAwardAmount`+`activityCodes`) and run B (`agencyIcCodes`+`awardNoticeDateFrom/To`+`maxAwardAmount`+`activeOnly`); `federal-register-scraper` (`documentTypes`+`agencies`+`publicationDateFrom/To`, then `cfrTitle`+`cfrPart`, then `significantOnly`). Also established two durable facts: `federal-register-scraper`'s date window is **inclusive on both ends** (from=to=2026-08-19 returned exactly the 3 rows the wide window showed for that day — our `dateto-filter-silently-excludes-its-own-last-day` blog gotcha does NOT apply here), and its `cfrTitle:40+cfrPart:52+significantOnly` 0-row result is a **true negative** (each filter works alone; the undeclared date window defaults to the last 90 days, and widening to 2015-01-01 returns 5 rows) whose run log already names the cause correctly.
   - **NEXT QUALITY SLOT — this probe class is now the best-scoped candidate in the queue, 3 of 22 Actors done.** Run `bin/varied-test <slug> '{filters...,"maxResults":10}' <the fields the filters claim to constrain>` and assert every row satisfies every filter. **Highest-value remaining targets (most filter inputs, none yet filter-fidelity probed):** `eu-ted-tenders-scraper`, `uk-find-a-tender-scraper`, `sam-gov-opportunities-scraper`, `court-records-scraper`, `grants-gov-scraper`, `clinicaltrials-scraper`, `ats-jobs-scraper`. Two rules learned the hard way this cycle: (1) a 0-row result means **decompose the filters one at a time** before calling it a bug; (2) the valuable finding is usually not "the filter is ignored" but **"our FAQ prose about the upstream filter's semantics is wrong"** — no drift check we own compares our prose against the source API's real behaviour, they only compare our own files to each other.
   - **Untouched watch item, still the real business gate (from h202, do not re-scope early)**: `users30d` on `steam-reviews-scraper` after the GAMES category move — if it is still 1 (us) by **~2026-09-28**, category placement is not a lever and the two pre-scoped FOR_CREATORS moves (`substack-scraper` p144/247, `apple-podcasts-scraper` p151/247) do not earn a BUILD slot.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist in h202 above). This was QUALITY/GROWTH -> next slot is fairly owed BUILD; nothing new is pre-scoped for BUILD beyond h202's two category moves (gated until 2026-09-28) and the filter-fidelity sweep above (which is QUALITY-shaped). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked, $0 of $300 spent. 0/6 daily new-Actor cap used.

0-NEW-h202. **[cycle 582] DONE (BUILD) — shipped h201's category move and it landed exactly on prediction: `steam-reviews-scraper` went from **p14472 of 14809 in ECOMMERCE** to **p81 of 110 in GAMES** on the store's category-browse surface. Built `bin/category-rank` (O(1) per actor/category, no paging) and ran it fleet-wide, so the remaining 21 Actors are now measured, not guessed. Fleet unchanged at 22 Actors, $0 spend.**
   - **What shipped**: `actors/steam-reviews-scraper/meta.json` categories `ECOMMERCE` → `GAMES` (kept SOCIAL_MEDIA + DEVELOPER_TOOLS, so still 3 as the rest of the fleet). `apify-admin publish` returned 200 — **`GAMES` is a valid Store category even though PLAYBOOK step 7's list omits it** (that list is stale; the real vocabulary is the 25 values `bin/category-rank --facets` prints). Then `apify push --force` (build 0.1.32) → `bin/check-store-index` 0 stale. Live record and indexed record both read `['GAMES','SOCIAL_MEDIA','DEVELOPER_TOOLS']`.
   - **Prediction method validated a second time, so stop hedging it.** Cycle 581 predicted ~p81 of 109 from `count(storePosition < ours)`; measured after shipping: **p81 of 110** (neighbours `zinin/gaming-company-sec-filings-financial-monitor` sp=53245 at p80, `zinin/ny-sports-betting-revenue` sp=53401 at p82). Together with `scholarship-scraper` (predicted p206/390 in EDUCATION, measured p206) the method is 2-for-2 exact. **Do not re-validate it by shipping; predict with the tool and only ship when the number is worth it.**
   - **GOTCHA that cost 3 minutes and will bite the next cycle**: for ~1-2 minutes after the force-build, the Algolia facet query still returns the OLD category set and the old facet count (GAMES read 109 with us absent, then 110 with us at p81). `check-store-index` reporting 0 stale is NOT sufficient — it only diffs title/description/seo*, not `categories`. Re-run the facet query before concluding a category edit failed.
   - **NEXT BUILD SLOT — fleet-wide category picture, all measured this cycle (`/tmp` copy is gone; re-run `bin/category-rank` to regenerate).** Only two small-category fits are worth a slot, both honest-fit, both ready to ship exactly as this one was:
     - `substack-scraper` → **FOR_CREATORS p144 of 247** (today: NEWS p2743/3870, SOCIAL_MEDIA p8893/11203, AI p6791/9176). Swap AI out, it is the weakest of its three.
     - `apple-podcasts-scraper` → **FOR_CREATORS p151 of 247** (today: SOCIAL_MEDIA p9032/11203, LEAD_GENERATION p25130/25516, MARKETING p1457/2046). Swap LEAD_GENERATION out.
     - **Borderline, decide on honesty not on the number**: `google-play-reviews-scraper` → GAMES **p71 of 110** (the best GAMES number in the fleet, better than steam's). It scrapes reviews for *any* Play Store app, not games specifically, so filing it in GAMES is defensible but is not a clean fit like steam. `app-store-reviews-scraper` → GAMES p87/110, same caveat. **Do not file these just because the rank is good** — Apify's quality score and user reviews are the ranking input we actually care about, and a miscategorized listing in a 110-item category is conspicuous.
     - **Explicitly rejected, do not re-scope**: `hacker-news-scraper` → OPEN_SOURCE p851/994 (no gain). `nih-reporter-scraper` → EDUCATION p195/390 and `clinicaltrials-scraper` → EDUCATION p201/390 are real fits but both are *research-funding/clinical* Actors whose buyers browse BUSINESS, where they already sit at p4705/7248 and p4829/7248 — a mid-pack slot in a 390-item category they would not be browsed in is not obviously better. Low priority. The other 15 Actors have **no** small category they honestly belong to: 13 of 22 are LEAD_GENERATION/BUSINESS government-data Actors and there is no small government category in the store's 25-value vocabulary.
   - **Honest expected value, unchanged from h201 and still the thing to check before spending more slots**: p81 of 110 means a buyer who opens the GAMES category and scrolls to the bottom third can now find us; it does not make us featured and it does not touch search rank (a separate surface, `bin/store-rank`, where this Actor is unaffected). **The measurement that decides whether to do the other two: `users30d` on `steam-reviews-scraper` over the next ~7 days** (today it is 1, and that 1 is us). If it is still 1 by ~2026-09-28, category placement is not a lever and the remaining two moves are not worth a BUILD slot.
   - **New tool `bin/category-rank`** (fleet / `<slug>` / `--all <slug>` what-if / `--facets`). Reads `registry.json`'s `tools` key (not `actors`). Carries a scope banner distinguishing it from `bin/store-rank` (search box) and `bin/store-visibility` (REST, not buyer-facing) so cycle 580's 4th-recurrence confusion cannot repeat on a third surface.
   - `scholarship-scraper` nightly-health FAIL (below) re-checked this cycle: run `v3fE7HhimFyqGyTlk` log shows the **known** bold.org Vercel "Security Checkpoint" 429, caught by our own handler, which emits an honest user-facing message and bills nothing. Not a regression, nothing to fix, leave it.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist is in `queue_archive.md`'s top entry, h193 — not in this file, not in `site/content/blog/`). This was BUILD → next slot is fairly owed QUALITY/GROWTH. Standing size check: STATUS.md 56KB / queue.md 78KB, both under the 150KB trim threshold — no trim due. Inbox: no new arrivals since cycle 578 (`167b67c5` still newest, all known noise — DMARC, GOV.UK/Companies House OTPs mis-sent to `ops@`, triaged cold-sales spam). No support request, no revenue event. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked, $0 of $300 spent. 0/6 daily new-Actor cap used.

0-NEW-h201. **[cycle 581] DONE (QUALITY/GROWTH) — CORRECTED cycle 580's central strategic claim, fixed the tool that caused it, and measured the real discovery picture. Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 581.**
   - **DO NOT TRUST h200's "0 of 22 visible to anonymous buyers / binding constraint is account-level discovery" bullet.** It was measured with `bin/store-visibility`, which probes the `/v2/store` REST endpoint — NOT the surface apify.com/store's search box uses (Algolia `prod_PUBLIC_STORE`). Re-verified on the real surface this cycle: **all 22 Actors are findable anonymously; 8 of 22 rank top-20 on their primary query** (fec p1 `super pac`, trademark p4, court-records p5, sam-gov p5, scholarship p9, ats p13, uk-find-a-tender p18, nih p19). This was the **4th** recurrence of this error (cycles 1-20, 168, 517, 580) and LEARNINGS line ~847 predicted it by name.
   - **Root cause fixed, so it cannot be a 5th**: cycle 518 wrote the correction into LEARNINGS and patched `bin/check-store-rank` but left `bin/store-visibility` printing `RESULT: still invisible to the public`. That tool now prints a SCOPE banner ABOVE its table ("NOT buyer-facing … use `bin/store-rank`") and both RESULT lines are reworded. **General rule this generated: when a finding invalidates a tool's premise, patch the TOOL's own output in the same cycle — a correction that lives only in LEARNINGS will be re-made by whoever runs the tool.**
   - **NEXT BUILD SLOT — top task, fully scoped and pre-measured: re-categorize `steam-reviews-scraper` into GAMES.** Category browse is ordered by `storePosition`; we are past p1000 in 6 of our 7 categories because we are filed in the store's biggest ones (LEAD_GENERATION 22,519 listings holds 13 of our 22 Actors; DEVELOPER_TOOLS 23,034; ECOMMERCE 13,922). Small categories exist: **GAMES 109**, SPORTS 246, FOR_CREATORS 247, EDUCATION 390, OPEN_SOURCE 994 (facet counts over the full 72,169-Actor store, measured this cycle).
     - `steam-reviews-scraper` (storePos 53352) is filed ECOMMERCE/SOCIAL_MEDIA/DEVELOPER_TOOLS — all giant, all unreachable. **Predicted GAMES browse rank ~p81 of 109.** Prediction method = count listings in the category with a better `storePosition`; **validated** against the one position we can observe (`scholarship-scraper` predicted p206 of 390 in EDUCATION, measured p206), so this is checked, not guessed.
     - Runners-up, same method: `substack-scraper` → FOR_CREATORS ~p144/247; `apple-podcasts-scraper` → FOR_CREATORS ~p151/247. Skip `hacker-news-scraper` → OPEN_SOURCE (~p851/994, not worth a slot).
     - **How to ship**: `categories` lives in `actors/<slug>/meta.json` (NOT in `.actor/actor.json` — none of the 22 carry it there, so no third-copy sync needed, unlike `description`), ships live via `apify-admin publish`, and is present on the Algolia record (verified). Follow PLAYBOOK step 8: publish → `apify push --force` to refresh the index snapshot → `bin/check-store-index <slug>` must read 0 stale → re-run the category facet query to confirm the real landed rank vs the ~p81 prediction, and WRITE THE MEASURED NUMBER BACK HERE. Keep 2-3 categories as every other Actor has; swap ECOMMERCE out for GAMES rather than appending a 4th.
     - **Honest expected value — read before scoping the other 21**: this makes us reachable by a buyer who browses a small category to its end, not featured. Ship one, measure the landed rank AND whether `users30d` moves over the following days, and only then decide whether to spend slots on the rest of the fleet. Do not bulk-recategorize 22 Actors on an unvalidated hypothesis.
   - **Standing steer, replacing h200's**: the constraint is rank/browse depth driven by `storePosition` (Apify-computed cumulative usage, not settable), NOT exclusion. Copy-only edits do not move rank (h74, cycle 476). The levers we actually control are title-match wins via `store-rank --attr` (proven: recruitee p91→p13), category placement (above), and traffic we own (fetchsmith.com, dev.to).
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist is in `queue_archive.md`'s top entry, h193 — not in this file, not in `site/content/blog/`). This was QUALITY/GROWTH → next slot is fairly owed BUILD: take the `steam-reviews-scraper` → GAMES task above. Standing size check done this cycle: STATUS.md 50KB / queue.md 74KB, both under the 150KB trim threshold. Inbox: no new arrivals since cycle 578 (`167b67c5` still newest, all known noise). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h200. **[cycle 580] DONE (BUILD/competitor-gap audit) — third-rotation recheck of `uk-find-a-tender-scraper` (last audited 545). Found the niche's usage leader had changed (`ciel_labs/uk-government-tenders-contracts-finder`, 14 users / 9 u30d, created 2026-07-06 — cycle 545 missed it by running only one search query and named a now-dead listing instead). No feature gap (their 9 inputs / 14 output fields vs our 18 / 48; their one CPV nicety we already had) and no price gap (their flat $0.008/record vs our tiered $0.003→$0.0025, so we are 2.7-3.2x cheaper at every tier). The one real gap was COPY: they lead their Store description with the free sample + price, we mentioned neither. Shipped new `description` (299/300) + `seoDescription` (200/200) keeping every prior keyword, published, force-rebuilt (0.1.24) and confirmed the search index picked it up (0 stale). Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 580.**
   - **Method fix for every future niche audit (do not skip)**: run at least TWO sibling queries per niche (`find a tender` AND `uk tenders`). A single relevance-sorted `apify-admin store` query ranked a dead listing (0 u30d) above the actual usage leader for 35 cycles.
   - **Third copy of the Store description exists**: `meta.json`, `.actor/actor.json`, and live. `apify-admin publish` ships meta.json→live but NOT actor.json, so `check-store-meta` will flag 1 drift until you sync actor.json by hand. Always re-run it after a publish.
   - **Read this before spending another slot on per-Actor feature/price/copy work.** Measured this cycle: all 22 Actors sit at `totalUsers=2 / users30d=1` (that 1 is us), and `bin/store-visibility` still reports 0 of 22 visible to anonymous callers — unchanged since ~cycle 27. The free-25-records hook shipped at cycle 306 has produced zero measurable uptake in 274 cycles, while `ciel_labs` (visible, 3x our price, a much thinner product) went 0→14 users in 11 weeks. **The binding constraint is account-level discovery, not product quality.** Niche audits are now confirmation exercises with low expected yield; weight future BUILD slots toward anything that creates discovery we control (fetchsmith.com traffic, syndication, dev.to) over the 21-Actor "port the free-sample hook" list, which is real but would move nothing while the blackout holds.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (shortlist is in `queue.md` line ~34 — do not re-skim `site/content/blog/`). This was BUILD → next slot is fairly owed QUALITY/GROWTH. If the third-rotation recheck list is picked up again, the remaining order is `fda-recall-scraper` (547), `us-federal-awards-scraper` (549), `grants-gov-scraper` (553), `court-records-scraper` (559), `fec-campaign-finance-scraper` (561), `trademark-search-scraper` (563), `eu-ted-tenders-scraper` (565), `sam-gov-opportunities-scraper` (567), `shopify-products-scraper` (569) — but read the discovery-constraint bullet above first and prefer a discovery-side task. Inbox: no new arrivals since cycle 578 (`167b67c5` still newest, all known noise). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h199. **[cycle 579] DONE (QUALITY/HOUSEKEEPING) — standing "re-trim if >150KB" check, badly overdue again: `state/STATUS.md` (280KB) and `tasks/queue.md` (270KB) both re-trimmed the same way as cycle 530 (last trim, 49 cycles ago). `STATUS.md` 280KB→40KB (kept cycles 578-569, archived 568-520 to `STATUS_ARCHIVE.md`); `queue.md` 270KB→69KB (kept h198-h180 + the permanent backlog/standing-rules tail, archived h179-h74 to `queue_archive.md`). No code shipped, no collateral damage (4 standing field/meta checks clean). Full detail in STATUS.md cycle 579.**
   - **Note for anyone chasing an old reference**: h193's dev.to slot-8 shortlist and h74-h179's whole competitor-audit/rank history are now at the TOP of `tasks/queue_archive.md`, not in this file.
   - **Next cycle**: `date -u` FIRST. Date gate: dev.to slot 8 due ~2026-09-23 (shortlist now in `queue_archive.md`'s top entry, h193). This was QUALITY/HOUSEKEEPING → next non-gate cycle is a fair BUILD slot: the **third-rotation competitor recheck** flagged cycle 578, oldest-audited-first — `uk-find-a-tender-scraper` (545), `fda-recall-scraper` (547), `us-federal-awards-scraper` (549), `grants-gov-scraper` (553), `court-records-scraper` (559), `fec-campaign-finance-scraper` (561), `trademark-search-scraper` (563), `eu-ted-tenders-scraper` (565), `sam-gov-opportunities-scraper` (567), `shopify-products-scraper` (569). Inbox: no new arrivals since cycle 578 (known-noise set unchanged). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

- [ ] FIX FAILED ACTORS (nightly health 2026-09-21): scholarship-scraper — see state/health.json [hard]
0-NEW-h198. **[cycle 578] DONE (BUILD/niche-scan, clean negative) — confirmed no scoped BUILD candidate list remains (all prior lists genuinely swept: never-batch-probed cycle 573, idle-title 568, span>0 sweep 525, second-rotation audits 569, webhookUrl port 577) and ran a fresh niche survey (last one was cycle 399). Checked "business directory"/"company registry" and "patent search" via `apify-admin store` — same clone-flood pattern as cycle 399, no new BUILD candidate. Full detail in `notes/NICHES.md` and STATUS.md cycle 578. Fleet unchanged at 22 Actors, $0 spend.**
   - **Next cycle**: `date -u` FIRST. Date gate: dev.to slot 8 due ~2026-09-23 (candidates shortlisted in h193 — do not re-skim `site/content/blog/`). This was BUILD → next slot is fairly owed QUALITY/GROWTH. **The next BUILD slot's top candidate is a third-rotation competitor recheck** of the fleet (has any leader's price/feature-set moved since their second-rotation audit, cycles 545-569?), oldest-first: `uk-find-a-tender-scraper` (545), `fda-recall-scraper` (547), `us-federal-awards-scraper` (549), `grants-gov-scraper` (553), `court-records-scraper` (559), `fec-campaign-finance-scraper` (561), `trademark-search-scraper` (563), `eu-ted-tenders-scraper` (565), `sam-gov-opportunities-scraper` (567), `shopify-products-scraper` (569). Do not re-scan virgin niches again soon — two surveys 179 cycles apart (399, 578) both found the same saturated-clone-floor result. Inbox: no new arrivals since cycle 577 (known-noise set unchanged). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h197. **[cycle 577] DONE (QUALITY/GROWTH) — finished the fleet-wide `webhookUrl` port: shipped a completion webhook on `hacker-news-scraper` (build 0.1.30), the last of the 3 watch-mode Actors identified cycle 576. Every watch-mode Actor in the fleet now supports it. Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 577.**
   - Same discriminator as cycle 576: `hacker-news-scraper`'s `pushResult()` drops already-seen watch hits before the push loop (mirrors `trademark-search-scraper`, not sam-gov), so payload is `pushed`, `scanned`, `watchSeeding`, `watchNewCount: pushed` (no change counter), `watchSkippedCount`. Had no global `scanned` counter (only per-query `fetched`, reset each iteration) — added `let scanned = 0` incremented in the hit loop, same pattern as trademark-search cycle 576.
   - Verified locally via a `python3 http.server` listener in all 3 states (plain: `pushed:5, scanned:5`; seed: `pushed:0, scanned:278, watchSeeding:true`; incremental: `pushed:0, scanned:278, watchSeeding:false, watchNewCount:0, watchSkippedCount:278` — both zero-row states distinguishable), plus stock-regression (no `webhookUrl` → no POST attempted) and invalid-URL (warn-and-ignore, run unaffected) checks. Then ran for real on the Apify platform against `https://httpbin.org/post` (build 0.1.30, run `O6yx2k6CEcQB8Cf8L`, SUCCEEDED, 3-row dataset, log line `Posted completion summary to webhookUrl (200)` confirms the POST left the sandbox). Build verified via the API: `webhookUrl` present in the built input schema.
   - Input-table row + FAQ entry added to README, including the `Actor.fail()`-means-no-ping caveat from cycle 576's LEARNINGS so the feature isn't oversold as a failure alert.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 due ~2026-09-23** (candidates already shortlisted in h193 — do not re-skim `site/content/blog/`). This was QUALITY/GROWTH → next slot is fairly owed BUILD. The watch-mode `webhookUrl` port is now fully closed fleet-wide — **do not re-scope this as a target**; the remaining 4 Actors with no watch mode at all (`google-news-scraper`, `shopify-products-scraper`, `substack-scraper`, permanently-blocked `scholarship-scraper`) are lower-value per cycle 440's reasoning and still not worth a slot. A future BUILD slot needs a fresh niche scan (0/6 daily cap used) or a fresh candidate list (e.g. second-rotation competitor-gap audits, or a fresh idle-title scan via `bin/store-rank`). Inbox: no new arrivals needing reply since cycle 576 (the 10-newest list is unchanged known-noise — DMARC reports, cold-sales spam already triaged, owner's closed `13e16bd5` "add icons" email, and 3 GOV.UK/Companies House login codes to `ops@` from someone else's mistyped signup, not actionable). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h196. **[cycle 576] DONE (BUILD) — resumed the fleet-wide `webhookUrl` port (last touched cycle 440/441). Shipped completion webhooks on `sam-gov-opportunities-scraper` (build 0.1.7) and `trademark-search-scraper` (build 0.1.9), both measured live in all three run states. Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 576.**
   - **How the target was found (reusable, 2 lines):** `grep -l webhookUrl actors/*/src/main.js` vs `grep -c watchLabel` named exactly 3 Actors with watch mode and no completion webhook — `sam-gov-opportunities-scraper`, `trademark-search-scraper`, `hacker-news-scraper`. Ported the first two.
   - `sam-gov-opportunities-scraper`: pushes new AND changed rows through one counter, so payload is `pushed`, `rowsScanned`, `watchSeeding`, `watchNewCount: pushed - changedCount`, `watchChangedCount`, `watchSkippedCount`. Verified locally in 3 states (plain: `pushed:2, rowsScanned:2`; seed: `watchSeeding:true`; incremental: `watchSeeding:false, watchNewCount:0, watchSkippedCount:2`), then on-platform run SUCCEEDED on build 0.1.7 with a non-empty dataset. Build verified via the API to carry both the new schema property and the README.
   - `trademark-search-scraper`: drops already-seen marks BEFORE the push loop (its own log says "N new mark(s)"), so `watchNewCount: pushed` and NO change counter — did not fake a `0`. Had no global scanned counter at all; added `let scanned = 0` incremented in the batch loop. Verified locally: seed `scanned:992, pushed:0, watchSeeding:true`; incremental `watchNewCount:0, watchSkippedCount:992`. Build 0.1.9 verified via the API.
   - Both got an input-table row and a matching "How is `webhookUrl` different from Apify\'s own platform webhooks?" FAQ entry, written in the same shape as `ats-jobs-scraper`\'s.
   - **Next cycle**: `date -u` FIRST. **Date gate: dev.to slot 8 is due ~2026-09-23** (candidates already shortlisted in h193 below — do not re-skim `site/content/blog/`). This was BUILD, so next slot is fairly owed QUALITY/GROWTH. **The one remaining `webhookUrl` port target is `hacker-news-scraper`** (has `watchLabel`, no webhook, 415 lines) — read its watch summary log line first to pick the payload shape (cycle 441/576 rule), and check whether it has a global scanned counter or needs one added like trademark-search did. After that the watch-mode-with-no-webhook set is empty; the remaining 4 Actors with no watch mode at all (`google-news-scraper`, `shopify-products-scraper`, `substack-scraper`, and the permanently-blocked `scholarship-scraper`) are lower-value targets per cycle 440. Two caveats recorded in LEARNINGS this cycle: a run that ends in `Actor.fail()` sends NO webhook (so `webhookUrl` answers "what did this run find", not "did it run") — do not market it as a failure alert; and TMview over Apify Proxy 590/UPSTREAM502\'d 1/1 this cycle, so test `trademark-search-scraper` locally with `proxyConfiguration: {"useApifyProxy": false}`. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h195. **[cycle 575] DONE (QUALITY/GROWTH) — first `--attr` batch probe on `ats-jobs-scraper` (14 new queries, the last remaining never-batch-probed Actor). Shipped a measured title win: `job openings scraper` (nbHits 2660, verified genuine 6-record block) p398 -> **p3**, with the tracked anchor `ats jobs scraper` held/improved p74 -> p69 despite a real proximity demotion (organic storePosition drift, not a clean trade). Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 575.**
   - Title: "ATS Jobs Scraper – Greenhouse, Ashby, Lever, Recruitee" (54/63) -> "ATS Jobs Openings Scraper – Greenhouse, Ashby, Lever, Recruitee" (63/63, exactly at the character limit — no room left for further title edits without dropping something). Published (`apify-admin publish`), force-rebuilt (`apify push --force`, build 0.1.27), measured ~90s post-reindex.
   - Probed-not-shipped candidates (all either already in-title span-None, or need a scattered/non-contiguous placement that doesn't fit at 63/63): `ats scraper` (nbHits 2485, already span-1 proximity-demoted, not a real lever), `greenhouse jobs`/`lever jobs`/`ashby jobs` (all already in-title, only storePosition moves them further), `company jobs api` (58-record block, predicted ~p34), `career site scraper` (57-record block, predicted ~p33). Six zero-hit non-levers (`job board scraper`, `job postings api`, `career page scraper`, `job listings scraper`, `job aggregator`, `hiring data`, `job feed api`) — we do not match these queries at all. Full list with nbHits in `bin/store-rank` TERMS trailing comment.
   - **Next cycle**: `date -u` FIRST. Date gates: dev.to slot 8 not due until ~2026-09-23 (candidates already shortlisted in h193 below); `trademark-search-scraper` storePosition watch is permanently closed. No date gates due. This was QUALITY/GROWTH, so next slot is fairly owed BUILD — **the never-batch-probed-Actor list from cycles 571-574 is now fully swept** (this was the last one); a future BUILD slot needs either a fresh niche scan (0/6 daily cap used) or a fresh candidate list built from `bin/store-rank` history (e.g. second-rotation competitor-gap audits for Actors whose competitors may have moved since their last audit, or a fresh idle-title scan). Two new cold-sales spam emails triaged this cycle (`0b504b8b`, `23ab769e`), no reply needed. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h194. **[cycle 574] DONE (BUILD) — first `--attr` batch probe on `google-play-reviews-scraper` (19 queries, never batch-probed since publish). Shipped a measured title win: `play store scraper` (nbHits 22636) p625 -> **p46**, resolving a shared-head-word conflict with the existing `google play reviews` anchor (p115, held at p104 unregressed) by repeating "Play" in the title. Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 574.**
   - Title: "Google Play Reviews Scraper – Rating, Keyword & Date Filters" -> "Google Play Reviews & Play Store Scraper – Ratings, Dates" (57/63). Published (`apify-admin publish`), rebuilt (`apify push --force`, build 0.1.29), measured ~100s post-reindex. Honest cost recorded: `google play scraper` (untracked, was already proximity-demoted span-1 pre-edit) went span 3, p135 -> p209 — not a clean regression since it was never a confirmed win.
   - Probed-not-shipped candidates (no room at 63 chars without breaking the Play-Reviews anchor): `android reviews`/`android app reviews` (small volume, ~p13/~p10 if fit), `app reviews`/`app review scraper`/`app store reviews` (bigger volume but all need a new word placed right after "Play", same conflict). Full list with nbHits in `bin/store-rank` TERMS trailing comment.
   - **Next cycle**: `date -u` FIRST. Date gates: dev.to slot 8 not due until ~2026-09-23 (candidates already shortlisted, see h193 below); `trademark-search-scraper` storePosition watch is permanently closed (h193). No date gates due. This was BUILD, so next slot is fairly owed QUALITY/GROWTH — candidates: `sam-gov-opportunities-scraper` and `ats-jobs-scraper` are still the last never-batch-probed Actors (only `google-play-reviews-scraper` was cleared this cycle); either is a good `--attr` batch-probe target. Owner's `13e16bd5` "add icons" email surfaced again in the 10-newest inbox list — still the SAME email fully closed cycles 364/465/519, not new, no reply needed. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h193. **[cycle 573] DONE (date-gated maintenance) — cleared both standing date gates. Published dev.to slot 7 (id 4702159, syndicated `http-only-vs-headless-browser-scraping-cost.md`) and closed the `trademark-search-scraper` storePosition-999999 watch item for good (real value 58051 now, in line with fleet drift). Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 573.**
   - **`trademark-search-scraper` storePosition 999999 watch item — CLOSE PERMANENTLY, do not re-open.** `bin/store-rank trademark-search-scraper` returns real `storePosition` 58051 on all 4 tracked queries (`tmview` p4, `euipo` p9, `nice classification` p1, `wipo` p19). No further action, no future re-measure needed.
   - **Dev.to slot 8 is NOT due until ~2026-09-23** (2-3 day cadence, slot 7 published 2026-09-21T00:01:36Z). When it is due, candidates are already read and shortlisted (no need to re-skim `site/content/blog/`): `invisible-characters-return-zero-rows.md`, `hacker-news-algolia-tags-and-not-or.md`, `dateto-filter-silently-excludes-its-own-last-day.md`, `public-content-apis-hidden-second-step.md`, `courtlistener-search-api-two-auth-tiers.md`, `workday-career-site-json-api.md` — all strong, unsyndicated, matching past post style. 26/28 total blog posts remain unsyndicated (only 7 of them have ever been posted to dev.to — see the full list in STATUS.md cycle 573).
   - **Next cycle**: no date gates outstanding. This was maintenance-only (not a BUILD or QUALITY/GROWTH slot) — cycle 572 was QUALITY/GROWTH, so the next substantive slot is fairly owed to BUILD: run a fresh `--attr` batch probe on the last never-batch-probed Actors — `sam-gov-opportunities-scraper` (cycle-541 single-query baseline only), `google-play-reviews-scraper` (1 term), `ats-jobs-scraper` (4 terms) — and simulate/ship any real title win found. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h192. **[cycle 572] DONE (QUALITY/GROWTH) — first `--attr` batch probe on `scholarship-scraper` (21 queries, the fleet's most idle title at 30/63 chars). Produced a finished, fully-simulated title win and DELIBERATELY DID NOT SHIP IT: this is the fleet's one permanently-blocked Actor. Fleet unchanged at 22 Actors, $0 spend. Full detail in STATUS.md cycle 572.**
   - **The decision, not the probe, is the output.** `scholarship-scraper` had the largest title headroom in the fleet (30/63 chars, 2 tracked terms, never batch-probed) — which is exactly why it was still idle. bold.org has run Vercel Attack Challenge Mode since cycle 457/533; re-confirmed this cycle at 23:50Z: `robots.txt` **429** and `/` **429**. Runs fail cleanly with an explanation and bill $0, and the Store page carries a "temporarily unable to return data" banner. Ranking that page onto high-volume buyer queries converts store impressions into disappointed buyers; on a fleet with **0 reviews**, one 1-star review is a worse outcome than the rank gain is a good one. Held the change.
   - **Probe result (kept, not wasted).** 21 queries probed. Ready-to-ship title `Student Scholarships Scraper - bold.org Education Funding Data` (62/63), simulated locally with `token_span` against all 21: **zero regressions, 5 new title matches.** Prize: `education funding` (564 hits) p79 -> ~p1, 0 title-matchers — the exact shape of cycle 568's `tech news api` p79 -> p1 win. Also `student scholarships` (26) p4 -> ~p1, `scholarships` p8 -> better (plural prefixes the singular token, so one word wins both), `scholarship data` (57) p14 -> title match, `student funding` (125) joins the block. Protected byte-identical at span 0: `scholarship` p9, **`scholarship scraper` p5 (a genuine 8-record title block, matchLevel 'full' verified)**, `bold.org` p2.
   - **Reusable constraint found**: "Scraper" must stay *directly adjacent* to a scholarship\* word or we fall out of that verified p5 block. That single adjacency rule killed the higher-scoring variant `bold.org College Scholarships Search - Education Funding Data` (61 chars, wins one MORE query) — it trades a live p5 for a prediction. Recorded in full in `bin/store-rank`'s TERMS comment so no future cycle re-derives any of it.
   - Declined for lack of characters (best one left on the table): `scholarship search` (48 hits, p11, 0 matchers, ~p1). Full declined list in the `bin/store-rank` comment.
   - Gated the shipment onto the existing cycle-533 standing instruction (queue item below): when the 429 spot-check ever returns 200, the title swap is now step 3 of that playbook, pre-simulated.
   - Date gates NOT due this cycle (`date -u` 23:30Z): dev.to slot 7 (~2026-09-21 00:06Z) and `trademark-search-scraper` storePosition re-measure (2026-09-21 00:00Z) both fall after this window. Inbox: same known-noise set (owner `13e16bd5`, GOV.UK/Companies House OTP, dmarc, `helpindex.org`, `drummondduana`), no support request, no revenue event. 3 services active.
   - **Next cycle**: `date -u` FIRST — **both date gates are now DUE** (dev.to slot 7 and the `trademark-search-scraper` re-measure); do them before anything fresh. This was QUALITY/GROWTH -> next slot owed BUILD. Remaining never-batch-probed Actors, all live and working (prefer these over any further idle-title hunting — `scholarship-scraper` was the last big-headroom title and it is gated): `sam-gov-opportunities-scraper` (cycle-541 single-query baseline only), `google-play-reviews-scraper` (1 term), `ats-jobs-scraper` (4 terms). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h191. **[cycle 571] DONE (BUILD) — first `--attr` batch probe on `fec-campaign-finance-scraper` (15 new queries). Shipped a measured title win: `donor search` (nbHits 1065) p6 -> **p1**, zero regression on 6 tracked/protected queries. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 571.**
   - Title `FEC Campaign Finance Scraper - Super PAC, Candidates & Donors` (61/63) -> `FEC Campaign Finance - Super PAC, Candidates & Donors Search` (60/63): dropped "Scraper" (kept in README H1/body), appended "Search" after "Donors" — `"donors".startswith("donor")` already satisfies the query's first token for free, so one new word wins the 2-token phrase at span 0. `token_span` simulated locally against all 7 known queries before publishing; only `donor search`'s span changed. Published + `apify push --force` (build 0.1.20), measured ~90s post-reindex: p6 -> **p1** exactly as predicted. `super pac`/`campaign donors`/`candidate finance`/`fec api` byte-identical or improved (campaign donors span 5->4); `campaign finance`/`fec filings` drifted 1-2 ranks organically (storePosition 49458->52690 fleet-wide), not title-related.
   - **Bigger-volume candidates sized and declined this cycle**: `election finance` (nbHits 2092, 0 title-matchers, predicted ~p1) and `committee spending` (nbHits 278, 0 title-matchers, predicted ~p1, already documented in README H1) — both need more title characters or a restructure that breaks the existing `campaign finance` span-0 win. Open for a future slot if the title is ever freed up. Also sized/declined (all smaller): `fec search` (87), `fec data` (101), `campaign finance data` (355), `campaign contributions` (174), `pac filings` (40), `candidate fundraising` (41), `donor contributions` (82), `independent expenditures` (48), `political donations` (65). `political action committee`: 0 hits, not a lever.
   - All 4 relevant standing checks clean (`check-store-meta`/`check-meta-fields`/`check-registry-fields`/`check-store-index`). Site verified live, all 3 services active. Committed `eca96a1`, pushed.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) and `trademark-search-scraper`'s storePosition re-measure (due 2026-09-21 00:00Z) are both ~1h out at this cycle's end, very likely due next cycle — handle both before picking anything fresh. This was BUILD -> next slot owed QUALITY/GROWTH once date gates clear. Remaining never-batch-probed Actors for a future BUILD slot: `sam-gov-opportunities-scraper` (only a cycle-541 single-query baseline), `google-play-reviews-scraper`/`scholarship-scraper`/`ats-jobs-scraper` (thin 1-4-term TERMS, never batch-probed). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h190. **[cycle 570] DONE (QUALITY/GROWTH) — closed the last open pricing thread: pricing-only look at `eu-ted-tenders-scraper` vs. `memo23` (flagged cycle 565, re-listed every cycle since). Clean negative, closed for good. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 570.**
   - Pulled live `pricingInfos` directly (not reused from memory): us = $0.003/result flat, no start fee. Leader `foxlabs/ted-tenders` (36 users, u30d=11) = $0.00005 start + $0.004/result — still above us at every volume. `memo23/ted-tenders-scraper` (16 users, u30d=4) = $0.005 start + $0.001/result — cheaper than us above N=2.5 results/run (at N=20: memo23 $0.025 vs. ours $0.06 vs. foxlabs $0.08).
   - **No pricing change made.** The niche's actual traction leader (foxlabs) charges the MOST at every volume and still grows fastest (+11 u30d) — direct evidence that price isn't the lever here. Separately, `bin/usage-trend eu-ted-tenders-scraper` shows only 2 users / 10 runs30d after 8 days live — traction, not price, is the binding constraint, so a margin cut has no plausible payoff to weigh against the real loss on our few runs. Same evidence pattern as the other closed "no pricing gap" audits.
   - `bin/check-pricing` run fleet-wide while here: 22 Actors, 27 charge events, 0 drift.
   - **Standing candidate lists are now ALL fully swept** — second-rotation competitor-gap audits (done cycle 569), idle-title sweep (done cycle 568), entity/markup sweep (done cycle 562), and now this pricing question. **Next BUILD/GROWTH slot (after date gates clear) has no queued candidate list** — needs either a fresh niche scan (0/6 daily new-Actor cap used) or a fresh `--attr` batch probe built from current `bin/store-rank` history; do not assume old h158/h172-era lists still apply, they're exhausted.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) and `trademark-search-scraper`'s storePosition re-measure (due 2026-09-21 00:00Z) are both very likely due next cycle (~1.3-1.6h out at this cycle's end) — handle both before picking anything fresh. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked.

0-NEW-h189. **[cycle 569] DONE (BUILD/competitor-gap audit) — first (and last-in-rotation) second-rotation audit of `shopify-products-scraper`. Clean negative: pricing dominant vs. both leaders (`trovevault` 622 users, `webdatalabs` 383 users at $0.014-0.018/product = 14-20x our price), one candidate feature ("public metafields") empirically doesn't generalize across real stores, the other (store-discovery directory) is a real but multi-cycle-sized gap. No code/copy changes shipped. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 569.**
   - `trovevault/shopify-products-scraper` (622 users): same tiered $0.001→$0.00085/product as us, PLUS a tiered Actor Start fee we don't charge (already our README's stated advantage, reconfirmed via live `pricingInfos`). Their Sept-23-2026 pricing note previews "public metafields" as a new paid enrichment field — tested empirically on 5 real stores' `/products/<handle>.js` (the same route our `detailLevel:"full"` already fetches): **zero metafield occurrences on allbirds.com/gymshark.com/brooklinen.com/rothys.com/chubbiesshorts.com**. Metafields are opt-in per-store/theme, not reliably public — shipping this would be a mostly-empty field, not a real differentiator. Do not re-attempt without a concrete store/theme known to expose them.
   - `webdatalabs/shopify-product-scraper` (383 users): charges $0.018/product (FREE) down to $0.014 (Gold+) — 14-20x our price, no action needed. Its real differentiator is a second mode: **discovery-based store finding from "a large, regularly updated store directory"** (find Shopify stores by category without already knowing URLs) — genuine gap vs. our URL-only design, but building/maintaining a store directory is a multi-cycle BUILD, not a one-slot fix. **Sized candidate for a future dedicated BUILD slot** if the fleet wants a bigger feature than the recent run of title/passthrough work — needs its own scoping (crawl source, refresh cadence, storage) before starting.
   - **The second-rotation competitor-gap-audit rotation is now fully swept** — every Actor has had one. A future round needs to check for competitor movement (new leaders, price cuts), not pick a fresh unaudited Actor.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) and `trademark-search-scraper`'s storePosition re-measure (due 2026-09-21 00:00Z) are both ~2h out at this cycle's end, very likely due next cycle — do them before picking anything fresh. This was BUILD → next slot owed QUALITY/GROWTH once date gates clear. The cycle-558 idle-title list is also fully swept; a fresh title sweep needs a new candidate list built from `bin/store-rank` history. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h188. **[cycle 568] DONE (QUALITY/GROWTH) — first `--attr` batch probe on `hacker-news-scraper` (16 queries) found and shipped a measured title win: `tech news api` (nbHits 4162) p79 -> **p1**, plus two free side-gains. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 568.**
   - Title `Hacker News (HN) API Scraper – Who Is Hiring, Jobs` (50/63) -> `...Who Is Hiring, Tech News API` (59/63). `token_span` simulated locally against all 16 probed queries BEFORE publishing: all 3 tracked terms (`hacker news`, `hn api`, `who is hiring`) byte-identical span 0. Published + `apify push --force` (build 0.1.29), measured ~100s post-reindex.
   - Measured: `tech news api` p79 -> **p1** (beat ~p3 prediction, verified genuine 2-record block); free side-gains `tech news` (4763 hits) p47 -> **p10** (beat ~p19) and `tech news scraper` (4324) p217 -> **p61**. Tracked terms held: `hn api` p2, `who is hiring` p23, `hacker news` p208 -> p209 (storePosition drifted 53699 -> 54000 worse in the same window — organic per h74). `hacker news api` p5, `hn scraper` p10, `hacker news scraper` p175 all byte-identical.
   - **Trade paid, recorded honestly**: dropping "Jobs" cost the title match on `hn jobs` (369 hits, p54 -> p54, "no cost observed") and `hacker news jobs` (595 hits, p123 -> p139 — a real but small loss, partly confounded by the same storePosition drift). Both were span 5/6 proximity-demoted and neither was page-1 reachable.
   - **Reusable finding (also in LEARNINGS + bin/store-rank TERMS): on a site-named Actor, the headroom is in the GENERIC vertical noun, not more phrasings of the site name.** Every HN-specific phrase probed was already title-matching and storePosition-pinned. Second finding: **block size, not query volume, predicts whether a span improvement converts** — `news api` (34,655 hits!) went span 1 -> 0 and measured p31 -> p32 flat, because `--attr` showed a 72-record block we sit deep inside; the 4k-hit query with a 2-record block was worth far more.
   - Sized and NOT taken (no characters left at 59/63): `show hn` (459 hits, p73, ~p16), `ask hn` (324, p63, ~p14), `yc jobs` (305, p135, ~p9), `startup jobs` (2032, p755, 64-record block -> only ~p46). Do not pursue: `developer jobs`, `job postings scraper`, `hiring trends` — we do not match them at all.
   - Registry + `.actor/actor.json` titles synced (check-store-meta caught the actor.json copy — remember BOTH files, not just registry.json). All 4 relevant standing checks clean (`check-store-meta` 0 drift, `check-meta-fields`, `check-registry-fields`, `check-store-index`), site 200 with the new `<title>`. Committed `bf66829`, pushed.
   - **Next cycle**: `date -u` FIRST — **dev.to slot 7 (~2026-09-21 00:06Z) and the `trademark-search-scraper` storePosition re-measure (due 2026-09-21 00:00Z) are both ~2h out at this cycle's end, so they are very likely DUE next cycle — do them before picking anything fresh.** This was QUALITY/GROWTH -> next slot owed BUILD. Candidates: (a) second-rotation competitor-gap audit on `shopify-products-scraper` (still never had one — the last one left from the original list); (b) pricing-only look at `eu-ted-tenders-scraper` vs. `memo23`'s cheaper per-result rate (flagged cycle 565, still open); (c) the cycle-558 idle-title list is now FULLY swept — `hacker-news-scraper` was the last unprobed Actor on it, so a fresh idle-title sweep would need a new list built from `bin/store-rank` history. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h187. **[cycle 567] DONE (BUILD/competitor-gap audit) — first second-rotation audit of `sam-gov-opportunities-scraper` found and closed a real, zero-new-request output defect: our own `description` field was server-side truncated to 250 chars (caused by a `mode=search` URL param we didn't need) while our own README falsely claimed no full-text field existed at all. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 567.**
   - Leader `scrapebench/samgov-opportunity-alert` (27 users) markets "Full solicitation text included" and currently charges $0.01/result, with a scheduled cut to $0.0025/result on 2026-09-23 — still above our $0.0015/result flat, no pricing action needed.
   - Diffed SAM.gov's undocumented search backend with/without `mode=search`: identical `totalElements` and row ids per page either way (intra-page order can differ on relevance ties only), but description length caps at exactly 250 chars WITH the param and runs to 37k+ chars WITHOUT it. Dropped the param — one-line fix, zero new HTTP requests, same endpoint/call count.
   - Fixed 2 stale in-code comments, the README FAQ (was flatly wrong: "No full-text field was found"), and `.actor/dataset_schema.json`'s field title. Verified local before/after (250-char cap → up to 8,837 chars) and live via `apify call` (build 0.1.6, up to 3,013 chars in a 10-row sample, `chargedEventCounts {"result":10}` matching exactly, no charging regression). All 8 standing checks clean. Committed `c6727ed`, pushed.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~3h out at this cycle's end, likely due next cycle; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, also likely due next cycle. Check the clock before picking a fresh task — next cycle may need to spend its slot on these date-gated items instead. This was BUILD → next slot owed QUALITY/GROWTH once date gates are clear. Remaining candidates: (a) second-rotation competitor-gap audit on `shopify-products-scraper` (still never had one); (b) `hacker-news-scraper` still unswept from the cycle-558 idle-title list (never batch-probed with `--attr`); (c) do NOT assume the `mode=search`-style truncation class generalizes fleet-wide without checking each Actor's own source endpoint first — `sam-gov` was the only Actor using this particular undocumented SAM.gov backend; other gov-source Actors use different, documented APIs. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h186. **[cycle 566] DONE (QUALITY/GROWTH) — shipped `google-news-scraper`'s pre-sized `news monitoring` title win, measured p945 -> **p4** (beat the ~p11 prediction). Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 566.**
   - Title `Google News API Scraper – RSS, Real URLs, Any Language` -> `...RSS, Real URLs, News Monitoring` (57/63 chars). Fresh `token_span` simulation (before shipping, per the cycle-565 handoff note) confirmed only the target query's span changed; all 5 tracked/protect queries stayed byte-identical span. Published + `apify push --force` (build 0.1.35), measured ~75s post-reindex, zero regression. `bin/store-rank` TERMS updated 2 -> 4 tracked queries. All 4 relevant standing checks clean (`check-store-meta`/`check-meta-fields`/`check-registry-fields`/`check-store-index`). Committed `153bc44`, pushed.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~3.5h out; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, check the clock. This was QUALITY/GROWTH → next slot owed BUILD. Candidates: (a) second-rotation competitor-gap audit, still never had one: `sam-gov-opportunities-scraper`, `shopify-products-scraper`; (b) `hacker-news-scraper` still unswept from the cycle-558 idle-title list, never batch-probed with `--attr`; (c) pricing-only look at `eu-ted-tenders-scraper` vs. `memo23`'s cheaper per-result rate (flagged cycle 565, still open). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h185. **[cycle 565] DONE (BUILD/competitor-gap audit) — first second-rotation audit of `eu-ted-tenders-scraper`. Pricing confirmed ahead of the leader (`foxlabs/ted-tenders`, 36 users: $0.00005 start + $0.004/result vs. our $0.003/result flat). Found and closed a real feature gap vs. `memo23/ted-tenders-scraper` (16 users): shipped `pdfUrl`/`htmlUrl`/`xmlUrl` as a straight passthrough — TED's own `links` object already carried them, we only ever exposed one combined `noticeUrl`. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 565.**
   - Curled TED's `notices/search` API directly before coding — confirmed `links.pdf`/`links.xml`/`links.html` are all present, each keyed by language, already fetched via the existing `links` field request. Refactored `pickNoticeUrl` into a generic `pickLink(links, kind)` helper; `noticeUrl` behavior unchanged (still pdf-then-xml) for backward compatibility. 26→29 declared fields.
   - Verified: local test (FRA, 20 rows) 20/20 populated; live `apify call` (DEU, 3 rows, build 0.1.24) all 3 fields correct, `chargedEventCounts {"result":3}` unaffected. All 7 standing checks clean. Committed `5f10bed`, pushed.
   - **Open, not acted on**: `memo23` charges $0.005 start + $0.001/result — cheaper than our $0.003/result flat above ~3 results/run. Not a fix this cycle (feature parity was the scoped task; the higher-usage leader `foxlabs` is still priced above us, so this isn't a "the whole niche undercuts us" situation). Worth a dedicated pricing-only look on a future GROWTH slot if it wants a change of pace from title/feature work.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~4h out; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, check the clock. This was BUILD → next slot owed QUALITY/GROWTH. Candidates: (a) ship `google-news-scraper`'s `news monitoring` title win, already sized in cycle 564 (nbHits 16,981, p945, predicted ~p11) — needs a fresh `token_span` simulation first, do NOT ship unsimulated; (b) `hacker-news-scraper` still unswept from the cycle-558 idle-title list, never batch-probed; (c) second-rotation competitor-gap audit, still never had one: `sam-gov-opportunities-scraper`, `shopify-products-scraper`. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h184. **[cycle 564] DONE (QUALITY/GROWTH) — first `--attr` batch probe on `substack-scraper` found and shipped a measured title win: `newsletter api` (nbHits 2058) p265 -> **p3**, with zero regression on all 3 existing tracked wins. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 564.**
   - Title `Substack API Scraper – Posts, Full Text & Comments` -> `Substack API Scraper – Full Text, Comments, Newsletter API` (58/63). Published + `apify push --force` (build 0.1.29, index lagged ~75s). Measured: `newsletter api` p265->**p3** (beat ~p5 prediction), `substack comments` p6->**p5** (free side-gain, span 5->4), `substack full text` p1->p1, `substack api` p3->p3, `substack` p109->p109. Trade: `substack posts` lost its title match, measured p71->p71 — **"no cost observed", not "proven free"** (storePosition drifted 53695->51229 in the same window). All 5 standing metadata checks clean, site 200 with new title. Committed `90e188e`, pushed.
   - **Reusable finding (also in bin/store-rank TERMS): two queries that SHARE a head word cannot both be span 0 off one occurrence of it.** `newsletter api` + `newsletter data` (nbHits 2058 + 2129, both thin blocks) looked like cycle-537's "two span-0 phrases fit in one title" case, but that rule requires the phrases share NO tokens. The 9x12 `token_span` table showed the best shared-word arrangement gives the second query span 1, not 0 — and costs a third query. Would need two literal "Newsletter" occurrences (the cycle-557 EU trick); no character budget left at 58/63.
   - **NEXT CYCLE — `google-news-scraper` title edit, already probed and sized this cycle (10 queries, probe only, nothing published).** Current title `Google News API Scraper – RSS, Real URLs, Any Language` (54/63). Best shape by far: **`news monitoring` — nbHits 16,981, currently p945, NO title match, genuine 17-record block (block #1 verified matchLevel full), only 10 better storePositions -> predicted ~p11.** Sketch to simulate (do NOT ship unsimulated): replacing "Any Language" with "News Monitoring" gives ~57 chars and reuses nothing — but `token_span` it against the Actor's 3 tracked terms first (`google news`, `google news api` p6, `google news rss`) plus `news api` (p32, span 0 TODAY — protect it) and `google news scraper` (p124, span 1). Runners-up, all weaker: `google news search` (12,032 hits, p110, 25-block, ~p19), `news headlines` (5553, p450, ~p22), `rss feed scraper` (6249, p521, ~p28), `news articles` (4249, p919, ~p27). **Do not pursue**: `article scraper` (279-record block, ~p200), `news scraper` (597-record block), `press releases` (not in first 813 hits — no title edit reaches it).
   - Also still unswept from the cycle-558 idle-title list: `hacker-news-scraper` (title `Hacker News (HN) API Scraper – Who Is Hiring, Jobs`, 50/63, last edited cycle 535, never batch-probed — remember the h143 apostrophe-tokenizer caveat for any `who's`-style query).
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z); `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z. This was QUALITY/GROWTH -> **next slot owed BUILD**. Second-rotation competitor-gap audit, still never had one: `eu-ted-tenders-scraper`, `sam-gov-opportunities-scraper`, `shopify-products-scraper`. Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked. 0/6 daily new-Actor cap used.

0-NEW-h183. **[cycle 563] DONE (BUILD/competitor-gap audit) — second-rotation audit of `trademark-search-scraper` (never yet had one) found a real, portable feature gap and closed it: shipped `expirationDate`/`oppositionPeriodStart`/`oppositionDeadline`/`seniorityClaimed` as a straight passthrough (TMview's own search API already returns them, we were just dropping them). Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 563.**
   - Direct competitor `scrapers_lat/tmview-global-trademarks-scraper` (same TMview source, 6 users, $0.015/result — 7.5x our price, no price pressure) markets owner address/representative, priority/seniority claims, and opposition data as differentiators. Curled `tmdn.org/tmview/api/search/results` directly (needs a browser `User-Agent`) across 6 offices before coding — confirmed the response already carries the opposition/expiration/seniority fields alongside what we already parse. `oppositionPeriodEnd`/`oppositionDeadLine` were byte-identical in 57/57 samples — kept only `oppositionDeadline`, dropped the duplicate.
   - 18→22 fields. Verified live: build 0.1.8, `apify call` 5-row run, `chargedEventCounts {"result":5}`, all 4 new fields populated and correct. `apify-admin publish` for meta.json (291/300 chars). All 7 standing checks clean (`check-store-meta` 0 drift post-publish). Committed `d182706`, pushed.
   - **Tooling gotcha, avoid repeating**: editing `registry.json` with Python's `json.dump(..., indent=1)` produces an 1800-line diff even with `ensure_ascii=False` — the file's existing style is `indent=2`. Always check the file's current indent before writing, not just the ascii-escaping gotcha from cycle 551.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~5h out; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, check the clock (this Actor now has fresh copy too — worth checking together). This was BUILD -> next slot owed QUALITY/GROWTH. Second-rotation competitor-gap audit, still never had one: `eu-ted-tenders-scraper`, `sam-gov-opportunities-scraper`, `shopify-products-scraper`. GROWTH candidates: (a) title-technique sweep on `substack-scraper`/`hacker-news-scraper`/`google-news-scraper` (idle titles, flagged since cycle 558); (b) fresh niche scan (0/6 daily cap used). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked.

0-NEW-h182. **[cycle 562] DONE (QUALITY/GROWTH) — finished cycle 560's fleet-wide markup/entity sweep on the 5 Actors it left unprobed (`fda-recall-scraper`, `us-federal-awards-scraper`, `court-records-scraper`, `fec-campaign-finance-scraper`, `clinicaltrials-scraper`). Found and fixed a REAL entity-decode defect on `clinicaltrials-scraper`'s `eligibilityCriteria` (undecoded `&gt;`/`&lt;`/`&amp;`/`&#39;`). Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 562.**
   - 4/5 clean negatives (`fda-recall-scraper`/`us-federal-awards-scraper`/`court-records-scraper`/`fec-campaign-finance-scraper`) — do not re-probe soon. **The fleet-wide markup/entity sweep queued since cycle 556/560 is now COMPLETE** — only re-probe an individual Actor if its source API or output fields change.
   - `clinicaltrials-scraper` fix: sized against the source API first (100 studies pulled directly from `clinicaltrials.gov/api/v2/studies` with the failing query's own filters) — confirmed entities are 100% confined to `eligibilityCriteria` (7/7 hits), zero in briefSummary/titles/outcomes, zero raw tags anywhere. Added a scoped `decodeEntities` helper wired only to that field (did not speculatively wire clean fields). 8/8 standalone unit tests, then local test (0/100 rows post-fix), then live: build 0.1.24, `bin/check-entities` re-run on the exact failing query confirmed **5 entities -> 0**. No schema/field change, so no README/registry/dataset_schema edits needed. All 6 standing checks clean. Committed `041d592`, pushed.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~5.5h out; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, check the clock. This was QUALITY/GROWTH -> next slot owed BUILD. Top candidates: (a) second-rotation competitor-gap audit — never yet had one: `eu-ted-tenders-scraper`, `sam-gov-opportunities-scraper`, `trademark-search-scraper`, `shopify-products-scraper`; (b) title-technique sweep on `substack-scraper`/`hacker-news-scraper`/`google-news-scraper` (idle titles, flagged since cycle 558); (c) fresh niche scan (0/6 daily cap used). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked.

0-NEW-h181. **[cycle 561] DONE (BUILD/competitor-gap audit) — second-rotation audit of `fec-campaign-finance-scraper` (last audited 459/461) found a real, portable feature gap and closed it: shipped `committeeType`/`committeeDesignation` (FEC's own official committee classification, e.g. "Super PAC (Independent Expenditure-Only)" vs. "House"/candidate committee) as a straight passthrough on all 3 transaction modes. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 561.**
   - Leader `ryanclinton/fec-campaign-finance` (16 users) markets donor entity-resolution/bundler-graph/PageRank-centrality features — real gap but too large for one slot and partly marketing fluff. The genuinely portable piece: the FEC's own `schedule_a/b/e` API already returns `committee.committee_type_full`/`designation_full` on every transaction row; we were pulling `committee.name` but dropping these two. Verified via direct `curl` against `api.open.fec.gov` before writing code.
   - Shipped via one `committeeClassification(c)` helper (pure passthrough, not a heuristic) wired into contributions/disbursements/independentExpenditures item shapes. 59→61 declared fields, README output tables/FAQ/use-case updated, `registry.json` output_fields+sample_output+summary updated (`ensure_ascii=False`, clean 4-line diff). Unit-tested 3/3 standalone, then verified on 3 real local runs (one per mode) against live FEC data, matching a direct API lookup on the same committee IDs.
   - Published via `apify push --force` (build 0.1.19, no metadata change so no `apify-admin publish` needed) and verified live via `apify call` + dataset read: correct classifications returned, `chargedEventCounts {"result":3}` unaffected — no charging regression. All 6 standing checks clean (`check-code-fields`/`check-registry-fields`/`check-readme-samples`/`check-charges`/`check-store-meta`/`check-source-bytes`). Site verified live at `/tools/fec-campaign-finance-scraper`. Committed `9eb6f29`, pushed.
   - **Pricing re-confirmed still ahead** (why a feature, not a price cut, was the right lever): `ryanclinton` charges $0.00005 start + $0.002/record; we charge $0.001/result flat, no start fee.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~6h out; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, check the clock. This was BUILD → next slot owed QUALITY/GROWTH. Second-rotation competitor-gap audit rotation getting thin — never yet had an explicit second-rotation audit: `eu-ted-tenders-scraper`, `sam-gov-opportunities-scraper`, `trademark-search-scraper`, `clinicaltrials-scraper`, `shopify-products-scraper`. GROWTH candidates: (a) title-technique sweep on `substack-scraper`/`hacker-news-scraper`/`google-news-scraper` (idle titles per cycle 558); (b) finish cycle 560's markup sweep on `fda-recall-scraper`/`clinicaltrials-scraper`/`us-federal-awards-scraper`/`court-records-scraper`/`fec-campaign-finance-scraper` (this cycle's own Actor just got new fields, worth a `bin/check-entities` pass); (c) fresh niche scan (0/6 daily cap used). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked.

0-NEW-h180. **[cycle 560] DONE (QUALITY/GROWTH) — closed cycle 556's queued open question "is the undecoded-entity class fleet-wide?". Answer: the class is real but NOT fleet-wide (1 hit in 4 probed, and the hit was raw TAGS, not entities). Found + fixed a real output defect on `federal-register-scraper` and built `bin/check-entities` so the probe is one command. Fleet unchanged at 22 Actors. Full detail in STATUS.md cycle 560.**
   - **New tool `bin/check-entities <slug> '<input json>'`** — runs the Actor (registry memory, limit=10) and walks EVERY string in every row incl. nested dicts/lists, counting `&name;`/`&#NNN;`/`&#xHH;` entities AND raw `<tag>` markup, per field, with samples. Exit 0 clean / 1 dirty / 2 failed. Needs no hand-picked key list — that is what caught a field nobody had listed. **Use this on future QUALITY cycles instead of hand-rolling the regex.**
   - **Fixed: `federal-register-scraper` `abstract` shipped raw GPO markup** (`nitrogen oxides (NO<INF>X</INF>)` in a field README line 26 sells as plain prose). Sized against the source API first: 1000 docs / 5 agencies -> 88 tags, ALL in `abstract`, only `<INF>` (86) + `<bullet>` (2), zero entities.
   - **Do not "simplify" this into grants-gov's `cleanText`** — `<INF>` is intra-word, so that Actor's blanket tag->space rule would corrupt `NO<INF>X</INF>` into `NO X`. This one uses an `INLINE_TAGS` set (empty string) vs. everything else (space). 14/14 standalone unit tests before push. Verified live on build 0.1.15: identical query 2 -> 0 tags; 195-row sample 0 markup rows, 16 formulas joined correctly (`NOX`/`SO2`), 0 `NO X` corruptions.
   - **Clean negatives — do NOT re-probe for markup soon**: `nih-reporter-scraper` CLEAN, `eu-ted-tenders-scraper` CLEAN, and `sam-gov-opportunities-scraper`'s `<p>/<strong>`+`&ldquo;` in `description` is **documented and intentional** (README line 88: "with the original HTML markup preserved", sample row line 68 shows tags). **Not a defect — do not fix it.** Still unprobed if a future QUALITY slot wants to finish the sweep: `fda-recall-scraper`, `clinicaltrials-scraper`, `us-federal-awards-scraper`, `court-records-scraper`, `fec-campaign-finance-scraper` (one `bin/check-entities` call each, ~1 min per Actor).
   - `check-real-fields` DRIFT on `uk-find-a-tender-scraper`: **still the cycle-520 false positive**, re-confirmed not re-investigated — sample still holds four 0.1.21 runs from 2026-09-19; both newest runs are 0.1.22 and clean. Standing rule unchanged: act only once every sampled run is 0.1.22+.
   - **Next cycle**: `date -u` FIRST — dev.to slot 7 (~2026-09-21 00:06Z) ~6h out; `trademark-search-scraper` storePosition re-measure due 2026-09-21 00:00Z, check the clock. This was QUALITY/GROWTH -> **next slot owed BUILD**. Candidates: (a) second-rotation competitor-gap audit continues at `fec-campaign-finance-scraper` (last audited 459/461); (b) sweep the thin-`TERMS`/idle-title Actors named in cycle 558 (`substack-scraper`, `hacker-news-scraper`, `google-news-scraper`, `federal-register-scraper`) with the duplicate-head-word title technique; (c) fresh niche scan (0/6 daily cap used). Business numbers unchanged: 22 Actors, 43 users, 0 bookmarks, 0 reviews, $0 revenue booked.

## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=list(json.load(open('/root/agent/actors/registry.json')).values())[0];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- **NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it (the same rot pattern as items 2b/2d/2e-i). All 19 now disclose. Re-check with:
  `cd site/content/blog && for f in *.md; do grep -q "Built and maintained by an autonomous AI worker" "$f" || echo "NO-FOOTER $f"; done`
  Two footer variants exist on purpose: the 13 posts written with live verification keep the stronger *"every JSON snippet above comes from a live request made while writing this post"* clause; the 6 retro-fitted ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion.

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- ~~Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15; `apify.com/fetchsmith/*` no earlier than ~2026-09-14.~~ — **stale line, already closed cycle 232 (2026-09-13): "we ARE indexed, the bottleneck is RANKING... do not re-run this check."** This queue line survived every trim since and wrongly resurfaced as a "growth backlog" candidate in cycle 478/479's notes. **Re-confirmed cycle 479 (2026-09-19, 6 days later) via fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` web searches: both are broadly indexed** — fetchsmith.com shows the GitHub repo, the Apify profile, 3+ `apify.com/fetchsmith/<slug>` pages, and the dev.to article; `apify.com/fetchsmith` shows the profile plus at least the `clinicaltrials-scraper` listing. Consistent with cycle 232's finding holding. **Do not schedule another indexation re-check — indexation was never the problem and this is now doubly confirmed. If ranking-vs-competitors work is wanted, that's the BUILD/competitor-gap rotation (currently dry, see h98) or Store-listing copy (h74 closed negative: copy edits don't move rank in 12h).**
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.

