0-NEW-h285. **[cycle 657] IN PROGRESS (BUILD — continued the arc: fixed `fec-campaign-finance-scraper` (pre-scoped target) and resolved the naming collision on `federal-register-scraper` in the same pass, since fixing it required adding the same real eviction counters anyway. Builds fec 0.1.22, federal-register 0.1.17. Commit `fd0fac2`.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 18:06Z, 2026-09-22. **dev.to slot 8 due 09-23 — STILL NOT DUE, gate stayed closed.** 3 services active, site 200 on `/` and `/tools`, `/health` `{"tools":22}`, mem 553/1967 MB. Inbox unchanged since cycle 652 (`116f7cc3` already answered) — no mail, no revenue event.
   - **Shipped on `fec-campaign-finance-scraper`** (5000/20000 SEED_CAP/WATCH_KEEP — the smallest cap left in the fleet besides ats-jobs/google-play/hacker-news/trademark): same shape as `us-federal-awards-scraper` — `baselineTruncated`/`baselineTruncatedTotal` counters, a log warning, a `truncationNote()` on the status message (baseline-run, nothing-new, and now also the delivering-run branch that previously had no status message at all), `truncatedLastRun`/`truncatedTotal` persisted in the watch record, both counters added to the `webhookUrl` payload, and a new README "Baseline size cap" paragraph. Did NOT add the RUN_SUMMARY KV record this Actor is still missing (see cycle-656 note below) — kept in scope, not folded in.
   - **Resolved the NAMING COLLISION on `federal-register-scraper` flagged by cycle 656:** its `RUN_SUMMARY`/webhook field `baselineTruncated` was fed `runState.seedCapped` (the one-time baseline-walk depth cap, SEED_CAP=20000) — a different thing from what every other h285-fixed Actor means by that name (WATCH_KEEP record eviction, cap=60000 here). Renamed the old field to `seedCapped` and gave it real `baselineTruncated`/`baselineTruncatedTotal` counters wired the same way as the rest of the arc, plus the same log warning / `truncationNote()` / README paragraph. This also means fedreg (which was never in the original 18-Actor h285 count, since it happened to already emit a same-named field) is now actually fixed, not just relabeled.
   - **Verification (lighter than cycle 656's flagship depth, matching the "copy the shape" plan for arc entries #2+):** live plain-mode smoke run on each Actor (no regression), live watch-mode baseline-seed run on each Actor exercising the new `saveWatchRecord`/`truncationNote` code path end-to-end (0 dropped, no truncation note, exactly as expected below the cap — `RUN_SUMMARY` on fedreg's smoke run confirmed `seedCapped`/`baselineTruncated`/`baselineTruncatedTotal` all present and correctly `null` outside watch mode). All 9 fleet static checks 0 drift. Did not reproduce the live "charged twice" money-impact scenario with a patched-low cap this time (cycle 656 already did that once against the identical code shape); worth doing again only if a future arc entry's shape actually diverges. Test watch-baseline KV keys deleted after verification (`fetchsmith-fec-watch`/`fetchsmith-fedreg-watch`, key `watch-h285-qa-*-657`).
   - **h285 SCAN SET — 13 Actors left** (both `fec-campaign-finance-scraper` and `federal-register-scraper` now done; ranking below is what cycle 656 wrote, unchanged except those two are struck):
     1. `app-store-reviews-scraper` (20000/40000, a popular app has far more reviews than the cap) — highest volume of the remaining.
     2. `steam-reviews-scraper` (20000/40000, millions of reviews per popular game).
     3. `clinicaltrials-scraper`, `court-records-scraper`, `eu-ted-tenders-scraper`, `fda-recall-scraper`, `grants-gov-scraper`, `uk-find-a-tender-scraper` (all 20000/60000).
     4. `nih-reporter-scraper` (15000/60000).
     5. `ats-jobs-scraper`, `google-play-reviews-scraper`, `hacker-news-scraper`, `trademark-search-scraper` (5000/20000 — same-sized cap as FEC, similarly fast to reach on a broad/unfiltered watch).
     6. `apple-podcasts-scraper` (60000, no `SEED_CAP`) — lowest priority.
   - **Still noted, not acted on:** `fec-campaign-finance-scraper` has no `RUN_SUMMARY` KV record at all (only status messages) — a leftover h250-class gap the h250 sweep never covered. Separate follow-up, not part of h285.
   - **Left for next cycle (precise):** (1) **dev.to slot 8 DUE 2026-09-23 — `date -u` first, `bin/devto-post` before anything else** the moment that date arrives; h285 is still strong material. (2) Continue h285 down the 13-Actor ranked list above, 2-3 per BUILD cycle. (3) `grants-gov-scraper`'s missing run-level id set and the sam-gov `maxResults`-watch-coverage question (cycle 649) still open judgement calls. (4) queue.md/STATUS.md climbing toward the ~145KB trim line — check next QUALITY cycle. (5) EDUCATION category slot still empty (option, not commitment). (6) `fec-campaign-finance-scraper`'s missing RUN_SUMMARY, noted above. h263 cosmetic (git gc); benign.

0-NEW-h285. **[cycle 656] DONE (BUILD — opened a NEW defect-class arc: silent watch-baseline eviction re-charges the buyer. Fixed the fleet's worst instance, `us-federal-awards-scraper`. Build 0.1.30 live.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 17:30Z, 2026-09-22. **dev.to slot 8 due 09-23 — STILL NOT DUE, gate stayed closed.** 3 services active, site 200, `/health` `{"tools":22}`, mem 574/1967 MB. Inbox unchanged since cycle 652, no revenue event.
   - **The class:** every watch-mode Actor saves its baseline as `Array.from(watchSeen).slice(-WATCH_KEEP)`. A dropped id is not in the baseline next run, so it is **re-delivered and re-CHARGED** for rows the buyer already paid for. **Only `sam-gov` and `shopify-products` detect it; 16 Actors truncated in silence.** Found by the cycle-645 fleet-scan method applied to a new pattern.
   - **Shipped on `us-federal-awards-scraper`** (worst ratio in the fleet: `SEED_CAP == WATCH_KEEP == 20000`, so a full baseline evicts on the *first* incremental run): `baselineTruncated`/`baselineTruncatedTotal`, a log warning naming the consequence, a `truncationNote()` on the status message (empty unless something was dropped), `truncatedLastRun`/`truncatedTotal` persisted in the record, both counters on the webhook, a new README "Baseline size cap" paragraph (the 20,000 limit was undocumented), and a status message on delivering runs that truncate (previously only set when `pushed === 0`). Verified with `WATCH_KEEP` patched to 3 against the live API — run 2 **charged for 8 already-recorded awards**, reproducing the defect; negative control at the real cap stayed silent. QA gate `vQjSV2OClaI0HCG4U` SUCCEEDED 100 rows. 8 checks 0 drift. Commit `f774404`.
   - **h285 SCAN SET — 15 Actors left, ranked by how fast eviction is reachable (`SEED_CAP`/`WATCH_KEEP`, then source volume). The fix is formulaic — copy the `us-federal-awards` shape; 2-3 per BUILD cycle is realistic.**
     1. `app-store-reviews-scraper` (20000/40000, a popular app has far more reviews than the cap) — highest volume of the remaining.
     2. `steam-reviews-scraper` (20000/40000, millions of reviews per popular game).
     3. `clinicaltrials-scraper`, `court-records-scraper`, `eu-ted-tenders-scraper`, `fda-recall-scraper`, `grants-gov-scraper`, `uk-find-a-tender-scraper` (all 20000/60000).
     4. `nih-reporter-scraper` (15000/60000).
     5. `ats-jobs-scraper`, `fec-campaign-finance-scraper`, `google-play-reviews-scraper`, `hacker-news-scraper`, `trademark-search-scraper` (5000/20000 — slower to reach, but the 20,000 cap is small and FEC/Google-Play sit on very high-volume sources; **FEC was read in full this cycle and is confirmed to have the defect**, `saveWatchRecord` at src/main.js:187, so it is pre-scoped and ready to fix).
     6. `apple-podcasts-scraper` (60000, no `SEED_CAP`) — lowest priority.
   - **NAMING COLLISION to resolve while doing the arc:** `federal-register-scraper:738` already emits a field called `baselineTruncated`, but feeds it `runState.seedCapped` — the **seed cap**, not WATCH_KEEP eviction. Two Actors now use the same field name for two different things. Decide one way (rename fedreg's to `seedCapped`, which is what it means, and give it a real eviction counter too) before more Actors copy either shape.
   - **Also noted, not acted on:** `fec-campaign-finance-scraper` has no `RUN_SUMMARY` KV record at all (only status messages) — a leftover h250-class gap that the h250 sweep never covered. Separate follow-up, not part of h285.
   - **Left for next cycle (precise):** (1) **dev.to slot 8 DUE 2026-09-23 — `date -u` first, `bin/devto-post` before anything else** the moment that date arrives; h285 is strong material ("we found we were about to charge buyers twice for rows they already bought"). (2) Continue h285 down the ranked list above. (3) `grants-gov-scraper`'s missing run-level id set and the sam-gov `maxResults`-watch-coverage question (cycle 649) still open judgement calls. (4) queue.md/STATUS.md climbing toward the ~145KB trim line — check next QUALITY cycle. (5) EDUCATION category slot still empty (option, not commitment). h263 cosmetic (git gc); benign.

0-NEW-h284. **[cycle 655] DONE (QUALITY/GROWTH — standing checklist clean fleet-wide, then a live filter-fidelity sweep on `ats-jobs-scraper`'s 8 filters via `bin/varied-test`. No bug found, no code changed.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 17:00Z, 2026-09-22. **dev.to slot 8 due 09-23 — STILL NOT DUE, gate stayed closed.** 3 services active, site 200 on `/` and `/tools`, `/health` `{"tools":22}`, mem 550/1967 MB. Inbox unchanged since cycle 652 (`116f7cc3` already answered) — no mail, no revenue event. dev.to poll: 7 articles, 3 total comments (same as cycle 648's count), all already answered, 0 new.
   - **Standing checklist: all 9 static checks 0 drift** (`check-code-fields`, `check-readme-samples` 31/46, `check-source-bytes` 404 files, `check-registry-fields`, `check-charges` 23 priced, `check-filter-reach` 23/15, `check-blog-claims` 3+6, `check-meta-fields` 8, `check-pricing` 23/28).
   - **Picked `ats-jobs-scraper` for the QUALITY-cycle "re-run platform tests with different inputs" task — it has the richest filter surface in the fleet (8 filter fields) and no record of a full sweep.** 3 live combos via `bin/varied-test`, all correct against README-documented semantics: `remoteOnly`+`departmentKeyword` (10/10 rows `isRemote:true`, department/team chain matched "Engineering"); `hasSalary`+`minSalary:150000` (3/10 rows had `salaryMin<150000` but every row's `salaryMax≥150000` — that's the documented "overlaps this floor or higher" behavior, not a bug); `postedAfter`/`postedBefore` August window (10/10 `publishedAt` inside the window). No defect found, no code changed this cycle.
   - **Re-tripped the exact wrong-output-key trap `bin/varied-test`'s own docstring warns about, twice, before getting a clean read.** Guessed `remote` (real field: `isRemote`) and `postedAt` (real field: `publishedAt`) — both printed `None` for every row and looked exactly like a silently-broken filter until cross-checking `.actor/dataset_schema.json` caught the typo. Worth restating since this is a recurring trap, not a one-off: **always grep the target Actor's `dataset_schema.json` for the exact output field name before reading a `varied-test` result as a bug.**
   - **Left for next cycle (precise):** (1) h250 arc is closed (cycle 654) — next BUILD cycle needs a fresh defect class via the cycle-645 fleet-scan method, or a growth-backlog item below. (2) `grants-gov-scraper`'s missing run-level id set and the sam-gov `maxResults`-watch-coverage question (cycle 649) are still open judgement calls. (3) **dev.to slot 8 DUE 2026-09-23 — `date -u` first, `bin/devto-post` before anything else** the moment that date arrives; story pool candidates unchanged (651's "offline fixture passed, first real run still found the bug", or now the h250 arc retrospective). (4) queue.md (~135KB) and STATUS.md (~131KB) are both under the 150KB trim threshold but climbing steadily — re-check size next QUALITY cycle, trim if either crosses ~145KB. (5) EDUCATION category slot still empty (option, not commitment). (6) Remaining filter-fidelity sweep candidates not yet done this arc: `remote-jobs-scraper` (4 filters), `fda-recall-scraper`/`scholarship-scraper`/`uk-find-a-tender-scraper` (1 filter each) — low priority, `check-filter-reach` already proves static reachability for all of them. h263 cosmetic (git gc); benign.

0-NEW-h283. **[cycle 654] DONE (BUILD — closed the h250 scan set's LAST target: `federal-register-scraper` had zero `RUN_SUMMARY`/`setStatusMessage` coverage. Build 0.1.16 live. The h250 arc that ran since cycle 628 is now fully closed fleet-wide.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 16:30Z, 2026-09-22. **dev.to slot 8 is due 09-23 — STILL NOT DUE, gate stayed closed.** 3 services active, site 200 on `/` and `/tools`, `/health` reports `{"tools":22}`, mem 626/1967 MB. Inbox unchanged since cycle 652 (`116f7cc3` still newest, already answered) — no mail, no revenue event, nothing owner-actionable.
   - **Standing checklist: all 5 touched static checks 0 drift** (`check-code-fields`, `check-readme-samples` 31 blocks/46 bullets, `check-source-bytes` 404 files, `check-registry-fields`, `check-charges` 23 priced).
   - **The defect, same shape as court-records/uk-find-a-tender/sam-gov:** `apiGet()` returning `null` after 4 failed retries (a dead API) and the walk's own `next_page_url` genuinely running out (a real end of results) both hit a bare `if (!page) break;` / `if (!results.length) break;` with no state recorded — a permanent Federal Register API failure mid-walk produced the identical `Done. Pushed N documents...` log line as a small-but-real result set. This Actor is a single index (unlike court-records' two-index merge), and it also has a watch-mode baseline seed walk (`seedBaseline()`) with the exact same unguarded stop path — a failed seed would have saved a partial baseline as if complete, then over-charged the next incremental run for documents that existed all along but were never seeded.
   - **Shipped:** `failed`/`exhausted`/`seedCapped` tracked in one shared `runState` object across both the main walk and the seed walk; `apiGet()` takes an optional `state` param to record WHY a null came back (network error / 4xx-5xx / non-JSON, same as court-records' pattern); a `RUN_SUMMARY` KV record, the Actor's first-ever `setStatusMessage`, and the same object as `summary` on the webhook payload; `complete`/`incompleteReason` (`source-error`/`seed-cap`/`charge-limit`/`max-results`/`stopped-early`), first-cause-wins. **Deliberately no `declaredMatches` field** — the file's own top-of-file comment already documents that the API's `count` is clamped at 10000 regardless of the true match size, so surfacing it as a total would misstate an unknown as a known number (measured-first, not assumed).
   - **Verified four ways.** Local run against the real live API: a 5-row `maxResults` cut correctly reports `complete:false, incompleteReason:"max-results"`; a narrow agency+date filter that hit a genuine dead end (9 of 9 presidential documents) correctly reports `exhausted:true, complete:true`. **Offline stub server** (temp copy in `/tmp`, base URL patched, not committed): page 1 succeeds and delivers 1 row, page 2 returns 502 four times — RUN_SUMMARY correctly shows `failed:true, exhausted:false, incompleteReason:"source-error"` with the HTTP code, where the pre-fix code would have logged the identical clean-looking `Done. Pushed 1 documents...` with nothing to distinguish it from a genuinely tiny result set. **Live**: custom-filter run `UJkbg5CZ3KXvZS8ZT` (5/5, `max-results`, RUN_SUMMARY fetched via the platform API) and the default-input QA gate run `N4matURJkukopbZYl` (SUCCEEDED, 100/100, same `max-results` shape). Commit `2a7a66c`, pushed, tree clean (stale `.git/gc.log` noise on every commit, benign, h263).
   - **Left for next cycle (precise):** (1) **h250 scan set is now EMPTY — the arc is done fleet-wide.** Worth a dev.to retrospective post if the slot-8 story pool needs refreshing (candidates: "we swept the fleet for a silent-failure bug and closed it Actor by Actor" or 651's "offline fixture passed, first real run still found the bug"). (2) `grants-gov-scraper`'s missing run-level id set — still an open judgement call. (3) sam-gov `maxResults`-bounds watch-coverage question from 649 — still open. (4) **dev.to slot 8 DUE 2026-09-23 — `date -u` first, `bin/devto-post` before anything else** the moment that date arrives. (5) EDUCATION category slot still empty (option, not a commitment). (6) With the h250 arc closed, next BUILD cycle needs a fresh target — re-run the fleet scan method from cycle 645 (`grep -l` for `RUN_SUMMARY`/`setStatusMessage`/watch-mode markers) against a DIFFERENT defect class, or pick from the growth-cycle backlog below. h263 cosmetic (git gc); benign.

0-NEW-h282. **[cycle 653] DONE (BUILD — closed the h250 scan set's second-to-last target: `court-records-scraper` had NO completeness contract at all (0 hits on `RUN_SUMMARY`/`setStatusMessage`). Build 0.1.23 live.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 16:00Z, 2026-09-22. **dev.to slot 8 is due 09-23 — STILL NOT DUE, gate stayed closed.** 3 services active, site 200 on `/` and `/tools`, `/health` reports `{"tools":22}`, mem 548/1967 MB. Inbox unchanged since cycle 652 (`116f7cc3` still newest, already answered) — no mail, no revenue event, nothing owner-actionable.
   - **Standing checklist: all touched static checks 0 drift** (`check-code-fields`, `check-readme-samples` 31 blocks/46 bullets, `check-source-bytes` 404 files, `check-registry-fields`, `check-charges` 23 priced).
   - **The defect:** `walk()`'s only stop path was `if (!page) { state.url = null; break; }` — a permanent CourtListener failure (4 failed retries, a non-200, a non-JSON body) set `state.url = null` in EXACTLY the same way a genuinely exhausted index does (`!results.length` or no `next` cursor). Since this Actor merges two independent indexes (opinions/dockets) with no total-match assertion anywhere, a mid-walk 500 on one index and that index having zero real matches produced the identical dataset and the identical `Done. Pushed N record(s)...` log line — the h250 class, same shape as `sam-gov`/`uk-find-a-tender`, just never touched before.
   - **Shipped:** per-index `pages`/`scanned`/`delivered`/`declaredMatches`/`exhausted`/`failed`/`lastError` tracked inside `walk()` and `apiGet()` (which now takes an optional `state` to record WHY a null came back); a `RUN_SUMMARY` KV record + the Actor's first-ever `setStatusMessage` + the same object as `summary` on the webhook payload; `complete`/`incompleteReason` (`source-error`, `max-results`, `charge-limit`, `seed-cap`, `stopped-early`), first-cause-wins. `declaredMatches` (total) is `null`, not summed as 0, unless BOTH indexes ever reported their own count — present-and-0 would misreport "the archive has none" when really one index failed before ever answering.
   - **Verified three ways.** Local happy-path (12/12 pushed, `max-results`, `opinions` 956 declared + `dockets` 202 declared, correctly `complete:false`). Local exhausted-both-indexes (0 real matches on a nonsense query): `exhausted:true` on both, `complete:true` — proves natural end reports clean. **Offline stub server** (temp copy in `/tmp`, `BASE` overridden via env, not committed): opinions index gets page 1 then a 400 (simulating a dead upstream), dockets gets one full page with no `next` — RUN_SUMMARY correctly shows `opinions.failed:true/exhausted:false` next to `dockets.exhausted:true/failed:false` **in the same run**, with `incompleteReason:"source-error"` naming the dead index and its HTTP code — proof the pre-fix code could not have told these apart (both used to just set `url = null`). **Live**: run `cvzMOjelzThJx3cM1` (10/10, `max-results`, RUN_SUMMARY fetched via the platform API) and the default-input gate run `jvblCyOAqGMis8uwE` (SUCCEEDED, dataset non-empty, same `max-results` shape as the local test). README gained a "Did this run get everything? Check RUN_SUMMARY" section. Commit `15a4f9c`, pushed, tree clean (stale `.git/gc.log` noise on every commit, benign, h263).
   - **Left for next cycle (precise):** (1) **h250 scan set is now 1: `federal-register-scraper`** — 0 hits on `cat federal-register-scraper/src/*.js | grep -c "RUN_SUMMARY\|setStatusMessage"` (note the `cat` form, per cycle 650's `grep -c` bug). Once this closes, the h250 arc that has run since cycle 628 is fully done fleet-wide — worth a dev.to retrospective if the slot-8 story pool needs refreshing. (2) `grants-gov-scraper`'s missing run-level id set — still an open judgement call. (3) sam-gov `maxResults`-bounds watch-coverage question from 649 — still open. (4) **dev.to slot 8 DUE 2026-09-23 — `date -u` first, `bin/devto-post` before anything else** the moment that date arrives; strongest material bank is still 651's "offline fixture passed, first real run still found the bug" or now this cycle's stub-server proof of the same shape. (5) EDUCATION category slot still empty (option, not a commitment). h263 cosmetic (git gc); benign.

0-NEW-h281. **[cycle 652] DONE (QUALITY/GROWTH — owner forwarded an Apify "Under maintenance" flag; retired `scholarship-scraper` because bold.org blocked ALL automated access. First inbox change since cycle 578.) 22 live Actors (was 23), $0 spend, $0 revenue.**
   - `date -u` FIRST: 15:30Z, 2026-09-22. **dev.to slot 8 is due 09-23, so the gate was still CLOSED and did not fire. The first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200 on `/`, `/tools`, `/docs`. **Inbox CHANGED for the first time since cycle 578 — newest is now `116f7cc3` (owner), not `167b67c5`:** Apify mailed them that `fetchsmith/scholarship-scraper` failed automated QA three days running and was flagged "Under maintenance" (their failing run `dwDRWBx7ldg8FkDaY`).
   - **Standing checklist: all 9 static checks 0 drift** after the change (`check-filter-reach` 23/15, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims` 3+6, `check-meta-fields` 8, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28). They still count **23**, correctly — the source tree, READMEs and Apify pricing are untouched; only the registry `status` moved.
   - **Measured before deciding; the block is total.** Live probe of 4 bold.org URLs with a real browser UA — `robots.txt`, `sitemap.xml`, `/scholarships/`, a category page — **all four return HTTP 429 with a ~34 KB `Vercel Security Checkpoint` interstitial.** `robots.txt` itself being gated is the tell: site-wide Vercel challenge mode, not a rate limit and not a path rule. Run history dates it exactly: SUCCEEDED through **2026-09-20 03:31Z**, FAILED every run from **04:03Z** onward (8 straight).
   - **Retired rather than patched, and the reason is a rule, not difficulty.** The only routes through a Vercel challenge are residential proxies or a browser solving it — evading protection the site deliberately switched on (rule 1). **Also deliberately did NOT file Apify's "skip automated tests" form**, which was the tempting move: it clears the flag while leaving a product on sale that cannot return a single row. Rejected "make it exit 0 with no rows" for the same reason — that is exactly the silent-success failure mode the last six BUILD cycles have been deleting.
   - **Why it mattered commercially at $0 revenue:** the "Under maintenance" flag sits on the **account** quality score that ranks all our Actors, so one dead scraper was taxing the other 22. `apify-admin store "bold.org scholarship"` now returns only the competitor (`jungle_synthesizer/bold-org-scholarship-database-scraper`, 3 users, **0 runs30d** — equally blocked, so this is the source, not us).
   - **Shipped:** `isDeprecated: true` via `PUT /v2/acts/<id>` (reversible; `isPublic` left true so the 2 existing users keep access, and Apify's own flag field is `notice: "UNDER_MAINTENANCE"` on the Actor record — useful to know). registry `status` → **`retired`** plus a `notice` stating exactly why. **One flag was enough because `public_tools()`, the sitemap, `/api/v1/run`, `/docs` and `bin/actor-health` ALL already gated on `status in ("live","beta")`** — worth remembering before adding a new listing surface. `/health` now reports `{"tools":22}`.
   - **The only part that needed new code: `/tools/scholarship-scraper` must not 404** — it is indexed and linked from a published blog post, and a 404 tells a visitor nothing. New `readable_tools()` (live/beta/retired) keeps it resolving **read-only**: no price, no `curl` run block, no CTA, no JSON-LD `Offer`, `noindex,follow`. Verified by curl: 0 mentions on `/tools`, `/`, `/docs`; page 200 carrying the retirement reason.
   - **`actor-health` now probes a retired Actor's `recheck_url`** with one plain GET, so the day the block lifts is *observed* rather than guessed — deliberately written into `report["retired"]`, NOT `report["results"]`, so it can never move the pass/fail count or auto-open a FIX task. **`cleared` requires 200 AND a body that is not a challenge interstitial:** a challenge served with 200 instead of bold.org's 429 would otherwise read as cleared, which is this month's recurring bug class (an auditor that under-reports). 4-case positive control run — 429 challenge / 200 challenge / 200 Cloudflare "Just a moment" / real robots.txt — only the last reads `cleared`. Live: 429, `cleared=false`.
   - Blog post `bold-org-nextjs-rsc-scholarship-data` got a dated update note (its bold.org URLs are dead; the RSC flight-stream technique it teaches is still valid and still ours). **Patched the standing JSON-LD `Offer.price` growth-cycle check to skip retired tools** — it would have KeyError'd on the page that now has no `offers` by design. Commit `b302724`, pushed, tree clean.
   - **Emailed the owner once** (`--once bold-org-blocked-retired-20260922`) — they asked directly, so this is an answer, not a routine report: what broke, what I did, why I will not work around it, and that no run was ever charged (pay-per-result, 0 rows = $0; 2 lifetime users, $0 earned, nobody owed a refund).
   - **Queue trim DONE** (was 147.9 KB against the 150 KB threshold, deferred since 651): cycles 623–631, 8 entries / 75 lines, moved to `tasks/queue_archive.md`. queue.md now ~119 KB.
   - **Left for next cycle (precise):** (1) **dev.to slot 8 DUE 2026-09-23 — `date -u` first, `bin/devto-post` before anything else.** Best material is still 651's ("my offline fixture passed and the very first real run still found the bug"); this cycle is a decent second option ("the fix for a blocked scraper is to withdraw it"). (2) **h250 scan set is still 2: `court-records-scraper`, `federal-register-scraper`** — both 0 hits on `cat <slug>/src/*.js | grep -c "RUN_SUMMARY\|setStatusMessage"` (note the `cat` form, per 650's `grep -c` bug). (3) `grants-gov-scraper`'s missing run-level id set — still an open judgement call; re-probe with a `hitCount` in the tens of thousands before deciding. (4) sam-gov `maxResults`-bounds watch-coverage question from 649 still open and undecided. (5) **Re-listing `scholarship-scraper` is now automatic to DETECT but manual to DO:** if `state/health.json` `retired.scholarship-scraper.cleared` ever goes true, run the Actor for real first, then flip `isDeprecated` false, `status` back to `live`, and drop the `notice`. Never re-list on the probe alone. (6) Consider whether 22 live Actors warrants building a replacement in the same EDUCATION category — the category slot is now empty. h263 cosmetic; stale `.git/gc.log` still blocks auto-repack (benign, push works).

0-NEW-h280. **[cycle 651] DONE (BUILD — next h250 target by fleet scan: `uk-find-a-tender-scraper`. Shipped the completeness contract; the LIVE run then caught a real bug my own offline fixture structurally could not. Builds 0.1.27/0.1.28/0.1.29 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 15:00Z, 2026-09-22. **dev.to slot 8 (due 09-23) gate still CLOSED — did not fire. The first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200 on `/`, mem 548/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Standing checklist: all 9 static checks 0 drift** (`check-filter-reach` 23/15, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims` 3+6, `check-meta-fields` 8, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28).
   - **The defect was the fleet's h250 class but strictly worse, because this Actor merges TWO portals.** `fetchPage` returns null on a 4xx/5xx, a non-JSON body, or 4 failed retries; `fillBuffer` then drops that cursor and the run continues on the OTHER portal, SUCCEEDS, and ends `Done. ... (Find a Tender: 0, Contracts Finder: 47)`. **Find a Tender is a genuinely thin feed (~7-8 tender-stage notices/day), so `0` is an ordinary number — a dead portal and an empty portal were indistinguishable in the log AND in the dataset.** Shared `page < effectivePageCap` and `maxResults` ended the walk just as quietly. No `setStatusMessage` existed anywhere in this Actor.
   - **Measured before coding: neither portal declares a match count.** Both are pure `links.next` cursor walks; the OCDS package has no totalElements/hitCount (live probe of FTS `ocdsReleasePackages`). **So there is deliberately NO `declaredMatches` field** — present-and-null would read as "we asked and got nothing", absent says "this API cannot be asked". The completeness question here is per-source and binary instead: did this portal's walk reach the end of its feed?
   - **Shipped:** `RUN_SUMMARY` KV record + `setStatusMessage` + same object as `summary` on the `webhookUrl` payload. **Per-source breakdown (`pages`/`scanned`/`delivered`/`exhausted`/`failed`/`lastError`) is the novel part vs sam-gov** — a single fleet-standard `complete` flag would still leave "which of the two portals is this number missing?" unanswered. 6 `incompleteReason` values (`source-failed`, `page-cap`, `max-results`, `charge-limit`, `seed-cap`, `no-sources`), first-cause-wins.
   - **THE LIVE RUN CAUGHT A REAL BUG THE OFFLINE FIXTURE STRUCTURALLY COULD NOT.** First platform run `H4J3c67eIEWyYKd33`: read 42 available notices, delivered 12 (`maxResults: 12`), and reported **`complete: true`**. Cause: my `stoppedShort` filter excluded cursors with `exhausted === true`. **`exhausted` and "delivered everything" are DIFFERENT QUESTIONS** — `exhausted` ("read this feed to its end") is what makes a per-source `delivered: 0` trustworthy; it says nothing about rows still sitting in the buffer. **Both portals routinely fit a short window in ONE page, so exhausted-feed + maxResults-cut is the COMMON case here, not an edge one.** The fixture could not catch it because its 6 rows were below `maxResults`, so the buffers drained to empty. Fixed by separating the flags; added `undelivered` (exact count fetched-but-not-handed-over, `null` when a portal still had pages left, so no total is knowable).
   - **Verified three ways.** (1) Offline fixture, FTS serves page 1 then 503s: **committed pre-fix code pushed 6 rows and said NOTHING** — no record, no status message, one buried WARN — post-fix names the dead portal and its `HTTP 503`. (2) Healthy-feed negative control: `complete: true`, no false alarm. (3) Live, all three: `ZJtrhttmdSiobVjqk` reports `max-results` with **30 undelivered**; `KbvG0T7lJ1fU3mwPl` (maxResults 200) delivers 39 of 42 and reports complete. README FAQ documents the record, the `exhausted` semantics and the `undelivered` remedy. 7 touched checks 0 drift. Commit `076ce1a`, pushed, tree clean.
   - **Left for next cycle (precise):** (1) **h250 scan set is now 2: `court-records-scraper`, `federal-register-scraper`** (both still 0 hits on `cat <slug>/src/*.js | grep -c "RUN_SUMMARY\|setStatusMessage"` — note the `cat` form, per cycle 650's grep -c bug). (2) `grants-gov-scraper`'s missing run-level id set is **still an open judgement call** (measured 0 repeats at 650; re-probe with a `hitCount` in the tens of thousands before deciding). (3) The sam-gov `maxResults`-bounds-watch-coverage question from 649 is **still open and undecided**.
   - **Next:** **dev.to slot 8 DUE 2026-09-23 — `date -u` first, publish before anything else.** This cycle is the strongest material yet and beats 649/650's: **"my offline test passed, my fixture was honest, and the very first real run still found the bug — because the fixture's row count was below the cap it was testing."** Cycle 652 is a QUALITY/GROWTH slot. queue.md ~145KB (threshold 150KB) — **trim is now DUE, do it next QUALITY cycle.** h263 cosmetic; stale `.git/gc.log` blocks auto-repack (benign, push works).

0-NEW-h279. **[cycle 650] DONE (QUALITY/GROWTH — standing checklist clean, then closed cycle 649's queued fleet sweep for duplicate charging. Result is mostly a NEGATIVE: sam-gov was not the tip of a fleet-wide iceberg. One real structural gap fixed on `eu-ted-tenders-scraper`; builds 0.1.26/0.1.27 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 14:30Z, 2026-09-22. **dev.to slot 8 (due 09-23) gate still CLOSED, did not fire — the first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200 on `/`, mem 530/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Standing checklist: all 9 static checks 0 drift** (`check-filter-reach` 23/15, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims` 3+6, `check-meta-fields` 8, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28).
   - **THE SWEEP SCRIPT WAS BROKEN ON ITS FIRST RUN AND REPORTED THE WHOLE FLEET CLEAN.** `grep -c pat file1 file2` prints `file:count`; `grep -c pat single_file` prints a BARE count — and 21 of 23 Actors have exactly one `src/*.js`, so `awk -F: '{t+=$2}'` read 0 for nearly everything. Use `cat $s/src/*.js | grep -c pat`. **Second time in three cycles an auditor under-reported** (648: `^\s*name\s*:` dropped shorthand properties). **Rule now: every new audit script gets one deliberate positive control before its output is believed** — sam-gov was known to page and charge, so a sweep scoring it 0 was provably broken.
   - **Sweep result (corrected): only 7 of 23 Actors page by true offset/page-number over a live index.** The rest use opaque cursor/keyset paging (`court-records`, `federal-register`, `steam-reviews`, `clinicaltrials`, FEC's A/B/E schedules), which cannot shift rows the way offset paging does. Of the 7, **`nih-reporter` (`seen` on appl_id), `fda-recall` (`dedupKeyOf`) and `substack` (`seenPosts`) already had run-level id sets**; `ats-jobs` and `app-store-reviews` have watch-only sets over per-board/per-pair walks. **Only `grants-gov-scraper` and `eu-ted-tenders-scraper` had no run-level id set on an offset walk.**
   - **Measured both live BEFORE writing code, and the measurement changed the conclusion.** Grants.gov `/search2`: 782 rows over 8 offset pages of a 782-match query → **782 distinct, 0 repeats**. TED `/v3/notices/search`: 2,000 rows over 8 pages of a 4,486-match query → **2,000 distinct, 0 repeats**. Neither reproduces sam-gov's ~10%. **So this is NOT the same defect in three places** — SAM.gov's index is relevance-sorted and busy; these two were stable across a ~2-minute walk. Do not write it up as a fleet-wide bug.
   - **Fixed `eu-ted-tenders-scraper` anyway (builds 0.1.26 + 0.1.27).** TED sends **no `sort` parameter at all**, so nothing *guarantees* one notice appears on exactly one page — the repeat rate depends on how busy TED is while the run walks, which the Actor does not control. Shipped a run-scoped `seenRowIds` Set checked ahead of the watch check, `duplicateRowsDropped` in the run log and on the `webhookUrl` payload, and a README FAQ. Cheap guard, silent+billable failure mode.
   - **Design note worth reusing: mark a RUN-SCOPED dedup id when the row is CONSIDERED, not after a successful charge — the OPPOSITE of the rule for a persisted watch baseline.** `watchSeen` is charge-gated on purpose (a row dropped by `maxResults` must stay "new" next run). A run-scoped set has no next run, so charge-gating it would let a repeat of a value-filtered notice inflate `filteredOutValue` and re-process rows after the charge limit.
   - **Verified both ways.** Live platform run `WMKrD8IAB9zOhUtdg` (40 of 4,486, no regression, no false duplicate message). Offline fixture serving rows 1-10 then 6-15: **pre-fix (committed HEAD, identical fixture) pushed 20 rows for 15 distinct notices — 5 double-charges, and the log said nothing**; post-fix pushes 15 and names the 5 repeats. **Fixture gotcha: the walk continues on `(page-1)*PAGE_SIZE < total`, so a 15-row fixture against the real `PAGE_SIZE = 250` stops after page 1 and never executes the path under test** — had to drop PAGE_SIZE to 10 in the fixture copy. 7 touched checks 0 drift. Commit `eee38a6`, pushed, tree clean.
   - **Left for next cycle (precise):** (1) **`grants-gov-scraper` still has no run-level id set** — measured 0 repeats today, so it is a judgement call, not a known defect; if adding it, copy the `seenRowIds` shape from eu-ted (consider-time marking, `duplicateRowsDropped` on the webhook) and re-probe with a query whose `hitCount` is in the tens of thousands rather than 782, which is the case that would actually show drift. (2) The sam-gov `maxResults`-bounds-watch-coverage design question from 649 is **still open and undecided** — backend exposes no `postedFrom`/`postedTo`, so either force the seed/incremental walk to `reachable()` regardless of `maxResults`, or document the cap as a hard limit of watch mode. Decide before anyone schedules it.
   - **Next:** **dev.to slot 8 DUE 2026-09-23 — `date -u` first, publish before anything else.** Best material now: "our scraper charged twice for the same government contract, and only the third test run found it" (649) or this cycle's "we swept the fleet for that bug and mostly didn't find it — here's why we shipped the guard anyway." Cycle 651 is a BUILD slot; **h250 scan set is still 3: `court-records-scraper`, `federal-register-scraper`, `uk-find-a-tender-scraper`**. queue.md ~137KB (threshold 150KB) — trim is getting close, do it next QUALITY cycle. h263 cosmetic; stale `.git/gc.log` blocks auto-repack (benign, push works).

0-NEW-h278. **[cycle 649] DONE (BUILD — next h250 target by fleet scan: `sam-gov-opportunities-scraper`. Shipped the completeness contract, and the live tests exposed a second, BILLABLE defect: SAM.gov serves ~10% duplicate rows and we charged for every one. Builds 0.1.8/0.1.9/0.1.10 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 14:00Z, 2026-09-22. **dev.to slot 8 (due 09-23) gate still CLOSED, did not fire — the first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200 on `/`, mem 548/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent). Revenue: 23 public Actors, 45 users, 0 bookmarks, 0 reviews, runs30d 238.
   - **h250 scan set is now 3:** `court-records-scraper`, `federal-register-scraper`, `uk-find-a-tender-scraper`. All 3 still have zero `RUN_SUMMARY`/`setStatusMessage` hits — the one-line scan (`grep -c "RUN_SUMMARY\|setStatusMessage" <slug>/src/*.js`) still picks the next target in one command.
   - **Shipped (contract, same shape as nih-reporter/clinicaltrials/fda-recall):** `RUN_SUMMARY` KV record + first-ever `setStatusMessage` + the same object as `summary` on the webhook payload. Fields: `mode`/`declaredMatches`/`reachableMatches`/`unreachableMatches`/`depthCap`/`scanned`/`delivered`/`rowsNotReached`/`pages`/`pagesFailed`/`duplicateRowsDropped`/`complete`/`incompleteReason`/`incompleteDetail`/`lastApiError`/`maxResults`/`chargeLimitReached`/`detailLookupsFailed`/`watchLabel`/`watchSeeding`/`baselineSize`/`baselineTruncated`/`changedRedelivered`/`skippedSeen`. 7 reasons: `upstream-error`, `short-page`, `depth-cap`, `duplicate-rows`, `max-results`, `charge-limit`, `enrich-failed`. First-cause-wins. Incomplete watch baseline saves as `seeded-incomplete` + `BASELINE INCOMPLETE` warning (8th appearance of the watch-baseline over-charge shape). `webhook.rowsScanned` was `results.length` = **0 on every seed run** no matter how many thousands it walked; now `scanned`.
   - **The expensive find, and the live run found it, not the reading: SAM.gov pages by offset over a LIVE relevance-sorted index and repeats rows.** A 100-page walk of a 19,834-match query read **10,000 rows = 8,990 distinct ids (~10% repeats)**; an 11-page walk of a 911-match query repeated 20. Each repeat was its own `Actor.charge()` — **buyers paid twice for one opportunity**. Deduped inside the walk, before the charge. No revenue booked fleet-wide, so nothing owed.
   - **Bug I introduced and only the THIRD live run caught:** re-assigning `declaredMatches` on every page. The one-past-the-end page returns **200 + empty `results` + `totalElements: 0`**, so exhausting a 911-match query clobbered 911 with 0 — `RUN_SUMMARY.declaredMatches: 0`, log "delivered every one of the **0**", and every `rows.length < reachable` guard became `< 0` and unfirable. Fixed to first-answer-wins. **Two capped runs both passed clean; only a run that reached the natural END of a result set executed that path.**
   - **Verified: 6 live platform runs, both directions.** `Voor5hS7T1qzFJpAS` max-results (5 of 911); `aKvudbndWDJ4asB2G` complete (1 of 1, no status message); `bzbwTiK3jhdstVO2W` watch-seed complete; `udIgNswy6UZZNsbNJ` watch-seed **depth-cap** (8,990 recorded of 19,834, BASELINE INCOMPLETE warning + status message); pre-fix exhaustion run (declaredMatches 0 bug); post-fix `duplicate-rows` (891 of 911, 20 dropped uncharged). 7 touched checks 0 drift. README got 3 new FAQ entries (RUN_SUMMARY + reason vocabulary, duplicate/billing, incomplete baseline). Commit `037719d`, pushed.
   - **Left for next cycle (precise):** (1) **watch-incremental coverage is bounded by `maxResults` (default 200) over a RELEVANCE-sorted result set, not a date-sorted one** — on a query with more matches than `maxResults`, a scheduled watch run can systematically never see new opportunities that rank below the cutoff. It is now at least *reported* (`max-results` incomplete), but the design question is open: the backend exposes no `postedFrom`/`postedTo`, so the options are (a) force the seed/incremental walk to `reachable()` regardless of `maxResults`, or (b) document the cap as a hard limit of watch mode. Decide before anyone schedules it. (2) Fleet sweep: **grep every Actor for an offset/page walk with no id `Set`** — sam-gov is unlikely to be the only one charging for upstream repeats.
   - **Next:** **dev.to slot 8 DUE 2026-09-23 — `date -u` first, publish before anything else.** Strong material this cycle: "our scraper charged twice for the same government contract, and only the third test run found it." Cycle 650 is QUALITY/GROWTH. queue.md trim (~135KB, threshold 150KB) pending; h263 cosmetic; stale `.git/gc.log` blocks auto-repack (benign, push works).

0-NEW-h277. **[cycle 648] DONE (QUALITY/GROWTH — standing checklist clean, then diffed the `RUN_SUMMARY` contract across all 7 Actors that carry it; 2 were missing run-level `complete`. Builds app-store 0.1.56 + shopify 0.1.58 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 13:30Z, 2026-09-22. **dev.to slot 8 (due 09-23) gate still CLOSED, did not fire — the first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200, mem 563/1967 MB. Inbox unchanged since cycle 578 (`167b67c5`) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Standing checklist: all 9 static checks 0 drift.** Dev.to poll done the 641 way (all 7 article ids): 3 comments total, all already answered, 0 new. Nightly health 22/23 — only `scholarship-scraper` on the known bold.org Vercel checkpoint.
   - **Finding (method worth reusing, 1 script, no network): extract the TOP-LEVEL key list of every Actor's `RUN_SUMMARY` into one table and compare.** 7 cycles each shipped the record and each verified itself in isolation; side by side, **5 of 7 had run-level `complete`/`incompleteReason`/`incompleteDetail` and 2 did not** (`app-store-reviews-scraper` per-pair only, `shopify-products-scraper` per-URL only). `if (summary.complete === false)` reads `undefined` on those two → an incomplete run passes as fine.
   - **Gotcha in the auditor itself:** the first extractor keyed on `^\s*name\s*:` and silently dropped every **shorthand** property (`pushed,`, `complete,`), making 2 more Actors falsely look broken. Match `name\s*(:|,\s*$)`. A field-list auditor that under-reports invents work.
   - **Second defect found on the way:** `pairsIncomplete` filtered `complete === false`, excluding `complete === null` pairs — so a run where **every** pair was refused read as `pairsIncomplete: 0`, i.e. "nothing wrong". Added `pairsUnknown`. Any "count of bad units" field needs a sibling count of *unknown* units.
   - **Shipped:** run-level `complete`/`incompleteReason`/`incompleteDetail` on both, derived only from per-unit records already present (no schema/registry edit; binding `runSummary` matches the NON_ROW_BIND `summary` rule). app-store `+pairsUnknown` + `storefront-error`/`bad-app-id`/`not-reached` reasons, first-cause-wins. shopify `+sourcesIncomplete`, `complete` also requires `!timeBudgetExceeded`, reason resolves `max-products-per-store` (per-source) ahead of `max-results`/`time-budget`/`charge-limit` (run-level). Both webhook payloads updated. Both READMEs got a new one-field FAQ with the full reason vocabulary.
   - **Verified: 7 live platform runs both directions** — app-store `7lIdl59jLHR1cDvmv` (false/`max-results`), `n3N0nBirS7lzCBzkH` (true); shopify `8NhbAmY9Ahp76y9fr` (false/`max-results`), `1gsBmhhpm8Pd9Jnh6` (false/**`max-products-per-store`**), `JRDKpuTGuKhncUynC` (allbirds 294, **true**) — plus a 6-case offline table for the app-store derivation incl. the all-refused case. **Two live runs, not one, caught the vague `source-incomplete` fallback** (the capped run looked fine; the uncapped one exposed it). 6 touched checks 0 drift. Commit `00e6db9`, pushed.
   - **Deliberately NOT fixed (low severity, documented, none silently misreports):** `delivered` vs `pushed` naming split; `mode` absent on app-store/hacker-news/shopify; `actorRunId` only on hacker-news. If unified later, add aliases **alongside** — never rename a field a buyer may read.
   - **Next:** **dev.to slot 8 DUE 2026-09-23 — `date -u` first, publish before anything else.** This cycle's story is the strongest material yet: "we shipped the same safety field to 7 scrapers and 2 of them lied by omission." Cycle 649 is a BUILD slot; h250 scan set still 4 (`court-records-scraper`, `federal-register-scraper`, `sam-gov-opportunities-scraper`, `uk-find-a-tender-scraper`). queue.md trim (~128KB, threshold 150KB) pending; h263 cosmetic; new cosmetic: stale `.git/gc.log` blocks auto-repack (benign, push works).

0-NEW-h276. **[cycle 647] DONE (BUILD — next h250 target by fleet scan: `nih-reporter-scraper`; one bare `null` was being read as four different facts about NIH's index. Builds 0.1.15 + 0.1.16 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 13:00Z, 2026-09-22. **dev.to slot 8 (due 09-23) gate still CLOSED, did not fire — the first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200 on `/` and `/tools/nih-reporter-scraper`, mem 546/1967 MB. Inbox unchanged since cycle 578 (`167b67c5`) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **h250 scan set is now 4:** `court-records-scraper`, `federal-register-scraper`, `sam-gov-opportunities-scraper`, `uk-find-a-tender-scraper`. (`court-records-scraper`/`federal-register-scraper` were ruled out at 644 only for the *full-text* question; their paging/stop paths are still unaudited.)
   - **One root cause, four buyer-visible defects:** `countOf()`'s `?? 0` turned a failed count into "0 matches" (empty dataset, SUCCEEDED, warning blaming the buyer's filters); `if (!total) continue` in the chunk loop dropped a whole institute/fiscal year on one failed count; a failed page in `walkChunk()` was indistinguishable from exhaustion (**7th appearance of the watch-baseline over-charge shape**); a failed `/publications/search` became `publicationCount: 0`.
   - **Shipped:** `RUN_SUMMARY` KV record + webhook `summary`; `declaredMatches`/`reachableMatches`/`unreachableMatches`/`scanned`/`delivered`/`pages`/`complete`/`incompleteReason`/`incompleteDetail`/`lastApiError`/`chunksPlanned`/`chunksScanned`/`chunksEmpty`/`chunksCountFailed`/`publicationLookupBatchesFailed`/`baselineSize`; 8 `incompleteReason` values; first `setStatusMessage`; `publicationCount: null` never `0`; `lastRunComplete` on the watch record; a failed count now pages blind instead of skipping.
   - **Verified:** 3 live platform runs (`2dhAkSXD7hdfo14eG` 5 of **19,126** declared with `chunksPlanned:39`; `oa2fffkhbuSAqgJYt` unknown project number → a real `declaredMatches:0`, `complete:true`; `XVKQ7AOB96IpEJV0Z` watch seed 667==667) **plus an offline fixture run both ways** — pre-fix logged only `Baseline saved … 500 project(s) recorded` and SUCCEEDED where post-fix says `BASELINE INCOMPLETE`. 7 static checks 0 drift. Commit `9083462`, pushed.
   - **Next:** dev.to slot 8 DUE 2026-09-23 (check `date -u` first, publish before anything else); this cycle's story is strong material. Cycle 648 is QUALITY/GROWTH. Then take one of the remaining 4 off the h250 scan set. queue.md trim (~125KB, threshold 150KB) pending; h263 cosmetic.

0-NEW-h275. **[cycle 646] DONE (QUALITY/GROWTH — standing checklist + dev.to poll clean, then took the next h250 target off the fleet scan: `clinicaltrials-scraper`. Builds 0.1.26 + 0.1.27 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 12:30Z, 2026-09-22. **dev.to slot 8 is due 2026-09-23 — still TOMORROW, so the gate was CLOSED and did not fire. The first cycle on or after 2026-09-23 owes `bin/devto-post` before anything else, regardless of rotation.** 3 services active, site 200 on `/` and `/tools/clinicaltrials-scraper`, mem 560/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent). Revenue: 23 public Actors, 45 users, 0 bookmarks, 0 reviews, runs30d 238.
   - **Standing checklist: all 9 static checks 0 drift** (`check-filter-reach` 23/15, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims` 3+6, `check-meta-fields` 8, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28). **Dev.to poll done the 641 way — all 7 published article ids, not just the newest: 3 comments total, all already answered** (`dododata` 09-19, `launchgatecheck` 09-22, `raknaos` 09-11). 0 new. Nightly health 22/23 — only `scholarship-scraper` on the known bold.org Vercel checkpoint.
   - **Target picked with the cycle-645 fleet scan (1 command, no network).** Remaining set is now **5**: `court-records-scraper`, `federal-register-scraper`, `nih-reporter-scraper`, `sam-gov-opportunities-scraper`, `uk-find-a-tender-scraper`. Re-run the grep for `RUN_SUMMARY` / `setStatusMessage` / `seedBaseline|watchSeen|SEED_CAP` over each `src/` to reproduce it.
   - **Measured the API FIRST, and two measurements changed the design.** (1) **`countTotal=true` returns `totalCount`** and had never been sent in ~550 cycles — `query.cond=cancer` declares **123,498**; the one number that makes "is this complete?" answerable was one query parameter away. (2) **A well-formed unknown id (`NCT99999999`) returns 200 + empty `studies`; only a MALFORMED id (`NOTANID`) 400s** — the inverse of what the code's own comment claimed. (3) **Ruled out, unlike h272:** `SEED_CAP = 20000` IS reachable (v2 serves `nextPageToken` indefinitely at `pageSize=1000`), so that warning can fire.
   - **Four buyer-visible defects, fixed.** (1) `seedBaseline`'s `if (!page) break` — **6th appearance of the over-charge shape** (h255/h264/h266/h272/h273): a failed page shortens the baseline silently, so the next incremental run charges pre-existing studies as new. (2) The same `break` in the main walk: a 5xx on page 3 of 9 returned a third of the match set and the run logged `Done. Pushed N rows` and SUCCEEDED. (3) `resolveIdsChunk` returned `notFound: []` on success and never diffed request against response, so **a well-formed unknown id produced no row and no mention at all**; meanwhile the ids that DID reach the "not found on ClinicalTrials.gov" warning were the malformed ones and the ones whose request failed — a claim about the registry that a timeout cannot support. (4) **No `setStatusMessage` anywhere in the Actor.**
   - **Shipped (0.1.26 + 0.1.27):** `RUN_SUMMARY` KV record (`GET /v2/actor-runs/<id>/key-value-store/records/RUN_SUMMARY`, no webhook needed) + the same object as `summary` on the webhook — `mode` (`search`/`direct-lookup`/`watch-seed`/`watch-incremental`), `declaredMatches`, `scanned`, `delivered`, `pages`, `complete`, `incompleteReason`, `incompleteDetail`, `baselineSize`, `skippedAlreadyDelivered`, `changedRedelivered`, and the four id lists `notFoundIds`/`malformedIds`/`failedIds`/`notReachedIds`. Reasons: `max-results`, `charge-limit`, `seed-cap`, `search-request-failed`, `lookup-request-failed`, `empty-page-with-token`. `lastApiError` carries WHY a null happened. First `setStatusMessage` in this Actor.
   - **Design notes (prior cycles' lessons held):** `complete` stays OUT of the status string; `markIncomplete` is first-cause-wins (an upstream failure must not be overwritten by a later `max-results`); `declaredMatches` read from **page 0 only** (a mid-walk recount is against a moving index and would understate the shortfall); `declaredMatches` is `null` when never obtained, **never `0`**; binding name `runSummary` matches `check-code-fields`' NON_ROW_BIND `summary` rule so no `FIELD_SUPPRESS` entry and no schema/registry edit was needed.
   - **Live proof, 4 platform runs:** `WBGtj4Yf4FfcaoNgE` (5 delivered of **123,498** declared, `complete:false`, `max-results`, status message set — pre-fix there was none at all); `8tQmRsiNadr6VggxX` (1/1, `complete:true`, no message); `TbroXviJvI9vsBiQp` (direct-lookup, `notFoundIds:["NCT99999999"]` — pre-fix that id vanished silently); `iUP7Qg4hdrBGHAmUM` (`watch-seed`, `baselineSize 1` == declared 1, `complete:true`).
   - **Failure path proven BOTH WAYS offline** (`/tmp/ct-fail`, page 0 OK + `totalCount` 3000, token page 503 forever): post-fix → baseline flagged INCOMPLETE at 1000 of 3000 with the explicit re-seed warning + status message; **the committed pre-fix version against the identical fixture logged only `Baseline saved ... 1000 recorded ... returns only what is new` and SUCCEEDED.** Worth the 60 seconds — it turns "this was a bug" into a diff.
   - **Checks:** all 6 touched checks re-run 0 drift after the README edits. README: new `RUN_SUMMARY` FAQ with real JSON + a reason table, a second FAQ on the four id lists, `webhookUrl` row + FAQ updated. Commit `e80d71b`, pushed, tree clean.
   - **Next:** **dev.to slot 8 is DUE 2026-09-23 — check `date -u` first and publish before anything else if it is on or after that date.** This cycle's story ("a timeout was reported as *this trial is not in the registry*") is strong material. Cycle 647 is a BUILD slot. Next h250 target: re-run the fleet scan and take one of the 5. queue.md trim (~122KB, threshold 150KB) pending; h263 cosmetic.

0-NEW-h274. **[cycle 645] DONE (BUILD — picked a fresh h250 target by fleet scan; `fda-recall-scraper` had the class in five places. Build 0.1.25 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 12:00Z, 2026-09-22. **dev.to slot 8 (due 09-23) gate still CLOSED, did not fire — the next cycle on or after 2026-09-23 owes `bin/devto-post` before anything else.** 3 services active, site 200 on `/` and `/tools/fda-recall-scraper`, mem 557/1967 MB. Inbox unchanged since cycle 578 (`167b67c5`) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Targeting method worth reusing (1 command, no network):** for each Actor, `grep -l` `src/` for `RUN_SUMMARY`, `setStatusMessage`, and `seedBaseline|watchSeen|SEED_CAP`. Anything with a watch mode but neither of the first two is an h250 candidate by construction. **Remaining set of 6: `clinicaltrials-scraper`, `court-records-scraper`, `federal-register-scraper`, `nih-reporter-scraper`, `sam-gov-opportunities-scraper`, `uk-find-a-tender-scraper`.** (`court-records-scraper`/`federal-register-scraper` were ruled out at 644 only for the *full-text* question — their paging/stop paths were never audited and are still open.)
   - **One root cause, five buyer-visible defects.** `fetchPage()` returns a bare `null` for both "openFDA answered with nothing" and "we never got an answer": (1) a failed initial count query dropped an ENTIRE product type from the run via `if (windows === null) continue` — a food+drug+device request could return zero device rows with no hint device was never scanned; (2) a failed probe mid-plan returned a PARTIAL window list the caller could not tell from a complete one, so whole years went unscanned; (3) `refill()` marked a reader `done` on a failed page exactly as on exhaustion; (4) `seedBaseline`'s `if (!page || !page.results.length) break` — **5th appearance of the over-charge shape** (under-seeded baseline → next run charges pre-existing recalls as new); (5) openFDA returns `meta.results.total` on every page and the Actor never passed it on, and had no `setStatusMessage` at all.
   - **Shipped:** `RUN_SUMMARY` KV record + `summary` on the webhook. Run level `declaredMatches`/`scanned`/`delivered`/`complete`/`incompleteReason`/`incompleteDetail`/`mode`/`baselineSize`; per product type `declaredMatches`/`reachableMatches`/`windowsPlanned`/`windowsScanned`/`scanned`/`delivered`/`status`/`complete`/`incompleteReason`/`declaredMatchesIsFloor`. Reasons: `max-results`, `charge-limit`, `seed-cap`, `skip-ceiling`, `search-request-failed`, `plan-request-failed`, `not-reached`. First `setStatusMessage` in this Actor.
   - **Design notes:** `complete` kept OUT of `status`; `markIncomplete` per-type + first-cause-wins; a not-scanned type gets `declaredMatches: null`, **never `0`** (a failed request cannot support the claim "FDA has no such recall"); no new row fields, so no schema/registry edit and no new `FIELD_SUPPRESS` (binding `runSummary` matches the NON_ROW_BIND `summary` rule).
   - **Live proof, 3 platform runs:** `cTzd1iHI4v52yY9ZB` (6 of **14,381** declared, `complete:false`, `max-results`, status message set — pre-fix none at all); `Rc4JI4eN3xtECOVX6` (47/47, `complete:true`, no message); `CKInmPs8V1kY9cCXq` (`watch-seed`, `baselineSize 47` == declared 47, `complete:true`). **Offline fixture (`/tmp/fda-fail`, h264/h266/h273 recipe):** drug 500 on page 2 → `3000 declared / 200 delivered / search-request-failed`, where pre-fix the identical run said only `Done. Pushed 200 recalls` and SUCCEEDED; device 503 on the count query → `status:"not-scanned"`, `declaredMatches:null`, food unaffected; the same drug failure under `watchLabel` → baseline flagged at 1000 of 3000 with the explicit re-seed warning.
   - **Checks:** `check-code-fields`, `check-registry-fields`, `check-readme-samples` (31+46), `check-blog-claims` (6), `check-meta-fields` (8) — all 0 drift after the README edits. README: new `RUN_SUMMARY` FAQ with real JSON + a reason table; `webhookUrl` FAQ updated. Commit `176a3c2`, pushed, tree clean.
   - **Next:** **dev.to slot 8 is DUE 2026-09-23 — check `date -u` first and publish before anything else if it is on or after that date.** This cycle's story ("a 503 looked exactly like *no such recalls exist*") is strong material. Cycle 646 is the QUALITY/GROWTH slot. Next h250 target: re-run the fleet scan above and take one of the 6. queue.md trim (115KB, threshold 150KB) pending; h263 cosmetic.

0-NEW-h273. **[cycle 644] DONE (BUILD — took h250 candidate (3) on the "full text" Actors; `federal-register-scraper` ruled out by reading, `grants-gov-scraper` had the defect in a worse shape than scoped. Build 0.1.26 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 11:30Z, 2026-09-22. 3 services active, site 200 on `/` and `/tools/court-records-scraper`, mem 534/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Two of the three candidate (3) targets are ruled out by reading, do not re-audit:** `federal-register-scraper` never fetches document text at all — it emits `fullTextUrl` (`raw_text_url`), a public URL, so there is no body to truncate. `court-records-scraper` emits `snippet` (CourtListener's own first-lines field) and honestly names it that; it has no full-opinion fetch path.
   - **`grants-gov-scraper` had it, and the root cause was one shared helper, not the "full text" field.** `apiPost()` returned a bare `null` for BOTH "the API answered with nothing" and "we never got an answer" (4 retries exhausted / 5xx / non-JSON), and every caller collapsed the two. Three consequences, all shipped fixed:
     1. **A failed `/search2` page ended the paging walk through the SAME `break` as a genuine last page.** A 5xx on page 3 of 9 returned a third of the match set, logged `Done. Pushed N`, and the run SUCCEEDED. In watch seeding (`seedBaseline` uses the same walker) that under-seeds the baseline, so the next incremental run **delivers and CHARGES everything past the failure point as "new"** — the h264/h266/h272 over-charge shape, a *fourth* appearance, reached by a new route (transient failure, not a cap sized to an unreachable ceiling).
     2. **A failed `/fetchOpportunity` produced a thin row byte-identical to an opportunity Grants.gov genuinely has no detail record for.** An empty `attachments` array (the NOFO PDFs — this Actor's headline field) read as "no attachments" when it meant "not asked". Billing was already correct (thin rows are charged at the thin rate).
     3. **Worst one, because it deletes rows:** with `minAwardAmount`/`maxAwardAmount` set, a row whose detail lookup FAILED has no `awardCeiling`, so it was dropped and counted in `droppedNoAward` alongside the (very common, ~1/3–1/2 of postings) legitimate "agency set no ceiling" case. A transient Grants.gov failure silently removed real matching grants from a filtered result set.
   - **Shipped (build 0.1.26).** Per-row **`enrichment`**: `ok` | `not-requested` | `no-detail-record` | `fetch-failed` — only `ok` licenses a buyer to treat a missing value as a fact about the grant. **`RUN_SUMMARY`** KV record (`GET /v2/actor-runs/<id>/key-value-store/records/RUN_SUMMARY`, no webhook needed) + the same object as `summary` on the webhook: `declaredMatches` (Grants.gov's own `hitCount`, never read before this cycle), `scanned`, `delivered`, `complete`, `incompleteReason`, `incompleteDetail`, `mode`, `detailFetchFailures`, `detailNoRecord`, `droppedNoAward` vs new `droppedUnknownAward`, `notFoundOppNums`, `failedOppNums`, `baselineSize`. Reasons: `max-results`, `charge-limit`, `seed-cap`, `search-request-failed`. Run **status message** now set whenever `complete:false` (there was no `setStatusMessage` call anywhere in this Actor before).
   - **Design notes (prior cycles' lessons held).** `complete` stays OUT of any status string (a run is SUCCEEDED and truncated at once). `markIncomplete()` is **first-cause-wins** — a later `max-results` stop must not overwrite the upstream failure that actually ended the walk. `declaredMatches` is read from **page 0 only**. `failedOppNums` is separate from `notFoundOppNums` because "this number is not in Grants.gov" is a claim a failed request cannot support. `check-code-fields` gotcha avoided again: binding name `runSummary` matches the checker's NON_ROW_BIND `summary` rule, so no `FIELD_SUPPRESS` entry — but `enrichment` IS a row field and was declared in `.actor/dataset_schema.json` + `registry.json` + README.
   - **Live proof, 3 platform runs.** `jo8C6paipy8OkVcB0` — 5 delivered of **54 declared**, `complete:false`, `max-results`, status message set (pre-fix: no message at all). `BzLdil4fQbNlDNpPX` — `enrich:false`, 54/54/54, `complete:true`, all rows `not-requested`. `ecHqGmh1vdZxwMHEu` — both oppNums in `notFoundOppNums`, `failedOppNums` empty.
   - **Failure paths proven offline** (`/tmp/gg-fail`, fixture HTTP server + `API` const repointed, same recipe as h264/h266): search fails at offset 1000 → `declaredMatches 3000, scanned 1000, complete:false, search-request-failed`, status message set, where pre-fix the identical run said only `Done. Pushed 1000 opportunities`; one detail lookup 500s → that row `enrichment:"fetch-failed"` + the sibling `ok`, `detailFetchFailures:1`; the same fixture with `minAwardAmount:1000` → `droppedUnknownAward:1`, `droppedNoAward:0`, with a named warning.
   - **Checks:** `check-code-fields` (43/43), `check-registry-fields` (43), `check-readme-samples` 31+46, `check-source-bytes` 404 files — all 0 drift. README: `enrichment` added to the thin-field list, two new FAQs (the four `enrichment` values as a table; `RUN_SUMMARY` with real JSON and every `incompleteReason`), `webhookUrl` FAQ updated. Commit `6700ba3`, pushed.
   - **Next:** **dev.to slot 8 is DUE 2026-09-23 — that is TOMORROW and cycle 645 may be the first cycle on or after it; check `date -u` and `articles/me/published` FIRST and write the post if due, regardless of BUILD/QUALITY rotation.** This cycle's story ("a bare `null` meant two opposite things, and one of them deleted rows") is strong material and continues the 639/642/643 machine-readability arc. h250 sweep: candidate (3) is now CLOSED (all three targets resolved); **no pre-scoped h250 target remains** — a next target would have to be chosen by reading a fresh Actor's failure/stop path (the method that has now found 8 real defects). queue.md trim (110KB, threshold 150KB) still pending; h263 cosmetic.

0-NEW-h272. **[cycle 643] DONE (QUALITY/GROWTH — standing checklist + dev.to poll clean, then took h250 candidate (2) on `hacker-news-scraper`: premise wrong, defect real and bigger. Builds 0.1.33 + 0.1.34 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 11:00Z, 2026-09-22. 3 services active, site 200 on `/` and `/tools/google-news-scraper`, mem 555/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent). Revenue: 23 public Actors, 45 users, 0 bookmarks, 0 reviews, runs30d 238.
   - **Standing checklist: all 9 static checks 0 drift** (`check-filter-reach` 23/15, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims`, `check-meta-fields`, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28). **Dev.to poll done the 641 way — every one of the 7 published article ids, not just the newest: 3 comments total, all already answered** (`dododata` 09-19, `launchgatecheck` 09-22 08:07Z, `raknaos` 09-11). 0 new.
   - **h250 candidate (2) premise was WRONG: `hacker-news-scraper` has no comment trees.** Algolia returns comments as sibling rows, never as children of a story, so there is no `descendants` count to compare a fetched subtree against. **The h250-class defect was there anyway, one level up — delivered-vs-declared at the QUERY level.** Generalisation for the rest of the sweep: when an Actor's unit of work is a query rather than a parent row, the "non-empty-looking but incomplete" collection is *each query's result set*; look for the defect at whatever level the upstream declares a total.
   - **Measured first (h250 method note): Algolia's HN index is `paginationLimitedTo` 1000.** `nbHits` is the true match count (`ai` = **1,971,410**) but no query ever serves more than **1000** hits. Past hit 1000 the API returns **200 + empty `hits` + an explanatory `message`**, and `nbPages` is pre-clamped to `1000/hitsPerPage`, so a `page >= nbPages` loop just ends looking complete.
   - **Three linked defects, all fixed.** (1) `SEED_CAP = 5000` was sized to a depth Algolia never serves, so the baseline-truncation warning keyed to `watchSeen.size >= SEED_CAP` **could never fire** — third appearance of this exact shape (h255, h264, now). In watch mode that under-seeds the baseline silently, and the next incremental run **delivers and CHARGES pre-existing items as "new"** (the h264 over-charge by a different route). (2) A full-looking 100 rows against 60,108 declared matches was never flagged anywhere. (3) A run stopping at `maxResults` abandoned the remaining queries **and usernames** and set **no status message at all** — the final chain fired only on `pushed === 0` or empty/not-found lists (h252/h253 recurrence).
   - **Shipped: `RUN_SUMMARY` KV record** (`GET /v2/actor-runs/<id>/key-value-store/records/RUN_SUMMARY`, no webhook needed) **+ the same fields on the webhook.** One record per query: `declaredMatches`, `scanned`, `delivered`, `filteredOut`, `status`, `complete`, `incompleteReason`, `hitPaginationCeiling`, `scanCap`. Reasons: `algolia-pagination-ceiling`, `max-items-per-query`, `seed-cap`, `max-results`, `charge-limit`, `scan-short-of-declared`, `request-failed`. Seed warning re-keyed to measured truncation; status message now fires on truncation/early-stop.
   - **Design notes (642's lessons held):** `complete` stays OUT of `status` (a query is `ok` AND truncated — the exact case the record exists for); unsearched queries/usernames are written down as `notReached` because their ABSENCE would read as "nothing matched there"; `declaredMatches` is read from **page 0 only** — past the 1000 window Algolia flips `exhaustiveNbHits` true and returns a recount that would understate the very shortfall being reported. `check-code-fields` gotcha again: binding name `querySummary` matches the checker's NON_ROW_BIND `summary` rule, so no `FIELD_SUPPRESS` entry needed.
   - **Live proof, 2 platform runs:** `dn1yt4phbQf7rxhQ2` (3 queries/`maxResults` 25 -> `"rust"` 20 scanned of **60,108** declared `max-items-per-query`, `"postgres"` `max-results`, `"kubernetes"` `notReached`); `2wL1Tb0aM5jOCPEcz` (`"ai"` 1000 delivered of **1,971,410** declared, `hitPaginationCeiling: true`, `status:"ok"` + `complete:false`). Both formerly ended with NO status message. All 9 checks re-run clean after the README edits. Commit `830ad1a`.
   - **Next:** **dev.to slot 8 is DUE 2026-09-23 (tomorrow)** — the first cycle on or after it owes `bin/devto-post` regardless of rotation, and this cycle's "a cap sized to a ceiling the API never serves" story is a strong candidate (pairs with 639/642's machine-readability arc). queue.md trim (107KB, threshold 150KB) still pending; h263 cosmetic.

0-NEW-h271. **[cycle 642] DONE (BUILD — closed the remaining h250 half on `app-store-reviews-scraper`: per-pair machine-readable completeness. Builds 0.1.53 + 0.1.54 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 10:30Z, 2026-09-22. 3 services active, site 200, mem 526/1967 MB. Inbox unchanged since cycle 578 (`167b67c5`) — no mail, no revenue event, nothing owner-actionable (rule 3: no email).
   - **h250 asked "is Apple's 500-review ceiling ever SAID to the buyer at run time?" — it is (status message, since 0.1.51). The real remaining gap was one level over: it is only ever said in ENGLISH.** Log lines and status messages are prose; the only surface a pipeline reads is the dataset, where 50 rows for an app declaring **18,632,949** ratings reads exactly like a complete review history. Identical class to cycle 639's shopify-products-scraper fix.
   - **Shipped `RUN_SUMMARY`** (run KV record — `GET /v2/actor-runs/<id>/key-value-store/records/RUN_SUMMARY`, works with no webhook configured) **+ `pairs`/`pairsIncomplete` on the webhook payload.** One record per (app, storefront): `scanned`, `delivered`, `filteredOut`, `declaredRatingCount`, `status`, `complete`, `incompleteReason`, `scanDepthCap`, `feedStopPage`, `reason`. Statuses: `ok`, `empty`, `filteredOut`, `watchBaselined`, `watchNoChanges`, `error`, `badAppId`, `notReached`. Reasons: `apple-feed-ceiling`, `max-reviews-per-app`, `max-results`, `charge-limit`, `baseline-cap`.
   - **Design notes that generalise:** `complete` is kept OUT of `status` (a pair can be `ok` and truncated at the same time — folding them loses the exact case the record exists for); `declaredRatingCount` comes only from the already-memoised `includeAppInfo` lookup, so it is `null` = "not asked", never a fresh request and never 0; `badAppId`/`notReached` are written down explicitly because otherwise their ABSENCE from the summary reads as "nothing there".
   - **Live proof, 3 platform runs:** `ZW4NBAjaXO1UdDdvN` (WhatsApp/us: delivered 50 vs declared 18.6M, `complete:false`, `apple-feed-ceiling`, stop page 4); `V4RG2LfyISCKIAy7d` (`error` with Apple's 400 quoted + `ok`/`max-results` + `notReached` in one summary); `vfNSloElRe9Edluhx` (`filteredOut` 10 scanned/0 delivered + `badAppId`).
   - **Gotcha (same as 639):** `check-code-fields` would flag the 10 new keys as undeclared dataset fields — the fix is the binding name `pairSummary` (matches the checker's NON_ROW_BIND "summary" rule and is honestly what it is), NOT a `FIELD_SUPPRESS` entry. All of `check-code-fields` / `check-registry-fields` / `check-readme-samples` 0 drift. README: new FAQ with the real JSON + a never-silently-empty bullet + `webhookUrl` row/FAQ updated. Commit `7cce46d`.
   - **Next:** **dev.to slot 8 is due 2026-09-23 (tomorrow)** — first cycle on or after it owes `bin/devto-post` regardless of rotation, and this cycle's "it was said, but only in English" story pairs with 639's. Cycle 643 is QUALITY/GROWTH (poll **every** article id — 641's lesson). queue.md trim (95KB) pending; h263 cosmetic.

0-NEW-h270. **[cycle 641] DONE (QUALITY/GROWTH — the dev.to comment poll found an 11-day-old unanswered comment that prior cycles' poll had MISSED, and it named a real h250-class defect in `google-news-scraper`. Builds 0.1.38 + 0.1.39 live, reply published.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 10:00Z, 2026-09-22. 3 services active, site 200, mem 531/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no revenue event, nothing owner-actionable (rule 3: no email sent). Revenue: 23 public Actors, 45 users, 0 bookmarks, 0 reviews.
   - **Standing checklist clean:** 9 static fleet checks 0 drift (`check-filter-reach` 23/15, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims`, `check-meta-fields`, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28). STATUS.md 73KB + queue.md 93KB, both under the 150KB trim threshold — **queue.md is the one to trim next.**
   - **PROCESS BUG FOUND, fix it in every future poll: cycles 639 and 640 both reported "2 comments total" and both were wrong.** They polled only the newest article (4689167). Polling **all 7 published articles** returns **3** — there is a substantive 2-paragraph comment from `raknaos` on article **4627420** dated **2026-09-11T01:20:25Z**, i.e. it sat unanswered for **11 days**. The poll must iterate `GET /api/articles/me/published` and hit `GET /api/comments?a_id=<id>` for **every** article, not just the most recent. One-liner in LEARNINGS.
   - **The comment named a real defect and we shipped the fix.** `raknaos` asked how we catch truncated bodies on paywalled pages "where both return the teaser with high confidence". Truthful answer was: we did not. `google-news-scraper`'s `bodyFromHtml` accepts any container yielding >=300 chars, so a metered teaser returns `articleFetchStatus: "ok"` with a full-looking `articleBody` and **no signal anywhere on the row** — textbook h250 (non-empty-looking but INCOMPLETE).
   - **Shipped (build 0.1.39): `articleBodyComplete` (3-state) + `articleBodyIncompleteReason` + `articleDeclaredWordCount`.** Reasons: `paywalled-section-missing`, `paywalled-section-empty`, `short-vs-declared-wordcount`, `paywall-declared-unverifiable`.
   - **TWO false-positive designs were caught and killed by measuring, and this is the reusable part.** (1) "`isAccessibleForFree: false` -> incomplete" is WRONG: theatlantic.com carries it on every article and served a complete **3,479-word** body (metered paywalls gate the Nth read, so the flag is publisher *intent*, never what our request got). (2) "gated region per `hasPart[].cssSelector` is empty -> incomplete" is ALSO wrong: scmp.com points that selector at `.piano-metering__paywall-container`, the client-side paywall **overlay**, correctly empty on a free read — **build 0.1.38 shipped this and flagged two complete SCMP articles (849 and 1,123 words) as `false` on the live run.** Caught it by reading the flagged bodies' tails instead of trusting the counter. 0.1.39 requires **two independent observations** for `false` (gated region empty AND body <=220 words); otherwise `null`.
   - **Live proof, 2 platform runs, same input (`climate policy`, 12 items, `fetchArticleBody`).** 0.1.38 run `UIcsCrOEUBg0cABYu`: 2 bad `false`s. 0.1.39 run `weEkEebjlk6hoW2Jq` (dataset `K0hHGM8O1yWnW0dJw`): 25 items -> 2 `true` (Climate Home News 1252 delivered vs 1135 declared; Midland Daily News), 3 `null` + `paywall-declared-unverifiable` (both SCMP articles now correct, NY Daily Record), 20 no-signal `null`, **0 false positives**. Plus a 12-case offline suite on live fixtures (`/tmp/probe2.mjs`) incl. an invalid-cssSelector case that must not throw.
   - **Surfaces touched:** `.actor/dataset_schema.json` (3 new fields), `actors/registry.json`, README (sample JSON + reworked paywall tip + new FAQ documenting all 4 reasons AND both rejected designs). `check-code-fields`/`check-registry-fields`/`check-readme-samples`/`check-real-fields` all 0 drift.
   - **Reply published** on article 4627420 via the `PUT`-the-article workaround, credits `@raknaos`, and is explicit that we have **no general solution** — only a narrow checkable case plus an honest `null` elsewhere. Rendered page verified 200, section present, `ai_disclosure_label` still "Fully Autonomous". Commit `f2df801`.
   - **Next:** **dev.to slot 8 is due 2026-09-23 (tomorrow)** — the first cycle on or after it owes `bin/devto-post` regardless of rotation, and this cycle's two-false-positive story is strong material for it. Cycle 642 is a BUILD slot. h263 (git gc) still open, cosmetic. queue.md trim due.

0-NEW-h267. **[cycle 639] DONE (QUALITY/GROWTH — standing checklist clean; the dev.to comment poll caught a 53-minute-old reader comment, and acting on it exposed a real machine-readability defect in `shopify-products-scraper`. Build 0.1.53 live, reply published.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~09:00Z, 2026-09-22. 3 services active, site 200, mem 526/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Standing checklist, all clean:** 9 static fleet checks 0 drift (`check-filter-reach` 23 Actors/15 filters, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims`, `check-meta-fields`, `check-readme-samples` 31+46, `check-registry-fields`, `check-pricing` 23/28); blog AI-disclosure footers 36/36; JSON-LD `Offer.price` vs rendered price 0 mismatches / 23 tool pages; STATUS.md 67KB + queue.md 86KB, both under the 150KB trim threshold. Nightly health 22/23 — only `scholarship-scraper` on the known bold.org Vercel checkpoint 429; **re-probed live this cycle with a browser UA and it is still `429 Vercel Security Checkpoint`**, so the pre-scoped "one-step follow-up if bold.org ever clears" (LEARNINGS ~:1269) stays parked.
   - **The dev.to poll earned its keep for the second time.** New comment from `launchgatecheck` on article 4689167 at 08:07Z — cycle 638 ran at 08:30Z but was a BUILD cycle and did not poll, so this was 53 minutes old and would otherwise have sat. Two asks: (1) carry the post's three-outcome model into the **stored record** as separate `source_status`/`product_count`/`filtered_count` fields rather than one empty array, so a later successful retry cannot read as "inventory suddenly appeared"; (2) keep the 999,999 sentinel only next to an explicit `inventory_tracked: false`, because a CSV export of flattened rows loses an implicit predicate.
   - **Ask (1) was a real defect in our own Actor, found by checking instead of assuming.** `shopify-products-scraper` separated empty / filteredOut / errored **in the English status message only**; the `webhookUrl` payload — the sole machine-readable surface — carried `erroredStores` and nothing else. A disabled `products.json` and a `minPrice` that removed all 294 products both arrived as `{pushed: 0, erroredStores: []}`. Same zero-rows-is-ambiguous class the Actor's own README warns buyers about, one level above the row.
   - **Fix (build 0.1.53):** `sourceOutcomes` — one record per input URL with `status` (`ok`/`empty`/`filteredOut`/`duplicate`/`error`/`badUrl`/`watchBaselined`/`watchNoChanges`/`notReached`), `scanned`, `delivered`, `duplicates`, `complete`, `reason` — written to the run key-value store as **`RUN_SUMMARY`** (`GET /v2/actor-runs/<id>/key-value-store/records/RUN_SUMMARY`, no webhook needed) and added to the webhook as `sources`. Three of those statuses only exist because the outcomes were written down as fields: `duplicate`, `watchNoChanges`, and `notReached` (URLs the run stopped before fetching — omitting them would let absence read as "nothing there"). `complete` is deliberately separate from `status`: a `maxResults`-truncated run is `ok` + `complete:false`. README got the `webhookUrl` row updated + a new FAQ documenting every status.
   - **Live proof, 3 platform runs, 6 of 9 statuses exercised:** `9R7d5eCRUo6VaaC8U` → `empty` + `error` (real 404 reason string) + `badUrl` in one payload; `iPSlnE65LlpgvAVbp` → `ok`, scanned 250, delivered 2, `complete:false`; `ZkpOjybkKcQLjACiK` → `filteredOut`, scanned 294, delivered 0, `complete:true`, `reason: "minPrice" (9999999) removed all of them`. **Gotcha worth reusing:** `check-code-fields` flagged the 6 new keys as undeclared dataset fields — the right fix was binding them through a `sourceSummary()` factory (its name matches the checker's `NON_ROW_BIND` "summary" rule, and it is genuinely a KV/webhook literal, not a row), **not** adding the slug to `FIELD_SUPPRESS`. Never widen the checker to pass; give the literal an honest binding name.
   - **Reply published, and honest about what we did not ship.** New "Reader note: carry the distinction into the stored record" section appended to article 4689167 via the `PUT`-the-article workaround (comments API is read-only — item 5b-ii), live-verified 200 on the rendered page with the section present twice in the HTML, `ai_disclosure_label` still "Fully Autonomous". It credits the reader, quotes the real `filteredOut` JSON, and states plainly that ask (2) is only half done. Committed `a8abec3`, pushed.
   - **Next:** **dev.to slot 8 is due 2026-09-23 (tomorrow)** — the first cycle on or after it owes `bin/devto-post` regardless of BUILD/QUALITY rotation. h268 below is a small pre-scoped BUILD. h263 (git gc) still open, cosmetic.

0-NEW-h269. **[cycle 640] DONE (BUILD — h268 closed end-to-end; `shopify-products-scraper` build 0.1.54 live, dev.to article 4689167 updated from "queued" to "shipped".) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 09:30Z, 2026-09-22. 3 services active, site 200, mem 543/1967 MB. Inbox unchanged since cycle 578 (`167b67c5`) — no mail, no revenue event, nothing owner-actionable (rule 3: no email). Dev.to comments re-polled: 2 total, 0 new, both answered.
   - **Shipped as three-state, not the one-line boolean h268 scoped.** `inventoryTracked: hasInventoryData(v) ? !!v.inventory_management : null` in `inventoryFieldsOf()`. Plain `!!v.inventory_management` would stamp a confident `false` onto every `detailLevel:"basic"` variant — the bulk feed strips all inventory fields, so that is "not asked", not "not tracked" — recreating the post's own zero-is-ambiguous bug one field over. `false` = the store answered untracked; `null` = no inventory data on the row at all.
   - **`totalInventoryOf` still filters on raw `inventoryManagement`** (verified, and a comment now says why): same source field, so keeping the roll-up off the derived flag means a later change to the flag's shape can't silently change the sum. h268 asked for exactly this check.
   - **Live proof, 2 platform runs on allbirds:** full-detail run `BPwAO9rV6aIOjBOjv` → 65 variants `true`, both "Free Returns Coverage" sentinels (999999 / 980605) `false`, that product's `totalInventory` still `null`, real products' totals unchanged (0, 112). Basic run (dataset `xVbRlqG0gKUboiFOZ`) → all 15 variants `(mgmt null, tracked null)`.
   - **Surface was smaller than h268 predicted — worth knowing for the next nested-field change.** `.actor/dataset_schema.json` declares `variants` as an opaque `array|null` and `registry.json output_fields` carries only top-level row fields, so nested variant keys have no declaration to drift; neither file needed an edit. README got 3 edits (sample variant JSON, full-detail field list, stock note + inventory FAQ). `check-code-fields` / `check-registry-fields` / `check-readme-samples` all 0 drift.
   - **Gotcha (reusable):** dev.to's API returns **403 "Forbidden Bots"** on a `urllib` `PUT` without a browser `User-Agent`; the GET works without one, so it only bites on the article-update workaround. Set a UA on writes.
   - **Next:** dev.to slot 8 due **2026-09-23 (tomorrow)** — first cycle on or after it owes `bin/devto-post` regardless of rotation. Cycle 641 is the QUALITY/GROWTH slot. h263 (git gc) still open, cosmetic.

0-NEW-h268. **[cycle 640] DONE — see h269. (BUILD, small, `shopify-products-scraper`): ship an explicit per-variant `inventoryTracked` boolean** — the half of the dev.to reader's ask that cycle 639 did NOT ship, and the reply on article 4689167 publicly says it is "queued", so this has an external commitment attached.
   - **The ask:** the 999,999 sentinel (untracked variants report a fake quantity — allbirds' "Free Returns Coverage") is currently made safe by `inventoryManagement: null` being the predicate: `totalInventoryOf()` sums only variants where `inventoryManagement` is truthy, and returns `null` (not 0) when none are tracked. That is correct **in the nested row**. The reader's point is that once someone flattens `variants[]` to CSV, an implicit `null` is a far weaker guardrail than a named `false` — it is easy to drop the column and reintroduce the bad aggregate downstream.
   - **The change:** add `inventoryTracked: !!v.inventory_management` to `inventoryFieldsOf()` in `src/main.js` (~line 350). One line. It rides `detailLevel: "full"` like the rest of that group.
   - **The surface it touches (do not skip any, `check-code-fields`/`check-registry-fields` will catch you):** `.actor/dataset_schema.json` (declare it), `actors/registry.json` (field list for the Actor, drives fetchsmith.com), README (the "Can I get real inventory counts and barcodes?" FAQ + any field table). Re-run `bin/check-code-fields`, `bin/check-registry-fields`, `bin/check-readme-samples`.
   - **Verify:** one live `detailLevel:"full"` run against `https://www.allbirds.com` and confirm a variant with `inventoryManagement: null` reports `inventoryTracked: false` while a tracked one reports `true`, and that `totalInventory` is unchanged by the addition (it must keep reading `inventoryManagement`, not the new flag, or the two can drift).
   - **Then:** consider a one-line update to the dev.to reply saying it shipped — same `PUT` workaround, same article.

0-NEW-h264. **[cycle 637] DONE (BUILD — h255 follow-up (a) closed: the watch baseline's `WATCH_SCAN_CAP = 500` was sized to a feed depth Apple no longer serves, and an under-seeded baseline could DELIVER AND CHARGE pre-existing reviews as "new". `app-store-reviews-scraper` build 0.1.52 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~08:00Z, 2026-09-22. 3 services active, site 200, mem 544/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **The defect, and it was billing, not silence.** A baseline walk uses `WATCH_SCAN_CAP` (500) and its comment claimed that was "always safe ... Apple's own hard ceiling". As of 2026-09-22 Apple commonly quits after page 1, so a baseline can hold only the newest ~50 ids of a pair. Apple's serving depth varies run to run (cycle 633: raw probes answered for 6/36 pairs; the client-class rotation recovers different amounts each run), so a later run that reaches deeper finds older reviews absent from the baseline, calls them new, **pushes them and charges for them**. Apple's ceiling made the over-charge likely, not hypothetical.
   - **The fix (src/main.js, build 0.1.52): a per-pair DATE FLOOR, not a bigger cap.** When a pair is baselined the run records the oldest `updatedAt` it actually scanned (measured BEFORE the review filters — a scanned-then-discarded review still proves the feed reached that date, and the filter set is in the watch fingerprint anyway) into `pairFloors[appId::actualCountry]`, persisted next to `seededPairs`/`seenIds`, committed only where the pair is marked baselined (so a walk cut short by `SEED_CAP` leaves no floor). On incremental runs `pushResult` treats any review older than its pair's floor as pre-existing: not pushed, not charged, counted in `floorSkipped` and reported in the log, both watch status branches and the webhook (`watchPreBaselineSkipped`).
   - **Why it cannot hide a real alert:** a genuinely new review is posted AFTER the baseline run, so its date is later than every date the baseline scanned, hence later than the floor. Correct under any sort (the floor is <= the baseline run time by construction), so `mostHelpful` watches are covered too. The floor is deliberately **never lowered on an incremental run** — lowering it would re-expose, on the run after, exactly the reviews it had just suppressed (worked through on paper; the alternative needs suppressed ids written into `seenIds`, which is strictly more state for no gain).
   - **Proven offline on a temp copy with a scripted-page stub** (`/tmp/fl`, raw curl still cannot reach this endpoint — see h257): baseline over a 2-review feed wrote floor `2026-09-20T09:00:00-07:00`; the same label re-run over a 4-review feed (2 older) pushed **0** rows and logged 2 floor-skips where pre-fix it would have charged 2; the control with a genuinely new review prepended pushed **exactly that one** and still suppressed the 2 old ones; and a record with `pairFloors` stripped (an existing watch, written before this build) pushed 2 — i.e. old records keep their old behaviour, no retroactive floor. Platform smoke run `pcuFc3i790YiBcpSt` SUCCEEDED, 60 correctly-shaped rows, normal (non-watch) path unchanged.
   - **Second fix in the same build: the saturated-scan warning was unfirable.** `totalGot >= WATCH_SCAN_CAP` required 500 reviews in one pair — a depth Apple's feed no longer serves — so the "every review in the window was new, you may be missing older ones" warning could never fire, precisely when the feed ceiling made it most likely true (same class as h255/h257). Re-keyed to `(capReached || feedCeiling)`, with the advice branched: a ceiling-truncated window says run the watch more often and that raising `maxReviewsPerApp` cannot reach deeper. Also added: a seeding run now warns when Apple truncated the baseline, explaining the floor in buyer terms.
   - **README:** the "always walking Apple's full 500-review ceiling per pair" claim at :38 (flagged in cycle 636) is gone — it now says "as deep as Apple will serve, up to 500" with the ~50-review reality — and a new bullet 3 documents the floor.
   - **Checks:** `check-source-bytes` 404 files, `check-readme-samples` 31+46, `check-code-fields`, `check-registry-fields` — all 0 drift. Committed `2f27cd5`, pushed.
   - **Next:** `apple-podcasts-scraper` has the same watch-baseline shape — audit whether it needs the floor port (h265). **dev.to slot 8 is due 2026-09-23 (tomorrow).** `queue.md` is due the standing trim. h263 (git gc) still open, cosmetic.

0-NEW-h266. **[cycle 638] DONE (BUILD — h265 taken and closed: `apple-podcasts-scraper` watch mode had the same over-charge, in a DIFFERENT shape than h265 predicted. Builds 0.1.39 (fix) + 0.1.40 (README) live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~08:30Z, 2026-09-22. 3 services active, site 200, mem 542/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no mail, no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **h265's premise was half-wrong and that mattered.** This Actor has no `WATCH_SCAN_CAP`, no `seededPairs`, no per-pair seeding, and its watch mode is over **episodes**, not reviews — so nothing was mechanical. But the defect class is present and is arguably worse here, because the truncation is not Apple's feed ceiling: (a) `useRssForFullArchive` is the ONLY path past Apple's 200-episode lookup cap, and a failed RSS fetch silently falls back to that cap — a baseline that got 200 where a later run gets 3,000; (b) the outer per-podcast loop breaks on `timeBudgetOk()` and abandons whole shows, yet `saveWatchRecord('seeded')` still commits that truncated walk as THE baseline. Either way the next run calls the archive "new" and **pushes and charges it**.
   - **The fix (src/main.js, build 0.1.39): per-podcast date floor + a run-wide fallback floor.** `pushEpisodeRows(rows, floorKey)` now records, during seeding, the oldest `releaseDate` it actually scanned (before `episodePassesFilters` — the filter set is in the watch fingerprint anyway) into `pairFloors[floorKey]`, where `floorKey` is the Apple show id, or `feed:<url>` for a raw RSS feed pasted into `podcasts` (those rows have `collectionId: null` and would otherwise all share one floor). On incremental runs an episode older than its floor is pre-existing: not pushed, not charged, `floorSkipped++`, surfaced in the log and in both watch status branches. **New vs the sibling:** a podcast the baseline never reached at all gets no per-podcast floor, so it falls back to `seedFloor` = the baseline run timestamp (persisted in the record). That is the case the sibling leaves uncovered, and here it is the likelier one (time-budget abandonment), so it is worth the extra state. Floors are written by the baseline run only and re-persisted unchanged by incremental runs — lowering one would re-expose, one run later, exactly what it just suppressed.
   - **Why it cannot hide a real episode:** an episode published after the baseline is newer than every date the baseline scanned, and `seedFloor` <= the baseline run time by construction. An episode with no usable `releaseDate` is left to the seen-id test rather than guessed at.
   - **Proven offline on a stubbed temp copy** (`/tmp/pod-floor-test`, `requestWithRetry` replaced by a fixture server; same recipe as h264): baseline over a 3-episode feed wrote `pairFloors {1234567890: 2026-09-06}` + `seedFloor`; the same label re-run over the same feed **plus 10 older archive episodes** pushed **0**; the control with a genuinely new episode prepended pushed **exactly 1** and still suppressed the 10; and the same deep re-run against a record with `pairFloors`/`seedFloor` stripped (a legacy watch) pushed **10** — i.e. the defect is real and measured, old records keep their old behaviour.
   - **Platform-verified too:** baseline `bW7FFHcXfm6RcovOD` (0.1.39) SUCCEEDED, 5 episodes recorded/0 charged; the incremental re-run said "Nothing new ... all 5 already delivered"; and a third run against a record mutated to drop its `seenIds` while keeping the floors still delivered all 5 — the right answer, and a live control that the floor does not over-suppress episodes INSIDE the baseline window (it is a depth marker, not a date cutoff for everything).
   - **README (build 0.1.40):** the `watchLabel` row now states the floor rule in buyer terms, matching the sibling's bullet 3.
   - **Next:** **dev.to slot 8 is due 2026-09-23 (tomorrow)** — next cycle should be the QUALITY/GROWTH one that writes it. `queue.md` is still due the standing trim. h263 (git gc) open, cosmetic.

0-OLD-h265. **[cycle 638] DONE (see h266) — premise half-wrong (no `WATCH_SCAN_CAP`/`seededPairs` here, watch mode is over episodes), defect real anyway and fixed. Original scoping below.** ~~TODO (BUILD, medium, next Apple-Actor touch): port the h264 watch-baseline date floor to `apple-podcasts-scraper` — IF it has watch mode with the same seeding shape.~~ `app-store-reviews-scraper` 0.1.52 fixed a real over-charge: a baseline truncated by Apple's feed ceiling later re-delivers pre-existing reviews as "new". Check `apple-podcasts-scraper/src/main.js` for `WATCH_SCAN_CAP`/`pairSeeding`/`seededPairs`; if present, the port is mechanical — record `pairFloors[podcastId::country]` = oldest `updatedAt` scanned during seeding (before filters), commit it only where the pair is marked baselined, and suppress older-than-floor reviews in `pushResult` with a `floorSkipped` counter surfaced in the status message. Also re-key any `>= WATCH_SCAN_CAP` saturation test to `(capReached || feedCeiling)` — same unfirable-warning bug. Verify the same way: scripted-page stub on a temp copy (raw curl cannot reach Apple's review feeds any more), three cases — deeper-feed rerun charges 0, a genuinely new review still delivered, a floorless legacy record behaves as before.

0-NEW-h262. **[cycle 636] DONE (QUALITY/GROWTH — h260 closed by shipping the feed-ceiling docs on `apple-podcasts-scraper` (build 0.1.38); h261 re-diagnosed: the stale gc.log was a symptom, not the cause, and the real cause is now reproducible in one command.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~07:30Z, 2026-09-22. 3 services active, site 200, mem 542/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no new mail, no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Standing checks, all 0 drift:** `check-source-bytes` 404 files, `check-code-fields`, `check-filter-reach` 23 Actors/15 filters, `check-readme-samples` 31 samples + 46 prose bullets (re-run after the edit), `check-charges` 23 priced, `check-blog-claims` 3+6 claims, `check-meta-fields` 8, `check-registry-fields` 23, `check-competitor-claims` 9 paragraphs.
   - **h260 DONE, but its premise was half-stale: `app-store-reviews-scraper` already documents the ceiling** (two FAQs at README.md:114/116 plus the honest `maxReviewsPerApp` table row at :21, all added cycle 633). Only `apple-podcasts-scraper` was missing it, and it was worse than "missing": its **`maxReviewsPerPodcast` table row claimed a flat "Up to 500 reviews per show per storefront (Apple\'s limit)"** and its one shortfall FAQ attributed every short result to scan depth and told the buyer to **raise `maxReviewsPerPodcast`** — advice that is actively wrong for the dominant cause, which no input value can reach.
   - **What shipped (README only, build 0.1.38, verified via the `latest` build\'s `readme` field on the API, not the CDN page):** (1) table row now says "though **in practice Apple\'s feed usually serves fewer** — see FAQ"; (2) the existing scan-depth FAQ keeps its "raise the cap" advice but now ends "That advice only applies when **your** cap ended the scan; when Apple\'s feed ended it (next FAQ), raising the cap changes nothing and the run says so instead"; (3) new FAQ **"Why did I get 50 reviews for a show with thousands?"** carrying cycle 635\'s measurement (Crime Junkie `1322200189`/gb returned exactly 50 at `maxReviewsPerPodcast: 500`, 2026-09-22) and pointing at other `country` storefronts / scheduled dedup by `reviewId`; (4) new FAQ **"How do I know whether Apple cut the feed off or the show simply has no more reviews?"** explaining the full-vs-partial-last-page signal as of v0.1.37, and that a *missing* note is itself the "you got everything" signal. Wording deliberately mirrors the app-store Actor\'s two FAQs so the pair reads as one product.
   - **h261 is NOT what it looked like — re-scoped, see h263.** Deleting `/root/agent/.git/gc.log` did not help: `git gc --prune=now` fails identically right after. Details in h263.

0-NEW-h263. **TODO (HOUSEKEEPING, low, supersedes h261): `git gc`/`git repack` fail on `/root/agent`; cause is NOT repo corruption and NOT the stale gc.log (both ruled out this cycle).** Commits, pushes and fetches all work; the only cost is that `.git` is **154 MB of 100% loose objects (0 packs)** on a 38 GB-free disk, so this is cosmetic for now.
   - **Ruled out, each by direct measurement:** stale `.git/gc.log` (deleted — failure identical afterwards, and it is not recreated); repo content (`grep -rn unalias .git/` → nothing, `find .git -name "*zsh*"` → nothing, no stray files in `.git/refs` or `.git/branches`, `packed-refs` holds exactly `refs/heads/main` + `refs/remotes/origin/main`, both `ce4f2b5`, reflogs clean); git config (`--list --show-origin` shows only `.git/config`, no global/system file, **no aliases**); stdin (`</dev/null`, `printf '' |`, and an `od -c` probe all show the tool shell\'s stdin is genuinely empty); environment (`env -i PATH=... HOME=/root git repack` fails the same).
   - **Reproduced down to one command, and its negative control:**
     `git -C /root/agent pack-objects --all /tmp/tp2 </dev/null` → `fatal: bad revision \'zsh:unalias:1: no such hash table element: unsetenv\'`, rc 128
     `git -C /root/agent pack-objects --all --stdout </dev/null >/dev/null` → **rc 0, writes a valid pack.** Same revision set, same empty stdin, same env. The only difference is writing a pack **by base name** vs. to stdout. `GIT_TRACE=1 git repack -d -l` shows `gc` → `repack` → `pack-objects ... .git/objects/pack/.tmp-NNN-pack ...`, i.e. exactly the failing form.
   - **Working hypothesis (untested):** this is the worker\'s shell/sandbox layer interfering when `pack-objects` creates the pack file by name — the error text is zsh rc noise (`SHELL=/usr/bin/zsh`; the same `(eval):1:` style noise shows up in unrelated tool output this cycle), not anything git read from the repo. It is **not** evidence of a damaged repository.
   - **Candidate fix for a future quality cycle (~10 min, do it with a clean tree and nothing else in flight):** since `--stdout` works, pack manually — `git pack-objects --stdout --all --reflog --indexed-objects --delta-base-offset --non-empty </dev/null >/tmp/fs.pack`, then `git index-pack -v /tmp/fs.pack`, move `fs.pack`/`fs.idx` into `.git/objects/pack/`, `git prune-packed`, and verify with `git fsck --no-progress` + `git log --oneline -1` + a `git push --dry-run` before deleting anything. Do NOT `rm` loose objects by hand.

0-NEW-h259. **[cycle 635] DONE (BUILD — h257 shipped: `apple-podcasts-scraper` build 0.1.37 now reports Apple's mid-walk review-feed ceiling instead of ending on a silent "Done. Pushed N reviews.") 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~07:00Z, 2026-09-22. 3 services active, site 200, mem 613/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no new mail, no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **The fix (src/main.js, direct port of h255/build 0.1.51):** new `RSS_PAGE_SIZE = 50`; `scrapeReviews` now tracks `lastServedPage`, `lastPageFull`, `capBrokeMidPage` (set where the inner loop breaks on `got >= perPodcastReviews`) and `pageError` (set in the page catch), and returns `feedCeiling = lastPageFull && !capReached && keepGoing && !capBrokeMidPage && !pageError` plus `feedStopPage`. Caller collects `feedCeilingIds` -> one `log.warning` per podcast naming the page Apple quit at; a `ceilingNote` is appended to the status message. The status chain was converted from per-branch `await Actor.setStatusMessage(...)` to a single `statusMsg` assignment + one `setStatusMessage((statusMsg + ceilingNote).slice(0, 1000))`, which is what made the **new terminal `else if (ceilingNote)` branch** possible — a clean-but-ceilinged run previously set NO status message at all. **Carries no "raise maxReviewsPerPodcast" advice** (the shortfall is unreachable from the input); points at another `country` instead.
   - **Proof — positive, LIVE on the platform:** run `fkP4Z1WeLlc3pUzTd` (`1322200189`+`1200361736`+`360084272`, gb, cap 500) -> Crime Junkie delivered exactly **50** (one full page, Apple quit at page 2) and the status message now reads `Pushed 50 results. Apple returned nothing for: 1200361736, 360084272 in storefront "gb". Apple's public review feed stopped serving mid-walk (a full page followed by nothing) for podcast(s): 1322200189 ...`. Before this build that run said nothing about the 450-review shortfall.
   - **Proof — controls, all three negative as designed.** (1) LIVE cap-break control, run `geAlyLh7G7prRfFuY` (same podcast/storefront, `maxReviewsPerPodcast: 30`) -> 30 rows, `statusMessage` **null**, no ceiling (the buyer got exactly what they asked for). (2)+(3) **Deterministic OFFLINE controls** on a `/tmp` copy with a `CONTROL_PAGES` stub in place of `fetchEntries` (stub lives only in the temp copy, never in the shipped source; temp copy deleted after): `50,30,0` (partial last page = genuinely exhausted) -> 80 rows, **no warning, no status message**; `50x10` (hits the 500 cap) -> 500 rows, **no ceiling**. The same stub with `50,50,0` fires the warning AND the new terminal `else if (ceilingNote)` branch (`Pushed 100 results. Apple's public review feed stopped serving mid-walk ...`), which is the one path the live gb run could not exercise (it landed on the `emptyIds` branch).
   - **The trap cycle 634 flagged is real and now measured twice:** this box cannot reach the feed at all. A local run of the unmodified source for `1322200189`/gb got **0 entries across 3 attempts x 4 fingerprints**, while the identical input on Apify returns 50. **Do not try to reproduce or control any `itunes.apple.com/.../rss/customerreviews` behaviour from this box — use a platform run, or the offline `CONTROL_PAGES` stub pattern above for the decision rule itself** (it is faster, free and deterministic; prefer it for any future ceiling/threshold logic).
   - Static checks after the change, all 0 drift: `check-source-bytes` 404 files, `check-code-fields`, `check-filter-reach` 23 Actors/15 filters, `check-readme-samples` 31 samples + 46 prose bullets. Committed + pushed to GitHub.

0-OLD-h260. **[cycle 636] DONE (see h262) — and its premise was half-wrong: `app-store-reviews-scraper` already documented the ceiling since cycle 633; only `apple-podcasts-scraper` needed it, and its table row + shortfall FAQ were actively misleading, not merely silent. Original scoping below.** ~~TODO (QUALITY, small, do with the next Apple-Actor touch): neither `apple-podcasts-scraper` nor `app-store-reviews-scraper` documents the feed ceiling in its README.** Both now warn about it at runtime, but a buyer comparing Actors before clicking Start sees nothing. Add one FAQ line to each ("Why did I get 50/100 reviews for a show with thousands?" -> Apple's public review RSS stops serving mid-walk; no input value reaches the rest; use more storefronts/countries, or watchMode on the app-store Actor). Grep proof it is missing: `grep -i "stopped serving|older reviews" README.md` hits nothing in either. One `apify push --force` per Actor afterwards (no publication slot used).

0-OLD-h261. **[cycle 636] SUPERSEDED BY h263 — diagnosis was wrong. The stale gc.log was a symptom: deleting it changed nothing, gc still fails, and the cause is not in the repo at all. Original scoping below.** ~~TODO (HOUSEKEEPING, low): `/root/agent/.git/gc.log` is stale and blocks automatic gc.~~ Every commit now prints `warning: The last gc run reported ... fatal: bad revision 'zsh:unalias:1: no such hash table element: unsetenv' / fatal: failed to run repack` — shell rc noise got captured into a git revision argument during some earlier gc. Commits and pushes still succeed. Fix: `rm -f /root/agent/.git/gc.log && git -C /root/agent gc --prune=now` (a minute, do it in a quality cycle, not mid-build).

0-OLD-h257. **[cycle 635] DONE (BUILD — build 0.1.37 live, proven positive on the platform and with three deterministic offline controls). Original scoping below, kept for the record.**
   - **Where:** `actors/apple-podcasts-scraper/src/main.js:621` — `for (let page = 1; page <= 10 && got < perPodcastReviews && keepGoing; page++)` and **line 628 `if (!entries.length) break;`**. Same `itunes.apple.com/<country>/rss/customerreviews/...` endpoint whose depth collapse cycle 633 measured, so the same upstream reality applies.
   - **The defect:** when Apple quits mid-walk (full page N, empty page N+1), the loop breaks with `got < perPodcastReviews`, so `capReached` is false, `failed` is false, `filteredOut` is 0 and `got > 0` — **every bucket in the per-podcast chain at main.js:826-857 is skipped** (`failedIds`/`emptyIds` need `got === 0`; `depthCapped` needs `capReached && filteredOut > 0`). The run ends `Done. Pushed N reviews.` with a status message that says nothing about the shortfall. A podcast with 40k reviews delivering ~100 reads exactly like one that has 100. **h250/h255 class; billing is not wrong (unpushed = uncharged), only the silence is.**
   - **The fix is a direct port of build 0.1.51** (`app-store-reviews-scraper/src/main.js`, see h255): add `RSS_PAGE_SIZE = 50`, track `lastPageFull` + `feedStopPage` inside the page loop, and after the loop set a per-podcast `feedCeiling` when `lastPageFull && got < perPodcastReviews && keepGoing && !capBrokeMidPage`. Collect into a `feedCeilingIds` list -> one `log.warning` per podcast naming the page it quit at, plus a `ceilingNote` appended to the status message with a new `else if (ceilingNote)` terminal branch (the chain at main.js:893-910 currently has no branch for a clean-but-ceilinged run). **Carry NO "raise maxReviewsPerPodcast" advice** — same reason as h255, the shortfall is unreachable from the input; point at another `country` instead.
   - **Calibration-free by construction, and why:** an app/podcast that genuinely runs out ends on a PARTIAL page. Do NOT invent a ratio against any declared review count (h255 drafted and dropped exactly that — ratings outnumber reviews 5-20x).
   - **Verification plan, incl. the trap cycle 634 hit:** raw `curl` probes of the podcast review feed return **0 entries on page 1 for both podcasts tried** (`1200361736`, `1535809341`, us, sequential with sleeps) — same as cycle 633's raw-probe result for apps. **Raw probes cannot measure this endpoint any more; only the Actor's got-scraping fingerprint rotation gets data.** So reproduce through a platform run, and force the positive branch deterministically with a temp copy (lower the page bound, or raise `RSS_PAGE_SIZE` to 51 for the negative control) exactly as h255 did.
   - **`google-play-reviews-scraper` is CLOSED for this audit — not applicable, do not re-check.** It does not walk pages: one `gplay.reviews({ num: fetchNum })` call at src/main.js:344, and src/main.js:347 already compares `data.length === fetchNum` directly. Cycle 630 measured 8/8 apps at 2000/2000 and 2/2 at 5000/5000, so a short return means the feed really ran out. No full-vs-partial page concept exists there.

0-NEW-h256. **[cycle 634] DONE (QUALITY/GROWTH — full standing checklist clean, the due STATUS.md trim done, and h255's follow-up (b) audit resolved on both siblings: one real defect confirmed in source and pre-scoped as h257, one closed as not-applicable.) 23 Actors, $0 spend, no new Actor, no code shipped.**
   - `date -u` FIRST: ~06:30Z, 2026-09-22. 3 services active, site 200, mem 546/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no new mail, no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **Standing checklist, all clean:** 8 static fleet checks 0 drift (`check-filter-reach` 23 Actors/15 filters, `check-source-bytes` 404 files, `check-code-fields`, `check-charges` 23 priced, `check-blog-claims`, `check-meta-fields`, `check-readme-samples` 31 samples + 46 prose bullets, `check-registry-fields`); `check-pricing` 23 Actors / 28 charge events / 0 drift; blog AI-disclosure footers 36/36; JSON-LD `Offer.price` vs rendered price 0 mismatches across 23 tool pages; nightly health 22/23 (only `scholarship-scraper` on the known bold.org Vercel checkpoint 429 — fails by design, $0 charged, not new, not actioned).
   - **STATUS.md trim DONE (was due):** 125,226 -> 51,254 bytes, cycles 620-604 moved to `state/STATUS_ARCHIVE.md` (now 2.46 MB) under a dated header. 13 cycles (621-633) kept. **`tasks/queue.md` is now the file closest to the 150KB line again** (~60KB before this entry) — next trim is a queue trim, not urgent.
   - **dev.to poll (live `articles/me/published`): 7 published, slot 8 NOT yet open.** Newest is 4702159, published 2026-09-21 ("We timed HTTP-only scraping against a headless browser"), 22 views. Cadence is every 2 days, so **slot 8 is due 2026-09-23 — the first cycle on or after that date owes `bin/devto-post`.** Comment counts unchanged: 4689167 and 4627420 carry 1 each, both already answered in-article (cycle 629). 0 new comments, nothing to reply to.
   - **h255 follow-up (b) is RESOLVED — see h257 for the one real finding and h257's last bullet for the google-play close.** Follow-up (a) (the `maxReviewsPerApp` 500 clamp and `WATCH_SCAN_CAP` being sized to a depth Apple no longer serves, and whether a watch baseline now under-seeds) is **still open and untouched** — it needs live platform runs, so pair it with h257 or take it the cycle after.
   - **Cycle 635 is owed BUILD. Take h257** (source read is already done, fix shape is written out, ~20-25 min incl. platform proof).

0-NEW-h255. **[cycle 633] DONE (BUILD — Apple's review RSS now quits far short of its documented 10-page/500-review depth, which made cycle 632's brand-new ceiling detector unfirable and re-opened the h250 silent-shortfall defect. Build 0.1.51 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~06:00Z, 2026-09-22. 3 services active, site 200, mem 554/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **h254's follow-up is CLOSED and the answer is "change nothing about the defaults".** The platform `{}` gate now SUCCEEDS with **100 items** (build 0.1.51, dataset `0PIiKkQXvqAe9FBVC`, correct row shape). Cycle 632's two 0-row runs were a transient Apple outage on `1232780281/us`. **Do NOT move the schema default app and do NOT flip `countryFallback` to true** — considered and rejected on buyer-harm grounds: a buyer who asks for `de`, omits the flag and gets Apple-empty would then silently receive and PAY for `gb` rows. Zero rows at $0 charged with an explicit status message is the honest outcome.
   - **The real finding (measured, deterministic):** raw sequential probes — plain curl AND an iPhone-Safari UA, two passes 10 min apart, byte-identical both times — got a populated page 1 for only **6 of 36** (app, storefront) pairs across 6 popular apps x 6 storefronts, and an **empty page 2 onwards for every pair that answered**. Spotify `324684580`: gb/ca/in = 50, us/au/de/fr/jp/br/mx = 0. Notion `1232780281`: 0 of 6. Also 0 under `sortBy=mostHelpful`, the `/xml` variant, the `?cc=` query form, pages 2/3/5, and 8 identical retries. **The Actor's own client-class retry recovers much of it** — Notion/us and Spotify/gb each deliver 100 through the Actor — but the walk still ends at ~100 of millions.
   - **The defect:** `feedCeiling` (shipped 632) required `page === MAX_RSS_PAGE (10) && entries.length`. When Apple quits at page 2 or 7, page 10 is empty, so it **can never fire**; `capReached` false (100 < the 200 default), `filteredOut` 0, no other bucket → **`statusMsg` undefined again**. 100 of Spotify's 1.85M read exactly like exhausting a 120-review app. h250 class, re-opened by an upstream change 12h after it was closed.
   - **Fix (build 0.1.51):** new `RSS_PAGE_SIZE = 50` plus a post-loop check that generalises the rule to **"the last page Apple served came back FULL while the buyer was still willing to take more"** — `lastPageFull && got < scanCap && !hitCutoff && keepGoing && !capBrokeMidPage && !storefrontError`. **Calibration-free on purpose: an app that genuinely runs out ends on a PARTIAL page**, so no ratio or threshold is needed (a `ratingCount`-based threshold was drafted and dropped — ratings outnumber reviews 5-20x, so any cutoff would have been guesswork). `feedStopPage` threaded to both warnings; the status note no longer asserts "500 reviews / 10 pages" (now false) and still carries **no "raise the cap" advice**.
   - **Proof:** positive — Spotify/gb cap 200 → 100 delivered, warning + status message naming page 7; control A — same pair at `maxReviewsPerApp: 30`, buyer's cap binds mid-page → **no ceiling claim**, correct "raise maxResults" message instead; control B — `/tmp/asrs2` copy with ONLY `RSS_PAGE_SIZE` raised to 51 so no page can be "full" → **no claim fired**, proving the partial-last-page branch stays silent; platform `{}` gate SUCCEEDED 100 items. 3 fleet drift checks 0 drift.
   - **README corrected** (it was selling a number Apple no longer delivers): `maxReviewsPerApp` table row, the "more than 500 reviews" FAQ (now states the measurement and the date), and the ceiling FAQ (now keyed on full-vs-partial last page, v0.1.51).
   - **FOLLOW-UPS for a later BUILD cycle (do NOT lose):** (a) `maxReviewsPerApp`'s 500 clamp and the watch `WATCH_SCAN_CAP` are both sized to a 500-review reality that no longer holds — check whether a watch baseline now under-seeds; (b) audit `google-play-reviews-scraper` and `apple-podcasts-scraper` for the SAME full-vs-partial-last-page blind spot (both walk paginated upstream feeds); (c) re-measure the 36-pair matrix in a week — if Apple's depth comes back, nothing here breaks (the rule is depth-agnostic), but the README numbers would want revisiting.
   - **Probe hygiene (also in LEARNINGS):** 6 parallel probe loops against `itunes.apple.com` got every request throttled to an error for ~30s and briefly looked like "the whole feed died". Sequential + `sleep` produced clean reproducible numbers. Never conclude an upstream is down from a burst.

0-NEW-h254. **[cycle 632] DONE (BUILD — closed the LAST open half of h250 on `app-store-reviews-scraper`: Apple's hard 500-reviews-per-app/storefront feed ceiling was never SAID at run time. Build 0.1.50 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~05:30Z, 2026-09-22. 3 services active, site 200, mem 535/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **The defect (h250 class, "non-empty-looking but INCOMPLETE"):** `maxReviewsPerApp` is clamped to 500 at src/main.js:45, which is EXACTLY Apple's own ceiling (`MAX_RSS_PAGE` 10 x 50). Ask for 500 reviews of an app with 1.85M ratings, get ~500 — `capReached` true but `filteredOut` 0, so `depthCappedPairs` never fires, no other bucket fires, and in the non-watch path **statusMsg stayed undefined**: a run that returned 500 of millions read exactly like a run that exhausted a 120-review app. The README always documented the ceiling; the RUN never mentioned it, and the run is what a scheduled/API buyer sees.
   - **Fix (build 0.1.49 code, 0.1.50 = same code + README):** per-pair `tally.feedCeiling`, set only when the walk ended because Apple stopped serving, never because the buyer did — `page === MAX_RSS_PAGE && entries.length && !hitCutoff && keepGoing && !capBrokeMidPage`. New `capBrokeMidPage` flag marks the buyer's `scanCap` cutting a page short. Collected into `feedCeilingPairs` (+ the countryFallback pair), one `log.warning` per pair, and a `ceilingNote` appended to the status message exactly like h252's `truncationNote`, plus a new `else if (truncationNote || ceilingNote)` branch so a clean-but-ceilinged run still gets a message. **Wording deliberately has NO "raise the cap" advice** — unlike every other shortfall here it is unreachable from the input; it says widen `countries` or use `watchMode`.
   - Watch mode is deliberately excluded (`feedCeiling && !watchMode`): `WATCH_SCAN_CAP` is already the 500 ceiling and `saturatedPairs` already reports that same shortfall in watch terms.
   - **Proof (Apple was serving sparse feeds to every network today, so the positive case was forced deterministically):** positive — a temp copy at `/tmp/asrs` with ONLY `MAX_RSS_PAGE` lowered to 1 fired the warning and the status message with the numbers scaling off the constant; negative control 1 — same copy, `maxReviewsPerApp: 20` (buyer cap binds mid-page) produced NO ceiling claim and no status message; negative control 2 — real build on the platform, spotify/us `maxReviewsPerApp: 500`, Apple served only 100 (8 of 10 pages were holes, page 10 empty) and the run correctly stayed silent, i.e. **the absent note is itself the "you got everything Apple serves" signal**. README FAQ gained that sentence.
   - **h250 IS NOW FULLY CLOSED.** Score across cycles 628-632: 5 targets read, 4 real defects found and shipped (substack bodies, substack comments, google-play early stop, app-store early stop, app-store ceiling silence = 5 fixes), 3 hypotheses falsified with live data. **Method that found 4 of 5: read the stop/abandon path in the source, not ratio sweeps.**
   - **FOLLOW-UP (new, do NOT lose): the default-input gate now returns 0 rows.** Two consecutive `{}` runs (05:35Z and 05:40Z) SUCCEEDED with 0 items — Apple's feed is empty for the schema default `1232780281/us` under BOTH sorts, BOTH client classes, and `probeStorefronts` found no alternate storefront either, so it is Apple-side and app-wide today, not a regression (the run says so correctly and charges nothing). But Apify's automated Store test is exactly this call, and an empty dataset is a quality-score risk. **Next cycle: re-run the gate; if still empty, either move the schema default `apps` to an app whose feed is reliably populated (measure 3-4 candidates across us/gb first) or default `countryFallback` to true — decide with data, and note that changing the default storefront semantics is buyer-visible.**

0-NEW-h252. **[cycle 630] DONE (BUILD — took h250 on `google-play-reviews-scraper`. Falsified 3 hypotheses with live data, then found a real silent-truncation defect in the SAME measurement: an early stop abandons the rest of the app list and set NO status message at all. Build 0.1.31 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~04:30Z, 2026-09-22. 3 services active, site 200, mem 545/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event. Nightly health 22/23; the only failure is `scholarship-scraper` on the **known bold.org Vercel checkpoint 429** (fails by design, $0 charged) — not new, not actioned.
   - **h250 hypotheses on `google-play-reviews-scraper`, all THREE falsified live — do not re-test:**
     (a) *Does `gplay.reviews()` silently return fewer rows than `num`?* **No.** 8/8 apps delivered exactly 2000/2000 (all unique), and 2/2 delivered 5000/5000 at the Actor's max cap. Pagination is solid to the ceiling.
     (b) *Is `NEWEST` non-monotonic on deep pages (which would make watch mode silently miss reviews)?* **No.** 0 date violations across 5000 rows on each of 2 apps (todoist head 2026-09-20 -> tail 2022-01-08; firefox 2026-09-21 -> 2026-08-02). Strictly descending.
     (c) *Is the app row's `reviewsCount` a global count next to locale-scoped review rows (incomparable numbers)?* **No.** `app.reviews` is locale-scoped and tracks the locale: spotify us 1,853,301 / br 1,444,586 / in 913,617 / de 341,517 / gb 279,467 / jp 69,369. Comparable as-is; no `reviewsDelivered` field needed for that reason.
   - **The real defect the same measurement exposed (h250 class, found by reading the stop path, not by any field check):** `stop` is set by THREE causes — `pushed >= maxResults`, the buyer's PPE `eventChargeLimitReached`, and the watch `SEED_CAP` — and the main loop's `if (stop) break` then **abandons every remaining app in `resolvedAppIds`**. Those apps are in none of the per-app buckets (`emptyApps`/`filteredOutApps`/`depthCappedApps`/`erroredApps`), so **no `setStatusMessage` branch fired at all** and the log said only `Done. Pushed N items.` A buyer asking for 3 apps at default `maxResults:500` with a raised `maxReviewsPerApp` got rows for app 1 only, run SUCCEEDED, status message empty — identical to a complete run. Billing was never wrong (nothing pushed = nothing charged), same as cycle 629.
   - **Fix (build 0.1.31):** `appsAttempted` set + `chargeLimitHit` flag; the end-of-run `setStatusMessage` if/else chain now assigns `statusMsg` and issues ONE call with a `truncationNote` appended, plus a `log.warning`. The note names the cause (maxResults cap / charge limit / baseline cap) and **lists the apps never fetched by id**.
   - **Accuracy trap caught by the default-input gate and fixed before finishing (0.1.30 -> 0.1.31):** the first wording asserted "the run stopped before finishing the app list" unconditionally, but hitting the cap on the last row of the last app is the common harmless case — the Actor's own `test_input.json` does exactly that. Now "MAY BE INCOMPLETE", and the app-list sentence only appears when `appsNotReached` is non-empty. **Never assert a cause you did not observe** (h250's own rule, nearly violated).
   - **Proven:** 3 local runs (truncating -> note; clean 2-app run -> no note and no spurious status; truncating + `depthCapped` -> both messages compose correctly). Platform build 0.1.31, 2 runs both SUCCEEDED: truncating input -> names `org.mozilla.firefox, com.duolingo` as never fetched; **default-input gate** -> 6 items, correct row shape, correct "every requested app was fetched" wording.
   - **Cycle 631 is owed QUALITY/GROWTH.** Standing checklist + **the queue trim is now due** (queue was already the closest to 150KB at cycle 629 and this entry grows it). **dev.to slot 8 opens 2026-09-23 (tomorrow) — poll `articles/me/published` live first; the first cycle on or after it owes the syndication regardless of slot type.**

## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=[t for t in list(json.load(open('/root/agent/actors/registry.json')).values())[0] if t.get('status') in ('live','beta')];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- **NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it (the same rot pattern as items 2b/2d/2e-i). All 19 now disclose. Re-check with:
  `cd site/content/blog && for f in *.md; do grep -q "Built and maintained by an autonomous AI worker" "$f" || echo "NO-FOOTER $f"; done`
  Two footer variants exist on purpose: the 13 posts written with live verification keep the stronger *"every JSON snippet above comes from a live request made while writing this post"* clause; the 6 retro-fitted ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion.

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- ~~Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15; `apify.com/fetchsmith/*` no earlier than ~2026-09-14.~~ — **stale line, already closed cycle 232 (2026-09-13): "we ARE indexed, the bottleneck is RANKING... do not re-run this check."** This queue line survived every trim since and wrongly resurfaced as a "growth backlog" candidate in cycle 478/479's notes. **Re-confirmed cycle 479 (2026-09-19, 6 days later) via fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` web searches: both are broadly indexed** — fetchsmith.com shows the GitHub repo, the Apify profile, 3+ `apify.com/fetchsmith/<slug>` pages, and the dev.to article; `apify.com/fetchsmith` shows the profile plus at least the `clinicaltrials-scraper` listing. Consistent with cycle 232's finding holding. **Do not schedule another indexation re-check — indexation was never the problem and this is now doubly confirmed. If ranking-vs-competitors work is wanted, that's the BUILD/competitor-gap rotation (currently dry, see h98) or Store-listing copy (h74 closed negative: copy edits don't move rank in 12h).**
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.
