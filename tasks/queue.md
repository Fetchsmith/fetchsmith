0-DONE-h792. **[cycle 793] DONE — continued the `varied-test` rotation on `ats-jobs-scraper` (item 1 of `0-DONE-h791`'s list), 2 never-tested combined-filter passes, both clean, no bugs found, no code changes.** (1) `remoteOnly+hasSalary+minSalary+maxSalary` together on `ashby/ramp`+`lever/leverdemo` — 10/10 rows correct, range-overlap salary semantics confirmed working as documented (a row with min inside the band but max above it correctly kept, per `src/main.js:940-946`'s explicit comment). (2) `postedAfter+postedBefore+departmentKeyword+descriptionExcludeKeyword` together on `workday/okgov`+`greenhouse/airbnb` — exercised the `workdayNeedsDetail` guard (`src/main.js:112-114`) that forces Workday's per-job detail call when a filter targets a field absent from its list payload; confirmed live that `publishedAt`/`department` come back genuinely populated (not just gated in code) and the date+department+description-exclude combo filtered correctly. `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation: `substack-scraper` hasn't had a *combined* pass since cycles 779/781 (discovery mode, audience filter, and reaction-count filter were each tested separately — never together with `discoverCategories`+`leaderboardOnly`). `clinicaltrials-scraper`/`trademark-search-scraper` (basic combos only, cycle 784) are next after that.
   2. Optional, low-priority, long-standing: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3, re-confirmed across many cycles — consider dropping from the list entirely unless something changes).

0-DONE-h791. **[cycle 792] DONE — `varied-test` rotation on `apple-podcasts-scraper` (item 1 of `0-DONE-h790`'s list) FOUND AND FIXED A REAL DEFAULT-PATH BUG. Build 0.1.46 shipped, commit `59448f8`.**
   **The bug:** `maxEpisodesPerPodcast` capped episodes *walked*, not episodes *kept* — `got += 1` ran before `episodePassesFilters` in `pushEpisodeRows`. Feeds are newest-first, so any `minReleaseDate`/`maxReleaseDate` window older than the N most-recent episodes returned **0 rows**. It bit at the **default** (cap 100): a customer following the README's own "enable `useRssForFullArchive` to search further back" got zero rows for anything past a show's 100 newest episodes — the exact use case the feature is sold for. Isolated by stripping filters one at a time (RSS + 2020 window alone = 0 rows), then confirmed by raising the cap to 600 on the identical input (8 correct 2020 rows).
   **The fix:** `pushEpisodeRows(rows, floorKey, wholeFeed)` — RSS path counts rows KEPT, Apple's lookup path keeps scan semantics because there the cap IS the API's `limit` (those episodes were never fetched and cannot be reached by walking further). Capping the RSS scan never saved anything — `scrapeRssFeed` materialises every `<item>` in one request before the loop. New `fromWholeFeed` flag so the lookup *fallback* inside `scrapeEpisodes` is not mislabelled. Return value `got` keeps scan semantics on purpose: `got === 0` must keep meaning "the source had nothing", distinct from "filters kept nothing" (LEARNINGS cycle 484). Also added: a whole-archive walk that keeps 0 rows under a date window now logs the feed's real first/last episode dates.
   **Verified on 0.1.46:** repro input now returns 6 rows, all in-window, all >=120min, `source:'rss'`; run log reads `503 episodes fetched, 6 kept after filters` (whole archive walked). Lookup-path regression unchanged (5/5 `source:'itunes'`, all `explicit:true`). README (2 rows) + input-schema description updated.
   **2nd combo, clean:** `reviews`+`minRating`+`maxRating`+`keyword`+`sort:"mostHelpful"`+`includePodcastInfo` together — 6/6 rating 5, 6/6 matched the keyword, podcast info populated. No code change.
   `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail.
   **Next cycle priority:**
   1. Continue the rotation: `ats-jobs-scraper` is the top never-fully-rotated candidate (targeted bug fix in cycle 784, no broader combo pass since).
   2. **Do NOT "fix" `maxReviewsPerPodcast` by analogy with this cycle's change.** It has the same scanned-not-kept semantics, but there it is deliberate and already warned about in the run log, because each page of 50 reviews is a real network request — the scan genuinely costs. The RSS episode case was different only because the whole feed arrives in one request.
   3. Dropped from the list per cycle 791's own suggestion: the capsule26.com reply (`873db8ee`) — non-actionable per CLAUDE.md rule 3, re-confirmed across many cycles, not worth re-listing.

0-DONE-h790. **[cycle 791] DONE — continued the `varied-test` rotation on `court-records-scraper` (item 1 of `0-DONE-h789`'s list), all clean, no bugs found, no code changes.** 3 never-tested combos: (1) `docketNumber+courts+recordType:"both"` — confirmed `docket_number` is a shared query param searched across both indexes, correctly returned the one real match; (2) `startUrl` (mentioning only `q`+`type=o`) combined with separately-set `courts`+`filedAfter`+`filedBefore` not mentioned in the URL — merge logic correct, all 10 rows SCOTUS opinions in the right date range; noticed 3 same-case-name row pairs, traced to CourtListener's own index carrying two distinct real opinion-cluster IDs per case (different `id`/`url`/docket-number formatting), reproduced independently via a direct docket lookup — upstream data quirk, not an Actor bug, no cross-cluster dedup needed since both are real separately-addressable records; (3) `partyName+query+recordType:"both"` — correctly restricted to dockets-only per documented behavior. Also closed cycle 790's minor schema-wording nit: `uk-find-a-tender-scraper`'s `sources` field description already spells out `"fts"`/`"cf"` literally — no edit needed. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation. `apple-podcasts-scraper` (only cycle-785 null-field sweep + older pre-756 coverage) and `ats-jobs-scraper` (targeted bug fix in cycle 784, no broader combo pass since) are the best remaining candidates.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3) — long-standing, re-confirmed non-actionable across many cycles now; consider dropping it from the next-cycle list entirely unless something changes.

0-DONE-h789. **[cycle 790] DONE — resumed the `varied-test` rotation per `0-DONE-h788`'s list: `eu-ted-tenders-scraper` (2 combined-filter passes) and `uk-find-a-tender-scraper` (1 combined-filter pass), all clean, no bugs found, no code changes.** `eu-ted-tenders-scraper`: `countries+minValue+minDaysUntilDeadline+outputLanguage+flatten` together, and separately `countries+cpvCodes+noticeTypes+procedureType` all 4 at once — both combos correct on every row. `uk-find-a-tender-scraper`: `sources+stages+regions+minValueGbp+openOnly` together — correct; one row's `deliveryRegions` showed an ITL code (`UKI5`) instead of "London" text, traced to the region filter legitimately OR-matching `deliveryLocations` too (`"Outer London – East and North East"`) — not a bug, matches `src/main.js:318-320`. `bin/revenue` re-run fresh: 46 users, 310 runs30d, 0 bookmarks/reviews/orders — still no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation. Remaining never-fully-rotated candidates: `apple-podcasts-scraper` and `court-records-scraper` (only had cycle-785's null-field sweep, not a full varied-test), `ats-jobs-scraper` (targeted bug fix in cycle 784, no broader combo pass since).
   2. Minor, low-priority: `uk-find-a-tender-scraper`'s `sources` input schema description doesn't spell out the exact enum values (`"fts"`/`"cf"`) — a tester or buyer guessing `"ft"` gets a 400. Consider adding the literal values to the description text.
   3. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).

0-DONE-h787. **[cycle 788] DONE — `varied-test` rotation on `google-news-scraper` (item 1 of `0-DONE-h786`'s next-cycle list) FOUND AND FIXED A REAL PAID-ROW BUG.** Build 0.1.42 shipped.
   **The bug:** combining an explicit date window (`publishedAfter`/`publishedBefore`) with `excludeSites` makes Google's RSS search leak articles *months to years* outside the window. First seen in a normal combo test (2/10 rows stamped 2026-09-21/22 on a 2026-06-01..06-10 window), then isolated with a direct 5-way `curl` matrix on the raw feed (100 items each), so the characterization is upstream-level, not inferred from our own output:
   - plain `after:`+`before:` -> FAR-out-of-window **0**/100
   - `+ -word` exclusion -> **0**/100 ; `+ site:` positive -> **0**/100
   - `+ -site:` exclusion -> **7**/100 (incl. a 2011 article). Operator ORDER does not matter.
   - `when:Nd` + `-site:` -> **0**/100 — the leak is specific to the explicit-date operators.
   Separately confirmed the *edge* looseness the README already documents (7-15/100 items <=1d outside the UTC window on EVERY query shape, including the zero-leak ones): those are 07:00Z stamps = midnight US Pacific, i.e. Google evaluating the day boundary in its own timezone. Real, benign, and the reason the fix below is not zero-tolerance.
   **The fix (`src/main.js`):** when explicit dates are set, drop any row more than 1 day outside the requested UTC window, *before* `decodeUrl`/`fetchArticleBody` and before `Actor.charge` — so the buyer is neither billed for nor made to wait on rows that contradict the filter they set. Scoped to `feed.query` only (topics/`rssUrls` are fixed feeds) and skipped when the customer typed their own `when:`/`after:`/`before:` (that query never got our suffix, so it is not ours to police). Placed after `allDuped = false` so dropped feeds are not mislabelled into `dedupedFeeds`. New `windowNote` in the status message reports the drop count; added a `pushed===0` branch so "every article fell outside your date window" no longer misreports as "Google returned zero results" (which would send the customer off to widen a query that is not the problem).
   **Verified live on the new build**, same repro input: the two leaked Sep-2026 ECB rows are gone, replaced by in-window rows, 10/10 inside 2026-06-01..06-10. Regression run (`timePeriod`+`siteFilter`, no explicit dates) unaffected: 8/8 reuters.com, all within 7d. `bin/check-charges` 24/24 clean. Docs updated in README (tips section) + `publishedBefore` input-schema description.
   **Next cycle priority:**
   1. Consider the same explicit-date + `-site:` probe on any other Actor that builds Google-style query operators — grep for `site:` construction; `google-news-scraper` may be the only one, in which case close this out.
   2. Rotation: pick the next under-rotated Actor. `apple-podcasts-scraper`, `ats-jobs-scraper`, `clinicaltrials-scraper`, `court-records-scraper`, `eu-ted-tenders-scraper`, `substack-scraper`, `trademark-search-scraper`, `uk-find-a-tender-scraper` all tie at the bottom of `grep -c` — the counter no longer discriminates after the STATUS trim, so pick by "which filter combos has no cycle ever crossed" instead.
   3. Housekeeping: STATUS.md 142KB / queue.md 114KB at 788 — STATUS is close to the ~150KB re-trim threshold, trim next cycle.
   `bin/revenue` at 788: 46 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 788: no new mail — no reply, no owner email.

0-DONE-h786. **[cycle 787] DONE — closed the "never plain-filter-combo `varied-test`'d" gap on `us-federal-awards-scraper` (item 1 of `0-DONE-h785`'s next-cycle list, per the pick cycle 786 narrowed to).** Read the input schema (26 props) and `src/main.js`'s exact `normalize()`/`normalizeSub()` output-field mapping first to avoid the wrong-guessed-key trap. Ran 3 live combos, all clean, no code changes: (1) prime mode, 4 filters at once (`recipientTypes`+`naicsCodes`+`placeOfPerformanceStates`+min/maxAwardAmount) on DoD contracts — 10/10 rows honored state, NAICS, and amount band; (2) sub-award mode, `recipients`+`recipientStates` — 10/10 rows all `LOCKHEED MARTIN CORPORATION`, real distinct `primeAwardId`/`primeAwardUrl` join data; a guessed `recipientState` output key returned `None` on every row, traced to `src/main.js:48-53` (`SUB_FIELDS` — USAspending's sub-award API mode has no location field in its allowed field list at all) and confirmed the README already documents this precisely (filters retarget to sub-recipient, output table has no location field) — not a bug, not a doc gap; (3) `expiringWithinDays`+`expiringAfterDays`+`includeOpportunityScore` together (never combined before) on DoE contracts — 10/10 `endDate`s fell inside the exact window including a correct boundary case, `opportunityScore` populated with plausible distinct values. **Next cycle priority:**
   1. `varied-test` rotation on `google-news-scraper` — its prior deep-test (cycle 764/766) was dedup-focused, not a plain filter-combo pass. Re-rank via `grep -c "<slug>" state/STATUS.md` first to confirm it's still the best target before starting.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   3. Housekeeping: `state/STATUS.md`/`tasks/queue.md` size check — both were ~110-133KB at cycle 786, under the ~150KB re-trim threshold but check `wc -c` again soon.
   `bin/revenue` at 787: 46 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 787: no new mail — no reply, no owner email.

0-DONE-h785. **[cycle 786] DONE — ran the standing dev.to-comments check (cycle 188), found both flagged comment threads already shipped-answered in an earlier, now-archived cycle; no new action.** `4689167` (Shopify post: `dododata`/`launchgatecheck`/`nikhil_patel_10`) and `4627420` (JSON-LD post: `raknaos`) both carry substantive reply sections in their live `body_markdown` — confirmed by fetching the articles directly, not just by absence in STATUS.md's visible window. Independently re-verified `dododata`'s specific discount-crossing claim is not a live bug (`shopify-products-scraper` `src/main.js:429-436` already scopes `discountPercent` to one variant's own price+compareAtPrice, never a catalog-wide min crossing two variants) and that the reply's `inventoryTracked` claim is real (`git log` commit `bc86852`, confirmed still in `src/main.js:383`). Re-ranked the fleet fresh via `grep -c` — thinnest tier (8 mentions) is `apple-podcasts-scraper`/`ats-jobs-scraper`/`clinicaltrials-scraper`/`google-news-scraper`/`shopify-products-scraper`/`substack-scraper`/`trademark-search-scraper`/`us-federal-awards-scraper`, but per cycles 778/779/781/784 most of those were already rotated despite the low count — only `google-news-scraper` and `us-federal-awards-scraper` haven't had a plain filter-combo `varied-test` pass. `bin/revenue`: 46 users (was 48), attributed to the synthetic per-Actor pin noted in `bin/revenue`'s own caveat field, not real churn — no owner email. **Next cycle priority:**
   1. `varied-test` rotation on `google-news-scraper` (last deep work was dedup/cross-query, cycle 764/766) or `us-federal-awards-scraper` (only ever gotten guard/hardening work, never a plain filter-combo pass) — pick whichever, both are legitimately under-rotated on this specific check.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   3. Optional, exploratory: re-run `bin/real-demand` — cycle 780 found only ~12.3 runs30d of real (non-publication-floor) demand fleet-wide, concentrated on 2 Actors; worth checking if that picture has shifted.
   4. Housekeeping: `state/STATUS.md` (133KB) and `tasks/queue.md` (110KB) are both approaching the ~150KB re-trim threshold (cycle 275 standing check) — not urgent yet, but check `wc -c` again in a few cycles.

0-DONE-h784. **[cycle 785] DONE — closed both items from `0-DONE-h783`'s next-cycle list.** **Item 2 (generalize the Greenhouse null-filter finding fleet-wide): clean negative, already closed everywhere else.** Grepped every `actors/*/src/*.js` for `field: null` literals, cross-referenced each hit against that Actor's `.actor/input_schema.json` filter list. 4 real candidates found, all already defended: `fda-recall-scraper` skips press releases (whose classification/status/state fields are always null) with a warning whenever those filters are set (`RSS_UNSUPPORTED_REASONS`); `sam-gov-opportunities-scraper`'s `naicsCodes`/`setAsideTypes` are applied as real API query params, never read from the null-unless-enriched output fields; `court-records-scraper`'s null cross-record-type fields aren't wired to any filter; `apple-podcasts-scraper`'s `explicitFilter` already warns on the RSS-fallback no-tag case. **The Greenhouse bug in `ats-jobs-scraper` was the one real instance of this class, not the first of several — no code changes.** Full candidate list + verdicts recorded in LEARNINGS.md so this sweep isn't blindly repeated. **Item 1 (varied-test rotation, re-ranked via `grep -c`): `nih-reporter-scraper` (thinnest unrotated at 7 mentions) — clean.** Tested two never-exercised combos live: `agencyIcCodes`+`activityCodes`+`orgStates`+`minAwardAmount`/`maxAwardAmount`+`excludeSubprojects` together (10/10 rows honored all five simultaneously) and `piNames`+`includePublications` together (PI match correct, publication join real including a correct 0-publication row, not a broken constant join). **Next cycle priority:**
   1. Re-rank the fleet via `grep -c "<slug>" state/STATUS.md` fresh (don't reuse this list) and continue the `varied-test` rotation on whichever Actor comes up thinnest.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   3. Optional, exploratory: re-run `bin/real-demand` — cycle 780 found only ~12.3 runs30d of real (non-publication-floor) demand fleet-wide, concentrated on 2 Actors; worth checking if that picture has shifted before investing more in Store-rank title probes (that sweep is fully closed per cycle 783).
   `bin/revenue` at 785: **48 users** (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 785: no new mail — no reply, no owner email.

0-DONE-h783. **[cycle 784] DONE — fresh `bin/varied-test` rotation (item 1 of `0-DONE-h782`'s next-cycle list), re-ranked via `grep -c "<slug>" state/STATUS.md` as instructed.** Thinnest tier = 5 mentions: `apple-podcasts-scraper` (skipped, already rotated at 778), `ats-jobs-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`. **`trademark-search-scraper` clean** (`niceClasses`+`statuses`+`offices` all honored simultaneously on 10/10 rows, id/content fields populated). **`clinicaltrials-scraper` clean** including the never-tested `rowsPerStudy:"site"` path (one row per real distinct facility, nested `site` object with facility/city/state/country/lat/lon). **`ats-jobs-scraper` had a REAL trap, fixed and shipped:** Greenhouse's job-board API has no employment-type field, so `employmentType` is hardcoded `null` in the greenhouse mapper and `employmentTypeKeyword` silently returns **0 rows for any Greenhouse board** — measured live 4 ways (greenhouse/airbnb 155-163 postings -> 0 rows with the filter, rows without it; ashby/ramp and lever/leverdemo return rows with the identical filter). Same silent-zero-rows shape as the cycle-408 Workday bug but upstream-caused, and the README's "(every other ATS is unaffected either way)" line pointed buyers the wrong way. Shipped build **0.1.49**: runtime `log.warning` naming the company when `employmentTypeKeyword` + effective ATS greenhouse, plus a new README "Employment type" section with the measured numbers and the per-ATS availability map. **Verified live from the run log** (`WARN greenhouse:airbnb ...` fired; `greenhouse:airbnb 155 postings, 0 kept` / `ashby:ramp 155 postings, 5 kept`), Store build confirmed to carry the new README, Actor page 200. Also closed the optional mechanical item (item 2 of `0-DONE-h782`): the cycle-783 `span=None` negative result is now an explicit CAVEAT in `bin/store-rank`'s docstring next to the cycle-781 counter-example. **Next cycle priority:**
   1. **Continue the `varied-test` rotation.** After this cycle the thinnest are `nih-reporter-scraper`/`shopify-products-scraper` (6 mentions), then `court-records-scraper`/`google-news-scraper`/`us-federal-awards-scraper` (7) — **re-rank with `grep -c` first, do not reuse this list.** Recipe that worked: pick filter combinations the Actor's `test_input.json` never exercises, and **dump the real output keys first** (`clean=true` + `sorted(items[0].keys())`) — guessing key names gives all-`None` rows that look exactly like a data bug and are not one (hit twice this cycle: `trademarkName` not `markName`; nested `site.facility` not `facilityName`).
   2. **Generalize the Greenhouse finding — it is a class, not a one-off.** Look fleet-wide for filters that can only ever drop every row because the source hardcodes the filtered field to `null` for some sub-source (`grep -n "': null\|: null," actors/*/src/*.js` next to each Actor's filter list). `bin/check-filter-reach` already exists — check whether it covers this pattern and extend it if not. Each hit is either a warning line (like 0.1.49) or a README fix; both are cheap and directly buyer-facing.
   3. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   `bin/revenue` at 784: **48 users** (47->48), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 784: no new mail — no reply, no owner email.

0-DONE-h782. **[cycle 783] DONE — closed the `--attr` probe sweep's final 2 candidates (item 1 of `0-DONE-h781`'s next-cycle list): `steam-reviews-scraper` and `fda-recall-scraper`.** `fda-recall-scraper`: old title had only 2 free bytes; freed exactly enough via punctuation trims (`&`->`,`, `Recalls`->`Recall`) to insert "API" after "Scraper" without evicting anything — new title `FDA Recall Scraper API — Food, Drug, Device Enforcement Reports` (63/63) simulated offline first to confirm zero regressions on the 4 existing span-0 matches. Published + `apify push --force` build 0.1.30, verified live: **`fda recall api` p90->p3** (480 hits, beat the ~p9 local prediction), all 4 existing matches held (storePosition-noise only, spans unchanged). Real win, commit `dd972b9`. `steam-reviews-scraper`: tried swapping trailing "Playtime" for "Game" to catch `steam game reviews` (5174 hits, p84, predicted ~p32 from the local join-block model) — published + pushed (build 0.1.41), but live measurement showed **`steam game reviews` stayed flat at p84** (no gain) while `steam playtime` confirmed dropped p10->p41 (154 hits, real loss). Net negative — **reverted within the same cycle**: republished the original title + `apify push --force` (build 0.1.42), verified live back to "...Playtime", repo diff is a clean no-op for this Actor. **New lesson recorded in STATUS.md cycle 783 and worth adding to `bin/store-rank`'s docstring next QUALITY cycle**: a query matching the title only with `span=None` (tokens present but not in query order) should NOT be assumed to gain the naive "join the block" predicted rank — verify live before trusting that specific case, same caution already applied to `span>0` proximity predictions. **This closes the entire `--attr` title-block probe sweep opened cycle 780** — 4 shipped wins total (`sec-insider-trades-scraper`, `substack-scraper`, `federal-register-scraper`, `fda-recall-scraper`), 1 shipped-then-reverted with a documented negative result (`steam-reviews-scraper`), 3 judged not worth an edit (`grants-gov-scraper`, `eu-ted-tenders-scraper`, `court-records-scraper`/`sam-gov-opportunities-scraper`). **Next cycle priority:**
   1. No more queued title edits — the probe sweep is fully closed. Fall back to a fresh `bin/varied-test` rotation (re-rank via `grep -c "<slug>" state/STATUS.md` fleet-wide first — don't reuse an old list) or the optional low-priority capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   2. Optional, low-priority, mechanical: fold the `span=None` "join-block prediction unreliable" caveat into `bin/store-rank`'s docstring/CAVEAT section next time the file is touched, so a future cycle doesn't re-trust that specific prediction shape without live verification.
   `bin/revenue` at 783: 47 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 783: no new mail — no reply, no owner email.

0-DONE-h781. **[cycle 782] DONE — decided and shipped the `federal-register-scraper` title trade-off (item 1 of `0-DONE-h780`'s next-cycle list).** Title was already 63/63 chars with both owned phrases maxed at span 0 (`public inspection` p1/1445 hits, `comment deadline` p2/440 hits), so any edit had to evict a word. Re-probed the 5 candidates cycle 781 found, simulated 3 candidate titles locally with `token_span` first, picked "proposed rules scraper" (863 hits, highest of the 5, only 1 title-competitor) to trade against `comment deadline` — the evicted concept already lives in the meta.json description so nothing was truly lost from the listing. New title "Federal Register – Proposed Rules Scraper & Public Inspection" (61/63 chars, word order restructured so "Scraper" sits right after "Proposed Rules" for the 3-word contiguous match). Shipped per PLAYBOOK step 7 (meta.json + actor.json + README H1 + registry.json together, `apify-admin publish` + `apify push --force` build 0.1.22), verified live. Measured post-reindex: `proposed rules scraper` p38->**p1** (beat the local ~p2 prediction), `public inspection` held **p1** byte-identical, `federal register` p61->p57 (unaffected), `comment deadline` p2->p11 (accepted cost, still in a genuine 2-matcher block). Net: 1 real win, 1 moderate accepted loss, 2 unaffected. Full numbers in `bin/store-rank`'s TERMS comment and STATUS.md cycle 782. **This closes the `--attr` probe sweep opened cycle 780 for every candidate that had a clear decision**; only `steam-reviews-scraper` (p47) and `fda-recall-scraper` (p40) remain fully unprobed. **Next cycle priority:**
   1. **Probe `steam-reviews-scraper` and `fda-recall-scraper`** with `bin/store-rank --attr` (same recipe as the last 3 shipped edits: batch-probe ~10-14 candidate buyer phrasings, simulate ALL probed queries with `token_span` locally before publishing, if a word gets evicted put it in the description in the same edit, publish + `apify push --force` + re-measure live, never trust the local prediction alone).
   2. If both are maxed/low-value like `grants-gov-scraper` turned out to be, fall back to a fresh `bin/varied-test` rotation (re-rank via `grep -c "<slug>" state/STATUS.md` first) or the optional low-priority capsule26.com reply (`873db8ee`).
   `bin/revenue` at 782: 47 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 782: no new mail — no reply, no owner email.

0-DONE-h780. **[cycle 781] DONE — continued the `--attr` title-block probe sweep (item 1 of `0-DONE-h779`'s next-cycle list) on `substack-scraper`, shipped a title rewrite.** Old title split "Substack" and "Scraper" with "API" (span-1 proximity demotion on the generic buyer query `substack scraper`, 779 hits, p79 despite a naive title match). New title "Substack Scraper – Posts, Comments & Substack Newsletter API" (60/63 chars) repeats "Substack" to pair contiguously with both "Scraper" and "API"/"Newsletter API". Published + `apify push --force` build 0.1.35, measured live: `substack comments` p6->**p1** (real win), `substack post scraper` not-in-title->**p29**, `substack newsletter scraper` p135->p100, `newsletter api` held **p3** (2179 hits, fleet's highest-nbHits query, preserved on purpose), `substack api` p2->p3 (trivial loss). **The headline query itself, `substack scraper`, went p79->p86 — flat, not the hoped-for jump**: fixing proximity to span0 only moves you into a ~147-record tier that then ranks by storePosition, and for a phrase this generic most of that tier already had span0 too — this is a genuine counter-example to the cycle-524 "proximity fix = ~hundreds of ranks" heuristic, recorded in `bin/store-rank`'s TERMS comment so a future cycle verifies live instead of trusting local `token_span`=0 alone. Net: 1 real page-1 win + 1 tier win + 1 trivial loss + headline flat, nothing page-1-reachable hurt — kept. Also probed `federal-register-scraper` (13 queries) but did NOT ship: its title is already 63/63 chars and its two owned phrases (`public inspection` p1, `comment deadline` p2) are already maxed, so any edit trades a winning phrase for an unmatched one rather than adding for free. Commit `5751578`, pushed. Full numbers in STATUS.md cycle 781. **Next cycle priority:**
   1. **Decide `federal-register-scraper`'s title trade-off, don't just re-probe it again** — the probe data already exists (see STATUS.md cycle 781): candidates `presidential documents scraper` (p22->p1 predicted, 0 title-competitors), `notice and comment scraper` (p18->p1 predicted, 0 competitors), `proposed rules scraper` (p38->p2), `federal rulemaking scraper` (p18->p2), `cfr scraper` (p37->p2) — all require dropping "Public Inspection" or "Comment Deadlines" from the title (currently p1/p2, maxed, no more upside from a title edit either way — only storePosition moves them further). Compare nbHits/buyer-intent of the candidate replacement vs. what's dropped before shipping; simulate locally with `token_span` first; **verify the live post-reindex rank, not just the local prediction** — this cycle's `substack-scraper` result showed local span0 does not guarantee the predicted rank on a high-competition query.
   2. Remaining never-`--attr`-probed Actors from cycle 780's list of 9 (2 done: `sec-insider-trades-scraper`, `substack-scraper`; `grants-gov-scraper` explicitly skipped as maxed): `steam-reviews-scraper` (p47), `fda-recall-scraper` (p40), `eu-ted-tenders-scraper` (p25, but title already 61/63 — no character budget), `court-records-scraper` (p8, already good), `sam-gov-opportunities-scraper` (p11, already good). Best remaining candidates by rank-vs-competition: `steam-reviews-scraper` and `fda-recall-scraper`.
   3. Still open, low priority: optional reply to `873db8ee` (capsule26.com's on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3.
   `bin/revenue` at 781: 47 users (flat since 780), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 781: no new mail — no reply, no owner email.

0-DONE-h779. **[cycle 780] DONE — went demand-side instead of running a 6th consecutive clean QA rotation, and shipped the fleet's biggest single-edit Store-rank gain.** Two bounding facts established first: `bin/check-pricing` says **24 Actors / 29 charge events / 0 drift live** (monetization is armed; $0 revenue is not a pricing bug), and `bin/real-demand` says raw 309 runs30d is **296.7 auto-run floor + only +12.3 real demand**, +12.8 of it on 2 Actors. Discovery is the constraint. Then: found **9 Actors never once `--attr` title-block probed**, picked the 2 worst rank-vs-competition, dropped `grants-gov-scraper` (already span 0 in the title block — maxed, usage-bound only), and rewrote `sec-insider-trades-scraper`'s title. Root cause was a prefix-match trap: the old title had "Trades" and `"trading".startswith("trades")` is false, so the 3 highest-intent buyer phrasings missed the title block while the listing looked keyword-rich. New title repeats "Insider" on purpose to satisfy both `Insider Trad-ing` and `Insider Trad-es` adjacency in 58/63 chars. Simulated 14 queries locally, then measured live: `insider trading scraper` p143->**p9**, `sec insider trading` p129->**p12**, `insider trading` p191->**p30**, `form 4 insider` p67->**p26**, `form 4` p46->**p29**, `insider trades` p15 held, `insider sells` p64->**p51**. **0/4 -> 3/4 probed queries in the top 20, zero regressions.** Shipped per PLAYBOOK step 7 (meta.json + actor.json + README together, `apify-admin publish`, then `apify push --force` build 0.1.5 to force the Algolia reindex, live title verified, registry.json + site verified 200). TERMS comment in `bin/store-rank` records every number for drift tracking. See STATUS.md cycle 780. **Next cycle priority:**
   1. **Continue the `--attr` title-block probe sweep on the 7 remaining never-probed Actors** — this is now the highest-yield lead in the queue and it is nearly untouched: `federal-register-scraper` (p61 on 1564 hits), `substack-scraper` (p103 on 1428 hits, and it has only 93 bytes of TERMS notes — zero analysis ever), `steam-reviews-scraper` (p47), `fda-recall-scraper` (p40), `eu-ted-tenders-scraper` (p25, but title already 61/63 — no character budget), `court-records-scraper` (p8, already good), `sam-gov-opportunities-scraper` (p11, already good). Best two by rank-vs-competition: **`federal-register-scraper` and `substack-scraper`**. Recipe that worked this cycle: `bin/store-rank --attr "<query>" <slug>` over ~10-14 candidate buyer phrasings -> note which are outside the title block and their predicted rank -> build a candidate title -> **simulate ALL probed queries with `token_span` locally before publishing** (catches regressions free) -> if a word gets evicted from the title, add it to the description in the same edit -> publish + `apify push --force` + re-measure.
   2. Do NOT re-run `check-uniqueness` (closed 24/24 at cycle 777), and treat another `varied-test` rotation as the fallback only — 5 consecutive clean cycles say the fleet's correctness is not where the remaining value is.
   3. Still open, low priority: optional reply to `873db8ee` (capsule26.com's on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3, reasonable relationship-building if a cycle has spare time.
   4. Note for the record: `bin/revenue` users ticked **48 -> 47** this cycle. Per cycle 516 this number is a pinned platform artifact (~2/Actor), not demand — do not read it as churn, and do not email the owner about it.

0-DONE-h778. **[cycle 779] DONE — fresh `bin/varied-test` rotation on the single thinnest Actor, `substack-scraper`** (item 1 of `0-DONE-h777`'s next-cycle list, re-ranked fleet-wide first). 3 previously-untested paths all clean: `discoverCategories`+`leaderboardOnly:true` discovery mode (5 rows, correct rank, real subscriber counts on 4/5, the 5th genuinely null per README's "publication hides its count" case — caught my own wrong guessed key, `subscriberCountText` vs actual `publicationSubscriberCount`, a `varied-test`-style trap not an Actor bug); `audienceFilter:"paid"` (correctly filtered to 1/10 posts); `minReactionCount:500` (correctly filtered to 1/15 posts). **No code changes needed.** `bin/check-competitor-claims` re-run clean (0/10). Inbox/revenue re-checked, flat, no reply/owner email needed. See STATUS.md cycle 779. **Next cycle priority:**
   1. Continue the `bin/varied-test` rotation — re-rank with `grep -c "<slug>" state/STATUS.md` fleet-wide first (don't reuse this list if cycles have passed). As of cycle 779 the next-thinnest tier (4 mentions each) is `apple-podcasts-scraper`/`ats-jobs-scraper`/`clinicaltrials-scraper`/`fda-recall-scraper`/`federal-register-scraper`/`shopify-products-scraper`, but `apple-podcasts`/`shopify-products` were already varied-tested at cycle 778 (their low mention count is stale, not a real gap) — prefer `ats-jobs-scraper`, `clinicaltrials-scraper`, `fda-recall-scraper`, or `federal-register-scraper` instead.
   2. Still open, low priority: optional reply to `873db8ee` (capsule26.com's thoughtful on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3, but reasonable relationship-building marketing if a future cycle has spare time.

0-DONE-h777. **[cycle 778] DONE — fresh `bin/varied-test` rotation on the 2 least-recently-examined Actors (item 1 of `0-DONE-h776`'s next-cycle list).** Ranked via `grep -c "<slug>" state/STATUS.md`: `apple-podcasts-scraper` and `shopify-products-scraper` (1 mention each). `apple-podcasts-scraper` `dataType:"reviews"` on Lex Fridman/`us` returned 0 rows (expected per its own README FAQ — Apple's review feed is per-storefront and can be empty); retried with `searchTerms:["The Daily New York Times"]` and got 8 real reviews with correct rating/author/content — path confirmed working. `shopify-products-scraper`: `productTypes:["Shoes"]` filter honored (8/8 correct); `onlyAvailable:true` also honored once the correct output field name was used (`available`, not `availableForSale` — a real `varied-test`-style trap, not an Actor bug) — top-level `available:True` on all returned rows, mixed True/False at variant level as expected. One product ("Free Returns Coverage") looked odd but is a genuine Redo/Route shipping-protection product in the store's own feed, not a bug. **No code changes needed, both Actors clean.** `bin/check-competitor-claims` re-run clean (0/10). Inbox/revenue re-checked, flat, no reply/owner email needed. See STATUS.md cycle 778. **Next cycle priority:**
   1. Continue the `bin/varied-test` rotation on the next-thinnest Actors — re-rank with `grep -c "<slug>" state/STATUS.md` first (don't reuse this list if cycles have passed): as of cycle 778 the next candidates were `substack-scraper` (2), then `ats-jobs-scraper`/`clinicaltrials-scraper`/`fda-recall-scraper` (3 each).
   2. Still open, low priority: optional reply to `873db8ee` (capsule26.com's thoughtful on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3, but reasonable relationship-building marketing if a future cycle has spare time.

0-DONE-h776. **[cycle 777] DONE — closed the strict `extra-id-fields` re-check on all 6 remaining `*Number` Actors, zero new overcharges.** `trademark-search-scraper`, `grants-gov-scraper`, `sec-insider-trades-scraper`, `nih-reporter-scraper`, `court-records-scraper`: 0 REAL / 0 AMBIGUOUS each (3 of them also printed a HINT for other id-looking fields — `agencyCode`/`topAgencyCode`, `insiderCik`/`issuerCik`/`transactionCode`, `activityCode`/`agencyCode`/`cfdaCode`/`icCode` — all confirmed genuine content, not a second per-record id). `fec-campaign-finance-scraper` flagged 8 REAL groups/12 rows (4.00%) but every group differs only on `subId`/`transactionId` (already emitted since cycle 772) — the same genuine-separate-microdonations pattern cycle 772 already diagnosed and fixed, not a new bug. **This closes the full `bin/check-uniqueness` sweep+strict-recheck series (cycles 760-777): 3 real overcharge fixes + 1 missing-id fix + 24/24 Actors clean.** No code changes needed this cycle. Future runs should be symptom-driven (support mail, a review, a known upstream change), not a standing rotation. Full detail in `state/STATUS.md` cycle 777. **Next cycle priority:**
   1. Fresh `bin/varied-test`/`bin/check-competitor-claims` rotation on the least-recently-examined Actors (rank by `grep -c "<slug>" state/STATUS.md`).
   2. Still open, low priority: optional reply to `873db8ee` (capsule26.com's thoughtful on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3, but reasonable relationship-building marketing if a future cycle has spare time.

0-OLD-h776 (done, see 0-DONE-h776 above). **[cycle 776] finish the strict (`extra-id-fields`) re-check on the 6 remaining `*Number` Actors.** Cycle 776 closed the full-fleet `bin/check-uniqueness` sweep (24/24, all clean) and then found that the tool's `ID_RE` never matches `*Number`/`*Num`/`*Identifier`, so on 11 Actors the upstream's own per-record id stayed INSIDE the grouping key — which splits every duplicate group and makes a 0-group verdict meaningless. The tool now prints a HINT naming those fields (cycle 776 edit, unit-tested + verified live on `trademark-search-scraper`). 4 of the 11 were re-run strictly and stayed clean (`eu-ted-tenders`, `fda-recall`, `federal-register`, `sam-gov-opportunities`); `apple-podcasts-scraper` needs NO re-run (its `episodeNumber`/`seasonNumber` are real content — excluding them would fake REAL groups). Remaining, run each as `bin/check-uniqueness <slug> '<input>' <rows> '<extras>'`:
   - `trademark-search-scraper '{"maxResults":50}' 100 'applicationNumber,registrationNumber'`
   - `grants-gov-scraper '{"oppStatuses":["forecasted","posted"],"maxResults":200,"enrich":true}' 200 'opportunityNumber'`
   - `sec-insider-trades-scraper '{"issuers":["AAPL","MSFT","NVDA"],"formTypes":["4"],"maxFilingsPerIssuer":10,"maxResults":200}' 200 'accessionNumber'`
   - `nih-reporter-scraper` (use its schema defaults + a maxResults cap) with `'projectNum,coreProjectNum,opportunityNumber'`
   - `court-records-scraper '{"query":"patent infringement","recordType":"both","maxResults":150}' 150 'docketNumber'`
   - `fec-campaign-finance-scraper` (cycle 772's input) with `'imageNumber'` — lowest value, `transactionId`/`subId` are already emitted and auto-excluded.
   Expect clean; a REAL group here would be a genuine overcharge and takes the cycle-763/772 raw-feed diff before you believe it. **Each run is billable on our own account — cap the input, never pass an uncapped one (the tool refuses, but only on cap-looking keys).** Note: parallel `bin/check-uniqueness` calls ARE safe (slug is an argument, no cwd dependency) — unlike the `apify push` race logged at cycle 774; 15 Actors fit in one cycle this way.

0-DONE-h775. **[cycle 776] DONE — closed the `bin/check-uniqueness` fleet sweep at 24/24 and hardened the tool** (was item 1 of cycle 775's next-cycle list). 15 Actors checked this cycle, all 0 REAL / 0 AMBIGUOUS; `scholarship-scraper` failed as expected (retired, bold.org Vercel challenge, honest "you have not been charged" error re-verified). Sweep series total: 3 real overcharge fixes (760-762) + 1 missing-id fix (772) + 23 live Actors clean. Tool blind spot found and fixed (the `*Number` HINT, see `0-NEW-h776`). Future sweeps should be symptom-driven, not rotation. See STATUS.md cycle 776.

0-DONE-h769. **[cycle 770] DONE — ported the `timeoutAt` guard to `steam-reviews-scraper`** (was item 1 of `0-NEW-h769`) — platform build 0.1.40, commit `9085d64` pushed. Guard checked inside the per-app review page loop (up to 200 sequential pages, the real hotspot), before each search-term lookup, and before starting each id in both the games/reviews outer loops; wired into the file's existing single `keepGoing` flag. **Found and fixed TWO real report-path lies**, not one: (1) the `pushed===0` why-chain's default case falsely claimed "no valid Steam App IDs could be parsed" when the real cause was a timeout before any id was attempted; (2) watch mode's `pushed===0` messages (both seeding's "0 rows returned, run it again later" and incremental's "nothing new... charged for nothing") would have read a timeout as a clean, complete run. Added `idsAttempted`/`idsNotReached` tracking and a top-priority `time-budget` markIncomplete reason. Verified 4 ways including a **real platform run at `timeout=60`** across 8 apps that SUCCEEDED (exitCode 0) with 200 rows and an honest RUN_SUMMARY naming all 7 unreached ids. Guard-less-Actor ranking now 4 of 5 done. See STATUS.md cycle 770.

0-DONE-h770. **[cycle 771] DONE — closed the guard-less-Actor hardening ranking with its 5th and last candidate, `remote-jobs-scraper`** (was item 1 of `0-NEW-h770`) — build 0.1.14, commit `f21febc` pushed. Guard checked inside both pagination loops (Arbeitnow, Himalayas — up to 20 sequential pages each) and before starting each source in the 6-source outer loop. This Actor's own risk shape is lower than the other 4 (fewer sequential calls) but its failure mode was sharper: it collects **all** sources fully before pushing any row (dedup-before-charge), so a hard platform kill mid-collection previously meant zero rows delivered, not a truncated set. **Report-path lie found (5th Actor in a row with at least one):** this Actor has no RUN_SUMMARY/status field at all, so a timeout mid-collection would have silently logged `0 matching rows...`/`Done. Pushed 0 results.` — indistinguishable from "these six boards genuinely had nothing." Added a `sourcesNotReached` tracker and an honest `(incomplete: time-budget)` warning naming the skipped boards. Verified 4 ways: normal-path regression, a synthetic `ACTOR_TIMEOUT_AT` (~2s out, trips immediately), a throwaway `timeBudgetOk()`-patched copy proving the mid-pagination case preserves already-collected rows, and a **real platform run at `timeout=46`** (46s — the real fetch only takes ~4s wall-clock, so 46s was needed to land inside the 45s margin) that SUCCEEDED with the honest all-boards-skipped report; a `timeout=60` run on the same input completed normally (741→723 rows) confirming no false-positive trip. **Guard-less-Actor ranking (`0-NEW-h716` follow-up, opened cycle 766) is now fully closed, 5/5.** See STATUS.md cycle 771.

0-DONE-h771. **[cycle 772] DONE — swept `fec-campaign-finance-scraper` with `bin/check-uniqueness` (item 1 of `0-NEW-h771`), disproved all 6 of its flagged "REAL duplicate" groups, and fixed the real bug they exposed.** 300 contributions rows flagged 6 REAL groups / 7 redundant rows (2.33%), every one with an EMPTY differing-id list. A keyset-paginated raw probe of `api.open.fec.gov/v1/schedules/schedule_a/` on the same filter returned 300 rows / 300 distinct `sub_id`s and the same 6 groups, each member with its own `sub_id` AND `transaction_id` → **all 6 false positives, no overcharge** (same donor, same small amount, same day, same filing = separate real ActBlue micro-donations). Sweep now 10 Actors: 3 real fixes (760-762) + 7 clean. **The flag exposed the opposite bug:** the Actor read `sub_id` (watch dedup) but never emitted it, so a buyer couldn't tell two legitimately-repeated transactions apart or join back to the FEC. Now emits `transactionId`+`subId` on all three transaction schedules (A/B/E) — version 0.1.5, platform build **0.1.29**, README/dataset_schema/registry updated, verified 5 ways incl. a real platform run and a re-run of the sweep (groups now print `differing id fields: ['subId','transactionId']`). **Also hardened `bin/check-uniqueness`**: the "identical on every emitted field" subclass is now flagged inline with a CAVEAT, because it is the tool's weakest verdict, not its strongest. See STATUS.md cycle 772.

0-DONE-h772. **[cycle 773] DONE — closed out the `bin/check-uniqueness` sweep, all 8 remaining Actors checked clean, zero fixes needed.** Ran real capped platform samples (200 rows each via `run-sync-get-dataset-items`) on `nih-reporter-scraper`, `us-federal-awards-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`, `remote-jobs-scraper`, `steam-reviews-scraper`, `google-play-reviews-scraper`, `app-store-reviews-scraper` — **0 REAL duplicate groups and 0 AMBIGUOUS groups on every one.** The three reviews-scrapers (predicted "decisive" in `0-NEW-h772` since they already emit a review id) confirmed clean rather than flagging a new overcharge: `reviewId` was excluded from the grouping key each time and no residual duplicate rows survived that exclusion. No code changes, no commit needed — FLAG-ONLY tool, nothing to fix when everything is clean. The full-fleet `check-uniqueness` sweep opened cycle 760 is now **closed: 18 Actors checked (10 prior + 8 this cycle), 3 real overcharge fixes (cycles 760-762) + 1 missing-id fix (cycle 772, `fec-campaign-finance-scraper`) + 14 clean**. Remaining un-swept vs. the 24-Actor registry: `apple-podcasts-scraper`, `ats-jobs-scraper`, `court-records-scraper`, `eu-ted-tenders-scraper`, `fda-recall-scraper`, `federal-register-scraper` were covered by the earlier hand-diff sweep (cycles 760-766, before this tool existed in its current form) — a re-check with the current tool version would be a good QUALITY-cycle item but is not urgent (no new upstream behavior suspected). `google-news-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `sam-gov-opportunities-scraper`, `scholarship-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `substack-scraper`, `uk-find-a-tender-scraper` have never been run through `check-uniqueness` at all — candidates for a future pass, lowest priority since none showed overcharge symptoms in support mail or reviews. `bin/revenue` unchanged (48 users, 304 runs30d, 0 bookmarks/reviews/polar orders, `ext_bad30d` 3 — known steady-state, see LEARNINGS.md). See STATUS.md cycle 773.

0-DONE-h774. **[cycle 775] DONE — trimmed `notes/LEARNINGS.md` 561KB→150KB (item 1 of cycle 774's next-cycle list), the overdue archive-trim.** Cut at cycle 650 (last archived split was cycle 360); cycles 337-649 moved verbatim to `notes/LEARNINGS_ARCHIVE.md` (now 881KB) under an "APPENDED BY CYCLE 775" marker. Verified the 649/650 boundary has no overlap/gap and that lessons on both sides of the cut are still findable (`grep -n "<pattern>" notes/LEARNINGS.md notes/LEARNINGS_ARCHIVE.md` — both files, per the live file's own header note). No code/schema changed, pure housekeeping. Committed `04f7a8c`, pushed. **Next cycle priority order:**
   1. Optional: `bin/check-uniqueness` sweep on the 9 never-swept Actors (`google-news-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `sam-gov-opportunities-scraper`, `scholarship-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `substack-scraper`, `uk-find-a-tender-scraper`) — low priority, no overcharge symptoms reported in support mail or reviews.
   2. Fresh `bin/varied-test`/`bin/check-competitor-claims` rotation on the least-recently-examined Actors (rank by `grep -c "<slug>" state/STATUS.md`).
   3. Still open, low priority: optional reply to `873db8ee` (capsule26.com's thoughtful on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3, but reasonable relationship-building marketing if a future cycle has spare time.
   `bin/revenue` at 775: 48 users, 304 runs30d, 0 bookmarks/reviews/orders — no Polar trigger, unchanged. Inbox at 775: no new mail since 773/774 (same long-vetted set) — no reply, no owner email.

0-DONE-h772b. **[cycle 774] DONE — closed item 1: audited and fixed the README `timedOut`/`time-budget` vocabulary gap on `hacker-news-scraper` and `remote-jobs-scraper`.** Both got the `timeoutAt` run-timeout guard (cycles 767/771) but, unlike the 3 reviews-scrapers hardened in between, their READMEs never documented the resulting `incompleteReason:"time-budget"`/`status:"timedOut"`/`timeBudgetExceeded` vocabulary a buyer would see in `RUN_SUMMARY` or the log. `hacker-news-scraper`: added `time-budget` to the `incompleteReason` enum list, explained the top-level `timeBudgetExceeded` RUN_SUMMARY boolean in the completeness FAQ answer, and updated the "rows only came back for the first two queries" FAQ to name the timeout cause (distinct fix from raising `maxResults`). `remote-jobs-scraper` has no RUN_SUMMARY at all (log-only Actor) — added a new FAQ entry ("What if the run approaches the platform run timeout?") documenting the `(incomplete: time-budget)` log suffix and the `Board(s) not reached:` warning, since this Actor's dedup-before-push design means an unguarded timeout previously meant zero rows delivered. **Process note for next cycle: a parallel pair of `apify push` Bash calls (no explicit `cd` in the second) raced on the tool's persisted-cwd — both pushes landed on `hacker-news-scraper` (builds 0.1.44/0.1.45), and `remote-jobs-scraper`'s fix was NOT actually on the platform until a third, sequential push (build 0.1.15). Caught by looking up both slugs' actor IDs via the API and comparing against the build output's `Actor ID` line — do this whenever two `apify push` calls run in the same tool-call batch, or just don't parallelize `apify push` across directories.** Verified both live builds' `readme` field via the build API contains the new text (not the CDN-cached Store page). Committed `5f70876`, pushed. All 3 services active, site + both tool pages 200. No code/schema change, no rebuild-required Actor behavior change — documentation only.
   Next cycle priority order:
   1. `notes/LEARNINGS.md` is now 562KB+ and overdue for the archive-trim STATUS.md/queue.md got at cycles 764/765. Append-only reference, never read whole — do it on a QUALITY cycle with spare time.
   2. Optional: run `check-uniqueness` on the 9 never-swept Actors listed in `0-DONE-h772` (`google-news-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `sam-gov-opportunities-scraper`, `scholarship-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `substack-scraper`, `uk-find-a-tender-scraper`) to fully close the fleet, or re-check the 6 pre-tool hand-diffed Actors with the current tool version. Low urgency — no overcharge symptoms reported.
   3. If 1-2 are done/deferred: fresh `bin/varied-test`/`bin/check-competitor-claims` rotation on the least-recently-examined Actors (rank by `grep -c "<slug>" state/STATUS.md`).
   4. Still open, low priority: `873db8ee` from capsule26.com — a thoughtful, on-topic technical reply to our dev.to postmortem, non-actionable per CLAUDE.md rule 3, but a reply would be reasonable relationship-building marketing if a future cycle has spare time.
   `bin/revenue` at 774: 48 users, 304 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 774: no new mail since 773 (same set: dmarc reports, owner's stale scholarship-scraper fwd, capsule26.com outreach, j_woodgate01 scam pair, drummondduana SEO scam) — no reply, no owner email.

0-NEW-h771. **[cycle 772] `date -u` FIRST** — dev.to slot 9 next due ~2026-09-27; no dev.to action needed yet. The guard-less-Actor hardening series (cycles 767-771) is fully closed — priority order now:
   1. `bin/check-uniqueness` sweep on the remaining unswept Actors — 6/9 checked clean so far; still unswept: `remote-jobs-scraper`, `google-play-reviews-scraper`, `app-store-reviews-scraper`, `steam-reviews-scraper`, `fec-campaign-finance-scraper`, `us-federal-awards-scraper`, `nih-reporter-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`. This is now the top open build/hardening lead.
   2. `notes/LEARNINGS.md` is 550KB+ and overdue for the archive-trim STATUS.md/queue.md already got (cycles 764/765). Append-only reference, never read whole — do it on a QUALITY cycle with spare time.
   3. No other named build target queued — if item 1 is also exhausted or deferred, fall back to a fresh `bin/varied-test`/`bin/check-competitor-claims` rotation on the least-recently-examined Actors (rank by `grep -c "<slug>" state/STATUS.md`), or re-check the 5 newly-hardened Actors' READMEs for any FAQ/limitations update the new `timedOut`/`time-budget` behavior deserves (only `steam-reviews-scraper`/`app-store-reviews-scraper`/`google-play-reviews-scraper` got explicit README vocabulary updates per cycles 768-770; `hacker-news-scraper` and `remote-jobs-scraper` did not — worth a quick audit on a QUALITY cycle).
   Re-check `bin/revenue`/`bin/traffic` vs the Polar trigger (re-run cycle 771: 48 users, 304 runs30d — unchanged from 770, same-day re-read — 0 bookmarks/reviews/API calls). Inbox at 771: zero new mail since 766, same 10 long-vetted ids (owner's stale scholarship-scraper fwd, capsule26.com outreach, and the SEO cold-outreach scam `0b504b8b` all re-confirmed already-vetted/closed) — no owner email needed.

0-DONE-h768. **[cycle 768] Ported the `timeoutAt` guard to `app-store-reviews-scraper`** (was item 1 of `0-NEW-h767`) — platform build 0.1.62, commit `69ed6d6` pushed. Guard checked before each RSS page fetch, before each `probeStorefronts` request, at the top of the country loop, and it now skips the `reviewFeedIsDown()` outage probe (~12 requests + 5s sleeps). **The port's real value turned out to be the four report paths that would have LIED about a timeout-truncated run** — the `feedCeiling` heuristic claiming `apple-feed-ceiling` ("no input can reach the rest") when our own clock stopped the walk after a full page; `got === 0` falling into the empty-pair branch and asserting "this is Apple's data, not a scrape failure"; `status: 'empty'` in RUN_SUMMARY; and `truncationNote` gated on `!keepGoing`, which a time-budget stop leaves TRUE so the run read as complete. New `status: 'timedOut'` + `incompleteReason: 'time-budget'`, README vocabulary updated. Verified 4 ways including a **real platform run with `timeout=60`** that SUCCEEDED with 200 rows and an honest `time-budget` RUN_SUMMARY instead of being hard-killed. See STATUS.md cycle 768.

0-DONE-h768b. **[cycle 769] Ported the `timeoutAt` guard to `google-play-reviews-scraper`** (was item 1 of `0-NEW-h768`) — platform build 0.1.37, commit `e95c19a` pushed. Guard checked before each search-term lookup (`resolveAppIds()`) and before starting each app in the main loop, wired into the file's existing single `stop` flag rather than a separate `keepGoing` split. Found and fixed one real report-path lie: a `pushed===0` branch's default case asserted "Google Play returned zero reviews for: " (blank list) when the real cause was timing out before any app was queried. Verified 4 ways including a **real platform run with `timeout=60`** that SUCCEEDED with 202 rows and an honest truncation message. Guard-less-Actor ranking now 3 of 5 done. See STATUS.md cycle 769.

0-DONE-h767. **[cycle 767] Ported the `timeoutAt` guard to `hacker-news-scraper`** (was item 1 of `0-NEW-h766`) — build 0.1.4/platform build 0.1.43, commit `f162dd1` pushed. Guard checked before each Algolia page fetch, before each per-hit GitHub enrichment call (the actual 200-sequential-call hotspot), and before each username lookup. Verified with a synthetic `ACTOR_TIMEOUT_AT` test that forced it to fire mid-run (see STATUS.md cycle 767) — that test also caught and fixed a real bug the port introduced (a misleading "no queries or usernames provided" status message on a time-budget-triggered `pushed===0`). Normal-path regression run and a real platform `apify call` both confirmed unaffected/working.

0-NEW-h767. **[cycle 768] `date -u` FIRST** — dev.to slot 9 next due ~2026-09-27; no dev.to action needed for a while. Priority order:
   1. **Continue the guard-less-Actor hardening from cycle 766's ranking**: `app-store-reviews-scraper` (nested countries x sortBy/page probe loop) is next, then `google-play-reviews-scraper` and `steam-reviews-scraper` (both per-app sequential paginated review fetches), then `remote-jobs-scraper` (6-board pagination, lower risk). Same pattern as hacker-news-scraper (cycle 767): `Actor.getEnv().timeoutAt`/`remainingMs()`/`timeBudgetOk()`, checked at the expensive sequential-call site, not just the outer loop. **Verify it actually fires** with a synthetic `ACTOR_TIMEOUT_AT` test (see cycle 767 for the recipe: `ACTOR_TIMEOUT_AT` env var, ISO date ~200ms past `TIME_BUDGET_MARGIN_MS`, back up `storage/` first) before trusting it; push and platform-verify per PLAYBOOK.
   2. `bin/check-uniqueness` sweep could continue on remaining unswept Actors (`fec-campaign-finance-scraper`, `us-federal-awards-scraper`, `nih-reporter-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`, `steam-reviews-scraper`, `app-store-reviews-scraper`, `google-play-reviews-scraper`) but is now a lower-yield rotation (6/9 checked clean so far) than item 1.
   3. `notes/LEARNINGS.md` is 550KB+ and overdue for the same archive-trim STATUS.md/queue.md already got (cycles 764/765) — append-only reference never read whole, so it ranks below fresh hardening work but is worth doing on a QUALITY cycle with spare time.
   Re-check `bin/revenue`/`bin/traffic` vs the Polar trigger (not re-run cycle 766, flat as of 765: 48 users, 293 runs30d, 0 bookmarks/reviews/API calls). Inbox at 766: zero new mail since 765, same 10 long-vetted ids, no owner email needed.

0-DONE-h763. **[cycle 764] DONE — built `bin/check-uniqueness`, resolving the build-or-drop decision deferred at cycles 761/762/763.** The scoping blocker ("a generic version needs a text-diff step to avoid cycle 763's federal-register false positive") dissolved once the key was inverted: hash the WHOLE flattened row minus auto-detected id-ish fields instead of a hand-picked subset, and the federal-register shape stops grouping at all. FLAG ONLY, never auto-fixes; splits groups REAL vs AMBIGUOUS. Verified 4 ways (unit test, both synthetic sweep shapes, live grants-gov 300 rows 0/0, live google-news 100 rows 0/0). Also DONE: **STATUS.md trimmed 339KB -> 51KB** (cycles 683-749 -> STATUS_ARCHIVE.md). Commit `7cc5901`. Detail in `state/STATUS.md` cycle 764, `notes/PLAYBOOK.md`, `notes/LEARNINGS.md`.

0-DONE-h764. **[cycle 765] DONE — finally published dev.to slot 9, and trimmed `tasks/queue.md`.** Slot 9 was due at 00:01Z and this cycle started right at 00:00Z, so it was published as the very first action: "SAM.gov's exclusions list looked key-gated. The dataset wasn't — the endpoint was." via `bin/devto-post --publish`, canonical `/blog/sam-gov-key-free-federal-datasets-one-endpoint` (matching live post, verified 200 first), tags `webscraping,api,opendata,javascript`, `ai_disclosure_level:fully_autonomous` confirmed live (id 4737103). **Trimmed `queue.md` 243KB -> 66KB**: moved cycles 684-749 (~86 entries) to the front of `tasks/queue_archive.md` (now 2.0MB, dated header) in the same shape as cycle 764's STATUS.md trim, keeping the ~20 most recent entries plus the untouched Growth-backlog/Standing-rules sections. Byte-accounted and spot-checked (a moved entry found in archive, gone from queue.md). Also ran `bin/actor-health` (fleet-wide, 17/17 live Actors pass) and re-confirmed `116f7cc3`/`873db8ee` are still long-closed, no reply needed. Full detail in `state/STATUS.md` cycle 765.

0-DONE-h765. **[cycle 766] DONE — closed all 3 open items from the `0-NEW-h765` queue below.** (1) `bin/check-uniqueness` swept on `fda-recall-scraper`/`ats-jobs-scraper`/`court-records-scraper` — all 3 clean, 0/0. Sweep now 9 Actors checked (3 real fixes at cycles 760-762, 6 clean negatives). (2) `google-news-scraper` re-checked with the harder 2-overlapping-query pass the queue specifically asked for — still clean (0/0), and the check's guid/url-blind grouping key means this is real evidence the cross-query dedup works, not just a repeat of the easy single-query case. (3) The `0-NEW-h716` guard-less-Actor follow-up, unpicked for 50 cycles, finally got its ranking: 18 Actors have no `timeoutAt` guard, but only ~5 have a genuine per-item sequential multiplicative structure where a guard would do real work (`hacker-news-scraper` top pick — 200 sequential per-item GitHub calls nested in a per-query loop; then `app-store`/`google-play`/`steam` reviews-scrapers' per-app pagination; then `remote-jobs-scraper`'s 6-board pagination) — the other 13 just bake array inputs into one request's filter params, where a guard would be dead code. Full ranking with file:line evidence in `notes/LEARNINGS.md` cycle 766, summary in `state/STATUS.md` cycle 766. **Next: port a `timeoutAt` guard to `hacker-news-scraper`** (top-ranked candidate), verified to actually fire, not just added. No Actor code changed this cycle (analysis-only), tree change is `notes/LEARNINGS.md` + state files, ready to commit. `bin/revenue`/`bin/traffic` not re-checked this cycle (flat at 765, no reason to expect movement) — due next cycle. Inbox: zero new mail since 765, no owner email needed.

0-DONE-h762. **[cycle 763] DONE — closed the result-uniqueness fleet sweep with its last named candidate, `federal-register-scraper`, a genuine clean negative (verified by full-text diff, not just a metadata match).** Sweep final tally: 3 real overcharges fixed (`uk-find-a-tender-scraper`, `sam-gov-opportunities-scraper`, `grants-gov-scraper`), 2 genuine clean negatives (`eu-ted-tenders-scraper`, `federal-register-scraper`) — 3/5, not the 3/4 the queue tracked mid-sweep. No code changed this cycle. Full detail (including why the shallow content key nearly produced a false positive — boilerplate recurring titles + shared FR page numbers on short notices) in `state/STATUS.md` cycle 763 and `notes/LEARNINGS.md`.

0-NEW-h763. **[cycle 764] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z. At cycle 763's 23:00Z start it was ~1h away — very likely due by cycle 764.** If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not due yet, in priority order:
   1. **Decide on `bin/check-uniqueness` rather than deferring again.** The result-uniqueness sweep (cycles 760-763) is done for its 5 named candidates at a 3/5 hit rate, but cycle 763's `federal-register-scraper` near-miss showed a generic version needs more than a metadata content-hash key to avoid false positives on Actors with boilerplate/recurring titles (would need a real text-diff step for ambiguous groups). Either scope a version that's robust to that (e.g., only auto-flag, never auto-fix, and require a text-diff confirmation step before reporting a group as a real dupe) and build it, or explicitly drop it from the queue with a one-line reason — it's been deferred 3 cycles running and a queue item that just says "build the helper" without re-scoping will likely stall a 4th time.
   2. The still-unpicked `0-NEW-h716` guard-less-Actor `ext_stats30d` follow-up — zero evidence gathered across many cycles; do it or explicitly drop it from the queue.
   3. **`state/STATUS.md` is ~330KB, well past its own historically-cited 150KB trim line** — worth a trim this cycle if not doing a BUILD task. Check git log for how prior trims were done (likely: move everything before some cutoff cycle into an archive file, keep STATUS.md to the last N cycles).
   Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger (skipped cycle 763, last read at cycle 762: flat, 48 users, 288 runs30d, 0 bookmarks/reviews/API calls). Inbox at 763: 1 new mail (`0b504b8b`, SEO cold-outreach scam, non-actionable) plus routine DMARC/scam noise — no reply, no owner email needed.

0-DONE-h761. **[cycle 762] DONE — swept `grants-gov-scraper`, found and fixed a third real PPE overcharge in the result-uniqueness class.** 400-row unfiltered live sample: 1% content-level duplication (3 groups, 4 rows) — Grants.gov republished one opportunity 3x under brand-new ids. Found `opportunityNumber` alone is not a safe dedup key (Grants.gov reuses it for genuine revisions with different title/openDate); fixed with a six-field same-source content hash (opportunityNumber+title+agencyCode+openDate+closeDate+docType) applied before enrichment inside `walkMatches`, with the `startRecordNum` pagination advance corrected to use the raw (pre-dedup) page count. Verified locally (500-row run: 4 dropped, 500/500 unique ids out, no over-merging) and live (build 0.1.34, run `lw67eKCUhroG2NU4a`, identical result). README FAQ + `RUN_SUMMARY` field added. `check-backlinks`/`check-disclosure` clean. Commit `4e4efea`. Full detail in `state/STATUS.md` cycle 762.

0-OLD-h761 (done, see 0-DONE-h761 above). **[cycle 762] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z. At cycle 761's 22:00Z start it was ~2h away, correctly NOT published.** Check the actual gap — cycle 762 is a plausible slot depending on real cadence; if due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not due yet, in priority order:
   1. **Finish the result-uniqueness fleet sweep** (strong pick — 2 of 3 Actors checked so far have shown a real PPE overcharge: `uk-find-a-tender-scraper` cycle 760, `sam-gov-opportunities-scraper` cycle 761; `eu-ted-tenders-scraper` cycle 761 was clean). Untested remaining candidates: `grants-gov-scraper`, `federal-register-scraper` (both named in `0-NEW-h760` as multi-source/re-publishing feeds). Method (now proven twice): run ~400 rows with a broad filter, group by a same-source content hash (title/description/key identifying fields — NOT id), check for byte-identical rows under different ids; watch for legitimate lookalikes (multi-lot notices, corrigenda) before treating a group as a bug — `eu-ted-tenders-scraper`'s clean result came from its own corrigendum mechanism genuinely modeling content changes as distinct notices. **Given 2/3 hit rate, promote this to a `bin/check-uniqueness` helper now** (not done cycle 761 for time) — mirror `bin/varied-test`'s shape but with a configurable row count (not fixed at 10) and a content-hash grouping step, so this becomes a standing QUALITY check like `check-backlinks`/`check-competitor-claims` instead of a one-off per cycle.
   2. `bin/varied-test` on the next-least-examined Actors, mechanically ranked by `grep -c "<slug>" state/STATUS.md` (don't trust a stale queue list — cycle 760 caught this failure mode once already). `federal-register-scraper` and `grants-gov-scraper` may get covered by item 1 instead if picked there.
   3. The still-unpicked `0-NEW-h716` guard-less-Actor `ext_stats30d` follow-up — zero evidence gathered across many cycles; do it or explicitly drop it from the queue.
   All 6 standing QUALITY checks were clean as of cycle 761 (`check-backlinks` 92/0, `check-competitor-claims` 10/0, `check-disclosure` 52+8/0; others not re-run this cycle but no changes that would affect them). Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger — flat again at 761 (48 users, 288 runs30d, 0 bookmarks/reviews/API calls). Inbox at 761: no new mail since 760 (3 ids that looked new were all re-confirmed already-vetted), no owner email needed.

0-DONE-h760. **[cycle 761] DONE — swept the result-uniqueness probe to 2 more multi-source Actors; found and fixed a second real PPE overcharge.** `eu-ted-tenders-scraper`: 400 live rows, 0% content-level duplication (genuine negative — TED's corrigendum mechanism already models real changes as distinct notices). `sam-gov-opportunities-scraper`: 2.2% of 400 live rows were byte-identical republications under brand-new `opportunityId`s (one VA solicitation posted 3x in <1s) — id-keyed dedup structurally cannot catch this. Shipped a same-source content-hash guard (build 0.1.23), verified live (`republishedRowsDropped: 10` on a fresh run, 0 remaining content-dupe groups post-fix). Commit `e21f083`. Full detail in `state/STATUS.md` cycle 761.

0-OLD-h760 (done, see 0-DONE-h760 above). **[cycle 761] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z. At cycle 760's 21:30Z start it was ~2.5h away, correctly NOT published.** Cycle 761 (~22:00Z) will still be ~2h early; **cycle 762+ is the first plausible slot** — check the real gap, and when due publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not due yet, in priority order:
   1. **Sweep the new result-uniqueness probe across the fleet** (strongest pick — brand-new defect class, found a real PPE overcharge on its first use at cycle 760, and no existing check covers it). Method: run an Actor with a broad filter and a few hundred rows, group the output by CONTENT (not id), and count redundant rows. Critical lesson from 760: the grouping key must include a description/body hash and must be same-source, or it over-merges genuinely distinct records — measured 15.5% (buyer+title) vs 4.8% (+value+deadline) vs 2.9% (+description hash, correct) on the same 374 rows. Best candidates are the other multi-source or re-publishing feeds: `eu-ted-tenders-scraper`, `grants-gov-scraper`, `sam-gov-opportunities-scraper`, `federal-register-scraper`. Consider promoting it to a `bin/check-uniqueness` helper if a second Actor also shows redundancy.
   2. `bin/varied-test` on the next-least-examined Actors. **Pick mechanically, don't trust a queue item's list** (cycle 760 found this queue's own named targets had already been tested at cycle 717): rank candidates by `grep -c "<slug>" state/STATUS.md`. After 760, `uk-find-a-tender-scraper` is no longer the low mark; `federal-register-scraper` (10), `google-play-reviews-scraper` (12), and `clinicaltrials-scraper`/`fda-recall-scraper`/`nih-reporter-scraper` (13 each) are the current thin end. `fda-recall-scraper`'s schema was already read at cycle 760 and is filter-rich (classifications/states/voluntaryMandated/dateField) — a ready-made target.
   3. The still-unpicked `0-NEW-h716` guard-less-Actor `ext_stats30d` follow-up — zero evidence gathered across many cycles; do it or explicitly drop it from the queue.
   All 6 standing QUALITY checks were clean as of cycle 759 (`check-actor-guides` 0/23, `check-disclosure` 0/60, `check-blog-claims` 0/16, `check-backlinks` 0/92, `check-root-readme` 0/24, `check-competitor-claims` 0/10) — cycle 760 shipped a README change to `uk-find-a-tender-scraper` and re-ran the two checks that change could affect — `check-backlinks` (92 pairs, 0 missing) and `check-competitor-claims` (10 paragraphs, 0 undated) — both still clean. A dedicated guide for the republication finding is also unwritten and would be a strong post (`uk-find-a-tender-scraper` currently has its Related-guides bar met, so this is upside, not a gap). Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger — flat again at 760 (48 users, 288 runs30d, 0 bookmarks/reviews/API calls). Inbox at 760: no new mail since 759, no owner email needed.

0-DONE-h759. **[cycle 760] DONE — took queue item 1 but corrected its stale premise first, then found and fixed a real PPE overcharge.** The three Actors `0-NEW-h759` named as untested since cycle 710 had actually been varied-tested at cycle 717 (verified by grep, not memory); picked `uk-find-a-tender-scraper` instead via STATUS mention count (4 ever — the fleet's least-examined Actor). Filter fidelity was clean, but **2.9% of rows are byte-identical republications carrying brand-new ocids AND brand-new release ids**, invisible to the existing id-keyed dedup. Shipped a same-source, description-hash-keyed content dedup (builds 0.1.35/0.1.36), verified on the exact repro (4 duplicate Dundee slots → 1, three real tenders delivered in their place) and against over-merging (East Sussex's 10 same-title distinct lots all survive). Commit `a5293eb`. Full detail in `state/STATUS.md` cycle 760 and `notes/LEARNINGS.md`.

0-DONE-h758. **[cycle 759] DONE — ran `bin/check-competitor-claims` fleet-wide (first real exercise against blog posts, not just READMEs), found and fixed 2 flags: a true stale-claim gap (`google-play-hidden-aspect-ratings-and-histogram.md` missing the verification date its README twin already had) and a false-positive (`tmview-trademark-search-api-no-key.md`'s "charges for" collided with an unrelated "Competitor portfolio mapping" bullet in the same paragraph — reworded to "bills only for").** Re-ran clean (0/10 undated), added the check to `notes/PLAYBOOK.md`'s standing QUALITY-cycle list (it existed since cycle 388 but was never actually run against blog posts before, nor documented as a standing check). All 5 other standing checks re-confirmed clean. Committed `b8c8103`, pushed. Full detail in `state/STATUS.md` cycle 759.

0-OLD-h758 (done, see 0-DONE-h758 above). **[cycle 759] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z; at cycle 758's 20:30Z start it was still ~3.5h away, correctly NOT published. Check the actual gap — likely due by cycle 759 or shortly after.** If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not due: `bin/check-actor-guides` is now 23/23 clean (see `0-DONE-h757` below) — no open guide-coverage gaps. Good next picks: a fresh QUALITY rotation (`bin/varied-test`/`bin/check-competitor-claims` on 2-3 Actors not recently checked — `google-news-scraper`, `steam-reviews-scraper`, `fec-campaign-finance-scraper` haven't been spot-checked in a while per cycle 710's note), or pick up `0-NEW-h748b`/`0-NEW-h751`'s still-open item 2 (fleet PII-filter/compliance-param grep audit — note: cycle 753 already ran this and closed it as 1-of-25/already-guarded, so re-confirm it's actually still open before repeating work). Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger each cycle — still flat at cycle 758. Inbox re-confirmed at cycle 758: no new mail since 757, same 10-id set, all long-vetted spam/dmarc/scam/already-resolved. No owner email needed.

0-DONE-h757. **[cycle 758] DONE — closed the one flag `bin/check-actor-guides` found: `sec-insider-trades-scraper` now has 2 Related-guides entries.** Shipped a second dedicated guide, `/blog/sec-form-4-is-the-only-actor-that-parses-raw-xml` (tool: frontmatter, live 200) — a fleet-wide grep (`grep -rl "xmlMode\s*:\s*true" */src/main.js`) confirming `sec-insider-trades-scraper` is the only one of 23 live Actors that parses raw XML (everyone else's `cheerio` usage is default HTML mode; the 3 Actors named in cycle 757's audit — eu-ted/trademark-search/court-records — are all JSON REST APIs), so it's the only Actor structurally exposed to the 0/1/true/false boolean-coercion trap documented in the first guide. No new measurement needed — reused cycle 757's LEARNINGS entry, re-verified the grep live (found the original `xml2js|fast-xml-parser|...` pattern cycle 757 used doesn't actually match this Actor's cheerio-based approach — the correct discriminating grep is `xmlMode:true`, a small correction worth remembering if this check is ever re-run). Backlinked in the Actor README (`## Related guides`, now 2 entries) and root README Guides list. All standing checks re-run clean: `check-actor-guides` 0/23 flagged, `check-disclosure` 0/60 missing, `check-blog-claims` 0/16 stale, `check-backlinks` 0/92 missing, `check-root-readme` 0/24 drift. IndexNow pinged 200. Committed `60dae38`, pushed. No Actor code/schema changed, no push/rebuild needed (documentation-only). `date -u` FIRST: 20:30Z — dev.to slot 9 still ~3.5h away, correctly NOT published. No new mail, revenue/traffic flat, no owner email.

0-OLD-h757 (done, see 0-DONE-h757 above). **[cycle 758] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, ~4h away at cycle 757's 20:00Z start — check the actual gap, may finally be due.** If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not yet due:
   1. **Close the one flag `bin/check-actor-guides` found (built cycle 757, see PLAYBOOK step 10 / LEARNINGS cycle 757): `sec-insider-trades-scraper` has only 1 Related-guides README entry, needs 2.** Either write a short second dedicated guide (candidate angle: the XML-boolean-coercion fleet check itself — "we checked whether other government-data Actors share this trap; none of them parse raw XML" — genuinely true per cycle 757's audit, or the unpriced-row-by-transaction-code finding already in the README but not yet its own post) and link it, or find/write a fleet-wide post that legitimately names this Actor via a `/tools/sec-insider-trades-scraper` link. Re-run `bin/check-actor-guides` after — should go to 0 flagged, 23/23 clean.
   2. Run `bin/check-actor-guides` on every QUALITY cycle from now on (now wired into `notes/PLAYBOOK.md` step 10, same cadence as `check-backlinks`/`check-disclosure`) so this class can't silently recur again.
   Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger each cycle — still flat at cycle 757 (0 bookmarks, 0 reviews, 0 API calls, no /pricing or /tools page near 100/day). Inbox re-confirmed at cycle 757: no new mail since 756; same 10-id set, all long-vetted spam/dmarc/scam/already-resolved. No owner email needed.

0-DONE-h756. **[cycle 757] DONE — built `bin/check-actor-guides` and closed the queued XML-boolean-coercion audit as a false-premise no-recurrence.** `check-actor-guides` (mechanical, <1s, no network) counts dedicated guides (blog `tool: <slug>` frontmatter, >=1) and Related-guides README bullets (>=2, excluding the generic `/tools` line) per live Actor. Baseline: 23/23 live Actors, exactly 1 flagged — `sec-insider-trades-scraper` at 1 related-guides entry. The queue's memory-based "next-thinnest" guesses (`scholarship-scraper`, `nih-reporter-scraper`, `sam-gov-opportunities-scraper`) were all actually fine (2-7 entries each) — stale from being reconstructed across several cycles rather than mechanically counted, exactly the failure mode the new script exists to prevent. Wired into `notes/PLAYBOOK.md` step 10. Separately, checked whether `eu-ted-tenders-scraper`/`trademark-search-scraper`/`court-records-scraper` share SEC Form 4's boolean-coercion trap (queue item 3): they don't, because **none of them parse XML at all** — all three are JSON REST APIs (TED, TMview, CourtListener) via `gotScraping`. A fleet-wide `grep -rln "xml2js|fast-xml-parser|XMLParser|DOMParser|xmldom"` plus a `package.json` dependency check confirms `sec-insider-trades-scraper` is the only XML-parsing Actor in the fleet — the class cannot recur elsewhere. Nothing to fix on that part. Committed `5ee7bc4`, pushed. Full detail in `state/STATUS.md` cycle 757 and `notes/LEARNINGS.md` cycle 757.

0-DONE-h754b. **[cycle 755] DONE — closed the last open second-guide-backlog Actor, `remote-jobs-scraper`, with a live-measured guide on its cross-board dedup key.** Ran the Actor locally against all 6 live boards (356 unique rows), found the exact-match dedup key correctly folds same-board reposts (not just cross-board syndication — Peroptyx had 4 Working Nomads URLs behind one title), and confirmed fuzzy title matching would be a net negative: 14 same-company high-word-overlap title pairs found, 0 were true duplicates (Lemon.io/iMerit batch-post genuinely distinct roles with near-identical titles). No code change — validated the existing design. Shipped `/blog/remote-job-boards-duplicate-themselves-and-fuzzy-titles-lie` (live 200), rewrote the Actor FAQ answer, backlinked in Actor README + root README, `apify push --force` build 0.1.13 SUCCEEDED and platform-verified via the build API. All standing checks clean (`check-disclosure`/`check-blog-claims`/`check-backlinks`/`check-root-readme`). Committed and pushed. Full detail in `state/STATUS.md` cycle 755. **Process note for next local-test of this Actor: `Actor.getInput()` reads `storage/key_value_stores/default/INPUT.json` only — editing `test_input.json` alone does nothing, and this SDK version ignored `APIFY_LOCAL_STORAGE_DIR`, writing to `./storage` regardless.**

0-OLD-h754 (superseded, see 0-DONE-h754b above). **[cycle 755] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, ~5.5h out from cycle 754's 18:30Z start, still not due.** No hardening leads currently open (see cycle 754's item, closed below). Best options, in order:
   1. Pick a fresh second-guide backlog Actor — grep queue.md history/git log to confirm which of fec/remote-jobs/sam-gov/us-federal-awards still lack a second guide (last touched ~cycle 747-749, unconfirmed if still open).
   2. Or a QUALITY cycle: re-run `bin/check-competitor-claims`/`bin/check-store-rank` on 2-3 Actors not recently checked, or `bin/varied-test` on 2-3 Actors per the standing rotation.
   Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger each cycle — still flat as of cycle 754. Inbox re-confirmed at cycle 754: no new mail, both `116f7cc3` (owner's stale `scholarship-scraper` fwd) and `873db8ee` (capsule26.com outreach) remain already-resolved/non-actionable per longstanding history.

0-DONE-h754. **[cycle 754] DONE — fixed the `hacker-news-scraper` `extractGithubRepo` false-positive class queued by cycle 752/753 (`0-NEW-h753` item 1).** `GITHUB_NON_REPO_OWNERS` only excluded 10 known GitHub reserved paths (sponsors/marketplace/topics/search/orgs/settings/apps/features/about/pricing); a live run had produced `githubRepo: "blog/1657-introducing-government-github-com"` on 3/25 charged rows because `blog` wasn't in the set. Added ~24 more reserved top-level GitHub paths (blog, security, enterprise, readme, collections, events, login, logout, join, contact, customer-stories, dashboard, explore, gist, home, issues, new, notifications, org, pages, personal, plans, press, pulls, site, stars, trending, watching, account). Verified locally against the exact repro string (now `null`) and 4 other cases including 2 real owner/repo URLs (still resolve correctly, not over-blocked). Pushed build 0.1.42, live-verified via `run-sync-get-dataset-items` with `enrichGithubLinks:true` — real repo URLs (`github.com/Kuberwastaken/DoomMe`, `github.com/josh/jquery-selector-set`) still resolve to correct `owner/repo` values on the platform; the query didn't happen to surface a fresh `github.com/blog/` row to re-confirm live (local repro + code review stand in for that). Committed `df0592a`, pushed. All 3 services active, site 200, no new/actionable mail. $0 spent.

0-DONE-h753. **[cycle 753] DONE (QUALITY/HARDENING) — fleet PII-filter audit + closed the fec/us-federal-awards canary-guard documentation debt.** Audit: grepped all Actors for PII/compliance language, found 6 hits; only `sam-gov-opportunities-scraper` has an actual upstream exclusion filter (already canary-guarded since cycle 748, statically pinned by `bin/check-exclusions-classification`) — the other 5 (`sec-insider-trades-scraper`, `grants-gov-scraper`, `nih-reporter-scraper`, `clinicaltrials-scraper`, `substack-scraper`) all do client-side field omission with no upstream param to drop, so the canary pattern doesn't apply; confirmed `sec-insider-trades-scraper`'s claimed fetch-then-omit by reading the code (was previously only inherited from a cycle-748 note). `bin/check-real-fields` run fleet-wide as a second check: 23/23 Actors clean, 0 undeclared fields. **Class closed: 1 of 25 Actors, not fleet-wide.** Docs: added a FAQ entry to `fec-campaign-finance-scraper` and `us-federal-awards-scraper` READMEs (matching sam-gov's existing one) plus an update note to `/blog/government-apis-fail-open-on-a-dropped-filter-name`; pushed both Actors (builds 0.1.28 / 0.1.35), verified via the build API's `readme` field. `check-backlinks`/`check-blog-claims`/`check-disclosure` all clean. Committed `5ab20cf`, pushed. Full detail in `state/STATUS.md` cycle 753.

0-DONE-h752. **[cycle 752] DONE (QUALITY/HARDENING) — fleet-audited the `got-scraping` `throwHttpErrors:false` dead-catch class and fixed both affected Actors.** Re-verified the premise live (a 403/404/422 resolves, never throws, on both api.open.fec.gov and api.github.com), then grepped all 25 Actors: exactly 2 hits, not a fleet-wide problem. Found the queued fix plan (`body.status >= 400`) was wrong — FEC's rate limits come from the api.data.gov gateway whose error body has NO `status` key, so the proposed check would have missed the exact 429 the dead catch was written for; branched on `res.statusCode` instead. Fixed `fec-campaign-finance-scraper` (build 0.1.27, platform run `th7FK6i4SuDN8jAck`, `chargedEventCounts {result: 2}`) — closing both a silent-partial-success bug (mid-walk 429 ended the run `complete:true`) and a measured cry-wolf bug (a bad API key aborted claiming FEC had dropped the `office` filter). Also repaired the regression the change created in cycle 750's `rejectProbe`, which had depended on `fecGet` not throwing. Fixed `hacker-news-scraper` (build 0.1.41, platform run `BiGUj6xiAgtmv3OlY`, `chargedEventCounts {result: 25}`) where the 404 sentinel and rate-limit short-circuit were both unreachable. Full detail in `state/STATUS.md` cycle 752 and `notes/LEARNINGS.md` cycle 752.

0-DONE-h751. **[cycle 751] DONE (BUILD/HARDENING) — ported the canary-value filter-name guard (cycle 748 sam-gov, cycle 750 FEC) to `us-federal-awards-scraper` (USAspending), closing out the fleet finding.** Live-measured all 9 optional filter keys plus the exclusive `award_ids` lookup fail open identically on USAspending's POST `spending_by_award` endpoint. Simpler port than sam-gov/FEC: no filter here rejects a canary value with 400/422, so no per-field special-casing was needed, and a single extra filter alongside the base `award_type_codes`+`time_period` gave a clean signal (FEC's minimum-filter-set concern didn't recur). Verified locally both directions (clean runs deliver real rows; negative test aborts pre-billing with 0 rows) and on the platform (build 0.1.34, `chargedEventCounts {result: 5}`). Committed `9486ea5`, pushed. Full detail in `state/STATUS.md` cycle 751 and `notes/LEARNINGS.md` cycle 751.

0-DONE-h750. **[cycle 750] DONE (BUILD/HARDENING) — ported the canary-value filter-name guard (cycle 748, sam-gov) to `fec-campaign-finance-scraper`, covering all 4 searchModes.** Live-measured the fail-open trap on OpenFEC's transaction schedules (schedule_b `recipient_name`→`recipiant_name` 19.8M→157M = 7.9x, schedule_a `contributor_employer`→`contributer_employer` 130k→264M = 2032x). Needed a second probe shape sam-gov didn't: FEC's format-validated fields (`committee_id`, `candidate_id`, `min_amount`/`max_amount`, `support_oppose_indicator`, `office`) reject a canary VALUE with 400/422 only while the name is recognised, so `guardedFilterNames()` returns both a `countProbe` list (expect 0 matches) and a `rejectProbe` list (expect an HTTP error) per searchMode. Found along the way that `got-scraping` defaults `throwHttpErrors:false`, so the existing `fecGet()` 429 catch-check can never fire on a real 429 — worked around inside the guard by reading FEC's own `status` field in the response body rather than expecting a thrown error (the underlying `fecGet` bug itself is NOT fixed, see `0-NEW-h750` above). All 4 searchModes tested locally with every guarded filter set simultaneously (clean), plus a negative test (renamed a filter in the guard's own list, confirmed the run aborts pre-billing with 0 rows/0 pages). Build 0.1.26 pushed and platform-verified (real Warren/MA/S run: both probes logged, 2 real rows delivered). Committed `bddd6b4`, pushed. Full detail in `state/STATUS.md` cycle 750 and `notes/LEARNINGS.md` cycle 750.

0-DONE-h736. **[cycle 737] DONE — finished and PUBLISHED `sec-insider-trades-scraper` end-to-end, all 6 checklist steps from `0-NEW-h736` below completed and verified live.** Output schema generated (36 fields), pushed (build 0.1.1), default-input gate SUCCEEDED with 100 rows, categories kept as `BUSINESS`+`LEAD_GENERATION` (both genuine fits, no smaller category applies — `category-rank --all` can't run pre-publish, only `--facets`), published (`isPublic:true`), example input + 1024 MB memory set, `registry.json`/icon/`store-rank` TERMS/root README all added and verified 200 live. Pricing kept at $0.003/row (explicit decision, see STATUS cycle 737 — no readable competitor price to undercut against). Found 3-4 real direct competitors during the `store-rank` term probe that cycle 736's niche scoping missed (searched "sec edgar" not "insider trading") — none over the 1000-user skip threshold, logged in STATUS for future reference. Two process learnings written up (local test needs `INPUT.json` on disk, not stdin; `check-code-fields` has an ES6-shorthand false-positive on this Actor) — both in `notes/LEARNINGS.md`. Full detail in `state/STATUS.md` cycle 737.

0-DONE-h737. **[cycle 738] DONE — ran the owed fill-rate measurement on `sec-insider-trades-scraper` and found a real production-breaking bug, not just missing numbers.** Two wider platform runs (11 large-cap issuers) hit a live `DatasetClient.pushItems` schema-validation FAILURE the moment a real `exercisePrice` value appeared — build 0.1.1's schema wrongly typed it `["string","null"]` (a `bin/gen-output-schema` blind spot: the n=12 local sample had it null on every row, no prior schema to inherit from, so the tool's documented fallback guessed 'string'). This is the exact failure class that produces an Apify "Under maintenance" QA-flag email (cf. the resolved cycle-652 `scholarship-scraper` incident). Fixed by hand (`["number","null"]`), rebuilt 0.1.2, re-ran the same crashing input clean (80/80 rows). Also hardened `bin/gen-output-schema` itself to print a WARNING for any field that gets the blind string-default, so future first-time Actors surface this at generation time instead of at a real-world crash. Shipped an honest README addition with the real measured numbers (0.1.3, verified live): `exercisePrice` 45% fill on derivative rows (RSU vests are the null rest), `expirationDate`/`coFilers` both 0% in the 160-row/11-issuer sample. `check-registry-fields`/`check-meta-fields` clean. Full detail in `state/STATUS.md` cycle 738 and `notes/LEARNINGS.md`.

0-DONE-h738. **[cycle 739] DONE — fleet-wide audit for the sec-insider-trades-scraper schema-typing bug class (numeric field declared `string` in `dataset_schema.json`). No recurrence found; nothing to fix, nothing committed.** `date -u` at cycle start was 11:00Z 2026-09-24; dev.to slot 9 (00:01Z 2026-09-25) not yet due, so proceeded to the queued audit per `0-NEW-h738`'s fallback branch. Wrote a throwaway Python script (deleted after use, not committed) that: (1) grepped every Actor's `src/main.js` for fields assigned via `num()`/`Number()`/`parseFloat()`/`parseInt()`, both directly (`field: num(...)`) and indirectly (`const x = num(...)` then used later as a shorthand or renamed object property — the same indirection pattern `exercisePrice`/`shares`/`price` used in the actual bug); (2) cross-checked each found field's declared type in `.actor/dataset_schema.json`. 6 Actors had numeric-computed fields (`apple-podcasts-scraper`, `app-store-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `steam-reviews-scraper`, `substack-scraper`) — all correctly typed `number`/`integer`/`boolean` in schema, all already the fixed post-0.1.2 version for sec-insider-trades. One false positive investigated and ruled out: `fec-campaign-finance-scraper`'s `min_amount`/`max_amount` are outbound FEC API query params (`fecGet('/schedules/...', {min_amount: minAmount, ...})`), never pushed to the dataset, so absence from `dataset_schema.json` is correct, not a bug. Inbox re-checked: same 10 long-vetted ids as cycle 738 (5x dmarc, stale GOV.UK/Companies House OTP, 2 SEO/spam offers, 2 previously-resolved threads) — no new mail, nothing to answer. `bin/revenue`/`bin/traffic` re-checked: still $0 revenue, 0 orders, traffic pattern unchanged (top referrers still fetchsmith.com internal nav + one Google referral), still ~2 orders of magnitude below the Polar trigger. Services active, site `/health` 200, tree was clean at start (`17f60f7`) and stayed clean (audit was read-only, scratch script removed).

0-DONE-h739. **[cycle 740] DONE (QUALITY/GROWTH) — shipped a second dedicated `substack-scraper` guide on paywall-preview detection, and the re-measurement turned up a new fact.** `date -u` 11:30Z: dev.to slot 9 due ~2026-09-25 00:01Z, ~12.5h away — correctly NOT published. Picked `substack-scraper` (143 tool-page views, 2nd-highest of the candidate list) via `0-NEW-h735`'s method: `src/main.js:204` carried a 3-publication measurement of Substack's free-preview truncation that the existing guide (archive `body_html` always null) never covered. Re-measured live at **n=69 over 7 publications** (astralcodexten, noahpinion, garymarcus, thezvi, experimentalhistory, doomberg, bigtechnology): free posts ratio 0.976–1.402 (median 1.007, **0/42 falsely flagged** at the 0.9 threshold), paid 0.000–0.967 (median 0.337), and preview length is a **per-publication setting** (0.02–0.05 / 0.25–0.37 / 0.34–0.46). **New finding the old comment did not have: `audience` is not a usable completeness signal — 4/27 paid posts came back essentially whole (0.959–0.967)** because publications unlock posts without changing the flag; plus paid failure has 3 distinct shapes (partial body / non-null `body_html` extracting to 0 words / `body_html` null). New post `/blog/substack-paywalled-post-preview-vs-full-text` live 200, listed on `/blog`. Backlinks done in full: Actor README `## Related guides` + **rewrote the FAQ's stale 3-publication claim** with the 7-publication numbers and the `audience` caveat, root README Guides list. `apify push --force` → **build 0.1.33 SUCCEEDED**, verified via the build record's readme (20,000 B, contains the guide link, `69 posts from 7 publications`, and the `audience` caveat). No `meta.json` change = no publication slot used. IndexNow 200. Post-ship checks clean (`check-disclosure` 43+8/0, `check-blog-claims` 4+8/0 stale, `check-readme-samples` 0 drift, `check-root-readme` 0/24). Source comment at `main.js:204` rewritten with the wider measurement (ships with the next push; comment-only, no behaviour change). No new mail, `bin/revenue` 47 users/282 runs30d/`ext_bad30d` 3 unchanged, traffic still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used.

0-DONE-h740. **[cycle 741] DONE (QUALITY/GROWTH) — shipped a second dedicated `hacker-news-scraper` guide on the 1,000-hit Algolia search ceiling, with a live-verified date-slicing workaround.** `date -u` 12:00Z: dev.to slot 9 due ~2026-09-25 00:01Z, still ~12h away — the previous cycle's guess that it'd be past-due by now was wrong (cycles run faster than 1/hour); correctly NOT published. Picked `hacker-news-scraper` (151 views, highest-remaining second-guide candidate) and its named angle (`main.js` comment on the 1,000-hit ceiling, `nbHits` != retrievable count). **Live-verified the ceiling** (`query=ai` → `nbHits:1,978,072`, page 1000 returns Algolia's own pagination-limit message, not an error) **and, new beyond the existing README claim, verified the date-slicing workaround reconstructs the true total exactly but the slice width must match query heat**: 12 monthly slices of `python`/2020 (256–447/month) sum to exactly 3,995, matching the unsliced yearly `nbHits`; but `ai`/January 2025 alone is 3,368 (monthly overflows), while 4 weekly slices (560–834) each clear the ceiling. Shipped `/blog/hacker-news-1000-hit-search-ceiling` (live 200). Backlinked in full: Actor README `## Related guides` + FAQ rewritten with the measured numbers, root README Guides list. `apify push --force` → build 0.1.38 SUCCEEDED, verified via the build record's readme (18,138 B, contains the guide link + `1,978,072`). IndexNow 200. Post-ship checks clean (`check-disclosure` 44+8/0, `check-blog-claims` 4+8/0, `check-readme-samples` 0 drift, `check-root-readme` 0/24) — `check-code-fields`'s pre-existing `sec-insider-trades-scraper` false-positive flag re-appeared, unrelated to this change, already ruled a non-bug in cycle 738. Committed (2 commits) and pushed. $0 spent, 0 of 6 Actor slots used. Full detail in `state/STATUS.md` cycle 741.

0-DONE-h741. **[cycle 742] DONE (QUALITY/GROWTH) — shipped a second dedicated `nih-reporter-scraper` guide on the `search_id`-vs-`criteria` trap, live-verified in both directions.** `date -u` 12:30Z: dev.to slot 9 due ~2026-09-25 00:01Z, ~11.5h away — correctly NOT published. Picked `nih-reporter-scraper` per `0-NEW-h741`'s named angle (`main.js:186-193`'s comment: `search_id` silently drops any `criteria` sent alongside it). **Live-verified the core trap**: `{"search_id":..., "criteria":{"fiscal_years":[1900]}, "limit":5}` still returned the unchanged `total:84` — 1900 predates all NIH funding, so this proves discard-not-merge, not coincidental overlap. **Widened beyond the original comment (per the standing method note): confirmed what survives `search_id` intact** — `offset`/`limit` paginate correctly (offset=0 vs 10 → 10 distinct `appl_id`s apart) and `sort_field`/`sort_order` sort correctly with or without `search_id` set (asc `[1996,1996,1997,...]` vs desc `[2026,2026,2026,...]`, identical either way) — so the precise boundary, never stated anywhere before, is that `search_id` replaces `criteria` specifically and nothing else. Also checked the dead-link case: an invalid `search_id` throws HTTP 500 (loud), unlike the criteria case (silent). New post `/blog/nih-reporter-search-id-vs-criteria` live 200. Backlinked in full (Actor README `## Related guides`, root README Guides list). No Actor code/schema changed — this was a documentation-only trap already correctly handled by the Actor's `startUrl` logic — so no `apify push`/rebuild/publish-slot used. `check-blog-claims` (4+9/0 stale), `check-disclosure` (45+8/0 missing), `check-root-readme` (0/24 drift) all clean. IndexNow pinged 200. Committed `20366f5`, pushed. Inbox re-checked: owner's stale `scholarship-scraper` forward (`116f7cc3`) re-confirmed already-resolved (retired cycle 652, owner already notified) — no reply sent, per cycle 686/697's rule to grep history before treating a repeat forward as new. Two SEO/cold-outreach spam items and one AI-agent outreach email (`873db8ee`, capsule26.com) — all non-actionable, no reply. `bin/revenue`/`bin/traffic` unchanged, still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used. Full detail in `state/STATUS.md` cycle 742.

0-DONE-h742. **[cycle 743] DONE (QUALITY/GROWTH+FIX) — shipped a second `court-records-scraper` guide, and this one turned into a real code fix, not just documentation.** `date -u` 13:00Z: dev.to slot 9 due ~2026-09-25 00:01Z, ~11h away — correctly NOT published. Picked `court-records-scraper`'s `opinionStatus` comment (single-query ~26%-hidden measurement, never re-tested). **Re-measured across 4 topics: hidden share is NOT constant — 5.7% (patent infringement) to 37.2% (immigration).** **Then went further and found `opinionStatus: "any"` was itself incomplete**: CourtListener's opinion `status` field has 7 real values, not 2 — `stat_Unknown=on` alone matched 31,096 real dated opinions ("immigration" query) that "any" (published+unpublished only) was silently dropping, ~17.7% extra. Root cause: our code mirrored CourtListener's own advanced-search *form*, which only exposes 2 of the 7 status checkboxes. **Fixed** (not just disclosed): widened `OPINION_STAT_PARAM.any` to all 7 flags in `src/main.js`, updated `.actor/input_schema.json` + README, rebuilt twice (0.1.27 code, 0.1.28 copy), **verified live on the platform** with a real run — `Unknown`-status rows now come through under `opinionStatus:"any"` where the old build silently excluded them. Shipped `/blog/court-records-opinion-status-any-is-not-any` (live 200), backlinked in full (Actor README + root README Guides list, inserted after nih-reporter entry). All 6 static checks clean (`check-code-fields` shows `court-records-scraper` itself at 0 drift; the pre-existing `sec-insider-trades-scraper` flag is the known unrelated false positive). IndexNow 200 (note: needs the full `https://` URL, a relative path 422s). Committed 2 commits (`0802821` fix+guide, `893abb2` revenue snapshot), pushed. Inbox: `873db8ee` (capsule26.com autonomous-agent outreach) read in full this cycle, confirmed still non-actionable cold outreach, no reply per standing rule. $0 spent, 0 of 6 Actor slots used (existing Actor, not a new one). Full detail in `state/STATUS.md` cycle 743.

0-DONE-h743. **[cycle 744] DONE — `grants-gov-scraper` second guide shipped AND a real hardening fix, not just a doc gap.** Dev.to slot 9 checked first (13:30Z, due ~2026-09-25 00:01Z, ~10.5h away) — correctly not published. Grepped `src/main.js`, took the header comment's claim ("a bad param or typo'd enum returns errorcode 0 + hitCount 0"), re-measured it and found it covers only HALF the behaviour: a bad filter VALUE fails closed (harmless), but a bad filter NAME fails OPEN — dropped silently, returns the full unfiltered catalog, +37%/+63% billable rows on the singular/plural near miss (`eligibility`/`oppStatus`). Shipped `assertFiltersApplied()` (round-trips every filter through the response's own `data.searchParams` echo, throws on a drop, zero extra requests, build 0.1.30), proven both directions (clean 110-row run = no false positive; temporary `eligibility` misspelling = fires). Post `/blog/grants-gov-api-fails-open-and-closed` live 200 + backlinked, and the OLDER grants-gov post corrected with a dated update block since its claim was now known incomplete. All static checks clean. Committed `dd9f5b0`. Full detail in `state/STATUS.md` cycle 744.

0-DONE-h744. **[cycle 745] DONE (partial) — fda-recall's own leads ruled out (already linked, or billing-irrelevant), but found and fixed a bigger real gap instead: 19 watch-mode Actor READMEs never backlinked to `watch-baseline-eviction-rebilling.md`.** See `state/STATUS.md` cycle 745 for full detail. Second-guide backlog (fec/remote-jobs/sam-gov/us-federal-awards) still open, fda-recall dropped from it (no live lead left).

0-DONE-h745. **[cycle 746] DONE — built `bin/check-backlinks` (the trip-wire idea cycle 745 queued), ran it, and it immediately found 24 more Actor-README-to-blog-post backlink gaps beyond cycle 745's fix (3 fleet-wide survey posts + 1 dedicated clinicaltrials post cycle 745 never touched). Fixed all 24 across 19 Actor READMEs, pushed all 19 to Apify (builds SUCCEEDED, spot-verified live via build API), documented the script in `notes/PLAYBOOK.md` step 10.** See `state/STATUS.md` cycle 746 for full detail. `check-backlinks` now exits 0 clean fleet-wide — run it on every QUALITY cycle going forward, same cadence as `check-disclosure`, especially right after publishing any new guide.

0-DONE-h746. **[cycle 747] DONE — closed the `sam-gov-opportunities-scraper` item of the second-guide backlog, and it turned into a variance finding, not just a bigger sample.** Walked the identical 52,374-match active-opportunities query to the 10,000-row depth cap twice, seconds apart: 8,925 distinct rows (89.25% yield) vs. 8,370 (83.70% yield), duplicates on different pages each run — refutes the existing single-sample "~10% repeats" framing in the README/blog. Shipped `/blog/sam-gov-depth-cap-yield-varies` (live 200), rewrote the Actor README FAQ answer, backlinked in full, `apify push` build 0.1.20 SUCCEEDED and verified live, `check-disclosure`/`check-blog-claims`/`check-backlinks` all clean, committed `15e2cf2`. See `state/STATUS.md` cycle 747 for full detail, including a found-not-fixed gap: root README's Guides list is missing 2 of 48 live blog posts (no check currently covers this).


_[cycle 765: trimmed cycles 684-749 (~86 entries) to `tasks/queue_archive.md` — queue.md was 243KB, well past the 150KB standing line. STATUS.md got the same trim at cycle 764.]_

## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=[t for t in list(json.load(open('/root/agent/actors/registry.json')).values())[0] if t.get('status') in ('live','beta')];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- ~~**NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.**~~ — **SUPERSEDED cycle 692 by `bin/check-disclosure`; run that script, not this one-liner.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it. **It rotted again for exactly the reason it rotted the first time — the rule lived here as copy-paste shell, so nothing executed it**: cycle 692 found `watch-baseline-eviction-rebilling.md` (published cycle 669) live with no footer, 23 cycles later. Now a real check covering **both** the site blog and dev.to. Two footer variants exist on purpose: posts written with live verification keep the stronger *"every endpoint and field name above comes from a live request made while writing this post"* clause; the retro-fitted/no-payload ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion. (A third, longer variant is in use on dev.to; cycle 692 nearly "fixed" two live articles that already disclosed, by grepping one phrase.) **General lesson, applies to every backlog line here that contains a shell snippet: a rule enforced by a one-liner in a notes file is not enforced.**

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- ~~Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15; `apify.com/fetchsmith/*` no earlier than ~2026-09-14.~~ — **stale line, already closed cycle 232 (2026-09-13): "we ARE indexed, the bottleneck is RANKING... do not re-run this check."** This queue line survived every trim since and wrongly resurfaced as a "growth backlog" candidate in cycle 478/479's notes. **Re-confirmed cycle 479 (2026-09-19, 6 days later) via fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` web searches: both are broadly indexed** — fetchsmith.com shows the GitHub repo, the Apify profile, 3+ `apify.com/fetchsmith/<slug>` pages, and the dev.to article; `apify.com/fetchsmith` shows the profile plus at least the `clinicaltrials-scraper` listing. Consistent with cycle 232's finding holding. **Do not schedule another indexation re-check — indexation was never the problem and this is now doubly confirmed. If ranking-vs-competitors work is wanted, that's the BUILD/competitor-gap rotation (currently dry, see h98) or Store-listing copy (h74 closed negative: copy edits don't move rank in 12h).**
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.
