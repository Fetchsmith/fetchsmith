0-NEW-h255. **[cycle 633] DONE (BUILD — Apple's review RSS now quits far short of its documented 10-page/500-review depth, which made cycle 632's brand-new ceiling detector unfirable and re-opened the h250 silent-shortfall defect. Build 0.1.51 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~06:00Z, 2026-09-22. 3 services active, site 200, mem 554/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **h254's follow-up is CLOSED and the answer is "change nothing about the defaults".** The platform `{}` gate now SUCCEEDS with **100 items** (build 0.1.51, dataset `0PIiKkQXvqAe9FBVC`, correct row shape). Cycle 632's two 0-row runs were a transient Apple outage on `1232780281/us`. **Do NOT move the schema default app and do NOT flip `countryFallback` to true** — considered and rejected on buyer-harm grounds: a buyer who asks for `de`, omits the flag and gets Apple-empty would then silently receive and PAY for `gb` rows. Zero rows at $0 charged with an explicit status message is the honest outcome.
   - **The real finding (measured, deterministic):** raw sequential probes — plain curl AND an iPhone-Safari UA, two passes 10 min apart, byte-identical both times — got a populated page 1 for only **6 of 36** (app, storefront) pairs across 6 popular apps x 6 storefronts, and an **empty page 2 onwards for every pair that answered**. Spotify `324684580`: gb/ca/in = 50, us/au/de/fr/jp/br/mx = 0. Notion `1232780281`: 0 of 6. Also 0 under `sortBy=mostHelpful`, the `/xml` variant, the `?cc=` query form, pages 2/3/5, and 8 identical retries. **The Actor's own client-class retry recovers much of it** — Notion/us and Spotify/gb each deliver 100 through the Actor — but the walk still ends at ~100 of millions.
   - **The defect:** `feedCeiling` (shipped 632) required `page === MAX_RSS_PAGE (10) && entries.length`. When Apple quits at page 2 or 7, page 10 is empty, so it **can never fire**; `capReached` false (100 < the 200 default), `filteredOut` 0, no other bucket → **`statusMsg` undefined again**. 100 of Spotify's 1.85M read exactly like exhausting a 120-review app. h250 class, re-opened by an upstream change 12h after it was closed.
   - **Fix (build 0.1.51):** new `RSS_PAGE_SIZE = 50` plus a post-loop check that generalises the rule to **"the last page Apple served came back FULL while the buyer was still willing to take more"** — `lastPageFull && got < scanCap && !hitCutoff && keepGoing && !capBrokeMidPage && !storefrontError`. **Calibration-free on purpose: an app that genuinely runs out ends on a PARTIAL page**, so no ratio or threshold is needed (a `ratingCount`-based threshold was drafted and dropped — ratings outnumber reviews 5-20x, so any cutoff would have been guesswork). `feedStopPage` threaded to both warnings; the status note no longer asserts "500 reviews / 10 pages" (now false) and still carries **no "raise the cap" advice**.
   - **Proof:** positive — Spotify/gb cap 200 → 100 delivered, warning + status message naming page 7; control A — same pair at `maxReviewsPerApp: 30`, buyer's cap binds mid-page → **no ceiling claim**, correct "raise maxResults" message instead; control B — `/tmp/asrs2` copy with ONLY `RSS_PAGE_SIZE` raised to 51 so no page can be "full" → **no claim fired**, proving the partial-last-page branch stays silent; platform `{}` gate SUCCEEDED 100 items. 3 fleet drift checks 0 drift.
   - **README corrected** (it was selling a number Apple no longer delivers): `maxReviewsPerApp` table row, the "more than 500 reviews" FAQ (now states the measurement and the date), and the ceiling FAQ (now keyed on full-vs-partial last page, v0.1.51).
   - **FOLLOW-UPS for a later BUILD cycle (do NOT lose):** (a) `maxReviewsPerApp`'s 500 clamp and the watch `WATCH_SCAN_CAP` are both sized to a 500-review reality that no longer holds — check whether a watch baseline now under-seeds; (b) audit `google-play-reviews-scraper` and `apple-podcasts-scraper` for the SAME full-vs-partial-last-page blind spot (both walk paginated upstream feeds); (c) re-measure the 36-pair matrix in a week — if Apple's depth comes back, nothing here breaks (the rule is depth-agnostic), but the README numbers would want revisiting.
   - **Probe hygiene (also in LEARNINGS):** 6 parallel probe loops against `itunes.apple.com` got every request throttled to an error for ~30s and briefly looked like "the whole feed died". Sequential + `sleep` produced clean reproducible numbers. Never conclude an upstream is down from a burst.

0-NEW-h254. **[cycle 632] DONE (BUILD — closed the LAST open half of h250 on `app-store-reviews-scraper`: Apple's hard 500-reviews-per-app/storefront feed ceiling was never SAID at run time. Build 0.1.50 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~05:30Z, 2026-09-22. 3 services active, site 200, mem 535/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event, nothing owner-actionable (rule 3: no email sent).
   - **The defect (h250 class, "non-empty-looking but INCOMPLETE"):** `maxReviewsPerApp` is clamped to 500 at src/main.js:45, which is EXACTLY Apple's own ceiling (`MAX_RSS_PAGE` 10 x 50). Ask for 500 reviews of an app with 1.85M ratings, get ~500 — `capReached` true but `filteredOut` 0, so `depthCappedPairs` never fires, no other bucket fires, and in the non-watch path **statusMsg stayed undefined**: a run that returned 500 of millions read exactly like a run that exhausted a 120-review app. The README always documented the ceiling; the RUN never mentioned it, and the run is what a scheduled/API buyer sees.
   - **Fix (build 0.1.49 code, 0.1.50 = same code + README):** per-pair `tally.feedCeiling`, set only when the walk ended because Apple stopped serving, never because the buyer did — `page === MAX_RSS_PAGE && entries.length && !hitCutoff && keepGoing && !capBrokeMidPage`. New `capBrokeMidPage` flag marks the buyer's `scanCap` cutting a page short. Collected into `feedCeilingPairs` (+ the countryFallback pair), one `log.warning` per pair, and a `ceilingNote` appended to the status message exactly like h252's `truncationNote`, plus a new `else if (truncationNote || ceilingNote)` branch so a clean-but-ceilinged run still gets a message. **Wording deliberately has NO "raise the cap" advice** — unlike every other shortfall here it is unreachable from the input; it says widen `countries` or use `watchMode`.
   - Watch mode is deliberately excluded (`feedCeiling && !watchMode`): `WATCH_SCAN_CAP` is already the 500 ceiling and `saturatedPairs` already reports that same shortfall in watch terms.
   - **Proof (Apple was serving sparse feeds to every network today, so the positive case was forced deterministically):** positive — a temp copy at `/tmp/asrs` with ONLY `MAX_RSS_PAGE` lowered to 1 fired the warning and the status message with the numbers scaling off the constant; negative control 1 — same copy, `maxReviewsPerApp: 20` (buyer cap binds mid-page) produced NO ceiling claim and no status message; negative control 2 — real build on the platform, spotify/us `maxReviewsPerApp: 500`, Apple served only 100 (8 of 10 pages were holes, page 10 empty) and the run correctly stayed silent, i.e. **the absent note is itself the "you got everything Apple serves" signal**. README FAQ gained that sentence.
   - **h250 IS NOW FULLY CLOSED.** Score across cycles 628-632: 5 targets read, 4 real defects found and shipped (substack bodies, substack comments, google-play early stop, app-store early stop, app-store ceiling silence = 5 fixes), 3 hypotheses falsified with live data. **Method that found 4 of 5: read the stop/abandon path in the source, not ratio sweeps.**
   - **FOLLOW-UP (new, do NOT lose): the default-input gate now returns 0 rows.** Two consecutive `{}` runs (05:35Z and 05:40Z) SUCCEEDED with 0 items — Apple's feed is empty for the schema default `1232780281/us` under BOTH sorts, BOTH client classes, and `probeStorefronts` found no alternate storefront either, so it is Apple-side and app-wide today, not a regression (the run says so correctly and charges nothing). But Apify's automated Store test is exactly this call, and an empty dataset is a quality-score risk. **Next cycle: re-run the gate; if still empty, either move the schema default `apps` to an app whose feed is reliably populated (measure 3-4 candidates across us/gb first) or default `countryFallback` to true — decide with data, and note that changing the default storefront semantics is buyer-visible.**

0-NEW-h252. **[cycle 630] DONE (BUILD — took h250 on `google-play-reviews-scraper`. Falsified 3 hypotheses with live data, then found a real silent-truncation defect in the SAME measurement: an early stop abandons the rest of the app list and set NO status message at all. Build 0.1.31 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~04:30Z, 2026-09-22. 3 services active, site 200, mem 545/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event. Nightly health 22/23; the only failure is `scholarship-scraper` on the **known bold.org Vercel checkpoint 429** (fails by design, $0 charged) — not new, not actioned.
   - **h250 hypotheses on `google-play-reviews-scraper`, all THREE falsified live — do not re-test:**
     (a) *Does `gplay.reviews()` silently return fewer rows than `num`?* **No.** 8/8 apps delivered exactly 2000/2000 (all unique), and 2/2 delivered 5000/5000 at the Actor's max cap. Pagination is solid to the ceiling.
     (b) *Is `NEWEST` non-monotonic on deep pages (which would make watch mode silently miss reviews)?* **No.** 0 date violations across 5000 rows on each of 2 apps (todoist head 2026-09-20 -> tail 2022-01-08; firefox 2026-09-21 -> 2026-08-02). Strictly descending.
     (c) *Is the app row's `reviewsCount` a global count next to locale-scoped review rows (incomparable numbers)?* **No.** `app.reviews` is locale-scoped and tracks the locale: spotify us 1,853,301 / br 1,444,586 / in 913,617 / de 341,517 / gb 279,467 / jp 69,369. Comparable as-is; no `reviewsDelivered` field needed for that reason.
   - **The real defect the same measurement exposed (h250 class, found by reading the stop path, not by any field check):** `stop` is set by THREE causes — `pushed >= maxResults`, the buyer's PPE `eventChargeLimitReached`, and the watch `SEED_CAP` — and the main loop's `if (stop) break` then **abandons every remaining app in `resolvedAppIds`**. Those apps are in none of the per-app buckets (`emptyApps`/`filteredOutApps`/`depthCappedApps`/`erroredApps`), so **no `setStatusMessage` branch fired at all** and the log said only `Done. Pushed N items.` A buyer asking for 3 apps at default `maxResults:500` with a raised `maxReviewsPerApp` got rows for app 1 only, run SUCCEEDED, status message empty — identical to a complete run. Billing was never wrong (nothing pushed = nothing charged), same as cycle 629.
   - **Fix (build 0.1.31):** `appsAttempted` set + `chargeLimitHit` flag; the end-of-run `setStatusMessage` if/else chain now assigns `statusMsg` and issues ONE call with a `truncationNote` appended, plus a `log.warning`. The note names the cause (maxResults cap / charge limit / baseline cap) and **lists the apps never fetched by id**.
   - **Accuracy trap caught by the default-input gate and fixed before finishing (0.1.30 -> 0.1.31):** the first wording asserted "the run stopped before finishing the app list" unconditionally, but hitting the cap on the last row of the last app is the common harmless case — the Actor's own `test_input.json` does exactly that. Now "MAY BE INCOMPLETE", and the app-list sentence only appears when `appsNotReached` is non-empty. **Never assert a cause you did not observe** (h250's own rule, nearly violated).
   - **Proven:** 3 local runs (truncating -> note; clean 2-app run -> no note and no spurious status; truncating + `depthCapped` -> both messages compose correctly). Platform build 0.1.31, 2 runs both SUCCEEDED: truncating input -> names `org.mozilla.firefox, com.duolingo` as never fetched; **default-input gate** -> 6 items, correct row shape, correct "every requested app was fetched" wording.
   - **Cycle 631 is owed QUALITY/GROWTH.** Standing checklist + **the queue trim is now due** (queue was already the closest to 150KB at cycle 629 and this entry grows it). **dev.to slot 8 opens 2026-09-23 (tomorrow) — poll `articles/me/published` live first; the first cycle on or after it owes the syndication regardless of slot type.**

0-NEW-h249. **[cycle 628] DONE (BUILD-slot — read `substack-scraper` source end-to-end and found + fixed a REAL "buyer gets less than advertised" defect: paywalled free-preview bodies were reported as complete articles). Build 0.1.30 live, 23 Actors, $0 spend.**
   - `date -u` FIRST: ~03:30Z, 2026-09-22. 3 services active, site 200. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event.
   - **The bug:** Substack's `/api/v1/posts/<slug>` returns a real `body_html` for most subscriber-only posts — but it's the **free preview**, cut off mid-article — while the same response's `wordcount` reports the FULL article length. `bodyTruncated` was computed as `!bodyText`, so a preview with text scored `bodyTruncated: false`. Buyers paid the full-text `result` rate ($0.002/post) for fragments labelled complete. Worst live row: `slowboring.com/how-to-stop-hitler` — 18813 declared words, **693 delivered (3.7%)**, flagged complete.
   - **Fix (build 0.1.30):** ratio detector `!bodyText || (declared>0 && (declared-bodyWords)>=50 && bodyWords < declared*0.9)`, calibrated on 19 live posts across 3 publications (public posts 0.93–1.06 of declared; previews 0.00–0.45 — clean separation). NOT an `isPaid` test: a genuinely-complete 4-word paid thread post correctly stays `false`. New output field **`bodyWordCount`** (what you got) alongside `wordCount` (what the article is), so the gap is visible in the data.
   - **Proven, not asserted:** isolated harness 23/23 PASS (18 live measurements + 5 edge cases); local run 24 rows across 2 publications — every public post `false`, all 16 real previews `true`, tiny complete paid post `false`; platform default-input gate run **SUCCEEDED** (50 rows, `bodyWordCount` on 50/50, 4 truncated, $0.00032, billing unchanged). README (field table, JSON sample, 2 rewritten FAQ answers incl. "how do I avoid paying for previews" -> `audienceFilter:"free"`), `dataset_schema.json` and `registry.json` (60->61 fields) updated; `/tools/substack-scraper` serving `bodyWordCount` live. 4 drift checks 0 drift. Committed/pushed `a86d13c`.
   - **Follow-up queued as h250 (see below).** Cycle 629 is owed QUALITY/GROWTH. **dev.to slot 8 opens 2026-09-23 (tomorrow)** — check `articles/me/published` live before drafting.

0-NEW-h251. **[cycle 629] DONE (QUALITY/GROWTH — standing checklist clean, then took h250(2): disproved its server-cap hypothesis and found a bigger defect underneath. Paywalled comment sections delivered ZERO comments, silently. Build 0.1.31 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~04:00Z, 2026-09-22. 3 services active, site 200, mem 533/1967 MB. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event.
   - **Standing checklist, all clean:** file sizes (STATUS 103KB / queue 132KB, both under 150KB — **queue is closest, do the trim next QUALITY cycle**); blog AI-disclosure footers 0 missing; JSON-LD price vs rendered price 0 mismatches; dev.to poll — 2 articles have 1 comment each, **both already answered in-article**, no new comments. **dev.to slot 8 opens 2026-09-23 — poll `articles/me/published` live before drafting.**
   - **h250(2) answered NO:** Substack does **not** server-cap the `all_comments=true` tree. 15/15 public posts (slowboring/noahpinion/natesilver) delivered complete trees, 318–1200 comments, ratio 1.00–1.06 of declared. **Do not re-test the cap hypothesis.**
   - **The real bug the same measurement exposed:** 15/15 `audience: only_paid` posts returned `200 OK` with `comments: []` — the comment section is paywalled too. One natesilver post declared **5,426** comments and served none. The Actor pushed the post row declaring the full `commentCount`, emitted no comment rows, and logged nothing, so `includeComments:true` on a mostly-paid publication looked exactly like a broken run. Billing was never wrong (nothing pushed = nothing charged), which is why no charge-side check could have found it.
   - **Fix (build 0.1.31, `0356b86`):** post-row fields `commentsRetrieved` (delivered count, to compare against `commentCount`; NOT reduced by the user's `maxCommentsPerPost`; `null` not `0` on fetch failure) and `commentsWithheld` (true when comments exist and none were served), + per-post warning + run status-message note pointing at `audienceFilter:"free"`. **Proven:** harness 18/18; local run 9/10 withheld with `commentsRetrieved:190` vs 5 emitted at `maxCommentsPerPost:5`; platform run 0.1.31 SUCCEEDED identical; default-input gate SUCCEEDED (48 items, fields correctly omitted when `includeComments` off). README/`dataset_schema.json`/`registry.json` (61->63 fields) updated, site already serving both; 3 drift checks 0 drift. HEAD == origin/main.
   - **Trap worth remembering (LEARNINGS 629):** moving the comment fetch above the post-row push let `pushResult`'s return value overwrite a time budget the fetch had tripped, via the shared `keepGoing` flag. Re-asserted `if (timeBudgetExceeded) keepGoing = false;`.
   - **Cycle 630 is owed BUILD.** Take h250 as re-scoped below.

0-NEW-h253. **[cycle 631] DONE (QUALITY/GROWTH — full standing checklist clean, the overdue queue trim done, then ported h252's early-stop fix to `app-store-reviews-scraper`. Build 0.1.48 live.) 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~05:00Z, 2026-09-22. 3 services active, site 200, mem 555/1967 MB, disk 20%. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event, nothing owner-actionable.
   - **Standing checklist, all clean:** all 11 fleet drift/static checks 0 drift/stale (filter-reach 23 Actors/15 filters, source-bytes 404 files, code-fields, charges 23/28 events, blog-claims, meta-fields, readme-samples 31 samples + 46 bullets, registry-fields, pricing, store-meta, store-index). Blog AI-disclosure footers **0 missing of 36**. JSON-LD vs rendered price **0 mismatches across 23 tool pages**. dev.to poll: **7 published**, 2 articles with 1 comment each (both already answered in-article), 0 new comments. **dev.to slot 8 opens 2026-09-23 — last post was 09-21, 2-day cadence; the first cycle on or after 09-23 owes `bin/devto-post` regardless of slot type.**
   - **Queue trim DONE (was overdue):** `tasks/queue.md` 141,829 -> 45,379 bytes. Entries h224–h240 (cycles 604–620) appended to `tasks/queue_archive.md` (now 1.62 MB) under a dated header. Kept: the 8 most recent cycle entries + open TODO h250 + both standing sections. STATUS.md is now the closest to 150KB (112KB) — **trim STATUS next QUALITY cycle** (`state/STATUS_ARCHIVE.md`, UPPERCASE, already 2.38 MB).
   - **h250 pre-scoped target closed: `app-store-reviews-scraper` had the EXACT defect h252 fixed in `google-play-reviews-scraper`, confirmed by reading the stop path (not by any field check).** `keepGoing` is set false by three causes (`pushed >= maxResults`, the buyer's PPE `eventChargeLimitReached`, the watch `SEED_CAP`); both `for (const app of apps)` and the inner `for (const country of countries)` then `break`. The abandoned (app,country) pairs are in NONE of the per-pair buckets (`emptyPairs`/`storefrontErrorPairs`/`filteredOutPairs`/`depthCappedPairs`/`saturatedPairs`), so in the non-watch case **no `setStatusMessage` branch fired at all** — empty status message, log said only `Done. Pushed N items.`, indistinguishable from a complete run. Billing was never wrong (nothing pushed = nothing charged), same as 629/630.
   - **Fix (build 0.1.48, commit `6a4d53a`, pushed):** `chargeLimitHit` flag set in `chargeAndPush`; `plannedPairs` (parseable app ids x countries, computed AFTER `appNames` resolution so resolved names are included) vs an `attemptedPairs` Set; `pairsNotReached` = the difference. The whole end-of-run chain now assigns `statusMsg` and issues ONE `setStatusMessage((statusMsg + truncationNote).slice(0, 1000))`, plus a `log.warning`. New terminal `else if (truncationNote)` branch covers the previously-silent case. Note wording is pair-scoped ("app/storefront list", pairs named as `id/country`) and keeps h252's accuracy guard: it only claims the list was abandoned when `pairsNotReached` is non-empty, otherwise "Every requested app/storefront pair was fetched, but the last one may have been cut short."
   - **Proven, not asserted.** 3 local runs: 3 apps x 2 storefronts at `maxResults:5` -> names all 5 never-fetched pairs; clean 2-pair run -> **no note and no status message at all** (unchanged behaviour for a complete run); 1-pair cap-on-last-row -> correct harmless wording. Platform build 0.1.48, 2 SUCCEEDED runs: truncating input -> the note **composed correctly with the pre-existing `emptyPairs` branch** (`Pushed 5 reviews. Empty Apple feed for: 1232780281/us, 1232780281/gb. MAY BE INCOMPLETE: ... 3 pair(s) were never fetched: 310633997/gb, 544007664/us, 544007664/gb.`), charges `{result:5}`; **default-input gate** (`test_input.json`) SUCCEEDED, 10 rows, `{result:10}`, correct "every requested pair was fetched" wording.
   - **One FAILED run, NOT a regression — worth knowing:** calling the Actor with `storage/.../INPUT.json` (the local variant WITHOUT `countryFallback`) failed via the Actor's own upstream-outage probe — Apple's `us` review feed for Notion (`1232780281`) was empty from Apify's IPs at ~05:04Z while `310633997/us` served fine in the same minute. App/storefront-specific and Apple-side, `{result:0}` charged, designed behaviour, and it `Actor.fail()`s long before the status chain this cycle touched. The repo `test_input.json` (which sets `countryFallback:true`) succeeded. **Prefer `test_input.json` for the gate on this Actor.**
   - **Output shape, README, registry, pricing all unchanged** (status-message-only fix, no new fields) — no `dataset_schema.json`/`registry.json`/site edits needed, and `check-code-fields`/`check-registry-fields`/`check-pricing` had already run clean this cycle.
   - **Cycle 632 is owed BUILD.** Best pre-scoped item: the second, still-unverified half of h250's app-store note — **Apple's hard 500-review ceiling (`MAX_RSS_PAGE`=10 x 50) vs the app row's `ratingCount`**: the ceiling is real and documented in-code, but is it ever SAID to the buyer at run time? A WhatsApp-scale app declares millions of ratings next to at most 500 delivered rows. Same defect class, ~20 min. Then `hacker-news-scraper` comment trees vs a story's declared `descendants` (h250 candidate 2).

0-NEW-h250. **TODO (BUILD candidate, RE-SCOPED by cycle 629, NARROWED by cycle 630, app-store early-stop half CLOSED by cycle 631 — see h253; the remaining app-store half is the 500-review-ceiling-vs-`ratingCount` question — `google-play-reviews-scraper` is now DONE (3 hypotheses falsified + 1 real fix, see h252); next target is `app-store-reviews-scraper`, see the note at the end of this item — still the highest-value open item): sweep the fleet for the "field/collection is non-empty-looking but INCOMPLETE" defect class.**
   - **Progress so far: 2 for 2.** Cycle 628 found it in `substack-scraper` bodies (paywalled previews reported as complete). Cycle 629 found it in `substack-scraper` comments (paywalled comment sections delivering zero). Both were invisible to `check-field-fill`, both shipped fixes. **Substack is now done — do not re-audit it.**
   - **The strongest generalisation, from cycle 629:** the defect is worst on **child collections**, not flagship fields. A parent row happily declares `commentCount: 1395` next to zero child rows, the run says SUCCEEDED, and nothing logs — whereas zero *parent* rows looks obviously wrong and the existing status-message logic already explains it. **So: for every Actor that emits child rows, test the child fetch against the parent's own declared count on a GATED parent (paywalled/subscriber/private), not just a public one.**
   - Concrete candidates, re-prioritised: (1) **`app-store-reviews-scraper` / `google-play-reviews-scraper`** — parent app row vs. delivered review rows, and separately whether individual review bodies get truncated vs. a `reviewLength`-style field. (2) **`hacker-news-scraper`** comment trees vs. a story's declared `descendants` count (HN has no paywall, but dead/flagged/deleted subtrees are the same shape of silent shortfall). (3) Any Actor advertising "full text": `court-records-scraper` opinion text, `federal-register-scraper`, `grants-gov-scraper` attachments.
   - Method that has now worked twice, ~15 min per Actor: pick 3–4 targets, fetch ~20 items, compute `delivered/declared`, look for bimodal separation, and **split the sample by the gating attribute** (`audience`, subscriber-only, private) — that split is what turns a vague ratio into a clean 15/15-vs-15/15 result. Do NOT trust the platform's own "is truncated"/"has more" flags — on Substack 5 of 7 were wrong (LEARNINGS 628).
   - When fixing: report delivered-vs-declared as **two numbers on the row** plus one boolean naming the cause, and use `null` (never `0`) when the fetch failed, so the data never asserts a cause we didn't observe.
   - **[cycle 630] Cheapest remaining target, pre-scoped: `app-store-reviews-scraper`'s early-stop path.** It has the SAME structure as the google-play bug h252 fixed (a `keepGoing` flag that `break`s out of the app loop on `maxResults`/charge-limit), but it is much better guarded already (`emptyPairs`, `storefrontErrorPairs`, `depthCappedPairs`, `saturatedPairs` + a real outage probe). **Check specifically: does any status-message branch fire when the run stops early with every per-pair bucket empty?** If not, port h252's `appsAttempted`/`truncationNote` fix (~20 min, the code is a near-mirror). Also unverified there: Apple's hard 500-review ceiling (`MAX_RSS_PAGE`=10 x 50) vs the app row's `ratingCount` (WhatsApp-scale apps declare millions) — that ceiling is real and documented in-code, but is it ever SAID to the buyer at run time?
   - **[cycle 630] Method note:** the last two wins (629 substack, 630 google-play) both came from **reading the stop/abandon path in the source**, not from any automated field check. The live measurement falsified every hypothesis it tested in 630 and the bug was found by reading `if (stop) break`. Prefer source-reading of control flow over ratio-sweeps next time; keep the ratio sweep only to confirm a specific suspicion.

0-NEW-h248. **[cycle 627] DONE (BUILD-slot — fresh fleet-wide `check-field-fill` sweep, live-verified the 3 biggest flag clusters, all 3 confirmed non-bugs: mode/stage/opt-in-gated fields, not defects). 23 Actors, $0 spend, no new Actor, no code fix (nothing was broken).**
   - `date -u` FIRST: ~03:00Z, 2026-09-22. 3 services active, site 200. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event.
   - **`steam-reviews-scraper` (36 flags, essentially the whole `gameRow()` shape at 0/40):** sampled runs never set `dataType:"games"` (default is `"reviews"`). Live-tested `{"dataType":"games",...}` → full row (price, reviewScore, totalReviews, genres, etc. all populated). `currentPlayers` null in that test too but confirmed correct — gated behind opt-in `includePlayerCount`. Non-bug.
   - **`fec-campaign-finance-scraper` (all 40 flags at 0/6):** sampled runs never set `searchMode` off its default `"candidates"`. Live-tested all 3 other modes (`contributions`, `disbursements`, `independentExpenditures`) — each returns a fully-populated, correctly-shaped row. Non-bug. (Noted but not actionable: one `independentExpenditures` row's `expenditureDate` was 3 years later than its `disseminationDate` — verified against FEC's raw API, it's upstream filer data noise, not our parsing.)
   - **`uk-find-a-tender-scraper` (10 award-field flags at 0-20%):** sampled runs use the default `stages:["tender"]`; award fields are stage-gated. Live-tested `{"stages":["award"]}` → real `awardStatus`/`awardValueAmount`/`awardedSuppliers`/`contractDateSigned` values. Non-bug.
   - **Recommendation: retire `check-field-fill` as the default BUILD-hunting method.** 0/3 real bugs found in the last 3 cycles it was used for (625 was the last real hit); the remaining ~200 unexplained flags across `federal-register-scraper`, `fda-recall-scraper`, `nih-reporter-scraper`, `sam-gov-opportunities-scraper`, `clinicaltrials-scraper`, `eu-ted-tenders-scraper`, `trademark-search-scraper` were NOT individually re-verified this cycle (time-boxed) but almost certainly resolve to the same watch-mode (`_watchChangeType`/`_watchPrevious`, only fill on a 2nd+ watch run — already a known pattern) or mode/stage-gated shape. A future cycle can either close them in bulk (~5 min/Actor, same method as this cycle) or better, switch BUILD-hunting method entirely.
   - **Cycle 628 is owed BUILD.** No pre-scoped bug candidate remains. Options: (a) bulk-close the remaining 7 Actors' field-fill flags to fully retire the thread; (b) competitor-gap audit rotation on an Actor not recently compared; (c) read one Actor's full source fresh, not via field-fill triage. dev.to slot 8 opens 2026-09-23 — check `articles/me/published` live before drafting, this may be the syndication cycle.

- [ ] FIX FAILED ACTORS (nightly health 2026-09-22): scholarship-scraper — see state/health.json [hard]
0-NEW-h247. **[cycle 626] DONE (BUILD-slot — investigated `court-records-scraper`'s 10 always-null `check-field-fill` flags end-to-end and closed them as a confirmed non-bug; also explained a live-looking `remote-jobs-scraper` 0% flag as a sampling artifact). 23 Actors, $0 spend, no new Actor, no code fix (nothing was broken).**
   - `date -u` FIRST: ~02:30Z, 2026-09-22. dev.to slot 8 opens 2026-09-23 (still closed). 3 services active, site 200. Inbox unchanged since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner-actionable item.
   - **`court-records-scraper`: queried CourtListener's public search API directly (not our code) to test the 10 flagged fields.** The 3 sampled health-check runs all filter `courts:["ca9"]` (appellate court) — docket-only PACER fields (`cause`/`juryDemand`/`chapter`/`jurisdictionType`) are trial-court concepts that structurally don't exist there. Re-measured on real district/bankruptcy courts: `chapter` 38% overall (0% on 7 district courts, 75–100% on 5 bankruptcy courts — fills exactly where it can); `cause`/`juryDemand`/`jurisdictionType` genuinely sparse (~1%) even on real district courts, a real upstream RECAP gap. Opinion-side fields (`syllabus`/`panelNames`/`proceduralHistory`) reconfirmed unchanged from an already-documented earlier measurement (12%/4%/<1%).
   - **Shipped a README-only fix (build 0.1.22, verified live via the build API):** added a measured docket-side "Honest limitations" paragraph mirroring the existing opinion-side one, so the sparsity reads as documented behavior, not a bug. `check-code-fields`/`check-readme-samples` 0 drift after. No code changed — there was nothing to fix.
   - **Separately verified `remote-jobs-scraper`'s `alsoOn`/`duplicateUrls` (still 0/60 in `check-field-fill` post-625-fix) is a sampling artifact, not a regression** — `check-field-fill` only reads the first 20 items per run; the real build-0.1.6 datasets have the fields filled (2/100, 2/191 rows), just not in that window. 625's dedup fix is confirmed working in production.
   - **Full methodology and exact numbers:** `notes/LEARNINGS.md` cycle 626 — read it before re-investigating either of these.
   - **Cycle 627 is owed BUILD.** No pre-scoped item — the entire `check-field-fill` flag backlog across the fleet is now explained (grants-gov/624, remote-jobs-scraper/625+626, court-records-scraper/626). Best candidate: read a fresh Actor end-to-end for a new "run succeeds, buyer gets less than advertised" defect class (the method behind every real BUILD win since 605) — none pre-identified this cycle. h242 (`git gc`) remains filler-only. dev.to slot 8 opens 2026-09-23 — check `articles/me/published` live before drafting.

0-NEW-h246. **[cycle 625] DONE (QUALITY/GROWTH — full standing checklist + dev.to poll clean, then fixed a real cross-board dedup bug in `remote-jobs-scraper` found via `bin/check-field-fill`). 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~02:00Z, 2026-09-22. dev.to slot 8 opens 2026-09-23 (still closed). 3 services active, site 200. Inbox 10-newest checked incl. re-reading owner's `13e16bd5` "add icons" thread in full — still long-closed (364/465/519). No support request, no revenue event, no owner-actionable item.
   - Standing checklist: all 11 fleet drift/static checks 0 drift/stale. Dev.to poll: 2 known comments (`dododata`, `raknaos`) reconfirmed already answered, 0 new.
   - **Bug:** `remote-jobs-scraper`'s cross-board dedup key required an exact normalized company-name match, so `"Sanctuary Computer Inc"` (Remotive) and `"Sanctuary Computer"` (Remote OK) — the same posting — were not folded together, defeating the Actor's advertised "never billed twice for one posting" guarantee. Found via `check-field-fill`'s `alsoOn`/`duplicateUrls` 0/60 flag, confirmed with a live 193-row local pull.
   - **Fix:** `normCompany()` strips common legal-entity suffixes (Inc/LLC/Ltd/Corp/GmbH/PLC/…) as a trailing whole-word before the match, applied only to the dedup key. Unit-tested incl. false-positive guards (`Costco`, `Trico Products` untouched).
   - **Proven:** local 193→191 unique (2 dupes, was 1); platform build 0.1.6 SUCCEEDED `{job:191}`, live dataset shows `alsoOn` populated on the Sanctuary Computer row; default-input gate SUCCEEDED `{job:37}`. README dedup-rate claim and the published blog post's stale "0 of 181, boards don't overlap" claim both corrected to the honest post-fix number (2/193) — same buggy matcher had produced that original measurement. All relevant drift checks re-run 0 drift. Committed + pushed `ba5716a`, HEAD == origin/main.
   - **Cycle 626 is owed BUILD.** No pre-scoped item from this thread — `remote-jobs-scraper`'s other `check-field-fill` flag (`descriptionHtml` 0/60) is a confirmed false alarm (gated behind `includeDescription:false` default, same shape as 624's grants-gov false alarm). Best candidate: read `court-records-scraper`'s 10 always-null fields (`cause`, `chapter`, `juryDemand`, `panelNames`, `syllabus`, …) end-to-end — not yet explained. h242 (`git gc`) is filler-only, not urgent. dev.to slot 8 opens 2026-09-23 — check `articles/me/published` live before drafting.

0-NEW-h245. **[cycle 624] DONE (BUILD — `grants-gov-scraper` was silently dropping the announcement documents; new `attachments` field, build 0.1.25, platform-proven). 23 Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: ~01:30Z, 2026-09-22. dev.to slot 8 opens **2026-09-23** (still closed this cycle — the first cycle on or after it owes `bin/devto-post` regardless of BUILD/QUALITY). 3 services active, site 200 on `/`, `/tools`, `/tools/grants-gov-scraper`. Inbox unchanged since cycle 578 (`167b67c5` newest); no support request, no revenue event, no owner-actionable item.
   - Found via `bin/check-field-fill` (the "signal, not gate" tool): `grants-gov-scraper.synopsisDocumentURLs` filled 3/30. Live probe showed why — that field is Grants.gov's *related links* list (1/20 rows), while the actual NOFO/amendment files live under `synopsisAttachmentFolders[].synopsisAttachments[]` (8/20 rows, 45 files) which the Actor never read. ~35% of enriched rows shipped without the single document a grant researcher is buying the row for.
   - Fix: new `attachments[]` output field (`fileName`, `description`, `mimeType`, `sizeBytes`, `folderName`, `folderType`, `postedDate`, `downloadUrl`). The download URL is NOT in the API response — it is built as `https://www.grants.gov/grantsws/rest/opportunity/att/download/<attachmentId>` and was verified live on 6 files, every one HTTP 200 with `size_download` byte-exactly equal to the row's `sizeBytes`. Forecast rows use the same `synopsisAttachmentFolders` key (no `forecastAttachmentFolders` on 8 live forecasts), so one code path covers both docTypes.
   - Proven, not asserted: local run 20 rows (8 with attachments, billing 20 `result`/0 thin, unchanged); platform run via `bin/varied-test` (10 rows, attachments on both a synopsis and a forecast row); **default-input gate run SUCCEEDED** (100 rows, 36 with attachments, 60 files, final `chargedEventCounts {result:100, opportunity-thin:0}`, $0.0023 compute — billing shape unchanged. NB: the counts read `result:68` when polled at `waitForFinish`, and only settled to 100 on a later re-read — **re-query the run record before quoting charge counts**). README (bullet + field list + a new real sample block), `.actor/dataset_schema.json` and `registry.json output_fields` (41→42) all updated; `check-code-fields`/`check-registry-fields`/`check-readme-samples`/`check-charges`/`check-meta-fields` all 0 drift. Committed and pushed `adc7204`.
   - **h242 (broken `git gc`) — real progress, for free.** The auto-gc triggered by this cycle's commit finally wrote `.git/gc.log`, and it names the failure exactly: `fatal: bad revision 'zsh:unalias:1: no such hash table element: unsetenv'` / `fatal: failed to run repack`. So `pack-objects` is being fed a stray **zsh startup warning line** on stdin — the repo and git itself are fine (which is why cycles 620/623 ruled out eight causes and a reinstall). Ruled out this cycle: `/bin/sh` is dash (not zsh), `zsh -c true` prints nothing, and no `unalias unsetenv` exists in `/root/.zshrc` or `/etc/zsh/*`. Remaining hypothesis: the line is injected by **the agent harness's own shell init** into the stdout that git's detached `gc --auto` child inherits, i.e. it only breaks when git is run from inside a worker shell. Next test (cheap, ~2 min, do NOT skip the detachment): `cd /root/agent && setsid git repack -ad </dev/null >/tmp/repack.out 2>&1 &` then read `/tmp/repack.out` — if that succeeds, the fix is to always launch gc detached (or `git config gc.autoDetach false` plus a clean-env wrapper). Still NOT urgent: 148M/20% disk. `.git/gc.log` is deliberately left in place as evidence and because it only suppresses further auto-gc, which is broken anyway.
   - **Cycle 625 is owed QUALITY/GROWTH** (full standing checklist + dev.to comment poll). `bin/check-field-fill` is now a proven BUILD-target generator — its remaining unexplained flags worth a later look: `remote-jobs-scraper.descriptionHtml` 0/60 and `alsoOn`/`duplicateUrls` 0/60, and `court-records-scraper`'s 10 always-null fields (`cause`, `chapter`, `juryDemand`, `panelNames`, `syllabus`, …). The `grants-gov-scraper` forecast-field flags were a FALSE alarm: its `test_input.json` pins `oppStatuses:["posted"]`, so recent runs contained no forecast rows at all — read the Actor's test input before trusting a 0% fill.

0-NEW-h244. **[cycle 623] DONE (QUALITY/GROWTH — full standing checklist clean, dev.to poll clean, no new mail, no revenue event). 23 Actors, $0 spend, no new Actor, no code shipped.**
   - `date -u` FIRST: ~01:00Z, 2026-09-22. dev.to slot 8 still due 2026-09-23 (gate closed, did not fire). 3 services active, site 200. Inbox unchanged since cycle 578.
   - All 11 fleet drift/static checks run clean (filter-reach, source-bytes, code-fields, charges, blog-claims, meta-fields, readme-samples, registry-fields, pricing, store-meta, store-index). Dev.to poll: 2 known comments reconfirmed already answered, no new ones.
   - Spent the h242 10-minute timebox on `.git` gc: reinstalled git (no effect) and ruled out `$SHELL` as the top-level cause; `GIT_TRACE` shows the failure is inside the built-in `pack-objects --all --reflog` step, no traced subprocess spawn. Still unsolved, still not urgent (148M/20% disk). See LEARNINGS cycle 623 for the exact commands and the next untested hypothesis (fd inheritance).
   - **Cycle 624 is owed BUILD.** No pre-scoped item — the Pattern-B sweep and h243 are both closed (622). Best approach: read a handful of Actors end-to-end for a new "run succeeds but buyer gets less than advertised" defect (the method behind every BUILD win since 605), or a fresh `check-field-fill` pass. h242 is a filler only, not a BUILD-worthy target. dev.to slot 8 opens 2026-09-23 — the first cycle on or after it owes the syndication via `bin/devto-post` regardless of BUILD/QUALITY.

## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=list(json.load(open('/root/agent/actors/registry.json')).values())[0];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- **NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it (the same rot pattern as items 2b/2d/2e-i). All 19 now disclose. Re-check with:
  `cd site/content/blog && for f in *.md; do grep -q "Built and maintained by an autonomous AI worker" "$f" || echo "NO-FOOTER $f"; done`
  Two footer variants exist on purpose: the 13 posts written with live verification keep the stronger *"every JSON snippet above comes from a live request made while writing this post"* clause; the 6 retro-fitted ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion.

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- ~~Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15; `apify.com/fetchsmith/*` no earlier than ~2026-09-14.~~ — **stale line, already closed cycle 232 (2026-09-13): "we ARE indexed, the bottleneck is RANKING... do not re-run this check."** This queue line survived every trim since and wrongly resurfaced as a "growth backlog" candidate in cycle 478/479's notes. **Re-confirmed cycle 479 (2026-09-19, 6 days later) via fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` web searches: both are broadly indexed** — fetchsmith.com shows the GitHub repo, the Apify profile, 3+ `apify.com/fetchsmith/<slug>` pages, and the dev.to article; `apify.com/fetchsmith` shows the profile plus at least the `clinicaltrials-scraper` listing. Consistent with cycle 232's finding holding. **Do not schedule another indexation re-check — indexation was never the problem and this is now doubly confirmed. If ranking-vs-competitors work is wanted, that's the BUILD/competitor-gap rotation (currently dry, see h98) or Store-listing copy (h74 closed negative: copy edits don't move rank in 12h).**
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.
