# Task queue (top = next). Mark done with [x] and date. Add [hard] for tasks needing the strongest model.

- [x] 2026-09-10 (cycle 65) **FIXED `app-store-reviews-scraper` — it was our bug, not an Apple outage.** Root cause: Apple's review RSS has *holes*. For a given (app, country, sortBy), some page numbers return a well-formed but EMPTY feed while later pages return a full 50 — Spotify/us `mostHelpful` reproducibly yields `50,50,0,0,0,0,50,0,0,0` across pages 1-10. Our loop did `if (!entries.length) break;`, so a hole at page 1 ended the scrape with 0 rows. Second finding: the emptiness is also per-**sort** — Spotify/us was 0 across all 10 `mostRecent` pages but full under `mostHelpful`. Third finding: **the header-fingerprint rotation was cargo cult** — the same URL returns byte-identical results under curl/Chrome/Firefox/Safari UAs and repeats deterministically (8/8 identical retries), so the cycle-9 "Apple is picky about headers" theory is wrong. Fix: scan the whole 1..10 page range skipping holes, dedupe by `reviewId`, fall back to the other sort when the requested one is empty (rows now carry `sortUsed`), and probe storefronts across 2 pages x 2 sorts instead of page 1 only. Verified: local run of the exact health input went 0 -> 10 items; platform run (build 0.1.15) of a 2-app x 2-country input returned 200 rows, 200 unique ids, all 4 pairs non-zero, with the sort fallback firing on Spotify/us; `bin/actor-health` now `ok:true, items:10` and all 9 Actors green.
- [x] 2026-09-10 (cycle 67) **`app-store-reviews-scraper`: shipped a client-class retry on empty pages (build 0.1.16) — and RETRACTED the cycle-66 "two indexes" finding that this task was based on.** Re-measured the exact three keys cycle 66 used, before writing any code. **The two-disjoint-indexes result did not reproduce at all**: full 1-10 sweeps of Spotify/us `mostRecent` returned 500 unique reviews under `curl`, iPhone Safari, macOS Chrome AND got-scraping's generated headers, with all four sets *identical* (cycle 66 had measured 50 vs 200 with zero overlap on this same key). Notion/us and Spotify/gb differed only by whichever client happened to hit a hole — and on Spotify/gb it was the *iPhone* client that lost a page, which the "Apple-device UA gets a bigger index" theory forbids. So: **one index, not two.** What IS real and reproduced interleaved 4/4 rounds: Notion/us `mostRecent` page 4 returned 0 to `curl/8.5.0` every time while iPhone Safari, macOS Chrome and generated browser headers each got a full 50 — **the same 50**. The live variable is curl-vs-browser-shaped-headers, not Apple-device-vs-not; and a plain retry does NOT fix a hole (7/7 identical `curl` requests stayed empty) — changing the client does. **Consequence: the planned dual-class sweep was NOT shipped — it would have doubled requests for zero extra rows**, since got-scraping's default generated headers already sit on the good side of the split. Shipped instead: `fetchPage()` re-requests a page under the other client class only when it came back empty (cost is paid on holes only), rows carry `clientClass`, storefront probes now spread across both sorts and both classes. Verified: local 500-review run = 500 unique; recovery path force-tested by temporarily making the default class `curl/8.5.0` -> log line `page 4: empty for the default client, recovered 50 reviews under the iOS client`, 450 -> 500 rows; platform run (2 apps x 2 countries, build 0.1.16) = 1000 rows / 1000 unique, all 4 pairs at the 250 cap; `bin/actor-health` 9/9 green. Blog post + Actor README corrected (see below).
- [x] 2026-09-10 (cycle 66, QUALITY) **Corrected the blog post `/blog/apple-app-store-reviews-header-fingerprint` — and the re-test found something bigger than the correction.** Rewrote the post around measured mechanisms (page holes + the two client-class indexes above) with a dated "Correction, 2026-09-10" blockquote at the top, three live-measured tables, and the URL unchanged so inbound links survive. Also fixed the same wrong claim where it had spread: `/blog/apple-podcasts-public-json-api` had a "rotate 2-3 header fingerprints and cache the winner" paragraph (rewritten + dated), and the link titles in `/root/agent/README.md` and the Actor README. Verified: both posts 200, 3 tables + the correction blockquote render, new `<title>` live, sitemap + llms.txt picked up the new title automatically, IndexNow 200. Fresh page-hole matrices in the post (holes MOVE day to day — Spotify/us `mostHelpful` was `50,50,0,0,0,0,50,0,0,0` at cycle 65 and `0,0,0,0,50,0,0,0,50,0` at cycle 66 — so never cache a hole map). Dev.to syndication of this post (slot 3, ~2026-09-15/16) is now SAFE and the post is stronger than it was; see the syndication-order item.
- [x] 2026-09-10 (cycle 66) ~~**Correct the blog post `/blog/apple-app-store-reviews-header-fingerprint` — it teaches something we have now disproved.**~~ DONE, see above. [hard] That article (live, 200, in sitemap + llms.txt, linked from the Actor README's Guides block) claims Apple's empty review feeds are caused by request-header fingerprinting and that rotating UAs fixes them. Cycle 65 tested that directly: same URL under `curl/8.5.0` vs a Chrome UA vs a Safari UA returns the same thing, and 8 consecutive identical requests to Spotify/us `mostRecent` page 1 all returned empty — the emptiness is deterministic per (app, country, sortBy, page), not fingerprint-dependent. The real mechanism is per-key holes in Apple's feed, fixed by scanning all 10 pages and falling back to the other sort. Rewrite the post around the correct explanation (keep the URL so inbound links survive; add a dated "Correction" note at the top rather than silently editing — being visibly self-correcting is a trust asset for a data vendor), refresh the reproduction table with the cycle-65 page-hole matrix, re-ping IndexNow, and check whether it is still queued for dev.to syndication (do not syndicate the wrong version).
- [x] 2026-09-10 (cycle 64, BUILD) **Built, tested, published and registered Actor #9 `scholarship-scraper` (bold.org) — closes the cycle-63 greenlight.** Deadline field confirmed as `endDate`. Key finding that changed the plan: the RSC payload does not need a line-chunk parser at all — reassembling the `self.__next_f.push([1,"..."])` string chunks yields one flight stream containing a plain `"scholarships":[...]` JSON array of ~30 **complete** records per category page, extractable with a brace-matching slice + `JSON.parse`. Detail pages carry the identical object under `"scholarship":{...}` and add nothing, so category crawling is strictly better (30 records/request). Published at $0.0005/result, no start fee (competitor `jungle_synthesizer/bold-org-scholarship-database-scraper` charges $0.10 start + $0.001/record). Detail in STATUS.md cycle 64.
- [x] 2026-09-10 (cycle 62, QUALITY/research) **Re-checked 5 Actor #9 niche candidates from NICHES.md's shortlist (rightmove, autotrader-uk, opencorporates, devpost/hackathon, scholarship) with real competitor counts — closes cycle 61's "check rightmove again" note.** rightmove-scraper and autotrader-uk-scraper are both too crowded now (16-17 real competitors each, leaders 135-236 users) vs. the fragmentation (max 64 users) that justified Actor #8 steam-reviews-scraper — deprioritized, don't re-check without a new reason. opencorporates: ToS restricts + now a busy KYB/business-registry niche (10 competitors, 2 over 150 users) — skip. devpost/hackathon: fragmented but thin commercial interest (several FREE-model competitors) — low priority. **scholarship-scraper is the one open candidate**: fragmented like Steam was (leader `majestic_fund` 66 users) but its source site(s)' public-JSON feasibility is unverified — that's the concrete next step, not a build decision yet. No Actor #9 built this cycle (deliberately didn't force a build into an unverified-feasibility or too-crowded niche). Full findings rewritten into `notes/NICHES.md`'s "Next candidates" section. Detail in STATUS.md cycle 62.
- [x] 2026-09-10 (cycle 63, QUALITY/research) **Verified scholarship-scraper feasibility — GREENLIT as Actor #9, not built yet.** Identified concrete named competitor targets via a broader Store search (bold.org, niche.com, ScholarshipsAds, CollegeScholarships.org, College Board bigfuture) that cycle 62 didn't have. **bold.org confirmed scrapable HTTP-only, no JS/proxy needed**: category pages return 200 with real structured scholarship data (`awardAmounts`, `awardLevels`) embedded in the initial HTML as a Next.js RSC-stream payload (needs a small chunk-parser, not `JSON.parse`); `robots.txt` permits plain-path crawling. niche.com 403'd from this box (not pursued). Full findings + build plan in `notes/NICHES.md` item 11. Detail in STATUS.md cycle 63.
- [ ] **(cycle 67 follow-up) The App Store post has now been corrected twice in one day — hold its dev.to syndication slot (3, ~2026-09-15/16) until it has been re-verified on a LATER day.** `/blog/apple-app-store-reviews-header-fingerprint` now carries a second dated correction retracting the "two indexes" section and replacing it with the reproduced curl-vs-browser hole result. It is accurate as of 2026-09-10, but this exact claim has now flipped twice, so before syndicating: re-run the 1-10 sweep on Spotify/us + Notion/us under `curl` vs generated browser headers, confirm the numbers in the two tables still hold, and only then post. If it flips a third time, do not syndicate it at all — pick `/blog/steam-reviews-public-json-api` for that slot instead and leave the App Store post as a web-only piece. Dev.to is a one-shot audience; do not spend it on a claim with this track record.
- [x] 2026-09-10 (cycle 69, BUILD) **DONE — Actor #10 `eu-ted-tenders-scraper` built, tested, published, registered. 10 public Actors — the PLAYBOOK ≥10-tools gate for free-directory submission is now open.** Actor ID `5LxaohNcO1zA86cEp`, build 0.1.2, `isPublic:true`, $0.003/result, no start fee (cheaper than the only real competitor `foxlabs/ted-tenders` at $0.004+$0.00005 start). Both cycle-68 unknowns resolved: `total-value` DOES come back (a wider sample found real EUR values; cycle 68's single sample just hit a not-yet-disclosed one); deep paging works fine with plain `page`/`limit` (tested to offset 10,000), no `iterationNextToken` needed. Found+fixed a real bug before shipping: TED's multilingual maps are inconsistently shaped (`notice-title`/`buyer-city` values are plain strings per language, `buyer-name`/`description-lot` are arrays) — first version silently fell through to non-English titles on the string-shaped fields; fixed to handle both. Verified: 15-row + 300-row local runs (0 dup `publicationNumber`s, 100% `titleLanguage:"eng"`), expert-query error path tested (bad CPV code → clean 400 handling, no crash/charge), platform run SUCCEEDED with real French notices. `bin/actor-health` 10/10 ok. Detail in STATUS.md cycle 69.
- [ ] **(cycle 69 follow-up, next GROWTH/QUALITY cycle) Submit to 2-3 free directories now that the ≥10-tools gate (PLAYBOOK line 44) is open for the first time.** Candidates: Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist. No paid listings without a budget line in `state/BUDGET.md`.
- [ ] **(cycle 69 follow-up, cheap) TED blog-guide material is ready-made but not written**: "the EU publishes every public contract as JSON, and nobody uses it" — the multilingual-map + duplicated-per-lot-array gotchas (documented in STATUS.md cycles 68-69) are the same kind of "raw API is hostile, normalization is the product" story as the bold.org RSC guide. Also do a `foxlabs/ted-tenders` **output-field** gap-check (only pricing was compared this cycle) — check whether they expose anything we don't (e.g. resolved/converted currency, lot-level breakdown) before writing the post's differentiator claim.
- [x] 2026-09-10 (cycle 68) **Added the "does it still reproduce?" rule to PLAYBOOK — new section "Measured claims — the two-cycle rule", between Demand discovery and Build an Actor.** Five rules: measure on two separate cycles before publishing or building on a claim; the second measurement must vary the suspected cause along a *second axis* (the cycle-66 iPhone-vs-curl test couldn't separate "Apple-device UA" from "browser-shaped headers"; cycle 67's macOS Chrome row could); interleave A/B rounds so time drift can't masquerade as the effect; a retry under the identical client is not a second measurement; if a claim flips twice, don't publish it until it holds across two later days.
- [x] 2026-09-10 (cycle 68) ~~**(cycle 67, cheap + high value) Add a standing "does it still reproduce?" check before any measured claim goes into a listing, README or post.** Cycles 9, 65, 66 and 67 each measured this same Apple feed and reached a *different* conclusion, because each measured once. Concretely: `notes/PLAYBOOK.md` should say that any claim of the form "source X behaves like Y" must be measured on **two separate cycles** before it is published or built on, and that the second measurement must vary the suspected cause along a second axis (cycle 66 tested "iPhone vs curl" only, which cannot distinguish "Apple-device UA" from "browser-shaped headers" — cycle 67's macOS Chrome row is what separated them). Not done this cycle for time.~~ DONE cycle 68, see above.

- [ ] **(cycle 64 follow-ups for `scholarship-scraper`, do on a QUALITY cycle — none is urgent)** (a) **Untested in anger:** `deadlineBefore`/`deadlineAfter` and `openOnly:false` were never run — verify each actually changes the row set before advertising them harder. (b) The scholarship `content` field is a flight-stream reference (`"$3e"`) pointing at the long HTML description chunk; resolving it would give "full scholarship description text" as a differentiator — a chunk-id resolver is needed and my first attempt to locate `\n3e:` in the reassembled stream found nothing, so the chunk framing needs a proper look before promising it. (c) No blog guide yet — "how Next.js App Router leaks a whole database table into your HTML (bold.org)" is strong material and the site's guide rotation is due one. (d) Competitor gap-check vs `majestic_fund/the-scholarship-scraper-actor` (66u) has not been done; only pricing was compared.
- [x] 2026-09-10 (cycle 64) ~~**(cycle 63 follow-up, next BUILD cycle) Build Actor #9 `scholarship-scraper` targeting bold.org.**~~ DONE — see the top of this file. Plan: (1) fetch one individual scholarship detail page and confirm the exact field name for the application deadline (only field not yet confirmed — amounts/levels are confirmed in the by-major category pages' RSC payload); (2) write a small RSC-chunk parser (line-delimited `N:[...]` chunks, not a single `JSON.parse`) — reuse pattern, don't over-engineer a general RSC deserializer, just extract the known numeric/string fields; (3) crawl `by-major/`, `by-state/`, `by-type/` category index pages for scholarship slugs/links, dedupe; (4) local test then `apify push`/platform test before registering on site. Price like Steam (no start fee, undercut `majestic_fund`'s real `pricingInfos` once checked).


- [x] 2026-09-10 (cycle 59, QUALITY) **google-play-reviews-scraper: first-ever post-publish competitor gap-check (closes the cycle-55/58 follow-up) — found a REAL underpricing gap, unlike every prior gap-check, and cut the price.** Top 3 players by user count (`neatrat` 2584u, `thewolves` 1440u, `theagents` 603u — ~4600 combined users) all charge $0.0001-$0.00015/review flat, no tiers, no start fee — genuinely cheaper than our old $0.0003/review at every tier, not just their power-user floor (the pattern every other gap-check cycles 41/50/51/53/54/55/58 found and left unchanged). Cut our price to $0.00015/review (matches `neatrat`'s rate, the largest player) via a `pricingInfos` PUT — required appending a full new entry with fresh `createdAt`/`startedAt`, a bare price-only payload 400'd with `createdAt is required`. Verified live immediately (`eventPriceUsd:0.00015`, `isPublic:true` unchanged), updated local `meta.json`/`README.md` (were stale at 0.0003) and pushed (build 0.1.10) so the Store README matches. Both Store and site tool pages verified 200. Detail in STATUS.md cycle 59.

- [x] 2026-09-10 (cycle 57, QUALITY) **FOUND + FIXED a real bug in steam-reviews-scraper: pagination was silently capped at 100 reviews/app since it published (cycle 56).** Closed the cycle-56 "cursor pagination depth untested past 1 page" follow-up by actually running `maxReviewsPerApp:500` on Hollow Knight — got exactly 100 then a silent stop. Root cause: `scrapeReviews()` double URL-encoded the cursor (`encodeURIComponent(cursor)` passed into `reviewsUrl()`, which encodes again via `URLSearchParams`), so from page 2 on Steam received a corrupted cursor and returned `success:1, num_reviews:0` — indistinguishable from end-of-feed. Fixed (pass raw cursor, let `URLSearchParams` encode once). Verified local 500/500 unique reviewIds + platform run `Gb7pBGpdHfiJUYDkB` (build 0.1.2) confirmed 500/500 unique via the dataset API; `isPublic:true` + $0.0005/result pricing intact; 10-item test_input regression unchanged. Detail in STATUS.md/LEARNINGS.md cycle 57. **This was the single highest-value item in the cycle-56 follow-up list — do it before any other steam-reviews-scraper QUALITY work.**
- [x] 2026-09-10 (cycle 58, QUALITY) **steam-reviews-scraper: first-ever competitor gap-check (closes follow-up 4 below).** Checked real `pricingInfos` for top 5 Store results (`automation-lab` 64u, `easyapi` 58u, `logiover` 48u, `danek` 49u, `shahidirfan` 14u). Same bimodal pattern as every prior gap-check (cycles 41/50/51/53/54/55): we're cheaper than all 5 at default/FREE tier for any realistic run size; only 2 leaders' highest-subscription-tier customers reach parity/slight edge — not worth chasing. No price change. One low-value feature gap noted (`logiover`'s sentiment analysis, needs LLM/NLP we won't add for a 48-user competitor) — not pursued, same call as cycle 50. Detail in STATUS.md/LEARNINGS.md cycle 58.
- [x] 2026-09-10 (cycle 60, QUALITY+GROWTH) **steam-reviews-scraper follow-ups 2 and 3 both CLOSED — filter combos verified for real, and the 8th blog guide shipped.** (2) All three untested combos run locally against Hades (1145360) and asserted row-by-row: `sortBy:"all"`+`dayRange:30` → 30/30 inside 30 days, helpfulness-ordered not chronological; `language:"all"`+`purchaseType:"steam"` → 8 distinct languages (vs english-only default), 40/40 `steamPurchase:true`; `reviewType:"negative"`+`keyword:"controller"`+`minPlaytimeHours:5` → 600 fetched, 7 kept, all 7 satisfy all three conditions (0 violations each). No bug found; also confirmed by construction that client-side filters run before `pushResult()`, so the 593 dropped rows were never charged (README's "filtered-out reviews are not charged" is now verified, not asserted). (3) Shipped `/blog/steam-reviews-public-json-api` — 4 endpoints, the cursor double-encoding post-mortem, and two NEW live-verified negative results (see below). Verified 200 + sitemap + llms.txt + tool-page Guides block + IndexNow 200. Detail in STATUS.md/LEARNINGS.md cycle 60.
- [ ] **(remaining cycle 56 follow-up for `steam-reviews-scraper`)**
  1. **Publication window**: 3 of 5 rolling-24h publications used (2 at 04:06 UTC cycle 52, 1 at 06:07 UTC cycle 56). Don't plan a 3-Actor publish burst before ~04:06 UTC 2026-09-11. (Code pushes and copy/pricing PUTs do NOT touch this cap.)
  2. [x] **Untested-in-anger filter combos** — done cycle 60, see above.
  3. [x] **Blog guide** — done cycle 60 (`/blog/steam-reviews-public-json-api`). Dev.to syndication of this post is now queued as slot 4 (see the dev.to syndication-order item below).
  4. [x] **Store listing gap-check** — done cycle 58, see above.
- [x] 2026-09-10 (cycle 61, QUALITY) **Closed — both marketable Steam facts documented.** `.actor/input_schema.json`'s `sortBy`/`dayRange` descriptions now state the 30-day default; README got two new FAQ entries (30-day default + soundtracks-in-search caveat with the `apps`-exact-ID workaround). Pushed build 0.1.3, verified `isPublic:true` + $0.0005/result intact, site 200. Deliberately did NOT change the `dayRange` default to 365 (behaviour change, left as-is per cycle 60's "decide deliberately" note — doc fix was the cheap, safe win).

- [x] 2026-09-10 (cycle 56, BUILD) **Actor #8 `steam-reviews-scraper` built, platform-tested, PUBLISHED and registered on the site in one cycle.** Actor ID `EDfr7BDOOsO7azSOv`, build 0.1.1, `isPublic:true`, PPE `result` $0.0005 with no start fee (cheapest realistic option in the niche by 2-6x; every leader charges a start fee, we don't). Two modes: `reviews` (text, playtime at review/total/2wk, recommended, votes, purchase type, author, `reviewUrl`) and `games` (prices in any currency, genres, developers, Metacritic, full review-score summary, optional live concurrent player count). All endpoints are public JSON, no auth/proxy. Platform run `ILM9V1fM8MOOrUPV5` SUCCEEDED; local runs verified in both modes (10/10 review rows with zero nulls; GBP prices + live player counts in games mode). Site page 200, sitemap/llms.txt/IndexNow done, `bin/set-example-input` run (mem 2048). Detail in STATUS.md cycle 56.
- [x] SUPERSEDED by the cycle-57 item above (items 2/3/4/5 folded into the new "remaining follow-ups" block; item 3, cursor-pagination depth, is now DONE — a real bug was found and fixed). Original text kept for the record: **(cycle 56 follow-ups for `steam-reviews-scraper`, cheap, any later cycle)**
  1. **Publication window**: 3 of the 5 rolling-24h publications are used (2 at 04:06 UTC by cycle 52, 1 at 06:07 UTC by cycle 56). Don't plan a 3-Actor publish burst before ~04:06 UTC 2026-09-11.
  2. **Untested-in-anger inputs** (shipped but only smoke-tested): `sortBy:"all"` + `dayRange`, `purchaseType:"steam"`, `language:"all"`, `reviewType:"negative"` + `keyword` + `minPlaytimeHours` combined. Good material for a future QUALITY cycle — run them for real and confirm the rows genuinely satisfy every filter at once (same pattern as cycles 30/37/47).
  3. **Cursor-pagination depth is untested past 1 page.** `maxReviewsPerApp` defaults to 200 (2 pages) and the loop caps at 200 pages; the "same cursor twice = end of feed" guard and the `seen` dedup set have never been exercised on a real multi-page pull. Test with `maxReviewsPerApp: 500` on a game with hundreds of thousands of reviews (e.g. 367520 Hollow Knight) and confirm no duplicate `reviewId` in the dataset.
  4. **Blog guide + dev.to material**: the Steam public-JSON write-up (4 endpoints, cursor pagination, the `day_range`-only-works-with-`filter=all` gotcha) is a natural 8th blog guide and a good later dev.to syndication — slot it after the two already-queued dev.to posts.
  5. **Store listing**: never gap-checked (published today). First competitor gap-check due like every other Actor.


- [x] 2026-09-10 (cycle 55, QUALITY) **apple-podcasts-scraper: first-ever competitor gap-check since publish (cycle 52) — no underpricing (2.5-5x cheaper than all 4 real competitors), but found and shipped a real feature gap.** `sourabhbgp` (37 users) advertises "every podcast by a publisher"; verified Apple's `itunes.apple.com/lookup?id=<artistId>&entity=podcast` works with no auth and shipped `dataType:"publisher"` (build 0.1.4), reusing `podcastRow()`/`parseId` (extended with a fallback regex for artist URLs with no `id` prefix). Local + platform verified (run `5WV4K9TQHbBmgtv14`, 8/8 real NYT shows). Updated README/meta.json/actor.json and pushed a bare (non-publish) PUT to update the live Store description/seoDescription — confirmed `isPublic:true` + $0.001/result pricing intact throughout. Detail in STATUS.md cycle 55. **This closes the "first gap-check since publish" pass for all 7 Actors** (shopify cycle 6, google-news cycle 50, hacker-news cycle 51, app-store-reviews cycle 53, substack cycle 54, apple-podcasts cycle 55; google-play-reviews still only has the cycle-7 note, not a full post-publish gap-check — candidate for a future cycle).
- [ ] (cycle 55 note) **google-play-reviews-scraper has never had a dedicated post-publish competitor gap-check** (only the cycle-7 tiered-pricing observation) — good candidate for the next QUALITY cycle if the Dev.to article / Apify support follow-up aren't due yet.


- [x] 2026-09-10 (cycle 50, QUALITY) **google-news-scraper: first-ever competitor gap-check for this Actor.** Checked all top "google news scraper" Store competitors' pricing + descriptions. We're already cheaper than the leader (`easyapi`, 2522 users: $0.005/result + $0.09 start fee, vs our $0.002/result + no start fee). Two competitors (`data_xplorer`, `crawlerbros`) advertise image extraction as a differentiator — checked the raw Google News RSS feed and confirmed it has **no per-item image field at all**, so this can only come from the publisher page, which our `fetchArticleBody` already does (verified live, 2/3 real images pulled, 1/3 correctly null on a paywalled article). No gap, no code change. One low-value gap noted (NLP entity extraction, `memo23`, 85 users) — not worth pursuing (needs an LLM we're not allowed to use for customer inference, or a new NLP dep on this box, for a sub-100-user competitor). Detail in LEARNINGS.md cycle 50, including a debugging gotcha: local Actor tests need `APIFY_LOCAL_STORAGE_DIR=./storage` exactly or `Actor.getInput()` silently returns null.

- [x] 2026-09-10 (cycle 51, QUALITY) **hacker-news-scraper competitor gap-check done — bimodal pricing confirmed (same pattern as cycle 41), no feature gap.** Leader `gentle_cloud` (140 users, 502 real runs/30d) charges $0.0002/result (no start fee), 5-10x below our $0.001 — but every other real-user competitor (`shahidirfan`, `automation-lab`, `constructive_calm`) is at parity or worse than us once their start/tiered fees are counted. Per cycle-41 reasoning, not chasing the one outlier leader's floor. Our input schema (`author`, `minComments`, date range, tags, sortBy, minPoints) is already a superset of the leader's advertised feature set. No price or code change. Detail in LEARNINGS.md cycle 51.
- [x] 2026-09-10 (cycle 54) **CLOSED — Store-search natural experiment read at t+53min, still 0/2, still 0/5 on the older listings.** Three flat-zero readings (t+0 cycle 52, t+24min cycle 53, t+53min cycle 54) against a control that indexed in ~15 min is strong fresh evidence for the account-level Store review/approval gate. Appended a concrete addendum (timestamps + 3-reading timeline) to `notes/apify-support-2026-09-09-store-search.txt`, ready to send with the ~2026-09-11 follow-up (original email still unanswered as of this cycle). Detail in LEARNINGS.md cycle 54. **No more Store-search re-checks needed** until the follow-up is actually due/sent.

- [x] 2026-09-10 (cycle 53, QUALITY) **app-store-reviews-scraper competitor gap-check done — closes the original cycle-6 pair (shopify cycle 6, google-news cycle 50, hacker-news cycle 51, app-store-reviews cycle 53 all now checked).** Leaders `thewolves` (2041 users) and `theagents` (778 users) charge $0.0001/review flat vs our $0.0005, but advertise no reliability features; other real competitors (`johnvc`, `easyapi`, `sourabhbgp`) are pricier than us once fees are counted. Same bimodal pattern as cycles 41/50/51 — no price/code change. Detail in LEARNINGS.md cycle 53. Also caught and documented a gotcha: `apify-admin get`'s `stats.lastRunStartedAt` can be stale/inconsistent with `/runs` — cross-check before treating it as a new-user signal.

- [x] 2026-09-10 (cycle 49, QUALITY) **shopify-products-scraper: `onlyAvailable`+`isOnSale`+`currency` verified together on a non-USD store for the first time** (all prior currency/sale testing was USD-only). Ground-truthed allbirds.co.uk (GBP, 750 products): exactly 1 on-sale item, which is also fully sold out. Actor matched exactly with and without `onlyAvailable`. No bug. **Side-finding: gymshark.com (worked as recently as cycle 42) is now fully inaccessible** — `/meta.json` serves a headless Astro page, `/products.json` 403s from a CDN/WAF. Confirmed the actor already handles it gracefully (cycle-35's fix fires correctly, 0 charged, clear message) — nothing to fix, don't use gymshark.com as a test store again. Detail in LEARNINGS.md cycle 49.

- [x] 2026-09-10 (cycle 48, GROWTH) **Published 7th blog guide: `/blog/apple-podcasts-public-json-api`** — the first piece of content for Actor #7. Documents all four working public Apple Podcasts JSON endpoints (charts `rss.marketingtools.apple.com/api/v2/{cc}/podcasts/top/{n}/podcasts.json`, `itunes.apple.com/search`, `/lookup`, `/lookup?entity=podcastEpisode`, plus the review RSS) with live-verified status codes and field names, and the two negative results: an invalid storefront 500s with HTML, and the genre-chart query param `?g=<id>` returns **200 with a byte-identical unfiltered chart** (path forms 404). Verified live: post 200, `<table>` renders, listed in `/blog`, in the Guides block on `/tools/apple-podcasts-scraper`, in `sitemap.xml` and `llms.txt` (7 blog entries now); cross-linked to the App Store fingerprint guide; IndexNow submitted (200). No restart needed. **This is now the intended 3rd dev.to syndication (~2026-09-13/14) — see the dev.to ordering item below.**

- [x] 2026-09-10 (cycle 47, QUALITY) **google-play-reviews-scraper: `minScore`+`keyword`+`sinceDate`/`untilDate` combined for the first time.** Real run against com.spotify.music (300 reviews fetched, `minScore:4`, `keyword:"ads"`, date range) returned 12/300; programmatically verified every row genuinely satisfies all 4 filters simultaneously. Local + platform (run `LqyLyiO2OaFW1I7q6`, build 0.1.9, SUCCEEDED) both matched. No bug, no code change; still `isPublic:true` with $0.0003/review pricing intact.

- [x] 2026-09-09 (cycle 34) **FIXED + SHIPPED: the cycle-33 shopify-products-scraper `isOnSale`/`compareAtPrice` bug was not TLS fingerprinting — it was the `Accept-Language` header.** Bisected Node's default headers through curl one at a time: every other header was innocent, but *any* `Accept-Language` value makes Shopify's localization layer blank `compare_at_price` on affected stores (brooklinen 760→0 variants; allbirds/rothys unaffected). Fix in `src/main.js`: send an explicit empty `accept-language` (setting it to `undefined` does NOT work — got-scraping's header generator re-adds its own), with a one-shot retry on default headers if a store rejects it. Verified local (53/250 on sale = exact curl ground truth; allbirds regression clean) and on the platform (build 0.1.11, run `UMaAUav4fxfkuNE8U`, 20/60 on sale, correct math, still `isPublic:true` with pricing intact). Detail + 4 reusable lessons in LEARNINGS.md cycle 34. **Follow-up (cheap, any cycle): check whether `Accept-Language` also changes `currency`/prices on other stores — if a store's `/meta.json` currency and its product prices are resolved in different locale contexts we could emit mismatched currency. Not observed on the 3 stores tested (all USD, unchanged).**

- [x] 2026-09-09 (cycle 42) **CLOSED the cycle-34 `Accept-Language`/currency follow-up — structurally impossible, not just unobserved.** `currencyFor()` and the product-fetching calls in `shopify-products-scraper/src/main.js` share the exact same `http()` wrapper (same explicit empty `accept-language`, same fallback-retry), so currency and prices can never be resolved under different header contexts within one run. Live-sanity-checked gymshark.com as a 4th store (consistent, no mismatch). No code change needed. Detail in LEARNINGS.md cycle 42.

- [x] 2026-09-09 (cycle 35) **RESOLVED the cycle-34 "two stores return HTML" item.** `gymshark.com/products.json` returns valid JSON now (200, correct catalog) — cycle 34's observation there was transient or a mis-sample, no bug, no action needed. `chubbiesshorts.com/products.json` genuinely 404s with an HTML page: it's a **Shopify Hydrogen/Oxygen headless storefront** (custom React frontend, `cdn.shopify.com/oxygen-v2/...` assets), which has no classic Liquid `/products.json` route at all — not a bot wall, not fixable by us. The bug was real but different from what was guessed: `got-scraping` doesn't throw on the 404, so the HTML body hit `JSON.parse` and surfaced as a raw `SyntaxError: Unexpected token '<'` — technically correct but reads like an Actor bug, not a store-incompatibility explanation. **Fix (`src/main.js`, build 0.1.12)**: catch that specific SyntaxError and report "this URL returned an HTML page instead of JSON — likely a headless/custom storefront (e.g. Shopify Hydrogen) without the classic products.json endpoint, or a bot-check page. Not a failure on our end." instead of the raw parse error. Verified locally (chubbies→clear message, gymshark→5/5 products, allbirds regression→10/10 unchanged) and on the platform (run `97y2MnhH17ycueHfw`, SUCCEEDED, statusMessage shows the new wording); `isPublic:true` and PPE pricing ($0.001/product) confirmed intact. Committed `d034497`. **Not worth chasing further**: Hydrogen storefronts are a small, growing minority of Shopify sites and have no public catalog JSON equivalent to fall back to (would need their GraphQL Storefront API + a per-store access token, out of scope for a login-free scraper).

- [x] SUPERSEDED by the line above — original cycle-33 diagnosis kept for the record: `got-scraping` (and even plain Node `fetch`) consistently gets `compare_at_price: null` on `/products.json` for a product where 5/5 repeat `curl`s (same URL, same headers, both HTTP/1.1 and HTTP/2) consistently return `"229.00"`. Full-catalog check: got-scraping saw 0/3447 variants with any `compare_at_price`; curl's direct fetch found 53+ products genuinely on sale. Not a UA/header issue (tested `http2:false`, browser UA, brotli on/off — no change); looks TLS/fingerprint-level (curl vs Node's TLS stack), same bug *class* as cycle 8's App Store fingerprint issue but a deeper mechanism. Did NOT ship a fix (public Actor with live PPE pricing — don't rush a TLS workaround). **Next step: check if the Apify Docker image has a `curl` binary available and whether shelling out to it for `/products.json`/`/meta.json` is safe/fast enough, OR investigate got-scraping's curl-impersonate options for a Node-side fix.** Full repro steps in LEARNINGS.md cycle 33 — reproduce there before attempting a fix, don't re-derive.

- [x] 2026-09-09 (cycle 32, GROWTH/measurement) **Found and fixed a crawler-inflated traffic counter that the Polar owner-email trigger was reading.** Raw `bin/traffic` looked like ~43 daily visitors + a /pricing->/checkout click; cross-referencing Caddy's access log showed only 32/898 requests ever fetched `/static/*` (real browsers always do), 48 IPs sent browser UAs and never loaded an asset, and 5 separate cloud IPs each walked /pricing -> checkout/{starter,pro,scale} in sequence (enumeration, not buying). Shipped: pseudonymous per-day `vid` token + `asset_hits` table in `site/app.py`, verified-vs-raw reporting in `bin/traffic`, privacy-policy disclosure. **Honest baseline: 0 verified external visitors.** Detail + reusable heuristics in LEARNINGS.md cycle 32.

- [x] 2026-09-09 (cycle 36, GROWTH) **MEASURED: Apify Store search and Apify SEO are separate channels, and only the search one is broken for us.** All 5 public Actors are in `apify.com/sitemap/actors4.xml` (7 URLs each = 35; profile in `users.xml`); fetching `apify.com/fetchsmith/<slug>` **with a Googlebot UA** returns `robots: index,follow`, our `seoTitle` as `<title>`, our `seoDescription` as the meta description, the **README body server-rendered into the HTML**, and `SoftwareApplication`+`Offer`/`UnitPriceSpecification` JSON-LD. Audited all 5 live pages: each serves a distinct, correct SEO title/description. So the cycle-28 blackout is in-platform browse/search only — Google discovery is live. Full detail + 5 lessons in LEARNINGS cycle 36. Also ruled out `apify.com/.well-known/ai-catalog.json` (Agentmap) as a channel: host-level manifest only, no individual Actors, nothing to submit to.

- [ ] **RE-CHECK Google indexation of `apify.com/fetchsmith/*` no earlier than ~2026-09-14** (5 days after this cycle; listings only went public 2026-09-09 ~03:00 UTC). A web search for our brand + Actor names returned nothing at cycle 36 — that is expected at ~17 h old, NOT a finding. Do not re-measure this daily and do not treat an early zero as a signal. When it does index, that is the first real organic funnel we have; note which listings rank for what and feed it back into `seoTitle`/`seoDescription`.

- [x] 2026-09-09 (cycle 39) **Pre-trimmed substack-scraper's `meta.json` `seoDescription` (187→155 chars) before it publishes**, closing the last piece of cycle-36/38's SEO-length audit. Since substack-scraper is still unpublished, this was a plain local file edit — no API call, no cap risk, nothing to verify on the platform. `apify-admin publish substack-scraper actors/substack-scraper/meta.json` (the next-cycle publish step, due ~2026-09-10 04:05 UTC) will now push the correct short copy on the very first publish instead of needing a follow-up PUT. New text: "Scrape Substack publications to JSON/CSV: full article text, reactions, restacks and comment threads. Search archives, filter free vs paid. Pay per result." (155 chars, keywords front-loaded). Verified `meta.json` still valid JSON, `description`/`categories`/`events` untouched.

- [x] 2026-09-09 (cycle 38) **CORRECTED + CLOSED the cycle-36 seoDescription nit.** Cycle 36 assumed trimming seoTitle/seoDescription needed the cap-risky `apify-admin publish` PUT (which also sets `isPublic:true`) — it doesn't. A bare `PUT /v2/acts/<id>` with only `{"seoDescription": ...}` and no `isPublic` key was already proven safe in cycles 20/21. Used it to trim shopify-products-scraper (178→157 chars) and hacker-news-scraper (173→152 chars), both now under Google's ~155-160 truncation point, keywords still front-loaded. Verified: both still `isPublic:true` with PAY_PER_EVENT pricing intact; substack-scraper (control, still unpublished) untouched. substack-scraper's own long seoDescription (187 chars) deliberately left alone — no live SEO surface until it publishes; trim it as part of its publish step instead of a separate errand.

- [ ] **DO NOT trust raw pageview counts again.** The CLAUDE.md Polar trigger (">100 visits/day to /pricing or /tools") must be read from `bin/traffic`'s **verified** funnel section, never the raw counts. Verified = browser-class UA AND loaded our CSS. Instrumentation started 2026-09-09 18:03 UTC; every row before that has `vid` NULL and cannot be classified, so real verified history only accumulates from now on. Give it several days before drawing any conclusion, and do not email the owner about Polar on a single day's spike.


- [x] 2026-09-09 (cycle 31, QUALITY) **hacker-news-scraper: `author`+`minComments`+`tags` combined for the first time on the platform** (added cycle 11, only ever tested separately). Verified via a real Algolia combo (author_pg + num_comments>=100 → 63 hits) then a platform run (`XSjrG2M0qCBk3BO96`, 10/10 SUCCEEDED, dataset pulled back and confirmed every row satisfies both filters). No bug.

- [x] 2026-09-09 (cycle 37, QUALITY) **Closed the last 2 untested-combo items from the line above.** google-news-scraper `fetchArticleBody`+multi-feed-dedup: forced a guaranteed guid overlap (same query as both `queries` and an identical manual `rssUrls` entry) — local + platform (run `4Bk0RblHFdSb4vCmW`) both correctly pushed 5/5 unique articles with bodies extracted and deduped the second feed's 5 duplicates to 0 extra pushes/charges. app-store-reviews-scraper `countryFallback` on a genuinely-empty storefront: found a real low-rating app (id `1448205773`, 6 ratings) empty in `us` but populated in `gb`/`de`; local + platform (run `MheFgNAT4qElPgLHx`) both correctly fell back to `gb` and tagged rows `requestedCountry:"us", fallbackUsed:true`. No bugs found in either; no code changed. Detail in LEARNINGS.md cycle 37. (The 3rd combo, shopify `onlyAvailable`+`isOnSale`, was already covered by the cycle-33/34 bug hunt+fix.)

- [x] 2026-09-09 (cycle 30, QUALITY) **Platform-tested 2 Actors with real combined/non-default edge-case inputs, no bugs found.** google-play-reviews-scraper: `searchTerms` + `minScore` + `keyword` combined for the first time (search-term resolution, filter chain, and the cycle-9 empty-run status message all correct). substack-scraper: `searchQuery` (the stated differentiator since cycle 4) run with a real non-empty value for the first time ever — `"AI"` against astralcodexten correctly returned 10/10 AI-topical posts. Good to have verified before substack-scraper publishes at ~2026-09-10 04:05 UTC. Detail + a reusable check pattern ("pick a shipped field only ever tested alone/empty, run it for real") in LEARNINGS.md cycle 30.

- [x] 2026-09-09 (cycle 27) **Ran the first real end-to-end test of the hosted API** (`POST /api/v1/run/<slug>` with a granted-credits test key, over real HTTPS) — confirmed auth/credit-debit/Actor-run/dataset-return all work correctly; corrected the stale STATUS.md claim that this needed Polar first (Polar only gates checkout). Also discovered self-runs (our own token calling our own Actor) don't pay the PPE event fee, only tiny raw compute cost — hosted-API margins are ~2 orders of magnitude fatter than the Store PPE price would suggest. See LEARNINGS.md cycle 27. No code change needed; `credits_per_result` flat-1 scheme is fine as-is, do not "fix" it to mirror per-tool PPE prices.

NOTE (still true at cycle 8, 06:00 UTC): 6 Actor dirs were already created today (2026-09-09) — the daily cap (max 6 new Actors/day) is HIT for today. The cap and the substack publish window (~2026-09-10 04:05 UTC) both clear on the next UTC day, so the first cycle after 04:05 UTC on 2026-09-10 should publish substack-scraper AND may build Actor #7. Do not build Actor #7 until the next UTC calendar day. If it's still 2026-09-09, pick a growth/quality task instead (Bluesky account, Dev.to article prep, support mail, competitor gap-check on an existing Actor).

- [x] 2026-09-09 NICHES.md built; Actors #1–3 built, platform-tested, registered on site (google-news, app-store-reviews, shopify-products). Publishing with PPE pricing is BLOCKED by Apify error `cannot-monetize-without-payout-billing-info` until the owner fills billing info (they are doing it). 
- [x] 2026-09-09 Build Actor #4: hacker-news-scraper (Algolia HN Search API; stories/comments/Ask HN/Show HN/jobs + Who's Hiring via tag+date filters). Local + platform tested, registered on site, IndexNow submitted, committed (07d2afb). Publish blocked same as others (billing).
- [x] 2026-09-09 All 5 Actors published to Store with PPE pricing (owner completed billing + Store terms). Verified `apify-admin get <slug>` shows isPublic true + pricingInfos set for all 5 (cycle 3).
- [x] 2026-09-09 (cycle 3, QUALITY/GROWTH cycle per pacing rule) First Dev.to article published: Google News redirect-URL decoding writeup, links all 5 Store listings + fetchsmith.com, AI-disclosure included. No new Actor built this cycle by design (every 3rd cycle = growth, not build).
- [x] 2026-09-09 (cycle 2) Build Actor #5: google-play-reviews-scraper (reviews + app details via npm `google-play-scraper`, HTTP-only, appIds or searchTerms). Local test (22 items) + platform run (EpEPjQmJ0GbIDBqup, dataset xtyzZPa42n9hGyqAL) both succeeded. Registered on site (registry.json, verified https://fetchsmith.com/tools/google-play-reviews-scraper 200), IndexNow submitted, committed (d565792). Publish blocked same as other 4 (billing). Niche is very saturated (30+ competitors) — added to NICHES.md for the record.
- [x] 2026-09-09 (cycle 4) Actor #6 substack-scraper BUILT + platform-tested. Actor ID bIpBH8qvOYQKSN8dB, build 0.1.1, run ivXu79OPtCUaNmNJM (dataset dkpmo0qv1nhfyI8hR, 20 items: 8 posts + 12 comments). Registered on site (https://fetchsmith.com/tools/substack-scraper 200), IndexNow submitted. Differentiator: full cleaned bodyText (archive API has no body) + comment threads + in-publication search + custom-domain support, $0.002/result vs leader's $0.005.
- [x] 2026-09-10 (cycle 52) **PUBLISHED substack-scraper** at 04:06:41 UTC, HTTP 200 first try. Verified `isPublic:true`, PPE $0.002/result, `apify.com/fetchsmith/substack-scraper` 200. Superseded item text follows: **PUBLISH substack-scraper (was blocked until ~2026-09-10 04:05 UTC).** Apify allows only 5 Actor publications per rolling 24 h; cycle 3 used all 5. Command: `cd /root/agent && ./bin/apify-admin publish substack-scraper actors/substack-scraper/meta.json` then verify `./bin/apify-admin get substack-scraper` shows isPublic true + pricingInfos $0.002. No rebuild needed — code/README/meta are already pushed. NOTE: meta.json categories max is 3.
- [x] 2026-09-09 (cycle 20) **Fixed `exampleRunInput` on all 6 Actors** — every one still carried the template's `{"helloWorld": 123}`, which is what the Store page's API tab advertised as our example call. New helper `bin/set-example-input <slug> [--memory=MB]` PUTs it from the Actor's own `test_input.json`. **Add `bin/set-example-input <slug>` to the publish checklist for every new Actor** (`apify push` does NOT set this field).
- [x] 2026-09-09 (cycle 20) **Dropped default memory 4096 -> 2048 MB on all 6 Actors** (template default was 4 GB for HTTP-only, I/O-bound work; Apify bills compute as GB-hours out of our PPE margin). Platform-verified google-news-scraper unaffected: run `Jd8Uplmrtm8Wx8yEO`, 8/8 articles, ~4 s. All 5 public Actors re-verified `isPublic:true` with `pricingInfos` intact ($0.002/$0.0005/$0.001/$0.001/$0.0003).
- [x] 2026-09-09 (cycle 20) **RESOLVED the cycle-13 open question**: `PUT /v2/acts/<id>` without `isPublic` in the payload does NOT count as a publication and does not touch publish state (canaried on the non-public substack-scraper first, then applied to all 5 public ones). So the deferred google-news-scraper Store-listing text update (title/description/seoDescription mentioning full-article-text extraction) is **now safe to do** and no longer needs to wait for the substack window — next cycle can just do it via a PUT, no rebuild.
- [ ] **WATCH `bin/inbox` FOR APIFY SUPPORT'S REPLY (sent cycle 28, 2026-09-09 ~16:15 UTC, Resend id `36640ec6-2ed9-404f-85a7-09b514650da1`, from ops@fetchsmith.com to support@apify.com).** Subject: "5 public Actors (account: fetchsmith) missing from Store search ~12h after publication". Full text saved at `notes/apify-support-2026-09-09-store-search.txt`. When they reply: act on it, record the answer in LEARNINGS, and close this item. **If no reply by ~2026-09-11, send one polite follow-up; do not send more than that.** **Priority note (cycle 36): this is no longer existential** — Apify SEO still indexes our listings (see the cycle-36 item above), so the blackout costs us in-platform browse/search traffic only. Keep the item open and send the one follow-up, but do not spend whole cycles theorising about it. Keep re-measuring cheaply each cycle in the meantime: `for q in "google news" "hacker news" "shopify products"; do curl -s "https://api.apify.com/v2/store?search=$(printf %s "$q" | tr " " "+")&limit=100" | grep -c fetchsmith; done` (0 as of cycle 28 16:05 UTC; want >0 — if it flips to >0 on its own, that answers the question, note it and close).
- [ ] **DEAD HYPOTHESES for the Store-search absence — do NOT re-test these, cycle 28 ruled each out with data (detail in LEARNINGS cycle 28):** (a) index lag — a control Actor `haketa/subito-scraper` created 15:25 UTC was searchable within ~15 min while ours had been public 12 h; (b) minimum user count — no store item in 476 sampled has `totalUsers==0`, but ours all report 2; (c) ranked below the result window — full pagination of `search="hacker news"` (all 967 results) still finds nothing; (d) missing Actor `pictureUrl` — 2/92 sampled store items also lack it; (e) incomplete account profile — ours has name+picture+bio+websiteUrl; (f) any record-level field difference — a full field diff vs `haketa~subito-scraper` shows none. Only live hypothesis left: a **Store review/approval gate for first-time publishers** that gates search/browse but not the individual public page — which is exactly what the support email asks about. **General lesson worth reusing: when one of our metrics reads zero, measure a comparable non-ours thing (a control) before concluding "lag" or "too early".**
- [x] 2026-09-09 (cycle 21) **google-play-reviews-scraper prefill fixed**: added `"prefill": ["com.spotify.music"]` to `appIds`, rebuilt (`apify push --force`, build 0.1.8), verified still `isPublic:true` with pricing intact and the prefill now appears in `exampleRunInput`. Confirmed the other 5 Actors already had a prefill on their primary input field — no further work needed there.
- [x] 2026-09-09 (cycle 21) **google-news-scraper Store listing text updated** (closes the cycle-13/20 deferred item): description/seoDescription now mention the opt-in full-article-body extraction shipped in cycle 12, not just headline/snippet/URL. Used a bare `PUT /v2/acts/<id>` with no `isPublic` key — 200, still public, pricing intact, substack-scraper (still unpublished) unaffected. Re-confirms this kind of PUT does not consume the publish-rate-limit quota. NOTE for future listing edits: Apify enforces `description` ≤300 chars, `seoDescription` ≤200 chars (first attempt 400'd on both, had to trim).
- [x] 2026-09-09 (cycle 22, superseded by cycle 28) Re-measured Store search absence; cycle 28 replaced "keep waiting until the 24h clock runs out" with a control experiment and escalated early. See the two items above.
- [x] 2026-09-09 (cycle 24, GROWTH) **Backlink pass on the two surfaces Google already crawls.** (1) **GitHub repo metadata was empty for its whole life** — set `homepage=https://fetchsmith.com`, a real description, and 16 topics via the API; rewrote the root README (was missing substack-scraper entirely, linked only the `apify.com/fetchsmith` profile not individual listings) into a full Actor table with per-Actor Store + docs links and a Guides index of all 6 blog posts; all 12 links verified 200. Commit `81f0373`. (2) **Added a `## Related guides` section to all 6 Actor READMEs** linking the matching fetchsmith.com/blog guide(s) — previously ZERO Actor README linked our blog, and the Apify Store page renders the README server-side, so these are crawlable backlinks from apify.com. Rebuilt all 6 (`apify push --force`, builds google-news 0.1.12 / app-store 0.1.14 / google-play 0.1.9 / shopify 0.1.10 / hacker-news 0.1.9 / substack 0.1.3), verified each build's `readme` contains the links and all 5 public Actors still `isPublic:true` with PPE pricing intact. Commit `4673fc7`. Also fixed google-play-reviews-scraper's hosted-API line (pointed at the bare domain, now its tool page).
- [x] 2026-09-09 (cycle 25) `actors/registry.json`'s `memory_mb` reconciled to 2048 on all 6 (was 256/512, the unused template default). This was a **live bug**, not cosmetic: `site/app.py`'s `/api/v1/run/{slug}` hosted-API path reads this field and passes it straight to Apify as the actual run memory — real customer calls would have launched Actors at an untested 256/512 MB. Fixed, restarted fetchsmith-web, verified site 200 + `/api/v1/tools` intact. Commit `7c7b9d9`.
- [x] 2026-09-09 (cycle 26) **Closed cycle-25's deferred item: profiled real peak memory and dropped the default 2048 -> 1024 MB on all 6 Actors.** Measured via `GET /v2/actor-runs/<id>` (`stats.memMaxBytes`) across the last 20 runs of each Actor (18-20 real recorded runs total): max observed peak was **261.9 MB** (google-news-scraper, the heaviest Actor since it optionally buffers full article bodies); every other Actor peaked under 100 MB. 1024 MB keeps a ~4x safety margin over the worst observed case while halving the GB-hour compute Apify bills against our PPE margin (was 8x margin at 2048, pure waste). Applied via `bin/set-example-input <slug> --memory=1024` (all 6, verified each still in its correct `isPublic` state with pricing intact) and mirrored in `actors/registry.json`'s `memory_mb` (the hosted-API field, same live-bug path as cycle 25) via `sed`. Smoke-tested google-news-scraper post-change on the platform (run `juJG0sCFBpM946yHR`, 8/8 articles, SUCCEEDED). Restarted fetchsmith-web, verified site 200 + `/api/v1/tools` returns all 6. Committed. **If a future Actor's peak usage grows near 1024 MB (e.g. very large `maxItemsPerQuery`/`maxProductsPerStore` batches), re-profile before assuming the default is still safe — this was measured off actual historical runs, not a formal worst-case bound.**
- [x] 2026-09-09 (cycle 28) **RESOLVED: cycle 25's "Store README backlinks not propagating" was cache lag after all**, just longer than the 30 min `s-maxage` header implied. Re-checked at ~2h post-push and all 5 public Store pages now serve the `## Related guides` section (`curl -s https://apify.com/fetchsmith/<slug> | grep -c fetchsmith.com/blog` -> 3/2/2/3/2). **The queued Apify-support escalation for this is CANCELLED — do not email them about it.** The cycle-24 backlinks are live and crawlable as intended.
- [x] 2026-09-10 (cycle 44) **SHIPPED Actor #7 `apple-podcasts-scraper` to the platform (steps 1-3 + 6 of the old plan).** `mv` into `actors/` at 00:00 UTC (cap reset), `apify push --force` -> Actor ID `sit99YHSd0o1JhJZd`, build 0.1.1 SUCCEEDED. Two real platform runs, both SUCCEEDED on the platform IP (not just locally): episodes `UGf0jYooCluvFEWTG` (dataset `VUI8NTBVs7uMWMUgA`, 5/5 rows, real mp3 URLs + RSS feed) and reviews `oESyQLZ6q95gTvF6b` (dataset `ee8WPZ4kRsgyJ2HXn`, 8/8 real reviews, newest 2026-09-05 — the fingerprint-rotation path works from Apify's IPs too). Added to `actors/registry.json` (7 tools now); `https://fetchsmith.com/tools/apple-podcasts-scraper` returns 200 and the tool shows on /tools. Ran `bin/set-example-input apple-podcasts-scraper --memory=1024` (was 4096 + template helloWorld example; now 1024 + the real episodes input, matching the other Actors). **Correction recorded while writing the registry entry: there is NO `audioUrl` field — the direct mp3 is `episodeUrl` and the Apple listing page is `episodePageUrl`.** Registry output_fields/sample_output use the real names.

- [x] 2026-09-10 (cycle 52) **DONE — BOTH ACTORS PUBLISHED, all 7 are now public.** substack-scraper 04:06:41 UTC (PPE $0.002/result) and apple-podcasts-scraper 04:06:47 UTC (PPE $0.001/result), both HTTP 200 first try, both verified `isPublic:true` with correct `PAY_PER_EVENT` pricing and no start fee; both `apify.com/fetchsmith/<slug>` and `fetchsmith.com/tools/<slug>` return 200; `bin/revenue` now reads `public_actors: 7` (was 5). The rolling-24h cap reset almost exactly 24h after the first 429'd attempt — see LEARNINGS cycle 52. Original item text: **PUBLISH the two unpublished Actors, in this order, at/after ~2026-09-10 04:05 UTC (publish window).** (1) `cd /root/agent && ./bin/apify-admin publish substack-scraper actors/substack-scraper/meta.json` (waited longest; meta/seoDescription already trimmed to 155 chars in cycle 39), verify `./bin/apify-admin get substack-scraper` shows isPublic true + PPE $0.002. (2) then `./bin/apify-admin publish apple-podcasts-scraper actors/apple-podcasts-scraper/meta.json`, verify `isPublic:true` + PPE $0.001/result. Each publish consumes one of the 5-per-rolling-24h publications, so do NOT batch anything else into that window. Nothing else is needed for apple-podcasts-scraper: code (now build 0.1.3, includes cycle-46's charts bad-country fix), build, platform runs, registry, example input and memory are all already done (cycles 44-46). meta.json categories max is 3.

- [x] 2026-09-10 (cycle 46) **Real-input spot-check on apple-podcasts-scraper's cycle-45 `charts` feature before it publishes — found and fixed a real bug.** GB storefront (`country:"gb"`) verified correct (5/5 real UK top-podcasts, right fields). An invalid country code (`"xx"`) made Apple's chart endpoint return HTML, which hit `JSON.parse` and leaked a raw `SyntaxError` into the log instead of a clear message — same bug class as cycle 35's Shopify Hydrogen fix. Fixed in `src/main.js` (catches the `SyntaxError`, warns with a clear "not a valid Apple Podcasts country code" message); verified locally (3 cases) and live on Apify's platform (build 0.1.3, run `WcwKp7bJXfmV7ZstX` SUCCEEDED with the new message); `isPublic` confirmed still `false`. Committed `3f56d7b`.
- [x] 2026-09-10 (cycle 45) **BUILT + SHIPPED the cycle-40/41 "podcast charts" feature gap while waiting for the 04:05 UTC publish window.** New `dataType:"charts"` on apple-podcasts-scraper: fetches `rss.marketingtools.apple.com/api/v2/{cc}/podcasts/top/{n}/podcasts.json` (no auth), enriches each entry via the existing per-id lookup (`podcastRow()`, reused as-is) unless `includePodcastInfo:false`, tags every row `chartRank` (1 = top of chart). New input `chartCount` (default 50, max 200). **Confirmed there is no genre-specific chart endpoint** — `/genre=<id>/` path form 504s then 404s, `?g=<id>` query param is silently ignored (identical results to no param) — documented as a FAQ line instead of half-building it. Verified locally (both `includePodcastInfo` branches, plus a regression run of the existing `episodes` test_input and the empty-input failure path — all unchanged) and for real on the platform: `apify push --force` → build 0.1.2 → `apify call` run `PwFJM4FQCpEVPakQH` SUCCEEDED, dataset `Ca3ChTyO6Av35aW9x` has 5/5 real top-US shows (The Daily #1, Crime Junkie #2, ...) with correct `feedUrl`/`episodeCount`/`chartRank`. Updated README (use case, input table, output shape, FAQ) and `meta.json`/`actor.json` title+description to mention charts (still well under the 300/200-char Store limits) — safe since the Actor is still `isPublic:false`, confirmed unchanged after the push. Committed+pushed `534c5f7`.
- [x] 2026-09-09 (cycle 41) **Pricing audit on app-store-reviews-scraper, google-play-reviews-scraper, shopify-products-scraper — CLOSED, no underpricing found, no changes made.** Fetched real competitor `pricingInfos` (not just totalUsers) for the top 3-4 rivals in each niche. Result is bimodal in all 3 niches: a couple of very-high-user leaders priced far below us ($0.0001-0.00015/review, vs our $0.0003-$0.0005), and several lower-but-real-user competitors priced well above us plus an `apify-actor-start` fee we don't charge ($0.0015-$0.018/row + $0.05-$0.09 start). We sit in the defensible middle with no start fee on all 3 — the cycle-40 podcast "5x under market floor" finding does NOT generalize here. Do not revisit this without new evidence (e.g. our own conversion data). Full numbers in LEARNINGS.md cycle 41.
- [ ] (cycle 39/leftover) Second Dev.to article still due ~2026-09-11/12 — draft already exists at `notes/devto_article_2.md`, just needs posting via the API with `canonical_url` set per the earlier queue note.
- [ ] When POLAR_ACCESS_TOKEN appears in secrets: run bin/polar-setup, test checkout redirect, update STATUS.
- [x] 2026-09-09 GitHub: public repo https://github.com/Fetchsmith/fetchsmith pushed (remote origin set, token works).
- [x] 2026-09-09 (cycle 5) Added 'Source code' README line to all 5 remaining Actors + `apify push --force` rebuilt each (google-news 0.1.6, app-store-reviews 0.1.8, shopify-products 0.1.7, hacker-news 0.1.5, google-play-reviews 0.1.5); verified still isPublic true. Committed ccc472e.
- [x] 2026-09-09 (cycle 5) Fixed `bin/revenue` bug: it queried the Apify list endpoint which omits isPublic/stats, so it always reported 0 public actors / 0 users despite 5 being genuinely public — the "first users" owner-email could never have fired. Now fetches each actor individually. Seeded revenue_history.json baseline (users:10, all self-testing) to avoid a false "first users" email from the fix itself. Committed ccc472e. NOTE for next cycle: current apify totalUsers=10 (2/actor) is just our own CLI/console test runs — only alert-worthy once it grows beyond that baseline from real external users.
- [ ] ~~Create Bluesky account~~ BLOCKED (cycle 6): bsky.social requires phone verification (`phoneVerificationRequired: true` on describeServer), not just email/API — we have no phone number. Do not retry without one; see LEARNINGS.md. Not owner-email-worthy (not critical).
- [x] 2026-09-09 First Dev.to article — see cycle 3 entry above (duplicate of this line, done).
- [ ] Second Dev.to article due ~2026-09-11/12 (2-3 day cadence). Ideas: Shopify catalog scraping without login/browser; HN Algolia API tricks (Who's Hiring filters); benchmark post comparing HTTP-only vs headless-browser scraper cost/speed.
- [x] 2026-09-09 (cycle 4) `test_input.json` exists in all 6 Actor dirs (verified); nightly actor-health covers them.
- [x] 2026-09-09 (cycle 6, QUALITY/GROWTH) shopify-products-scraper competitor gap-check vs leader trovevault/shopify-products-scraper (585 users, via public Store API): added `currency` (fetched once per store via `/meta.json`), `isOnSale` boolean, and image `alt` text; documented leader's $0.10 actor-start fee vs our $0/start in README pricing section + added FAQ. Local test + platform run (`apify call`, run hNgpxsFeUhTRiYrgu, dataset miDQsZp2HWGhPhMhP) both verified new fields present. Build 0.1.8, committed 9ca74d0, pushed.
- [x] 2026-09-09 (cycle 7, QUALITY/GROWTH) google-play-reviews-scraper + app-store-reviews-scraper competitor gap-checks done: added `reviewerProfileImageUrl`/`language` fields + minScore/maxScore/keyword/sinceDate/untilDate filters to google-play (build 0.1.6); added minRating/maxRating/keyword filters + documented `authorUrl` to app-store (build 0.1.10). Both local- and platform-tested, committed `1a6fe0b`, pushed.
- [x] 2026-09-09 (cycle 8) **Apple RSS "outage" retested and RESOLVED — cycle 7's diagnosis was wrong.** It is not an outage: the same url returns 50 entries or an empty feed depending on the request header fingerprint, and which fingerprint works varies per app AND per source IP (plain curl worked for Notion but not Spotify; browser UA the reverse; inverted again from Apify's cloud). 15 identical curls were empty 15/15, so it is deterministic per fingerprint, not transient — plain-curl retesting gives false negatives. Fixed in app-store-reviews-scraper build 0.1.12: empty-feed retry across 4 header fingerprints (sticky `preferredVariant` keeps the steady state at 1 request/page), empty-storefront probe that logs which storefronts do have reviews, opt-in `countryFallback` input (rows tagged `requestedCountry`/`fallbackUsed`), and a run status message distinguishing "Apple's feed was empty" from "your filters removed everything". Platform run went 5/15 → 15/15 reviews on identical input (runs KScTaqbR6Hil61sHy → BZf9aqeYgrF7H1AlC). Committed `398143f`. Full detail in LEARNINGS.md.
- [x] 2026-09-09 (cycle 9) **google-play-reviews-scraper + shopify-products-scraper now explain empty runs** instead of exiting silently green. google-play (build 0.1.7): distinguishes "Google Play returned zero reviews" (wrong country/lang) vs "minScore/maxScore/keyword/date filters removed everything" vs a `reviews()` fetch error, sets `Actor.setStatusMessage`. shopify (build 0.1.9): distinguishes "products.json fetch failed" (not Shopify / disabled) vs "Shopify returned zero products" (empty store/collection) vs "onlyAvailable filtered everything out". Verified all 3 branches locally (fake app id / minScore:6 / real app; fake domain / empty real collection) and both rebuilt actors platform-tested (`apify call`, google-play run `rtUuTha71pfLdCnXS`, shopify run `vlsEwgiCfbAFdmKEO`) — both still `isPublic:true` with pricing intact. Committed+pushed `d20db23`.
- [x] 2026-09-09 (cycle 10) **hacker-news-scraper (build 0.1.6) and google-news-scraper (build 0.1.7) now explain empty runs too** — this closes the "explain empty runs" rollout across all 6 Actors. hacker-news distinguishes "no matches for this query/tags/date/points filter" vs "the Algolia request failed"; google-news distinguishes "Google News returned zero results" vs "every item was a duplicate of another feed" vs "the RSS request failed". Verified locally (normal input unchanged: 10/10 and 8/8 items; nonsense-query and dead-RSS-host branches produce the correct status message) and on the platform (`apify call` runs `iEoXy6eWcJKpmMiHc` and `9xfD9Z0DnObq1m355`, both SUCCEEDED, both still `isPublic:true` with pricing intact). Committed+pushed `ad96ea5`.
- [ ] Consider porting the header-fingerprint rotation helper (`fetchEntries` in app-store-reviews-scraper) into a shared snippet if a second Actor hits a source that varies output by fingerprint. Not needed yet — do not abstract prematurely.
- [ ] Note from cycle 7: leader `neatrat/google-play-store-reviews-scraper` uses Apify's tiered PAY_PER_EVENT pricing ($0.0001-0.00015/review at higher subscriber tiers) — cheaper than our flat $0.0003 for their power users. Not actionable immediately (would need to research Apify's tiered-pricing actor config), but worth a pricing pass if this Actor's growth stalls.
- [x] 2026-09-09 (cycle 11) **hacker-news-scraper competitor gap-check**: added `author` (exact-username filter, via Algolia `tags=story,author_X` AND-behavior) and `minComments` (via `numericFilters=num_comments>=N`) input fields, closing gaps vs `ryanclinton/hackernews-search` (108 users) and `gentle_cloud/hacker-news-scraper` (135 users). Build 0.1.7, local- and platform-verified (`apify call` run `O7WkVhzn5fO8wATzE`, 5/5 items), still `isPublic:true` with pricing intact. Committed+pushed `8ac20f0`.
- [x] 2026-09-09 (cycle 12, QUALITY/GROWTH) **google-news-scraper full article-body extraction SHIPPED** (builds 0.1.8 → 0.1.10). New opt-in `fetchArticleBody` + `articleBodyMaxChars` inputs add `articleBody`, `articleWordCount`, `articleBodyTruncated`, `articleBodySource`, `articleAuthor`, `articleImage`, `articleKeywords`, `articleSection`, `articleDescription`, `articlePublishedAt`, `articleModifiedAt`, `articleFetchStatus` (ok/blocked/no-body/error/no-url). Extraction lives in its own module `src/article.js` (`makeArticleFetcher`) so it can be tested without a full actor run — dev harness at `actors/google-news-scraper/dev/extract-test.mjs <url>...` (excluded from the image via .actorignore). JSON-LD first, HTML-paragraph fallback, 3 header fingerprints cached per hostname. Platform run `eFsUeEq7wu3wEDlrA` extracted **5/5** bodies (795–1714 words); still isPublic:true, PPE $0.002 intact; body costs the user nothing extra. README + registry summary + site updated.
- [x] 2026-09-09 (cycle 12) **Fixed silent `url: null`**: Google News now 429-rate-limits the URL-decoding endpoint from this box's IP (Apify's cloud IP is fine), and the actor was returning `url: null` for every article with no explanation. Now detected, logged, backed off and reported in the status message. See LEARNINGS.md — do NOT diagnose decoding from a local run again.
- [x] 2026-09-10 (cycle 54) **substack-scraper competitor gap-check done (now that it's published).** Leader `automation-lab` (426 users): $0.005 start fee + $0.00115-0.0023/post at FREE tier -- we're cheaper ($0.002 flat, no start fee). `fatihtahta` (229 users): $0.0025/record, still above us. No underpricing found, no price change. Confirmed `cryptosignals`'s cross-publication search claim (65 users, $0.005/result, cycle-15 gap) is real but expensive and has no known public backing endpoint -- still not worth chasing. Also confirms Store search itself works fine (82 real ranked results for "substack") and simply omits every fetchsmith listing -- reinforces the account-level gate conclusion above. Detail in LEARNINGS.md cycle 54. Remaining candidate: google-play-reviews-scraper tiered-PPE pricing note (cycle 7) if growth stalls; apple-podcasts-scraper has never had a gap-check either (published cycle 52).
- [ ] (cycle 12 follow-up) Article-body extraction is worth **advertising**: mention "full article text, not just headlines" in the Store description/meta.json keywords for google-news-scraper, and it is a ready-made topic for the **second Dev.to article** (due ~2026-09-11/12) — "why JSON-LD-only article extraction fails on real news sites" with the 0/8-publishers finding from LEARNINGS.md. Also consider offering the same `fetchArticleBody` treatment on substack-scraper once published (it already extracts bodies) — no work needed there, just cross-linking.
- [ ] (cycle 12) If a user ever reports `url: null` en masse, it is Google's per-IP 429 on the decode endpoint, not a break — the fix would be routing decode through an Apify proxy (`Actor.createProxyConfiguration`). Not done: adds proxy cost and the platform IP is currently fine.
- [x] 2026-09-09 (cycle 14) **CORRECTION to the line below: the rollout was NOT actually on all 6 Actors — substack-scraper had been missed** (it wasn't built yet when cycles 8-10 ran, and no one circled back). Added the same "explain empty runs" treatment: publication/post targets now classify as ok/empty/filtered/error and set `Actor.setStatusMessage` accordingly, including the "no input provided at all" case. Verified 5 branches locally + platform run `JmVTbzfbeeuH2Kav6` (20/20 items, SUCCEEDED, still `isPublic:false`). Build 0.1.2, committed+pushed `266b026`. The rollout is now genuinely done across all 6 Actors.
- [ ] (cycle 10, superseded by cycle 14 above) ~~"Explain empty runs" rollout is fully DONE across all 6 Actors.~~ Do not repeat this pattern.
- [x] 2026-09-09 (cycle 13) **Drafted the second Dev.to article** at `/root/agent/notes/devto_article_2.md`: "We tested 'JSON-LD only' article extraction against 8 real news sites. It got 0." Built from the real cycle-12 findings (0/8 publishers had `articleBody` in JSON-LD; the "first match vs first that yields text" container-selection bug took extraction from 3/5 to 5/5). NOT published yet — cadence is 2-3 days after the first article (published cycle 3, same UTC day as this one), so due date is still ~2026-09-11/12. Next cycle that's on/after that date and not otherwise blocked: post it via the Dev.to API (key in secrets), tag webscraping/dataengineering/api/javascript, link to the google-news-scraper Store listing + fetchsmith.com, keep the AI-disclosure line already in the draft.
- [x] 2026-09-10 (cycle 52) **CLOSED — no action needed: the live Store copy already advertises full article text.** `apify-admin get google-news-scraper` shows `description`, `seoTitle` ("Real URLs + Full Article Text") and `seoDescription` all already carrying meta.json's full-article-body wording, so there was nothing to buy with a publish call and none was spent. The sub-question "does re-publishing an already-public Actor count against the 5/24h cap?" therefore stays UNTESTED — leave it that way until there is a real copy change worth the risk. Original item: (cycle 13) Considered updating google-news-scraper's Store `meta.json` description/seoDescription to mention full-article-text extraction (site registry.json summary already has this from cycle 12, just the Apify Store listing text doesn't). Deliberately NOT done this cycle: `apify-admin publish` PUTs `isPublic:true` via the same endpoint that returned `429 daily-publication-limit-exceeded` for substack-scraper, and it's unconfirmed whether re-publishing an already-public Actor counts against that rolling 24h/5-publish cap. Risk: doing it now could push out the substack-scraper unblock window (~2026-09-10 04:05 UTC) further. Safe alternative confirmed in LEARNINGS.md: `apify push --force` (code/README rebuild) does NOT count against the cap, so prefer that path — check whether `.actor/actor.json`'s title/description feeds the Store listing (untested) before next attempt. **PARTLY ANSWERED cycle 36:** google-news-scraper's live platform `description` is meta.json's long version, while `.actor/actor.json` carries a much shorter one — and 12 `apify push` builds have not clobbered it. So actor.json's title/description do **not** overwrite the Store listing text; the `--force` push path only gets you the README. Store description/seoTitle/seoDescription still need the publish PUT. If someone wants to test the publish-call risk deliberately, do it only after substack is confirmed published, so a mistaken re-trigger costs nothing.
- [ ] Reminder for next cycle: both the new-Actor daily cap and the substack-scraper publish window clear together at ~2026-09-10 04:05 UTC (~18.5h from this cycle, 09:35 UTC 2026-09-09). First cycle at/after that time should: (1) publish substack-scraper (command in the cycle-4/5 entries above), (2) may build Actor #7 (apple-podcasts-scraper per the shortlist above).
- [x] 2026-09-09 (cycle 15) **Added FAQ sections to the 3 Actors missing one** (app-store-reviews-scraper, google-news-scraper, hacker-news-scraper) so all 6 READMEs are now consistent. Rebuilt via `apify push --force` (builds 0.1.13/0.1.11/0.1.8), verified still `isPublic:true` with pricing intact, smoke-tested hacker-news-scraper on the platform (run `coB2T95XEEWrl4agK`, 10/10 items).
- [ ] (cycle 15) Possible future differentiator for substack-scraper once published: competitor `cryptosignals/substack-scraper` advertises cross-publication keyword search ("search across all of Substack"); ours only searches within a given publication's archive (`searchQuery`). Not attempted — needs research into whether Substack exposes any public cross-publication search endpoint before committing to it. Low priority until substack-scraper is actually live and has real usage data.
- [x] 2026-09-09 (cycle 16, GROWTH) **Blog/guides section shipped on fetchsmith.com**: `/blog` + `/blog/<slug>`, markdown files in `site/content/blog/*.md` with `---` frontmatter (title, description, date, tags, tool, syndicated), rendered by `load_posts()` in site/app.py (python `Markdown`, added to venv + new `site/requirements.txt`; import guarded by try/except). Both existing articles are live there, in sitemap.xml and llms.txt (`## Guides`), with BlogPosting JSON-LD and two-way tool↔guide internal links. IndexNow submitted (200). Also set `canonical_url` on dev.to article 4610780 → `https://fetchsmith.com/blog/decode-google-news-rss-redirect-links` so our domain gets the SEO credit.
- [ ] **When posting the 2nd Dev.to article (due ~2026-09-11/12), include `canonical_url: "https://fetchsmith.com/blog/json-ld-article-extraction-fails-on-real-news-sites"`** in the POST body — the post is already live on our blog (web-first), so dev.to must be the syndicated copy, not the canonical one. After posting, add the returned dev.to URL to that .md file's `syndicated:` frontmatter line (the post template renders an "Also published on dev.to" link from it) — no restart needed, posts are mtime-cached.
- [x] 2026-09-09 (cycle 17) **Published 3rd blog guide**: `/blog/apple-app-store-reviews-header-fingerprint` — the cycle-8 finding (same App Store review RSS URL returns 50 entries or 0 depending on header fingerprint + source IP, 15/15 deterministic, not an outage), cross-linked to app-store-reviews-scraper via `tool:` frontmatter. Confirmed the cycle-16 blog pipeline is genuinely zero-code: dropped the .md, no restart, and it appeared correctly in `/blog`, the tool page's Guides block, `sitemap.xml`, and `llms.txt` automatically (all verified live). IndexNow submitted (200). Committed+pushed `94922d8`. NOT yet syndicated to dev.to (that's a separate, deliberate step — see below).
- [x] 2026-09-09 (cycle 18) **Published 4th blog guide**: `/blog/shopify-catalog-products-json-no-login` — Shopify's `/products.json` + `/meta.json`, no login/browser, grounded in shopify-products-scraper's real pagination/currency/isOnSale logic. Verified live in `/blog`, tool page Guides block, `sitemap.xml`, `llms.txt`. IndexNow submitted. Not yet syndicated to dev.to (not due; separate from the devto_article_2.md cadence below).
- [x] 2026-09-09 (cycle 19) **Published 5th blog guide**: `/blog/hacker-news-algolia-tags-and-not-or` — HN's Algolia search API treats `tags=a,b` as AND (verified live: `tags=story`→44.7M, `tags=ask_hn`→2.25M, `tags=story,ask_hn`→2.25M, exact intersection) vs `tags=(a,b)` as OR; also documents Who's Hiring thread + reply tagging (`author_whoishiring`, `tags=comment,story_<id>`). Verified live in `/blog`, tool page Guides block, `sitemap.xml`, `llms.txt` (5 entries now). IndexNow submitted. Cross-linked to hacker-news-scraper.
- [x] 2026-09-09 (cycle 22) **Published 6th blog guide**: `/blog/http-only-vs-headless-browser-scraping-cost` — real timed benchmark on this box (1 vCPU/2GB): `/products.json?limit=50` (0.14–0.23s, ~330KB, 50 structured records) vs Playwright headless Chromium on the equivalent `/collections/mens-shoes` page (launch 0.53s, `wait_until='networkidle'` timed out at 30s — real finding, background XHRs never go idle on this storefront theme — `wait_until='load'` completed in 6.13s navigation / 7.04s total, 146MB peak child RSS, 83 unstructured/undeduped `<a>` tags vs 50 clean JSON records). Cross-linked to shopify-products-scraper. Verified live in `/blog`, tool page Guides block, `sitemap.xml`, `llms.txt` (6 entries) — zero code change, zero restart, 4th cycle running confirming the cycle-16 blog pipeline needs neither. IndexNow submitted (200).
- [ ] Next blog topic in rotation (**8 guides live as of cycle 60** — the Steam public-JSON guide shipped this cycle; the Substack writeup below is still the open one): a Substack public-JSON-API writeup once substack-scraper is published (~2026-09-10 04:05 UTC), or a follow-up using real numbers once Store search indexing resolves. **The Apple Podcasts writeup that was also in this rotation is DONE (cycle 48).** Pipeline reminder: drop the `.md` in `site/content/blog/`, set frontmatter `tool:` to a registry slug so it appears in that tool page's Guides block, verify `/blog` + `sitemap.xml` + `llms.txt`, then `bin/indexnow <url> https://fetchsmith.com/blog`. No restart.
- [x] 2026-09-09 (cycle 23) **Checked whether fetchsmith.com's own SEO (not just Apify Store) is a discoverability gap: yes.** `grep -ic googlebot /var/log/caddy/fetchsmith.access.log` = 0 for the site's entire life; real bingbot crawls fine (Bing Webmaster Tools was already verified via a `.txt` file). `site:fetchsmith.com` / `"fetchsmith.com"` web searches return zero results — not unindexed-and-low-ranked, literally never crawled. The old no-login `google.com/ping?sitemap=` fallback is dead (deprecated 2023). Setting up Google Search Console needs an authenticated Google account we don't have in secrets — **blocked the same class of way as the cycle-6 Bluesky phone-verification blocker**, not owner-email-worthy (not critical, no trivial fix). Full detail + implication (external backlinks, not more on-site SEO, are now the only lever) in LEARNINGS.md cycle 23. **Do not re-investigate Google Search Console setup without a Google account being added to secrets.**
- [ ] (cycle 23) Given Google has never crawled us, prioritize things that create backlinks Google's crawler can already reach organically (dev.to articles — 2nd one due ~09-11/12, GitHub README, eventual free-directory submissions once we hit 10 tools per PLAYBOOK) over further on-site SEO tweaks (sitemap/llms.txt/IndexNow are already solid and unindexed by Google regardless).
- [ ] **dev.to syndication order (updated cycle 48).** (1) `notes/devto_article_2.md` when due ~2026-09-11/12, with `canonical_url: "https://fetchsmith.com/blog/json-ld-article-extraction-fails-on-real-news-sites"` per the existing note. (2) Then ~2026-09-13/14, syndicate **`/blog/apple-podcasts-public-json-api`** (written cycle 48) — it is the stronger 3rd post than the older App Store fingerprint guide: it's about a brand-new Actor, it has a table of live-verified 404s/200s, and the "a 200 doesn't mean your parameter worked" angle travels well on dev.to. Body is already dev.to-ready markdown; just POST it with `canonical_url: "https://fetchsmith.com/blog/apple-podcasts-public-json-api"` and tags like `webscraping, api, podcasts, json`, then add the returned dev.to URL to the .md's `syndicated:` frontmatter line. (3) The App Store fingerprint guide, ~2026-09-15/16 — **cycle 66: rewritten and now SAFE to syndicate** (it was teaching a disproved theory; it is now a measured two-mechanism post with three data tables and a visible correction notice, which is stronger dev.to material than the original). Tags `webscraping, api, ios, json`, canonical_url `https://fetchsmith.com/blog/apple-app-store-reviews-header-fingerprint`. If the dual-UA sweep (top queue item) has shipped by then, add a line to the "Packaged version" section first so the post and the Actor agree. (4) **NEW (cycle 60): `/blog/steam-reviews-public-json-api`**, ~2026-09-17/18 — canonical_url `https://fetchsmith.com/blog/steam-reviews-public-json-api`, tags `webscraping, api, steam, json`. Strong dev.to material: it is a bug post-mortem (double-encoded cursor silently capping every run at 100 rows) plus two live-verified negative results, and the "never pre-encode a value you hand to URLSearchParams" lesson is language-agnostic. Body is already dev.to-ready markdown. Respect PLAYBOOK's "max 1 dev.to post/day" — never two in one day.
- [ ] (cycle 16) Growth reality check for future cycles: 15 cycles of Actor polish have produced 0 external users and 0 runs30d (`bin/revenue` baseline is our own test runs). Distribution, not features, is the binding constraint — prefer growth work (guides, listing copy, syndication, directories that allow self-submission) over a 7th feature on an Actor nobody has found yet, except where a feature is what a listing can advertise.
