0-DONE-h736. **[cycle 737] DONE — finished and PUBLISHED `sec-insider-trades-scraper` end-to-end, all 6 checklist steps from `0-NEW-h736` below completed and verified live.** Output schema generated (36 fields), pushed (build 0.1.1), default-input gate SUCCEEDED with 100 rows, categories kept as `BUSINESS`+`LEAD_GENERATION` (both genuine fits, no smaller category applies — `category-rank --all` can't run pre-publish, only `--facets`), published (`isPublic:true`), example input + 1024 MB memory set, `registry.json`/icon/`store-rank` TERMS/root README all added and verified 200 live. Pricing kept at $0.003/row (explicit decision, see STATUS cycle 737 — no readable competitor price to undercut against). Found 3-4 real direct competitors during the `store-rank` term probe that cycle 736's niche scoping missed (searched "sec edgar" not "insider trading") — none over the 1000-user skip threshold, logged in STATUS for future reference. Two process learnings written up (local test needs `INPUT.json` on disk, not stdin; `check-code-fields` has an ES6-shorthand false-positive on this Actor) — both in `notes/LEARNINGS.md`. Full detail in `state/STATUS.md` cycle 737.

0-DONE-h737. **[cycle 738] DONE — ran the owed fill-rate measurement on `sec-insider-trades-scraper` and found a real production-breaking bug, not just missing numbers.** Two wider platform runs (11 large-cap issuers) hit a live `DatasetClient.pushItems` schema-validation FAILURE the moment a real `exercisePrice` value appeared — build 0.1.1's schema wrongly typed it `["string","null"]` (a `bin/gen-output-schema` blind spot: the n=12 local sample had it null on every row, no prior schema to inherit from, so the tool's documented fallback guessed 'string'). This is the exact failure class that produces an Apify "Under maintenance" QA-flag email (cf. the resolved cycle-652 `scholarship-scraper` incident). Fixed by hand (`["number","null"]`), rebuilt 0.1.2, re-ran the same crashing input clean (80/80 rows). Also hardened `bin/gen-output-schema` itself to print a WARNING for any field that gets the blind string-default, so future first-time Actors surface this at generation time instead of at a real-world crash. Shipped an honest README addition with the real measured numbers (0.1.3, verified live): `exercisePrice` 45% fill on derivative rows (RSU vests are the null rest), `expirationDate`/`coFilers` both 0% in the 160-row/11-issuer sample. `check-registry-fields`/`check-meta-fields` clean. Full detail in `state/STATUS.md` cycle 738 and `notes/LEARNINGS.md`.

0-DONE-h738. **[cycle 739] DONE — fleet-wide audit for the sec-insider-trades-scraper schema-typing bug class (numeric field declared `string` in `dataset_schema.json`). No recurrence found; nothing to fix, nothing committed.** `date -u` at cycle start was 11:00Z 2026-09-24; dev.to slot 9 (00:01Z 2026-09-25) not yet due, so proceeded to the queued audit per `0-NEW-h738`'s fallback branch. Wrote a throwaway Python script (deleted after use, not committed) that: (1) grepped every Actor's `src/main.js` for fields assigned via `num()`/`Number()`/`parseFloat()`/`parseInt()`, both directly (`field: num(...)`) and indirectly (`const x = num(...)` then used later as a shorthand or renamed object property — the same indirection pattern `exercisePrice`/`shares`/`price` used in the actual bug); (2) cross-checked each found field's declared type in `.actor/dataset_schema.json`. 6 Actors had numeric-computed fields (`apple-podcasts-scraper`, `app-store-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `steam-reviews-scraper`, `substack-scraper`) — all correctly typed `number`/`integer`/`boolean` in schema, all already the fixed post-0.1.2 version for sec-insider-trades. One false positive investigated and ruled out: `fec-campaign-finance-scraper`'s `min_amount`/`max_amount` are outbound FEC API query params (`fecGet('/schedules/...', {min_amount: minAmount, ...})`), never pushed to the dataset, so absence from `dataset_schema.json` is correct, not a bug. Inbox re-checked: same 10 long-vetted ids as cycle 738 (5x dmarc, stale GOV.UK/Companies House OTP, 2 SEO/spam offers, 2 previously-resolved threads) — no new mail, nothing to answer. `bin/revenue`/`bin/traffic` re-checked: still $0 revenue, 0 orders, traffic pattern unchanged (top referrers still fetchsmith.com internal nav + one Google referral), still ~2 orders of magnitude below the Polar trigger. Services active, site `/health` 200, tree was clean at start (`17f60f7`) and stayed clean (audit was read-only, scratch script removed).

0-DONE-h739. **[cycle 740] DONE (QUALITY/GROWTH) — shipped a second dedicated `substack-scraper` guide on paywall-preview detection, and the re-measurement turned up a new fact.** `date -u` 11:30Z: dev.to slot 9 due ~2026-09-25 00:01Z, ~12.5h away — correctly NOT published. Picked `substack-scraper` (143 tool-page views, 2nd-highest of the candidate list) via `0-NEW-h735`'s method: `src/main.js:204` carried a 3-publication measurement of Substack's free-preview truncation that the existing guide (archive `body_html` always null) never covered. Re-measured live at **n=69 over 7 publications** (astralcodexten, noahpinion, garymarcus, thezvi, experimentalhistory, doomberg, bigtechnology): free posts ratio 0.976–1.402 (median 1.007, **0/42 falsely flagged** at the 0.9 threshold), paid 0.000–0.967 (median 0.337), and preview length is a **per-publication setting** (0.02–0.05 / 0.25–0.37 / 0.34–0.46). **New finding the old comment did not have: `audience` is not a usable completeness signal — 4/27 paid posts came back essentially whole (0.959–0.967)** because publications unlock posts without changing the flag; plus paid failure has 3 distinct shapes (partial body / non-null `body_html` extracting to 0 words / `body_html` null). New post `/blog/substack-paywalled-post-preview-vs-full-text` live 200, listed on `/blog`. Backlinks done in full: Actor README `## Related guides` + **rewrote the FAQ's stale 3-publication claim** with the 7-publication numbers and the `audience` caveat, root README Guides list. `apify push --force` → **build 0.1.33 SUCCEEDED**, verified via the build record's readme (20,000 B, contains the guide link, `69 posts from 7 publications`, and the `audience` caveat). No `meta.json` change = no publication slot used. IndexNow 200. Post-ship checks clean (`check-disclosure` 43+8/0, `check-blog-claims` 4+8/0 stale, `check-readme-samples` 0 drift, `check-root-readme` 0/24). Source comment at `main.js:204` rewritten with the wider measurement (ships with the next push; comment-only, no behaviour change). No new mail, `bin/revenue` 47 users/282 runs30d/`ext_bad30d` 3 unchanged, traffic still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used.

0-DONE-h740. **[cycle 741] DONE (QUALITY/GROWTH) — shipped a second dedicated `hacker-news-scraper` guide on the 1,000-hit Algolia search ceiling, with a live-verified date-slicing workaround.** `date -u` 12:00Z: dev.to slot 9 due ~2026-09-25 00:01Z, still ~12h away — the previous cycle's guess that it'd be past-due by now was wrong (cycles run faster than 1/hour); correctly NOT published. Picked `hacker-news-scraper` (151 views, highest-remaining second-guide candidate) and its named angle (`main.js` comment on the 1,000-hit ceiling, `nbHits` != retrievable count). **Live-verified the ceiling** (`query=ai` → `nbHits:1,978,072`, page 1000 returns Algolia's own pagination-limit message, not an error) **and, new beyond the existing README claim, verified the date-slicing workaround reconstructs the true total exactly but the slice width must match query heat**: 12 monthly slices of `python`/2020 (256–447/month) sum to exactly 3,995, matching the unsliced yearly `nbHits`; but `ai`/January 2025 alone is 3,368 (monthly overflows), while 4 weekly slices (560–834) each clear the ceiling. Shipped `/blog/hacker-news-1000-hit-search-ceiling` (live 200). Backlinked in full: Actor README `## Related guides` + FAQ rewritten with the measured numbers, root README Guides list. `apify push --force` → build 0.1.38 SUCCEEDED, verified via the build record's readme (18,138 B, contains the guide link + `1,978,072`). IndexNow 200. Post-ship checks clean (`check-disclosure` 44+8/0, `check-blog-claims` 4+8/0, `check-readme-samples` 0 drift, `check-root-readme` 0/24) — `check-code-fields`'s pre-existing `sec-insider-trades-scraper` false-positive flag re-appeared, unrelated to this change, already ruled a non-bug in cycle 738. Committed (2 commits) and pushed. $0 spent, 0 of 6 Actor slots used. Full detail in `state/STATUS.md` cycle 741.

0-DONE-h741. **[cycle 742] DONE (QUALITY/GROWTH) — shipped a second dedicated `nih-reporter-scraper` guide on the `search_id`-vs-`criteria` trap, live-verified in both directions.** `date -u` 12:30Z: dev.to slot 9 due ~2026-09-25 00:01Z, ~11.5h away — correctly NOT published. Picked `nih-reporter-scraper` per `0-NEW-h741`'s named angle (`main.js:186-193`'s comment: `search_id` silently drops any `criteria` sent alongside it). **Live-verified the core trap**: `{"search_id":..., "criteria":{"fiscal_years":[1900]}, "limit":5}` still returned the unchanged `total:84` — 1900 predates all NIH funding, so this proves discard-not-merge, not coincidental overlap. **Widened beyond the original comment (per the standing method note): confirmed what survives `search_id` intact** — `offset`/`limit` paginate correctly (offset=0 vs 10 → 10 distinct `appl_id`s apart) and `sort_field`/`sort_order` sort correctly with or without `search_id` set (asc `[1996,1996,1997,...]` vs desc `[2026,2026,2026,...]`, identical either way) — so the precise boundary, never stated anywhere before, is that `search_id` replaces `criteria` specifically and nothing else. Also checked the dead-link case: an invalid `search_id` throws HTTP 500 (loud), unlike the criteria case (silent). New post `/blog/nih-reporter-search-id-vs-criteria` live 200. Backlinked in full (Actor README `## Related guides`, root README Guides list). No Actor code/schema changed — this was a documentation-only trap already correctly handled by the Actor's `startUrl` logic — so no `apify push`/rebuild/publish-slot used. `check-blog-claims` (4+9/0 stale), `check-disclosure` (45+8/0 missing), `check-root-readme` (0/24 drift) all clean. IndexNow pinged 200. Committed `20366f5`, pushed. Inbox re-checked: owner's stale `scholarship-scraper` forward (`116f7cc3`) re-confirmed already-resolved (retired cycle 652, owner already notified) — no reply sent, per cycle 686/697's rule to grep history before treating a repeat forward as new. Two SEO/cold-outreach spam items and one AI-agent outreach email (`873db8ee`, capsule26.com) — all non-actionable, no reply. `bin/revenue`/`bin/traffic` unchanged, still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used. Full detail in `state/STATUS.md` cycle 742.

0-NEW-h742. **[cycle 743] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z; by cycle 743's start it is very likely due (check the actual gap, don't assume).** If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). Once slot 9 is out, slot 10 (next calendar day, max 1 dev.to post/day) is a good syndication fit for one of the recent unsyndicated posts — TMview, eu-ted deadline-fields, substack paywall-preview, HN 1,000-hit-ceiling, or this cycle's nih-reporter search_id post; pick whichever is due first. If dev.to is not due: the second-guide-per-Actor backlog still has room on `court-records-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `grants-gov-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `us-federal-awards-scraper`. **Method note (now confirmed 6x): the value is in the re-measurement finding something the original comment did NOT have — widen the sample along a new dimension rather than just re-confirming the old number.** `0-NEW-h716`'s guard-less-Actor `ext_stats30d` follow-up is still unpicked and still has zero evidence. Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger each cycle — still flat as of cycle 742.

0-STALE-h739. **[done, see 0-DONE-h739 above] [cycle 740] `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z; by cycle 740's start this is very likely PAST due, so check first and publish `notes/devto_article_8.md` before any other work if so (command in `0-NEW-h710`).** After that: the "second-guide-per-Actor" README pattern still has room on `court-records-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `substack-scraper`, `us-federal-awards-scraper`; `0-NEW-h716`'s guard-less-Actor follow-up (still zero `ext_stats30d` evidence) is also unpicked. Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger each cycle — still flat as of cycle 739.

0-STALE-h736. **[superseded, see 0-DONE-h736 above] finish and publish `sec-insider-trades-scraper` (cycle 736 built + local-tested it; steps 4b onward of PLAYBOOK "Build an Actor" are NOT done).** `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~14.5h away as of cycle 736's 09:30Z start, so it is very likely DUE by cycle 737); if due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`), then come back to this. Exact remaining checklist for the Actor, in order:
   1. `bin/gen-output-schema actors/sec-insider-trades-scraper` (needs a local test dataset — re-run the local test first, the storage dir was cleaned before commit; INPUT used was `{"issuers":["AAPL","NVDA"],"formTypes":["4"],"maxFilingsPerIssuer":3,"maxResults":25}`).
   2. `apify push --force -w 600`, then the **default-input gate**: `curl -s -X POST "https://api.apify.com/v2/acts/fetchsmith~sec-insider-trades-scraper/runs?token=$APIFY_TOKEN&timeout=300&memory=1024" -H 'Content-Type: application/json' -d '{}'` and confirm SUCCEEDED **with a non-empty dataset** (the `issuers` seed field already carries a real `default` of `["AAPL","NVDA","JPM"]`, not just a prefill — that is the cycle-96 trap and it is already handled, but VERIFY by running, not by reading the schema).
   3. `bin/category-rank --all sec-insider-trades-scraper` BEFORE `apify-admin publish` — `meta.json` currently guesses `["BUSINESS","LEAD_GENERATION"]` (FINANCE is rejected by the API, cycle 125). Category size is a real lever; size the move first.
   4. `apify-admin publish sec-insider-trades-scraper meta.json` (uses a publication slot), then `bin/set-example-input sec-insider-trades-scraper --memory=1024`.
   5. registry.json entry + `bin/gen-actor-icons` (add a glyph+accent for the slug first) + verify `https://fetchsmith.com/tools/sec-insider-trades-scraper` 200 and the icon 200.
   6. Add the slug to the `TERMS` map in `bin/store-rank` (or every rank tool silently skips it — cycle 520) and to the root `README.md` Actor table.
   **Pricing decision to sanity-check before publishing:** `result` is set to $0.003/row. Competitors scoped this cycle (see STATUS) sit around $1.8-$2 per 1k = $0.0018-$0.002 per item, so $0.003 is currently ABOVE them — either justify it on the parsed-transaction differentiation or cut to ~$0.0015 to undercut by 20-40% per PLAYBOOK step 6. Decide explicitly, do not leave it unexamined.
   **Measurement owed before any blog claim (two-cycle rule):** the local test was n=12 rows over 6 filings (AAPL+NVDA). `exercisePrice`/`expirationDate`/`coFilers` were 100% null on that sample — plausibly correct (the single derivative row was an RSU, which has no exercise price) but NOT established. Re-measure fill rates on a wider sample (>=10 issuers incl. one with a known joint filing and one with real option exercises) before any of it goes in a README claim or a guide.
   If for any reason the Actor is abandoned, say so explicitly here with the reason — do not leave it half-published.
   Unchanged background options: second-guide-per-Actor pattern still has room (`court-records-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `substack-scraper`, `us-federal-awards-scraper`); `0-NEW-h716`'s guard-less-Actor follow-up (still zero `ext_stats30d` evidence). Re-check `bin/traffic` vs the Polar trigger each cycle (tools 19 views / 7 visitors, pricing 1 this cycle — still ~2 orders of magnitude below).

0-DONE-h736. **[cycle 736] DONE (BUILD — first new Actor in a long while; 733/734/735 were all QUALITY so a BUILD was due). Built and locally verified `sec-insider-trades-scraper`, the fleet's 24th Actor and its first finance/markets dataset. NOT yet pushed or published — see `0-NEW-h736` for the exact remaining checklist.** Scoped the niche first per the pace rule: `apify-admin store "sec edgar"` / `"sec filings"` returned ~20 competitors but the largest is 97 users (free) and every single one reports `runs30d=0` — no dominant incumbent, so the >1000-user skip test does not fire. **Differentiation is real and deliberate: every competitor scoped returns filing *indexes* (10-K/10-Q/8-K lists); this Actor opens the raw ownership XML behind each Form 3/4/5 and returns one flat row per actual transaction.** Live-verified from this box before writing any code (SEC needs a declared User-Agent): `data.sec.gov/submissions/CIK0000320193.json` 200 (164 KB, 1000 filings), and the raw XML trick — `filings.recent.primaryDocument` points at the XSL-rendered HTML view (`xslF345X06/form4.xml`), and stripping that directory prefix yields the machine-readable XML at the same path (200, 9,257 B). Local test (`ACTOR_TEST_PAY_PER_EVENT=true CRAWLEE_STORAGE_DIR=./storage`) pushed **12 real rows over 6 filings, 2 issuers, zero nulls in key fields**, including a correctly-decoded `S`/`F`/`M` code mix (8/2/2), a signed `transactionValueUsd` of -474813.22 on a disposition, a carried-through 10b5-1 footnote, and 1 derivative row. **PII: the ownership XML carries the insider's street address/city/zip; the Actor never emits the address block by design** (same fail-closed rule as sam-gov's Individual-classified exclusions) — verified on the test rows, `addr leak: False`. Also carries the fleet's standard `timeoutAt`/`remainingMs` time-budget clamp. Committed `49608b2` and pushed to GitHub. Checks this cycle: 3 services active, inbox `list 10` no new mail (all 10 ids the long-vetted set, `116f7cc3` bold.org fwd resolved cycle 652), `bin/revenue` `ext_bad30d` unchanged at 3, dev.to slot 9 correctly NOT published (`date -u` 09:30Z, due ~2026-09-25 00:01Z), $0 spent. Note for the record: `state/BUDGET.md` shows Ads `$0 / not approved (no card)` — paid acquisition is blocked by the absence of a payment method, not by a pending decision, so stop re-considering it.

0-DONE-h735. **[cycle 735] DONE (QUALITY/GROWTH) — shipped a second dedicated `federal-register-scraper` guide on the Public Inspection desk's undocumented daily blackout window, plus live proof of the 1-day early-access claim.** Followed `0-NEW-h734`'s candidate list, picked `federal-register-scraper` (45-49 views, tied-highest). The existing guide covers the `count`/paging clamps and `referencedCitations`; this one covers `dataset:"publicInspection"`, a part of the schema no guide had touched. Live-verified: PI document `2026-19681` fully readable (title/agency/PDF) at 05:01 ET 2026-09-24, while `GET /documents/2026-19681.json` on the official endpoint 404s — proof of the ~1-day early-access claim. New finding not previously documented anywhere (README or blog): the desk has a daily blackout (its own `meta.pil_unavailability_message` says unavailable 12AM-8:45AM Eastern) but during it the endpoint returns **stale leftover documents, not an empty list** — verified live, 3 docs all carrying yesterday's `last_public_inspection_issue`. A naive daily cron running before 8:45am ET will misread this as either duplicate "new" items or a falsely-thin desk. New post `/blog/federal-register-public-inspection-early-access` (live, 200). Backlinked in full (Actor README `## Related guides` + rewrote "Advance notice" section with the blackout finding + root README Guides list). `apify push --force` build 0.1.19 SUCCEEDED, verified via the build record's readme (18,234 B, contains both the guide link and `pil_unavailability_message`). No `meta.json` change, no publication slot used. IndexNow 200. Post-ship checks all clean (`check-blog-claims`, `check-disclosure` 42+8/0 — new post initially MISSING, fixed by adding the standard strong-claim AI-disclosure footer, same as every other post — `check-readme-samples` 0 drift, `check-root-readme` 0/23, `check-meta-fields` 0 stale). Dev.to slot 9 checked first via `date -u` (09:00Z): due ~2026-09-25 00:01Z, ~15h away — correctly NOT published. No new mail, `ext_bad30d` unchanged at 3, traffic still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used.

0-NEW-h735. **[cycle 736] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~15h away as of cycle 735's 09:00Z start; very likely due by cycle 736 or shortly after). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). Once slot 9 is out, dev.to slot 10 is a good fit for syndicating one of the recent unsyndicated posts (TMview, eu-ted deadline-fields, or this cycle's federal-register Public Inspection post) — pick whichever is due first, max 1 dev.to post/day.** If neither dev.to slot is due yet: the second-guide-per-Actor pattern still has room — remaining candidates from `0-NEW-h733`'s list with exactly 1 dedicated guide and real tool-page traffic: `court-records-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `substack-scraper`, `us-federal-awards-scraper`. Method that worked three times now (cycles 733/734/735): grep the Actor's `src/main.js` for a comment or a rarely-used schema branch (e.g. a whole alternate `dataset`/mode) recording a real measurement never turned into a guide, then re-measure it live before shipping — cheaper than inventing a new angle from nothing, and remember to add the standard AI-disclosure footer to any new post file (`check-disclosure` will catch it if forgotten, as it did this cycle). Other non-urgent options unchanged: a fresh `apify-admin store "<niche>"` scoping pass outside Shopify/reviews (confirmed crowded, cycles 730/731); `0-NEW-h716`'s guard-less-Actor follow-up (still zero `ext_stats30d` evidence). Re-check `bin/traffic` vs the Polar trigger each cycle.

0-DONE-h734. **[cycle 734] DONE (QUALITY/GROWTH) — shipped a second dedicated `eu-ted-tenders-scraper` guide on the three-deadline-field trap.** Followed `0-NEW-h733`'s candidate list, picked `eu-ted-tenders-scraper` (49 tool-page views, tied-highest). The angle was already sitting in the Actor's own source: `pickDeadline()`'s comment recorded a single small-sample measurement (9/10 tender-field) that was never turned into a guide. Re-measured at n=50 live, split by notice type (new dimension): `cn-standard` (biddable) 48/50 `deadline-receipt-tender-date-lot`, 1/50 `deadline-date-lot`, 0/50 `deadline-receipt-expressions-date-lot`, 1/50 none; `can-standard` (already-awarded results) **0/50 carry any of the three** — the live proof for why `onlyOpenDeadlines` treats "no deadline" as closed, not unknown. New post `/blog/eu-ted-deadline-lives-in-a-different-field` (live, 200). Backlinked in full (Actor README `## Related guides` + rewrote the `deadlineDate`/`deadlineType` output-table row with the measured split + root README Guides list). `apify push --force` build 0.1.30 SUCCEEDED, verified via the build record's readme (23,312 B, contains both the guide link and `48/50`). No `meta.json` change, no publication slot used. IndexNow 200. Post-ship checks all clean (`check-blog-claims`, `check-disclosure` 41+8/0, `check-readme-samples` 0 drift, `check-root-readme` 0/23). Dev.to slot 9 checked first via `date -u` (08:30Z): due ~2026-09-25 00:01Z, ~15.5h away — correctly NOT published. No new mail, `ext_bad30d` unchanged at 3, traffic still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used.

0-NEW-h734. **[cycle 735] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~15.5h away as of cycle 734's 08:30Z start; very likely due by cycle 735 or shortly after). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). Once slot 9 is out, dev.to slot 10 is a good fit for syndicating either the TMview post (`0-NEW-h732`'s note, command there) or this cycle's new eu-ted deadline-fields post — pick whichever is due first, max 1 dev.to post/day.** If neither dev.to slot is due yet: the second-guide-per-Actor pattern still has room — remaining candidates from `0-NEW-h733`'s list with exactly 1 dedicated guide and real tool-page traffic: `court-records-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `federal-register-scraper` (45-49 views), `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `substack-scraper`, `us-federal-awards-scraper`. Method that worked twice now (cycles 733/734): grep the Actor's `src/main.js` for a comment recording a real measurement that was never turned into a guide, then re-measure it live at a larger/more-structured sample before shipping — cheaper than inventing a new angle from nothing. Other non-urgent options unchanged: a fresh `apify-admin store "<niche>"` scoping pass outside Shopify/reviews (confirmed crowded, cycles 730/731); `0-NEW-h716`'s guard-less-Actor follow-up (still zero `ext_stats30d` evidence). Re-check `bin/traffic` vs the Polar trigger each cycle.

0-DONE-h733. **[cycle 733] DONE (QUALITY/GROWTH) — shipped a second dedicated Google Play guide (histogram + hidden `aspectRatings`/`vaf_never_display` fields), the shopify-pattern follow-up `0-NEW-h732` queued.** Corrected that queue note's stale candidate list along the way: `ats-jobs-scraper` already has 3 dedicated (`tool:` frontmatter) guides, not 1 — full per-Actor dedicated-guide count is now in `state/STATUS.md` cycle 733 if a future cycle wants it without re-deriving. Picked `google-play-reviews-scraper` (1 dedicated guide, 46 tool-page views). New post `/blog/google-play-hidden-aspect-ratings-and-histogram` (live, 200) on 3 live-measured findings: (1) `histogram` sums to the public `ratings` count within 0.0001% across 4 apps spanning 36M-244M ratings; (2) `aspectRatings` (our documented unique-vs-top-3-competitors field) re-measured across 800 reviews/4 apps at 27.3% coverage, taxonomy is category-aware (Candy Crush alone: 50 distinct criteria names vs 12-19 for non-games); (3) new finding — 1.8% of criteria entries use a `vaf_never_display_*` prefix, Google's own internal "don't render this" naming, present on every one of the 4 apps tested, yet not filtered from the raw API. Backlinked in full (Actor README + root README + rewrote the existing `aspectRatings` FAQ entry), `apify push --force` build 0.1.34 SUCCEEDED, verified via the build record's readme (15,076 B, contains both the guide link and `vaf_never_display`). No `meta.json` change, no publication slot used. IndexNow 200. Post-ship checks all clean (`check-blog-claims`, `check-disclosure` 40/0, `check-readme-samples`, `check-root-readme`, `check-meta-fields`). Committed `78870c5`. Dev.to slot 9 checked first via `date -u` (08:00Z): due ~2026-09-25 00:01Z, ~16h away — correctly NOT published. No new mail, `ext_bad30d` unchanged at 3, traffic still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used.

0-NEW-h733. **[cycle 734] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~16h away as of cycle 733's 08:00Z start; very likely due by cycle 734). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). Once slot 9 is out, the next dev.to slot (10) is a good fit for syndicating the TMview post per `0-NEW-h732`'s note (PLAYBOOK prefers syndicating an existing post over writing from scratch; `bin/devto-post site/content/blog/tmview-trademark-search-api-no-key.md --canonical https://fetchsmith.com/blog/tmview-trademark-search-api-no-key --tags webscraping,api,opendata,dataengineering --publish`; max 1/day, so not the same day as slot 9).** If neither dev.to slot is due yet: the second-guide-per-Actor pattern (this cycle, cycle 732's TMview post) still has room — Actors with exactly 1 dedicated guide and real tool-page traffic, per the full count now in `state/STATUS.md` cycle 733, include `federal-register-scraper` (45-49 views), `court-records-scraper`, `eu-ted-tenders-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `substack-scraper`, `us-federal-awards-scraper` — pick one with an underexplored field or behavior (grep its `src/main.js` for something the existing post doesn't cover, the way this cycle found `histogram`/`aspectRatings` sitting unused in `mapAppDetails`/`mapReview`). Other non-urgent options unchanged: a fresh `apify-admin store "<niche>"` scoping pass outside Shopify/reviews (confirmed crowded, cycles 730/731); `0-NEW-h716`'s guard-less-Actor follow-up (still zero `ext_stats30d` evidence). Re-check `bin/traffic` vs the Polar trigger each cycle.

0-DONE-h732. **[cycle 732] DONE (QUALITY/GROWTH) — shipped a measured TMview API guide and closed the fleet's last dedicated-blog-guide gap.** Ran the 8 cheap static checks first (`check-readme-samples`/`code-fields`/`registry-fields`/`meta-fields`/`root-readme`/`disclosure`/`blog-claims`/`pricing`): all 8 clean, 0 drift — fleet hygiene is saturated, more checking has ~0 marginal value. Mapped all 38 blog posts to the 23 live Actors and found **`trademark-search-scraper` was the only live Actor with no dedicated data-source guide**; cycle 730 already measured that dedicated guides are the one growth lever with evidence (shopify-products-scraper = only Actor with real organic excess usage AND the most blog-driven tool-page traffic). Published `/blog/tmview-trademark-search-api-no-key` (live, 200) on three findings measured live this cycle: (1) **TMview resets the TCP connection when `User-Agent` is absent rather than returning 403** — 4 header variants x3 attempts, `Content-Type` alone and `+Accept` both `000`/`000`/`000` (`curl: (56) Recv failure: Connection reset by peer`), browser UA `200`/`200`/`200`; the publishable point is that with no status line, `throwHttpErrors:false` + a `statusCode` check never sees it (same class as the Actor's existing `590 UPSTREAM502` CONNECT-level case); (2) **683/683 non-null dates across a 200-row 4-office sample are anchored at exactly `T12:00:00.000Z`** — deliberate, since midnight renders as the previous calendar day west of UTC; (3) **per-office fill rates** (second measurement of the README's existing claim per the two-cycle rule): `expirationDate` 49/50 GB, 30/50 EM, 0/50 US, 0/50 DE; `seniorityClaimed` 50/50 GB+DE, 0/50 US; `oppositionDeadLine` 47/50 EM, 0/50 US; `markImageURI` 50/50 US vs 21/50 EM, with image count == `viennaCodes` count exactly on EM/GB/DE. Also documented the upstream key-casing trap `oppositionDeadLine` (capital L). **Backlinks done in full** (PLAYBOOK step 10): Actor `## Related guides`, root `README.md` Guides list, and the Actor FAQ now carries the measured fill numbers — `apify push --force` → **build 0.1.14 SUCCEEDED**, verified via `/v2/actor-builds/<id>` that the *build record's* readme (9,545 B) contains both edits (cycle 728's method). No `meta.json` change = no publication slot used. IndexNow 200. Post-ship re-checks clean (`check-blog-claims` now 4 claims 0 stale, `check-disclosure` 39 posts 0 missing). Dev.to slot 9 checked first via `date -u` (07:30Z): due ~2026-09-25 00:01Z, ~16.5h away — correctly NOT published. No new mail, `ext_bad30d` unchanged at 3, traffic still ~2 orders of magnitude below the Polar trigger. $0 spent, 0 of 6 Actor slots used.

0-NEW-h732. **[cycle 733] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~16.5h away as of cycle 732's 07:30Z start). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`).** If not yet due, the strongest candidate is **syndicating cycle 732's new TMview post to dev.to as slot 10** once slot 9 is out (PLAYBOOK line 121 explicitly prefers syndicating an existing `/blog` post over writing from scratch; use `bin/devto-post site/content/blog/tmview-trademark-search-api-no-key.md --canonical https://fetchsmith.com/blog/tmview-trademark-search-api-no-key --tags webscraping,api,opendata,dataengineering --publish`, max 1 dev.to post/day so do NOT do this on the same day as slot 9). **Blog-per-Actor coverage is now complete: all 23 live Actors have at least one dedicated guide** — do not re-derive that audit, it is recorded here. Other non-urgent options: (1) a second guide for an Actor that has only one (the shopify pattern that measurably worked was *two* posts on one Actor — `ats-jobs-scraper`, `google-play-reviews-scraper` and `federal-register-scraper` are reasonable targets with real tool-page traffic of 45-49 views each); (2) a fresh `apify-admin store "<niche>"` scoping pass outside Shopify/reviews (that space is confirmed crowded, cycles 730/731); (3) `0-NEW-h716`'s guard-less-Actor follow-up — still zero `ext_stats30d` evidence, still correctly non-urgent. The 8 static checks were all clean at cycle 732, so skip re-running the full battery for at least a few cycles. Re-check `bin/traffic` vs the Polar trigger each cycle.

0-DONE-h731. **[cycle 731] DONE (BUILD gap-check) — closed cycle 730's `lurkapi/shopify-product-reviews-scraper-api` scoping lead with a decline: DECLINE, not a virgin gap.** `apify-admin store "shopify product reviews" 15` plus unauthenticated `GET /v2/acts/<slug>` on the top 4 hits shows this niche already has 3 live, actively-used generalist auto-detect multi-widget scrapers (`lurkapi` 47u/21u30d, `mighty_monk` 53u/15u30d, `thirdwatch` 26u/3u30d — all three explicitly auto-detect Judge.me/Loox/Yotpo/Stamped/Okendo, the same set we'd cover) plus a Judge.me specialist (`memo23` 39u/14u30d) and a Yotpo specialist. No visible field or pricing edge available from the public listing (pricing unreadable unauthenticated, per cycle 727). Building a 4th clone = feature-parity cloning into an already-served niche, same verdict as cycles 389/399/578/727 on other niches. **No Actor built (0 of 6/day cap used).** Also re-confirmed `0-NEW-h716`'s guard-less-Actor follow-up: fresh `ext_stats30d` on the unclamped candidates (`us-federal-awards-scraper`, `sam-gov-opportunities-scraper`, `fec-campaign-finance-scraper`, `eu-ted-tenders-scraper`) shows 0 FAILED/TIMED-OUT/ABORTED across all 4 — still no evidence, remains non-urgent. No new mail (10 ids all long-vetted), `bin/revenue` `ext_bad30d` unchanged at 3, `bin/traffic` still ~2 orders of magnitude below the Polar trigger. Dev.to slot 9 checked first via `date -u` (07:00Z 2026-09-24): due ~2026-09-25 00:01Z, ~17h away — correctly NOT published. $0 spent.

0-NEW-h731. **[cycle 732] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~17h away as of cycle 731's 07:00Z start; very likely due by cycle 732 or shortly after). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`).** If not yet due: both leads from cycle 730/731 are now closed with no actionable follow-up (shopify-reviews niche declined as crowded; guard-less-Actor follow-up still has zero `ext_stats30d` evidence across every unclamped candidate checked so far). No BUILD-blocking item queued. Reasonable options, none urgent: (1) a genuinely fresh `apify-admin store "<niche>"` scoping pass on a niche not yet checked (pick something outside Shopify/reviews since that space is now confirmed crowded) — respect the 6-new-Actors/day cap; (2) README/on-page copy pass on an Actor not recently touched; (3) `bin/varied-test` repeat only on an Actor whose source changed recently (full rotation is otherwise covered). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

0-DONE-h730. **[cycle 730] DONE (QUALITY/GROWTH investigation) — ran `bin/real-demand` fleet-wide and chased down the one Actor with a genuine organic-usage signal: `shopify-products-scraper`, +9.0 excess runs over the 30-day baseline (24 actual vs 15.0 expected), every other Actor reads noise (±1.7). Cross-checked from every angle: `/tools/shopify-products-scraper` has 169 pageviews plus two dedicated blog posts (85+63 views) — the most blog-driven traffic of any tool page; `store-rank --attr "shopify products"` shows our worst tracked query (p105) is already inside the title-match block with 0 token span, so no title edit can move it further, only more real usage can; pricing (`$0.001/product` = `$1/1,000` via live `currentPricingInfo`) is already at the cheap end vs visible competitor pricing (`apivault_labs` advertises `$3/1K` in its own title); the niche's two dominant incumbents (633/385 users) have no visible feature edge over our field set. Verified all 3 pages 200, no field-fill defect (already confirmed non-bug cycle 729). **Conclusion: nothing actionable — the Actor is healthy and its edge is already fully captured, logged so future cycles don't re-derive this from scratch.** One data point surfaced for a future BUILD scoping pass: `lurkapi/shopify-product-reviews-scraper-api` (Shopify product *reviews*, a different niche from ours) has 47 users/21 u30d — real demand, worth an `apify-admin store` gap-check before building. No code changed. Inbox: no new mail. `bin/revenue` `ext_bad30d` unchanged at 3, per-Actor `ext_stats30d` re-checked — no new signal for `0-NEW-h716`. `bin/traffic` still ~2 orders of magnitude below the Polar trigger. Dev.to slot 9 checked first via `date -u` (06:30Z 2026-09-24): due ~2026-09-25 00:01Z, ~17.5h away — correctly NOT published. $0 spent.

0-NEW-h730. **[cycle 731] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~17.5h away as of cycle 730's 06:30Z start; likely due by cycle 731 or soon after). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`).** If not yet due: no fresh QUALITY item queued (see `0-DONE-h730` — shopify-products-scraper investigated, confirmed healthy, nothing to fix). Reasonable options, none urgent: (1) a genuine new-Actor scoping pass — `lurkapi/shopify-product-reviews-scraper-api` (Shopify product reviews, 47 users/21 u30d) is a real-demand data point surfaced this cycle, worth an `apify-admin store "shopify reviews"` gap-check before building (respect the 6-new-Actors/day cap, skip if too crowded/can't differentiate); (2) `0-NEW-h716`'s guard-less-Actor follow-up (still non-urgent, no fresh `ext_stats30d` evidence, re-confirmed again cycle 730); (3) `bin/varied-test` rotation is fully covered fleet-wide, only worth repeating on an Actor whose source changed recently. Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

0-DONE-h729. **[cycle 729] DONE (QUALITY) — closed `0-NEW-h727`'s partial-fill mine of `check-field-fill`. Sampled ~15 Actors covering ~90+ of the 124 flagged lines with live per-run data pulls (not guesses): zero new defects.** Two root causes account for everything found: **(1) the tool pools the last 3 SUCCEEDED runs' rows with no type discriminator, and several Actors emit polymorphic rows** — confirmed live on `substack-scraper` (`type:post`/`comment`/`leaderboard` share almost no fields, e.g. leaderboard rows use `name`/`publicationDescription` where post rows use `publicationName`/`description`), `apple-podcasts-scraper` (`dataType:episodes` vs `reviews` runs pooled), `google-play-reviews-scraper` (one app-details row per app vs N per-review rows), `sam-gov-opportunities-scraper`/`fec-campaign-finance-scraper` (different `dataType`/`searchMode` pooled), `us-federal-awards-scraper` (`cfdaNumbers` real-absent on `contracts` rows since CFDA doesn't apply to contracts, `opportunityScore` gated by the `includeOpportunityScore` input flag) — in every case the flagged fill count exactly matched the row count of the run/type where the field legitimately applies. **(2) genuinely sparse upstream data**, same shape the tool's docstring already names — confirmed live on `ats-jobs-scraper`'s `region` (only set when the free-text location maps to a sub-national region; "Tokyo, Japan"/"Canada" correctly have none) and `shopify-products-scraper`'s `compareAtPriceMin/Max` (only the one SKU actually on sale that day). 5 smaller single-field entries (`eu-ted-tenders-scraper`, `uk-find-a-tender-scraper` x3, `fda-recall-scraper`, `clinicaltrials-scraper`, `grants-gov-scraper`) share the identical sparse-conditional-field shape but weren't individually re-verified with a live pull — logged in LEARNINGS as low-priority to revisit only if one looks structurally odd. **This closes the check-field-fill mine started cycle 728 (both tiers now triaged, one real bug shipped, `assistURL`). Don't re-run it expecting to find more** — full writeup + the reusable "group by discriminator" tool-improvement idea in LEARNINGS. No code changed this cycle (pure investigation). Inbox: no new mail (all 10 already-vetted). `bin/revenue` `ext_bad30d` unchanged at 3. `bin/traffic` still ~2 orders of magnitude below the Polar trigger. Dev.to slot 9 checked first via `date -u` (06:00Z 2026-09-24): due ~2026-09-25 00:01Z, ~18h away — correctly NOT published. $0 spent.

0-NEW-h729. **[cycle 730] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~18h away as of cycle 729's 06:00Z start; likely due by cycle 730 or soon after). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`).** If not yet due: the `check-field-fill` mine is now fully closed (see `0-DONE-h729`) — no fresh QUALITY target queued from it. Reasonable fallbacks, none urgent: `bin/varied-test` spot-checks on Actors not touched in a while; a competitor-gap pass on an Actor not yet audited (cycles 389/399/578/727 found nothing, low expected value); `0-NEW-h716`'s guard-less-Actor time-budget follow-up (still non-urgent, no fresh `ext_stats30d` evidence); sam-gov bid documents (exhausted index-name guesses, all 400). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

0-DONE-h727. **[cycle 728] DONE (QUALITY) — mined `check-field-fill`'s 200+ flag baseline for the first time since cycle 486 and shipped the one real finding.** Every QUALITY cycle since ~486 logged that tool as "the usual informational baseline" and moved on, burying the defect class it exists for. Method that works (now in LEARNINGS): run `--threshold 0.02` (0%-fill only, ~80 lines not 264), grep each flagged field's assignment, ask **"is it mode-gated?"** — `_watch*`, `sam-gov` dataTypes fields, `court-records` PACER docket fields, `grants-gov`'s seven `isForecast ?`-gated `est*`/`fiscalYear`, `remote-jobs` `descriptionHtml` (opt-in `includeDescription`, off by default) all explain themselves instantly. **Ungated-and-still-0% across 22 Actors = exactly one field.** That field: `grants-gov-scraper`'s `assistURL` — mapping is *correct* (`detail.assistURL || null`, key really exists in `/fetchOpportunity`) but the upstream is dead: **measured 48 live opportunities across 5 keyword searches, forecast+posted → `assistURL: ""` and `assistCompatible: false` on 48/48.** It was advertised unqualified in README, `registry.json` and `dataset_schema.json`. **Shipped disclosure, not deletion** (build 0.1.29): deleting breaks row shape and trips 4 drift checks, and the value isn't wrong, it's absent — so the key still emits `null` and README + schema `description` now state the 48/48 measurement and redirect buyers to `url`/`attachments[].downloadUrl`. **Inverse of the cycle-724/725 call: invented data gets deleted, merely-absent data gets disclosed.** Verified where it renders: the act record's and version record's `.readme` both read empty; the Store renders the **build** record's — `GET /v2/acts/<id>/builds/<buildId>` `.readme` 36,419 B with the disclosure, build 0.1.29 tagged `latest`. `meta.json` untouched so no `apify-admin publish` and no publication slot. Full detail in `state/STATUS.md` cycle 728.

0-NEW-h727. **[cycle 729] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z. As of cycle 728 (05:30Z 09-24) that is ~18.5h away; by cycle 729 it may be due. If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`).** If not yet due, the highest-value queued item is the **rest of the `check-field-fill` mine** — cycle 728 only worked the 0%-fill tier (`--threshold 0.02`). The **partial-fill tier is untouched and is where the original eu-ted `deadlineDate` bug actually lived (95% null, not 100%)**: run `bin/check-field-fill --threshold 0.3` (~206-264 flags), subtract the ~80 now-explained 0% entries, and apply the same mode-gated triage to what remains. A field that is 20-70% filled with *no* mode gate is the strongest remaining defect shape in the fleet and nothing else checks it. Budget a full cycle; do not try to clear all of it — work it Actor-by-Actor and record which Actors are triaged so the next cycle resumes rather than restarts. Lower priority, unchanged: `0-NEW-h716`'s guard-less-Actor time-budget follow-up (still non-urgent, no fresh `ext_stats30d` evidence); competitor re-scans (cycles 389/399/578/727 all found nothing actionable — low expected value); sam-gov bid documents (exhausted index-name guesses, all 400). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

0-DONE-h724. **[cycle 724] DONE (QUALITY) — completed the `bin/varied-test` rotation: all 23 Actors now covered, and found+shipped a real defect.** Ran varied inputs on the last 4 never-covered Actors: `hacker-news-scraper` (queries=rust+tags=show_hn+minPoints=50+sortBy=date+postedAfter=2026-01-01 → 5/5 show_hn stories, all points>=50, date-descending), `us-federal-awards-scraper` (awardCategories=grants+agencies="Department of Energy"+placeOfPerformanceStates=CA+minAwardAmount=1000000+includeOpportunityScore → 5/5 correct on every filter, opportunityScore populated), `google-play-reviews-scraper` and `remote-jobs-scraper` (see below). **Shipped fix: `remote-jobs-scraper` build 0.1.11** — `src/main.js` fromRemoteOK hardcoded `salaryPeriod: 'yearly'` on every numeric Remote OK row, but Remote OK's API carries **no period field at all** (verified by dumping the payload's keys), so the label was invented — contradicting the file's own "we do NOT infer a period from the magnitude" comment, the README's promise that period is set "only when the posting states it", AND the adjacent Jobicy branch which reads `j.salaryPeriod || null`. It published false data: a live posting paying 30-36/hour was rendered `"$30 - $36 per year"` (1 of 18 salaried rows that day; Jobicy's feed shows hourly rows are routine). Now `null`; numbers and `salaryOnly` unaffected. Platform-verified post-build on both paths (remoteok → no period, jobicy → `yearly` AND `hourly` both still correct from the board's own field). README updated in 2 places. `check-filter-reach` clean (23 Actors, 15 filters, 0 unreachable). No new mail (same 10 vetted spam/DMARC items), site /health 200, $0 spent.

0-DONE-h724. **[cycle 725] DONE — closed both `0-NEW-h724` items (a) and (b).** (a) `google-play-reviews-scraper` build 0.1.33: schema `sort` description + new README FAQ entry warn that `sort:RATING` fills the fetch cap with 5-star reviews before `ratingFilter` runs (0-row footgun, not a defect). (b) The grep found and fixed a second real defect of the same class: `remote-jobs-scraper` hardcoded `salaryCurrency:'USD'` for every Remote OK row despite Remote OK's API carrying no currency field at all — violates the standing fleet no-inference rule (LEARNINGS cycle 605). Fixed to `null`, build 0.1.12, platform-verified (local + real `apify call` run, 18 remoteok rows now bare-digit/no-currency, 37 jobicy rows unregressed with real currency+period). `check-charges`/`check-filter-reach`/`check-readme-samples`/`check-code-fields` all clean. Committed `893760b`. Full detail in `state/STATUS.md` cycle 725.

0-DONE-h725. **[cycle 726] DONE — closed both remaining `0-NEW-h725` leads, no code changes.** (a) Widened the defect-class grep to `Period:|Currency:|Type:|Format:|Unit:|Status:|Country:|Category:|Source:|Level:` (excluding `responseType|resultType|recordType` noise) and hand-checked every hit across `ats-jobs-scraper`, `us-federal-awards-scraper`, `eu-ted-tenders-scraper`, `app-store-reviews-scraper`, `google-news-scraper` — all legitimate (endpoint-sourced literals like `atsSource`/`awardLevel`, real per-row derivations like `workplaceType`, or internal status tags, none of them invented buyer-facing data like the salaryPeriod/salaryCurrency bugs). **This defect class is fully mined out fleet-wide — don't re-run without a genuinely new pattern.** (b) Re-ran `court-records-scraper` (`query:"qualified immunity"`, `recordType:opinions`, `courts:[scotus]`, `maxResults:30`, bypassing `varied-test`'s `limit=10` cap for a wider sample) — 30/30 unique `id`/`clusterId`, the 3 repeated-`caseName` groups (`City of Tahlequah v. Bond` x3, `Hanlon v. Berger` x3, `Ashcroft v. al-Kidd` x2) are each genuinely distinct CourtListener opinion records (different dates/judges/citations within the same case), confirming cycle 722's hypothesis with real multi-sample evidence. **Closed, not a bug, no fix needed.** Full detail in `state/STATUS.md` cycle 726.

0-DONE-h726. **[cycle 727] DONE (BUILD gap-check) — ran a third-rotation competitor scan on 2 of our own Actors' niches, per cycle 578's standing recommendation (re-scan own fleet's competitors > virgin-niche hunting). No actionable gap found, no code change.** `apify-admin store "clinical trials" 15`: 12 competitors visible, all small (single-to-mid-double-digit users on the ones that are genuinely different sellers), none with a richer field set than our `clinicaltrials-scraper` (condition/intervention/sponsor/location/facility filters + 6 date windows + one-row-per-site + watch mode) from title/summary alone. `apify-admin store "trademark search" 15`: leader `hanamira/patent-trademark-search` (81 users, bundles patents, not a direct match); several single-country USPTO/EUIPO scrapers (22-48 users); our `trademark-search-scraper` already covers 70+ offices via TMview in one listing (22 fields) — broader than every single-country competitor, and the one comparable multi-office competitor (`scrapers_lat/tmview-global-trademarks-scraper`, 6 users) isn't clearly ahead on fields. Tried to pull competitor PPE pricing via `GET /v2/acts/<id>` to compare price directly — `currentPricingInfo` is `null` unauthenticated for third-party Actors (confirms the existing note at queue.md line ~342 that tiered PPE pricing isn't readable this way; not worth scraping Store HTML for a non-actionable comparison). **Conclusion reaffirms cycles 389/399/578's standing diagnosis: clone-flooded niches, no dominant incumbent, no visible feature gap — virgin/re-scan niche hunting stays low-expected-value.** No new Actor built (0 of 6/day cap used). Full detail in `state/STATUS.md` cycle 727.

0-NEW-h726. **[cycle 727] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~19h away as of this cycle). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`).** If not yet due: no BUILD-blocking item queued, and this cycle's competitor scan (see `0-DONE-h726`) found nothing actionable on 2 of 23 Actors. Reasonable fallbacks, none urgent: spot-check 1-2 more Actors' competitor sets the same way (pick ones not recently audited — most of the fleet's second-rotation competitor audits predate cycle 578, so any Actor is fair game for a "has the leader moved?" recheck); a README/on-page copy pass; `0-NEW-h716`'s guard-less-Actor time-budget follow-up (still non-urgent, no fresh `ext_stats30d` evidence). Re-check `bin/traffic` for the Polar trigger (still ~2 orders of magnitude below).

0-DONE-h723. **[cycle 723] DONE (QUALITY) — ran `bin/varied-test` with varied non-default inputs on 3 of the 6 remaining never-covered Actors from `0-NEW-h722`'s list: `clinicaltrials-scraper` (conditions="diabetes"+phases=PHASE3+sex=FEMALE+overallStatus=COMPLETED), `grants-gov-scraper` (keyword="climate resilience"+oppStatuses=posted+fundingCategories=ENV), `nih-reporter-scraper` (keyword=alzheimer+fiscalYears=2024+activityCodes=R01+minAwardAmount=500000). All 3 clean, all filters compose correctly, no defects, no code change.** One schema note logged, not a defect: `clinicaltrials-scraper`'s `conditions` input is a plain `string` (not array like `phases`/`overallStatus`) — passing an array 400s. No new mail (inbox re-confirmed, all already-vetted spam/dmarc), `bin/revenue`'s `ext_bad30d` unchanged at 3, `bin/traffic` still ~2 orders of magnitude below the Polar trigger. Full detail in `state/STATUS.md` cycle 723.**

0-NEW-h723. **[cycle 723] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`) once due. If not yet due**: 3 Actors remain never-covered by the `bin/varied-test` QUALITY rotation — `google-play-reviews-scraper`, `hacker-news-scraper` (note: functionally tested many times via `--attr` store-rank work, but never via `varied-test`'s real-input-correctness angle), `remote-jobs-scraper`, `us-federal-awards-scraper` (that's 4, pick 2-3). Otherwise: `court-records-scraper`'s cycle-722 single-sample duplicate-SCOTUS-opinion-row observation is worth a wider-sample recheck if it recurs (not urgent, speculative). `0-NEW-h716`'s guard-less-Actor time-budget follow-up remains non-urgent (no fresh `ext_stats30d` evidence). Fleet has had no true BUILD (new Actor) cycle in a while — if no QUALITY item is more urgent, consider `apify-admin store "<niche>"` scoping for a new Actor candidate per PLAYBOOK's gap-check, respecting the 6-new-Actors/day cap. Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below as of this cycle).

0-DONE-h722. **[cycle 722] DONE (QUALITY) — closed `0-NEW-h721`'s `hacker-news-scraper` title question with a documented decline: no safe title edit exists.** `--attr` confirmed `ask hn`/`show hn`/`hn jobs` are genuine (~p14-15 predicted from p55-74), but `0-NEW-h721` missed a constraint — the current title's `(HN) API` adjacency also drives a real, tight `hn api` p2 (block of 4). Any rewrite keeping `who is hiring` + `tech news api` + `HN`-adjacent-`API` + a new `Ask HN` phrase all contiguous costs >=65/63 chars if "Hacker News" (the literal product name) stays in the title. The only ways to hit 63 either strip every comma (reads as keyword-stuffed) or drop "Hacker News" entirely (bad for buyer clarity) — both rejected as not worth the trade for ~p15 long-tail gain. No file touched, no publish. This closes the `0-NEW-h720`/`0-NEW-h721` sweep: all 5 hopeless-primary-query Actors now have either a shipped win (`app-store-reviews-scraper`) or a documented decline. Also ran `bin/varied-test` on `court-records-scraper` (recordType=opinions, courts=scotus) and `fda-recall-scraper` (productTypes=drug, brandName=metformin) — both clean, correct filtering, no code change; one low-confidence single-sample observation (a duplicate SCOTUS opinion row, `City of Tahlequah v. Bond` x2) logged but not chased — plausibly CourtListener's own index, not an Actor bug. Full detail in `state/STATUS.md` cycle 722.**

0-NEW-h722. **[cycle 722] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not yet due**: no BUILD-blocking item queued and the long-tail `--attr` sweep (`0-NEW-h720`/`0-NEW-h721`) is now fully closed — every candidate either shipped or was declined with a documented reason. Options, none blocking: (1) `bin/varied-test` QUALITY rotation — remaining never-covered Actors per `0-NEW-h719`'s list: `clinicaltrials-scraper`, `fda-recall-scraper` (now done), `google-play-reviews-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `us-federal-awards-scraper`, `court-records-scraper` (now done) — pick 2-3 of the remaining 6. (2) If `court-records-scraper`'s SCOTUS-opinions duplicate-row observation (this cycle's STATUS) recurs on a fresh sample, worth a real look at CourtListener's cluster/sub-opinion structure vs the Actor's own dedup key. (3) `0-NEW-h716`'s guard-less-Actor time-budget follow-up (still non-urgent, no fresh `ext_stats30d` evidence). (4) sam-gov bid documents (low priority, exhausted index-name guesses, all 400). (5) Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below as of this cycle).

0-DONE-h721. **[cycle 721] DONE (GROWTH) — continued `0-NEW-h720`'s batch `--attr` long-tail sweep on the 5 hopeless-primary-query Actors (34 queries across `hacker-news-scraper`, `google-news-scraper`, `shopify-products-scraper`, `app-store-reviews-scraper`, `substack-scraper`); shipped the one clean, zero-risk win found: `app-store-reviews-scraper` retitled `Mac App Store Reviews Scraper – iTunes Reviews, Any Country` (59/63, was 61/63) — `mac app store reviews` moved p9→p1 (0 title-matchers better, prediction exact), existing tracked queries unaffected. Published + `apify push --force` (build 0.1.59), `check-store-meta` 0 drift, site verified. Declined 2 tempting-looking edits after checking the constraint: `google-news-scraper`'s `media monitoring` (p613→~p2 predicted) has no safe title restructure that doesn't break the already-proven `news monitoring` p4 slot; `hacker-news-scraper`'s `ask hn`/`show hn`/`hn jobs` (all genuine, p50-70s→teens predicted) ran out of the 4 chars of title slack available — queued precisely for next cycle with the constraint spelled out. Full detail in `state/STATUS.md` cycle 721.**

0-NEW-h721. **[cycle 721] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not yet due**: `hacker-news-scraper` has 3 verified `--attr` candidates (`ask hn` p64/14-better→~p15, `show hn` p74/13-better→~p14, `hn jobs` p55/14-better→~p15) but the title `Hacker News (HN) API Scraper – Who Is Hiring, Tech News API` (59/63) has only 4 chars of slack — any edit must keep `who is hiring` (currently p19, top-20) and `tech news api` (currently p1) both intact as contiguous phrases while adding at least one of the Ask/Show HN phrases contiguously. Work out the exact candidate string with `--attr` predictions BEFORE editing files, same discipline this cycle used. If no safe string fits, that's a valid conclusion too (document why, per `google-news-scraper`'s `media monitoring` case this cycle). Otherwise: standard QUALITY rotation (`bin/varied-test` on Actors not yet covered — see `0-NEW-h719`'s list), answer any new mail, re-check `bin/traffic` vs the Polar trigger (still ~2 orders of magnitude below as of this cycle).

0-DONE-h720. **[cycle 720] DONE (GROWTH/BUILD) — first full-fleet `bin/store-rank` measurement since cycle 581, then shipped the single title edit it justified. `remote-jobs-scraper` moved 3 queries into the Apify Store top 20: `jobicy` p43->p12, `working nomads` p26->p12, `arbeitnow` p55->p20.** Title `Remote Jobs Scraper – 6 Boards, Deduped Feed` -> `Remote Jobs Scraper – Jobicy, Working Nomads, Arbeitnow +3` (58/63 cap), published + `apify push --force` (build **0.1.10**, mandatory for the Algolia reindex per cycle 515), reindex confirmed ~45s later, platform-verified real run SUCCEEDED `chargedEventCounts {job: 5}`. `--attr` predicted p12/p11/p20 and was 3-for-3. Brands chosen by cycle 523's lesson 2 (thinnest blocks / fewest better storePositions), NOT by predicted-jump size; `remotive` (~p26) and `remoteok` (~p69) declined — they do not fit in 63 chars and land outside the top 20 anyway. **Fleet snapshot: top-20 on 7/23 primary queries** (8/22 at cycle 581), `storePosition` better on 19/23. **`--attr` verified the other 3 best-looking candidates are NOT actionable** — `grants.gov` (p62), `usaspending` (p66) and `hacker news` (p193) are all already inside their title block with token span 0, so only real usage can move them. Collateral fix: `bin/check-store-meta` (unrun since before cycle 709) surfaced **pre-existing** drift on `sam-gov-opportunities-scraper` — stale local `.actor/actor.json` description (predated the cycle-709 exclusions ship) and a 73-char `registry.json` title that was over the 63-char publish cap; resolved with `--pull`, check now 0 drift. All standing checks clean, site curl-verified. Full detail in `state/STATUS.md` cycle 720.

0-NEW-h720. **[cycle 720] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` before any other work (exact command in `0-NEW-h710`).** Then the best-evidenced open item is the follow-up this cycle scoped: **batch-`--attr` the UNTRACKED long-tail/alias queries of the Actors whose primary query is hopeless** — `hacker-news-scraper` (p193/'hacker news'), `google-news-scraper` (p141), `shopify-products-scraper` (p105), `app-store-reviews-scraper` (p104), `substack-scraper` (p99). Method that worked this cycle, in order: (a) `bin/store-rank <slug>` for current tracked terms, (b) brainstorm 8-12 plausible buyer phrasings incl. competitor/product brand names the Actor genuinely covers, (c) `bin/store-rank --attr "<q>" <slug>` on each and **keep only the ones where `query in our title: False`** — a span-0 in-block result is unactionable, which is how 3 of this cycle's 4 candidates died, (d) rank survivors by *how many title-matchers still have a better storePosition* (cycle 523 lesson 2), not by predicted jump, (e) fit the winners into 63 chars keeping the head words that hold the primary query, (f) publish + `apify push --force` + re-measure after ~45s. Do NOT put a brand in a title the Actor does not actually cover (cycle 523 declined `steamspy` on exactly that ground). Two smaller notes: `himalayas` on `remote-jobs-scraper` read p471 pre-edit and p69 post-edit with no title involvement — treat the p471 as a bad read, not a win, and don't cite it. Re-check `bin/traffic` vs the Polar trigger each cycle (pricing 4/2, checkout 1/1, api 0 as of this cycle — still ~2 orders of magnitude below). Older fallbacks unchanged: `0-NEW-h716`'s guard-less-Actor follow-up (still no `ext_stats30d` evidence), sam-gov bid documents (exhausted index-name guesses, all 400), `bin/varied-test` on the 10 Actors `0-NEW-h719` lists as never covered by that rotation.

0-DONE-h719. **[cycle 719] DONE — ran `bin/varied-test` on 2 Actors not covered by the recent clamp/sam-gov rotations (picked via lowest queue-mention count as a staleness proxy): `federal-register-scraper` (documentTypes=RULE + significantOnly) and `eu-ted-tenders-scraper` (cpvCodes=72xxxxxx + onlyOpenDeadlines/minDaysUntilDeadline=14). Both clean, no defects, no code change.** `federal-register-scraper`: 10/10 rows correctly typed `Rule`+`significant:true`, `agencyNames`/`cfrReferences` populated. `eu-ted-tenders-scraper`: 10/10 rows carry a 72xxxxxx CPV code and `daysUntilDeadline` >= 14 (min 15) — both filters compose correctly. `bin/revenue`'s `ext_stats30d` re-checked first: only the 2 known/expected bad-run cases (`google-news-scraper` stale pre-fix TIMED-OUT, `scholarship-scraper` retired bold.org FAILED x2) — no new signal. `bin/traffic` still ~2 orders of magnitude below Polar trigger. Dev.to slot 9 checked first, still not due (~23h away at cycle start). Inbox re-confirmed no new/actionable mail (owner-fwd `116f7cc3` re-checked against the resolved-cycle-652 record, no re-reply). All 5 standing checks clean fleet-wide. Full detail in `state/STATUS.md` cycle 719.

0-NEW-h719. **[cycle 719] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z. Publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` (exact command in `0-NEW-h710`) the moment it's due, before any other work. If not yet due**: the varied-test rotation has now covered 8 Actors across cycles 711/717/718/719, zero defects found. Remaining Actors never confirmed by this specific rotation: `app-store-reviews-scraper`, `clinicaltrials-scraper`, `court-records-scraper`, `fda-recall-scraper`, `google-play-reviews-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `us-federal-awards-scraper` — good candidates for the next QUALITY cycle if no fresher signal exists. Otherwise fall back to `0-NEW-h716`'s guard-less-Actor follow-up (non-urgent, still no `ext_stats30d` evidence justifying it as of this cycle), bid documents on `sam-gov-opportunities-scraper` (low priority, exhausted guesses, all 400), or a competitor-gap pass on an Actor not yet audited. Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below threshold as of this cycle).

0-DONE-h718. **[cycle 718] DONE — ran `bin/varied-test` on the 2 Actors `0-NEW-h717` flagged as not covered by cycles 711/717: `apple-podcasts-scraper` (reviews mode, country=gb + minRating=4) and `sam-gov-opportunities-scraper` (exclusions with organizationId filter + PII-safe classification check, assistance-listings with keyword search). Both clean, no defects, no code change.** `apple-podcasts-scraper`: 9/9 review rows correctly filtered by rating>=4 and country=gb, real review content pulled. `sam-gov-opportunities-scraper` exclusions: 10/10 rows correctly filtered to organizationId=100013311 (Treasury/OFAC), zero Individual-classified rows (live re-confirmation the mandatory PII filter from cycle 709 still holds end-to-end). assistance-listings: keyword="flood" returned 10/10 real flood-related CFDA programs. `bin/revenue`'s `ext_stats30d` re-checked first: only the 2 known/expected bad-run cases (`google-news-scraper` stale pre-fix TIMED-OUT, `scholarship-scraper` retired bold.org FAILED x2) — no new signal. `bin/traffic` still ~2 orders of magnitude below Polar trigger. Dev.to slot 9 checked first, still not due (~23.5h away at cycle start). Full detail in `state/STATUS.md` cycle 718.

0-NEW-h718. **[cycle 718] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z. Publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` (exact command in `0-NEW-h710`) the moment it's due, before any other work. If not yet due**: the varied-test rotation has now covered every Actor touched by the recent clamp series and the newer sam-gov dataTypes (6 Actors across cycles 711/717/718), zero defects found — no fresh QUALITY target queued. Fall back to `0-NEW-h716`'s guard-less-Actor follow-up (non-urgent), bid documents on `sam-gov-opportunities-scraper` (low priority, exhausted guesses, all 400), or a competitor-gap pass on an Actor not yet audited. Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below threshold as of this cycle).

0-DONE-h717. **[cycle 717] DONE — ran `bin/varied-test` with varied non-default inputs on 4 Actors not recently spot-checked: `google-news-scraper` (siteFilter+timePeriod), `substack-scraper` (discoverCategories leaderboard mode), `steam-reviews-scraper` (reviewType negative + purchaseType steam), `fec-campaign-finance-scraper` (donorEmployer contributions). All 4 clean, no defects, no code change.** Notably confirms no regression in `google-news-scraper`/`substack-scraper` from the just-finished 5-Actor time-budget-clamp series (cycles 712-716). One non-actionable observation: `substack-scraper`'s leaderboard mode returned `publicationSubscriberCount:null` for 1 of 5 publications (ByteByteGo, a custom-domain pub) despite 4/5 others returning real counts — likely a per-publication `window._preloads` data-availability quirk on Substack's side, not queued as a fix (too speculative on one sample). `bin/revenue`'s `ext_stats30d` re-checked first per cycle 712's method: `google-news-scraper`'s one `TIMED-OUT` is the known pre-fix run (will age out of the 30-day window on its own), `scholarship-scraper` FAILED 2 is the known retired/bold.org case — no new failure signal. Full detail in `state/STATUS.md` cycle 717.

0-NEW-h717. **[cycle 717] Next cycle: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (previous post 2026-09-23T00:01Z, ~2 day cadence). Publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` (command in `0-NEW-h710`) the moment it's due, before any other work. If not yet due**: no BUILD-blocking item queued — options are `0-NEW-h716`'s guard-less-Actor follow-up (non-urgent, no user-visible evidence yet), bid documents on `sam-gov-opportunities-scraper` (low priority, exhausted guesses), a competitor-gap pass on an Actor not yet audited, or another `bin/varied-test` rotation on Actors not covered by cycle 711/717 (e.g. `apple-podcasts-scraper`, `sam-gov-opportunities-scraper`'s newer dataTypes). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below threshold as of this cycle).

0-DONE-h712d. **[cycle 716] DONE — `shopify-products-scraper` (4 of 4) ported, build 0.1.59 live, committed `254b347`. THE SERIES IS COMPLETE: 5/5 Actors clamped, 0 suspect fleet-wide.** Worst arithmetic of the set (`timeout.request: 40000` x got `retry.limit: 2` x an outer 3-attempt loop = up to ~360s handed off by a guard promising a 45s margin). Added `remainingMs()`/`MIN_REQUEST_MS`, refactored `timeBudgetOk()` onto it, clamped `timeout.request` + `retry.limit` per call, refuse a call with <3s left, clamped the inter-attempt backoff. Also clamped the second loop the queue flagged (`fetchProductsPage`'s `for (let attempt = 0; ; attempt++)`): it IS bounded by `attempt >= EMPTY_PAGE_RETRIES`, so it needed no new exit, but its 2s/4s sleeps + 2 extra requests could still run past the deadline — it now skips the re-confirm and returns the honest zero when the clock is short. This Actor already had `timeBudgetExceeded` wired into its status message (unlike `ats-jobs-scraper`), so no status-message work. `check-fail-ordering` has no line-keyed entry for this Actor. Verified 3 ways per cycles 712-715: local baseline unchanged (10 products, allbirds); standalone clamp-logic script against a **direct** black-holed socket (c715's proxy pitfall heeded) at a simulated 53s deadline — new code threw at 8.02s with `timeBudgetExceeded=true`, old code still blocked when force-killed at 75s (67s past where the new code already quit); platform run at `?timeout=90` SUCCEEDED, `chargedEventCounts {result:10}`. All 5 standing checks clean. No field/schema/registry/pricing change, no metadata republish needed.

0-NEW-h716. **[cycle 716] Follow-up from the completed clamp sweep — NOT urgent, no known user impact, do not treat as a BUILD blocker.** The sweep's verifier is now one line and green: `for f in $(grep -l timeoutAt actors/*/src/*.js); do grep -q remainingMs "$f" || echo "SUSPECT $f"; done` (5 CLAMPED / 0 SUSPECT). Run it after touching any Actor's request helper. **The residual risk is the other ~18 Actors, which have no `timeoutAt` guard at all** — not safe, just unmeasured: a guard-less Actor cannot stop early, so a slow upstream gives a paying user the same TIMED-OUT-with-nothing-returned outcome. Do NOT sweep them uniformly (most are single-request Actors where a guard would be dead code). If this is ever picked up: rank them by how much strictly-sequential per-item work one run can queue (stores x pages, companies x boards, queries x items), take only the top 2-3, and check `bin/revenue`'s per-Actor `ext_stats30d` first — a real TIMED-OUT there is the only evidence that justifies the work (that is exactly how cycle 712 found `google-news-scraper`).

0-NEW-h716b. **[cycle 716] Next cycle is due a QUALITY/GROWTH cycle** — 712/713/714/715/716 were all BUILD (five in a row; the clamp series ran long), so the every-3rd-cycle rule is overdue and there is now NO build-blocking item queued. Concrete options, in order: (1) **dev.to slot 9 / blog post — due ~2026-09-25, check `date -u`**; the canonical on-site post is already live (cycle 710) and the dev.to draft is prepped, so this may be publish-only — see `0-NEW-h708b` for the angle and the verified numbers, and re-verify every claim with `bin/check-blog-claims` first. (2) `bin/varied-test` on 2-3 Actors not recently spot-checked. (3) competitor-gap pass on an Actor not yet audited. (4) `bin/traffic` vs the Polar trigger. (5) bid documents on `sam-gov-opportunities-scraper` remains the one unshipped piece of cycle 703's audit (no index name found, all guesses 400, low priority).

0-DONE-h712c. **[cycle 715] DONE — `ats-jobs-scraper` (3 of 4) ported, build 0.1.47 live, committed `4fcb678`.** Same fix shape as `google-news-scraper`/`apple-podcasts-scraper` (outer 3-attempt `requestWithRetry()` loop): added `remainingMs()`/`MIN_REQUEST_MS`, clamped `timeout.request`+`retry.limit` per call, refuse a call with <3s left, clamped the inter-attempt backoff. Also added the `timeBudgetExceeded` flag this Actor never had (queue asked for it) and wired it into `Actor.setStatusMessage()` so an early stop is explained instead of silent. Verified: local baseline unchanged (52 postings/3 companies); standalone clamp-logic script against a real black-holed socket (53s simulated deadline) — new code threw at 8.02s, old code still blocked when force-killed externally at 75s (22s past deadline); platform run at `?timeout=90` SUCCEEDED, `chargedEventCounts {job:50}`. **New pitfall found and logged to LEARNINGS**: this Actor DOES have a `proxyConfiguration` input (unlike apple-podcasts-scraper), so the first test attempt routed the black hole through a local proxy — that hangs indefinitely regardless of the clamp, because got's `timeout.request` doesn't bound proxy CONNECT-tunnel establishment. Switched to a direct (no-proxy) black-holed socket, which is the correct test for a stalled-target bug anyway. `check-fail-ordering` has no line-keyed entry for this Actor, nothing to update there. No field/schema/registry/pricing change, no metadata republish needed. **Only `shopify-products-scraper` (4 of 4) remains — see `0-NEW-h712` below for its exact line numbers (re-grep fresh, they will have shifted) and the two-loop note (a second unbounded `for (let attempt = 0; ; attempt++)` around old L322 needs its own read, may need its own exit).**

0-DONE-h712b. **[cycle 714] DONE — `apple-podcasts-scraper` (2 of 4) ported, build 0.1.43 live, committed `4dd3295`.** This Actor's `requestWithRetry()` has its own outer 3-attempt connection-retry loop (like `google-news-scraper`, unlike `substack-scraper`), so copied the fuller google-news-scraper diff shape: `remainingMs()`, per-call clamp of `timeout.request` + `retry.limit`, refuse a call with <3s left, clamp the inter-attempt backoff. Also clamped a second unclamped `setTimeout` in the same request path — `EMPTY_RECONFIRM_DELAYS_MS`'s 2s/4s backoff before re-checking an empty first review page. Verified 3 ways: local baseline unchanged (5 episodes); since this Actor has no proxy-configuration input to route a black-hole test through (unlike google-news-scraper), extracted the byte-identical clamp logic into a standalone script against a real black-holed socket with a simulated 53s deadline — new code threw at 8.02s, old code still blocked when killed externally at 65s; platform run at `?timeout=90` SUCCEEDED, `chargedEventCounts {result:5}`. Standing checks clean after fixing one collateral issue: adding lines shifted `check-fail-ordering`'s line-keyed allowlist entry for this Actor from 1003 to 1024 (false SUSPECT until updated — same class of trap to watch for on the next two Actors). No field/schema/registry/pricing change, no metadata republish needed.

0-DONE-h712a. **[cycle 713] DONE — `substack-scraper` (worst of the four) ported, build 0.1.32 live, committed `f65be95`.** Added `remainingMs()`, clamped `getJson()` and `fetchPublicationInfo()`'s `gotScraping` calls (`perRequest = max(3000, min(wanted, left))`, `retryLimit = max(0, min(wanted, floor(left/perRequest)-1))`), refuse a call with <3s left. Verified 3 ways: local baseline unchanged (17 items); black-holed-socket test (`ACTOR_TIMEOUT_AT` now+53s) — new code exited at 8s, old code (`git stash`) still hung when force-killed by an external `timeout 75` at 75s (22s+ past where the new code already quit, same shape as cycle 712's proof); platform run at `?timeout=90` SUCCEEDED, `chargedEventCounts {result:2, comment:18}`. Standing checks clean (`check-charges`/`check-fail-ordering`/`check-code-fields`/`check-source-bytes`/`check-filter-reach`). No field/schema/registry/pricing change, so no metadata republish needed (same call cycle 712 made for google-news-scraper).

0-CLOSED-h712. **[CLOSED cycle 716 — all 4 ported, 5/5 clamped fleet-wide; kept for the test recipe only, nothing left to do here]** **[cycle 712, 2 of 4 done] was TOP BUILD ITEM — port the same request-clamp fix to the remaining 2 Actors.** Cycle 712 fixed `google-news-scraper` (build 0.1.40, `8c1fa50`); cycle 713 fixed `substack-scraper` (build 0.1.32, `f65be95`); cycle 714 fixed `apple-podcasts-scraper` (build 0.1.43, `4dd3295`, see `0-DONE-h712b` above). Root cause, written up in LEARNINGS: the `timeoutAt`-minus-45s guard is only checked between feeds/items, so it can pass with 45s left and then hand control to a single item worth minutes (got's own `retry.limit` x a 25-45s per-attempt `timeout.request`) and still get hard-killed. Remaining 2 — **re-grep line numbers fresh before editing, do not trust the numbers below** (cycle 714 found `apple-podcasts-scraper`'s had already shifted enough to trip `check-fail-ordering`'s allowlist):
   - `actors/ats-jobs-scraper/src/main.js` — guard around old L263-267, `requestWithRetry` around old L305-310, callers at old L320/532/556/728 all pass `30000` + `limit 2`. Note: this one never even sets a `timeBudgetExceeded` flag, so the status message can't explain the early stop — worth adding in the same pass. Check `check-fail-ordering`'s allowlist for a line-keyed entry on this Actor before committing; if the fix shifts a `Actor.fail()`/`saveWatchRecord()` line the allowlist references, update the tuple (see cycle 714's `apple-podcasts-scraper` fix for the exact shape of that follow-up edit).
   - `actors/shopify-products-scraper/src/main.js` — guard around old L234-239, retry loop old L270-275, old L272 `40000` + `limit 2`; also a second unbounded `for (let attempt = 0; ; attempt++)` loop around old L322 — read that one before clamping, it may need its own exit. Same allowlist-line-shift caution as above.
   - **Test each the way cycles 712/713/714 did**: local baseline unchanged, a real black-hole-socket test proving the new code exits at deadline-minus-margin while the old code overruns it (use the Actor's own proxy-configuration input if it has one, like google-news-scraper; otherwise extract the exact clamp logic into a standalone script against 127.0.0.1 the way cycle 714 did for apple-podcasts-scraper, since not every Actor has a proxy input to route through), then one real platform run at a deliberately tight `?timeout=90` confirming SUCCEEDED + non-zero `chargedEventCounts`. Do not edit `/etc/hosts` for this — cycle 714 found port 443 already bound by `caddy` and had to revert immediately; the standalone-script approach avoids touching system config entirely.
   - Also still worth considering: a `bin/check-time-budget` standing check flagging any `actors/*/src/*.js` that references `timeoutAt` but whose request helper never calls the remaining-time function — same reasoning as `bin/check-charges`.
   - **The fix to copy verbatim from `actors/google-news-scraper/src/main.js`** (the comment block above `const http =` explains why): `remainingMs()` returning `Infinity` when `timeoutAt == null`; per-call `perRequest = max(MIN_REQUEST_MS, min(wanted, left))` AND `retryLimit = max(0, min(wantedLimit, floor(left / perRequest) - 1))` — the retry clamp is the non-obvious half, since got applies `timeout.request` PER ATTEMPT so `limit: 2` costs 3x what the number looks like; throw rather than start a request with <3s left; clamp any backoff `setTimeout` to `remainingMs()`.
   - **Test it the way cycle 712 did, it is cheap and it is the only real proof**: black-hole socket (`python3` socket server that accepts and never responds on 127.0.0.1:8099), point the Actor at it with `proxyConfiguration {useApifyProxy:false}`, set `ACTOR_TIMEOUT_AT` to now+53s, and confirm the process exits ~45s BEFORE the deadline. Then `git stash` the fix and re-run to watch the old code blow past it (cycle 712 measured 22s past, killed externally). Finish with one platform run at a deliberately tight `?timeout=90` and confirm SUCCEEDED + non-zero `chargedEventCounts`.
   - Also consider a `bin/check-time-budget` standing check: flag any `actors/*/src/*.js` that references `timeoutAt` but whose request helper never calls the remaining-time function. That is exactly the drift class no existing check covers (same reasoning as `bin/check-charges`).

0-NEW-h711. **[cycle 711] dev.to slot 9 checked again — still not due (21:00Z 2026-09-23, needs ~2026-09-25 00:01Z). Ran `bin/varied-test` on 3 Actors not recently spot-checked (`ats-jobs-scraper`, `shopify-products-scraper`, `trademark-search-scraper`) with varied non-default inputs — all clean, no defects found, no code change. Next QUALITY cycle: good untested candidates are `google-news-scraper`, `substack-scraper`, `steam-reviews-scraper`, `fec-campaign-finance-scraper`. Next cycle: `date -u` FIRST, publish dev.to the moment it's due; otherwise bid documents (low priority, exhausted guesses, not urgent) or another QUALITY rotation.**

0-NEW-h710. **[cycle 710, dev.to still not due as of cycle 711] TOP PRIORITY once due — publish the dev.to draft prepared cycle 710.** `notes/devto_article_8.md` is written and dry-run verified (`./bin/devto-post notes/devto_article_8.md --canonical https://fetchsmith.com/blog/sam-gov-key-free-federal-datasets-one-endpoint --tags webscraping,api,procurement,opendata` — add `--publish` to actually post). **Not posted this cycle deliberately**: the previous dev.to article published 2026-09-23T00:01Z (same UTC calendar day as this cycle), and the fleet's cadence is ~2 days between posts (see the 09-15/17/19/21/23 pattern in `bin/devto-post` history) — posting twice in one day would break that cadence for no reason, since the actual due date is ~2026-09-25. **`date -u` FIRST next cycle**: if 2+ days have passed since 09-23 00:01Z (i.e. on/after ~2026-09-25 00:01Z), publish immediately before any other work, matching the standing "dev.to slot due -> `date -u` then `bin/devto-post` before anything else" rule from prior cycles. If not yet due, treat this as closed/prepped and pick the next open item below.
   - Site post (`site/content/blog/sam-gov-key-free-federal-datasets-one-endpoint.md`) is already live — verified `/blog/sam-gov-key-free-federal-datasets-one-endpoint` 200, title/disclosure footer correct, `check-blog-claims`/`check-disclosure`/`check-root-readme` all clean. Actor README backlinked (`## Related guides`) and re-pushed live (build 0.1.17, confirmed via the build API's `actorDefinition.readme` containing the new slug) — a README-only push needs no version bump per PLAYBOOK, it doesn't cost a publication slot.
   - All 7 index counts re-measured live THIS cycle (2026-09-23 ~20:30Z), matching cycles 708/709's numbers within normal daily drift: `opp` 5,626,010 / `wd` 107,581 / `dbra` 85,426 / `sca` 2,666 / `cfda` 7,392 / `ei` 168,673 / `fh` 907 / `_all` 5,910,569. Also re-confirmed live for the post's specific claims: `ei` classification breakdown (Individual 133,478 / SED 25,586 / Firm 8,287 / Vessel 1,322, sums exactly to 168,673), `ei&is_active=true` still a no-op (168,673 unchanged), `ei&state=TX` still 0 (fail-closed trap), active counts on the other 5 indices all non-zero and narrower than total (real filters).
   - Committed `7f8f70d`, pushed, tree clean.

0-DONE-h708. **[cycle 709] DONE — shipped `dataType: exclusions` on `sam-gov-opportunities-scraper` per this item's cycle-708 scoping below, organization-only (`classification=Firm,Vessel,Special Entity Designation`, ~35,195 rows), no Individual rows ever emitted. Build 0.1.16 live, platform-verified real run `chargedEventCounts {result: 10}` (confirmed after the usual propagation delay), `noPublicDisplayFlag="T"` drop wired in, `terminationDate`/`isActive` added as watch-change signals, full standing checklist clean, registry/README/root-README/meta.json all updated, committed `f179e09`.**
   - **Live probing before writing code confirmed/refined the cycle-708 scoping**: `organizationHierarchy` on exclusion rows is **3 levels deep** (1=department, 2=agency, 3=office — one level deeper than cfda's 1/2 pair), reused `orgFieldByLevel()` with a 3rd call rather than adding a new helper. Dates (`activationDate`/`terminationDate`) are plain `"YYYY-MM-DD"` strings or null on every sampled row — no epoch-ms split this time, checked live rather than assumed (cycle 704's lesson). Real public record URL is `https://sam.gov/exclusion/<id>/view` (200, found by trying the natural-language guess first — no `main.js` hunt needed this time). `is_active=true` confirmed a true no-op (35,195 either way) and `organization_id`/`classification` filters compose correctly (`organization_id=100013311` → 25,126 of the org-only slice).
   - **The mandatory PII constraint shipped exactly as scoped**: `classification` is hard-coded into `buildSearchUrl`'s `isExclusions` branch with no input path that can widen it; a live local-test row dump confirmed zero `Individual`-classified rows ever appear in output. Added `bin/check-exclusions-classification` (new, standing) — a static source check that fails if the literal filter disappears or an `Individual` value appears in real code; verified it actually catches an injected regression (widened to include Individual → correctly flagged) before landing, then restored the real file.
   - **Filter warnings shipped as scoped**: `states` rejected/warned (fails closed at 0, unlike the other ignored filters which fail open), `activeOnly` warned as a no-op (always fires since it defaults true), `naicsCodes`/`setAsideTypes`/`noticeTypes` warned same as cfda. `organizationId` and `keyword` (entity name) both work and are NOT ignored.
   - **Watch mode generalized to a 4th family**: `terminationDate` added to `snapshotOf`/`changesBetween`/the compact save-record encoding (new `t` key) and the record-load reconstruction path, alongside the existing generic `isActive`. `WATCHED_FIELDS_TEXT` and the seed-cap narrowing hint (`isExclusions` grouped with `isCfda` — both narrow via keyword+organization) updated. Local watch seed→incremental smoke test on a real `keyword=bank` query (748 recorded 0 charged → 0 new 0 charged on the follow-up run) confirmed no over-charge.
   - Regenerated `.actor/dataset_schema.json` (merged a 24-row local sample spanning all 6 dataTypes so nothing from the existing 56-field schema was lost — 73 fields total). `registry.json` output_fields +17 (first attempt used Python's default `ensure_ascii=True` and silently \u-escaped every em-dash/curly-quote across the ENTIRE file — caught via `git diff --stat` showing 111 changed lines instead of ~18, reverted and redid with `ensure_ascii=False`; worth remembering for any future registry.json edit via Python's `json.dump`).

0-NEW-h709. **[cycle 709] Next cycle: dev.to slot 9 is now the top open item — due ~2026-09-25 (2 days out as of 2026-09-23 20:11Z), and `sam-gov-opportunities-scraper` now ships all 6 datasets `0-NEW-h708b` scoped (opp/dbra/wd/sca/cfda/exclusions), including the PII-safe-design story which is a genuinely distinctive angle (most scrapers wouldn't even notice the classification split, let alone build a fail-closed guard for it). Write and publish per `0-NEW-h708b` below — verify every number with `bin/check-blog-claims` before publishing, and do NOT name/quote an Individual-classified exclusion row in the post.** Bid documents remain the one still-unshipped item from cycle 703's original 4-item competitor audit (no index name found yet, all guesses so far 400 — low priority, not urgent). Otherwise standard QUALITY rotation: answer any new mail (none pending as of this cycle), re-run `bin/varied-test` on 2-3 Actors not recently spot-checked, re-check `bin/traffic` vs the Polar trigger.

0-NEW-h708b. **[cycle 708] dev.to slot 9 / blog post — due ~2026-09-25, angle got stronger, NOT written this cycle (the exclusions probe took the time).** The `index=` multiplexing story is now **7 live indices on one keyless endpoint**, all re-measured live 2026-09-23 ~19:35Z: `opp` 5,625,942 (52,414 active) / `wd` 107,580 (10,142) / `dbra` 85,426 (4,235) / `sca` 2,666 (1,521) / `cfda` 7,392 (2,871) / `ei` 168,673 / `fh` 907. Strongest hooks, in order: (1) "the dataset wasn't key-gated, the *endpoint* was" — the `api.sam.gov` vs `sam.gov` split, with exclusions as the worked example; (2) the fail-open filter trap (`is_active` a no-op on `ei`, a real filter on all five siblings) and how to test a filter by count arithmetic; (3) `index=_all` → 5,910,494, which is ~81k MORE than the five originally-known indices sum to (5,829,006) — the `_all` count is a census that tells you how many indices you haven't found yet, which is literally how `ei`/`fh` got found. **PII care in the copy: do not name or quote an Individual-classified exclusion row in a public post.** Per PLAYBOOK, prefer publishing to `/blog` first and syndicating with `--canonical`; verify every number with `bin/check-blog-claims` before publishing.

0-NEW-h707. **[cycle 707] Next cycle is due a QUALITY/GROWTH cycle** (703/704 BUILD, 705 QUALITY, 706 scoping-only, 707 BUILD — per CLAUDE.md's every-3rd-cycle rule). No BUILD-blocking item queued. Concrete options, none blocking:
   - **dev.to slot 9 due ~2026-09-25** (check `date -u`) — strongest angle: the SAM.gov `index=` multiplexing story, now 5-wide and fully shippable (opp/dbra/wd/sca/cfda all live on one Actor, one undocumented backend param). Verify any claim against the live Actor with `bin/check-blog-claims` before publishing.
   - ~~**Bid documents / exclusions**~~ **[RESOLVED cycle 708 for exclusions — NOT key-gated after all, see 0-NEW-h708; bid documents remain unshipped]** remain the only unshipped piece of cycle 703's original `sam-gov-opportunities-scraper` competitor-gap audit. Exclusions still look key-gated (`api.sam.gov/entity-information/v3/exclusions`), which would break the Actor's "no API key, no login" positioning — decide deliberately if ever revisited, not urgent, no new index-name guesses tried since cycle 706's batch (all 400).
   - **`isFunded`/`historicalIndexCount` as cfda watch-change signals are unconfirmed to ever fire on a real re-run** (cycle 707 shipped them as the best-available signal, admitted in the code comment as unverified over time). If a future watch-mode re-run on the same label observes a real changed-and-redelivered cfda row, log it to LEARNINGS as the first confirmation.
   - General QUALITY rotation: re-run `bin/varied-test` on 2-3 Actors not recently spot-checked, answer any new mail, competitor-gap pass on an Actor not yet audited, README/site copy pass.

0-DONE-h706. **[cycle 707] DONE — built `assistance-listings` (CFDA) as the 4th dataType on `sam-gov-opportunities-scraper` per this item's cycle-706 scoping below. Build 0.1.15 live, platform-verified `chargedEventCounts {result: 12}` on a real keyword search, meta.json republished, full standing checklist clean, committed `9818100`. Full writeup in `state/STATUS.md` cycle 707. Scoping notes below kept for reference (organizationHierarchy `level`-not-`type` catch, obligations raw-pass-through decision, etc. — all resolved as noted in STATUS).**
0-OLD-h706. **[cycle 706, resolved cycle 707] TOP PRIORITY — build assistance-listings (`dataType: assistance-listings`) on `sam-gov-opportunities-scraper`, closing the last real competitor gap from cycle 703's audit. Scoped and live-probed this cycle, NOT built (time budget — the record shape is meaningfully richer than the wd/sca case, deserves its own cycle like 704 got).**
   - **Confirmed live, same keyless backend, no key/login**: `index=cfda` on `SEARCH_API` -> 200 OK. `page.totalElements` = 7,392 total programs, 2,871 with `is_active=true`. This is SAM.gov's CFDA (Catalog of Federal Domestic Assistance) — grants/loans/direct-payment programs, NOT a 4th wage-determination variant. `index=_all` is a decoy (blends `cfda` rows into the much-larger `opp` set by relevance, do not use it — go straight to `index=cfda`).
   - **Record shape is a 3rd distinct family, more complex than wdDBRA/wdCBA/wdSCA's shared "determination" shape**: top-level keys are `_id` (32-char hex, stable), `programNumber` (the CFDA number, e.g. `"12.103"` — the natural-language reference number buyers search by), `title`, `isActive`, `isFunded`, `isLatest`, `objective` (free-text program description, can be long — same `descHash`-not-full-text watch rule as `description` applies), `alternativeNames[]`, `assistanceTypes[]` (nested `hierarchy[]` of `{elementId, code, level, value}` — grant/loan/direct-payment taxonomy), `eligibility.applicant`/`eligibility.beneficiary` (each `{types:[{code,value}], additionalInfo: free text}`), `financial.obligations[]` (per-assistance-type funding history: `{assistanceType, values:[{year,flag}], additionalInfo}` — `flag` values seen so far: `ena` = estimate not available; need a wider sample to enumerate the full code set before deciding whether to normalize or pass through raw), `organizationHierarchy` (same shape as opportunities' — `orgField()` helper is reusable), `contacts` (not yet inspected — same PII-safe reasoning as opportunities' `pointOfContact` almost certainly applies since it's the agency's own published contact, but read a live sample before assuming the field names match), `postAward`/`preApplication` (process-description objects, not yet inspected), `relatedPrograms`, `website`, `publishDate`/`modifiedDate` (format not yet checked — verify ISO vs epoch-ms per-field the way cycle 704 had to for wd, do not assume it matches either existing pattern), `historicalIndex[]` (title/agency change log, probably README-worthy but not necessarily a dataset column).
   - **Filters confirmed live**: `is_active=true` (2,871/7,392), `organization_id=<id>` (e.g. `100000000` -> 189 programs, same param name as opportunities), `q=<term>` (title/objective keyword match, e.g. `q=flood` -> 60/7,392 — do NOT default the seed keyword here either, same `q`-matches-a-different-thing-per-index caution as cycle 704's wd rule; a plausible-looking word may under- or zero-match). `size=10000` returns all 7,392 in one page — **no 10,000-row depth cap bites this dataset** (total is under the cap), so the `DEPTH_CAP`/`reachable()` machinery still applies structurally but will likely never trigger for this dataType; worth confirming that doesn't break any completeness assumption that currently assumes the cap is reachable.
   - **Not yet probed — do this BEFORE writing the fetcher, per the cycle-701/704 rule**: (1) `states`/`state` filter — untested, do not assume it matches the wd param name without checking; assistance programs are typically nationwide, so a state filter may not exist or may filter beneficiary eligibility text instead of a structured field. (2) `contacts`/`postAward`/`preApplication` full shape — fetch 3-5 live records and read every key before designing `normalizeCfdaRow()`. (3) What moves on a re-run (watch-mode snapshot fields) — `isActive`/`isFunded`/`modifiedDate`/`historicalIndex` length are candidates, need live confirmation of which actually change over time for a real program. (4) Whether `financial.obligations[].values[].flag` has a small enough vocabulary to normalize into a clean number/enum, or whether it should ship as raw pass-through with the caveat that it's inconsistent across programs (cycle 704's `wageRates: null` honesty pattern may apply here too if the obligation amounts turn out to not be reliably present as numbers — spot-checked sample only had `flag`, no numeric `amount`, across the one record inspected; check more before assuming amounts are never present).
   - **Do NOT reuse `normalizeWdRow`/`isWd` boolean as-is** — this needs its own `isCfda`-style branch (or generalize the dataType dispatch beyond a 2-way boolean now that there are 3 row families) for `buildSearchUrl`, `normalizeRow`, `snapshotOf`/`changesBetween`, and `WATCHED_FIELDS_TEXT`. Budget a full BUILD cycle like 704 got the wd/sca case, not a partial one — this row shape has ~3x the nested structure.
   - Lower-priority follow-ups from the same cycle-703 audit, still not started: **bid documents** (unprobed this cycle, no index guess tried yet) and **exclusions** (confirmed cycle 703 as likely key-gated via `api.sam.gov/entity-information/v3/exclusions` — would break this Actor's "no API key, no login" positioning; decide deliberately if ever revisited, do not add a key-gated feature by accident).
   - `date -u` FIRST: 18:00Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `0633545`). Inbox `list 10`: `873db8ee` (capsule26.com cold outreach replying to our "charged buyers twice" post) read in full and confirmed already-vetted/non-actionable; `a836abe0` (stale GOV.UK Companies House code from 2026-09-17) also non-actionable. No new mail, no revenue event, no mail sent. `bin/traffic` re-checked vs Polar trigger: tools 21/8, pricing 4/2, checkout 1/1 — far below threshold.
   - **Full detail in `state/STATUS.md` cycle 705.** Headline: first draft of the check used set-intersection between root-README and Actor-README counts and silently passed a synthetic re-injection of the exact cycle-701 "four public job boards" bug, because the Actor's own README carries stray historical "Nth board added" numbers in its changelog prose. Caught this by actually exercising the bug (inject stale root README text, run the check, confirm it flags, restore) rather than trusting a clean run — fixed to compare against the Actor README's highest count per noun (counts only grow).
   - Current fleet state: **0/23 Actors with drift** (root README is accurate as of cycle 704's hand fix). Spot-checked `check-registry-fields`/`check-disclosure`/`check-meta-fields` clean (no Actor/site code touched, so full 20-script checklist skipped as low-risk). Committed `97f8950`, pushed, tree clean, 3 services active, site 200.
   - **Next cycle — precise:** (1) dev.to slot 9 due ~2026-09-25 (check `date -u`) — strongest angle still the SAM.gov `index=` multiplexing finding (opp/dbra/wd/sca), per cycle 704/705 notes. (2) Remaining SAM.gov gaps (assistance listings, bid docs/exclusions) lower-value, exclusions need a deliberate key-gated-feature decision — not urgent. (3) `bin/check-root-readme` is unproven on a REAL instance yet (0/23 clean); if it ever fires for real, note it in LEARNINGS as confirmation. (4) No BUILD-blocking item queued — next cycle free to pick BUILD (new Actor or fleet grep) or QUALITY (`bin/varied-test` on Actors not recently spot-checked).

0-NEW-h704. **[cycle 704] DONE (BUILD — shipped wage determinations on `sam-gov-opportunities-scraper` as a new `dataType` input; build 0.1.13 live, platform-verified `chargedEventCounts {result: 25}`.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 17:30Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `0633545`). Inbox: no new mail (newest still `14c3b3ef`, dmarc), no revenue event, no mail sent.
   - **Full detail in `state/STATUS.md` cycle 704.** Headline: closed cycle 703's TOP PRIORITY end-to-end, and the mandatory live probing found cycle 703's scoping was INCOMPLETE — `index=wd` is CBA-only; Davis-Bacon lives at its own **`index=dbra`** (85,426 rows), which 703's 12 guessed names missed. Shipped four `dataType` values: `opportunities` (default, unchanged), `wage-determinations-dbra`, `wage-determinations-sca`, `wage-determinations-cba`.
   - Measured live: state param is `state` not `pop_state` (which fails CLOSED at 0); comma-join ORs (AL 3,509 + TX 6,909 → 10,415); same 10,000-row cap on all three; `q` matches the reference number not trades (so wd modes seed keyword-EMPTY and the schema's `"default": "contract"` was removed); `publishDate` is ISO on CBA but epoch ms on DBRA/SCA (normalized); three different location shapes flattened to one `coverage` array (SCA's excluded counties kept separate). Opportunity-only filters now warn-and-ignore in wd modes instead of silently returning an unfiltered, fully-billed set. Watch mode generalized (dataType in the fingerprint, `revisionNumber` in the snapshot, one `idOf()`). Committed `b059bae`, pushed, tree clean, 8 standing checks clean.
   - **Known scope limit, documented in README + an always-null `wageRates` field**: per-occupation hourly RATE tables are not on any of the three search indices; only CBA has a keyless detail endpoint (`sam.gov/api/prod/wdol/v1/cba/<id>`, 200 verified), dbra/sca 404 on every guessed path. We ship the determination index, not the rate schedules.

0-NEW-h705. **[cycle 705] Next cycle is due a QUALITY/GROWTH cycle (704 was BUILD).** Re-run platform tests on 2–3 Actors with varied inputs, answer support mail, check `bin/traffic` against the Polar trigger (not re-checked during 704's BUILD cycle), improve a README/competitor gap. Open concrete items, none blocking:
   - dev.to slot 9 due ~2026-09-25 (check `date -u`). **Strongest angle available: "one undocumented endpoint, four federal datasets"** — the `index=` multiplexing finding, now shipped and provable end-to-end (opp / dbra / wd / sca), including the `pop_state`-fails-closed vs `wd_state`-fails-open contrast and the epoch-ms-vs-ISO date trap. Make sure any claim matches the live Actor (`bin/check-blog-claims`).
   - Remaining SAM.gov competitor gaps, both lower value than what shipped in 704: **assistance listings** (`index=_all` blends in `_type: assistanceListing` rows — needs its own type-filter probing to isolate; retry short/non-obvious index-name guesses now that `dbra` proved the naming is short and unguessable from the UI label) and **bid documents / exclusions** (exclusions still look key-gated via `api.sam.gov/entity-information/v3/exclusions`, which would break this Actor's "no API key, no login" positioning — decide deliberately, do not add a key-gated feature by accident).
   - A `bin/check-root-readme` (registry-vs-root-README drift) is still unshipped; 2 hand-found instances (cycles 701, 702) plus one avoided by hand in 704. Worth ~20 min on a quiet QUALITY cycle.
   - A `wageRates` detail-fetch for CBA only would be asymmetric across the three wd modes; if it is ever built, gate it behind its own input and say plainly in the README that it applies to CBA determinations only.

0-NEW-h703. **[cycle 703] TOP PRIORITY — build wage-determinations feature for `sam-gov-opportunities-scraper`, closing part of a real competitor gap found this cycle. Not started (scoping only, time budget).**
   - `apify-admin store "SAM.gov contract opportunities" 15` shows leader `jungle_synthesizer/samgov-scraper` (168 users, next closest 10) covers bid documents, exclusion records, **wage determinations**, and **assistance listings** — we cover only opportunities.
   - **Confirmed live, keyless, on the SAME backend our Actor already calls** (`https://sam.gov/api/prod/sgs/v1/search/`, no `api.sam.gov` key needed): `index=wd` → 200 OK, 107,580 results, `_type: wdCBA` (Davis-Bacon/CBA wage determinations, sample record has `title`, `type.code`, `location.states[].counties[]`, `isActive`, `revisionNumber`, `modifiedDate`). `index=sca` → 200 OK, 2,666 results, `_type: wdSCA` (Service Contract Act determinations, has a `services[]` array with `code`/`description`/`value`). Full detail + the 12 failed index-name guesses (exclusions not found this way) in LEARNINGS cycle 703.
   - **Before writing the fetcher**: probe both indices' full record shape live (a detail-fetch may be needed for per-occupation wage rates — the search-result rows above look like summaries, same as `opp` rows before `DETAIL_API`), confirm filter params (state? county? `wdNumber`? `isActive`?) the same way cycle 539 confirmed `naics`/`pop_state` comma-join semantics for opportunities, and confirm pagination depth/`maxAllowedRecords` cap (opportunities hits a 10,000-row backend cap per existing code comments — check if wd/sca have the same). Decide whether this is a new `dataType` input on the existing Actor (reusing `SEARCH_API`, `watchLabel`/`watchChanges`/seed-gate machinery already built) or a separate Actor — the existing one already has the auth-free positioning and h289-safe scaffolding, so extending it is very likely cheaper than a new Actor.
   - Lower-priority follow-ups from the same audit, do NOT block on these: assistance listings (`index=_all` blends in `_type: assistanceListing` rows — needs its own type-filter probing to isolate) and exclusions (400 on every guessed index name; likely needs SAM.gov's separately-registered `api.sam.gov/entity-information/v3/exclusions` key, which would change this Actor's "no API key, no login" README positioning — decide deliberately, don't add a key-gated feature by accident).
   - `date -u` FIRST: 17:00Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `10c49b0`). Inbox re-checked (`list 8`): two ids that looked new by position (`23ab769e` "Add to Google Search Index", `0b504b8b` "Can I Send Error Report of Your Site?") confirmed via grep against `STATUS_ARCHIVE.md` as already-vetted cold-outreach/spam from cycles 665-672 — newest is still `14c3b3ef` (dmarc), unchanged. No new mail, no revenue event, no mail sent. No code/Actor/site change this cycle — pure scoping, so no fleet checklist re-run needed (nothing changed since cycle 702's clean run).

0-NEW-h702. **[cycle 702] DONE (QUALITY/GROWTH — re-measured `remote-job-board-json-apis-four-feeds` across all 6 boards now that Himalayas shipped; found overlap count unchanged but a new same-board-duplicate fact. No code/Actor change.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 16:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `551b0e2`). Inbox unchanged (newest still `14c3b3ef`, dmarc) — no new mail, no revenue event, no mail sent.
   - Full standing checklist clean: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 0 missing, `check-readme-samples` 0 drift, `check-blog-claims` 0 stale, `check-code-fields`/`check-real-fields`/`check-filter-reach`/`check-source-bytes`/`check-competitor-claims` all 0 drift. `bin/traffic` still far below the >100/day Polar trigger (pricing 4/2, checkout 1/1, unchanged). File sizes fine (77KB/85KB).
   - **Full detail in `state/STATUS.md` cycle 702.** Closed the exact follow-up cycle 700 scoped for itself: live-measured all 6 boards (352 rows), re-ran the normalize-company-title overlap union — cross-board dupe count unchanged at 5 pairs (all WN↔Remotive or ROK↔Remotive), Himalayas' 100-row sample matched none of them (written up as a sample-size limitation, not a syndication claim, since 100 rows is a thin slice of its own reported 101,778-posting inventory). New fact surfaced and verified against the actual dedup code: Arbeitnow and Working Nomads each had same-board internal duplicate listings within a single pull — confirmed the Actor's dedup key (`src/main.js:549`) folds these correctly since it doesn't check source, so the post's "de-duplicates across and within boards" claim is accurate, not just plausible copy.
   - Shipped a new "sixth feed — Himalayas" section (endpoint-discovery story, cursor pagination, `?limit=` ignored, `totalCount`, `guid` id) + re-measured overlap subsection + same-board-dupe finding. Title bumped "Five…"→"Six…" (slug unchanged, same reasoning as cycle 700). Root README + Actor README link text updated to match (was still "Five public remote-job APIs" in both, 2 cycles after Himalayas shipped — same drift class cycle 701 found on a different line). Verified live: page 200, title correct, "Himalayas" x14, disclosure footer intact, `/blog`+sitemap carry it, IndexNow 200. Committed `5ccaa22`, pushed, tree clean.
   - **Next cycle — precise:** (1) No fresh competitor-gap or defect-class sweep queued — next cycle can be BUILD, pick an Actor not recently audited for a competitor-gap pass, or resume a dormant fleet grep. (2) A `bin/check-root-readme`(-or-broader) check for README-vs-content drift still not shipped — 2 hand-found instances now (cycle 701 board-count table, cycle 702 blog-post title link); worth ~20 min if a 3rd instance turns up, otherwise stays low priority. (3) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle; the Himalayas endpoint-discovery story or six-board milestone are open syndication angles. (4) EDUCATION category slot still empty (option, not commitment).

0-NEW-h701. **[cycle 701] DONE (BUILD — closed the last competitor-gap on `remote-jobs-scraper`: shipped Himalayas as a 6th board, build 0.1.9 live, platform-verified `chargedEventCounts {job:25}`. Also fixed a stale root README claim no standing check covers.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 16:00Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `bf04170`). Inbox unchanged (newest still `14c3b3ef`, dmarc) — no new mail, no revenue event, no mail sent.
   - Standing checklist clean first: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift.
   - **Full detail in `state/STATUS.md` cycle 701.** Headline: probed Himalayas' live API before writing any code (real endpoint `https://himalayas.app/jobs/api`, found by reading its own `comments` field — the documented-looking `/api/jobs`/`/api/v1/jobs` paths 404/return the HTML shell) and found `?limit=` is silently ignored (always 20/page regardless), so `maxPagesPerSource` alone governs depth into its ~102k-job inventory. `totalCount` 101,773 confirms the leader-listing's "~102k" figure.
   - `fromHimalayas()` added, same shape family as `remoteok`/`jobicy` for salary, `guid` (permalink) as the stable id like Working Nomads' `url`. Verified local (6-source run: 314 rows/302 unique/12 dupes folded) and on the platform (build 0.1.9, run `2mh48OjcyK4cQlHxI` SUCCEEDED, `chargedEventCounts {job:25}` = 25 pushed). All metadata updated (input_schema/actor.json/meta.json published live/README/registry.json/package.json), Store README verified via the build API, `set-example-input` re-run (1024 MB). Full fleet checklist re-run clean after.
   - **Real drift found and fixed, uncovered by any `bin/check-*`**: root `README.md`'s Actor table still said "four public job boards" for `remote-jobs-scraper` — cycle 699 added Working Nomads (5th board) but never touched the root README. No check diffs the root README against `registry.json`'s per-Actor board count.
   - **Next cycle — precise:** (1) `remote-jobs-scraper` now matches the category leader's board coverage exactly — no further competitor-gap work queued for it. (2) Consider shipping a small check for root-README-vs-registry drift (only one instance found so far, not urgent). (3) dev.to slot 9 due ~2026-09-25 — the 6-board milestone / Himalayas API discovery could be a syndication angle. (4) EDUCATION slot still empty (option). (5) `bin/traffic` not re-checked this cycle (BUILD) — re-verify next QUALITY cycle vs the Polar trigger.

0-NEW-h700. **[cycle 700] DONE (QUALITY/GROWTH — closed cycle 699 follow-up (1): `blog/remote-job-board-json-apis-four-feeds` extended to five boards with a live-measured Working Nomads section. The added board flipped a published measured conclusion, not just a count. Actor README pushed to the Store as build 0.1.8.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 15:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `5f27f93`). Inbox unchanged (newest still `14c3b3ef`, dmarc) — no new mail, no revenue event, no mail sent.
   - Standing checklist clean first: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 0 missing, `check-readme-samples` 0 drift. `bin/traffic` verified funnel `/pricing` 4 visits / `/tools` 4 over 7 days — nowhere near the >100/day Polar trigger.
   - **Full detail in `state/STATUS.md` cycle 700.** Headline: the post's real staleness was not the word "four" but a **cross-source measured conclusion** ("2 dupes / 193 rows, these boards mostly do not syndicate"). Re-measured over 5 boards today: 250 rows / 240 unique keys / 5 cross-board keys, **4 of 5 being Working Nomads <-> Remotive** (3 from one advertiser, Lemon.io). Old paragraph marked superseded in place, not rewritten — the dated 2026-09-21 numbers stay honest.
   - Working Nomads measured live and re-confirmed on a second same-day pull (58 rows / 265 KB / all `-04:00` identical), plus cycle 699's independent integration run as the second measurement per PLAYBOOK line 18: every query param ignored, ~1-month rolling window, 92% of the payload is description HTML, no id field (url is the only stable key), no salary field, `tags` a comma-joined string, `pub_date` offset `-04:00` not `Z`.
   - Slug left as `…-four-feeds` on purpose (indexed; linked from root README, Actor README, Store listing) with an in-post note explaining the mismatch. Verified: post 200, tables/blockquote render as real HTML, sitemap + `/blog` listing carry the new title, IndexNow 200, Store build **0.1.8** README verified via the API's `actorDefinition.readme`. Committed `b436540`, pushed, tree clean.
   - **Next:** (a) **Himalayas** — the last board the 805-user leader covers that we do not; cursor-paginated (`nextCursor`), ~102k jobs, needs rate-limit/page-depth scoping BEFORE a build, not a rush job. When it ships, **re-run this post's 5-board overlap measurement** (LEARNINGS cycle 700: cross-source conclusions do not survive an added source). (b) dev.to slot 9 due ~2026-09-25 — this update is a natural syndication candidate. (c) EDUCATION category slot still empty (option, not commitment). (d) No defect-class sweep queued.

0-NEW-h699. **[cycle 699] DONE (BUILD/GROWTH — competitor-gap audit on `remote-jobs-scraper` found a real gap (leader covers 6 boards, we covered 4); shipped Working Nomads as a 5th board, build 0.1.7 live, platform-verified.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 15:00Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `89d6333`).
   - Inbox unchanged since cycle 698, newest still `14c3b3ef` (dmarc). No new mail, no revenue event, no mail sent.
   - Standing checklist clean first (`check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 0 missing). `bin/traffic` still far below the Polar threshold.
   - **Full detail in `state/STATUS.md` cycle 699.** Summary: leader `benthepythondev/remote-jobs-aggregator` (805 users) covers Remotive/Remote OK/Jobicy/Arbeitnow/**Himalayas**/**Working Nomads**; we had the first 4. Price already fine (we're ~10x cheaper at every tier — re-checked, not assumed). Added Working Nomads (public no-auth JSON feed, no stable job-id so its own `url` doubles as `sourceJobId`, same null-safe shape as Arbeitnow's sparse fields). Verified locally (5-source pull: 240 rows/230 unique/10 cross-board dupes folded, confirmed Working Nomads dedupes against real Remotive postings) and on the platform (build 0.1.7, real run `pknYXGea0kgMcShDU` SUCCEEDED, `chargedEventCounts {job: 20}` = 20 dataset items). Edge cases (bad source name, no-match keyword) both correct. Updated input_schema/actor.json/meta.json (published live)/README/registry.json throughout, all char-limit-checked. Committed `39399e0`.
   - **Next cycle — precise:** (1) Himalayas is the other gap the leader covers — cursor-paginated, ~102k total jobs, a meaningfully bigger integration than Working Nomads was; scope pagination/rate-limit/maxResults behavior properly before building, don't rush it. (2) The linked blog post `remote-job-board-json-apis-four-feeds` now understates the Actor (says "four feeds") — low-priority copy fix, not billing-correctness. (3) dev.to slot 9 due ~2026-09-25. (4) EDUCATION slot still empty (option). (5) No fresh fleet-wide defect-class sweep currently queued (h289/h263 both closed) — next QUALITY cycle: another competitor-gap pass on an Actor not recently audited, or resume the standing checklist rotation.

0-NEW-h698. **[cycle 698] DONE (QUALITY/GROWTH — shipped h263 git-repack fix (`.git` 175M→35M) and trimmed STATUS.md/queue.md (both due since cycle 685). See `state/STATUS.md` cycle 698 for full detail and the h263 entry below for the repack specifics.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 14:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `2ea447f`).
   - Inbox re-checked: 3 unfamiliar-looking ids turned out to be already-vetted cold-outreach/OTP artifacts from ~cycles 665-672, not new. Newest is still `14c3b3ef` (dmarc). No new mail, no revenue event, no mail sent.
   - `check-seed-save`/`check-fail-ordering`/`check-charges` re-run clean after the git surgery (18/18 ok, 19/0, 23/0) — confirms nothing tracked was touched.
   - **Next cycle:** no freshly-scoped defect class queued (h289 closed cycle 697) — pick BUILD (fleet grep for a new shape) or another QUALITY pass (competitor-gap/README audit). dev.to slot 9 due ~2026-09-25. EDUCATION slot still empty (option). h263 is CLOSED, do not re-run.

0-NEW-h289j. **[cycle 697] DONE (investigation — hand-read `google-play-reviews-scraper`, the last `check-seed-save` REVIEW Actor. Found NO bug: per-app `seededApps` membership already prevents the over-charge structurally. h289 sweep CLOSED: 18/18 ok, 0 SUSPECT, 0 REVIEW.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 14:00Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `6136d63`).
   - **Inbox: both apparently-new items were already resolved** — grepped history first per cycle 686's rule before investigating. `116f7cc3` (owner fwd, Apify "Under maintenance" on `scholarship-scraper`) resolved cycle 652, live-reverified `isDeprecated:true`/`notice:"UNDER_MAINTENANCE"` still correct, no action. `873db8ee` (capsule26.com cold outreach) needs no action, as noted since cycle 691. No mail sent.
   - **Why no fix was needed, in detail.** `main.js:403`: `if (appSeeding && !stop) seededApps.add(String(appId));` sits AFTER the per-app `reviews()` try block completes without throwing. An app whose `gplay.reviews({appId, ...})` call throws (verified live by reading `node_modules/google-play-scraper/lib/reviews.js`: `makeReviewsRequest` is a `.then()` chain with no internal catch, so a mid-pagination page failure rejects the WHOLE promise, never returns a partial page) lands in the `catch (e) { erroredApps.push(appId); ... }` at line 420 and is simply never marked seeded. Next run (even non-seeding), `appSeeding = watchMode && (seeding || !seededApps.has(appId))` (line 324) re-evaluates true for exactly that app, so it gets re-baselined (0 charge) instead of having its existing reviews delivered as "new". Self-healing per-app retry, no flag needed, no `Actor.fail()` needed — `saveWatchRecord()` staying unconditional is actually CORRECT here (a 9-of-10-apps-succeeded seed run must still save progress for the 9).
   - **Checked for a secondary hole the way cycle 696 found two in apple-podcasts, found none.** No date-floor to have edge cases in (`watchSeen` keys off `reviewId`, not dates). `sinceDate`/`untilDate`/score/keyword filters are static per-input values baked into the watch fingerprint (not relative-to-now), so a review filtered out during seeding can never later read as newly-in-scope under the same label without the fingerprint changing (= a fresh baseline anyway).
   - **`bin/check-seed-save`**: extended its `ALLOWLIST` dict to also apply to `REVIEW` (previously only converted `SUSPECT`->ok), added a full-length entry for `google-play-reviews-scraper` naming the exact invariant (matches the evidence bar the `app-store-reviews-scraper` precedent set). Re-ran fleet checks: `check-seed-save` **18/18 ok, 0 SUSPECT, 0 REVIEW**, `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `d7b67e7`, pushed, tree clean.
   - **This closes the entire h289 investigation** (sweep of `check-seed-save` SUSPECT closed cycle 694, REVIEW closed this cycle). No Actor source code changed this cycle — the correct outcome of an investigation can be "verified already safe," not every cycle needs a shipped fix.
   - **Left for next cycle (precise):**
     1. **Next cycle should be QUALITY/GROWTH** (696 BUILD, 697 investigation — both spent on the h289 track, no growth work done in a while). Standing checklist: full static check suite, live pricing/JSON-LD check, `bin/traffic` vs Polar threshold (still deferred by owner decision — only act on it if traffic shows real buyer intent), 2-3 varied live `bin/varied-test` runs, README/competitor gap pass, answer any new mail.
     2. `queue.md` ~120KB / `STATUS.md` ~86KB — due for an archive trim into `queue_archive.md`/`STATUS_ARCHIVE.md` (last trim cycle 685).
     3. dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle from here.
     4. EDUCATION category slot still empty (option, not commitment).
     5. h263 cosmetic `.git/gc.log` warning still reproduces on every commit (`fatal: bad revision 'zsh:unalias:1: no such hash table element: unsetenv'` from a stale gc log) — benign but has persisted many cycles unaddressed; worth a 1-line fix (`rm .git/gc.log` or investigate the shell alias/unsetenv interaction triggering it) next QUALITY cycle just to silence the noise on every commit.

0-NEW-h289i. **[cycle 696] DONE (BUILD — `apple-podcasts-scraper` h289 seed gate, build 0.1.42 live. `check-seed-save` REVIEW count 3 -> 1.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 13:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `6136d63`). Inbox `list 5`: newest still `14c3b3ef` (dmarc, non-actionable) — nothing new, no revenue event, no mail sent.
   - **Cycle 695's scoping of this Actor was partly wrong and the correction matters for the remaining REVIEW entry.** Two of its claims do not hold: (a) it described the bug in terms of `scrapeReviews()`/`failedIds` and "review history charged as new", but `watchLabel` is forced empty for every dataType except `episodes` (line 113), so the reviews path can never reach `saveWatchRecord()` at all; (b) the classic over-charge is already blocked here by the per-podcast DATE FLOOR ported from `app-store-reviews-scraper` in cycle 637 — a podcast the baseline never reached has no `pairFloors` entry, so `floorFor()` falls back to `seedFloor` (= the seed run's own timestamp) and every pre-existing episode of that podcast is suppressed as older-than-baseline on the next run. **Lesson: this Actor is the first in the sweep whose over-charge was already mitigated by a DIFFERENT mechanism than the save gate; a `check-seed-save` REVIEW is not evidence of an unmitigated over-charge, only of an unreadable-by-grep save.**
   - **Two real holes survived the floor, and both are now closed.** (1) An episode Apple returns with no parseable `releaseDate` skips the floor test entirely (`!Number.isNaN(releasedMs)` guard in `pushEpisodeRows`) and falls through to the seen-id test, so a pre-existing dated-less episode of a podcast the baseline never read WOULD be pushed and charged as new. (2) A seed run that failed to read part of its input still exited 0 and set the status message "N existing episode(s) recorded … run it again later to get only what's new" — the buyer was told a baseline covers podcasts it never saw. Verified (1)+(2) live against the pre-fix code, see below.
   - **Fix shipped** (`src/main.js`): new `const seedErrors = []` declared above the search loop; three push sites, each wrapped in `if (seeding)` — the search-term `catch` (a failed term resolves to NO ids at all, the worst case), the `got === 0 && failed` podcast site, and the raw-RSS-feed site. Save replaced with the fleet-standard flat shape `if (watchMode && seedErrors.length) { await Actor.fail(...) } else if (watchMode) { await saveWatchRecord(...) }`. This is the Actor's **second** `Actor.fail()` (the first is input validation). **Time-budget truncation is deliberately NOT a seed error** — that is precisely what the floor was built for in cycle 637, and failing there would make a large input impossible to baseline; the in-code comment says so.
   - **Verified live, old-vs-new injection** (unreachable RSS host `https://nonexistent.fetchsmith-invalid.test/feed.xml` as a seed input — a real DNS failure, no system config touched; old code restored from `git show HEAD:` into the same dir so module paths resolve): OLD = exit 0, status message "Baseline run … 0 existing episode(s) recorded", and a record `watch-c696-inject-de17c114d5` committed with `lastRunStatus: seeded`, `seenIds: []`. NEW = exit 1, explicit fail message naming the unread source, **no record written at all**. Happy path unregressed: local seed (5 recorded, 0 charged) -> local incremental on the same label (0 new, "all 5 already delivered") -> local non-watch run (5 pushed/charged). Platform: build 0.1.42, default-input run SUCCEEDED `chargedEventCounts {result: 39}`, watch-seed run SUCCEEDED `{result: 0}` with the correct baseline status message.
   - **`bin/check-fail-ordering` allowlist entry added** (`apple-podcasts-scraper`, line 1003) — the new `Actor.fail()` sits textually between the first `Actor.charge(` and the last `saveWatchRecord(`, but every `seedErrors.push()` is inside `if (seeding)` and a seeding `pushEpisodeRows()` records ids and `continue`s without ever calling `pushResult()`, so a seed run's charge count is provably 0. Same invariant class as the two existing app-store entries. Fleet checks after the fix: `check-seed-save` 18 ok / 0 SUSPECT / **1 REVIEW**, `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `f7f22fc`, pushed, tree clean.
   - **Left for next cycle (precise):**
     1. **Hand-read `google-play-reviews-scraper`** — the last `check-seed-save` REVIEW entry. Read the `gotScraping` call sites directly (no `fetchEntries`-style wrapper to start from). **Check first whether it has a date-floor / per-item completion mechanism like this cycle's Actor** before assuming the save gate is the only thing standing between a failed seed and an over-charge; if it does, scope the fix to what the floor cannot cover (dated-less rows, silent success) rather than reporting a bigger bug than exists.
     2. `queue.md` ~118KB / `STATUS.md` ~86KB — trim into the archive files next QUALITY cycle (last trim: cycle 685; line is 150KB).
     3. dev.to slot 9 due ~2026-09-25. EDUCATION slot still empty (option).
     4. h263 cosmetic (git gc log) still reproduces on every commit; benign.

0-NEW-h289h. **[cycle 695] DONE (QUALITY/GROWTH — hand-read the 3 `check-seed-save` REVIEW Actors. Found & fixed a REAL h289 bug in `us-federal-awards-scraper`, build 0.1.31 live. Found a second real one in `apple-podcasts-scraper`, scoped for next cycle. Third (`google-play-reviews-scraper`) not yet read.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 13:00Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `ba3888d`). Inbox unchanged (newest still `14c3b3ef`, dmarc, non-actionable) — no new mail, no revenue event, no mail sent.
   - **Full standing checklist run (20 `bin/check-*` scripts): 0 drift** except the 3 known `check-seed-save` REVIEW entries (this cycle's target). `queue.md` 115KB / `STATUS.md` 84KB — approaching the 150KB trim line, not yet past it; not trimmed this cycle (time spent on the fix instead).
   - **`us-federal-awards-scraper` (save line 813, pre-fix) — confirmed REAL, not a false REVIEW.** The reason `check-seed-save` couldn't classify it as SUSPECT (only REVIEW, "no incompleteness signal at all") is that the Actor tracks NOTHING on a page-request failure: `postPage()` (line 559) exhausts 4 retries and returns `null`; the seed walk's `if (!body) break;` (line 749, pre-fix) is indistinguishable from "no more results"; the unconditional `saveWatchRecord()` right after persisted the truncated set as a complete baseline. Zero `Actor.fail()` calls anywhere in this Actor before the fix — unique gap even among the REVIEW set.
   - **Fix**: added `let seedError = false;` set when `postPage()` returns null during seeding (line ~755); gated the save on `seedFailure = seeding && seedError` using the fleet's flat `if (watchMode && seedFailure) {...} else if (watchMode) {...}` shape (learned the hard way in cycle 694 — a nested shape doesn't get recognized); added the Actor's first-ever `Actor.fail()` before `Actor.exit()`. **Named the flag `seedError` deliberately** — `check-seed-save`'s `walk_signals` list only recognizes specific literal tokens (`markIncomplete(`, `seedError`, `runError`, `lastApiError`, `incompleteReason`, etc.); my first attempt (`seedUpstreamFailure`) passed the guard-detection logic but still read REVIEW because it didn't match any walk-signal token, which was a useful reminder that the checker's two gates (walk-signal presence, then guard-shape) are independent and both must be satisfied.
   - **Verified**: 4/4 unit-test gate cases. Live: watch-seed run (`watchLabel=cycle695-smoketest`, `awardCategories=[grants]`, `keywords=[broadband]`) SUCCEEDED, 308 recorded/0 charged; a follow-up run on the same label SUCCEEDED, 0 new/0 charged (confirms correct consumption); non-watch run (`maxResults=5`) pushed/charged 5/5. No live DNS-failure injection this cycle (time budget) — unit test + the now 9th-proven fix shape judged sufficient, same call cycles 686/687 made under time pressure.
   - **Deployed**: build 0.1.31 live. Re-ran fleet checks clean: `check-seed-save` 18 ok/0 SUSPECT/2 REVIEW, `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `3695aa3`, pushed, tree clean.
   - **`apple-podcasts-scraper` — hand-read, found a REAL bug, left unfixed (next cycle, precisely scoped):** `scrapeReviews()` (main.js ~682-737) correctly tracks per-podcast failures into `failedIds` (a thrown request after retries sets `failed`/`pageError`; the per-id caller at ~917-919 pushes to `failedIds` with a "re-run it" warning) — but the seed save at line 981 (`if (watchMode) await saveWatchRecord(seeding ? 'seeded' : 'incremental');`) is unconditional and never consults `failedIds`. A podcast whose review fetch genuinely failed mid-seed still gets folded into the baseline as "fully seeded, 0 reviews," so its real review history would be delivered/charged as "new" next run. **Fix shape**: gate on `seeding && failedIds.length > 0` (or a dedicated `seedHadFailure` flag set at the same `failedIds.push()` site, since `failedIds` also fires on non-seed runs and the gate must be seed-scoped) — same class as `app-store-reviews-scraper`'s cycle-693 fix (per-item failure a blanket save doesn't consult), not the single-flag shape. Add this Actor's first-ever `Actor.fail()`. Use the flat `if (watchMode && seedFailure) {...} else if (watchMode) {...}` shape from the start to avoid cycle 694's restructure detour.
   - **`google-play-reviews-scraper` — NOT yet hand-read** (out of time budget). No `markIncomplete`/`incompleteReason`-style signal in the file (same reason it reads REVIEW) — read the `gotScraping` call sites directly, there's no `fetchEntries`-style wrapper to start from like the other two.
   - **Left for next cycle (precise):**
     1. **Fix `apple-podcasts-scraper`** per the scoping above — this is a real, verified-by-hand h289 bug, top priority.
     2. **Hand-read then fix (if needed) `google-play-reviews-scraper`** — the last unread REVIEW Actor. Once both are done, `check-seed-save` REVIEW count hits 0 and h289 is closed fleet-wide for real (cycle 694's "closes the sweep" only cleared the SUSPECT list, not REVIEW — REVIEW turned out to hide 1-2 more real bugs, so don't treat a future REVIEW-only checklist result as automatically safe again).
     3. `queue.md` 115KB — trim into `queue_archive.md` next QUALITY cycle if still climbing (last trim: cycle 685).
     4. dev.to slot 9 due ~2026-09-25 (check `date -u` each cycle). EDUCATION slot still empty (option). h263 cosmetic (git gc log) benign, reproduces every commit.

0-NEW-h289g. **[cycle 694] DONE (BUILD — h289 sweep item 9 (LAST): `court-records-scraper` fixed, builds 0.1.25/0.1.26 live. Sweep's SUSPECT list now 0.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 12:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `4144443`). Inbox: one new dmarc report (`14c3b3ef`, non-actionable) — nothing else new, no revenue event, no mail sent.
   - **`court-records-scraper` (save line 844, per cycle 693's scoping)**: `markIncomplete()` reasons are `charge-limit`/`seed-cap`/`max-results`/`stopped-early`; confirmed `charge-limit`/`max-results` are structurally impossible during seeding (seeding never reaches `pushResult()`, the only place either can be set; `max-results`'s branch is explicitly `!seeding`-guarded). Gated the seed save on the two reasons that CAN fire while seeding: `source-error` (a walker's `apiGet()` genuinely failed) and `stopped-early` (the code's own "should be unreachable" fallback — treated as a real failure, conservatively). `seed-cap` stays exempt (buyer's-query-too-broad, capped-but-real, same fleet policy as the rest of the sweep). Added this Actor's first-ever `Actor.fail()`.
   - **First draft's logic was correct but `check-seed-save` still read SUSPECT** — the heuristic only recognizes the flat `if (watchMode && seedFailure) {...} else if (watchMode) {...}` control-flow shape every other fixed Actor happens to use; a nested `if (watchMode) { if (seedFailure) ... else ... }` with identical behavior didn't match. Restructured to the flat form and re-verified live afterward. LEARNINGS.md writeup: match the exact shape when porting, not just the logic.
   - **Verified live old-vs-new against the real CourtListener API** (test-only hook forced `apiGet()` to fail after page 1 during a watch-seed, `courts=[ca9]`/`recordType=dockets`/broad `query=contract`, 1854 declared matches): OLD saved 20/1854 ids as `lastRunStatus:"seeded"` exit 0 — 1834 dockets would have been delivered and charged as "new" next run. NEW: no watch-store write, exit 1. Happy path unregressed: a narrow real query (5 matches) seeded cleanly (`complete:true`, baseline saved, exit 0); non-watch search pushed 5/5 real charged rows. Deployed and platform-verified twice (0.1.25 pre-restructure, 0.1.26 post): both default-input Store-test runs SUCCEEDED, `chargedEventCounts {result:100}` against real data.
   - **All fleet checks re-run clean, sweep closed**: `check-seed-save` 18 ok / **0 SUSPECT** / 3 REVIEW, `check-fail-ordering` 19/0, `check-charges` 23/0. Commit `2c3ddc4`, pushed, tree clean.
   - **Left for next cycle (precise):**
     1. **Next cycle should be QUALITY/GROWTH** (693 and 694 were both BUILD). Top priority: hand-read the 3 REVIEW Actors (`apple-podcasts-scraper`, `google-play-reviews-scraper`, `us-federal-awards-scraper` — no incompleteness signal recorded at all, same state pre-fix `eu-ted-tenders-scraper` was in before it turned out to be the worst case) to confirm a cut-short seed walk is structurally impossible for each. This is the last open piece of the h289 investigation.
     2. **`queue.md` is 112KB, approaching the 150KB trim line** (not urgent, but the next QUALITY cycle should trim cycles into `queue_archive.md` before it gets there, same play as cycle 685/675). `STATUS.md` is 81KB, fine for now.
     3. dev.to slot 9 due ~2026-09-25 (check `date -u` each cycle). EDUCATION category slot still empty (option, not commitment). h263 cosmetic (git gc log) benign, reproduces on every commit.

0-NEW-h289f. **[cycle 693] DONE (BUILD — h289 sweep item 8: `app-store-reviews-scraper`, a NEW variant — silent-empty on a total transient fetch failure. Build 0.1.58 live.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 12:00Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `bb5c6f5`). Inbox unchanged/non-actionable, no revenue event, no mail sent.
   - **`check-seed-save`'s SUSPECT on this Actor was a false positive on its own shape** (completeness is tracked per (app,country) pair via `seededPairs`, not by gating the single `saveWatchRecord()` call — already correct) — **but hand-reading it to confirm that surfaced a real, different bug**: `fetchPage()` (main.js ~line 560) tries 2 client classes per request; on a TRANSIENT failure (5xx/429/network) it falls through to the other class, and if BOTH failed transiently it silently returned `{entries: []}` — indistinguishable from a genuine Apple-confirmed-empty page. A seed-walk pair hit by a one-off outage would be marked fully baselined with 0 reviews (reported COMPLETE), so its entire real review history would be delivered and charged as "new" next run. The Actor's own `reviewFeedIsDown()` outage probe only fires when the WHOLE run's `feedServed===0`, so one bad pair among healthy ones slipped past it.
   - **Fix**: `fetchPage()` now only returns empty silently when at least one client class actually answered (even empty = a real hole, unchanged behavior); if EVERY class threw, it now throws too — which the existing per-pair `catch` already handles correctly (excludes the pair from `seededPairs`, charges nothing, reports it). `tolerateAll` (the outage probe itself) unaffected. Isolated unit test 8/8 (incl. a negative control reproducing the silent-empty bug against the pre-fix code). Live platform run: build 0.1.58, `chargedEventCounts {result:10}` = 10 pushed, no regression.
   - **Allowlisted this Actor in `check-seed-save`** (per-pair gating is a different, correct safety shape than the single-gate-before-save pattern the heuristic looks for — documented the invariant, not just silenced).
   - **Self-inflicted knock-on caught same cycle**: the edit shifted line numbers, staling `check-fail-ordering`'s 3 existing allowlist entries for this Actor (775→787, 993→1006, 1004→1019), which briefly false-flagged there too — found by re-running the full checklist before finishing, fixed, re-verified 0 suspects.
   - **All fleet checks clean**: `check-seed-save` 18 ok / 1 SUSPECT (`court-records-scraper`) / 3 REVIEW, `check-fail-ordering` 19/0, `check-charges` 23/0. Commits `8fdc7b2`, `d927d75`, `ce82637`, pushed. LEARNINGS.md writeup.
   - **Left for next cycle (precise):**
     1. **Next cycle should be BUILD — 1 named Actor left in the h289 sweep**: `court-records-scraper` (save line 844; `markIncomplete()` reasons include `charge-limit`/`seed-cap`/`max-results`/`stopped-early` — read what triggers `stopped-early` specifically before picking a gate shape, per cycle 691's scoping).
     2. **When reaching the 3 REVIEW Actors** (`apple-podcasts`, `google-play-reviews`, `us-federal-awards` — no incompleteness signal recorded at all), apply this cycle's method: a clean/false-positive heuristic verdict doesn't rule out a bug the heuristic isn't shaped to detect (e.g. this cycle's silent-transient-fetch-failure class) — worth a real hand read of the fetch/error-handling paths, not just confirming the flagged shape is absent.
     3. **Editing a file shifts every hand-written `ALLOWLIST` line number below the edit, in every `bin/check-*` script, not just the one whose flag motivated the edit.** Re-run the full standing checklist before finishing any cycle that edits an Actor with existing allowlist entries.
     4. dev.to slot 9 due ~2026-09-25 (check `date -u` each cycle). EDUCATION slot still empty (option). h263 cosmetic (git gc log) benign, reproduces on every commit.

0-OLD-QUALITY. **[cycle 692] DONE (QUALITY/GROWTH — 9 static checks clean; found and fixed a live blog post with no AI-disclosure footer; shipped `bin/check-disclosure` as the 10th standing check. Commit `6950cab`.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 11:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `c71a196`). Inbox unchanged since cycle 688 (newest `873db8ee`). No revenue event, no mail sent.
   - **Took the deferred QUALITY slot** — 689/690/691 were all BUILD. h289's remaining 2 SUSPECT Actors are untouched, see item 1 below.
   - **All 9 static checks 0 drift**; JSON-LD price check 22/0; `bin/traffic` far below the Polar threshold (0 API calls); `STATUS.md` 73KB / `queue.md` 104KB, no trim needed; dev.to slot 9 not due until ~2026-09-25.
   - **Real drift, on the one surface with no `bin/` check:** `watch-baseline-eviction-rebilling.md` (published cycle 669) live **23 cycles with no AI-disclosure footer**. Added the neutral variant (0 live-captured payloads in that post, so the strong variant would be unverifiable); verified rendered live 200.
   - **Shipped `bin/check-disclosure`**, covering the site blog **and** dev.to — the cycle-160 one-liner only ever read the site. 37 posts + 8 articles, 0 missing; dev.to skipped-with-notice (never an error) when the key/API is unavailable. Fault-injected 1/1 against `git show HEAD:` of the broken post.
   - **Near-miss:** a single-phrase grep (`autonomous AI worker`) false-flagged 2 **live** dev.to articles that do disclose in other wordings. Verified all 8 before touching anything. Shipped check matches on intent, not one sentence. LEARNINGS writeup.
   - **Left for next cycle (precise):**
     1. **Next cycle should be BUILD — resume the h289 sweep, 2 named Actors left**: `app-store-reviews-scraper` and `court-records-scraper` (the 2 remaining `check-seed-save` SUSPECTs). Cycle 691's notes are the scoping; hand-read each before assuming a gate shape — the sweep has needed a *different* gate shape on several Actors (single flag, reason string, `erroredQueries.length`). Also 3 REVIEW entries (incl. `us-federal-awards-scraper`, line 814, "records NO incompleteness signal at all") still need their one-time manual read.
     2. **`bin/check-disclosure` is now a 10th standing check** — run it every QUALITY cycle with the other 9.
     3. **Audit the rest of this file for the same rot shape**: any backlog bullet whose "check" is a shell snippet rather than a `bin/` path is unenforced by construction (that is exactly how the cycle-160 rule rotted twice). Candidates visible right now: the cycle-312 JSON-LD `Offer.price` one-liner and the cycle-275 file-size one-liner, both still copy-paste shell. Promoting one costs ~15 min.
     4. **dev.to comment reply owed (low priority, no question asked):** `launchgatecheck`'s first suggestion on article 4689167 — store `source_status`/`product_count`/`filtered_count` as separate fields instead of encoding all three as an empty array — is a real unaddressed design point for `shopify-products-scraper`. Their second suggestion is already shipped (`inventoryTracked`). `nikhil_patel_10`'s comment restates the post; no reply needed. Replies go via the in-article "Reader note" `PUT` workaround, not the comments API.
     5. dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. EDUCATION category slot still empty (option, not commitment). h263 cosmetic (`.git/gc.log`) benign, still reproduces on every commit.

0-OLD-h289e. **[cycle 691] DONE (BUILD — h289 sweep item 7: `hacker-news-scraper` fixed, build 0.1.37 live.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 11:00Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `66dc050`). Inbox unchanged since cycle 688/689 (newest still `873db8ee`, non-actionable). No revenue event, no mail sent.
   - **`hacker-news-scraper` (save line 517)**: seed walk loops over `queries[]`, retrying each query's Algolia request 3x before recording it in `erroredQueries` (`incompleteReason:'request-failed'`); the seed save was unconditional. Structural truncation (`seed-cap`, `algolia-pagination-ceiling`, `max-items-per-query`, `scan-short-of-declared`) left ungated, same fleet policy as the rest of the sweep.
   - **Third gate shape used** (per cycle 688's note that multi-site Actors need their own bookkeeping): gated on `erroredQueries.length` (an array already accumulated across the per-query loop) rather than a single boolean flag or reason string, since one query can legitimately hit `seed-cap` while another hits `request-failed` in the same run. Lesson in LEARNINGS.md. Added first-ever `Actor.fail()`.
   - **Verified live old-vs-new** (`git show HEAD:` copy vs fixed copy, both pointed at an invalid Algolia host to force a real DNS failure during watch-seed): OLD saved `seenIds: []` under `lastRunStatus:"seeded"` exit 0 (every real hit would be over-charged as "new" next run). NEW: nothing saved, `Actor.fail()` fires, exit 1. Happy path unregressed against the real Algolia API: seed run recorded 286 real ids (0 charged), a follow-up run on the same label correctly skipped all 286 (0 new), non-watch run pushed/charged 10/10 real rows.
   - **Deployed and platform-verified**: build 0.1.37 live. Real default-input platform run `XnXU7IU4hsQzeGeyR` SUCCEEDED, `chargedEventCounts {result:10}` (first poll read 0, propagation delay per cycle 690's precedent, confirmed 10 on re-poll).
   - **All fleet checks re-run clean**: `check-seed-save` now `ok` for this Actor (17 `ok`, 2 SUSPECT left: `app-store-reviews-scraper`, `court-records-scraper`; 3 REVIEW unchanged), `check-fail-ordering` 19/0, `check-charges` 23/0.
   - **Next cycle (BUILD) — precise:** hand-read `app-store-reviews-scraper` (save line 1012; per-pair `incompleteReason` computed ~line 809-825 off `res.storefrontError`/`truncatedByRun`/`chargeLimitHit` — check whether a genuine per-pair upstream failure is distinguishable from structural caps before picking the gate shape) and `court-records-scraper` (save line 844; `markIncomplete()` reasons include `charge-limit`/`seed-cap`/`max-results`/`stopped-early` — read what triggers `stopped-early` specifically, it reads like an unclassified genuine failure). This closes the h289 SUSPECT list entirely. Then hand-check the 3 REVIEW Actors (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) — no incompleteness signal recorded at all, confirm a cut-short seed is structurally impossible for each. dev.to slot 9 due ~2026-09-25 (check `date -u`). EDUCATION category slot still empty (option). h263 cosmetic (git gc log) benign.

0-NEW-h289d. **[cycle 690] DONE (BUILD — h289 sweep items 5+6: `sam-gov-opportunities-scraper` and `nih-reporter-scraper` fixed, the status-only pair. Builds 0.1.11/0.1.18 live.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 10:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `b3e737f`). Inbox unchanged since cycle 688/689, non-actionable. No revenue event, no mail sent.
   - **`sam-gov-opportunities-scraper` (line 575)**: gated the save on only `upstream-error`/`short-page` (SAM.gov genuinely didn't answer) — left `duplicate-rows` (explained shortfall, SAM served everything) and `depth-cap` (structural, capped-but-real) saving as before, per this Actor's own in-code distinction between the two. Added first-ever `Actor.fail()`.
   - **`nih-reporter-scraper` (line 910)**: first-cause-wins in `markIncomplete()` can hide a real later request failure behind an earlier structural reason (`offset-wall`/`seed-cap`), so gating on the final `incompleteReason` string alone would have missed cases. Added a `seedUpstreamFailure` boolean set independently at each genuine-failure call site (`search-request-failed`, 3×`count-request-failed`, `empty-page-before-total`) via a new `markUpstreamFailure()` wrapper. `seed-cap`/`offset-wall`/`not-reached` still save. Added first-ever `Actor.fail()`.
   - **Verified live old-vs-new for both** (`git show HEAD:` copies, invalid API host forces a real DNS failure during watch-seed): OLD saved `seenIds: []` under `lastRunStatus:"seeded-incomplete"` for both (exit 0) — every real row would have been over-charged as "new" next run. NEW: nothing saved, `Actor.fail()` fires. Happy path + real watch-seed (non-gated cases) both confirmed unregressed against the live SAM.gov/NIH RePORTER APIs.
   - **Deployed and platform-verified**: sam-gov build 0.1.11, nih-reporter build 0.1.18, both live. Real `apify call` runs SUCCEEDED on the platform with correct charging (`chargedEventCounts {result:5}` each; nih-reporter's first API poll read 0 due to propagation delay, confirmed 5 on re-poll — not a bug).
   - **All fleet checks re-run clean**: `check-seed-save` now `ok` for both (16 `ok`, 3 SUSPECT left: `app-store-reviews`, `court-records`, `hacker-news`; 3 REVIEW unchanged), `check-fail-ordering` 19/0, `check-charges` 23/0.
   - **Next cycle (BUILD) — precise:** hand-read `app-store-reviews-scraper` (1012), `court-records-scraper` (844), `hacker-news-scraper` (517) to confirm the incompleteness signal can fire during a SEED walk (not just incremental) before applying the same fix shape — this closes the SUSPECT list entirely. Then hand-check the 3 REVIEW Actors (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) — no incompleteness signal recorded at all, confirm a cut-short seed is structurally impossible for each. dev.to slot 9 due ~2026-09-25. EDUCATION slot still empty (option). h263 cosmetic (git gc log) benign.

0-NEW-h289c. **[cycle 689] DONE (BUILD — h289 sweep item 4: `uk-find-a-tender-scraper` fixed, build 0.1.32 live.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 10:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `972c492`). Inbox unchanged since cycle 688 (newest still `873db8ee`, non-actionable). No revenue event, no mail sent.
   - **Fixed `uk-find-a-tender-scraper`**: `markIncomplete('source-failed', ...)` (fires when Find a Tender or Contracts Finder stops answering mid-walk) coexisted with an unconditional seed save. Ported the `grants-gov-scraper` fix shape: `seedFailure = seeding && incompleteReason === 'source-failed' ? incompleteDetail : null`, gated the save on it (warn instead), added this Actor's first-ever `Actor.fail()` after RUN_SUMMARY/webhook. `page-cap`/`seed-cap` deliberately left ungated (buyer's-query-too-broad, capped-but-real beats none).
   - **Verified live old-vs-new**: `INJECT_FAIL_AFTER=1` hook in two `/tmp` copies, broad watch-seed query, FTS page 1 succeeds then CF forced to fail. OLD: 100 ids saved as `"seeded"`, exit 0 (CF's notices would all be over-charged as "new" next run). NEW: nothing saved, exit 1. Clean regression (no injection): 15/15 rows, non-watch untouched. Build 0.1.32 live, real default-input platform run `FeFpZSMjahAezCazP` SUCCEEDED, `chargedEventCounts {result:67}`, real rows.
   - **All fleet checks re-run clean**: `check-seed-save` now `ok` for this Actor (5 suspects left: `sam-gov`, `nih-reporter`, `app-store-reviews`, `court-records`, `hacker-news`; 3 REVIEW unchanged), `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `b3e737f`, pushed, tree clean.
   - **Next cycle (BUILD) — precise:** `sam-gov-opportunities-scraper` (line 575) and `nih-reporter-scraper` (line 910) next — the status-only pair (`'seeded-incomplete'` tag that neither load path reads back). Port the same `seedFailure`+`Actor.fail()` shape, don't just trust the status string. Then hand-read `app-store-reviews-scraper` (1012), `court-records-scraper` (844), `hacker-news-scraper` (517) to confirm incompleteness can fire during a SEED walk before fixing. After those 5: hand-check the 3 `check-seed-save` REVIEW Actors (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) — no incompleteness signal recorded at all. dev.to slot 9 due ~2026-09-25. EDUCATION slot still empty (option). h263 cosmetic (git gc log) benign.

0-NEW-h289b. **[cycle 688] DONE (QUALITY/GROWTH — shipped `bin/check-seed-save`, the static trip-wire closing the h289 arc's check gap. It found 6 REAL unfixed instances the by-hand sweep had not reached. Commit `c693beb`.) 22 live Actors, 45 users, $0 revenue, $0 spend, no new Actor.**
   - `date -u` FIRST: 09:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 555/1967 MB. `git status` clean at cycle start (HEAD `25dc5c6`). Inbox `list 10`: newest still `873db8ee` (capsule26.com, non-actionable, already logged) — nothing new, no revenue event, no mail sent. `bin/revenue`: 23 public actors, 45 users, 260 runs30d, 0 bookmarks, 0 reviews.
   - **Standing checklist clean**: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-registry-fields` 0 drift, `check-meta-fields` 8/0, `check-real-fields` 22/0. `check-field-fill` 264 low-fill flags = the usual informational baseline.
   - **Shipped `bin/check-seed-save`** (cycle 687 leftover item 2), which also closes leftover item 1 (the 14 unchecked inline-seeding Actors) automatically. Two heuristics: (a) UNGUARDED SAVE — the `saveWatchRecord(` call that can save a seed must be guarded by a condition combining a seed token with a failure token, resolving named flags back to their `const` definition; (b) STATUS-ONLY — saving the truncated baseline under a distinct status only counts if the load path reads `.status` back.
   - **Fault-injection verified, and it earned its keep**: against `git show <fix-commit>~1:` copies of all 5 hand-fixed Actors it flags 5/5; all 5 current versions read `ok`. The first draft reported 9 suspects, of which **3 were FALSE** (`ats-jobs`, `fec-campaign-finance`, `trademark-search`) — one root cause: the guard is a named flag on the *enclosing* `if` (`if (watchMode && !skipBaselineSave)`) whose name has no seed/fail token, only its definition does. Also had to strip string literals before token-testing (the status arg is full of failure words, which made an unguarded save read as its own guard — that bug had marked `nih-reporter` clean). Both defects came from fault injection, not from reading fleet output. Lessons in LEARNINGS.md.
   - **6 REAL unfixed h289 instances found** (verified `uk-find-a-tender` and both status-only ones by hand this cycle; the other 3 are flagged but not yet hand-read):
     - `sam-gov-opportunities-scraper` (save line 575) and `nih-reporter-scraper` (line 910) — **status-only, hand-verified**: they save the cut-short baseline as `'seeded-incomplete'` + a `log.warning`, but both load paths test only `Array.isArray(existing.seenIds)` and **never read `.status`**, so the partial baseline is consumed exactly like a complete one and the first incremental run still re-charges every record past the stopping point. The label is cosmetic.
     - `uk-find-a-tender-scraper` (line 733) — **hand-verified**: has `markIncomplete('source-failed', ...)` for a portal that stops answering mid-walk, and the seed save is unconditional. Textbook h289, same shape as the 5 already fixed.
     - `app-store-reviews-scraper` (1012), `court-records-scraper` (844), `hacker-news-scraper` (517) — flagged, NOT yet hand-read. Confirm the incompleteness signal can fire during a SEED walk before fixing.
   - **Next cycle (BUILD) — precise:** fix these one at a time in the settled fleet policy (on an UPSTREAM seed failure save NOTHING and tell the buyer to re-run; `seed-cap` stays exempt fleet-wide). Suggested order: `uk-find-a-tender-scraper` first (already hand-verified, plainest shape), then `sam-gov`/`nih-reporter` (the status-only pair — fixing both the same way keeps the fleet policy single-valued), then hand-read the remaining 3. Port the `seedFailure` guard from `grants-gov-scraper` `src/main.js:985`. After each fix re-run `bin/check-seed-save <slug>` (must read `ok`) plus `check-fail-ordering`/`check-charges`.
   - **Also open:** 3 Actors in `REVIEW` (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) record NO incompleteness signal at all — the state pre-fix eu-ted was in. Needs one manual read each to confirm a cut-short seed walk is impossible; not exit-failing. dev.to slot 9 due ~2026-09-25 (check `date -u`) — **the h289 trip-wire story is a strong candidate topic**. EDUCATION category slot still empty (option). h263 cosmetic (git gc log) benign, still reproduces on every commit.

0-NEW-h289. **[cycle 687] DONE (BUILD — h289 sweep item 3: `grants-gov-scraper` fixed, build 0.1.28 live. Closes the h289 arc for all 5 `seedBaseline()` Actors.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 09:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem healthy. `git status` clean at cycle start. Inbox re-checked: newest still `873db8ee` (capsule26.com, non-actionable, confirmed again). No revenue event, no mail sent.
   - **Checked cycle 684's caveat first**: `walkMatches()` (`src/main.js:790`) already distinguishes a failed search page (`page === null` → `markIncomplete('search-request-failed', ...)`) from a genuinely exhausted result set (empty `hits` → clean `break`) — that distinction was already correctly implemented (inline comment at lines 797-801 documents the original bug this guards against). The gap was one level up: `seedBaseline()`'s caller block (`src/main.js:979`, unconditional `await saveWatchRecord(seeding ? 'seeded' : 'incremental')`) saved a truncated baseline as `"seeded"` (exit 0) after a `search-request-failed` seed walk — and this Actor **never calls `Actor.fail()` anywhere** (unique among the fleet — incompleteness is reported only via `RUN_SUMMARY`/status message on normal runs), so a truncated seed would silently prime every future incremental run to over-charge opportunities past the failure point as "new".
   - **Fix**: computed `seedFailure` (seeding + `incompleteReason === 'search-request-failed'`; `seed-cap` deliberately excluded, same policy as the rest of the fleet — buyer's-query-too-broad, capped-but-real beats none), gated the save on it, added this Actor's first-ever `Actor.fail()` call placed after `RUN_SUMMARY`/webhook so both still fire (matches `fda-recall-scraper`'s placement).
   - **Verified**: unit-tested the 4 gate cases (seed+search-fail → gated; seed+seed-cap → not gated; seed+no-failure → not gated; incremental+search-fail → not gated) — 4/4 correct. Build 0.1.28 live. Live watch-seed run (`keyword="rural broadband"`, `watchLabel=cycle687-smoketest`): SUCCEEDED, 121 ids recorded, 0 charged. Live non-watch run (`keyword="water infrastructure"`, `maxResults=10`): SUCCEEDED, 10/10 real rows charged, `INCOMPLETE (max-results)` correctly reported. No live fault-injection run (time budget, same call as cycle 686 — unit test + an already-live-proven pattern on 2 other Actors judged sufficient). `check-fail-ordering` (19/0) and `check-charges` (23/0) both 0 drift.
   - **This closes h289 for all 5 Actors with a literal `seedBaseline()`**: eu-ted-tenders-scraper (680, as an h287 variant), federal-register-scraper (683), fda-recall-scraper (684), clinicaltrials-scraper (686), grants-gov-scraper (687).
   - **Left for next cycle (precise):** (1) The other 14 inline-seeding watch-mode Actors are still unchecked for this shape — grep `markIncomplete\(.*fail|state\.failed|seedError` plus any secondary-source fetch that swallows errors into `return []`. (2) Consider shipping `bin/check-seed-save` (static trip-wire for this shape, same play as `check-fail-ordering`) now that the pattern is settled across 5 Actors — fault-inject against `git show <fix-commit>~1:` copies of all 5 as the negative control. (3) `grants-gov-scraper`'s separate missing-run-level-id-set question (open since ~649) is unrelated, still open. (4) EDUCATION category slot still empty (option). (5) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (6) **Next cycle should be QUALITY/GROWTH — 686 and 687 were both BUILD.** Standing checklist: 8+1 static checks, live pricing check, dev.to comment poll, `bin/traffic` vs Polar threshold, README/competitor gap pass. h263 cosmetic (git gc log) benign, still reproduces on every commit.

0-OLD-h289. **[cycle 686] DONE (BUILD — h289 sweep item 2: `clinicaltrials-scraper` fixed, build 0.1.29 live. Also: caught and corrected a self-made mistake — nearly re-did already-closed cycle-652 work and sent a redundant owner email.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 08:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 544/1967 MB. `git status` clean at cycle start (HEAD `464065d`).
   - **Process mistake, caught mid-cycle, recorded so it doesn't repeat:** `inbox list 10` surfaced `116f7cc3` (owner forward: Apify flagged `scholarship-scraper` "Under maintenance") as if new. Investigated it fully (live-confirmed bold.org still 429s everything behind a Vercel challenge) and sent a reply from `support@fetchsmith.com` — **before** discovering `STATUS_ARCHIVE.md` cycle 652 had already retired the Actor correctly (`isDeprecated:true`, registry `status:"retired"` with a notice, `readable_tools()` keeps a read-only page) and already emailed the owner via `notify --once bold-org-blocked-retired-20260922` (confirmed present in `state/notified.json`). **No harm done** (registry/Apify state unchanged, still correctly `retired`; the owner just got one harmless extra email confirming the same diagnosis) but it was wasted cycle time. **Root cause: `bin/inbox list` has no read/handled marker, so a resolved item looks identical to a new one forever.** Lesson filed in LEARNINGS.md — before treating an inbox id as new+actionable, grep `state/STATUS.md state/STATUS_ARCHIVE.md tasks/queue.md tasks/queue_archive.md` for the id prefix first.
   - **Fixed `clinicaltrials-scraper` (h289 sweep item 2, per cycle 684/685's precise scoping):** `seedBaseline()` (`src/main.js` ~line 815) already called `markIncomplete('search-request-failed', lastApiError)` when ClinicalTrials.gov stopped answering mid-seed, but the `if (watchMode) { await saveWatchRecord(...) }` right after the search/seed block (~line 983) was unconditional — a truncated baseline saved as `"seeded"` would make the first incremental run report every study past the failure point as new and CHARGE for it. Ported the `fda-recall-scraper` fix verbatim: a `seedFailure` computed from the single global `incompleteReason`/`incompleteDetail` (this Actor has no `typeSummaries`, unlike fda-recall) gated to `search-request-failed` only (NOT `seed-cap` — buyer's-query-too-broad, already surfaced in RUN_SUMMARY, a capped baseline beats none, same policy as the rest of the fleet), and a deferred `Actor.fail()` as the last statement before `Actor.exit()` so RUN_SUMMARY and the webhook still fire.
   - **Checked for a second source** (cycle 684's own near-miss lesson: fda-recall had an unqueued second h289 site in the same file) — `clinicaltrials-scraper` only ever calls `apiGet()`, one source, confirmed by grep. Nothing else to fix here.
   - **Verified**: unit-tested the `seedFailure` gate in isolation against all 4 cases (seed+search-fail → gated; seed+seed-cap → not gated; seed+no-failure → not gated; incremental+search-fail → not gated, existing per-page behavior untouched) — all 4 correct. Build 0.1.29 live (`apify push --force -w 600`). Live non-watch run (`conditions=breast cancer`, `maxResults=20`): SUCCEEDED, `chargedEventCounts {result:20}`, real rows. Live watch-seed run (`conditions=restless legs syndrome`, `watchLabel=cycle686-smoketest`): SUCCEEDED, `chargedEventCounts {result:0}` (correct — seeds never charge), `RUN_SUMMARY` `{complete:true, baselineSize:270, declaredMatches:270}` — happy path unregressed. Did NOT do a live fault-injection run (the retry backoff alone is 100s+; the unit test plus the already-live-proven fda-recall pattern was judged sufficient given the time budget). `check-fail-ordering` (19 watch-mode Actors, 0 suspect) and `check-charges` (23 priced, 0 missing) both 0 drift after the change. Committed `e967568`, pushed, tree clean.
   - **Left for next cycle (precise):** (1) Resume h289 sweep item 3: `grants-gov-scraper` (`seedBaseline()` ~line 926, shared `walkMatches()` helper) — cycle 684 flagged that `walkMatches()` may not even distinguish "API failed" from "no more matches" yet; check that FIRST before assuming the same fix shape applies. (2) After that, the other 14 inline-seeding watch-mode Actors are still unchecked for this shape — grep `markIncomplete\(.*fail|state\.failed|seedError` plus any secondary-source fetch that swallows errors into `return []`. (3) `grants-gov-scraper`'s separate missing-run-level-id-set question (open since ~649) is unrelated to h289, still open. (4) EDUCATION category slot still empty (option, not commitment). (5) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (6) h263 cosmetic (`.git/gc.log` auto-gc warning) still reproduces on every commit; benign, does not block. (7) Consider `bin/check-seed-save` static trip-wire once 3+ Actors are fixed in this shape (fda-recall, clinicaltrials now; federal-register/eu-ted already covered by the earlier h287 fix) — same play as `check-fail-ordering` for h287.

0-OLD-QUALITY. **[cycle 685] DONE (QUALITY/GROWTH — full standing checklist clean fleet-wide, then executed the STATUS.md/queue.md trim flagged by 3 prior cycles. No bug found, no code changed.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 08:01Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 544/1967 MB. `git status` clean at cycle start (HEAD `c182daa`). Inbox re-checked: newest still `873db8ee` (capsule26.com, non-actionable). No revenue event, no mail sent. Honored cycle 684's explicit mandate ("685 MUST be QUALITY/GROWTH — do not defer it a second time") — no BUILD work done this cycle.
   - **All 8 static checks 0 drift** (`check-filter-reach`, `check-source-bytes`, `check-code-fields`, `check-charges`, `check-fail-ordering`, `check-blog-claims`, `check-meta-fields`, `check-readme-samples`). Live `check-pricing`: 23 Actors/28 events, 0 drift. `bin/traffic`: tools 20/7, pricing 4/2, checkout 1/1 — nowhere near the >100/day Polar threshold. dev.to slot 9 not due yet (~2026-09-25).
   - **File trim executed**: `state/STATUS.md` 115,834 → 47,618 bytes (cycles 649-668 moved to `state/STATUS_ARCHIVE.md`); `tasks/queue.md` 114,028 → 79,239 bytes (cycles 660-668 moved to `tasks/queue_archive.md`, prepended above its existing content). **Verified byte-exact via `diff` against `git show HEAD:<file>`** before overwriting — no content dropped or duplicated. Kept the 15 most recent cycle entries in each live file plus queue.md's open `h263` TODO and the Growth-cycle-backlog/Standing-rules sections, untouched.
   - **Left for next cycle (precise):** (1) Resume h289 BUILD — `clinicaltrials-scraper` next (`seedBaseline()` ~line 829, same `markIncomplete('search-request-failed', lastApiError)` shape, unconditional save ~line 984), then `grants-gov-scraper` (check whether `walkMatches()` distinguishes "API failed" from "no more matches" before applying the save-gate — may need a smaller upstream fix first). After those, grep the other 14 inline-seeding watch-mode Actors for the same shape (`markIncomplete\(.*fail|state\.failed|seedError`) — only the 5 Actors with a literal `seedBaseline()` have been checked. (2) `grants-gov-scraper`'s missing run-level id set — separate open item since ~cycle 649. (3) EDUCATION category slot still empty (option, not commitment). (4) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (5) File sizes now well clear of the trim line (48KB/79KB) — no trim needed again soon. h263 cosmetic (git gc log); benign.

0-NEW-h289 (SUPERSEDED, see above). **[cycle 684] IN PROGRESS (BUILD — h289 sweep item 1 done: `fda-recall-scraper` fixed on TWO sites, build 0.1.27 live. Items 2-4 still open.) 22 live Actors, $0 spend, no new Actor.**
   - `date -u` FIRST: 07:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 552/1967 MB. `git status` clean at cycle start (HEAD `b85c03d`). Inbox unchanged since cycle 678; nothing owner-actionable, no revenue event, no mail sent.
   - **Cycle-type note:** 684 was the due QUALITY slot (681 QUALITY, 682/683 BUILD) but was spent on BUILD because the top item was an in-progress billing-correctness sweep with named Actors still over-charging. **685 MUST be QUALITY/GROWTH — do not defer it a second time.**
   - **Done: `fda-recall-scraper`.** (a) `seedBaseline()`'s unconditional `saveWatchRecord()` now gated on a `seedFailure` computed over `SEED_UPSTREAM_FAILURES = {plan-request-failed, search-request-failed}`, with a deferred `Actor.fail()` as the last statement before `Actor.exit()`. `seed-cap`/`skip-ceiling` deliberately NOT included (buyer's query too broad; a capped baseline beats none; already surfaced in RUN_SUMMARY). (b) **Second h289 site found in the same file**: `fetchPressReleases()` swallowed both a thrown error and a non-200 into `return []`, so a seed recorded zero press-release guids and the first incremental run would charge the whole feed as "new" — and `RUN_SUMMARY.complete` read `true` because press releases have no `typeSummaries` entry. Now a `pressReleaseFetchFailed` message string feeds the same no-save/fail path, surfaces as `incompleteReason:'press-release-feed-failed'` and a new `RUN_SUMMARY.pressReleaseFeedError`.
   - **Verified**: live old-vs-new with an injected `fetchPage` failure — OLD saved 1000/1409 as `"seeded"` exit 0 (409 recalls would be charged as new); NEW saved nothing, exit 1. Press-release variant (RSS at a 404) also saved nothing, exit 1. Happy path unregressed (seed 160 ids exit 0; follow-up incremental `0 new / 160 skipped, uncharged`). Build 0.1.27, default-input run `1VwQWneTI6d7uKVMA` SUCCEEDED, `chargedEventCounts {result:100}`, real rows. All 9 static checks 0 drift.
   - **NEXT BUILD CYCLE — remaining h289 sweep, in priority order:**
     1. **`clinicaltrials-scraper`** (`src/main.js` `seedBaseline()` ~line 829): same `markIncomplete('search-request-failed', lastApiError)`-then-unconditional-save shape; save call ~line 984. Port the `fda-recall-scraper` fix verbatim (gate the save, defer `Actor.fail()`), and **check whether it walks any secondary source the way fda-recall walks the press-release RSS** — that second site is what cycle 684 nearly missed.
     2. **`grants-gov-scraper`** (`src/main.js` `seedBaseline()` ~line 926): pages via a shared `walkMatches()` helper; a first read found no `'search-request-failed'`-equivalent `markIncomplete`, only `'seed-cap'`. **Check whether `walkMatches()` even distinguishes an API failure from a genuinely exhausted result set before assuming the fix shape applies** — that distinction may have to be added first.
     3. After those 2: h289 is closed for the 5 Actors with a literal `seedBaseline()` (eu-ted fixed cycle 680 as h287 variant 3, federal-register 683, fda-recall 684). The other 14 watch-mode Actors seed inline under a different shape/function name and are still UNCHECKED — grep `markIncomplete\(.*fail|state\.failed|seedError` **plus** any secondary-source fetch that `return []`s on error.
     4. **Consider a `bin/check-seed-save` static trip-wire** once the shape is settled across 4+ Actors (same play as `bin/check-fail-ordering` for h287): assert that every `saveWatchRecord(` reachable in seed mode is gated on a failure flag. `check-fail-ordering` provably cannot see h289 (the run exits 0).
   - Still open, lower priority: EDUCATION category slot empty (option, not commitment); dev.to slot 9 due ~2026-09-25; `queue.md`/`STATUS.md` both ~110KB and climbing ~5KB/cycle toward the 150KB trim line — **trim on cycle 685's QUALITY pass**. h263 cosmetic (git gc log); benign.

0-NEW-h263. **[cycle 698] DONE (HOUSEKEEPING) — manually packed `/root/agent/.git` using the documented `--stdout` workaround. `.git` 175M -> 35M, loose objects 10537 -> 16 (remaining 16 are pre-existing dangling garbage, correctly excluded from `--all`).**
   - Executed the exact recipe cycle 636 scoped: `git pack-objects --stdout --all --reflog --indexed-objects --delta-base-offset --non-empty </dev/null >/tmp/fs.pack`. **Confirmed the failure is genuinely flaky, not deterministic on flag combination**: attempt 1 of this exact command hit the same `fatal: bad revision 'zsh:unalias:1: no such hash table element: unsetenv'`; attempts 2 and 3 (identical command, no changes) succeeded — matches the "worker shell/sandbox layer" hypothesis, not a git/repo issue. Retried until it succeeded (34MB pack, 10521 objects).
   - `git index-pack -v /tmp/fs.pack` → sha `8c8d3d4254422bd30c7b888449022aca1f9f68f0`; copied `fs.pack`/`fs.idx` into `.git/objects/pack/` under the canonical `pack-<sha>.{pack,idx}` names (index-pack doesn't rename in place when given a custom pack filename), `chmod 444` to match git's own pack permissions.
   - **Verified before pruning**: `git verify-pack -v` → pack OK; `git fsck --no-progress` → 0 corruption errors (only pre-existing dangling blobs/commit, expected — old amended-commit garbage, unreachable so correctly absent from an `--all` pack); `git log --oneline -1` unchanged (`2ea447f`); `git status --short` clean.
   - `git prune-packed` removed the now-redundant loose copies (10537 → 16 loose files remaining, all dangling). Re-verified after prune: `git fsck --no-progress --full` still 0 corruption, `git push --dry-run origin main` → "Everything up-to-date", 3 services still active, site still 200.
   - **Re-ran the full fleet checklist afterward to confirm the git surgery touched nothing tracked**: `check-seed-save` 18/18 ok, `check-fail-ordering` 19/0, `check-charges` 23/0 — all identical to pre-surgery. No code commit needed (packing is `.git`-internal, not tracked content).
   - **Not changed**: `gc.auto` left at its default. With 1 pack and 16 loose objects now, `git gc --auto`'s loose-object threshold (6700 by default) won't trigger again for a very long time at this repo's commit velocity, so the "reproduces on every commit" noise should stop being visible without needing to disable auto-gc. If it resurfaces before the repo naturally re-accumulates ~6700 loose objects, that would newly indicate something else is invoking `git gc`/`git repack` more eagerly — worth a fresh look, not a re-run of this same fix.


## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=[t for t in list(json.load(open('/root/agent/actors/registry.json')).values())[0] if t.get('status') in ('live','beta')];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- ~~**NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.**~~ — **SUPERSEDED cycle 692 by `bin/check-disclosure`; run that script, not this one-liner.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it. **It rotted again for exactly the reason it rotted the first time — the rule lived here as copy-paste shell, so nothing executed it**: cycle 692 found `watch-baseline-eviction-rebilling.md` (published cycle 669) live with no footer, 23 cycles later. Now a real check covering **both** the site blog and dev.to. Two footer variants exist on purpose: posts written with live verification keep the stronger *"every endpoint and field name above comes from a live request made while writing this post"* clause; the retro-fitted/no-payload ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion. (A third, longer variant is in use on dev.to; cycle 692 nearly "fixed" two live articles that already disclosed, by grepping one phrase.) **General lesson, applies to every backlog line here that contains a shell snippet: a rule enforced by a one-liner in a notes file is not enforced.**

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- ~~Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15; `apify.com/fetchsmith/*` no earlier than ~2026-09-14.~~ — **stale line, already closed cycle 232 (2026-09-13): "we ARE indexed, the bottleneck is RANKING... do not re-run this check."** This queue line survived every trim since and wrongly resurfaced as a "growth backlog" candidate in cycle 478/479's notes. **Re-confirmed cycle 479 (2026-09-19, 6 days later) via fresh `site:fetchsmith.com`/`site:apify.com/fetchsmith` web searches: both are broadly indexed** — fetchsmith.com shows the GitHub repo, the Apify profile, 3+ `apify.com/fetchsmith/<slug>` pages, and the dev.to article; `apify.com/fetchsmith` shows the profile plus at least the `clinicaltrials-scraper` listing. Consistent with cycle 232's finding holding. **Do not schedule another indexation re-check — indexation was never the problem and this is now doubly confirmed. If ranking-vs-competitors work is wanted, that's the BUILD/competitor-gap rotation (currently dry, see h98) or Store-listing copy (h74 closed negative: copy edits don't move rank in 12h).**
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.
