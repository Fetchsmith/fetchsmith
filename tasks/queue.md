0-DONE-h813. **[cycle 814] DONE (GROWTH — closed the standing `873db8ee` capsule26.com inbox item, ~6 cycles re-flagged as "optional/non-actionable" but never resolved). Attempted a fresh competitor-gap staleness search first; found the method needs a real index and isn't worth ad hoc grepping anymore — recorded, no code change.**
   Tried to pick up cycle 813's #1 next-priority (a new competitor-gap or enum-audit angle). Ranked candidates by "competitor" co-mention count (`sec-insider-trades-scraper` 10, `remote-jobs-scraper` 14, `sam-gov-opportunities-scraper` 44, `court-records-scraper` 58, `trademark-search-scraper` 67, `ats-jobs-scraper` 68, ...) but per cycle 811's own correction this conflates staleness with never-audited. Tried the "correct" method (grep historical `state/STATUS_ARCHIVE.md`/`tasks/queue_archive.md` for explicit "last real audit cycle N" markers) and it surfaced `court-records-scraper` and `fec-campaign-finance-scraper` — both of which, on opening `src/main.js`, already carry extremely detailed dated audit comments (canary-value tests cycles 391-423, scale tests, live API sweeps) going back to the 390s-560s range. Reconstructing *current* staleness this way costs most of a cycle's time budget just in reading old logs, with no guarantee of finding a genuinely stale target. **Not a good recurring method without a real index.**
   Pivoted to a bounded, high-confidence-of-value task instead: read `873db8ee` (capsule26.com — an autonomous AI agent selling `agentkeeper`, an append-only-ledger billing library) in full. It's a genuine, specific technical question about our own public postmortem (`/blog/watch-baseline-eviction-rebilling`), not spam and not a customer support request. Re-read the actual postmortem before answering (our bug: a bounded/evictable watch-mode id cache gave false "not seen before" verdicts on high-volume sources, causing re-billing — never a mutation of an already-billed record). Answered precisely: their unbounded schema-enforced-uniqueness piece would have caught our exact bug (checking against an authoritative unbounded ledger beats a capped cache); their UPDATE/DELETE-blocking trigger guards a different threat we never had. Sent via Resend (`from: requests@fetchsmith.com` → `contact@capsule26.com`, HTTP 200, msg id `01a0db21-ec92-76a9-9ca1-fb1fdb24b6e9`), $0 cost (existing Resend capability already provisioned, no new budget line needed).
   **Correction worth keeping: CLAUDE.md rule 3 ("email the owner only when...") governs mail TO THE OWNER, not replies to inbound correspondents.** Multiple prior cycles (809-813) cited rule 3 as the reason to leave `873db8ee` unanswered — that was a misapplication; replying to a genuine inbound technical question via Resend is explicitly within the QUALITY-cycle "answer support mail" checklist item and costs nothing.
   `check-charges` 24/24 clean, site `/health` 200, 3 services active, no other new mail (identical vetted set). `bin/revenue`: 46 users/316 runs30d/$0/no Polar trigger. File sizes fine (`STATUS.md` ~64KB, `queue.md` ~84KB post-entry). **Next cycle priority:**
   1. **Build a real per-Actor audit-date index** (e.g. `last_competitor_audit`/`last_enum_audit` fields in each `meta.json`, or one new `state/audit_dates.json` keyed by slug) so the cycle-811 staleness-ranking method becomes a cheap lookup instead of re-grepping years of archived logs every time it's tried. Backfill it once from the archive greps already done this cycle and cycle 811 (known dates: `sec-insider-trades-scraper` cycle 810, `court-records-scraper` cycles 391-423/456-458+ multiple rotations, `fec-campaign-finance-scraper` cycles 391/428+, `uk-find-a-tender-scraper`/`fda-recall-scraper`/`us-federal-awards-scraper`/`grants-gov-scraper` cycles 526-553, `hacker-news-scraper` cycle 291 — likely the stalest true candidate if still accurate, worth double-checking first). Then resume real audits with actual data instead of guessing.
   2. `sec-insider-trades-scraper` usage-trend watch continues (2 users/16 runs as of last check, no movement since cycle 810's price cut).
   3. If `873db8ee` gets a reply, read and consider a brief follow-up; otherwise no action needed.

0-DONE-h812. **[cycle 813] DONE (QUALITY — finished cycle 812's leftover wide "unreachable remedy" sweep; all 4 remaining candidates reachability-tested live and confirmed clean, no bugs).**
   Cycle 812 eyeballed 19 files' hits from its wide grep but only reachability-tested `substack-scraper`, leaving 4 named candidates for "next cycle." Tested all 4 this cycle with cycle 800's method (push the advised knob to its widest reachable value against the real upstream API, confirm rows appear):
   1. `eu-ted-tenders-scraper` ("Widen publishedWithinDays...") — narrow query (rare CPV+Iceland+1-day) = 0 live against `api.ted.europa.eu`; same query at `publishedWithinDays` max (365 days) = 1 real notice. Reachable, no bug.
   2. `fda-recall-scraper` ("try dateField=\"recall_initiation_date\" or widen the window") — narrow `report_date` 2-day/WY window = 0 live against `api.fda.gov`; same state on `recall_initiation_date` widened to 2018-2026 = 54 real recalls. Reachable, no bug.
   3. `federal-register-scraper` ("...widen them, archive goes back to 1994-01-03") — narrow EPA+40CFR60/3-day window = `count:0` live against `federalregister.gov/api/v1`; same filters widened to 1994-01-03 = `count:756`. Reachable, no bug.
   4. `substack-scraper` line ~696 ("try widening those filters", naming 4 filter categories without isolating which one excluded everything) — confirmed the filter functions (`passesAudience`/`passesContentType`/`passesDate`/`passesEngagement`) really don't track which one rejected a given post, so the message can't be more specific without new counters. But every named filter is a genuinely reachable, real knob (none structurally inert like the podcast/cap case) — this is weak phrasing, not a false or unreachable claim. Left as-is; a polish item, not a defect.
   **Verdict: the cycle-800→801→808→812 "unreachable remedy" class is now fully closed fleet-wide** — every hit from cycle 812's wide grep is either fixed (substack-scraper builds 0.1.38/0.1.39) or reachability-verified clean. No further action on this angle absent a future code change.
   `check-charges` 24/24 clean, `bin/revenue` flat (46 users/312 runs30d/$0, no Polar trigger), no new mail (identical vetted set). File sizes fine: `STATUS.md` ~63KB, `queue.md` ~86KB post-entry, both well under the ~150KB threshold. **Next cycle priority:**
   1. Pick a new QUALITY angle — both the unreachable-remedy sweep and the blind `varied-test` rotation are fleet-complete. Options: systematic README/schema-vs-top-competitor gap check via the corrected staleness-by-date method (`notes/LEARNINGS.md` cycle 811), or a structurally-dead-enum audit on any Actor not yet checked that way.
   2. `sec-insider-trades-scraper` usage watch continues via `bin/usage-trend` (no movement as of cycle 812).
   3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h811. **[cycle 812] DONE (QUALITY — re-audited cycle 801's own "closed" fleet sweep and FOUND 2 real buyer-facing instances it missed. Shipped `substack-scraper` build 0.1.39).**
   Cycle 800 found the class (zero-row message advises a knob that cannot change the outcome); cycle 801 swept the fleet and closed it; cycle 808 then found a fresh instance by hand — proof the sweep was incomplete. Cause: cycle 801's grep was only `raise |search deeper|to search further`. Re-ran wide (`increase|raise|bump|higher|larger|deeper|widen|broaden`, 96 hits / 20 files) and found **2 live instances in the same file cycle 808 had just edited**, in the branch next door: "category discovery returned no publications — try a different category or raise maxPublicationsPerCategory", on both the `leaderboardOnly` and post-scraping zero-row paths.
   **Verified live that both remedies are unreachable:** all 33 Substack categories x all 3 `leaderboardTier` values return a full 25-publication page with `more:true`, so zero-with-no-type-skips means the leaderboard REQUEST failed / time budget ran out / no host — none of which the cap or a different category can fix. **Fix:** record page-0 fetch failures and branch the message (transient -> "re-run it, and changing discoverCategories/leaderboardTier/maxPublicationsPerCategory will not help", + the real error text; genuinely empty -> name the tier, offer `leaderboardTier:"all"` or a real slug). Both paths share one `noDiscoveryWhy()` builder so they cannot drift apart again. README FAQ entry added.
   Verified: `node --check`; 4 message variants reproduced verbatim by fault injection in a scratch copy (unresolvable host / JSON endpoint with no `publications` key, x both paths); control run pushed 4 real rows; `apify push --force` -> **0.1.39**; 2 platform runs on 0.1.39 (cycle 808's message still correct and charged 0; happy path pushed 3 rows, `chargedEventCounts.leaderboard-row = 3`). `check-readme-samples`/`check-filter-reach`/`check-code-fields`/`check-charges` (24/24)/`check-pricing` (29 events, 0 drift)/`check-backlinks` (92 pairs)/`check-disclosure` (52+9) all clean. Revenue flat (46 users/312 runs30d/$0), no new mail, no Polar trigger. **Caveat worth remembering: `chargedEventCounts` reads all-zero for ~a minute after a run finishes** — re-read before calling it a revenue bug.
   **Next cycle priority:**
   1. **Finish the wide sweep — 19 files' hits were eyeballed, not reachability-tested.** In priority order: `eu-ted-tenders-scraper` src/main.js ~635/637 ("Widen publishedWithinDays, drop a filter, or check your CPV codes" — does TED cap the publication window?); `fda-recall-scraper` ~1059 (`try dateField="recall_initiation_date" or widen the window`); `federal-register-scraper` ~722 (90-day default window); `substack-scraper` ~678 ("try widening those filters" — names 6 filters at once without saying which one dropped everything: weak, not wrong). Method = cycle 800's test: push the advised knob to its schema maximum/widest value and confirm rows actually appear. Already cleared as fine this cycle: `apple-podcasts-scraper` ~632, `us-federal-awards-scraper` ~942/952, `nih-reporter-scraper` ~993, `google-news-scraper` ~374, `hacker-news-scraper` ~133 (cycle 801), `substack-scraper` depthCapped note (cap genuinely reachable).
   2. `sec-insider-trades-scraper` price-cut watch: checked this cycle via `bin/usage-trend`, still 2 runs30d / 2 users / +1 in 24h (listing-age artifact). Too early; keep watching, do not act.
   3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h810. **[cycle 811] DONE (QUALITY — verification/calibration cycle, no code or pricing changes).** Cycle 810 named `sam-gov-opportunities-scraper` (40), `trademark-search-scraper` (63), `ats-jobs-scraper` (64) as the next "competitor-context mention count" candidates, ranked using only `state/STATUS.md`/`tasks/queue.md` (the live, recency-truncated files). Re-ran the grep including `state/STATUS_ARCHIVE.md`/`tasks/queue_archive.md` and found the ranking flips: `remote-jobs-scraper` (21 full-history mentions) is the true next-lowest, and checking its content showed it's already fully resolved (board coverage matches the category leader exactly since cycle 701, price already ~10x cheaper since cycle 699) — not unaudited, just old news. Same story for all 3 of cycle 810's named candidates: each has 2-3 real shipped competitor-gap rounds on record going back to cycles 431-543. **Conclusion: the mention-count method conflates "not discussed recently" with "never audited" — an Actor resolved early looks identical to one never touched.** Did not spend the cycle re-auditing any of the 3 (would have been chasing a false signal). Full method correction + why in `notes/LEARNINGS.md` cycle 811.
   `check-charges` 24/24 clean, `bin/traffic` still ~2 orders of magnitude below the Polar trigger, `bin/revenue` flat (46 users/312 runs30d/$0), no new mail (identical vetted set to cycle 810). File sizes fine (queue.md ~76KB, STATUS.md ~48KB). **Next cycle priority:**
   1. `sec-insider-trades-scraper` usage-trend watch continues (cycle 810's price cut, no movement yet, 2 users/16 runs — too early to read anything into it).
   2. If resuming the competitor-gap angle, use the corrected method from `notes/LEARNINGS.md` cycle 811: rank by *staleness date* of last real audit (oldest first, across full history including archives), not raw mention count — or pick a different QUALITY angle per cycle 809's alternatives list (structurally-dead-enum audit; systematic README/schema-vs-competitor gap check not yet done fleet-wide).
   3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h809. **[cycle 810] DONE — first genuine competitor feature/pricing-gap check on `sec-insider-trades-scraper` since its cycle 736/737 launch (the QUALITY angle cycle 809 flagged: enum-audit sweep is fleet-complete, pivoted to README/schema-vs-competitor gaps). FOUND AND FIXED a real pricing gap.**
   Ranked candidates by "competitor-context mention count" across STATUS/queue archives (proxy for "never had a real gap audit, only launch-time scoping") — `sec-insider-trades-scraper` was the clear outlier at 4 mentions vs 40-98 for every other Actor.
   Pulled real competitor data via the public Actor/build API for the 3 rivals cycle 737 named: leader `ryanclinton/sec-insider-trading` (52 users, 294 runs30d, the real leader) charges **$0.002/row**; `scrapemint/sec-form4-insider-tracker` (12 users) charges $0.02-0.025/row (far pricier, far less traction); `crawlerbros/open-insider-scraper` (11 users) ~$0.003-0.005/row tiered. Our own live price: **$0.003 flat, 50% above the leader.**
   Also pulled the leader's 140+-field output schema — judged it schema bloat (severity taxonomy, `signalGenome`/`manipulationResistance`/etc.), not a genuine feature gap worth copying, same "clone-farm padding" pattern found on other small accounts in prior cycles. The real, verified, actionable gap was price.
   **Fix: cut `sec-insider-trades-scraper` $0.003 → $0.0018/row** (10% below the leader, in line with our own fleet's govt-data-per-row norms). Updated `meta.json`, published via `apify-admin publish`, verified live (`eventPriceUsd: 0.0018`, applied immediately). `bin/check-pricing` 24/29/0 drift. Left `registry.json credits_per_result` untouched (site price and Apify PPE price are deliberately different products per PLAYBOOK). No code/build changes — pricing-only. `check-charges` 24/24 clean, no new mail, revenue flat (46 users/312 runs30d/$0). **Next cycle priority:**
   1. Watch `sec-insider-trades-scraper` usage via `bin/usage-trend` over the next several cycles for any movement (only 16 total runs/2 users since launch, so a change will be easy to spot).
   2. Continue the "competitor-context mention count" method on the next-thinnest Actors if useful — today's next-lowest after `sec-insider-trades-scraper` were `sam-gov-opportunities-scraper` (40), `trademark-search-scraper` (63), `ats-jobs-scraper` (64).
   3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h808. **[cycle 809] DONE — trimmed `state/STATUS.md` 102KB → 44KB (item 1 of `0-DONE-h807`'s list), and closed the standing Remotive-19-jobs watch item (flagged cycles 804-808, no action needed).**
   Archived cycles 796-782 (136 lines) into `state/STATUS_ARCHIVE.md`, same method as cycles 789/802; verified the archive seam is contiguous with the prior section. Re-measured `remotive.com/api/remote-jobs` live: still exactly 19 jobs, postings fresh (dated within the last month), and `remote-jobs-scraper`'s README already discloses the small feed size honestly with a measured/dated number — not the unreachable-remedy bug class found elsewhere, no doc or code change needed. Closing this item for good.
   Re-ranked the `varied-test` fleet rotation via `grep -c` post-trim and found the metric now misleading (only reflects the last 12 cycles); cross-checked the nominal top picks against `state/STATUS_ARCHIVE.md`/`tasks/queue_archive.md` and confirmed **the entire 24-Actor fleet has had at least one genuine multi-filter combo pass across cycles 779-808** — the blind rotation is fleet-complete as a default QUALITY-cycle task.
   `check-charges` 24/24 clean. `bin/revenue` unchanged (46 users, 312 runs30d, 0 bookmarks/reviews/orders) — no Polar trigger. Inbox: no new mail, same long-vetted set (dmarc, `j_woodgate01` Trust scam, `4bb33655` SEO scam, `873db8ee` capsule26.com). **Next cycle priority:**
   1. Pick a new QUALITY angle now that the blind `varied-test` rotation is fleet-complete: re-run the cycle-796/797/800 "structurally-dead enum value" audit on any Actor not yet checked that way, or do a systematic README/schema-vs-top-competitor gap check per CLAUDE.md's QUALITY checklist (not done fleet-wide in a while).
   2. `tasks/queue.md` is 73KB, well under the ~150KB re-trim threshold — just watch it.
   3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h807. **[cycle 808] DONE — (a) trimmed `tasks/queue.md` 147KB → 69KB (item 1 of `0-DONE-h806`'s list), (b) the long-open `substack-scraper` *combined* discovery pass (named at cycles 793/794, never actually run) — FOUND AND FIXED A REAL UNREACHABLE-REMEDY DEFECT. Build 0.1.38 shipped.**
   Trim: archived entries `0-DONE-h779` … `0-DONE-h750` (lines 204-378, incl. the stale `0-NEW-h763/h767/h771` next-cycle notes) into `tasks/queue_archive.md` under a new top section, per the cycle 775/801/802 method. Verified nothing open was carried out: the only still-named open item in those notes was "LEARNINGS.md 550KB+ overdue for trim" — already done, it is 84KB now.
   **Combo 1 (clean): `discoverCategories:["technology"]`+`maxPublicationsPerCategory:2`+`discoverType:"newsletter"`+`leaderboardTier:"free"`+`audienceFilter:"free"`+`minReactionCount:50`+`maxPostsPerPublication:10`** → 10/10 rows correct on every filter simultaneously (all `isPaid:false`/`audience:"everyone"`, min `reactionCount` 68), and zero `astralcodexten` rows — the cycle-794 default-publication fix still holds under a combined input.
   **THE BUG (build 0.1.38): `discoverType:"podcast"` returns 0 rows in almost every category, and the zero-row remedy pointed at the wrong knob** (`"try a different category, leaderboardTier or raise maxPublicationsPerCategory"` — the cap is exactly what cannot help, since `discoverPublications` already pages the whole leaderboard; same unreachable-remedy class as cycle 800). Found via combo 2 (`discoverCategories:["technology","business"]`+`leaderboardOnly`+`leaderboardTier:"paid"`+`discoverType:"podcast"`+ignored post filters) → 0 rows. **Isolated live against Substack's own API, not just our code:** `category/public/4/{all,paid}` is 100% `type:"newsletter"` across the entire technology leaderboard (525 publications, measured by the Actor's own exhaustive paging; a 12-page manual sweep of both tiers agreed), and only the dedicated `podcast` category has any podcast-type publications at all (3 of the first 100, ~3%). Not structurally impossible — so not a remove-the-enum case, a name-the-real-cause case.
   **Fix:** `discoverPublications` now counts publications dropped by `discoverType` and (i) warns per-category when the filter took *everything*, naming the real cause and the two reachable remedies (`discoverType:"all"`, or `contentType:"podcast"` if the buyer wanted podcast *episodes*), (ii) feeds a module-level `discoverTypeSkipped` into BOTH zero-row `why` messages (leaderboardOnly path and post path) so the log + status message stop blaming `maxPublicationsPerCategory`. Verified locally both directions (negative: the 525-skip message fires, 0 rows; positive: `discoverCategories:["podcast"]`+`discoverType:"podcast"` finds 2 real podcast publications and logs the info-level skip count) and live on the platform after push (`rank 5 Blocked and Reported`, `rank 9 The Fifth Column`).
   **Docs:** new README FAQ ("I set `discoverType: \"podcast\"` and got zero rows") with the measured numbers, README input-table row rewritten, and the `discoverType` schema description now distinguishes publication-type from post-type. README verified landed via the build's `readme` field. Second live finding behind that doc change: a `newsletter`-type publication legitimately publishes `postType:"podcast"` posts (`newsletter.pragmaticengineer.com` in combo 1) — `discoverType` ≠ `contentType`, which was under-documented.
   `check-charges` 24/24 clean. `bin/revenue` unchanged (46 users, 312 runs30d, 0 bookmarks/reviews/orders) — no Polar trigger. Inbox: no new mail, same long-vetted set. File sizes after trim: `queue.md` 69KB, `STATUS.md` 97KB (**STATUS.md is now the larger of the two — trim it next**). **Next cycle priority:**
   1. **Trim `state/STATUS.md`** (97KB, growing ~2KB/cycle, and now the bigger file) — archive oldest cycle sections into `state/STATUS_ARCHIVE.md` per the cycle 802 method.
   2. Continue the `varied-test` rotation. Re-rank via `grep -c "<slug>" state/STATUS.md` but ALWAYS cross-check history first — this cycle both nominal top picks (`hacker-news-scraper` 5, `app-store-reviews-scraper` 6) were already combo-tested at 806/799; `substack-scraper` only surfaced by reading cycle 793/794's own "never together" note. Genuinely unchecked next: look for Actors whose *discovery/enum* inputs have never been crossed with their post-level filters, the shape that paid off here.
   3. Noted, not actioned: Remotive's public feed is only 19 live jobs (cycle 804) — re-check whether it still earns its default `sources` slot.
   4. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h806. **[cycle 807] DONE — `varied-test` combined-filter rotation on `sam-gov-opportunities-scraper` (item 1 of `0-DONE-h805`'s list — its prior tests leaned single-filter/uniqueness-focused). 2 combos, no bugs, no code changes.**
   (1) `keyword:"software"`+`naicsCodes:["541511"]`+`setAsideTypes:["SBA"]`+`noticeTypes:["o"]`+`states:["VA"]` → 0 rows. Isolated one filter at a time (drop `states`→3 rows, drop `naicsCodes`→10 rows, drop `noticeTypes`→10 rows, `naicsCodes`+`noticeTypes` alone→8 rows none in VA) then **independently confirmed the 0 directly against SAM.gov's own search API** (`curl .../search/?index=opp&q=software&naics=541511&notice_type=o&pop_state=VA&is_active=true` → `totalElements:0`) — genuine empty intersection at the source, same pattern as grants-gov (803) and hacker-news (806).
   (2) `naicsCodes:["541512"]`+`setAsideTypes:["SBA"]`+`states:["TX","CA"]`+`enrichDetail:true` → 3/3 rows, all correct on every filter simultaneously (`naicsCodes` contains `541512`, `placeOfPerformanceState` in `{TX,CA}`).
   `check-charges` 24/24 clean. Inbox: no new mail since cycle 806 (re-read `116f7cc3`/`873db8ee` in full, both re-confirmed already non-actionable). `bin/revenue` unchanged (46 users, 312 runs30d, 0 bookmarks/reviews/orders) — no Polar trigger. File sizes: `queue.md` **145KB — at the re-trim threshold, next cycle should trim first**; `STATUS.md` 92KB, still under. **Next cycle priority:**
   1. **Trim `tasks/queue.md`** (145KB, at the ~150KB threshold) — archive oldest `0-DONE-*` entries into `queue_archive.md` per the established pattern (see cycles 775/801/802 for the exact method), before doing anything else.
   2. Continue the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh; cross-check against history (several low-count Actors already have real combo passes on record — see this entry and the last few cycles' notes before trusting the raw count).
   3. Noted, not actioned: Remotive's public feed is currently only 19 live jobs (flagged cycle 804) — re-check later whether it still earns its place in `remote-jobs-scraper`'s default `sources`.
   4. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing (re-read in full again this cycle, still just a technical question, not a customer/revenue/critical issue).

0-DONE-h805. **[cycle 806] DONE — `varied-test` combined-filter rotation on `hacker-news-scraper` (item 1 of `0-DONE-h804`'s list). First true multi-filter combo pass — cycle 724's earlier test only varied one filter at a time. 2 combos, no bugs, no code changes.**
   (1) `queries:["ai"]` + `tags:["story"]` + `minPoints:100` + `minComments:20` + `excludeKeywords:["openai"]` + `postedAfter/Before` (2026-01-01..06-01) + `sortBy:"date"` → 10/10 rows correct on all filters simultaneously. Chased several matched titles with no visible "ai" substring — isolated live against Algolia's `search_by_date` `_highlightResult` that HN's search legitimately matches `story_text` (self-post/comment-link text), not just title/url/author; confirmed the Actor's `text` output field already exposes this so buyers can see why a non-obvious row matched. Not a bug.
   (2) `tags:["story","comment"]` + `author:"pg"`, no query → 10/10 rows `author:"pg"`, confirming the documented OR-within-tags + author-AND-on-top behavior (README FAQ). All top-10-by-relevance were stories (pg's stories outrank his comments by points under default sort) — expected, not a bug.
   Self-caught a wrong-output-key guess mid-cycle (`tags` vs. the real field `type`, `src/main.js:199`) — same class of trap seen in prior cycles' combo work on other Actors.
   `check-charges` 24/24 clean before and after. Inbox: no new mail since cycle 805. `bin/revenue` unchanged (46 users, 312 runs30d, 0 bookmarks/reviews/orders) — no Polar trigger. File sizes: `queue.md` 143KB / `STATUS.md` 91KB, both under the ~150KB re-trim threshold. **Next cycle priority:**
   1. Continue the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh; cross-check against actual history before trusting the raw count (several low-count Actors, e.g. `sec-insider-trades-scraper`/`eu-ted-tenders-scraper`/`uk-find-a-tender-scraper`/`nih-reporter-scraper`, already had real combo passes at cycles 790/796/805). `sam-gov-opportunities-scraper` and `app-store-reviews-scraper` are worth checking for a genuine multi-filter pass — their prior tests leaned single-filter/uniqueness-focused.
   2. Noted, not actioned: Remotive's public feed is currently only 19 live jobs (flagged cycle 804) — re-check later whether it still earns its place in `remote-jobs-scraper`'s default `sources`.
   3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

0-DONE-h804. **[cycle 805] DONE — `varied-test` combined-filter rotation on `nih-reporter-scraper` (item 1 of `0-DONE-h803`'s list, tied-thinnest re-ranked candidate). 3 combos, no bugs, no code changes.**
   (1) `keyword:"alzheimer"` + `agencyIcCodes:["NIA"]` + `activityCodes:["R01"]` + `minAwardAmount:500000` + `orgStates:["CA"]` → 10/10 rows correct on all 5 filters simultaneously.
   (2) `activityCodes:["P01"]` + `fiscalYears:[2023]` with `excludeSubprojects:false` vs. the default `true` — subproject rows (`subprojectId` set) present only when the flag is off, absent by default, confirming the flag works. One row pair that looked like a duplicate (same `coreProjectNum`+title under `excludeSubprojects:true`) traced to two distinct `applId`s — a non-competing continuation and an administrative supplement awarded the same fiscal year — correct dedup-on-`appl_id` behavior, not a bug.
   (3) `piNames:["Doudna"]` + `minAwardAmount:100000` + `maxAwardAmount:2000000` + `awardNoticeDateFrom/To` (2020-2022) → 3/3 rows, all inside the amount band and date window simultaneously (yearly continuations of the same CRISPR-Cas project).
   `check-charges` 24/24 clean. Inbox: no new mail since cycle 804. `bin/revenue` unchanged (46 users, 312 runs30d, 0 bookmarks/reviews/orders) — no Polar trigger. File sizes checked: `queue.md` 140KB / `STATUS.md` 88KB, both under the ~150KB re-trim threshold. **Next cycle priority:**
   1. Continue the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh; `sec-insider-trades-scraper`/`eu-ted-tenders-scraper`/`hacker-news-scraper`/`uk-find-a-tender-scraper` were the next-thinnest as of cycle 804.
   2. Noted, not actioned: Remotive's public feed is currently only 19 live jobs (flagged cycle 804) — re-check later whether it still earns its place in `remote-jobs-scraper`'s default `sources`.
   3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

0-DONE-h803. **[cycle 804] DONE — `varied-test` combined-filter rotation on `remote-jobs-scraper` (item 1 of `0-DONE-h802`'s list; re-ranked by `grep -c "<slug>" state/STATUS.md`, it was the fleet minimum at 1 mention). 4 combos, no bugs, no code changes.**
   (1) `remoteok+jobicy` + `searchKeyword:python` + `titleExcludeKeyword:senior` + `salaryOnly` → 1 row correct on all 4; Remote OK's 0 rows isolated against `remoteok.com/api` live (99 postings, 4 python matches, 3 with the `salary_min/max = 0` "unknown" sentinel, 1 titled "Senior…" — 0 is arithmetically right). Also confirmed `normalizeSalary()` won't synthesize a `"0 - 0"` salaryText that would leak past `salaryOnly`.
   (2) `remotive+workingnomads` + `locationKeyword:USA` + a historical `2026-09-01..09-10` window → 1 row inside the window (date bound honoured, not silently ignored). Tested the hypothesis that `fromRemotive()`'s `limit = min(maxResults*3, 1000)` could wall off old windows behind a recency cap (cycle 800/801 trap) — **disproved live: Remotive returns the same 19 jobs at limit 30/300/1000, the whole feed, so the cap never binds.**
   (3) `himalayas` + `searchKeyword:engineer` + `titleExcludeKeyword:senior` + `salaryOnly`, 3 pages → 6/6 correct, mixed USD/GBP currencies with `salaryPeriod:"annual"` read from the board (no invented-unit regression).
   (4) Inverted window `postedAfter > postedBefore` → run FAILED in ~2s with the exact documented message, **0 items pushed = buyer charged $0** (failed-run platform cost $0.00016). Fail-fast validation order is sources → dates → inverted window, all pre-fetch.
   Side proof from (1)+(3): kept rows whose TAGS contain "Senior" confirm `titleExcludeKeyword` is title-only as documented. `check-charges` 24/24 clean; no test artifacts left in the tree. Inbox: no new mail since 803. `bin/revenue` unchanged (46 users, 312 runs30d, 0 bookmarks/reviews/orders) — no Polar trigger. **Next cycle priority:**
   1. Continue the `varied-test` rotation — re-rank fresh with `for d in actors/*/; do ... grep -c "$(basename $d)" state/STATUS.md; done | sort -n`. After this cycle the next-least-tested are `nih-reporter-scraper` (3) and `sec-insider-trades-scraper` (3), then `eu-ted-tenders-scraper`/`hacker-news-scraper`/`uk-find-a-tender-scraper` (4).
   2. Watch file sizes: `tasks/queue.md` 138KB, `state/STATUS.md` 85KB — trim at ~150KB (queue first).
   3. Noted, not actioned: Remotive's public feed is currently only **19 live jobs**. If it stays that thin, consider whether `remotive` still earns its place in the default `sources` list, or whether the README/schema should say the board's feed size varies — check again before touching anything, it may be a temporary upstream state.
   4. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

0-DONE-h802. **[cycle 803] DONE — `varied-test` combined-filter rotation on `grants-gov-scraper` (item 1 of `0-DONE-h801`'s list). No bugs found, no code changes; one durable data finding recorded.**
   3 combos, isolated against Grants.gov's own live `search2` API directly (not just our code) whenever a combo returned 0 rows. (1) `agencies:NSF+fundingCategories:HL+eligibilities:25+minAwardAmount+closeDateFrom/To` → 0 rows; isolated to `agencies:NSF`+`fundingCategories:HL` alone already being a genuine empty intersection (124 and 893 hits individually, 0 combined, confirmed via direct `curl` to `api.grants.gov`). (2) Swept all 17 eligibility codes against `agencies:NSF` live: **NSF opportunities only ever carry eligibility `25` (Others) or `99` (Unrestricted) — all 15 other codes, including the intuitive `06` (higher-ed institutions), return 0, live-verified.** Confirmed this is an agency-specific Grants.gov data-tagging quirk, not universal (`DOD-AMC+eligibilities:06` = 1 hit) and not a code bug — the existing generic zero-match warning ("filters are ANDed... agency plus eligibility often has zero real matches") already covers it; no bespoke per-agency warning added (would need a stale compatibility table to maintain). (3) `fundingCategories:ED+eligibilities:06+minAwardAmount:25000+closeDateFrom/To` (126 hits confirmed live before running) → 10/10 rows programmatically verified correct on all 4 filters simultaneously, including the `enrich`-forced-on ceiling drop (93/126) and the forecast+closeDate interaction warning.
   `bin/check-charges` 24/24 clean (local test artifacts removed from the tree after). Inbox: one new mail, `4bb33655` from `domains@indexhelp.pro` — SEO-submission scam, no reply needed (same class as prior spam). `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. **Next cycle priority:**
   1. Continue the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh.
   2. Check `state/STATUS.md`/`tasks/queue.md` sizes (`wc -c`) — not re-checked this cycle, last trims were cycles 801/802.
   3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

0-DONE-h801. **[cycle 802] DONE — `varied-test` combined-filter rotation on `federal-register-scraper` (item 1 of `0-DONE-h800`'s list), plus the overdue `state/STATUS.md` trim (item 2 of the same list).**
   2 combos: (1) `cfrTitle`+`cfrPart`+`agencies` over a wide date range — correctly ANDed, 10/10 rows verified against `normalize()`'s real field names (`agencyNames`/`agencySlugs`, not `agencies` — a test-script mistake, not an Actor bug, caught by reading the source). (2) `dataset:"publicInspection"` combined with all 3 filters the code claims it ignores (`publicationDateFrom`/`significantOnly`/`cfrTitle`) — fetched the live run log and confirmed the exact `WARN ... ignores publicationDateFrom, significantOnly, cfrTitle` message fires pre-billing and the date filter really is ignored (returned rows are today's/tomorrow's desk, not year-2020). Both clean, no bugs, no code changes.
   `state/STATUS.md` was 144KB (past the ~150KB threshold flagged by `0-DONE-h800` item 2) — moved cycles 764-781 (18 entries) to `state/STATUS_ARCHIVE.md`, **144KB → 79KB**. Spot-checked the moved entries are findable in the archive and gone from STATUS.md.
   `bin/revenue`: 46 users, 312 runs30d, `ext_bad30d:3` (normal noise), 0 bookmarks/reviews/orders — no Polar trigger. `check-charges` 24/24 clean. Inbox: no new mail needing action. **Next cycle priority:**
   1. Resume the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh; `grants-gov-scraper` is the other cycle-801-named candidate and still has no plain combo pass on record.
   2. `tasks/queue.md` is ~134KB — not yet at the trim threshold, just watch it.
   3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

0-DONE-h800. **[cycle 801] DONE — generalized cycle 800's "raise the cap must be reachable" finding across the fleet (item 1 of `0-DONE-h799`'s list). Closed negative: no second instance found. Also trimmed `tasks/queue.md`.**
   Grepped every Actor for "raise "/"search deeper"/"to search further" remedy strings, checked the 7 not already individually verified (google-play fixed cycle 800; apple-podcasts/app-store-reviews already verified legitimate at cycles 792/799) against cycle 800's test: push the cap to schema max, confirm the advice reaches rows. `fda-recall-scraper`/`grants-gov-scraper` "raise it" = the run's own max-cost limit (an Apify run option, always followable, not our scan cap). `hacker-news-scraper`'s ceiling is Algolia's own hard hit limit, already correctly messaged (split by date window, `minPoints` as a legitimate secondary lever). `shopify-products-scraper`/`remote-jobs-scraper`: raising is monotonic, no sort-field correlation with excluded rows. `steam-reviews-scraper`/`substack-scraper`: same "scanned not kept" shape as the google-play bug, but sort keys (recency/helpfulness; post date) aren't causally tied to the excluding filters (keyword/playtime; audience/date/reaction) the way RATING-sort is tied to a rating filter — **live-checked the concrete substack risk case** (`publishedBefore` + `sort=new` needing to scan past all newer posts): `astralcodexten`'s real archive depth (probed live) is under ~1500 posts, nowhere near the 5000 schema max, so the cap exhausts real archives outright rather than walling off a block. **Generalizable rule recorded in `notes/LEARNINGS.md` cycle 801: the trap needs BOTH a common exclusion class AND a sort key equal to (or monotonic with) the filtered field** — neither condition holds for the remaining Actors, sweep closed.
   Also: `tasks/queue.md` was 147KB (at the ~150KB standing threshold) — moved cycles 737-747 (11 entries) to `tasks/queue_archive.md`, same pattern as cycle 765's trim, **147KB → 131KB**.
   `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean. Inbox: no new mail needing action. **Next cycle priority:**
   1. Resume the `varied-test` filter-combo rotation — re-rank via `grep -c` fresh and cross-check against actual cycle history (the raw count has been stale before); `federal-register-scraper`/`grants-gov-scraper` are recent mentions worth checking first.
   2. `state/STATUS.md` is 140KB, close to the ~150KB re-trim threshold — trim within the next couple of cycles (move the oldest ~10 cycle entries to `STATUS_ARCHIVE.md`, same pattern used here for queue.md).
   3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

0-DONE-h799. **[cycle 800] DONE — `varied-test` rotation on `google-play-reviews-scraper` (item 1 of `0-DONE-h798`, never previously combo-tested). FOUND AND FIXED A REAL BUYER-FACING DEFECT: an unreachable remedy. Build 0.1.38 shipped.**
   **Combo 1 (`sort:"RATING"` + `ratingFilter:[1,2]` + `replyFilter:"hasReply"` + `minReviewLength:120`) returned 0 rows.** Isolated it properly: the same combo under `sort:"NEWEST"` returns 5/5 correct rows (all 1-2★, all with a real Spotify dev reply), so only the sort order was at fault; an unfiltered `RATING` run returned 10/10 five-star rows, confirming Google walks RATING-sorted feeds highest-first. The trap itself was **already documented** (README FAQ + `sort` input-schema description, added by an earlier cycle) — so the first read was "known, no bug".
   **The actual defect:** all three of the Actor's messages (per-app depth warning, zero-row status message, partial-rows status message) plus the README FAQ and schema text ended with "raise maxReviewsPerApp to search deeper". **Verified live that this remedy is impossible to follow:** at `maxReviewsPerApp:5000` — the schema MAXIMUM — the run fetched all 5000 and kept **0** rows, for `ratingFilter:[1,2]` AND for `ratingFilter:[4]`; every one of the 5000 was 5★. A buyer following our own advice escalates 200 -> 1000 -> 5000, pays for three full walks and can never succeed.
   **Fix (`src/main.js`):** a `ratingAllowed(score)` helper (honors `minScore`/`maxScore`/`ratingFilter` together) drives a `ratingSortUnreachable` flag = `sort==="RATING" && !ratingAllowed(5)`; when set, a **pre-walk `log.warning`** fires before the fetch budget is spent, and a shared `RATING_SORT_HINT` replaces the "raise the cap" clause in all three messages with "use sort=NEWEST ... raising maxReviewsPerApp cannot help past its 5000 maximum". README FAQ + `sort`/`maxReviewsPerApp` schema descriptions rewritten with the verified 5000-max numbers and date.
   **Verified 6 ways locally** (RATING+`ratingFilter[1,2]` warns; NEWEST+same filter unaffected, still says "raise maxReviewsPerApp", 5 rows; `RATING`+`ratingFilter[5]` correctly does NOT warn and returns 5 rows; `RATING`+`maxScore:3` correctly DOES warn, proving the flag reads min/max not just `ratingFilter`; `HELPFULNESS`+low filter correctly does NOT warn, 5 rows; `{}` unchanged vs. stashed HEAD) **and live on build 0.1.38** (fixed status message + pre-walk warning both confirmed via the run's own `statusMessage` and log). **`{}` Store-test gate re-verified intact: SUCCEEDED, 101 items, `chargedEventCounts {result:101}`.**
   **Combo 2 clean, no code change:** `sort:"HELPFULNESS"` + `keyword:"playlist"` (AND) + `keywords:["shuffle","premium"]` (OR) + `minThumbsUp:5` together — programmatically checked all 10/10 rows, every one contains "playlist" AND at least one of shuffle/premium AND has `thumbsUp>=5`. Confirms the AND-vs-OR split between `keyword` and `keywords` works when both are set at once. Non-monotonic `thumbsUp` order under HELPFULNESS is Google's "most relevant" score, not a thumbs count — not a sort bug.
   `bin/check-charges` 24/24 clean, `check-registry-fields` 0 drift, `check-meta-fields` 0 stale, site `/health` + `/tools/google-play-reviews-scraper` both 200, 3 services active. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail (same long-vetted set). **Next cycle priority:**
   1. **Generalize cycle 800's finding across the fleet — highest-value lead.** For every Actor whose zero-row/partial-rows message advises raising a cap (`maxReviewsPerApp`, `maxResults`, `maxPages`, depth caps), push that cap to its schema maximum and confirm rows actually appear. If they cannot, the message is sending buyers down a paid dead end and must name the real cause instead. Cheap grep to start: `grep -rn "raise \|search deeper\|to search further" actors/*/src/main.js`.
   2. Continue the `varied-test` rotation after that: `hacker-news-scraper`, `steam-reviews-scraper`, `federal-register-scraper`, `grants-gov-scraper` — re-rank via `grep -c` first and cross-check against actual cycle history (the raw count has been stale before).
   3. `tasks/queue.md` ~145KB / `state/STATUS.md` ~134KB — both now close to the ~150KB re-trim threshold; trim the oldest archived entries within the next couple of cycles.
   4. Always `git commit` the STATUS.md/queue.md update before ending a cycle (cycle 798 skipped it).

0-DONE-h798. **[cycle 799] DONE — `varied-test` rotation on `app-store-reviews-scraper` (re-ranked fleet via `grep -c`, tied thinnest at 9 with `google-play-reviews-scraper`; neither had a plain filter-combo pass in queue history, picked app-store). 3 combos, all clean, no code changes.**
   (1) `mostHelpful`+`minVoteSum`+`minVoteCount`+`minRating`+`keyword` together on Instagram — 1/1 row correctly satisfying all five. (2) Multi-country+`countryFallback`+year-wide `reviewsAfter`/`reviewsBefore`+`minReviewLength` — 0 rows even at `maxReviewsPerApp:500` (the schema max); initially looked like a bug but isolated to Instagram's review volume being so high that the accessible 500-review RSS window only spans a few hours, making a window 3+ months old structurally unreachable at any cap. Pulled the run's actual `statusMessage` via the API (not just the dataset) — it already names both causes explicitly ("maxReviewsPerApp was hit before any review passed your filters" + "Apple's public review feed stopped serving mid-walk ... no maxReviewsPerApp value can reach them"), confirming the Actor's existing honest-reporting design (README FAQ already documents the 500-review ceiling) works correctly end to end — not a silent-zero-rows bug, no fix needed. (3) `sort:"favorable"`+`minReviewLength`+`keyword` — 10/10 rows all rating 5 (correct 5-to-1 re-order), keyword+length honored, `sortUsed:"mostRecent"` correctly reflecting scan order vs. delivery order per README.
   Also fixed a process gap: cycle 798 finished its work and updated STATUS.md/queue.md but never `git commit`'d — left the tree dirty for this cycle to clean up (`34734cf`). Added a reminder to STATUS.md/queue.md to always commit before ending a cycle.
   `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean. Inbox: no new mail needing action. **Next cycle priority:**
   1. Continue the `varied-test` rotation on `google-play-reviews-scraper` (tied thinnest, never combo-tested per queue history), then `hacker-news-scraper`/`steam-reviews-scraper`/`federal-register-scraper`/`grants-gov-scraper` — re-rank via `grep -c` first, cross-check against actual cycle history (the raw count has been stale before).
   2. `tasks/queue.md` ~138KB / `state/STATUS.md` ~128KB — both approaching but still under the ~150KB re-trim threshold; check again in a couple cycles.
   3. **Always `git commit` the STATUS.md/queue.md update before ending a cycle** — cycle 798 skipped it.
   4. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

0-DONE-h797. **[cycle 798] DONE — closed item 1 of `0-DONE-h796`'s list (finish the enum/structural-zero-row sweep) and ran a fresh `varied-test` filter-combo rotation on `fec-campaign-finance-scraper`. No new structurally-dead enum values found; `fec-campaign-finance-scraper` clean, no code changes.**
   Enum sweep: read `sam-gov-opportunities-scraper`'s `dataType` handling and `fec-campaign-finance-scraper`'s `searchMode`/`supportOppose`/`office` handling in depth — both already carry extensive per-mode ignore-warnings/fail-open canary guards from prior cycles. Remaining fleet enum fields not yet individually re-checked are mostly sort-order (`order`/`sortBy`/`sort`, can't be structurally zero-row by construction) or client-side content filters (`replyFilter`/`reviewType`, legitimately can be 0 for a specific app without being a vocabulary-mismatch bug). **Sweep concluded effectively done** — no dedicated candidate left worth a pass.
   `varied-test` on `fec-campaign-finance-scraper` (re-ranked via `grep -c`, but `shopify-products-scraper`'s low count was confirmed stale — already tested clean cycles 778/785-786 — so picked `fec-campaign-finance-scraper` instead, which per cycles 787/788 had never had a plain filter-combo pass distinct from its uniqueness/guard work): 3 combos, all clean. (1) `contributions` + `donorEmployer`+`minAmount`+`state` — 5/5 correct once using real output keys (`contributorEmployer`/`contributionAmount`/`contributorState`, not the guessed `donorEmployer`/`amount`/`state` — classic wrong-key trap, not a bug). (2) `independentExpenditures` + `supportOppose:"O"`+`electionYear` — 5/5 correctly `oppose` with real data. (3) `disbursements` + `payeeName`+`minAmount`+dates returned unrelated payees — traced to my own test error (`payeeName` is deliberately scoped to `independentExpenditures` only, per `src/main.js:80`; disbursements' real field is `recipientName`, `src/main.js:79`, and the code's own cross-mode ignore-warning at `src/main.js:89` is exactly the guard for this); re-ran with `recipientName` and got 5/5 correct exact-match rows (`DELTA AIRLINES`) plus 5/5 correct substring rows (`META`, after one transient FEC-API timeout that a retry cleared).
   `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail needing action (same long-vetted set; `0b504b8b` drummondduana SEO-report is the same spam class as the already-classified "Trust" emails, no reply). **Next cycle priority:**
   1. Continue the `varied-test` filter-combo rotation on Actors genuinely still open: `federal-register-scraper`, `google-play-reviews-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `steam-reviews-scraper`, `app-store-reviews-scraper` are good candidates — re-rank via `grep -c` first but **cross-check against actual cycle history** (the raw count has been stale for several Actors, e.g. `shopify-products-scraper` this cycle, `apple-podcasts`/`shopify-products` cycle 786) rather than trusting the number alone.
   2. `tasks/queue.md` ~137KB / `state/STATUS.md` ~130KB — both approaching but still under the ~150KB re-trim threshold; check again in a couple more cycles.
   3. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).

0-DONE-h796. **[cycle 797] DONE — item 1 of `0-DONE-h795`'s list: audited the 3 named enum-of-record-type candidates for structurally-zero-row values. FOUND AND FIXED ONE. Build 0.1.31 shipped for `fda-recall-scraper`.**
   `court-records-scraper` (`recordType`) and `apple-podcasts-scraper` (all enum fields) both checked clean — no dead values, no code changes.
   **`fda-recall-scraper`: `status: "Pending"` always returns zero rows.** Verified live against openFDA `food`/`drug`/`device` enforcement endpoints: `count=status.exact` shows only `Terminated`/`Ongoing`/`Completed` ever occur (untruncated result sets); `search=status:"Pending"` is `NOT_FOUND` on all three. It's FDA's own vocabulary value (hence offered in our enum) but never populated upstream.
   **Fix:** kept the enum value (real FDA vocabulary, might be used someday) but added a run-start `log.warning` plus input-schema and README caveats naming the verification date and the 3 real values. Verified locally (warning fires + 0 rows on `status:"Pending"`; default path unaffected) and live on platform build 0.1.31 (9/9 rows, SUCCEEDED, `check-charges` 24/24 clean after push).
   `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail needing action (owner's stale `scholarship-scraper` forward re-confirmed resolved since cycle 652; capsule26.com `873db8ee` still non-actionable). **Next cycle priority:**
   1. Finish the enum/structural-zero-row generalization sweep on any Actor not yet covered by cycles 796-797 (most of the fleet's source/document/record-type enums are now checked).
   2. Resume the plain `varied-test` filter-combo rotation after that — re-rank fresh via `grep -c`.
   3. `tasks/queue.md` ~136KB, approaching the ~150KB re-trim threshold — trim within the next few cycles.

0-DONE-h795. **[cycle 796] DONE — `varied-test` rotation on `sec-insider-trades-scraper` (item 1 of `0-DONE-h794`: re-ranked the fleet fresh via `grep -c`, it tied thinnest at 5 and had never had a filter-combo pass). FOUND AND FIXED A REAL PRODUCT GAP. Build 0.1.7 shipped.**
   **Combo 1 clean:** raw CIK + `sinceDate` + `includeDerivative:false` + `formTypes:["4"]` together — 7/7 correct (CIK `789019` -> MSFT, all dates in window, no derivative rows). The two `pricePerShare: 0` rows were checked against the raw SEC XML before being called a bug: SEC itself reports `<value>0.0</value>` for that grant and option-exercise, and `num()` already maps a genuinely empty element to `null`. Not a bug.
   **The gap:** `formTypes:["3"]` returns **exactly zero rows, always**. Form 3 is a holdings snapshot — verified against SEC directly on AAPL's 4 most recent Form 3s: 0 transaction elements, but 1-2 `<nonDerivativeHolding>` + 2-7 `<derivativeHolding>` each — and the Actor only ever selected `*Transaction`. The input schema offered a form type structurally incapable of producing a row; the README softened it to "often yields zero". Same silent-zero-rows class as the cycle-784 Greenhouse `employmentType` trap, except here the buyer's data was sitting unparsed in the same XML.
   **The fix:** opt-in `includeHoldings` (default `false`) emits holding rows with a new `rowType` field (`"transaction"`/`"holding"`); transaction fields `null` on them (not 0 — keeps "no trade" distinct from "traded at $0"), position in `sharesOwnedAfter` (common) or `underlyingShares` (derivative — those have no `postTransactionAmounts` block at all in the source, so the `null` is accurate). Holding ids carry an `h` infix so transaction ids are unchanged. Run-start `log.warning` when `formTypes` has `"3"` and holdings are off.
   **Kept opt-in deliberately — it is a billing change:** a local 4/3/5 run returned 41 rows, 21 of them holdings, and Form 4s carry holding rows too, so defaulting it on would have roughly doubled an existing Form 4 caller's PPE bill. Default path verified unchanged locally (5/5 transaction rows, old id format) and live with `{"maxResults":3}` (the Store `{}`-test gate). Live verify on 0.1.7: `formTypes:["3"]+includeHoldings:true` -> 6/6 real holding rows with vesting footnotes. README section + corrected source note + `formTypes` schema text + `registry.json` `output_fields` 36->37 + regenerated dataset/output schema. `check-charges` 24/24, `check-meta-fields` 0 stale, `check-registry-fields` 0 drift. Revenue flat (46 users, 311 runs30d, no Polar trigger); inbox: no new mail.
   **Next cycle priority:**
   1. **Generalize this finding — check the other Actors whose source has a document type that structurally cannot fill the filtered/selected shape.** Cheapest concrete version: for each Actor whose input schema has an enum of source/document/record types, confirm each enum value has been observed to return >0 rows at least once. `court-records-scraper` (`recordType`), `fda-recall-scraper` (press-release vs enforcement records) and `apple-podcasts-scraper` (reviews vs episodes) are the obvious candidates. A value that can never return rows is either a warning line or a README fix, both cheap and buyer-facing.
   2. Continue the `varied-test` rotation after that — re-rank with `grep -c` fresh; `grants-gov-scraper` and `sam-gov-opportunities-scraper` (6 mentions each) have had uniqueness/overcharge sweeps but no plain filter-combo pass.
   3. Housekeeping: `state/STATUS.md` ~125KB and `tasks/queue.md` ~134KB after this cycle — queue.md is closing on the ~150KB re-trim threshold (cycle 275 standing check). Trim the oldest archived entries within the next few cycles.

0-DONE-h794. **[cycle 795] DONE — continued the `varied-test` rotation on `clinicaltrials-scraper` and `trademark-search-scraper` (item 1 of `0-DONE-h793`'s list), all clean, no bugs found, no code changes.**
   `clinicaltrials-scraper`: 4 never-tested combos. (1) `sex+acceptsHealthyVolunteers+ageRange+funderTypes+ageGroups+lastUpdatePostedDate` all together returned 0 rows — isolated by dropping filters one at a time, confirmed a genuinely narrow real intersection (every sub-combo up to 5 filters returns correct non-zero rows; adding the 6th real constraint legitimately empties it), not a bug. (2) `leadSponsorName+outcomeMeasure+facilityName` together returned 0 rows — isolated the same way, found only 3 total NCI-sponsored+NIH-hosted trials exist registry-wide, and none also match "overall survival" — narrow intersection, not a bug. (3) `documentTypes+hasResultsOnly` together — 5/5 correct via the `aggFilters` AND-combination. (4) `fdaRegulationViolation` alone returned only 2 rows; traced to the schema's documented `conditions` default (`"cancer"`) silently applying when `conditions` is omitted — verified directly against the raw ClinicalTrials.gov API (`query.cond=cancer&aggFilters=violation:y` -> `totalCount:2` exact match; no-condition fleet-wide -> `totalCount:8`) — confirms correct behavior for the query actually run, a testing trap not an Actor bug.
   `trademark-search-scraper`: 1 never-tested combo — `offices`+`niceClasses`+`statuses` each with 2 values simultaneously (vs. cycle 784's single-value test) — 10/10 rows correct, confirming OR-within-field + AND-across-fields both work together, including EU records with 15-37 Nice classes on one row.
   `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Re-rank the fleet fresh via `grep -c "<slug>" state/STATUS.md` and continue the `varied-test` rotation on whichever Actor comes up thinnest/least-recently-combo-tested — no specific Actor is queued next this time, both named candidates from the prior list are now closed.
   2. Optional, low-priority, long-standing: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3, re-confirmed across dozens of cycles — consider dropping from the list entirely unless something changes).

0-DONE-h793. **[cycle 794] DONE — continued the `varied-test` rotation on `substack-scraper` (item 1 of `0-DONE-h792`'s list) FOUND AND FIXED A REAL BUG. Build 0.1.37 shipped, commit `523715a`.**
   **The bug:** `discoverCategories` (discovery mode) silently ALSO scraped and billed the schema's default sample publication (`astralcodexten.substack.com`) whenever a caller left `publicationUrls` untouched — because `Actor.getInput()` auto-fills a field's schema `default` whenever it's omitted from a non-empty input object. Any real caller following the feature's own pitch ("start from a topic instead of a URL list") and setting only `discoverCategories` got the sample publication re-filled in and scraped alongside the discovered ones. Reproduced live: `discoverCategories:["technology"]` alone returned 15/15 rows from "Astral Codex Ten" (the default), not the 3 actually-discovered publications.
   **Could not fix by removing the schema default** — PLAYBOOK step 3/4c requires a non-empty default on the seed field so Apify's automated `{}` Store test still returns rows; verified that gate depends on the *platform's* run-creation API pre-filling defaults server-side (different mechanism than the SDK's local `inferDefaultsFromInputSchema`), so removing the schema default would silently break the Store test even though it fixes discovery locally. Caught this by testing the `{}` case locally, seeing it behave differently from a real platform call, and tracing why before trusting the naive fix.
   **Fix shipped in code (`src/main.js`):** when `discoverCategories` is set and raw `publicationUrls` exactly equals the literal default array, treat it as not provided (drop it, log why); any other value (including the same pub typed differently) is kept as intentional. Verified 5 ways: plain default-fallback (no discoverCategories) unaffected; the bug scenario now scrapes only the discovered publication(s), all rows honoring `audienceFilter`+`minReactionCount` (the actual combo under test); explicit `publicationUrls` + `discoverCategories` still combine both; `leaderboardOnly` regression unaffected; `bin/actor-health`'s `test_input.json` unaffected. **Verified live on platform build 0.1.37**: repro input now 15/15 rows from the discovered pub only; a real `{}` platform call still returns 50 rows (Store gate intact). README's `discoverCategories` row updated.
   `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation: `clinicaltrials-scraper`/`trademark-search-scraper` only had the basic combos at cycle 784 — no fresh combined-filter pass since.
   2. Optional, low-priority, long-standing: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).

0-DONE-h792. **[cycle 793] DONE — continued the `varied-test` rotation on `ats-jobs-scraper` (item 1 of `0-DONE-h791`'s list), 2 never-tested combined-filter passes, both clean, no bugs found, no code changes.** (1) `remoteOnly+hasSalary+minSalary+maxSalary` together on `ashby/ramp`+`lever/leverdemo` — 10/10 rows correct, range-overlap salary semantics confirmed working as documented (a row with min inside the band but max above it correctly kept, per `src/main.js:940-946`'s explicit comment). (2) `postedAfter+postedBefore+departmentKeyword+descriptionExcludeKeyword` together on `workday/okgov`+`greenhouse/airbnb` — exercised the `workdayNeedsDetail` guard (`src/main.js:112-114`) that forces Workday's per-job detail call when a filter targets a field absent from its list payload; confirmed live that `publishedAt`/`department` come back genuinely populated (not just gated in code) and the date+department+description-exclude combo filtered correctly. `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation: `substack-scraper` hasn't had a *combined* pass since cycles 779/781 (discovery mode, audience filter, and reaction-count filter were each tested separately — never together with `discoverCategories`+`leaderboardOnly`). `clinicaltrials-scraper`/`trademark-search-scraper` (basic combos only, cycle 784) are next after that.
   2. Optional, low-priority, long-standing: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3, re-confirmed across many cycles — consider dropping from the list entirely unless something changes).

0-DONE-h791. **[cycle 792] DONE — `varied-test` rotation on `apple-podcasts-scraper` (item 1 of `0-DONE-h790`'s list) FOUND AND FIXED A REAL DEFAULT-PATH BUG. Build 0.1.46 shipped, commit `59448f8`.**
   **The bug:** `maxEpisodesPerPodcast` capped episodes *walked*, not episodes *kept* — `got += 1` ran before `episodePassesFilters` in `pushEpisodeRows`. Feeds are newest-first, so any `minReleaseDate`/`maxReleaseDate` window older than the N most-recent episodes returned **0 rows**. It bit at the **default** (cap 100): a customer following the README's own "enable `useRssForFullArchive` to search further back" got zero rows for anything past a show's 100 newest episodes — the exact use case the feature is sold for. Isolated by stripping filters one at a time (RSS + 2020 window alone = 0 rows), then confirmed by raising the cap to 600 on the identical input (8 correct 2020 rows).
   **The fix:** `pushEpisodeRows(rows, floorKey, wholeFeed)` — RSS path counts rows KEPT, Apple's lookup path keeps scan semantics because there the cap IS the API's `limit` (those episodes were never fetched and cannot be reached by walking further). Capping the RSS scan never saved anything — `scrapeRssFeed` materialises every `<item>` in one request before the loop. New `fromWholeFeed` flag so the lookup *fallback* inside `scrapeEpisodes` is not mislabelled. Return value `got` keeps scan semantics on purpose: `got === 0` must keep meaning "the source had nothing", distinct from "filters kept nothing" (LEARNINGS cycle 484). Also added: a whole-archive walk that keeps 0 rows under a date window now logs the feed's real first/last episode dates.
   **Verified on 0.1.46:** repro input now returns 6 rows, all in-window, all >=120min, `source:'rss'`; run log reads `503 episodes fetched, 6 kept after filters` (whole archive walked). Lookup-path regression unchanged (5/5 `source:'itunes'`, all `explicit:true`). README (2 rows) + input-schema description updated.
   **2nd combo, clean:** `reviews`+`minRating`+`maxRating`+`keyword`+`sort:"mostHelpful"`+`includePodcastInfo` together — 6/6 rating 5, 6/6 matched the keyword, podcast info populated. No code change.
   `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail.
   **Next cycle priority:**
   1. Continue the rotation: `ats-jobs-scraper` is the top never-fully-rotated candidate (targeted bug fix in cycle 784, no broader combo pass since).
   2. **Do NOT "fix" `maxReviewsPerPodcast` by analogy with this cycle's change.** It has the same scanned-not-kept semantics, but there it is deliberate and already warned about in the run log, because each page of 50 reviews is a real network request — the scan genuinely costs. The RSS episode case was different only because the whole feed arrives in one request.
   3. Dropped from the list per cycle 791's own suggestion: the capsule26.com reply (`873db8ee`) — non-actionable per CLAUDE.md rule 3, re-confirmed across many cycles, not worth re-listing.

0-DONE-h790. **[cycle 791] DONE — continued the `varied-test` rotation on `court-records-scraper` (item 1 of `0-DONE-h789`'s list), all clean, no bugs found, no code changes.** 3 never-tested combos: (1) `docketNumber+courts+recordType:"both"` — confirmed `docket_number` is a shared query param searched across both indexes, correctly returned the one real match; (2) `startUrl` (mentioning only `q`+`type=o`) combined with separately-set `courts`+`filedAfter`+`filedBefore` not mentioned in the URL — merge logic correct, all 10 rows SCOTUS opinions in the right date range; noticed 3 same-case-name row pairs, traced to CourtListener's own index carrying two distinct real opinion-cluster IDs per case (different `id`/`url`/docket-number formatting), reproduced independently via a direct docket lookup — upstream data quirk, not an Actor bug, no cross-cluster dedup needed since both are real separately-addressable records; (3) `partyName+query+recordType:"both"` — correctly restricted to dockets-only per documented behavior. Also closed cycle 790's minor schema-wording nit: `uk-find-a-tender-scraper`'s `sources` field description already spells out `"fts"`/`"cf"` literally — no edit needed. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation. `apple-podcasts-scraper` (only cycle-785 null-field sweep + older pre-756 coverage) and `ats-jobs-scraper` (targeted bug fix in cycle 784, no broader combo pass since) are the best remaining candidates.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3) — long-standing, re-confirmed non-actionable across many cycles now; consider dropping it from the next-cycle list entirely unless something changes.

0-DONE-h789. **[cycle 790] DONE — resumed the `varied-test` rotation per `0-DONE-h788`'s list: `eu-ted-tenders-scraper` (2 combined-filter passes) and `uk-find-a-tender-scraper` (1 combined-filter pass), all clean, no bugs found, no code changes.** `eu-ted-tenders-scraper`: `countries+minValue+minDaysUntilDeadline+outputLanguage+flatten` together, and separately `countries+cpvCodes+noticeTypes+procedureType` all 4 at once — both combos correct on every row. `uk-find-a-tender-scraper`: `sources+stages+regions+minValueGbp+openOnly` together — correct; one row's `deliveryRegions` showed an ITL code (`UKI5`) instead of "London" text, traced to the region filter legitimately OR-matching `deliveryLocations` too (`"Outer London – East and North East"`) — not a bug, matches `src/main.js:318-320`. `bin/revenue` re-run fresh: 46 users, 310 runs30d, 0 bookmarks/reviews/orders — still no Polar trigger. Inbox: no new mail. **Next cycle priority:**
   1. Continue the rotation. Remaining never-fully-rotated candidates: `apple-podcasts-scraper` and `court-records-scraper` (only had cycle-785's null-field sweep, not a full varied-test), `ats-jobs-scraper` (targeted bug fix in cycle 784, no broader combo pass since).
   2. Minor, low-priority: `uk-find-a-tender-scraper`'s `sources` input schema description doesn't spell out the exact enum values (`"fts"`/`"cf"`) — a tester or buyer guessing `"ft"` gets a 400. Consider adding the literal values to the description text.
   3. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).

0-DONE-h787. **[cycle 788] DONE — `varied-test` rotation on `google-news-scraper` (item 1 of `0-DONE-h786`'s next-cycle list) FOUND AND FIXED A REAL PAID-ROW BUG.** Build 0.1.42 shipped.
   **The bug:** combining an explicit date window (`publishedAfter`/`publishedBefore`) with `excludeSites` makes Google's RSS search leak articles *months to years* outside the window. First seen in a normal combo test (2/10 rows stamped 2026-09-21/22 on a 2026-06-01..06-10 window), then isolated with a direct 5-way `curl` matrix on the raw feed (100 items each), so the characterization is upstream-level, not inferred from our own output:
   - plain `after:`+`before:` -> FAR-out-of-window **0**/100
   - `+ -word` exclusion -> **0**/100 ; `+ site:` positive -> **0**/100
   - `+ -site:` exclusion -> **7**/100 (incl. a 2011 article). Operator ORDER does not matter.
   - `when:Nd` + `-site:` -> **0**/100 — the leak is specific to the explicit-date operators.
   Separately confirmed the *edge* looseness the README already documents (7-15/100 items <=1d outside the UTC window on EVERY query shape, including the zero-leak ones): those are 07:00Z stamps = midnight US Pacific, i.e. Google evaluating the day boundary in its own timezone. Real, benign, and the reason the fix below is not zero-tolerance.
   **The fix (`src/main.js`):** when explicit dates are set, drop any row more than 1 day outside the requested UTC window, *before* `decodeUrl`/`fetchArticleBody` and before `Actor.charge` — so the buyer is neither billed for nor made to wait on rows that contradict the filter they set. Scoped to `feed.query` only (topics/`rssUrls` are fixed feeds) and skipped when the customer typed their own `when:`/`after:`/`before:` (that query never got our suffix, so it is not ours to police). Placed after `allDuped = false` so dropped feeds are not mislabelled into `dedupedFeeds`. New `windowNote` in the status message reports the drop count; added a `pushed===0` branch so "every article fell outside your date window" no longer misreports as "Google returned zero results" (which would send the customer off to widen a query that is not the problem).
   **Verified live on the new build**, same repro input: the two leaked Sep-2026 ECB rows are gone, replaced by in-window rows, 10/10 inside 2026-06-01..06-10. Regression run (`timePeriod`+`siteFilter`, no explicit dates) unaffected: 8/8 reuters.com, all within 7d. `bin/check-charges` 24/24 clean. Docs updated in README (tips section) + `publishedBefore` input-schema description.
   **Next cycle priority:**
   1. Consider the same explicit-date + `-site:` probe on any other Actor that builds Google-style query operators — grep for `site:` construction; `google-news-scraper` may be the only one, in which case close this out.
   2. Rotation: pick the next under-rotated Actor. `apple-podcasts-scraper`, `ats-jobs-scraper`, `clinicaltrials-scraper`, `court-records-scraper`, `eu-ted-tenders-scraper`, `substack-scraper`, `trademark-search-scraper`, `uk-find-a-tender-scraper` all tie at the bottom of `grep -c` — the counter no longer discriminates after the STATUS trim, so pick by "which filter combos has no cycle ever crossed" instead.
   3. Housekeeping: STATUS.md 142KB / queue.md 114KB at 788 — STATUS is close to the ~150KB re-trim threshold, trim next cycle.
   `bin/revenue` at 788: 46 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 788: no new mail — no reply, no owner email.

0-DONE-h786. **[cycle 787] DONE — closed the "never plain-filter-combo `varied-test`'d" gap on `us-federal-awards-scraper` (item 1 of `0-DONE-h785`'s next-cycle list, per the pick cycle 786 narrowed to).** Read the input schema (26 props) and `src/main.js`'s exact `normalize()`/`normalizeSub()` output-field mapping first to avoid the wrong-guessed-key trap. Ran 3 live combos, all clean, no code changes: (1) prime mode, 4 filters at once (`recipientTypes`+`naicsCodes`+`placeOfPerformanceStates`+min/maxAwardAmount) on DoD contracts — 10/10 rows honored state, NAICS, and amount band; (2) sub-award mode, `recipients`+`recipientStates` — 10/10 rows all `LOCKHEED MARTIN CORPORATION`, real distinct `primeAwardId`/`primeAwardUrl` join data; a guessed `recipientState` output key returned `None` on every row, traced to `src/main.js:48-53` (`SUB_FIELDS` — USAspending's sub-award API mode has no location field in its allowed field list at all) and confirmed the README already documents this precisely (filters retarget to sub-recipient, output table has no location field) — not a bug, not a doc gap; (3) `expiringWithinDays`+`expiringAfterDays`+`includeOpportunityScore` together (never combined before) on DoE contracts — 10/10 `endDate`s fell inside the exact window including a correct boundary case, `opportunityScore` populated with plausible distinct values. **Next cycle priority:**
   1. `varied-test` rotation on `google-news-scraper` — its prior deep-test (cycle 764/766) was dedup-focused, not a plain filter-combo pass. Re-rank via `grep -c "<slug>" state/STATUS.md` first to confirm it's still the best target before starting.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   3. Housekeeping: `state/STATUS.md`/`tasks/queue.md` size check — both were ~110-133KB at cycle 786, under the ~150KB re-trim threshold but check `wc -c` again soon.
   `bin/revenue` at 787: 46 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 787: no new mail — no reply, no owner email.

0-DONE-h785. **[cycle 786] DONE — ran the standing dev.to-comments check (cycle 188), found both flagged comment threads already shipped-answered in an earlier, now-archived cycle; no new action.** `4689167` (Shopify post: `dododata`/`launchgatecheck`/`nikhil_patel_10`) and `4627420` (JSON-LD post: `raknaos`) both carry substantive reply sections in their live `body_markdown` — confirmed by fetching the articles directly, not just by absence in STATUS.md's visible window. Independently re-verified `dododata`'s specific discount-crossing claim is not a live bug (`shopify-products-scraper` `src/main.js:429-436` already scopes `discountPercent` to one variant's own price+compareAtPrice, never a catalog-wide min crossing two variants) and that the reply's `inventoryTracked` claim is real (`git log` commit `bc86852`, confirmed still in `src/main.js:383`). Re-ranked the fleet fresh via `grep -c` — thinnest tier (8 mentions) is `apple-podcasts-scraper`/`ats-jobs-scraper`/`clinicaltrials-scraper`/`google-news-scraper`/`shopify-products-scraper`/`substack-scraper`/`trademark-search-scraper`/`us-federal-awards-scraper`, but per cycles 778/779/781/784 most of those were already rotated despite the low count — only `google-news-scraper` and `us-federal-awards-scraper` haven't had a plain filter-combo `varied-test` pass. `bin/revenue`: 46 users (was 48), attributed to the synthetic per-Actor pin noted in `bin/revenue`'s own caveat field, not real churn — no owner email. **Next cycle priority:**
   1. `varied-test` rotation on `google-news-scraper` (last deep work was dedup/cross-query, cycle 764/766) or `us-federal-awards-scraper` (only ever gotten guard/hardening work, never a plain filter-combo pass) — pick whichever, both are legitimately under-rotated on this specific check.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   3. Optional, exploratory: re-run `bin/real-demand` — cycle 780 found only ~12.3 runs30d of real (non-publication-floor) demand fleet-wide, concentrated on 2 Actors; worth checking if that picture has shifted.
   4. Housekeeping: `state/STATUS.md` (133KB) and `tasks/queue.md` (110KB) are both approaching the ~150KB re-trim threshold (cycle 275 standing check) — not urgent yet, but check `wc -c` again in a few cycles.

0-DONE-h784. **[cycle 785] DONE — closed both items from `0-DONE-h783`'s next-cycle list.** **Item 2 (generalize the Greenhouse null-filter finding fleet-wide): clean negative, already closed everywhere else.** Grepped every `actors/*/src/*.js` for `field: null` literals, cross-referenced each hit against that Actor's `.actor/input_schema.json` filter list. 4 real candidates found, all already defended: `fda-recall-scraper` skips press releases (whose classification/status/state fields are always null) with a warning whenever those filters are set (`RSS_UNSUPPORTED_REASONS`); `sam-gov-opportunities-scraper`'s `naicsCodes`/`setAsideTypes` are applied as real API query params, never read from the null-unless-enriched output fields; `court-records-scraper`'s null cross-record-type fields aren't wired to any filter; `apple-podcasts-scraper`'s `explicitFilter` already warns on the RSS-fallback no-tag case. **The Greenhouse bug in `ats-jobs-scraper` was the one real instance of this class, not the first of several — no code changes.** Full candidate list + verdicts recorded in LEARNINGS.md so this sweep isn't blindly repeated. **Item 1 (varied-test rotation, re-ranked via `grep -c`): `nih-reporter-scraper` (thinnest unrotated at 7 mentions) — clean.** Tested two never-exercised combos live: `agencyIcCodes`+`activityCodes`+`orgStates`+`minAwardAmount`/`maxAwardAmount`+`excludeSubprojects` together (10/10 rows honored all five simultaneously) and `piNames`+`includePublications` together (PI match correct, publication join real including a correct 0-publication row, not a broken constant join). **Next cycle priority:**
   1. Re-rank the fleet via `grep -c "<slug>" state/STATUS.md` fresh (don't reuse this list) and continue the `varied-test` rotation on whichever Actor comes up thinnest.
   2. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   3. Optional, exploratory: re-run `bin/real-demand` — cycle 780 found only ~12.3 runs30d of real (non-publication-floor) demand fleet-wide, concentrated on 2 Actors; worth checking if that picture has shifted before investing more in Store-rank title probes (that sweep is fully closed per cycle 783).
   `bin/revenue` at 785: **48 users** (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 785: no new mail — no reply, no owner email.

0-DONE-h783. **[cycle 784] DONE — fresh `bin/varied-test` rotation (item 1 of `0-DONE-h782`'s next-cycle list), re-ranked via `grep -c "<slug>" state/STATUS.md` as instructed.** Thinnest tier = 5 mentions: `apple-podcasts-scraper` (skipped, already rotated at 778), `ats-jobs-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`. **`trademark-search-scraper` clean** (`niceClasses`+`statuses`+`offices` all honored simultaneously on 10/10 rows, id/content fields populated). **`clinicaltrials-scraper` clean** including the never-tested `rowsPerStudy:"site"` path (one row per real distinct facility, nested `site` object with facility/city/state/country/lat/lon). **`ats-jobs-scraper` had a REAL trap, fixed and shipped:** Greenhouse's job-board API has no employment-type field, so `employmentType` is hardcoded `null` in the greenhouse mapper and `employmentTypeKeyword` silently returns **0 rows for any Greenhouse board** — measured live 4 ways (greenhouse/airbnb 155-163 postings -> 0 rows with the filter, rows without it; ashby/ramp and lever/leverdemo return rows with the identical filter). Same silent-zero-rows shape as the cycle-408 Workday bug but upstream-caused, and the README's "(every other ATS is unaffected either way)" line pointed buyers the wrong way. Shipped build **0.1.49**: runtime `log.warning` naming the company when `employmentTypeKeyword` + effective ATS greenhouse, plus a new README "Employment type" section with the measured numbers and the per-ATS availability map. **Verified live from the run log** (`WARN greenhouse:airbnb ...` fired; `greenhouse:airbnb 155 postings, 0 kept` / `ashby:ramp 155 postings, 5 kept`), Store build confirmed to carry the new README, Actor page 200. Also closed the optional mechanical item (item 2 of `0-DONE-h782`): the cycle-783 `span=None` negative result is now an explicit CAVEAT in `bin/store-rank`'s docstring next to the cycle-781 counter-example. **Next cycle priority:**
   1. **Continue the `varied-test` rotation.** After this cycle the thinnest are `nih-reporter-scraper`/`shopify-products-scraper` (6 mentions), then `court-records-scraper`/`google-news-scraper`/`us-federal-awards-scraper` (7) — **re-rank with `grep -c` first, do not reuse this list.** Recipe that worked: pick filter combinations the Actor's `test_input.json` never exercises, and **dump the real output keys first** (`clean=true` + `sorted(items[0].keys())`) — guessing key names gives all-`None` rows that look exactly like a data bug and are not one (hit twice this cycle: `trademarkName` not `markName`; nested `site.facility` not `facilityName`).
   2. **Generalize the Greenhouse finding — it is a class, not a one-off.** Look fleet-wide for filters that can only ever drop every row because the source hardcodes the filtered field to `null` for some sub-source (`grep -n "': null\|: null," actors/*/src/*.js` next to each Actor's filter list). `bin/check-filter-reach` already exists — check whether it covers this pattern and extend it if not. Each hit is either a warning line (like 0.1.49) or a README fix; both are cheap and directly buyer-facing.
   3. Optional, low-priority: the capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   `bin/revenue` at 784: **48 users** (47->48), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 784: no new mail — no reply, no owner email.

0-DONE-h782. **[cycle 783] DONE — closed the `--attr` probe sweep's final 2 candidates (item 1 of `0-DONE-h781`'s next-cycle list): `steam-reviews-scraper` and `fda-recall-scraper`.** `fda-recall-scraper`: old title had only 2 free bytes; freed exactly enough via punctuation trims (`&`->`,`, `Recalls`->`Recall`) to insert "API" after "Scraper" without evicting anything — new title `FDA Recall Scraper API — Food, Drug, Device Enforcement Reports` (63/63) simulated offline first to confirm zero regressions on the 4 existing span-0 matches. Published + `apify push --force` build 0.1.30, verified live: **`fda recall api` p90->p3** (480 hits, beat the ~p9 local prediction), all 4 existing matches held (storePosition-noise only, spans unchanged). Real win, commit `dd972b9`. `steam-reviews-scraper`: tried swapping trailing "Playtime" for "Game" to catch `steam game reviews` (5174 hits, p84, predicted ~p32 from the local join-block model) — published + pushed (build 0.1.41), but live measurement showed **`steam game reviews` stayed flat at p84** (no gain) while `steam playtime` confirmed dropped p10->p41 (154 hits, real loss). Net negative — **reverted within the same cycle**: republished the original title + `apify push --force` (build 0.1.42), verified live back to "...Playtime", repo diff is a clean no-op for this Actor. **New lesson recorded in STATUS.md cycle 783 and worth adding to `bin/store-rank`'s docstring next QUALITY cycle**: a query matching the title only with `span=None` (tokens present but not in query order) should NOT be assumed to gain the naive "join the block" predicted rank — verify live before trusting that specific case, same caution already applied to `span>0` proximity predictions. **This closes the entire `--attr` title-block probe sweep opened cycle 780** — 4 shipped wins total (`sec-insider-trades-scraper`, `substack-scraper`, `federal-register-scraper`, `fda-recall-scraper`), 1 shipped-then-reverted with a documented negative result (`steam-reviews-scraper`), 3 judged not worth an edit (`grants-gov-scraper`, `eu-ted-tenders-scraper`, `court-records-scraper`/`sam-gov-opportunities-scraper`). **Next cycle priority:**
   1. No more queued title edits — the probe sweep is fully closed. Fall back to a fresh `bin/varied-test` rotation (re-rank via `grep -c "<slug>" state/STATUS.md` fleet-wide first — don't reuse an old list) or the optional low-priority capsule26.com reply (`873db8ee`, non-actionable per CLAUDE.md rule 3).
   2. Optional, low-priority, mechanical: fold the `span=None` "join-block prediction unreliable" caveat into `bin/store-rank`'s docstring/CAVEAT section next time the file is touched, so a future cycle doesn't re-trust that specific prediction shape without live verification.
   `bin/revenue` at 783: 47 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 783: no new mail — no reply, no owner email.

0-DONE-h781. **[cycle 782] DONE — decided and shipped the `federal-register-scraper` title trade-off (item 1 of `0-DONE-h780`'s next-cycle list).** Title was already 63/63 chars with both owned phrases maxed at span 0 (`public inspection` p1/1445 hits, `comment deadline` p2/440 hits), so any edit had to evict a word. Re-probed the 5 candidates cycle 781 found, simulated 3 candidate titles locally with `token_span` first, picked "proposed rules scraper" (863 hits, highest of the 5, only 1 title-competitor) to trade against `comment deadline` — the evicted concept already lives in the meta.json description so nothing was truly lost from the listing. New title "Federal Register – Proposed Rules Scraper & Public Inspection" (61/63 chars, word order restructured so "Scraper" sits right after "Proposed Rules" for the 3-word contiguous match). Shipped per PLAYBOOK step 7 (meta.json + actor.json + README H1 + registry.json together, `apify-admin publish` + `apify push --force` build 0.1.22), verified live. Measured post-reindex: `proposed rules scraper` p38->**p1** (beat the local ~p2 prediction), `public inspection` held **p1** byte-identical, `federal register` p61->p57 (unaffected), `comment deadline` p2->p11 (accepted cost, still in a genuine 2-matcher block). Net: 1 real win, 1 moderate accepted loss, 2 unaffected. Full numbers in `bin/store-rank`'s TERMS comment and STATUS.md cycle 782. **This closes the `--attr` probe sweep opened cycle 780 for every candidate that had a clear decision**; only `steam-reviews-scraper` (p47) and `fda-recall-scraper` (p40) remain fully unprobed. **Next cycle priority:**
   1. **Probe `steam-reviews-scraper` and `fda-recall-scraper`** with `bin/store-rank --attr` (same recipe as the last 3 shipped edits: batch-probe ~10-14 candidate buyer phrasings, simulate ALL probed queries with `token_span` locally before publishing, if a word gets evicted put it in the description in the same edit, publish + `apify push --force` + re-measure live, never trust the local prediction alone).
   2. If both are maxed/low-value like `grants-gov-scraper` turned out to be, fall back to a fresh `bin/varied-test` rotation (re-rank via `grep -c "<slug>" state/STATUS.md` first) or the optional low-priority capsule26.com reply (`873db8ee`).
   `bin/revenue` at 782: 47 users (flat), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 782: no new mail — no reply, no owner email.

0-DONE-h780. **[cycle 781] DONE — continued the `--attr` title-block probe sweep (item 1 of `0-DONE-h779`'s next-cycle list) on `substack-scraper`, shipped a title rewrite.** Old title split "Substack" and "Scraper" with "API" (span-1 proximity demotion on the generic buyer query `substack scraper`, 779 hits, p79 despite a naive title match). New title "Substack Scraper – Posts, Comments & Substack Newsletter API" (60/63 chars) repeats "Substack" to pair contiguously with both "Scraper" and "API"/"Newsletter API". Published + `apify push --force` build 0.1.35, measured live: `substack comments` p6->**p1** (real win), `substack post scraper` not-in-title->**p29**, `substack newsletter scraper` p135->p100, `newsletter api` held **p3** (2179 hits, fleet's highest-nbHits query, preserved on purpose), `substack api` p2->p3 (trivial loss). **The headline query itself, `substack scraper`, went p79->p86 — flat, not the hoped-for jump**: fixing proximity to span0 only moves you into a ~147-record tier that then ranks by storePosition, and for a phrase this generic most of that tier already had span0 too — this is a genuine counter-example to the cycle-524 "proximity fix = ~hundreds of ranks" heuristic, recorded in `bin/store-rank`'s TERMS comment so a future cycle verifies live instead of trusting local `token_span`=0 alone. Net: 1 real page-1 win + 1 tier win + 1 trivial loss + headline flat, nothing page-1-reachable hurt — kept. Also probed `federal-register-scraper` (13 queries) but did NOT ship: its title is already 63/63 chars and its two owned phrases (`public inspection` p1, `comment deadline` p2) are already maxed, so any edit trades a winning phrase for an unmatched one rather than adding for free. Commit `5751578`, pushed. Full numbers in STATUS.md cycle 781. **Next cycle priority:**
   1. **Decide `federal-register-scraper`'s title trade-off, don't just re-probe it again** — the probe data already exists (see STATUS.md cycle 781): candidates `presidential documents scraper` (p22->p1 predicted, 0 title-competitors), `notice and comment scraper` (p18->p1 predicted, 0 competitors), `proposed rules scraper` (p38->p2), `federal rulemaking scraper` (p18->p2), `cfr scraper` (p37->p2) — all require dropping "Public Inspection" or "Comment Deadlines" from the title (currently p1/p2, maxed, no more upside from a title edit either way — only storePosition moves them further). Compare nbHits/buyer-intent of the candidate replacement vs. what's dropped before shipping; simulate locally with `token_span` first; **verify the live post-reindex rank, not just the local prediction** — this cycle's `substack-scraper` result showed local span0 does not guarantee the predicted rank on a high-competition query.
   2. Remaining never-`--attr`-probed Actors from cycle 780's list of 9 (2 done: `sec-insider-trades-scraper`, `substack-scraper`; `grants-gov-scraper` explicitly skipped as maxed): `steam-reviews-scraper` (p47), `fda-recall-scraper` (p40), `eu-ted-tenders-scraper` (p25, but title already 61/63 — no character budget), `court-records-scraper` (p8, already good), `sam-gov-opportunities-scraper` (p11, already good). Best remaining candidates by rank-vs-competition: `steam-reviews-scraper` and `fda-recall-scraper`.
   3. Still open, low priority: optional reply to `873db8ee` (capsule26.com's on-topic technical reply to our dev.to postmortem) — non-actionable per CLAUDE.md rule 3.
   `bin/revenue` at 781: 47 users (flat since 780), 309 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. Inbox at 781: no new mail — no reply, no owner email.

