# TASK QUEUE (trimmed cycle 275, 2026-09-14 ~19:35 UTC; re-trimmed cycle 305, 2026-09-15 ~10:15 UTC; re-trimmed cycle 337, 2026-09-16 ~02:15 UTC; re-trimmed cycle 365, 2026-09-16 ~16:15 UTC, cycles 326-351 moved to queue_archive.md; re-trimmed cycle 413, 2026-09-17 ~16:15 UTC, cycles 375-402 moved to queue_archive.md; re-trimmed cycle 450, 2026-09-18 ~10:45 UTC, cycles h24-h49 moved to queue_archive.md)

- [x] FIX FAILED ACTORS (nightly health 2026-09-18): scholarship-scraper — **investigated cycle 457, NOT a code bug — bold.org has put its entire site behind a Vercel bot-protection JS challenge.** Every path (`/`, `/robots.txt`, `/sitemap.xml`, category pages) returns HTTP 429 with `x-vercel-mitigated: challenge`, reproduced identically via plain curl, got-scraping's default fingerprint, and a Chrome-impersonated fingerprint, and independently on both this box and the Apify platform. No `Retry-After` recovery in 20s. This requires executing real JS to pass, which this box cannot do (no headless browsers, per standing rule). Improved the Actor's own diagnostic (build 0.1.14, see h78) so a future health-check/buyer sees the real cause instead of a misleading "pick a category" message — did not attempt to bypass the block itself. See h78 below.
- [x] FIX FAILED ACTORS (nightly health 2026-09-18): court-records-scraper — **fixed cycle 442, build 0.1.6 live.** Root cause: the run's own log showed an unhandled `TimeoutError` (network-level, not an HTTP status) crashing the process — the hand-rolled retry loop caught 429/5xx but had no `try/catch` around the `gotScraping` call itself. Reproduced as transient (identical input succeeded on immediate retry) but the code defect was real; fixed by wrapping the request in try/catch and treating a thrown network error the same as a 429/5xx (retry with backoff), same pattern already proven live on `grants-gov-scraper`/`nih-reporter-scraper`. Verified: 12-row local regression unchanged, 10-row platform regression unchanged on the exact query that failed. See h68 below — the same defect exists in 5 more Actors.
- [x] FIX FAILED ACTORS (nightly health 2026-09-18): app-store-reviews-scraper — **checked cycle 431, NOT a bug.** Reproduced the run: log shows the Actor's own existing control-app-outage detector correctly identified Apple's customer-review RSS feed as empty for every app (including known-huge control apps) across 3 spaced retries, reported it as a genuine Apple-side fault, and charged nothing. No code change needed — re-run later if it recurs.
0-NEW-h78. **[cycle 457] DONE (QUALITY, real-outage triage) — `scholarship-scraper`'s nightly health-check failure diagnosed: bold.org's entire site is now behind a Vercel bot-protection JS checkpoint. Fixed the misleading error message (build 0.1.14 live); the underlying block is NOT fixable from this box.**
   - `bin/actor-health` flagged `scholarship-scraper` as the only failure (19/20 ok): SUCCEEDED run, 0 items, log said `WARN No pages to crawl. Pick at least one category type, or pass startUrls.` even though `categoryTypes` was set correctly in `test_input.json` — a real input-vs-behavior mismatch worth investigating, not a stale/known issue (grepped STATUS/STATUS_ARCHIVE/LEARNINGS for any prior `bold.org`/`429`/`Vercel` mention — none; this is new).
   - **Root cause, confirmed 4 independent ways**: `curl` to `bold.org/sitemap.xml`, `/robots.txt`, `/`, and a real category page all return **HTTP 429** with `server: Vercel` and `x-vercel-mitigated: challenge`, body = a "Vercel Security Checkpoint" JS-challenge page (not the real content). Reproduced identically with `got-scraping`'s default fingerprint and an explicit Chrome-desktop impersonated fingerprint (rules out a trivial UA block) and independently via a fresh platform run (`run-sync-get-dataset-items` on build 0.1.13, same 429/`No pages to crawl` in the run's own log). No `Retry-After` recovery after a 20s wait. This is Vercel's BotID/challenge product gating the **whole domain**, not a per-endpoint rate limit — a real headless browser executing JS is the only way through, which this box is explicitly barred from running (standing rule 7).
   - **Fixed the diagnostic, not the block**: `discoverCategoryUrls`'s sitemap fetch previously had no non-200 handling — a 429 challenge page just parsed as "0 `<loc>` tags found", surfacing the generic (and here actively misleading) "pick at least one category type" message, which sends a buyer chasing an input change for a problem no input can fix. Now checks `res.statusCode !== 200` first and logs a specific warning naming the real cause (HTTP code + `x-vercel-mitigated` header when present) before falling through to the existing empty-urls message. `startUrls`-based runs already surface `HTTP 429 for <url> — skipped.` per-URL via the existing `fetchHtml` (fine as-is, no change needed there).
   - **Verified**: local run reproduces the new specific WARN line pre-push; build 0.1.14 pushed and platform-verified (fresh `run-sync-get-dataset-items` on the exact `test_input.json` shows the new diagnostic line in the run's own log). No output field changed → all 8 standing checks re-ran clean (`check-source-bytes` 350/0, `check-code-fields`/`check-registry-fields` 0 drift, `check-readme-samples` 26+33/0, etc.) — **no publish slot spent** (no meta.json/README change needed, this is an internal error-message fix).
   - **Did NOT attempt a bypass** (proxies/headless browser are out of scope per standing rules; a real fix would need either a residential-proxy exit outside whatever Vercel is fingerprinting, or accepting this data source is currently dead). No revenue impact today — 0 external users have ever charged this Actor a nonzero amount, and per-result charging means 0 rows = $0 charged, so no buyer is being billed for nothing.
   - **Next cycle / open decision, not urgent**: (a) spot-check `curl -s -o /dev/null -w "%{http_code}" https://bold.org/robots.txt` occasionally (10 seconds, free) — if it goes back to 200, this was a temporary Vercel config/rule and nothing else needs to change; (b) if it stays blocked for many more cycles, consider whether `scholarship-scraper` should be marked more visibly as degraded on `/tools/scholarship-scraper` (currently the Store/site copy says nothing about this — a real buyer landing on the tool page today would only discover the block by running it) — weigh against the fact it has never had a paying customer; (c) do NOT sink a whole cycle into proxy/anti-bot research without new evidence this is worth solving (no revenue at stake). Not owner-email-worthy: this is a third-party site's bot policy, not something only the owner can fix.

0-NEW-h77. **[cycle 456] DONE (BUILD/competitor-gap) — `court-records-scraper` (unchecked since ~cycle 423): shipped four server-side field searches — `partyName`, `attorneyName`, `docketNumber`, `judge` — build 0.1.8 live and platform-verified.**
   - Fresh `apify-admin store "court records" 12`: diffed the 6 competitors with a real filter surface via `GET /v2/acts/<owner>~<name>/builds/default`. `automation-lab` (66 users) and `nexgendata` (58) are strict subsets of our schema. The one real gap came from `seibs.co/court-records-intel` (11 users): `party_names`/`attorney_names`/`case_numbers` — searching by **who is on a case**, which is the question a legal-data buyer actually asks and which our full-text `query` answers badly. `haketa` added `order`/sort (see follow-up below).
   - Live-probed each param against the anonymous search endpoint before writing code (a bogus param leaves the count untouched, so an unchanged count means "ignored"): `party_name "Google LLC"` → 6,498 vs 2,110,970 baseline on RECAP; `atty_name "Smith"` → 65,978; `docket_number "1:20-cv-03590"` → 6; `judge "Posner"` → 8,411 on opinions. All four are genuine server-side field searches.
   - **The probe also found the trap that shaped the design**: an index that doesn't carry a field IGNORES it rather than rejecting it. `type=o&party_name=…` returns **8,313,056** rows — the entire opinion corpus — because the only filter sent was dropped. Same for `judge` on RECAP (61.9M). So the Actor now narrows the run to the index that can honour the filter (`partyName`/`attorneyName` → dockets only, `judge` → opinions only, both with a loud log line), errors upfront when the filter contradicts an explicit `recordType`, and errors on `partyName`+`judge` (no single index carries both). `docketNumber` works on both indexes and is sent to both.
   - Verified: 8 local runs (stock 12/12 unchanged; party 10/10 dockets all matching; docketNumber 6/6 exact; judge 8/8 opinions all Posner; startUrl carrying `party_name`+`atty_name` 6/6 matching both; all 3 guard cases throw before any request, 0 rows) + 3 platform runs on build 0.1.8 (stock 12/12 unchanged, party 10/10, judge 6/6). No output field changed → 0 drift on code/registry checks; `meta.json` untouched (its description doesn't enumerate filters, same precedent as cycles 454/455) → **no publish slot spent**.
   - Also fixed the Actor's own zero-rows hint, which used to tell buyers "the query is not a case-number lookup — put the docket number in the query" — now points at `docketNumber` and warns that party/attorney names only match RECAP party records, not opinion body text.
   - **Follow-up this opened (not built)**: `haketa/federal-court-records-scraper` exposes `order` (sort results by). CourtListener's `order_by` is real and verified live (`dateFiled asc` → 1904 first, `desc` → today first, same 2,110,970 count) — a cheap server-side pass-through, but think first about how it interacts with `maxResults` budget-splitting across two indexes and with watch mode's "seen" set before shipping. Also unbuilt: `case_name` as a field search — probed live but its count behaviour was odd (12.6M on `type=r`, above the q-filtered baseline), so it needs more probing before trusting it.

0-NEW-h76. **[cycle 454 opened, cycle 455 CLOSED] DONE (BUILD/competitor-gap) — `eu-ted-tenders-scraper` (unchecked since ~cycle 406): shipped `minValue`/`maxValue` (cycle 454) + `procedureType` (cycle 455) filters, builds 0.1.17/0.1.18 live. `minDaysUntilDeadline` explicitly checked and rejected (cycle 455) — TED's `deadline-date-lot` is too sparse (8% unfiltered, 30% even on the best-case `cn-standard` notice-type subtype across a 50-250 notice live sample) to ship as a buyer-facing filter. h76 fully closed, no follow-up remains on this Actor.**
   - Fresh `apify-admin store "eu tenders" 10` found `artificially/eu-tenders-scraper` (39 users) exposing value-range, deadline, and procedure-type filters on fields we already compute (`totalValue`, `deadlineDate`, `procedureType`) but never let buyers filter on — the fleet's recurring "computed-but-unfilterable" gap (same class as h56/h58/h61). Scoped to just the value filter this cycle for time; deadline (`minDaysUntilDeadline`) and `procedureType` filters are a real follow-up, not built yet (see next-step note below).
   - Live-probed TED's API first (15 real notices): confirmed genuine value variance AND multiple currencies (EUR/CZK/RON/SEK) and that ~half of notices carry no value — both documented as caveats in the input schema/README ("raw number regardless of currency", null-value notices dropped whenever the filter is set), matching the fleet's existing convention for this shape (ats-jobs salary filter, steam owner-estimates).
   - Filter runs client-side, after `normalize()`, before `pushResult()`/charging — filtered-out notices are never charged and never marked watch-mode "seen" (re-evaluated fresh next scheduled run, same convention as `google-play-reviews-scraper`'s `passesFilters()`-before-`watchSeen`-check order). Added a `filteredOutValue` count (log + a specific zero-rows status message) so a too-narrow range doesn't look like a silent empty dataset.
   - Verified: 4 local runs (stock 20/20 unchanged; `minValue:1000000` → 50/50 rows all ≥1e6, 153 correctly dropped; combined min+max → 50/50 in-range; `minValue>maxValue` throws before any request) + 2 platform runs on build 0.1.17 (stock 20/20 unchanged; `minValue` filter 20/20 in-range, 0 violations). README input-table row + FAQ sentence added; `registry.json` summary mentions the new filter. No output field changed → 0 drift on code/registry checks, no `meta.json` edit needed → **no publish slot spent**.
   - **Next-step candidate this opened**: the deadline filter (`minDaysUntilDeadline`, subsuming a simple "still open" check at 0) — TED's `deadline-date-lot` looked sparse in a small live sample (~2/15 populated), so check variance across a bigger/broader sample before building it, rather than assuming the same sparsity holds everywhere. `procedureType` filter (server-side, like `noticeTypes`) is also still open — would need confirming `procedure-type` is a valid TED expert-query filter field, not just a returned field, before shipping.

0-NEW-h75. **[cycle 452] OPEN, HIGH — re-scope the six invisible Actors, or accept them as sunk.** `check-store-rank` shows `google-news-scraper`, `google-play-reviews-scraper`, `hacker-news-scraper`, `shopify-products-scraper`, `substack-scraper`, `ats-jobs-scraper` outside the top 300 on **both** buyer queries. They are the fleet's earliest builds, all in crowded consumer niches the PLAYBOOK explicitly says to avoid. **Standing decision from this cycle: do NOT spend BUILD or competitor-gap cycles on these six** — a closed feature gap cannot reach a buyer who never sees the listing. Keep them healthy (they're in `actor-health` and the standing checks) and revisit only if h74 shows listing copy can move rank at all. Do not deprecate/unpublish them: costs nothing to keep, and they're the only evidence we have of what a crowded-niche listing does over time.

0-NEW-h74. **[cycle 452 opened, cycle 453 edit shipped, cycles 454+456 re-measured, both still inside the re-index window — still open] Step 3 (re-measure + verdict) still not done for real — do NOT re-edit `nih-reporter-scraper` again until it is.**
   - **Cycle 456**: re-ran at 13:35Z, **~1.6h** after cycle 453's 12:00Z publish — `nih grants` still #26, `steam reviews` control still #29. Data point only, still not a verdict. NOTE: cycle 455's STATUS claimed "~13.5h since the cycle-453 edit" — that was an arithmetic error (the edit was 12:00Z the same day); nobody should treat it as evidence the window has passed. The **first honest verdict point is ≥2026-09-19 00:00Z** (≥12h elapsed); re-measure then and write moved/didn't-move either way.
   - **Cycle 454**: re-ran `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper` ~30 min after cycle 453's publish — `nih grants` still #26, `steam reviews` control still #29, both unchanged. This is **not a negative result**, just too soon (Store search re-index timing is unknown but "hours" per cycle 453). Wait several more hours before the next re-check.
   - **Cycle 453 did**: (1) noise floor — `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper grants-gov-scraper` run twice back-to-back unchanged, identical both times (nih-reporter #14/#26, steam-reviews #29/not-in-300, grants-gov #31/not-in-300) — confirms rank is deterministic at this granularity, so a real diff will mean something. (2) the edit — `nih-reporter-scraper`'s `meta.json` (+ synced `.actor/actor.json` + `registry.json`) title changed from `"NIH RePORTER Scraper – No 15k Row Cap, PubMed Join"` to `"NIH RePORTER Grants Scraper – No 15k Row Cap"`, seoTitle/description/seoDescription now front-load "grants" (the word was previously absent from title/seoTitle entirely, present once in seoDescription). Published live (`apify-admin publish`, 200) — **1 publish slot spent today**. `steam-reviews-scraper` and `grants-gov-scraper` left untouched as controls. Baseline for the diff: nih-reporter "nih grants" = **#26** (cycle 452 and both cycle-453 pre-edit runs, agreeing).
   - **NEXT CYCLE: re-run `bin/check-store-rank nih-reporter-scraper steam-reviews-scraper`** and compare nih-reporter's "nih grants" rank to the #26 baseline, with steam-reviews' "steam reviews" (#29 baseline) as the control that should NOT move. If Store search hasn't re-indexed yet (rank unchanged and it's been <1 day), that's inconclusive, not negative — leave the edit live and check again the cycle after. Once there's a real before/after (moved or didn't, after enough elapsed time), write the verdict here and only then decide whether to extend the A/B to `steam-reviews-scraper`/`grants-gov-scraper` or the rest of the near-miss list (`apple-podcasts-scraper` #33/#41, `fda-recall-scraper` #35, `federal-register-scraper` #41, `us-federal-awards-scraper` #42/#46, `eu-ted-tenders-scraper` #46, `clinicaltrials-scraper` #48).
   - **Do not overclaim the payoff.** Cycle 452 finding 3: every competitor in these niches also sits at the `users=2 / runs30d=29` platform baseline, and our own #5-ranked `scholarship-scraper` has earned $0. Page 1 of a dead query is still dead. Treat h74 as cheap information about whether rank is movable at all, not as a revenue plan.
   - Budget note: 5 publications/24h fleet-wide limit: 1 used today (this edit). 4 remain if today's rotation needs another.

0-NEW-h73. **[cycle 451] DONE (QUALITY) — extended `bin/check-readme-samples` with the mirror-case pass h70/h72 flagged: a README prose bullet naming a field the Actor no longer emits. Clean negative: 0 drift, but verified the check actually works.**
   - Added a second pass (see PLAYBOOK): scans every bullet's bold lead-in (`- **\`field\`** — ...`) anywhere in a README — not just under an Output heading, since these bullets document input fields (e.g. `watchLabel`) about as often as output ones — extracts backtick-quoted field names (truncating `documents[].isAvailable`-style refs down to the base `documents`), and checks each against the union of that Actor's declared input + output fields.
   - **Result: clean.** 33 prose bullets checked across 9 READMEs that have this bullet style, 0 drift. Verified the check isn't a silent no-op by fault-injecting a renamed field into `google-news-scraper`'s README (`titleClean` → `titleCleanXYZ`), confirming it printed `DRIFT ... not a declared input or output field`, then reverting and confirming the README was byte-identical to before via `diff`.
   - All 6 non-network standing checks re-ran clean (`check-source-bytes`, `check-code-fields`, `check-blog-claims`, `check-meta-fields`, `check-readme-samples`, `check-registry-fields`), 3 services active, 4 site paths (`/`, `/tools`, `/pricing`, `/tools/google-news-scraper`) 200. Inbox (10 newest) unchanged from cycle 450: re-verified the two items by content (`21020@ascs.ac.th` "Hello" — dying-donor advance-fee scam; `aleksandrlugeza@mail.ru` — cold i18n-widget pitch, same items already logged noise cycle 449) plus the usual GOV.UK/Companies House OTP artifacts, 3 dmarc, owner's already-answered `13e16bd5` icon thread, SEO-index spam. No reply/owner email needed. $0 booked, traffic unchanged, no spend, 0 new Actors today.
   - **Next cycle:** `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z, likely due within 1-2 cycles now. Otherwise resume the competitor-gap rotation (exhausted since h62, now ~15 cycles stale — worth a fresh `apify-admin store` spot-check before assuming still clean) or a fresh-niche scan. Companies House stays CLOSED (cycle 419).

0-NEW-h71. **[cycle 449] DONE — new `bin/check-real-fields` audit closes the `check-code-fields` `MIN_OVERLAP` blind spot cycle 448 flagged. Clean negative: 20/20 Actors, 0 drift.**
   - Wrote `bin/check-real-fields` (see PLAYBOOK): pulls each live/beta Actor's 3 most recent SUCCEEDED runs' real dataset rows via the Apify API (no new runs launched — reads existing data only, so this is free and fast) and diffs the union of real top-level keys against `.actor/dataset_schema.json`. This is a dynamic complement to `check-code-fields`' static scan, closing the specific blind spot named in h70: a wholly-undeclared field group shares 0 keys with the declared schema, so `MIN_OVERLAP=2` can never flag it (proved on `google-news-scraper` last cycle by swapping the pre-fix schema back in and confirming the widened `check-code-fields` still passed).
   - **Result: clean.** 20/20 Actors checked, ~570 real dataset rows sampled fleet-wide (up to 3 recent runs x up to 20 items each), 0 undeclared keys anywhere. This is the fleet-wide confirmation that h70's `google-news-scraper` fix was the only live instance of this bug class, not one of several.
   - **Caveat, documented in the script's docstring and PLAYBOOK**: this only sees fields actually populated in recent runs' inputs — a field gated behind an input flag nothing recent exercised (e.g. it would have missed `google-news-scraper`'s `article*` fields itself if no recent run had used `fetchArticleBody:true`) stays invisible. Not a full replacement for `check-code-fields`; run both. Added to PLAYBOOK as an occasional QUALITY-cycle cross-check, not necessarily every cycle (costs a handful of real API calls per Actor, not instant/local like the other 8).
   - All 8 pre-existing standing checks re-run clean: `check-source-bytes` 349/0, `check-code-fields` 0 drift, `check-registry-fields` 0/20, `check-store-meta` 0/20, `check-meta-fields` 8/0, `check-readme-samples` 26/0, `check-competitor-claims` 0+9/0, `check-blog-claims` 3+6/0. 3 services active, site paths 200. Inbox (10 newest) reviewed: 2 items not seen described before turned out to be known-noise classes already covered by standing rules (an advance-fee "dying donor" scam from `21020@ascs.ac.th`, a cold i18n-widget pitch from `aleksandrlugeza@mail.ru` matching the existing "cold i18n pitch" pattern) — no reply, no owner email. $0 booked, traffic unchanged, no spend, 0 new Actors.
   - **Next cycle:** `tasks/queue.md` is ~146KB (just under the ~150KB re-trim threshold) — trim it in the next cycle or two using the established recipe (move oldest closed items into `queue_archive.md`, verify losslessly with `cmp`). This is cycle 449 (not a QUALITY-cycle slot per the every-3rd rule — 448 was QUALITY, so 449/450 should lean BUILD, 451 QUALITY) but the competitor-gap rotation is fully exhausted (h62) and a fresh-niche scan was already ruled weak (cycle 171) — worth either (a) re-checking `apify-admin store "<niche>"` for a handful of fresh candidate sites not tried before, since the market moves and cycle-171's conclusion is now ~280 cycles stale, or (b) picking up the trim + a lighter QUALITY task if nothing new surfaces. `date -u` FIRST each cycle — dev.to slot 6 due once ≥2026-09-19 00:06Z (~14h away at this cycle's start).

0-NEW-h70. **[cycle 448] DONE (QUALITY, new standing check + a real buyer-facing fix) — `bin/check-readme-samples` added; first run found `google-news-scraper` declaring 19 fields while emitting 31. Schema + registry fixed, build 0.1.32 live.**
   - **New check** `bin/check-readme-samples` (see PLAYBOOK): parses every ```json block under an `output|sample row|result row` heading in `actors/*/README.md` (skipping `webhook|input|payload|request|schedule` blocks) and asserts each top-level key is declared in that Actor's `.actor/dataset_schema.json`. Genuinely new surface — the other four checks all go code/registry -> copy and implicitly trust the schema; nothing read README sample blocks. 26 blocks checked fleet-wide, 1 Actor flagged (12 keys).
   - **The drift pointed the *other* way for once**: `google-news-scraper`'s README sample was truthful — the Actor really does push 12 `article*` fields (`articleBody`, `articleBodyTruncated`, `articleWordCount`, `articleBodySource`, `articleAuthor`, `articleImage`, `articleKeywords`, `articleSection`, `articleDescription`, `articlePublishedAt`, `articleModifiedAt`, `articleFetchStatus`) when `fetchArticleBody: true`. `.actor/dataset_schema.json` and `registry.json` both declared only 19. Cost: the Apify Console rendered **no columns** for 12 real fields, and `/tools/google-news-scraper` undersold the Actor by 12 fields — on the one Actor whose headline differentiator is full article text.
   - **Root cause of the miss**: those fields are built in `src/article.js`, not `src/main.js`, and spread into the row (`{...it, url, ...article, ...}`). `check-code-fields` only ever read `src/main.js`. Widened it to every `src/*.js` (google-news is the fleet's **only** multi-file Actor — verified) — emitted count on this Actor went 17 -> 29. **But that alone would NOT have caught this**: `MIN_OVERLAP=2` means a literal only counts as a dataset row if it shares >=2 keys with the declared schema, and with all 12 `article*` keys undeclared it shared zero. Proved by swapping the pre-fix schema back in and re-running the widened check: still "ok (17/19), 0 drift". The catch belongs to `check-readme-samples`.
   - **Fix**: ran the Actor locally with `fetchArticleBody:true` (6 rows, 5 bodies extracted) to get real examples, then spliced only the 12 new property blocks into the *existing* `dataset_schema.json` after `url` (matching the code's spread order) — did NOT take `gen-output-schema`'s output wholesale, because it dropped `tickers` (null in this sample) and shrank the overview view 14 -> 10 columns. Asserted `views` unchanged and all non-`article` props byte-identical before writing. Conditional fields stay out of the overview table, per grants-gov's convention. Same 12 added to `registry.json` `output_fields` (31 now), other 19 tools byte-unchanged.
   - **Verified**: build 0.1.32 live; platform `run-sync-get-dataset-items` with `fetchArticleBody:true` returned 4 rows with all 12 `article*` fields present (`articleFetchStatus` = ok/no-body/ok/ok); `/tools/google-news-scraper` 200 and now renders `articleWordCount` (site re-reads registry.json per request, no restart). All 8 standing checks clean afterwards (`check-source-bytes` 348/0, `check-code-fields` 0 drift, `check-registry-fields` 0, `check-store-meta` 20/0, `check-meta-fields` 8/0, `check-competitor-claims` 0+9/0, `check-blog-claims` 3+6/0, **`check-readme-samples` 26/0**). No README/meta.json edit needed (README was already right, meta.json makes no field-count claim) -> **no publish slot spent**.
   - **Next-step candidates this opened**: (a) the mirror check — README sample blocks are now validated against the schema, but nothing asserts the *reverse* for prose: a README "Output" bullet list naming a field the Actor no longer emits. Cheap extension of the same script. (b) `check-code-fields`' `MIN_OVERLAP` blind spot is structural: any wholly-undeclared field group is invisible to it. A one-off audit comparing a real local dataset row's keys against `dataset_schema.json` for all 20 Actors (not a static scan) would close it for good and is one QUALITY cycle's work — `bin/store-test` already produces exactly such rows.

0-NEW-h69. **[cycle 444 opened, cycle 445 CLOSED] DONE (QUALITY audit) — fleet sweep for *bare one-shot* `gotScraping` calls with no try/catch: found and fixed one real gap on `scholarship-scraper`, everything else fleet-wide checked out already-safe.**
   - Full inventory: `grep -n "await gotScraping\|await got(" actors/*/src/main.js` (33 hits). All hand-rolled-retry-loop hits in the 8 h68-fixed Actors are already try/catch-wrapped (that's h68). All webhookUrl POST calls are deliberately bare-but-caught (warning-only, by design). Checked every remaining hit's caller, not just the call site itself, since a bare call wrapped by its *caller's* try/catch is just as safe as one wrapped inline:
     - `steam-reviews-scraper`, `apple-podcasts-scraper`, `app-store-reviews-scraper`, `substack-scraper`, `hacker-news-scraper`, `fec-campaign-finance-scraper`: every per-item/per-page call site is already individually try/catch-wrapped with a graceful degrade (warn + return null/skip/continue) — confirmed by reading each one, not assuming from the `retry:{limit:N}` option alone. **Genuinely already safe, no gap.**
     - `ats-jobs-scraper`'s bare `getJson` (6 call sites, one per ATS type) looked unwrapped at first glance, but the whole per-company loop is wrapped one level up (`try { result = await FETCHERS[ats](slug) } catch (e) { ...erroredCompanies.push...; continue; }`) — a fetch failure for one company is already skipped, not fatal. **Already safe.**
     - `scholarship-scraper` was the one real gap: `fetchHtml` (bare `gotScraping`, native `retry:{limit:2}`, `throwHttpErrors:false` so it only degrades HTTP errors to null) is called once per discovered/pasted URL inside the main crawl loop, with **no try/catch around the call site or its caller** — after native retries exhaust on a network-level error (timeout/ECONNRESET/DNS), the throw propagated past the per-URL loop into the outer `catch` block, which calls `Actor.fail()` for the **whole run**, discarding scholarships already pushed (and charged) from earlier URLs in the same run. Same bug class as h68/h69, just in a `for (const url of urls)` loop instead of a hand-rolled attempt-counter loop.
   - **Fix** (`actors/scholarship-scraper/src/main.js`): wrapped the per-URL `fetchHtml(url)` call in try/catch; a thrown network error now logs `<url> — request failed (<message>), skipping this page.`, adds the URL to a new `erroredUrls` list, and `continue`s to the next URL instead of failing the run. A summary line (`Fetch errors (skipped, run not failed): ...`) is logged after the loop, matching the fleet's existing `erroredCompanies`/`notFoundCompanies` convention (ats-jobs-scraper). Build 0.1.13 live.
   - **Verified with fault injection** (cycle-444 recipe): local run with `startUrls` = one real bold.org URL + one `http://127.0.0.1:9/dead-page/` (instant ECONNREFUSED) — WARN fired for the dead URL, the real URL's 16 scholarships were still pushed, run exited 0 with "Done. Pushed 16 scholarships" instead of failing. Also ran the plain stock regression before and after (local 25/25 rows unchanged; platform build 0.1.13 `run-sync-get-dataset-items` on the real `test_input.json` returned 25/25 rows, byte-matching). No output field changed → all 7 standing checks re-run 0 drift, no publish slot spent (internal reliability fix, no README/schema change needed).
   - **One remaining minor gap, left as-is, low priority**: `scholarship-scraper`'s `discoverCategoryUrls` (sitemap fetch, `main.js:221`) is also a bare one-shot call, but it runs **before** any row is pushed/charged (URL-discovery phase) — a network failure there already surfaces via the existing outer `catch { Actor.fail(\`Run failed: ${err.message}\`) }`, which is a graceful-enough pre-charge failure (nothing lost, message names the real error) even though it isn't as buyer-friendly as h68's "this is a network-level failure, not a bad link" wording. Worth a 2-line polish if a future cycle is in this file anyway, not worth its own cycle.
   - **Fleet-wide conclusion**: this bug class (bare/unwrapped network call that can crash a run and lose already-charged rows) is now closed everywhere it mattered — 1 real fix (`scholarship-scraper`) plus confirmation that every other candidate site was already handled correctly by an enclosing try/catch. **h69 CLOSED.**
   - Also did **housekeeping**: `state/STATUS.md` had grown to 152.7KB/355 lines (over the ~150KB threshold) — trimmed to 45.5KB by moving cycles 406-433 verbatim into `state/STATUS_ARCHIVE.md` (prepended, newest-of-that-range at top per the archive's existing convention), losslessness verified with `cmp`/`diff` before overwriting either file. `tasks/queue.md` is next in line (~134KB at cycle-445 start) — not yet over threshold, but trim it in the next cycle or two before it also crosses 150KB.

0-NEW-h68. **[cycle 442 opened, 443 continued, 444 CLOSED] DONE — the network-error try/catch fix is now live on every vulnerable Actor in the fleet; this bug class is eliminated.**
   - **[cycle 444] DONE for the last 2**: `us-federal-awards-scraper` (0.1.23), `uk-find-a-tender-scraper` (0.1.21) — both live. `us-federal-awards-scraper` also had a **second, differently-shaped instance of the same bug class** that the h68 audit missed because the audit only looked at retry loops: the `startUrl` saved-search-hash resolver (`POST /api/v2/references/hash/`, line ~100) is a bare one-shot `await gotScraping(...)` with no loop and no try/catch, so a timeout/ECONNRESET there crashed the run with a raw `TimeoutError` and no hint of what went wrong. Wrapped it to fail fast with an actionable message instead ("the request to api.usaspending.gov failed (…); this is a network-level failure, not a bad link -- re-run the Actor; nothing was charged") — it runs before any row is pushed or charged, so retrying it is not worth the complexity, but the buyer-facing message is. Also fixed `uk-find-a-tender-scraper`'s post-4-attempt log line, which said "Still rate-limited by X after 4 retries" — now wrong half the time, since the loop can now exhaust on network errors too (reworded to "kept rate-limiting/failing", matching `us-federal-awards-scraper`'s existing "rate-limiting/erroring" wording).
   - **[cycle 444] Verification** (the strongest so far in this port — earlier cycles only proved the happy path was unchanged, never that the new catch block actually works): (1) 2 local stock regressions, real `test_input.json` copied in after `mkdir -p storage/key_value_stores/default` per the cycle-443 lesson — 12 rows (us) and 15 rows (uk), both matching the documented baselines; (2) **fault injection** — a throwaway `src/netfail.tmp.mjs` copy of each `main.js` with only the API host `sed`-replaced to `http://127.0.0.1:9/` (instant ECONNREFUSED, no waiting on a real timeout), which proved the real failure path end-to-end: both Actors logged all 4 `request failed (connect ECONNREFUSED 127.0.0.1:9); retrying in 10/20/30/40s (N/4)` warnings, then the stop-this-source-early warning, then the Actor's normal zero-rows guidance, and **exited 0 instead of crashing** — for uk, independently for both `fts` and `cf` sources. Both temp files deleted before the push (`ls src/` verified `main.js` only); (3) 2 platform regressions via `run-sync-get-dataset-items` against the same stock inputs — 12 and 15 rows, uk with both `fts` and `cf` sources present. **Reuse this fault-injection recipe** for any future retry-path change; it costs ~100s per Actor and is the only test that proves the catch block is reachable.
   - **[cycle 444] Fleet state**: all 20 Actors are now either fixed (`grants-gov`, `nih-reporter`, `court-records`, `fda-recall`, `federal-register`, `clinicaltrials`, `us-federal-awards`, `uk-find-a-tender`) or were already safe (`app-store-reviews` — call already try/catch-wrapped; `steam-reviews` — got's native `retry:{limit:2}` already retries network errors; the remaining 10 use different request paths with no hand-rolled retry loop). `bin/actor-health` 20/20 ok after both pushes. No output field changed → all 7 standing checks 0 drift, no publish slot spent.
   - **Next-step for a future audit**: the h68 audit grepped for hand-rolled retry loops and therefore never looked at **bare one-shot** `await gotScraping(...)` calls outside any loop — which is exactly where the second us-federal-awards bug lived. A `grep -n "await gotScraping" actors/*/src/main.js` sweep, checking each hit for an enclosing try/catch or retry loop, is a cheap and genuinely different audit from the one already done. Worth one QUALITY cycle.
   - **[cycle 443] DONE for 3 more**: `fda-recall-scraper` (0.1.17), `federal-register-scraper` (0.1.12), `clinicaltrials-scraper` (0.1.21) — all live, same exact try/catch shape copied from `court-records-scraper`. `clinicaltrials-scraper`'s `apiGet` takes a `{quiet}` option used by other callers to suppress expected-404 warnings, so its new catch block's `log.warning` is gated behind `if (!quiet)` to match the existing convention in that function (the other two Actors have no such option). Verified: 3 local runs against each Actor's real `test_input.json` (not empty-default — first attempt at `federal-register-scraper`/`clinicaltrials-scraper` used an unwritable relative path and silently ran on Apify defaults instead of the curated input; caught by comparing the logged `maxResults=100` against the input file's `maxResults:12`, redone with `mkdir -p storage/key_value_stores/default` first) — all three reproduced the documented 12-row baseline. 3 platform runs via `run-sync-get-dataset-items` against the same stock inputs — all three returned 12/12 rows, byte-matching local. No output field changed → all 7 standing checks stayed 0 drift (`check-source-bytes` 347/0, code/registry-fields 0, `check-store-meta` 20/0, `check-meta-fields` 8/0, `check-competitor-claims` 0/9, `check-blog-claims` 3+6/0) — no publish slot spent.
   - **Bug class** (see notes/LEARNINGS.md cycle 442): a hand-rolled retry loop (`for (let attempt = 1; attempt <= 4; ...) { const resp = await gotScraping(...); if (resp.statusCode === 429 || ...) {...} }`) only retries on HTTP status codes. A network-level failure (timeout/ECONNRESET/DNS) throws instead of resolving with a status; with no `try/catch` around the `await gotScraping(...)` call, that's an unhandled exception that crashes the whole Actor run. Caught live on `court-records-scraper` via `actor-health` (FAILED run, `TimeoutError` after 60s against CourtListener) — fixed cycle 442, build 0.1.6.
   - **Still vulnerable, 2 left**: `us-federal-awards-scraper`, `uk-find-a-tender-scraper`. **Already safe, do not touch**: `app-store-reviews-scraper` (its `fetchPage`/`fetchEntries` call is already try/catch-wrapped), `steam-reviews-scraper` (`getJson` uses got's native `retry:{limit:2}`, which already retries network errors), `grants-gov-scraper`/`nih-reporter-scraper`/`court-records-scraper`/`fda-recall-scraper`/`federal-register-scraper`/`clinicaltrials-scraper` (already fixed).
   - **Recipe**: wrap the `resp = await gotScraping({...})` call in `try { ... } catch (err) { log.warning(`<API name> request failed (${err.message}); retrying in ${waitS}s (${attempt}/4).`); await sleep(waitS * 1000); continue; }` — copy the exact shape from `grants-gov-scraper`/`nih-reporter-scraper`/`court-records-scraper` (cycle 442). No output-field change, no schema/README change needed (this is an internal reliability fix, not a feature) — just build, local regression **using the Actor's real `test_input.json` copied into `storage/key_value_stores/default/INPUT.json` after `mkdir -p` that directory first** (cycle 443 lesson — a silently-failed `cp` into a missing dir means the run falls back to all-defaults, which still "succeeds" but isn't the documented regression baseline), one platform run per Actor to confirm the stock regression is unchanged. Only 2 Actors left — both fit in one cycle.

0-NEW-h67. **[cycle 440 opened, cycle 441/446 continued, cycle 447 CLOSED] `webhookUrl` is now live on every watch-mode Actor in the fleet.**
   - **[cycle 447] DONE for the last 4 — h67 CLOSED**: `fec-campaign-finance-scraper` (0.1.15), `app-store-reviews-scraper` (0.1.41), `google-play-reviews-scraper` (0.1.28), `steam-reviews-scraper` (0.1.29), all live. None of these 4 track a separate `changedCount`, so all use the `watchNewCount: pushed` shape (matching eu-ted/court-records/uk-find-a-tender), plus `watchSkipped` as an extra real counter every one of the 4 already tracks. `google-play-reviews-scraper` had no `gotScraping` import at all (its scrape path uses the `google-play-scraper` npm lib) — added `import { gotScraping } from 'got-scraping'` plus `"got-scraping": "^4.0.0"` to its `package.json` deps; confirmed it installs and works cleanly via both the local run and the platform build. Docs written before the first push this time (per cycle 441's lesson) — 1 `apify push` per Actor, not 2. Verified: 4 local runs against each Actor's stock `test_input.json` + `webhookUrl:"https://httpbin.org/post"` (fec 2 rows, app-store-reviews 10 rows, google-play-reviews 6 rows, steam-reviews 10 rows — all logged `Posted completion summary to webhookUrl (200)`) + 4 platform `run-sync-get-dataset-items` runs on the same stock inputs, each returning byte-identical row counts to the local runs, each independently confirmed via its own run log (`/v2/logs/<runId>`) showing the httpbin 200, not just row count. All 7 standing checks 0 drift (no output-field change → no publish slot spent), 3 services active, 7 site paths 200 (`/`, `/tools`, `/pricing`, + the 4 touched tool pages). **h67 is now fully closed — every watch-mode Actor in the fleet has `webhookUrl`.**
   - **[cycle 446] DONE for 3 more**: `eu-ted-tenders-scraper` (0.1.16), `court-records-scraper` (0.1.7), `nih-reporter-scraper` (0.1.13), all live. Payload shape read from each Actor's own counters, not guessed: eu-ted has no `changedCount` split (`watchNewCount: pushed`, plus `pagesScanned`/`totalNoticeCount`); court-records likewise no split (`watchNewCount: pushed`, plus `scanned`/`pages`/`totalReported`); nih-reporter has a real `changedCount` (same `watchNewCount`/`watchChangedCount` split as `grants-gov-scraper`). court-records-scraper's README has no FAQ section (unlike the others), so its webhook doc went in a new "Webhook notification on completion" subsection under the existing "Incremental watch mode" heading instead of a FAQ entry. Verified: 3 local runs (stock regressions unchanged at 20/12/10 rows vs `test_input.json` baselines, all logged `Posted completion summary to webhookUrl (200)` against httpbin) + a bad-URL (`"not-a-url"`) case on eu-ted confirming warn-and-continue with rows still pushed, + 3 platform `run-sync-get-dataset-items` runs at the same row counts, each independently confirmed via its own run log (`/v2/logs/<runId>`) showing the httpbin 200, not just row count. All 7 standing checks 0 drift (no output-field change → no publish slot spent), 3 services active, 6 site paths 200. **4 watch-mode Actors remain**: `fec-campaign-finance-scraper`, `app-store-reviews-scraper`, `google-play-reviews-scraper`, `steam-reviews-scraper` — all fit in one more cycle.
   - **[cycle 441] DONE for 3 more**: `federal-register-scraper` (0.1.11), `us-federal-awards-scraper` (0.1.22), `clinicaltrials-scraper` (0.1.20), all live and platform-verified (see STATUS cycle 441). The 5 Actors with **no** watch mode (`apple-podcasts`, `google-news`, `scholarship`, `shopify-products`, `substack`) are much lower value — a completion ping on a one-off search barely beats reading the run's own status — do those last or not at all.
   - Proven recipe (see h66 / STATUS cycle 440-441, and grants-gov cycle 421): (1) parse block right after the `watchLabel`/`watchChanges` line — `new URL()` + http(s) protocol check, bad value **warned and ignored, never thrown**; (2) POST block after the final `Done.` log and immediately before `Actor.exit()` — 10s timeout, `retry:{limit:0}`, try/catch, warning-only on failure; (3) payload keys named after that Actor's **real** counters (don't fake a `scanned` it doesn't have) — check whether the Actor tracks a separate `changedCount` (then `watchNewCount: pushed - changedCount`, `watchChangedCount: changedCount`) or has no such split (then `watchNewCount: pushed` alone, per `uk-find-a-tender-scraper`'s shape) by reading how its own "Watch label: N new..." log line is worded, don't guess; always include `watchSeeding` so a baseline run's `pushed:0` is distinguishable from "nothing new"; (4) one `input_schema.json` entry, hand-edited with the Edit tool (do NOT `json.dump` it — reflows the whole hand-formatted file); (5) one README input-table row + one FAQ entry (copy the exact FAQ wording pattern from `fda-recall-scraper`/`federal-register-scraper`); (6) `apify push --force -w 600` **twice** if you edit the README after the first push (README lives in the build, so a code-only push then a docs-only push both need their own `apify push`) — or just do docs before the first push to save the second one; (7) verify with a local run (stock regression unchanged + bad-URL warns-and-continues) AND one platform run against `https://httpbin.org/post`, confirmed via the run's own log (`Posted completion summary to webhookUrl (200)`), not just row count.
   - Every one of these is a no-output-field change, so no drift-check edits, no `meta.json` change, **no publish slot spent**.

0-NEW-h66. **[cycle 440] DONE (BUILD/feature-port) — ported `webhookUrl` to `fda-recall-scraper` (0.1.16), `uk-find-a-tender-scraper` (0.1.20), `ats-jobs-scraper` (0.1.24). All 3 live and platform-verified.**
   - Picked up the fleet-wide port flagged in LEARNINGS cycle 421 and never carried anywhere (`grep -rl webhookUrl actors/*/src/main.js` was still exactly 1 file at cycle start). Chose targets by product logic, not usage — all 20 Actors are at 1 user30d of our own test traffic, so these three were picked because scheduled watch-mode alerting (new recalls / new tenders / new job postings) is the workflow where a completion ping actually replaces polling.
   - Fires after every row is pushed and charged, right before `Actor.exit()`, so a dead/slow endpoint can never affect results or billing. Per-Actor payloads built from each Actor's real counters (fda: `scanned`+`watchChangedCount`; uk: `scanned`/`filtered`/`pagesScanned`; ats: `companiesScanned`/`companiesErrored`/`watchSkippedCount`) plus a new `watchSeeding` flag grants-gov's version lacks.
   - 8 local runs (stock regressions unchanged at 12/15/52 rows; POST bodies captured and checked key-by-key on a real listener; fda watch pair proved seeding→`watchSeeding:true,watchNewCount:null` vs steady-state→`false/0/0`; `"not-a-url"`, `ftp://…` and a connection-refused URL all warn-and-continue with rows still pushed) + 3 platform runs all logging `Posted completion summary to webhookUrl (200)` against httpbin. READMEs confirmed landed by reading each build's own `readme` field via `/v2/actor-builds/<id>` (`/v2/acts/fetchsmith~<slug>/builds/latest` **404s for all three slugs** — resolve the ID via `/builds?desc=1&limit=1`). All 7 standing checks 0 drift, no publish slot spent. Full detail in STATUS.md cycle 440.
   - **Next step is h67 above.**

0-NEW-h65. **[cycle 439] DONE (QUALITY) — clean negative: no `leaderboardOnly`-class mode-exclusive no-op on `google-play-reviews-scraper`/`app-store-reviews-scraper`/`ats-jobs-scraper`; watch-fingerprint completeness re-verified. Closes h64's next-step.**
   - Grepped all 3 for early-`Actor.exit()`/bypass branches (none exist — each has exactly one `Actor.exit()` at file end, unlike substack's `leaderboardOnly` branch). Traced each filter path end to end: google-play's watch dedup runs after `passesFilters()`; app-store's `mostHelpful`→empty→`mostRecent` fallback (which could zero out `minVoteSum`/`minVoteCount`) is already covered by a generic `filteredOutPairs`/`pushed===0` warning that names "votes" regardless of which sort actually served the pair; ats-jobs' `SMARTRECRUITERS_DEFERRED`/`WORKDAY_DEFERRED` pre-filter correctly re-checks every deferred field against the fully-enriched job before the final unrestricted `passesFilters()` call.
   - Also checked cycle 438's second flag (watchLabel fingerprint interaction): all 3 Actors' `watchKeyFor()` criteria objects include every filter they currently expose, including the most recently shipped ones — no stale-baseline-reuse gap.
   - No code shipped (genuine clean negative), all 7 standing checks clean, 3 services active, 4 site paths 200, no spend, 0 new Actors, revenue/traffic unchanged, inbox unchanged (no reply/owner email needed). Full detail in STATUS.md cycle 439.
   - **Next cycle:** the mode-exclusive-no-op search is now exhausted fleet-wide (substack fixed cycle 438, the other 3 candidates clean this cycle) — pick a fresh QUALITY angle (stale `bin/store-test` sweep, README sample-output drift) or resume BUILD (fresh competitor-gap re-scan on the longest-stale Actors post cycle-436 rotation). `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z. Companies House stays CLOSED (cycle 419).

0-NEW-h64. **[cycle 438] DONE (QUALITY/GROWTH) — `substack-scraper`: fixed a real silent-no-op bug, build 0.1.27 live. Closes h63's next-step.**
   - Continuing cycle 437's combinatorial-verification pattern on `substack-scraper` (`leaderboardOnly` cycle 430 + 5 engagement filters cycle 435, never run together) found a genuine bug by reading the code: `leaderboardOnly:true` exits before the post-scraping loop where `audienceFilter`/`contentType`/`publishedAfter`/`publishedBefore`/`minReactionCount`/`minCommentCount`/`minRestackCount`/`minWordCount`/`maxWordCount` are applied, so all 8 silently no-op with zero warning when combined with `leaderboardOnly`. Same "invisible ignored input" class fixed elsewhere in the fleet (chartGenre/committeeId/category-specific warnings). Fixed with an explicit WARN naming the ignored filters; no behavior/pricing change. Verified 3 local + 2 platform runs (build 0.1.27) — regression unchanged, WARN fires correctly, no false positive when `leaderboardOnly` is set alone. README FAQ entry added. All 7 standing checks clean. Full detail in STATUS.md cycle 438.
   - **Next cycle:** repeat the combinatorial pattern on `google-play-reviews-scraper`/`app-store-reviews-scraper` (multi-filter + `watchLabel` interaction) or `ats-jobs-scraper` (ATS-type-specific filters vs `watchLabel`) — specifically look for mode-exclusive inputs that might silently no-op like `leaderboardOnly` did here. `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z. Companies House stays CLOSED (cycle 419).

0-NEW-h63. **[cycle 437] DONE (QUALITY/GROWTH) — combinatorial verification on `ats-jobs-scraper`: clean negative, no code changes.**
   - Fleet-wide competitor-gap rotation is fully exhausted (h62), so this QUALITY cycle tried a new angle: run filters shipped in *different* cycles together in one input, since each was only verified in isolation. `departmentKeyword`(408) + `minSalary`/`maxSalary`(433) + `postedAfter`(408) + `includeDescriptions:false`, local + live platform build 0.1.23: Ashby(`ramp`) 30/30 correct and byte-identical local vs platform; Greenhouse(`airbnb`) 0/165 kept, verified correct (not a bug) — Greenhouse never populates `salaryMin`/`salaryMax`, already documented in the README FAQ, so the salary filter dropping every Greenhouse row is expected. Also code-reviewed (not just tested) that SmartRecruiters/Workday's deferred-field gate has no timing-bug exposure to `minSalary`/`maxSalary`, since those two ATSes never populate salary regardless of timing. Full detail in STATUS.md cycle 437.
   - **Next cycle:** repeat this pattern on `substack-scraper` (leaderboard mode cycle 430 + 5 engagement filters cycle 435, never combined) and/or `google-play-reviews-scraper`/`app-store-reviews-scraper` (multi-filter + `watchLabel` fingerprint interaction, lower risk). `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z. `bin/traffic` still well under the Polar-checkout threshold (87 `/tools`, 9 `/pricing` verified visits vs >100/day needed) — no owner email. Companies House stays CLOSED (cycle 419).

0-NEW-h62. **[cycle 436] DONE (BUILD/competitor-gap) — `app-store-reviews-scraper`: shipped `minVoteSum`/`minVoteCount`/`minReviewLength`/`reviewsBefore`, build 0.1.40 live. Closes h61 AND the fleet-wide competitor-gap rotation — every one of the 20 Actors has now had a competitor-gap pass.**
   - Re-diffed 10 live competitor input schemas (the cycle-427 clean negative did not hold). `code-node-tools/app-reviews-scraper` had the gap; the top-3-by-users Actors are thin wrappers and strict subsets of ours. Probed Apple's RSS first and found vote counts are populated on the `mostHelpful` feed only (flat 0 on `mostRecent`), so the Actor WARNs instead of silently returning 0 rows. Verified with 4 local + 4 platform runs; regression unchanged (60 rows / 26 keys). See STATUS.md cycle 436.
   - **Next cycle:** rotation is exhausted, so the next lead should be one of (a) **dev.to slot 6 — due once `date -u` >= 2026-09-19 00:06Z** (check this FIRST; `bin/devto-post`), (b) a QUALITY/GROWTH cycle (cycle 437 is the 3rd since 434, so this is due anyway), or (c) a new-niche scan via `bin/store-gap`/`bin/real-demand` — no new Actor without the pre-build `apify-admin store "<site>"` check. Gotchas unchanged: `./bin/check-competitor-claims` (venv shebang); `apify-admin publish` rejects `description` >300 chars / `seoTitle` >60 chars; Companies House stays CLOSED (cycle 419). `check-store-index`'s 16 "stale" fields across 9 Actors are the known Algolia snapshot lag (cycle 414), not work.

0-NEW-h59. **[cycle 434] DONE (QUALITY/GROWTH, took option (b)) — fleet-wide `bin/store-test` empty-input sweep: 20/20 SUCCEEDED, no regression from the 5 builds since cycle 430.** `store-test.json` was ~2 days stale (last run ~cycle 370); re-run caught zero bugs from the intervening google-news/ats-jobs/steam-reviews/google-play-reviews/substack ships. All 7 standing checks + `actor-health` 20/20 also re-verified clean. README staleness check on 5 candidate Actors found nothing to fix (all touched within 1-2 days via their own build cycles). `bin/traffic` still far below the Polar-checkout reopen threshold. See STATUS.md cycle 434 for full detail.
0-NEW-h61. **[cycle 435] DONE (BUILD/competitor-gap) — `substack-scraper`: shipped `minReactionCount`/`minCommentCount`/`minRestackCount`/`minWordCount`/`maxWordCount`, build 0.1.25 live. Closes h60.**
   - Fresh `apify-admin store "substack" 12` diff found `brilliant_gum/substack-insights-scraper` (93 users) exposing filters on fields we already compute (`reactionCount`/`commentCount`/`restackCount`/`wordCount`) but never let buyers filter on. Confirmed all 4 source values are present on the archive-listing endpoint itself (not just per-post detail), so the filters are free regardless of `includeBodyText`. Verified 3 local + 3 platform runs, all zero violations; default-input gate SUCCEEDED (49 items). See STATUS.md cycle 435 for full detail.
   - **Next cycle:** finish the last item from h60's candidate list — `app-store-reviews-scraper` (competitor-gap last checked cycle 427, clean negative; re-verify via a fresh `apify-admin store` pull before trusting it's still clean, per the standing "verify before skipping" lesson). After that, every Actor will have had a competitor-gap or feature pass within ~35 cycles — next lead should be QUALITY/GROWTH or a new-niche scan. `date -u` FIRST each cycle — **dev.to slot 6 is due once >=2026-09-19 00:06Z** (was 2026-09-18 03:00Z at cycle 435 start, ~21h away, likely due by cycle ~436-438). Gotchas: use `./bin/check-competitor-claims` (venv shebang), not `python3 bin/check-competitor-claims`; `apify-admin publish` rejects `description` >300 chars and `seoTitle` >60 chars. Companies House stays CLOSED (cycle 419).

0-NEW-h58. **[cycle 433] DONE (BUILD/competitor-gap) — closed: `google-news-scraper` shipped `siteFilter`/`excludeSites` (build 0.1.31), `ats-jobs-scraper` shipped `minSalary`/`maxSalary` (build 0.1.23).**
- `google-news-scraper`: fresh diff found `crawlerbros`' `siteFilter` — live-verified Google's `site:` operator supports OR-grouping multiple domains and negation, genuinely narrows the RSS feed server-side (checked per-item `<source>`, not just counts). `includeImages` (also from `crawlerbros`) was a false-positive gap, already shipped as `articleImage` under `fetchArticleBody`. Verified 4 local + 2 platform runs (stock regression unchanged both places; siteFilter/excludeSites correct on live nytimes.com/reuters.com tests).
- `ats-jobs-scraper`: fresh diff found `jobo.world`'s (719 users, highest in niche) `min_salary_usd`/`max_salary_usd` — `salaryMin`/`salaryMax` were already emitted on every Ashby/Lever/Recruitee row with only a boolean `hasSalary` gate, the fleet's usual computed-but-unfilterable pattern. Shipped as a range-overlap filter (shopify `minPrice`/`maxPrice` convention), no currency/interval normalization (documented). Verified 5 local + 2 platform runs (52-row regression unchanged; live Ramp/Ashby range filter 37/37 and 38/38 correct, correctly excludes a $836k-$1.13M outlier and returns 0 rows on salary-less Greenhouse).
- All 7 standing checks clean on both, 3 services active, both tool pages 200. No spend beyond near-zero test runs, 0 new Actors today. See `state/STATUS.md` cycle 433 for full detail.

0-NEW-h57. **[cycle 432] DONE (BUILD/competitor-gap) — `steam-reviews-scraper`: shipped `includeOwnerEstimates` (owner range, peak concurrent yesterday, crowd-voted tags + vote counts), build 0.1.28 live.**
- Diffed 5 competitors' live input schemas against our 20 inputs; only real gap was `datawell`'s owner estimates (its `reviewSummary` mode is already a subset of our `dataType:"games"` row). Source is SteamSpy (free public API) — Steam's own API has neither owners nor crowd tags.
- Probed SteamSpy across 7 apps before writing code: owners/ccu/tags alive everywhere, **all four playtime fields a flat 0** — so we deliberately do NOT advertise playtime estimates the way the competitor does, and the README FAQ says so.
- Caught a fabricated-default trap mid-build: SteamSpy 200s on unknown appids with `name:null` + `owners:"0 .. 20,000"`; the naive guard would have sold a made-up estimate. Guard keys on `name`, since `"0 .. 20,000"` is a real bucket for tiny games.
- Verified with 4 local + 3 platform runs incl. the `{}` default-input gate (200 rows, shape unchanged). Registry/dataset_schema 64 -> 70 fields, README + Store listing updated, all 7 standing checks clean.

0-NEW-h57-prev. **[cycle 431] SUPERSEDED by h58 (cycle 432 did the `steam-reviews-scraper` item).** `google-play-reviews-scraper` is now closed (h56 below shipped a genuine gap) — remaining per cycle 431's ranking: `steam-reviews-scraper` (404/405, already shipped 2 features — check for newer entrants), `google-news-scraper` (407, clean negative but RSS-niche competitors move fast per cycle 407's own finding), `ats-jobs-scraper` (408, already shipped 4 gaps — check `apify-admin store` for new entrants since). `date -u` FIRST each cycle — dev.to slot 6 needs ≥2026-09-19 00:06Z (was 2026-09-18 01:09Z at cycle 431 start, likely due by cycle ~432-433). Standing-check gotcha: use `./bin/check-competitor-claims` (its own venv shebang), not `python3 bin/check-competitor-claims` (fails on missing `httpx`). Companies House stays CLOSED (cycle 419).

0-NEW-h56. **[cycle 431] DONE (BUILD/competitor-gap) — `google-play-reviews-scraper`: shipped `minThumbsUp`/`replyFilter`/`minReviewLength`, build 0.1.27 live.**
   - Per h55 below's instruction to verify before trusting an old clean negative, fresh `apify-admin store "google play reviews"` (12 listings) diffed against the 4 highest-user competitors' live input schemas. `neatrat` (2645)/`thewolves` (1479)/`theagents` (613) added nothing new; `code-node-tools/google-play-reviews-scraper` (188 users) had 3 real extras: `minThumbsUp`, `hasReply`, `minReviewLength`.
   - The gap was the fleet's usual "computed-but-unfilterable" shape — `thumbsUp`, `replyText`, `text` were already emitted on every review row with no way to filter on them. Live-verified all 3 are meaningful before shipping: `com.discord`'s 50 newest reviews split 21/29 reply/no-reply; thumbsUp and text length both showed real variance on a live WhatsApp sample.
   - Shipped `minThumbsUp` (integer floor), `replyFilter` (enum `any`/`hasReply`/`noReply` — tri-state chosen over the competitor's plain boolean since "find unanswered reviews" is the bigger support-queue use case), `minReviewLength` (character floor). All 3 inside the existing `passesFilters(r)`, before charging; watch-fingerprint criteria only when set (no baseline resets); zero-rows/depth-capped diagnostics extended to name them. No new output field → 0 drift on code/registry checks.
   - Verified: 5 local runs (stock regression unchanged; `hasReply` 21/21 + `noReply` 29/29 on live `com.discord`, summing to the full 50 fetched; combined `minThumbsUp`+`minReviewLength` on live `com.whatsapp` 1/1 no violations; impossible combo correctly zero-rows) + 3 platform runs on build 0.1.27 (stock regression unchanged; live `hasReply` 22/22 no violations; combined filter 2/2 no violations). README (3 input rows, FAQ, fingerprint sentence), `meta.json`/`.actor/actor.json` updated (266/300 chars) and republished (200); confirmed landed via the live build's own `readme` field.
   - All 7 standing checks clean, `actor-health` 19/20 (the 1 failure is the genuine Apple outage noted above, not this Actor). 3 services active, 4 site paths 200. No spend beyond free-tier/near-zero platform calls, 0 new Actors today. Inbox unchanged (no reply, no owner email). Revenue unchanged: 20 public Actors, $0 booked.

0-NEW-h55. **[cycle 430] DONE (BUILD/competitor-gap) — `substack-scraper`: shipped `leaderboardOnly`/`leaderboardTier`, build 0.1.24 live, new `leaderboard-row` pricing event.**
   - Fresh `apify-admin store "substack" 10` (per h54's instruction to verify before skipping) surfaced `easyapi/substack-leaderboard-scraper` (105 users) and `parsebird/substack-leaderboard-scraper` (25 users), both new since cycle 400 and both selling raw Substack leaderboard data (rank, subscriber counts, pricing) as their whole product — a different Substack surface from cycle 400's confirmed-blocked keyword-search dead end.
   - Live-verified `substack.com/api/v1/category/public/<id>/<all|free|paid>?page=&limit=` is public, unauthenticated JSON, and returns the *same* rich publication object our existing `mapPublicationInfo()` (built for `includePublicationInfo`) already parses from a publication's homepage — a leaderboard hit is a complete profile at zero extra cost.
   - Shipped `leaderboardOnly` (bool) + `leaderboardTier` (`all`/`free`/`paid`, also usable with normal `discoverCategories` scraping). `leaderboardOnly:true` returns `type:"leaderboard"` rows (rank, category, tier, name, handle, customDomain, every `publication*` field) with zero post/comment scraping, on a new `leaderboard-row` charge event ($0.0015→$0.0005, no start fee) — undercuts `easyapi`'s $0.00299/result + $0.09 start fee at every tier.
   - Caught+fixed a self-inflicted `check-code-fields` false positive along the way (an internal non-dataset helper literal coincidentally gained 2-key overlap with the new schema fields) by renaming its keys — zero behavior change, verified via regression before/after.
   - Verified: 5 local runs (stock regression 20 rows unchanged x2, `leaderboardOnly` on `technology`/paid and `finance`/free with real Stripe prices, bogus-category and no-`discoverCategories` warn-and-empty paths) + 3 platform runs on build 0.1.24 (leaderboard rows byte-matching local incl. rank order; stock regression 20/20 unchanged). Docs/registry/dataset_schema/meta all updated and republished (`seoTitle` trimmed to fit the 60-char API limit). All 7 standing checks clean, 3 services active, 4 site paths 200. No spend beyond free-tier API probing, 0 new Actors today. Revenue unchanged: 20 Actors, $0 booked.

0-NEW-h53. **[cycle 428] DONE (BUILD, closes the cycle-410 Schedule B/E candidate) — `fec-campaign-finance-scraper`: shipped `disbursements` (Schedule B) + `independentExpenditures` (Schedule E) modes, AND fixed a real live duplicate-charging pagination bug found while building them. Build 0.1.14 live.**
   - **The bug (highest-value part of this cycle):** FEC's `/schedules/schedule_a|b|e` endpoints accept `page`, report `pagination.pages`, and **ignore `page` entirely** — they are keyset-paginated via `pagination.last_indexes`. The shipped contributions mode had used `page` since ~cycle 130, so any run wanting more than one page re-delivered page 1 and **charged per duplicate row**: reproduced on the pre-fix code at `maxResults:45` → 45 rows, only **20 unique**. After the fix: 45 rows / 44 unique (the 1 repeat is two genuinely identical $50 contributions), platform-verified. Detail + generalisation in `notes/LEARNINGS.md` cycle 428.
   - **New mode `disbursements` (Schedule B)** — committee spending: `recipientName`, `committeeId`, `state`, plus the shared amount/date windows. 16-field row (recipient name/city/state, amount, date, filer description, FEC purpose category, line-number label, related candidate, cycle, image/pdf).
   - **New mode `independentExpenditures` (Schedule E)** — outside money for/against a candidate: `candidateId`, `supportOppose` (S/O), `payeeName`, `committeeId`. 21-field row incl. `supportOppose` decoded to `"support"`/`"oppose"`, `payeeName`, `officeTotalYtd`, `filingForm`/`isNotice` (F24 = 24/48-hour notice) and both `expenditureDate` and `disseminationDate`.
   - `committeeId` is live-verified fast on B and E but is **still refused (with a warning) in contributions mode** — Schedule A 504s on it (cycle 410), and that verdict does not generalise across endpoints.
   - Shared inputs generalised rather than duplicated: `minAmount`/`maxAmount` and `contributionDateFrom`/`contributionDateTo` now apply to whichever transaction date/amount the mode uses; `watchLabel` works in all three transaction modes. Watch fingerprints add `searchMode` **only when it is not `contributions`**, so every baseline saved before this cycle keeps its key (same rule as cycles 404/408/410).
   - Verified: 4 local runs (contributions dedupe, disbursements with recipient+amount+date window, IE with candidate+supportOppose, candidates regression 2 rows unchanged) and **4 platform runs on build 0.1.14** — IE 25/25 unique, 100% `oppose` + correct candidate; disbursements 25/25 unique, 100% ≥ minAmount across 8 distinct vendors; contributions 45 rows/44 unique; default-input gate 20 candidate rows/21 keys unchanged.
   - Docs: README gets both output sections with real sample rows + 3 new FAQ entries (which mode answers "what did this campaign spend on Facebook ads", the year-3024 filer typo, why `committeeId` is contributions-mode-only); `registry.json` 33 → 58 output fields with a new IE example input/sample output; `meta.json`/`.actor/actor.json`/dataset_schema synced and the Store listing republished (`check-store-meta` 0/20). `/tools/fec-campaign-finance-scraper` renders the new copy live.
   - All 7 standing checks clean, `actor-health` 20/20, 3 services active, 4 site paths 200, no spend, 0 new Actors today. Inbox unchanged from cycle 427 (owner's already-answered icon thread, 1 cold pitch, 1 advance-fee scam, 3 dmarc) — no reply, no owner email. Revenue unchanged: 20 Actors, 40 users, 151 runs/30d, $0 booked. `date -u` 2026-09-17 23:30Z — dev.to slot 6 still not due (needs ≥2026-09-19 00:06Z).
   - **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 6 should be due on or just after 2026-09-19 00:06Z; (b) **h54 below is the highest-value follow-up** (fleet-wide sweep for the same ignored-pagination-param / duplicate-charging class); (c) then resume the competitor-gap rotation per cycle 427's audit (`substack-scraper` 400 — skip unless a new competitor appears, then `scholarship-scraper`/`google-play-reviews-scraper` 402, `steam-reviews-scraper` 404, `eu-ted-tenders-scraper` 406); (d) standing checks; (e) commit + push. Companies House stays CLOSED (cycle 419).

0-NEW-h54. **[cycle 428] DONE [cycle 429] (QUALITY audit, high value, scoped) — fleet-wide sweep for the "pagination param accepted but ignored → duplicate rows charged" class: CLEAN, no new bug found. `fec-campaign-finance-scraper` (cycle 428) stays the only known hit.**
   - Grepped every Actor's `src/main.js` for paging params (`page`, `offset`, `skip`, `start`, `cursor`, `pageToken`). Sorted into two buckets: (a) already cursor/token-based by construction (server hands back the next URL/token, code just follows it — structurally can't repeat a page since a stale/ignored cursor errors or empties rather than silently re-serving) — `clinicaltrials-scraper` (`pageToken`/`nextPageToken`), `court-records-scraper` (opaque `cursor=` on the response's own `next` URL), `uk-find-a-tender-scraper` (per-source `c.url` follows the API's own next link), `federal-register-scraper` (`applyNext()` follows `next_page_url`, either `search_after_cursor` or `page` depending on shape). None of these need testing — the mechanism rules the bug class out.
   - (b) genuine offset/page-number params sent by the Actor itself — tested each **directly against the live API with curl** (2 small requests, distinct `page`/`offset` values, diffed the returned identity field — no Actor code run, no Apify cost): `grants-gov-scraper` (`startRecordNum` 0 vs 5) — 10 distinct opportunity IDs, honored. `us-federal-awards-scraper` (`page` 1 vs 2) — 10 distinct award IDs, honored. `nih-reporter-scraper` (`offset` 0 vs 5) — 10 distinct `appl_id`, honored. `fda-recall-scraper` (`skip` 0 vs 5) — 10 distinct recall numbers, honored. `eu-ted-tenders-scraper` (`page` 1 vs 2) — 10 distinct publication numbers, honored. `hacker-news-scraper` (Algolia `page` 0 vs 1) — 10 distinct objectIDs, honored. `ats-jobs-scraper` (Workday `offset` 0 vs 5, live tenant `okgov.wd1.myworkdayjobs.com`) — 10 distinct job IDs, honored (the `total:0` on page ≥1 is the already-documented quirk, not this bug). `substack-scraper` (`offset` 0 vs 5, live `astralcodexten.substack.com`) — 10 distinct post IDs, honored.
   - **Two Actors turned out to already have this exact bug pre-caught by a dedup guard**, found while reading the code around their paging loops rather than needing a live test: `steam-reviews-scraper` has `if (seen.has(r.recommendationid)) continue; // Steam repeats the last page when the cursor runs out` sitting **before** `pushResult()` (the charge call) — confirmed the guard runs pre-charge, not post. `google-play-reviews-scraper` has the identical shape (`seenReviewIds`, comment "Google Play's paginated review endpoint can repeat a row across page boundaries", dedup before `Actor.charge`/`pushData`). Both were presumably fixed in earlier cycles; not a new finding, but confirms the fix is real and in the charge-gating position, not just cosmetic.
   - Remaining Actors (`app-store-reviews-scraper`, `apple-podcasts-scraper`, `google-news-scraper`, `scholarship-scraper`, `shopify-products-scraper`) have no page/offset paging at all (RSS page-range scan with documented holes, single-shot lookups, or full-page HTML crawls) — not in scope for this bug class.
   - **Conclusion: the FEC bug (cycle 428) was a one-off, not a fleet pattern.** No code shipped this cycle, no spend (all requests were free-tier public API calls, sub-cent). `notes/LEARNINGS.md` updated with the method + result so a future audit doesn't have to re-derive it from scratch.

0-NEW-h52. **[cycle 427] DONE (QUALITY/competitor-gap) — fresh competitor-gap re-scan on `app-store-reviews-scraper` (unchecked since cycle 400): clean negative, no code shipped.**
   - Cycle 426's candidate list (`substack-scraper`/`app-store-reviews-scraper`/`federal-register-scraper`/`fda-recall-scraper`) was stale, carried forward unedited across cycles — a full audit of `queue.md`+`queue_archive.md` found all 4 already had a competitor-gap pass (substack + app-store-reviews at cycle 400, federal-register at 412, fda-recall at 411). Built a fresh per-Actor "last competitor-gap cycle" table from the archive instead of trusting the inherited list; genuinely stalest are `substack-scraper`/`app-store-reviews-scraper` (both 400).
   - Picked `app-store-reviews-scraper` (its cycle-400 pass shipped real value; substack's only known gap is a confirmed cycle-400 source-side dead end not worth re-poking without new evidence). Live-diffed 4 highest-user competitors (`thewolves` 2115, `theagents` 790, `johnvc` 420, `easyapi` 530 — the last one new to this diff) via `actor-builds/<id>.inputSchema`. All subsets of our 14-input schema.
   - Checked `johnvc`'s 2 unmatched fields for a real gap: `parse_helpfulness` — Apple's raw RSS feed genuinely carries `im:voteSum`/`im:voteCount`, but our own code already emits both as `voteSum`/`voteCount` (shipped, not a gap). `normalize_dates` — our `updatedAt` already passes through Apple's native ISO-8601 timestamp, nothing missing. `start_page` (resume mid-scrape) is real but low-value/speculative (PPE reruns are cheap, no demand evidence) — logged, not built. `customMapFunction` (arbitrary injected JS) is deliberately out of scope.
   - **Conclusion: clean negative, no code shipped, 0 new Actors today.** All 6 standing checks clean, `actor-health` 20/20, 3 services active, 4 site paths 200. Inbox unchanged (no reply, no owner email). Revenue unchanged: 20 public Actors, 40 users, 151 runs/30d, $0 booked. `apify-admin runs` (bare) errors — needs an actor-slug arg; the 1 `ext_bad30d` failed run is still uninvestigated, still low priority.
   - **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z, very likely due soon; (b) continue the competitor-gap rotation using this cycle's fresh staleness ranking: `substack-scraper` (400, likely repeat clean-negative — verify via `apify-admin store` before skipping), then `scholarship-scraper`/`google-play-reviews-scraper` (402), `steam-reviews-scraper` (404), `eu-ted-tenders-scraper` (406), `google-news-scraper` (407), `ats-jobs-scraper` (408), `fec-campaign-finance-scraper` (410); (c) optional: find the right `apify-admin runs` syntax to ID the 1 failed external run, still low priority; (d) standing checks; (e) commit + push. Companies House stays CLOSED (cycle 419) — do not re-open without an owner-supplied API key.

0-NEW-h51. **[cycle 426] DONE (QUALITY/GROWTH) — full standing checklist clean + fresh varied-input platform verification on `uk-find-a-tender-scraper` (no regression found).**
   - Took the QUALITY/GROWTH option cycle 425 offered instead of another build. All 6 standing checks clean, `actor-health` 20/20, 3 services active, 5 site paths 200, no file near the 150KB trim threshold, dev.to slot 6 still not due, inbox reviewed with nothing needing a reply, `bin/traffic` nowhere near the Polar-checkout trigger threshold.
   - 2 live capped (`maxResults:5`) `run-sync-get-dataset-items` calls on `uk-find-a-tender-scraper` (last touched ~cycle 140, never had a post-ship varied-input platform check): `cpvCodes:["72000000"]` prefix filter → 5/5 rows genuinely carried a `72xxxxxx` code; `openOnly:true`+`minValueGbp:1000000` combo → 5/5 rows ≥1,000,000 GBP with a future deadline. Both correct, no bug found.
   - Small follow-up noted, not investigated: `bin/revenue`'s `ext_bad30d` shows 1 failed external run in the last 30 days — worth a quick `apify-admin runs` look next cycle to identify which Actor/input, low priority (150/151 runs succeeded).
   - No code shipped, no spend beyond the two near-zero-cost capped test runs, 0 new Actors today.
   - **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z; (b) continue the competitor-gap rotation on `substack-scraper`/`app-store-reviews-scraper`/`federal-register-scraper`/`fda-recall-scraper` (check `notes/LEARNINGS.md` for actual last-checked cycle before picking); (c) optionally check the 1 failed external run via `apify-admin runs`; (d) standing checks; (e) commit + push. Companies House stays CLOSED (cycle 419) — do not re-open without an owner-supplied API key.

0-NEW-h50. **[cycle 425] DONE (BUILD/competitor-gap) — `us-federal-awards-scraper` first-ever competitor-gap pass: shipped `expiringWithinDays`/`expiringAfterDays` (recompete finder), build 0.1.20 live.**
   - Diffed the 5 highest-user listings' live input schemas (all ≤2 users, this niche is as fragmented as grants-gov/nih-reporter) via `actor-builds/<latest-buildId>`. All were subsets of our 21-input schema except `groundtruth/usaspending-government-contracts`'s `expiring_within_days`/`expiring_after_days`, framed as a recompete/bid-lead-time tool.
   - **The gap was hiding behind a field we already emitted:** this Actor has fetched and output USAspending's period-of-performance "End Date" (`endDate`) on every contract/assistance row since ~cycle 130, but had no way to filter on it — the README even had a never-true "sort by end date" bullet (`sortBy` has no `endDate` option).
   - **Verified live this needs a client-side filter**: USAspending's `time_period.date_type` only accepts `action_date`/`date_signed`/`last_modified_date`/`new_awards_only` — `period_of_performance_current_end_date` is rejected with a 400. Filter runs client-side on the already-fetched `endDate`, before charging, protected by the existing `maxPagesPerCategory` page-cap (same "filter what we already have" shape as shopify's price filters).
   - Shipped `expiringWithinDays` (0 ≤ days-until-end ≤ N) and `expiringAfterDays` (requires the first, must be smaller, adds a lower bound — the "too-late-to-bid" cutoff). Correctly out of scope, warned not silently dropped: `awardLevel="subaward"` (FSRS rows carry no end date) and `loans` category (its field mapping never requests "End Date").
   - Verified 5 local runs (stock regression 12 rows unchanged; `expiringWithinDays:180` → 200/200 in `[0,180]`; `+expiringAfterDays:90` → 50/50 in `[90,180]`; `expiringAfterDays` alone warns-and-ignores; `expiringAfterDays≥expiringWithinDays` throws; `loans`+filter → 0 rows with new named diagnostic) + 2 platform runs on build 0.1.20 (12-row regression unchanged; live `[90,180]` window 40/40 rows, 0 violations, checked via the API dataset). No new output field → 0 drift on code/registry checks. README updated (input rows, FAQ, corrected the stale "sort by end date" bullet); `meta.json`/`.actor/actor.json` untouched, no publish-slot spent.
   - All 6 standing checks clean: `check-source-bytes` 347/0, `check-code-fields` 0 drift, `check-registry-fields` 0 drift, `check-store-meta` 0/20, `check-competitor-claims` 0/9, `check-meta-fields` 0/8. 3 services active, 4 site paths 200 (`/tools/us-federal-awards-scraper` confirmed 200). No spend, 0 new Actors today. Inbox reviewed (10 newest): owner's `13e16bd5` icon email still the same already-answered thread (cycle 364); 1 new cold i18n pitch (`aleksandrlugeza@mail.ru`, same class as cycle 354); 1 advance-fee scam (`21020@ascs.ac.th`); 3 dmarc reports — no reply, no owner email. Revenue: 20 public Actors, $0 booked.
   - **Note found while verifying `acts/<slug>/builds/latest`:** the bare-slug alias route 404s for this Actor ("Actor was not found") even though `apify-admin`/registry use the plain slug fine elsewhere — use the Actor ID or `owner~slug` form, or look up the build ID directly, when this shortcut is tried on this Actor again.
   - **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 6 due once ≥2026-09-19 00:06Z, still not due; (b) every fresh-diff Actor named in cycles 415-425 is now closed for a first pass — next competitor-gap candidates are `substack-scraper`/`app-store-reviews-scraper`/`federal-register-scraper`/`fda-recall-scraper` (check `notes/LEARNINGS.md` for actual last-checked cycle before picking); this is also QUALITY-cycle-due territory per the every-3rd-cycle rule (last clear QUALITY-labeled cycle was further back — consider a README/FAQ audit pass instead of another build); (c) standing checks; (d) commit + push. Companies House stays CLOSED (cycle 419) — do not re-open without an owner-supplied API key.

## Growth-cycle backlog (not time-gated, pick when 1-4 are all still locked)

- **NEW STANDING CHECK (cycle 312), run on every QUALITY cycle — structured data vs. rendered copy.** Cycle 312 found `site/templates/tool.html`'s JSON-LD `Offer.price` hardcoded to `0.0025` while the page body computed the real price — wrong by 2-3x on 5 of 19 tool pages, invisible to both existing drift checks (neither reads template `<head>`). ~10 seconds:
  `/root/agent/venv/bin/python -c "import json,re,httpx;ts=list(json.load(open('/root/agent/actors/registry.json')).values())[0];[print('MISMATCH',t['slug']) for t in ts if json.loads(re.search(r'ld\+json\">(.*?)</script>',httpx.get('https://fetchsmith.com/tools/'+t['slug'],timeout=30).text,re.S).group(1))['offers']['price']!=('%.4f'%((t.get('credits_per_result') or 1)*0.0025))]"`
  Generalise the rule when adding any new template metadata: **a number rendered in both `<head>` metadata and `<body>` copy must be one expression, never typed twice.**
- **NEW STANDING CHECK (cycle 275), run on every QUALITY cycle — check STATUS.md/queue.md size before doing anything else.** `wc -c /root/agent/state/STATUS.md /root/agent/tasks/queue.md` — if either exceeds ~150KB, re-trim it the same way cycle 275 did (keep last ~10 cycle entries + any still-live sections, move the rest verbatim into `STATUS_ARCHIVE.md`/`queue_archive.md`, prepended above their existing older content). Reason this exists: the cycle-156 "re-trim at ~60KB" rule sat unenforced for ~120 cycles because nothing ever re-checked file size — both files grew past 300KB before cycle 275 caught it, costing real tokens every cycle just to read. Don't wait for another accidental discovery.
- **NEW STANDING CHECK (cycle 188), run on every QUALITY cycle — poll dev.to comments.** Cycle 188 found a real, substantive technical question from a real developer that had sat unanswered for 2 days because no check ever looked (see item 5b-iii). Unauthenticated, no key needed, ~5 seconds:
  `/root/agent/venv/bin/python -c "import httpx;[print(a['id'],a['comments_count'],a['page_views_count'],a['title'][:50]) for a in httpx.get('https://dev.to/api/articles/me/published',headers={'api-key':__import__('os').environ['DEVTO_API_KEY']},timeout=30).json()]"` (needs the env sourced), then for any article with `comments_count > 0`: `GET https://dev.to/api/comments?a_id=<id>` and read `body_html` + `children`.
  **Views are not the signal** — article 2 had 1 view and the only comment; article 1 had 10 views and none. Note replies must go through item 5b-iii's `PUT`-the-article workaround, not the comments API (item 5b-ii).

- **DONE cycle 160 — government-data PILLAR PAGE `/blog/free-government-data-json-apis-no-key` published and the 8-guide topic cluster is now interlinked both ways.** Hub sorts all 8 gov APIs' failure modes into 3 classes (silent truncation / silent zero / silent everything) + a 4-assertion checklist; links out to 8 guides + 8 `/tools/<slug>` (17 links, all 200), and each of the 8 guides links back. In `/blog` + `sitemap.xml`, IndexNow 200 (new URL, `/blog`, `/tools`, and the 8 edited guides). Root README Guides list updated.
- **DONE cycle 162 — app/product-reviews PILLAR PAGE `/blog/app-store-review-apis-three-silent-empties` published**, synthesising `app-store-reviews`/`google-play-reviews`/`steam-reviews` around the shared "success response, ambiguous zero-reviews" failure shape. Links out to 3 guides + 3 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 6 + `/blog`/`/tools` verified 200, IndexNow 200, root README updated.
- **DONE cycle 165 — publisher/content PILLAR PAGE `/blog/public-content-apis-hidden-second-step` published**, synthesising `substack`/`apple-podcasts`/`google-news`/`hacker-news` around the shared "first response is a pointer/stub/syntax-trap, not the finished content" shape. Links out to 4 guides + 4 `/tools/<slug>` pages, each guide got a one-sentence backlink to the hub, all 5 + linked tool pages verified 200, appears in `/blog` listing + `sitemap.xml`, IndexNow 200, root README Guides list updated. **This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no pillar pages left in the backlog.** Next growth cycle needs a freshly-scoped idea (candidates: a 4th cluster if a new niche pair emerges, or shift growth-cycle time entirely to directory/syndication distribution since pillar pages haven't been given time to show indexation effect yet — don't re-measure `site:fetchsmith.com` before ~2026-09-15 per the standing note below).
- **NEW STANDING CHECK (cycle 160), cheap, no network — run on every QUALITY cycle.** Cycle 160 found **6 of 19 blog posts were missing the AI-disclosure footer entirely**; nothing enforced it (the same rot pattern as items 2b/2d/2e-i). All 19 now disclose. Re-check with:
  `cd site/content/blog && for f in *.md; do grep -q "Built and maintained by an autonomous AI worker" "$f" || echo "NO-FOOTER $f"; done`
  Two footer variants exist on purpose: the 13 posts written with live verification keep the stronger *"every JSON snippet above comes from a live request made while writing this post"* clause; the 6 retro-fitted ones use the neutral variant without it. **Do not "normalise" the neutral ones up to the strong claim** — it would be an unverifiable assertion.

- ~~**Edge-case test: no-match inputs on 3 Actors not recently edge-tested**~~ — **DONE cycle 157.** `clinicaltrials-scraper`, `us-federal-awards-scraper`, `google-play-reviews-scraper` all returned clean `201 []`, per-result charging confirmed safe.
- ~~**Edge-case test: no-match inputs, 2 more Actors**~~ — **DONE cycle 158.** `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge per-result (`Actor.charge({eventName:'result', count:1})`, `chargedCount===0` guard) so zero matches = zero charge. No bug found. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- ~~**Edge-case test: no-match inputs, last 3 Actors**~~ — **DONE cycle 159. This closes out the no-match sweep across all 17 Actors.** `federal-register-scraper` (`searchQuery`) and `scholarship-scraper` (`searchQuery`) both clean `201 []`. `apple-podcasts-scraper` initially looked broken — a `searchTerms` no-match returned 1 real result (Lex Fridman Podcast) — but this was a **test-methodology artifact, not a code bug**: its `podcasts` input field has a non-empty `default` in `.actor/input_schema.json` (the Lex Fridman URL, used for the example input), and Apify's `run-sync-get-dataset-items` substitutes that default whenever the field is *omitted* from the call, which isn't the same as sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`; also verified directly against `itunes.apple.com/search` that Apple genuinely returns 0 results for the test term. All three confirmed using the same safe per-result charging guard. **Lesson for future no-match tests, written up in `notes/LEARNINGS.md` cycle 159: check each Actor's primary list-input field for a non-empty `default`/`prefill` and send an explicit empty value for it — don't just omit fields you don't care about.**
- **All 15 public Actors + #16/#17 have been through one competitor-gap audit pass (closed cycle 146).** Next QUALITY cycle: either a second pass re-checking leaders for newly-added features, or — per the standing growth-reality-check below — weight toward distribution instead (answer support mail, directory research, on-page copy once Google indexes us).
- **NEW (cycle 171): the "scope a genuinely new Actor niche" build strategy (open item since cycle 167) is now much weaker than it looked.** Surveyed 6 fresh candidate niches via `apify-admin store` (FRED, US Census, World Bank, Crossref, OpenAlex, USGS earthquakes) — all free/cheap public APIs with plausible buyer demand — and found **every single one already has 6-8+ near-identical clone-account listings at 1-3 users each**, the same flooding pattern cycles 169/170 found occupying long-tail Store search queries. Full detail in `notes/LEARNINGS.md` cycle 171. **Conclusion: don't spend a cycle just scanning for "a niche nobody's touched" — that category appears exhausted for free-government/public-API data.** A new build is only worth it if it clears a real friction barrier others haven't (solvable key/auth gate) or ships a provably deeper field set/reliability than the existing clones. Do not re-run this exact 6-query survey; if trying again, need either a genuinely different data-source class (not another free public JSON API) or a specific competitor Actor with a closable, provable gap.
- ~~`uk-find-a-tender-scraper`: optional `includeRawOcds` opt-in field~~ — **found already fully shipped (cycle 278).** It was live in code, `.actor/input_schema.json`, `.actor/dataset_schema.json`, README, and `registry.json` already — this backlog line was stale, the feature must have shipped in an earlier cycle whose queue note never got back here to close it. Re-verified live end-to-end this cycle: a real run with `includeRawOcds:true` returns a `rawOcds` dict (`ocid`,`id`,`date`,`tag`,`description`,`initiationType`,`tender`,`parties`,...) on the row; a run without it correctly omits the key. No code change needed.
- ~~**`state/STATUS.md` is 274KB (cycle 148)**~~ — **stale as written; the cycle-149 trim had already moved cycles 71-138 into `state/STATUS_ARCHIVE.md` (note the UPPERCASE filename, not `STATUS_archive.md`). Re-trimmed cycle 156: 69.5KB/204 lines → 33.5KB/96 lines**, keeping cycles 155-147 and appending 146-and-older verbatim to `STATUS_ARCHIVE.md` (now 454KB). Standing rule: re-trim whenever STATUS.md passes ~60KB, keep ~8-10 cycles, append (never overwrite) to `STATUS_ARCHIVE.md`.
- ~~Add a pre-publish `meta.json` validator...~~ — **DONE cycle 149.** `bin/apify-admin publish` now validates title/seoTitle ≤70 and description/seoDescription ≤300 before sending, and skips resending `pricingInfos` when the price is unchanged (was 400ing on re-publish with "createdAt is required" — a real bug, not hypothetical). First real run of the validator caught `apple-podcasts-scraper`'s live title had drifted stale (meta.json was updated for the "Publishers" feature cycles ago but never actually re-published, blocked by the pricing bug); fixed and confirmed live. See `notes/LEARNINGS.md` cycle 149.
- `us-federal-awards-scraper`: optional `count=<field>.exact` aggregation output mode, and the `/api/v2/awards/<id>/` detail endpoint for sub-awards — both speculative, only build if a user actually asks.
- Google indexation re-checks: `site:fetchsmith.com` no earlier than ~2026-09-15 (baseline: 93 real Googlebot hits since 2026-09-10); `apify.com/fetchsmith/*` Google-indexation no earlier than ~2026-09-14. Don't re-measure before those dates.
- Free-directory submissions (Tiny Startups, Microlaunch, Dev Hunt, BetaList, SaaSHub, AlternativeTo, Peerlist) remain BLOCKED — all require interactive account creation/OAuth/CAPTCHA this box can't complete. Stays blocked until the owner creates accounts by hand (a scope decision), not worth re-checking without new information (e.g. a plain email-submission address discovered in a site's help docs).
- Substack cross-publication keyword search: the leader's global search endpoint returns empty JSON for us (checked cycle 110) — dead end, don't re-chase without new information.
- Scholarship-scraper `content` field (flight-stream chunk reference) needs a real per-chunk resolver — hard, only worth it if `description`/`essayTopic` prove insufficient to a real customer.

## Standing rules / dead ends (do not re-litigate without new information)

- **Growth reality check (cycle 16, still true):** many cycles of Actor feature polish have produced 0 external users/runs30d beyond our own test traffic. Distribution (guides, listing copy, syndication, indexing), not more features, is the binding constraint — except where a feature is what a listing can actually advertise.
- **Do not cut prices** (cycle 108/130): measured at-or-below every competitor in every audited niche already, with no start fee. `runs30d:0` is the Store-visibility discovery problem (item 4), not a price problem. **Narrowed cycle 179: this was true for the niches checked at the time, not universally.** A fresh competitor-gap audit on `app-store-reviews-scraper` found we were charging **5x** the two highest-volume leaders (`thewolves`/`theagents`, $0.0001/review vs our $0.0005) — cut to $0.0001 (still healthy margin on an HTTP-only Actor). **Standing rule now: re-check price specifically whenever running a competitor-gap audit on an Actor, don't assume "already checked" covers it** — see item 0-NEW-b below. **Narrowed again, harder, cycle 180: "our price" and "their price" are not single numbers.** Competitors price per *buyer plan tier* via `eventTieredPricingUsd`; reading only `eventPriceUsd` shows `null` for them and hides the comparison entirely. Every past "we are at or below every competitor" conclusion in this file was made by reading the flat field only, and was therefore **only ever true for FREE-tier buyers** — i.e. false for exactly the accounts with volume. Re-verify any niche audited before cycle 180 against the tiered key; see item 0-NEW-a(ix) for the method.
- **New-Actor title rule:** before setting any `title`/`seoTitle`, `GET /v2/store?search=<exact string>` — an exact match to a bigger competitor's listing hands them your headline. Differentiate with a capability provable from your own `input_schema.json`, never an adjective. Limit ~60 chars (longer titles fail `apify push` outright, cycle 121 finding).
- **`bin/store-test`** is the standing helper for empty-input/default Store-test verification (no args = sweep all, `<slug>` = one, `--concurrent` = burst test). Use it whenever an Actor is added or its input schema changes.
- **Cold-outreach spam** (backlink-for-trial offers, branding/logo/video pitches, etc. — recurring pattern, e.g. `lmybizexplore02@163.com`, `vancetorrescyiz@outlook.com`) — ignore, no reply, no spend, not owner-email-worthy. Only escalate if it starts looking like a real customer inquiry.
- **Rejected niches, do not re-audit without a new reason:** SEC EDGAR (leader ~90 users), Google/USPTO patents (leader ~80 users) — both too dense to differentiate cheaply.
- **Blocked, not retryable without new inputs:** Bluesky account (needs phone verification, we have none); Google Search Console (needs a Google account in secrets, none present).
- **Traffic/Polar trigger:** never act on raw pageview counts, only `bin/traffic`'s verified-funnel section (browser-class UA + loaded our CSS; instrumentation only valid from 2026-09-09 18:03 UTC on). Current: nowhere near the >100/day `/pricing`+`/tools` threshold — do not email the owner about Polar on a single day's spike.
- **PII rule reminder:** every gov/agency Actor has been checked for named-individual contact fields and either redacts them or ships clean. Apply the same check before any new gov-data Actor goes live. **Wording corrected cycle 148** — the old phrasing ("nih-reporter, verified no such field exists") was imprecise and reads as a compliance gap on first glance, because `nih-reporter-scraper` *does* ship `contactPiName`, `principalInvestigators[]` and `programOfficers[]`. What was actually verified is that the RePORTER schema has **no email and no phone field anywhere**, and that PI/program-officer *names* are the same class of already-public statutory disclosure as a federal contract awardee's name (reporter.nih.gov publishes them on every project page). That verdict is recorded in a comment at `actors/nih-reporter-scraper/src/main.js:241-245` and is a deliberate decision — **do not "fix" it as a bug without new information.**
  - **Observation logged cycle 152, no action taken, flagged for a considered decision (do not silently change either way):** `eu-ted-tenders-scraper` ships `buyerEmail`/`buyerPhone`, sourced from TED's **organisation**-level fields `organisation-email-buyer`/`organisation-tel-buyer` and added deliberately in cycle 70 as a competitor-gap close (`foxlabs/ted-tenders` ships the same 6 fields). In a live 6-row DEU sample most values are role mailboxes (`einkauf@…`, `zentrale-vergabestelle-vol@…`, `Service-Haus.Einkauf-Informationstechnik@…`), but **2 of 6 were an identifiable individual's work address** (`Emre.Guer@deutschebahn.com`, `t.gajewiak@gronau.de`). This is the same already-public-statutory-disclosure class as the nih-reporter verdict above — TED is the EU's official journal and that field is the buyer's *designated contact point for the procedure*, republished verbatim, not enriched or cross-referenced, and the product is a procurement-notice scraper, not a contact database. So it is very likely fine. But it is EU data and unlike nih-reporter the field genuinely is an email, so the "checked and ships clean" phrasing in the rule above is not strictly accurate for this Actor. **If a GDPR/PII question ever needs a real answer, this is the one to answer first**; the cheap mitigation if ever wanted is an opt-in `includeBuyerContact` flag (default off) rather than dropping the fields, which would preserve the cycle-70 competitive parity.

