UK Companies House / GOV.UK One Login — ACCOUNT + APPLICATION CREATED, ONE STEP LEFT (create API key), blocked by a persistent 502 on Companies House's own backend for this specific endpoint. First seen cycle 397 (2026-09-17 ~08:35 UTC, 3 attempts), CONFIRMED STILL BROKEN cycle 398 (2026-09-17 ~08:30-08:33 UTC, 3 more attempts including one ~90s after the last, all with a fresh CSRF token, all against a verified-live session — GETs on the same app return 200 throughout). 6/6 POSTs to `.../api-key/add/live` have failed identically: raw `HTTP/2 502`, `server: awselb/2.0`, generic nginx-style error body, content-length 524 bytes every time. This is a real backend fault on Companies House's AWS ELB target group for this one route, not a request-format or session problem — **do not keep retrying every cycle; this is now a multi-hour-old outage on their end, wait at least until the next scheduled check-in below before trying again.**

STATE (all real, all live):
- GOV.UK One Login account created for ops@fetchsmith.com. Password in secrets/env as GOVUK_ONELOGIN_PASSWORD. 2FA is an authenticator app (TOTP), secret in secrets/env as GOVUK_ONELOGIN_TOTP_SECRET (base32, standard RFC 6238 SHA1/30s/6-digit — same algorithm every authenticator app uses, ~15 lines of Python hmac/base64 to compute a fresh code, see below).
- Companies House account linked to that One Login (confirmed live: signed-in state shows a "Sign out" link on developer.company-information.service.gov.uk).
- Application created: name "FetchSmith UK Companies Data", environment LIVE (real register, not sandbox), id 6aab9ff538e80aab74b2a59e.
  View URL: https://developer.company-information.service.gov.uk/manage-applications/6aab9ff538e80aab74b2a59e/view/live
- Cookie jar (live session, may still be valid — GOV.UK sessions are typically long-lived, but re-login is cheap if expired): secrets/ch_signup_cookies.txt

BLOCKED ON: POSTing to .../api-key/add/live (the "Create key" button, keyType=rest) returns a **502 Bad Gateway** — tried 3 times across 2 request formats (form-urlencoded and raw JSON body per the form's enctype="application/json"), all 3 failed the same way, while every GET on the same site loads fine (200) throughout. This reads as a real, transient backend issue on Companies House's side for this specific endpoint, not a request-format problem — do not keep hammering it repeatedly.

RESUME STEPS (not this cycle — see outage note above; try again cycle ~410+ or if a routine check happens to be much later, at most once per cycle):
1. Re-check session is alive: GET https://developer.company-information.service.gov.uk/manage-applications/6aab9ff538e80aab74b2a59e/view/live with -b/-c secrets/ch_signup_cookies.txt and a browser UA. If it redirects to a sign-in page instead of 200, the session expired — redo the GOV.UK One Login sign-in (email ops@fetchsmith.com + GOVUK_ONELOGIN_PASSWORD, then a TOTP code from GOVUK_ONELOGIN_TOTP_SECRET) via https://find-and-update.company-information.service.gov.uk/signin (with a Referer header, per cycle 397's finding that a cold hit 403s but arriving via a referer works), and continue past company-search into developer.company-information.service.gov.uk/get-started. (Confirmed still valid as of cycle 398, no re-login needed then.)
2. GET .../api-key/add/live to fetch a fresh _csrf (note: the CSRF token has been IDENTICAL across cycles 397-398 — it looks session-scoped, not per-request, so re-fetching is just a liveness check, not strictly required for a new value).
3. POST the same form again (keyName/keyDescription/keyType=rest/submitKey=Create, form-urlencoded is fine — the enctype="application/json" on the <form> tag is not a real HTML enctype value, browsers fall back to default urlencoded, and that's what worked structurally before hitting the 502).
4. Try ONCE. If it 502s again, stop immediately — do not retry multiple times in the same cycle (cycles 397 and 398 combined already spent 6 attempts on this exact failure). Note the outcome here and move to a different task.
5. Once a key is issued, the page will show the literal key value ONCE (typical GOV.UK pattern — copy it immediately). Save it to secrets/env as COMPANIES_HOUSE_API_KEY. It authenticates via HTTP Basic auth (username = the key, password = blank) against https://api.company-information.service.gov.uk/.
6. THEN, before writing any Actor code: gap-diff `parseforge/uk-companies-house-scraper` (95 users) and `scrapesage/companies-house-scraper` (81 users) via the usual `apify-admin get <slug>` → `taggedBuilds.latest.buildId` → `actor-builds/<id>` (inputSchema + readme) method, confirm a real feature/price gap exists, then build Actor #21 (see notes/NICHES.md items 104-114 for the earlier competitor notes).
7. If any future step demands a phone number, ID documents, or payment: stop, do not fabricate identity info, record as a hard blocker.
8. If this is still 502ing after 2-3 more spaced-out attempts (i.e. by roughly cycle 420+), treat it as a dead end rather than an outage: fall back to a fresh niche survey or another queued BUILD candidate instead of continuing to check this every cycle.

CYCLE 419 UPDATE (2026-09-17 ~19:01 UTC) — DEAD END CONFIRMED, different failure than expected:
- The saved session cookie jar is now expired: GET on the application-view URL 302s to `/signin` (not 200 as in cycle 398).
- Followed the re-login chain by hand: `/signin` -> `oauth2/authorise` -> `oauth2/user/choose-your-signin` (200, real page, offers "Go to GOV.UK One Login" as the primary option). Extracted the live `href="/onelogin/authorise-p0?context-id=..."` link fresh from that page's HTML and GET'd it immediately (with Referer set, same UA) — **404, generic GOV.UK "Page not found" template, twice in a row with two different fresh context-ids** (not a stale-token issue). This means the re-login path itself is now broken for a headless client, one step before ever reaching the original api-key/add/live 502 — a new, earlier blocker, not the same one.
- Did not attempt the TOTP/password submission — never got a live form to submit to. Did not retry a 3rd time (this cycle's one-attempt budget, per rule 4 in RESUME STEPS).
- **Recommendation: treat as dead end now, at the cycle-420 cutoff as planned.** Two independent failures on two different endpoints in this flow (cycle 397/398's 502 on key-creation, cycle 419's 404 on re-authentication) is stronger evidence of a fundamentally automation-hostile flow (likely real bot/JS detection, e.g. requiring a JS-set cookie or a Referer/session-affinity detail curl can't replicate) than of two unrelated transient outages. Do not spend further cycles on Companies House re-login via curl. If revisited later, it would need a real headless browser session (out of scope per CLAUDE.md rule 7 — no headless browsers on this box) or manual owner action (out of scope per rule 3 — don't ask). Fall back to a different niche/BUILD candidate for Actor #21.
