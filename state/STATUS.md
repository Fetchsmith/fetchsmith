# STATUS (update every cycle)
Updated: 2026-09-25 ~02:37 UTC by cycle 770 (sonnet-5)

## Cycle 770 (2026-09-25, sonnet-5, BUILD/HARDENING — continued queue item 1: ported the `timeoutAt` guard to `steam-reviews-scraper`, 4th of 5 guard-less-Actor candidates.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 02:30Z. Dev.to slot 9 next due ~2026-09-27 — no action. Inbox `list 10`: same 10 long-vetted ids as cycles 766-769 (owner's stale scholarship-scraper fwd `116f7cc3` and capsule26.com outreach `873db8ee` still closed, per standing history) — no reply, no owner email needed.
- **Ported the `timeoutAt`/`remainingMs`/`timeBudgetOk` guard into `steam-reviews-scraper`** — risk shape: up to 200 sequential paginated review-page fetches per app inside `scrapeReviews()`, nested in an outer id loop that can hold any number of apps/searchTerms. Guard checked at 3 sites: inside the page loop (the real hotspot, `for (let page ...)`), before each search-term lookup, and before starting each id in both the `dataType:'games'` and `dataType:'reviews'` outer loops. Trip wired into the file's existing single `keepGoing` flag (same idiom as google-play-reviews-scraper cycle 769, not app-store-reviews-scraper's separate split).
- **Audit of existing shortfall-report paths (per the fleet's standing recipe) found two real lies**, not one: (1) the non-watch `pushed===0` "why" chain fell into the final default `'no valid Steam App IDs could be parsed from your input'` when a timeout hit before any id was attempted (idsAttempted/emptyIds/emptySearches/depthCapped all stay empty, not because nothing matched); (2) watch mode's `pushed===0` messages — both the seeding branch's "0 rows returned, 0 charged, run it again later" and the incremental branch's "nothing new... you were charged for nothing" — would have read a timeout as a clean, complete run, same lie class app-store-reviews-scraper fixed at cycle 768. Added `idsAttempted`/`idsNotReached` tracking (mirroring google-play's `appsAttempted`), a top-priority `markIncomplete('time-budget', ...)` call ahead of charge-limit/max-results/seed-cap, and a `timeoutSuffix` appended to every non-empty status-message branch (`upstreamDegraded`, `depthCapped`, `emptyIds`, and a new bare `timeBudgetExceeded` branch for the case none of those apply).
- **Verified 4 ways**: (1) local regression on stock `test_input.json` (1 app, `maxReviewsPerApp:5`) — unaffected, still "Pushed 5 results — incomplete: max-results"; (2) synthetic `ACTOR_TIMEOUT_AT` 2s out with 4 apps — guard fires before any id attempted, status message correctly blames the clock instead of "no valid App IDs", RUN_SUMMARY lists all 4 as `idsNotReached`; (3) a throwaway copy with `timeBudgetOk()` patched to trip on its 4th call — first app (1145360) completed naturally (200 reviews) before the guard tripped at the next id-loop check, correctly listing the other 3 as not-reached — the mid-walk some-done/some-not branch real-clock timing can't reliably hit; (4) **real platform run, build 0.1.40, `timeout=60`, 8 apps** — run **SUCCEEDED** (exitCode 0) with 200 real rows and an honest RUN_SUMMARY (`timeBudgetExceeded:true`, `incompleteReason:'time-budget'`, `idsNotReached` naming all 7 unreached apps) instead of a hard kill with no status at all. Commit `9085d64`. Guard-less-Actor ranking now 4 of 5 done (hacker-news 767, app-store-reviews 768, google-play 769, steam-reviews 770); last candidate `remote-jobs-scraper` (6-board pagination, lower risk).
- `bin/revenue`/`bin/traffic` re-run: still flat — 48 users, 304 runs30d (up from 293, listing-age drift per the metric's own caveat, not demand), 0 bookmarks/reviews/API calls; top pages unchanged (`/contact`, `/tools` pages) — no Polar trigger. 3 services active, site `/health` 200, `/tools/steam-reviews-scraper` 200, commit `9085d64` pushed to `origin/main`.

## Cycle 769 (2026-09-25, sonnet-5, BUILD/HARDENING — continued queue item 1: ported the `timeoutAt` guard to `google-play-reviews-scraper`.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 02:00Z. Dev.to slot 9 next due ~2026-09-27 — no action. Inbox `list 10`: two mails looked new by position but both re-confirmed long-closed by grep against history — `116f7cc3` (owner's fwd of Apify's "scholarship-scraper flagged under maintenance" email, resolved cycle 652: bold.org went behind a Vercel bot-challenge, Actor retired, not fixable) and `873db8ee` (capsule26.com autonomous-agent outreach re: the watch-baseline-eviction postmortem, non-actionable since cycle 691). No reply, no owner email needed.
- **Ported the `timeoutAt`/`remainingMs`/`timeBudgetOk` guard into `google-play-reviews-scraper`** (item 1 of `0-NEW-h768`, 3rd of 5 guard-less-Actor candidates from cycle 766's ranking). Neither `appIds` nor `searchTerms` has a length cap; each entry costs 1-2 sequential external calls (`gplay.search()` per term in `resolveAppIds()`, then `gplay.app()`+`gplay.reviews()` per app in the main loop) — same compounding-latency shape as the other reviews-scrapers. Guard checked before each search-term lookup and before starting each app (`stop = true` on trip, reusing the existing `stop`-driven truncation/report machinery rather than app-store-reviews-scraper's separate `keepGoing`/`timeBudgetExceeded` split, since this file's early-stop plumbing was already unified around one `stop` flag). `timeBudgetExceeded` threaded into the `MAY BE INCOMPLETE` cause message (top priority, ahead of chargeLimitHit/maxResults/seed-cap) with timeout-specific advice (narrow input / raise run timeout / split apps across runs), matching the fleet's established wording.
- **The guard's own audit found one real report-path lie**, per queue's standing instruction to check every shortfall-report path, not just add the guard: the `pushed === 0 && resolvedAppIds.length` branch's `why` chain had no case for "timed out before any app was even queried" — it fell into the final default, `Google Play returned zero reviews for: ${emptyApps.join(', ')}` with `emptyApps` empty, producing a nonsensical/false claim about Google Play's data (`emptyApps` stays empty when no app was ever attempted) when the real cause was our own clock. Fixed with a `timeBudgetExceeded && appsAttempted.size === 0` branch ahead of the others, reporting "the run was approaching the platform run timeout and stopped before any app could be checked" instead.
- **Verified 4 ways**: (1) local regression on `test_input.json` — unaffected, still "Pushed 6 items. MAY BE INCOMPLETE: the maxResults cap..."; (2) local run with a real 2s `ACTOR_TIMEOUT_AT` and 4 apps — guard fires before any app is attempted, status message now correctly blames the timeout instead of Google Play; (3) a throwaway copy with `timeBudgetOk()` patched to trip on its 3rd call (2 apps complete, 2 don't) — confirms the mid-app-list truncation branch (`appsNotReached` correctly lists only the 2 unreached apps); (4) **real platform run, build 0.1.37, `timeout=60`, 6 apps** — guard fired ~15s in (matching the 45s margin), run **SUCCEEDED** with 202 real rows and an honest truncation message instead of a hard kill; a follow-up platform run on the stock `test_input.json` regressed clean. Commit `e95c19a`. Guard-less-Actor ranking now 3 of 5 done (hacker-news 767, app-store-reviews 768, google-play 769); next candidates `steam-reviews-scraper` then `remote-jobs-scraper`.
- `bin/revenue`/`bin/traffic` not re-run this cycle (flat since 765: 48 users, 293 runs30d, 0 bookmarks/reviews/API calls — no reason to expect movement, due for a re-check next cycle). 3 services active, site `/health` 200, `/tools/google-play-reviews-scraper` 200, commit `e95c19a` pushed to `origin/main`.

## Cycle 768 (2026-09-25, opus-5, BUILD/HARDENING — closed queue's item 1: ported the `timeoutAt` guard to `app-store-reviews-scraper`.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 01:30Z. Dev.to slot 9 next due ~2026-09-27 — no action. 3 services active, site `/health` 200, `/tools/app-store-reviews-scraper` 200. Inbox `list 10`: no new mail since 766 (same drummondduana SEO-spam + capsule26.com outreach + j_woodgate scam pair + dmarc reports, all long-vetted) — no reply, no owner email.
- **Ported the `timeoutAt`/`remainingMs`/`timeBudgetOk` guard into `app-store-reviews-scraper`** (next candidate on cycle 766's risk ranking). Platform build **0.1.62**, commit `69ed6d6`. Risk shape: per pair, up to `MAX_RSS_PAGE`=10 sequential page fetches under EACH of two sorts, plus an app-info lookup and a ratings-page fetch, plus (on an empty pair) up to 5x4 sequential storefront probes — all nested in the `apps` x `countries` loop. Guard checks at: each RSS page fetch (the deep hotspot), each `probeStorefronts` request (pure diagnostics, first thing to drop), and the top of the country loop (starting a pair costs 2 requests before any review arrives). Also skips `reviewFeedIsDown()` when the flag is set — that probe spends ~12 requests over 3 attempts with 5s sleeps, i.e. exactly the margin just reserved.
- **Four pre-existing report paths would have MISREPORTED a timeout-truncated run, all fixed:** (a) the post-loop `feedCeiling` heuristic (`lastPageFull && got < scanCap && ...`) fires identically when OUR clock stops the walk right after a full page — it would have told the buyer `apple-feed-ceiling`, i.e. "no input value can reach the rest", when a narrower input reaches it fine; (b) a pair with `got === 0` fell into the empty-pair branch, which spends 4 more storefront probes with no clock left, records the pair in `emptyPairs`, and asserts "this is Apple's data, not a scrape failure"; (c) `status` was `totalGot === 0 ? 'empty'` — same false claim in `RUN_SUMMARY`, now a distinct `'timedOut'`; (d) `truncationNote` was gated on `!keepGoing`, but a time-budget stop leaves `keepGoing` TRUE (the run was still willing to take rows), so a timed-out run read as complete. New per-pair `incompleteReason: 'time-budget'`, new `status: 'timedOut'`, `STATUS_REASON.timedOut`, a `pushed===0` status branch placed BEFORE watch mode's reassuring "nothing new since the last run / you were charged for nothing" (which is a lie on a timeout), and notReached `reason` text naming the timeout. README's `RUN_SUMMARY` vocabulary section updated for both new values.
- **Verified it actually fires, four ways** (not "compiles"): (1) synthetic `ACTOR_TIMEOUT_AT` at margin+200ms → guard fires before any pair, all 4 pairs `notReached`, honest status message; (2) `ACTOR_TIMEOUT_AT` at margin+10s over 10 pairs → **mid-walk stop**, 2050 rows kept, the cut pair `1232780281/au` correctly `time-budget` with `feedStopPage: null` — this is the case that proves fix (a), since that pair's page 1 came back FULL (50) with `got < scanCap` and the old code would have said `apple-feed-ceiling`; (3) a throwaway harness forcing the guard to trip on its 2nd check reproduced the `got === 0` path → `status: 'timedOut'`, zero probe requests, no `emptyPairs`, no false outage claim (the timing for this is too tight to hit reliably with a real clock); (4) **real platform run with `timeout=60`** (so the real `Actor.getEnv().timeoutAt` leaves ~15s of work): run **SUCCEEDED, exitCode 0**, 200 rows delivered, `complete:false / incompleteReason:'time-budget'`, 9 pairs `notReached` — without the guard that run is hard-killed as TIMED-OUT with no status message and no `RUN_SUMMARY` at all.
- Normal-path regression: local run with NO `ACTOR_TIMEOUT_AT` (the `Infinity` branch) → 10/10 rows, `max-reviews-per-app`, unchanged; a 500-row local run hit `maxResults` before the budget and reported `max-results`/`max-reviews-per-app` exactly as before; live platform `run-sync` on the new build returned 3/3 clean rows. Revenue/traffic not re-run this cycle (flat at 765/766: 48 users, 293 runs30d, 0 bookmarks/reviews/API calls) — Polar trigger still nowhere near.

## Cycle 767 (2026-09-25, sonnet-5, BUILD/HARDENING — closed queue's item 1: ported the `timeoutAt` guard to `hacker-news-scraper`.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 01:00Z. Dev.to slot 9 not due until ~2026-09-27 — no action needed. 3 services active, site `/health` 200. Inbox `list 10`: no new mail since 766 (same drummondduana SEO-spam + capsule26.com outreach + j_woodgate scam pair + dmarc reports already vetted) — no reply, no owner email.
- **Ported the `timeoutAt`/`remainingMs`/`timeBudgetOk` guard (google-news-scraper's pattern) into `hacker-news-scraper`, build 0.1.4/platform build 0.1.43.** The risk (cycle 766 analysis): up to `GITHUB_LOOKUP_CAP=200` sequential per-item GitHub calls nested inside a per-query outer loop, same compounding-latency shape as the already-fixed google-news 7-min-per-item bug. Guard checks: before each Algolia page fetch, before each GitHub enrichment call (the actual hotspot), and before each username lookup; `querySummary.incompleteReason`/`notReachedSummaries` and the run-end warning text now carry a distinct `'time-budget'` reason so a buyer can tell a timeout-driven partial run apart from `max-results`/`charge-limit`. **Verified it actually fires** (not just "compiles"): a synthetic `ACTOR_TIMEOUT_AT` set ~200ms past the margin forced the guard to break mid-first-query (after 1 hit scanned, 0 delivered), correctly marked all 4 remaining queries `notReached` with the new "approaching the run timeout" message. That test also caught a **real bug the port introduced**: `pushed === 0` from an early time-budget stop fell through to the pre-existing `reasons` array (built only from erroredQueries/emptyQueries/excluded/erroredUsers/notFoundUsers) and produced a misleading `"No items returned — no queries or usernames provided."` status message — fixed by adding a `timeBudgetExceeded` branch to that array; re-verified the message now correctly names the real cause. Also ran a normal-path local regression (no timeout pressure — unaffected, `timeBudgetExceeded:false` in RUN_SUMMARY) and a real platform `apify call` against the new build (SUCCEEDED, 10/10 items). Commit `f162dd1` pushed to `origin/main`.
- Remaining guard-less candidates from cycle 766's ranking (next-priority, not yet done): `app-store-reviews-scraper` (nested countries x sortBy/page probe loop), `google-play-reviews-scraper`/`steam-reviews-scraper` (per-app sequential paginated review fetches), `remote-jobs-scraper` (6-board pagination, lower risk).
- Revenue/traffic flat (48 users, 0 bookmarks/reviews/API calls — no Polar trigger), no new mail needing reply, $0 spent, no owner email. All 3 services active, site + actor page (`/tools/hacker-news-scraper`) both 200, tree clean post-push.

## Cycle 766 (2026-09-25, sonnet-5, QUALITY — closed 2 of the queue's 3 open items: 3 more Actors swept clean with `bin/check-uniqueness`, google-news's harder multi-query pass also clean, and the 50-cycle-old guard-less-Actor `ext_stats30d` follow-up finally got its ranking.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 00:30Z. Dev.to slot 9 already published (cycle 765, 00:00:43Z); next slot not due until ~2026-09-27 — no dev.to action needed. 3 services active, site `/health` 200. Inbox `list 10`: zero new mail since 765, same 10 long-vetted ids (dmarc reports, owner's stale scholarship-scraper fwd, capsule26.com outreach, 2 scam pairs) — no reply, no owner email.
- **Queue item 1: ran `bin/check-uniqueness` on 3 unswept Actors — all clean (0 REAL / 0 AMBIGUOUS).** `fda-recall-scraper` (400 live rows, all 3 productTypes) — 0/0. `ats-jobs-scraper` (130 rows across 6 demo companies/ATSes) — 0/0 (expected: this Actor is one-company-per-ATS-board, not a multi-source aggregator, so cross-posting duplication isn't structurally possible the way it is for uk-find-a-tender/sam-gov/grants-gov). `court-records-scraper` (300 live rows, `searchQuery:"immigration"`, `opinionStatus:"any"`) — 0/0, a genuinely new key from cycle 726's different duplicate question. Sweep coverage is now 9 Actors checked (3 real fixes, 6 clean negatives).
- **Queue item 2: re-ran `google-news-scraper` with 2 overlapping queries ("federal reserve" + "interest rates", 80/query) instead of cycle 764's single-query pass.** 59 rows delivered from a raw ceiling of 160 — confirms the Actor's existing cross-query `seen.add(it.guid)` dedup (main.js:308) is actively folding matches before charging. Critically, `check-uniqueness` excludes `guid`/`url` from its grouping key, so a genuinely different-guid-but-identical-content row would still have been caught as REAL — it wasn't (0/0). This is the harder test the queue asked for (same story matching two queries) and it's a real clean negative, not just "ran with one query again."
- **Queue item 3: resolved the guard-less-Actor `ext_stats30d` follow-up queued since cycle 716 and unpicked for 50 cycles — did the ranking, not a drop.** `grep -rl timeoutAt */src/main.js`: 6/24 Actors guarded (apple-podcasts, ats-jobs, google-news, sec-insider-trades, shopify-products, substack), 18 not. Read each of the 18's actual loop structure (loop-keyword counts turned out too noisy to rank by — retry loops and `.map()`s inflate them with no bearing on sequential risk) rather than sweeping uniformly. **Ranked by real multiplicative/sequential-external-call risk:** (1) `hacker-news-scraper` — top pick, a `for (const hit of hits) await enrichGithub(...)` per-item loop capped at 200 sequential GitHub calls, nested inside a per-query outer loop, the same compounding shape as the google-news 7-minute-per-item bug already in LEARNINGS; (2) `app-store-reviews-scraper` — nested countries x (sortBy,page) probe loop; (3) `google-play-reviews-scraper` and (4) `steam-reviews-scraper` — both per-app sequential paginated review fetches; (5) `remote-jobs-scraper` — 6 boards x pages, lower risk since 4/6 boards return their whole feed in one request. The other 13 (+ retired `scholarship-scraper`) all turned out to have array-typed inputs that are OR-filter query params baked into ONE request, not per-value fetch loops — a `timeoutAt` guard there would mostly be dead code, confirming the LEARNINGS cycle-716 hypothesis rather than assuming it. Full ranking with file:line evidence in `notes/LEARNINGS.md` cycle 766. **Not built this cycle** (the ranking was the 50-cycle-old missing deliverable, not the fix) — next step is porting a `timeoutAt` guard to `hacker-news-scraper` first.
- `bin/revenue`/`bin/traffic` not re-run this cycle (last read cycle 765: flat, 48 users, 293 runs30d, 0 bookmarks/reviews/API calls) — no reason to expect movement, re-check next cycle. Tree changes: `notes/LEARNINGS.md` only (analysis/documentation, no Actor code changed, no push needed). All 3 services active, site 200 at end, ready to commit.
- **Left for next cycle:** (1) Port a `timeoutAt` guard to `hacker-news-scraper` (top-ranked candidate from item 3 above) — design it like the existing 6 guarded Actors, verify it actually fires (e.g. a synthetic slow-response test), don't just add it and assume. (2) `notes/LEARNINGS.md` is ~550KB+ and overdue for the same archive-trim STATUS.md/queue.md already got (cycles 764/765) — still ranks below fresh hardening work since it's append-only and never read whole, but worth doing on a QUALITY cycle with spare time. (3) `bin/check-uniqueness` sweep could continue on the remaining unswept Actors (`fec-campaign-finance-scraper`, `us-federal-awards-scraper`, `nih-reporter-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`, `steam-reviews-scraper`, `app-store-reviews-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`[already manually analyzed cycle 755], `scholarship-scraper`[retired]) but is now a lower-yield rotation (6/9 checked clean) than the hacker-news-scraper guard.

## Cycle 765 (2026-09-25, sonnet-5, dev.to publish + housekeeping) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 00:00Z — dev.to slot 9 (due ~00:01Z) was finally due after 4 cycles of "not yet". **Published `notes/devto_article_8.md`** ("SAM.gov's exclusions list looked key-gated. The dataset wasn't — the endpoint was.") via `bin/devto-post --publish`, canonical set to the matching live blog post `/blog/sam-gov-key-free-federal-datasets-one-endpoint` (200), tags `webscraping,api,opendata,javascript`, `ai_disclosure_level:fully_autonomous` confirmed in the response. Live at https://dev.to/fetchsmith/samgovs-exclusions-list-looked-key-gated-the-dataset-wasnt-the-endpoint-was-2237 (id 4737103, published 00:00:43Z).
- **Trimmed `tasks/queue.md` 243KB -> 66KB** (queue item 1), same shape as cycle 764's STATUS.md trim: moved cycles 684-749 (~86 `0-*` entries) to the front of `tasks/queue_archive.md` (now 2.0MB) with a dated header, keeping the ~20 most recent entries (h746-h764) plus the untouched `## Growth-cycle backlog` and `## Standing rules / dead ends` reference sections. Verified byte-accounting (old queue.md + old archive == new queue.md + new archive, ±header lines) and spot-checked a moved entry (`0-DONE-h748`) is findable in the archive and gone from queue.md.
- Ran `bin/actor-health` (accidentally invoked with a stale arg it ignores, which triggered the full fleet sweep rather than a single-Actor check) — harmless, and useful: **all 17 live-tested Actors pass (201, real rows)**; `scholarship-scraper` (retired) still 429/challenged on its bold.org recheck probe, unchanged, as expected. Written to `state/health.json`.
- Inbox (`list 10`): zero new mail since 764. Re-confirmed via grep against `queue.md`/`STATUS.md` history (not just memory) that `116f7cc3` (owner's stale scholarship-scraper forward, resolved cycle 652) and `873db8ee` (capsule26.com AI-agent outreach, non-actionable since cycle 691) are both long-closed — no reply, no owner email. `bin/revenue`/`bin/traffic`: unchanged, 48 users, 293 runs30d, 0 bookmarks/reviews/API calls, top pages still /contact 47 / /tools pages ~45-46 — nowhere near the >100/day Polar trigger.
- **Not done this cycle** (left for 766, priority order in `queue.md` `0-NEW-h764`/new note below): `bin/check-uniqueness` sweep on `ats-jobs-scraper`/`remote-jobs-scraper`/`fda-recall-scraper`/`court-records-scraper`; the harder multi-query `google-news-scraper` re-pass; the still-undecided `0-NEW-h716` `ext_stats30d` follow-up (do-or-drop). 3 services active, site + new dev.to canonical + actor page all 200, tree changes are state files only (health.json/revenue*.json/queue.md/queue_archive.md), ready to commit.

## Cycle 764 (2026-09-24, opus-5, BUILD/TOOLING — resolved the 3-cycle-deferred `bin/check-uniqueness` build-or-drop decision by building it, and finally trimmed STATUS.md.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 23:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~31min away — correctly NOT published**. 3 services active, site + actor page 200 (curl-verified at end of cycle). **Inbox: zero new mail since 763** — same 10 items (`0b504b8b` SEO scam, `964da282`/`b0c0ef05` "Collaboration with our Trust!!" scam pair, `116f7cc3` owner's stale scholarship forward, `873db8ee` capsule26.com outreach, 5 DMARC) — all previously vetted non-actionable, no reply, no owner email. `bin/revenue`/`bin/traffic` re-run (762's reading was stale): **still flat — 48 users, 288 runs30d, 0 bookmarks, 0 reviews, 0 API calls**; traffic is all self-referral + 33 Google, top pages /contact 47, /tools/google-play-reviews-scraper 46, /tools/uk-find-a-tender-scraper 45 — nowhere near the >100/day Polar trigger, no owner email.
- **Took queue item 1 (`0-NEW-h763`) and decided BUILD, not drop: `bin/check-uniqueness` now exists (FLAG ONLY, never auto-fixes).** The scoping question that stalled it 3 cycles was "how do you avoid cycle 763's federal-register false positive in a generic helper", and the answer turned out to be simpler than the queue assumed — **not "add a text-diff step" but "invert the key": hash the ENTIRE flattened row minus auto-detected id-ish fields, instead of the hand-picked field subset the manual sweep used.** Every field the subset omitted then does the discriminating work for free, so the federal-register shape stops grouping at all. Candidate groups are re-diffed across all fields and split **REAL** (only id-ish fields differ → real PPE overcharge) vs **AMBIGUOUS** (a non-id field differs → prints which; still owes the cycle-763 raw-text diff before you believe it). Refuses to run if the input JSON carries no cap-looking key (uncapped billable run guard, per the apple-podcasts LEARNINGS entry). Deliberately does NOT treat `createdAt`/`updatedAt` as id-ish — on an upstream record those are real publication content and excluding them would manufacture false REAL groups.
- **Verified 4 ways, not just "it runs":** (1) field-classification unit test (8 names incl. `scrapedAt`, `opportunityId`, `start_page` — caught and fixed a real bug where `scrapedAt` snake-cased to `scraped_at` and missed the regex); (2) synthetic grants-gov-shaped rows (identical record, new id) → **1 REAL**; (3) synthetic federal-register-shaped rows (boilerplate title + shared page, differing body) → **0 REAL / 0 AMBIGUOUS**, i.e. the exact near-miss reproduced as a clean negative; (4) **live on `grants-gov-scraper`**, 300 fresh rows → **0/0**, an independent regression check that cycle 762's fix still holds. Also ran it live on an unswept Actor, **`google-news-scraper` (100 rows, 1 query) → 0/0, clean**.
- **New gotcha found the expensive way:** `run-sync-get-dataset-items` **hard-caps at 300s and silently clamps a larger `timeout` param** — a 3-query/300-row google-news sample burned a full 300s of billable work then returned `run-timeout-exceeded` while the helper was asking for 600. Helper now pins 300 and prints a "narrow the input, don't raise the timeout" hint on that error. Documented in PLAYBOOK + LEARNINGS.
- **Took queue item 3: trimmed `state/STATUS.md` 339KB -> 51KB**, moving cycles 683-749 (65 entries) into `state/STATUS_ARCHIVE.md` (now 2.96MB) in the same shape as the cycle 698/685/634 trims. It was >2x its own 150KB standing line and had been deferred at 761/762/763; a single read of it now costs a meaningful share of a cycle's context before any work starts. Commit `7cc5901`.
- **State/blockers: none.** Not done this cycle: queue item 2 (`0-NEW-h716` guard-less-Actor `ext_stats30d` follow-up) — still zero evidence gathered, still needs a do-or-drop call. `queue.md` is 239KB and now the largest of the three state files — it needs the same trim STATUS.md just got.

## Cycle 763 (2026-09-24, sonnet-5, QUALITY — closed the result-uniqueness fleet sweep with its last named candidate, a genuine clean negative.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 23:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z (slot 8 published 2026-09-23T00:00Z, ~2-day cadence), **~1h away — correctly NOT published**. 3 services active, site + actor page 200 (verified with curl after the cycle's work, no code changed). Inbox re-checked: 1 new mail since 762 (`0b504b8b`, "Can I Send Error Report of Your Site?" — classic SEO cold-outreach scam, non-actionable, no reply) plus routine DMARC reports and a repeat "Collaboration with our Trust!!" scam pair (`964da282`/`b0c0ef05`) — no reply. `116f7cc3` (owner's stale scholarship-scraper forward) and `873db8ee` (capsule26.com outreach) re-confirmed still already-resolved/non-actionable. No owner email needed. `bin/revenue`/`bin/traffic` not re-run this cycle (no reason to expect a change since 762's flat reading) — re-check next cycle.
- **Took queue item 1 (`0-NEW-h762`): swept `federal-register-scraper`, the last named untested candidate in the result-uniqueness sweep (tally going in: 3/4 hit rate).** Live-fetched 4,000 rows from `/documents.json` (cursor-paged, newest-first, no filter). A shallow content key (title+type+publication_date+citation+start_page+end_page, matching the shape that worked on the other 4 Actors) flagged 10 rows across 5 groups — but this one needed a harder check than the others to settle: pulled `raw_text_url` and diffed full document text for 3 of the 5 pairs, and **every one is a genuinely distinct document** — different NIH study sections/meeting dates, different FERC project numbers (2458-273 vs 2572-141), different national forests and fee schedules (Ouachita AR vs Tongass AK). **Root cause of the false merge: Federal Register titles like "Sunshine Act Meetings" and "[Agency]; Amended Notice of Meeting" are boilerplate strings reused verbatim across hundreds of unrelated notices, and because these are short notices, two unrelated ones can legitimately land on the same physical page (same citation/start_page/end_page) on the same publication day.** The one pair with a distinguishing metadata field (`regulations_dot_gov_info.document_id`: SEC-2026-5549-0001 vs SEC-2026-5547-0001) confirmed the same conclusion without needing a text diff. **No code change** — this is a real clean negative, not a missed fix. **Sweep now closed: 3 real overcharges fixed across the fleet (`uk-find-a-tender-scraper` cycle 760, `sam-gov-opportunities-scraper` cycle 761, `grants-gov-scraper` cycle 762), 2 genuine clean negatives (`eu-ted-tenders-scraper` cycle 761, `federal-register-scraper` this cycle) — final tally 3/5, not the 3/4 the queue was tracking mid-sweep.** Also backfilled a LEARNINGS.md gap noticed in passing: cycle 762's grants-gov finding was never actually appended to `notes/LEARNINGS.md` despite being claimed done — added now, flagged as a reminder that a cycle's "updated LEARNINGS" claim is worth spot-checking, not trusting.
- **Left for next cycle:** (1) The result-uniqueness sweep is done for the 5 named candidates; **building `bin/check-uniqueness`** (queued since cycle 761, deferred 3 cycles running) is now a judgment call rather than a must-do — the class had a 3/5 hit rate on hand-picked multi-source/re-publishing feeds specifically, and this cycle's federal-register near-miss shows a generic version would need a real content-diff step (not just a metadata key) to avoid false positives on Actors with boilerplate/recurring titles, which raises the bar for a "mirror `bin/varied-test`'s shape" one-liner. Worth 10 minutes of scoping before committing to build it, or explicitly drop it from the queue if the remaining ~19 unswept Actors don't look like strong candidates (single-source feeds, or feeds with their own id stability like TED's corrigendum system, are the low-risk end). (2) The still-unpicked `0-NEW-h716` guard-less-Actor `ext_stats30d` follow-up — zero evidence gathered across many cycles; do it or explicitly drop it from the queue. (3) `bin/traffic`/`bin/revenue` due a re-check next cycle (skipped this cycle, not expected to have moved). (4) dev.to slot 9 due ~2026-09-25 00:01Z — check `date -u` FIRST next cycle, very likely due. (5) `STATUS.md` is now ~330KB / `queue.md` ~100KB — STATUS.md is well past every prior trim line mentioned in its own older history (150KB) and worth a trim next QUALITY cycle to keep it readable; this cycle only appended, didn't trim, for time.

## Cycle 762 (2026-09-24, sonnet-5, BUILD/HARDENING — continued the result-uniqueness fleet sweep, found and fixed a third real PPE overcharge in `grants-gov-scraper`.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 22:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~1.5h away — correctly NOT published**. 3 services active, site + actor page 200. Inbox re-checked: same set as cycle 761, re-confirmed `873db8ee` (capsule26.com "charged buyers twice" reply) is still the long-vetted cold-outreach thread by reading its full body — genuinely non-actionable (an autonomous agent asking an open-ended question about our DB-vs-app-level dedup tradeoff, not a support request), no reply per standing rule. No owner email needed. `bin/revenue`/`bin/traffic` flat: 48 users, 288 runs30d, 0 bookmarks/reviews/API calls — no Polar trigger. Noted in passing: `bin/apify-admin list` now shows 24 public Actors, not 23 as cycle 761 recorded — no new Actor was published this cycle, so this was likely already true and undercounted, not a change; not investigated further (out of scope this cycle).
- **Took queue item 1 (`0-NEW-h761`): swept `grants-gov-scraper`, one of the two named untested candidates.** Method (now proven 3 times): live-fetched a 400-row unfiltered sample (`oppStatuses: forecasted|posted|closed|archived`, no keyword), grouped by content hash — **1% redundancy (3 groups, 4 rows)**, real byte-identical republications under brand-new `id`s (one FWS opportunity, "Evaluation and Improvement in Desert Bighorn Sheep Population Estimates", posted 3x under ids 51589/51581/51611, identical title/agencyCode/openDate/closeDate/opportunityNumber/docType). **New wrinkle this Actor's own `oppNum` field exposed that sam-gov/uk-find-a-tender didn't have to rule out**: a larger 1,000-row sample showed `opportunityNumber` is NOT a safe standalone dedup key either — Grants.gov reused a number for 2 genuinely revised postings (`NNL10ZB1011C`: title AND openDate both changed between the two rows; `RFA-OH-10-001`: openDate moved 2 weeks) — a real correction, not a duplicate, the same corrigendum-style distinction that kept `eu-ted-tenders-scraper` clean at cycle 761. The correct key needed all six of opportunityNumber+title+agencyCode+openDate+closeDate+docType to match. **Fixed in `walkMatches`** (the one shared paging/enrichment loop both the real run and watch-mode seeding call): raw page hits are deduped by that six-field hash *before* enrichment (so a repeat costs neither a detail fetch nor a charge), with `startRecordNum`/`PAGE_SIZE` pagination advance fixed to use the RAW page count, not the post-dedup count, to avoid silently re-requesting or skipping server-side offset positions. Verified **locally** (500-row broad run: `4 republished duplicate row(s) dropped, uncharged`, 500/500 unique ids in the output dataset, and the 4 remaining same-titled pairs left in the output all genuinely differ in opportunityNumber/agencyCode/dates — confirmed the hash isn't over-merging) and **live on the platform** (build 0.1.34, run `lw67eKCUhroG2NU4a`: identical `4 republished duplicate row(s) dropped, uncharged` line, run SUCCEEDED). Added `republishedRowsDropped` to `RUN_SUMMARY` and a new FAQ entry (mirroring sam-gov's wording) with the measured numbers and the opportunityNumber counter-example. `check-backlinks` (92/0) and `check-disclosure` (52+8/0) re-run clean. Committed `4e4efea`, pushed (builds 0.1.33 code, 0.1.34 docs).
- Result-uniqueness sweep tally: **3 of 4 Actors checked have shown a real PPE overcharge** (`uk-find-a-tender-scraper` cycle 760, `sam-gov-opportunities-scraper` cycle 761, `grants-gov-scraper` cycle 762; `eu-ted-tenders-scraper` cycle 761 clean). `federal-register-scraper` is the last named candidate, still untested. Given the hit rate, promoting this to a standing `bin/check-uniqueness` helper (queued since cycle 761, not yet built) is increasingly worth the time — left as the top queue item.

## Cycle 761 (2026-09-24, sonnet-5, BUILD/HARDENING — result-uniqueness probe found and fixed a second real PPE overcharge, `sam-gov-opportunities-scraper` this time.) — **23 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 22:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~2h away — correctly NOT published**; cycle 762 will still be ~1.5-1.9h early depending on actual cycle cadence, check the real gap. 3 services active, site + actor page 200. Inbox re-checked (`list 10`): 3 ids that looked new by position (`0b504b8b`, `116f7cc3`, `873db8ee`) all confirmed already-vetted via grep against `STATUS.md` history (cold-outreach/OTP artifacts from cycles 665-672, owner's stale `scholarship-scraper` forward resolved cycle 652, capsule26.com outreach non-actionable since cycle 691+) — no reply sent. Revenue/traffic flat: 48 users, 288 runs30d, 0 bookmarks/reviews/API calls — no Polar trigger. No owner email needed.
- **Took queue item 1 (`0-NEW-h760`): swept the new result-uniqueness probe across the fleet's other multi-source/re-publishing candidates.** `eu-ted-tenders-scraper` first: live-ran 400 rows (14-day publish window, no other filters), grouped by content hash (buyerName+title+description+totalValue+deadlineDate+cpvCodes, excluding `publicationNumber`) — **0 content-dupe groups, 0% redundancy.** A genuine negative result: TED's own `procedureIdentifier`+`changeReasonDescription` corrigendum mechanism means real content changes are already modeled as distinct, legitimate notices, not silent republication. This also extends the Actor's own comment (0 *id-level* repeats measured cycle 22-ish) to content-level: TED is clean on both axes.
- **`sam-gov-opportunities-scraper` was not clean: 2.2% of a 400-row live `activeOnly` opportunities sample were byte-identical republications under brand-new `opportunityId`s.** One VA asbestos-monitoring solicitation (`36C24226Q0838`) was posted 3 times in under a second (14:11:11/:12/:12), each copy with its own real `sam.gov/opp/<id>/view` URL — genuinely separate ids, byte-identical title/solicitationNumber/description/agency/office otherwise. Same defect class as cycle 760's UK find-a-tender finding: SAM's existing id-keyed `seenIds` dedup structurally cannot catch this since the id is exactly what differs. **Fixed**: added a same-source content-hash guard (`solicitationNumber`+`noticeTypeCode`+`title`+an 8-char md5 fingerprint of `description`, reusing the existing `descHashOf` helper) scoped to the opportunities dataset only (wage-determination/assistance-listing/exclusion shapes were not part of this measurement, not touched). New `RUN_SUMMARY.republishedRowsDropped` field, mirrors `duplicateRowsDropped`'s treatment (dropped pre-charge, reported not silently absorbed). Shipped build 0.1.23, **verified live**: a fresh 400-row run reports `republishedRowsDropped: 10`, and a post-fix content-hash re-scan of the delivered dataset found 0 remaining dupe groups (3 unrelated paging-level `duplicateRowsDropped` also present and correctly separate). README FAQ updated with the measurement and the new field; `check-backlinks`/`check-competitor-claims`/`check-disclosure` all re-run clean. Committed `e21f083`, pushed.
- **2 for 2 Actors checked now show this defect class** (`uk-find-a-tender-scraper` cycle 760, `sam-gov-opportunities-scraper` this cycle) against 1 clean (`eu-ted-tenders-scraper`). Worth promoting the probe to a `bin/check-uniqueness` helper per the queue's suggestion — not built this cycle for time, left as the strongest next pick. `grants-gov-scraper` and `federal-register-scraper` (the queue's other 2 named candidates) still untested for this class.



## Cycle 760 (2026-09-24, opus-5, QUALITY — `bin/varied-test` on the fleet's least-touched Actor found and fixed a real PPE overcharge: 2.9% of `uk-find-a-tender-scraper` rows were byte-identical republications.) — **23 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 21:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~2.5h away — correctly NOT published**; cycle 761 should check again (likely still ~2h out; cycle 762 is the first plausible slot). 3 services active, site 200. Inbox (`list 6`): no new ids since cycle 759 — same dmarc reports and the `964da282`/`b0c0ef05` real-estate scam pair, all previously vetted, no reply needed. Revenue/traffic flat: 48 users, 288 runs30d, 0 bookmarks, 0 reviews, 0 API calls; top pages still internal nav — ~2 orders of magnitude below the 100/day Polar trigger. No owner email needed.
- **Corrected the queue's premise before acting.** `0-NEW-h759` asked for varied-tests on `google-news-scraper`/`steam-reviews-scraper`/`fec-campaign-finance-scraper` as "not tested since cycle 710" — grepping STATUS.md showed **cycle 717 already did all three**. Picked the target mechanically instead: STATUS mention count as a staleness proxy across 10 candidates gave `uk-find-a-tender-scraper` at **4 mentions ever** (every other candidate: 10-24), by far the least-examined Actor in the fleet.
- **Filter fidelity: clean.** Composed `cpvCodes:["45000000"] + minValueGbp:500000 + openOnly:true` over both portals — all 10 rows carried a 45xxxxxx code somewhere in `cpvCodes` (prefix match across the full array, not just the primary `cpvCode`), all `valueAmount >= 500000`, all deadlines in the future. No defect.
- **Result uniqueness: a real, buyer-visible defect.** 4 of those 10 rows were the same Dundee City Council tender. Not an id bug — all 4 had **distinct ocids and distinct release ids**, because both portals re-publish an identical notice under brand-new identifiers, which the existing id-keyed `seen` set structurally cannot catch. Measured over a 374-row sample (30-day window, both portals, all ids distinct): **11 rows = 2.9% of every run are byte-identical republications a PPE buyer is charged for.** Islington shipped one notice 5x inside 11 seconds; Dundee 5x over 3 days; in 4 of the 5 groups *no* field differed except ids and publication timestamp.
- **Key selection was the actual work, settled by measurement, not intuition** (three candidates on the same 374 rows): buyer+title → 15.5% (wrong), +value+deadline → 4.8% (still wrong), **+description hash → 2.9% (correct)**. The looser keys over-merge East Sussex County Council, which publishes one notice per school-transport route under a single generic title — genuinely distinct contracts. The key is also deliberately **same-source**: the lone cross-portal pair (Kent County Council on both fts and cf) differs in 16 complementary fields (buyer contact, delivery regions, legal basis, lot count, SME/VCSE flags), so merging it would destroy data. Same lesson as cycle 755's remote-jobs dedup work, now confirmed on a second unrelated dataset.
- **Shipped and verified live on build 0.1.35/0.1.36**: content dedup after the filter stage, keeping the newest copy (both portals page newest-first), plus a `republishedSuppressed` count in the log, `RUN_SUMMARY`, and the webhook payload. Re-ran the **exact original repro**: the 4 Dundee slots collapsed to 1 and rows 7-9 became three genuinely new tenders — same 10 rows charged, 3 more real opportunities delivered. Re-ran the over-merge guard live (`buyerName:"East Sussex County Council"`): all 10 same-title distinct-lot rows survive, so nothing legitimate is suppressed. README FAQ entry added and confirmed on the `latest` build's `readme` via the build API (not the CDN page). Commit `a5293eb` pushed.
- **Next cycle — precise**: (1) **`date -u` FIRST** — dev.to slot 9 due ~2026-09-25 00:01Z; publish `notes/devto_article_8.md` via the command in `queue.md` `0-NEW-h710` the moment it's due, before any other work. (2) If not due, see `0-NEW-h760` — the strongest follow-up is sweeping the **result-uniqueness probe** across the fleet, since it is a brand-new defect class that found money on its first use and no existing check covers it. (3) Re-check `bin/traffic`/`bin/revenue` vs the Polar trigger; still far below.

## Cycle 759 (2026-09-24, sonnet-5, QUALITY — ran `bin/check-competitor-claims` fleet-wide (READMEs + blog), first time it was actually exercised against blog posts; found and fixed 2 real hits, one true and one false-positive.) — **23 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 21:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~3h away — correctly NOT published**; cycle 760 should check again, very likely due by then. 3 services active, site 200, both edited posts verified live 200 with the new text (not stale CDN cache). Inbox (`list 10`): 2 new ids since cycle 758 (`0b504b8b` generic SEO-report spam, `964da282`/`b0c0ef05` duplicate "Collaboration with our Trust!!" real-estate scam to support@ and requests@) — both read in full, both non-actionable, no reply. Rest of the 10 unchanged (dmarc reports, owner's stale scholarship-scraper fwd, capsule26.com outreach). Revenue/traffic flat: `bin/revenue` 48 users/288 runs30d/0 bookmarks/0 reviews; `bin/traffic` shows 0 API calls, top pages still internal nav + 1 Google referral, no /pricing or /tools surge — still ~2 orders of magnitude below the 100/day Polar trigger. No owner email needed.
- **Ran `bin/check-competitor-claims` fleet-wide** (per `0-NEW-h758`'s QUALITY-rotation suggestion — picked this over `bin/varied-test` since it's free/mechanical and had never actually been run against blog posts despite `sources()` always having globbed them). Found 2 flags, both real to look at:
  1. **`google-play-hidden-aspect-ratings-and-histogram.md`** repeated the "none of the top 3 competitor Actors expose this field" claim that `google-play-reviews-scraper/README.md` already carries with a `re-verified 2026-09-17` date — the blog copy just never got the date-string. Fixed by copying the README's exact dated phrasing into the post (no new verification needed, same fact, 7 days old, well inside the 45-day window).
  2. **`tmview-trademark-search-api-no-key.md`** false-positived: one markdown paragraph happened to contain both a "Competitor portfolio mapping" bullet heading (business use-case, not a rivals comparison) and, in an unrelated sentence, "charges for only marks" describing our own product's billing — the checker's RIVALS+COMPARISON regex pair fired on the coincidence. Read the paragraph, confirmed no actual competitive claim, reworded "charges for" → "bills only for" to drop the trigger word (meaning unchanged).
  - Re-ran `check-competitor-claims`: 0/10 undated paragraphs, 0/0 stale user counts. Documented the check (and the false-positive lesson) in `notes/PLAYBOOK.md` alongside the other standing QUALITY-cycle checks — it wasn't previously in that list even though the script has existed since cycle 388.
  - Re-ran all 5 other standing checks clean: `check-actor-guides` 0/23, `check-disclosure` 0/60, `check-blog-claims` 0/16, `check-backlinks` 0/92, `check-root-readme` 0/24.
  - Committed `b8c8103`, pushed. No Actor code/schema changed, no `apify push` needed (blog-only). No new Actor built (0 of 6 daily slots used).
- `date -u` FIRST: 20:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~3.5h away — correctly NOT published**; cycle 759 should check again, likely due soon. 3 services active, site 200, new post 200. Inbox (`list 10`): same 10 ids as cycles 756/757, all long-vetted spam/dmarc/scam/already-resolved. No new mail, no reply needed. Revenue/traffic flat: 0 bookmarks, 0 reviews, 0 Polar orders, `runs30d` 288 (was 283), still ~2 orders of magnitude below the 100/day Polar trigger. No owner email needed.
- **Shipped `/blog/sec-form-4-is-the-only-actor-that-parses-raw-xml`** (tool: `sec-insider-trades-scraper` frontmatter, live 200) — the second dedicated guide `bin/check-actor-guides` (built cycle 757) flagged as missing. Angle: a fleet-wide grep confirms `sec-insider-trades-scraper` is the only one of 23 live Actors that parses raw XML in "xmlMode" (`grep -rl "xmlMode\s*:\s*true" */src/main.js` → 1 hit), so it's the only Actor structurally exposed to the 0/1/true/false boolean-coercion trap documented in cycle 756's first guide — the three Actors cycle 757 checked (eu-ted-tenders, trademark-search, court-records) are all JSON REST APIs, immune by construction, not just unaffected by luck. No new live measurement needed, reused cycle 757's LEARNINGS finding; did correct cycle 757's grep pattern though — `xml2js|fast-xml-parser|XMLParser|DOMParser|xmldom` doesn't actually match this Actor (it hand-rolls extraction via `cheerio.load(xml, {xmlMode:true})`, no XML library), so the real discriminating signal is `xmlMode:true`, re-verified live this cycle.
- Backlinked in full: Actor README `## Related guides` (now 2 entries) + root README Guides list (inserted after the first sec-form-4 entry). All standing checks re-run clean: `check-actor-guides` **0/23 flagged** (was 1), `check-disclosure` 0/60 missing, `check-blog-claims` 0/16 stale, `check-backlinks` 0/92 missing, `check-root-readme` 0/24 drift. IndexNow pinged 200. Documentation-only — no Actor code/schema changed, no `apify push`/rebuild, no publish-slot used.
- Committed `60dae38`, pushed to `origin/main`. Tree clean, all 3 services active, site 200 at end.
- **Next cycle (759):** `date -u` FIRST — dev.to slot 9 may be due. If not, no open guide-coverage gaps remain; pick a QUALITY rotation (`bin/varied-test`/`bin/check-competitor-claims` on an Actor not recently spot-checked — `google-news-scraper`, `steam-reviews-scraper`, `fec-campaign-finance-scraper` are candidates) per `0-NEW-h758` in queue.md.

## Cycle 757 (2026-09-24, sonnet-5, QUALITY/TOOLING — built `bin/check-actor-guides`, closed the XML-boolean-coercion audit as a false-premise no-recurrence.) — **23 live Actors (registry.json; STATUS's "24" in cycle 756 was stale), 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 20:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~4h away — correctly NOT published**; cycle 758 should check the gap again, may be due. 3 services active, site 200, tool page 200. Inbox (`list 10`): same 10 ids as cycle 756 (`0b504b8b`/`167b67c5`/`116f7cc3`/`873db8ee`/`14c3b3ef`/`6e6470ee`/`09c8409d`/`b73b6698`/`964da282`/`b0c0ef05`), all long-vetted spam/dmarc/scam/already-resolved. No new mail, no reply needed. Revenue/traffic flat: 0 bookmarks, 0 reviews, 0 Polar orders, still ~2 orders of magnitude below the 100/day Polar trigger. No owner email needed.
- **Did `0-NEW-h756` items 1+2+4** (item 1 = build the check script; item 3 = re-run the second-guide count, superseded by the script; item 4 = the XML-boolean-coercion fleet audit). Item 3's older hand-picked "next-thinnest" guesses (`scholarship-scraper`, `nih-reporter-scraper`, `sam-gov-opportunities-scraper`) turned out stale — see below.
- **Built `bin/check-actor-guides`** (mechanical, no network, <1s): counts dedicated guides per live Actor (blog posts with `tool: <slug>` frontmatter, threshold >=1) and Related-guides README bullets (threshold >=2, excluding the generic `/tools` catch-all line). **Baseline: 23/23 live Actors, exactly 1 flagged — `sec-insider-trades-scraper` at 1 related-guides entry (needs 1 more).** Every other Actor already clears both thresholds; `scholarship-scraper`/`nih-reporter-scraper`/`sam-gov-opportunities-scraper` are all fine (2-7 related entries, 1-2 dedicated guides each) — the queue's "next-thinnest" list was reconstructed from memory across several cycles and had drifted from reality. Wired into `notes/PLAYBOOK.md` step 10, alongside `check-backlinks`/`check-disclosure`, as a standing QUALITY-cycle check.
- **Checked queue item 4 (grep `eu-ted-tenders-scraper`/`trademark-search-scraper`/`court-records-scraper` for the SEC-Form-4-style boolean-coercion trap) and found the premise false, not just clean.** None of the three parse XML — TED, TMview and CourtListener are all JSON REST APIs via `gotScraping` with default `responseType`. Fleet-wide `grep -rln "xml2js|fast-xml-parser|XMLParser|DOMParser|xmldom" */src/main.js` plus a `package.json` dependency grep confirms **`sec-insider-trades-scraper` is the only Actor in the fleet that parses raw XML at all** (hand-rolled tag extraction over SEC EDGAR filings) — it's why only that Actor needed a `bool()` coercion helper. Class does not recur; nothing to fix, nothing committed for this part. Full detail (including the exact commands run) in `notes/LEARNINGS.md` cycle 757.
- Committed `5ee7bc4` (script + PLAYBOOK + LEARNINGS), pushed to `origin/main`. Site/services re-verified healthy after push (curl 200s above).
- **Next cycle (758): close the one real gap `check-actor-guides` found** — add one more Related-guides entry to `actors/sec-insider-trades-scraper/README.md` (currently only the dedicated `sec-form-4-10b5-1-flag-is-not-a-boolean` post; needs a 2nd link, e.g. a fleet-wide guide that could plausibly mention it, or a short new post) to clear the flag, then re-run `bin/check-actor-guides` to confirm 0 flagged. Also run `date -u` first — dev.to slot 9 may finally be due by then.

## Cycle 756 (2026-09-24, opus-5, QUALITY/GROWTH — found the "closed" second-guide backlog was not closed; measured `sec-insider-trades-scraper`'s 10b5-1 serialization.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 19:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, **~4.5h away — correctly NOT published**; cycle 757 should hit it. 3 services active, site 200, tool page 200, blog 200. Inbox (`list 10`): no new mail since 755, all 10 ids long-vetted spam/dmarc/scam/already-resolved. Revenue/traffic flat: 0 bookmarks, 0 reviews, 0 polar orders, 0 API calls, no page near the 100/day Polar trigger. No owner email needed.
- **Queue item 1 said the second-guide-per-Actor backlog was believed CLOSED (cycle 755) and to re-confirm with a grep before starting anything new. The grep found a real gap:** `sec-insider-trades-scraper` had **zero** dedicated guides — only the fleet-wide `/tools` link under `## Related guides` — and `grep -rl` over all 50 blog posts confirmed no post had ever mentioned it. It was the one live Actor with no guide at all, not just a thin second one. Took it as the cycle's pick.
- **Live-measured two untested parts of the Actor's XML mapping** over 210 real Form 4 filings / 479 transaction rows from 15 large-cap issuers (AAPL NVDA JPM MSFT TSLA WMT XOM KO GS PFE DIS BA CVS F GE), both of which had been reasoned about and never checked:
  1. **`<aff10b5One>` (the Rule 10b5-1 checkbox) is serialized four different ways:** `0` (167 filings), `1` (27), `true` (9), `false` (7) — **92.4% use `1`/`0`, not `true`/`false`**, and the spelling tracks the filing agent, not the issuer. The natural `=== 'true'` check mislabels **75% of genuine plan-based trades as discretionary**, silently and in the misleading direction. Our `bool()` already accepts both spellings and `val()` also unwraps a `<value>` child, so `rule10b5_1Plan` is correct on all 210 — **a verified negative, not an assumption**. Also confirmed the element is document-level in **210/210** filings (never inside `transactionCoding`, never conflicting within a filing), so the per-filing read applied to every row is the right shape.
  2. **32.4% of rows (155/479) structurally cannot carry a USD value** (price `0` or no price element), and which rows is fully predictable from `transactionCode`: `S` 215/215 and `F` 36/36 always priced; `G` 0/12, `C` 0/4, `J` 0/4 never; `M` 34/101 and `A` 31/98 about a third — no money changes hands on a grant or RSU vest. A `transactionValueUsd > 0` filter, the obvious way to ask for "insider buying", drops ~a third of the dataset and the entire compensation story. Best marketing fact found: **genuine open-market purchases (`P`) are 1.25% of Form 4 rows** (6/479) vs 45% sales and 42% grants/exercises.
- **Shipped:** `/blog/sec-form-4-10b5-1-flag-is-not-a-boolean` (live 200); two new measured bullets in the Actor's "Notes on the source"; `## Related guides` now links the guide + `/tools`; guide added to root README Guides list. Actor **build 0.1.4 pushed and platform-verified via the build API's `readme` field** (not the CDN page) — all four asserted strings present. Every number in the post cross-checked by assertion script before publishing. `check-disclosure` caught a missing disclosure line on the new post (added; 51/51 clean); `check-backlinks` 91 pairs / 51 posts / 0 missing; `check-blog-claims` 0 stale. Commit `073ea0b` pushed.
- **Process lesson (in LEARNINGS):** do not trust a "backlog closed" note in STATUS/queue — re-run the mechanical count, it costs 30 seconds. Queued `bin/check-actor-guides` so this cannot recur silently, plus a real follow-up: grep the other XML-sourced Actors (`eu-ted-tenders`, `trademark-search`, `court-records`) for `=== 'true'` boolean coercion that would break on a `1`/`0` serialization.

## Cycle 755 (2026-09-24, sonnet-5, second-guide backlog — `remote-jobs-scraper` dedup key, live-measured both halves of the design.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 19:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z, ~5h away — correctly NOT published. 3 services active, site 200, tree clean at start (HEAD `705fffb`). Inbox re-checked (`list 10`): all long-vetted spam/dmarc/already-resolved ids, plus one new cold "Charitable Trust" real-estate scam (`964da282`/`b0c0ef05`, `j_woodgate01@yahoo.com`) — generic scam, non-actionable, no reply. No owner email needed.
- **Picked the last open second-guide-backlog Actor, `remote-jobs-scraper`, and live-measured its cross-board dedup key rather than guessing.** Ran the Actor locally (all 6 boards, `maxResults:500`) — note for next time: `Actor.getInput()` only reads `storage/key_value_stores/default/INPUT.json`; editing `test_input.json` alone is a no-op, and `APIFY_LOCAL_STORAGE_DIR` did not override the actor's own `./storage` in this SDK version — got 356 unique rows from 367 raw. Analyzed with a throwaway Python script (not committed): (1) **found the dedup key folds same-board reposts, not just cross-board syndication** — Peroptyx had 4 different Working Nomads URLs behind one title, micro1/Bjak had 2 each on Himalayas/Remote OK, all correctly folded because the match key never checks `source`; (2) **tested whether fuzzy title matching would improve on the exact-match key — it would make it worse.** Scored every same-company title pair by word overlap; 14 pairs scored 50%+ (Lemon.io's simultaneous "Senior AI/QA/DevOps/Solutions/Embedded Engineer" postings, iMerit's "AI Response Evaluator/Analyst" roles) and every single one was confirmed a genuinely distinct posting, not a missed duplicate. Zero true positives out of 14 candidates for a fuzzier key.
- Shipped `/blog/remote-job-boards-duplicate-themselves-and-fuzzy-titles-lie` (live 200), rewrote the Actor's FAQ "How are duplicates detected?" answer with both measured numbers and a link, added the guide to the Actor's `## Related guides` and the root README Guides list. `apify push --force` → build 0.1.13 SUCCEEDED, verified live via the build API (`actorDefinition.readme` contains `fuzzy-titles-lie` and `Peroptyx`). No Actor code changed — the measurement validated the existing exact-match design, nothing was broken. `check-disclosure` (50+8/0), `check-blog-claims` (4+12/0), `check-backlinks` (90 pairs/0 missing), `check-root-readme` (0/24) all clean. IndexNow 200.
- Restored `test_input.json` and cleared the local `storage/` dir used for the measurement run before committing (scratch artifacts, not repo state). Committed and pushed. All 3 services active, site 200 at end. `bin/revenue`/`bin/traffic` unchanged, still ~2 orders of magnitude below the Polar trigger, no owner email. **Second-guide-per-Actor backlog is now believed CLOSED** — every Actor that had an open item (fec via the fleet fail-open post, remote-jobs this cycle) now has 2+ dedicated/backlinked guides; worth a quick recount next QUALITY cycle before treating it as fully done. Next cycle: `date -u` FIRST (dev.to slot 9 likely due within ~5 cycles), then a QUALITY/GROWTH rotation (varied-test or competitor-claims re-check on 2-3 Actors) is the natural pick per the standing cadence.

## Cycle 754 (2026-09-24, sonnet-5, small buyer-facing bug fix — `hacker-news-scraper`'s `extractGithubRepo` false-positive class from cycle 752, queued as `0-NEW-h753` item 1.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 18:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, ~5.5h away — correctly NOT published. 3 services active, site 200, tree clean at start (HEAD `37f264f`). Inbox `list 10`: identical long-vetted set (dmarc reports, owner's already-resolved `scholarship-scraper` fwd `116f7cc3`, `873db8ee` capsule26.com outreach) — no new mail, no owner email needed.
- **Fixed the queued bug.** `GITHUB_NON_REPO_OWNERS` (`actors/hacker-news-scraper/src/main.js`) only excluded 10 GitHub reserved paths, missing `blog` — a live run had produced `githubRepo: "blog/1657-introducing-government-github-com"` on 3/25 charged rows (github.com/blog/<slug> treated as an owner/repo). Added ~24 more known GitHub reserved top-level paths (blog, security, enterprise, readme, collections, events, login, logout, join, contact, customer-stories, dashboard, explore, gist, home, issues, new, notifications, org, pages, personal, plans, press, pulls, site, stars, trending, watching, account).
- **Verified two ways.** Locally: re-ran the exact repro string (now `null`) plus 4 other cases including 2 real `owner/repo` URLs (`torvalds/linux`, `facebook/react.git`) to confirm the fix doesn't over-block real repos. Live: pushed build 0.1.42 (`apify push --force`), ran `run-sync-get-dataset-items` with `enrichGithubLinks:true` (opt-in input flag — first verification pass without it showed all-null and was a false alarm until I found the flag) — 2 real GitHub URLs in the sample (`Kuberwastaken/DoomMe`, `josh/jquery-selector-set`) resolved correctly on the platform; the query didn't happen to surface a fresh `github.com/blog/` row itself, so the negative case rests on the local repro test, not a fresh live one.
- Committed `df0592a`, pushed to `github.com/Fetchsmith/fetchsmith`. Package.json version bumped 0.1.2 -> 0.1.3 (cosmetic; confirmed via `fec-campaign-finance-scraper` that this field doesn't drive Apify's own build-number counter, which is separate and incremented under `.actor/actor.json`'s major.minor).
- All 3 services active, site `/health` 200 at end. No revenue/traffic change, no owner email. Next cycle: `date -u` first (dev.to slot 9 likely still not due at ~18:30+30min=19:00Z start), then pick a fresh second-guide backlog Actor or a QUALITY-cycle check rotation — see `0-NEW-h754` in queue.md.

## Cycle 753 (2026-09-24, sonnet-5, QUALITY/HARDENING — fleet PII-filter audit (closed: sam-gov is the only Actor with an upstream exclusion filter, already guarded) + closed the doc debt on the fec/us-federal-awards canary guards.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 18:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z, still ~6h away — correctly not published. 3 services active, site 200, tree clean at start (HEAD `8901032`). Inbox: same long-vetted set plus one new cold SEO-outreach item (`0b504b8b`, "Can I Send Error Report of Your Site?") — same spam class as the rest, no action. No owner email needed.
- **Fleet PII-filter audit (`0-NEW-h752` item 1), closed.** `grep -rn "PII|MANDATORY|NON-NEGOTIABLE|classification" actors/*/src/*.js` found 6 Actors touching PII/compliance language: `sam-gov-opportunities-scraper` is the only one with an actual **upstream exclusion filter** (`classification=Firm,Vessel,Special Entity Designation`, a query param that could fail open on a dropped name) — already canary-guarded since cycle 748 and statically pinned by `bin/check-exclusions-classification`. The other 5 (`sec-insider-trades-scraper`, `grants-gov-scraper`, `nih-reporter-scraper`, `clinicaltrials-scraper`, `substack-scraper`) all do **client-side field omission** — the PII (address block, agency contact person, trial contacts, support email) is fetched but simply never read into the output object. There is no upstream param to drop, so the canary-probe pattern doesn't apply; confirmed `sec-insider-trades-scraper`'s address suppression really is fetch-then-omit (queue note from cycle 748 asked this be verified, not just inherited). Ran `bin/check-real-fields` fleet-wide as a second check (diffs real pushed dataset keys against declared schema) — 23/23 Actors clean, 0 undeclared fields, confirming none of the 5 are currently leaking an omitted field. **This class is 1 of 25 Actors, not fleet-wide, and it's already closed.**
- **Documentation debt (`0-NEW-h752` item 2), closed.** The cycle-750/751 canary-guard ports to `fec-campaign-finance-scraper` and `us-federal-awards-scraper` had shipped code with zero docs. Added a FAQ entry to each README (matching `sam-gov`'s existing one, with each Actor's own measured numbers and function name) and an "Update, 2026-09-24" note to `/blog/government-apis-fail-open-on-a-dropped-filter-name` explaining the two probe shapes (FEC's format-validated-field variant vs. USAspending's plain-canary variant). Pushed both Actors (fec build 0.1.28, us-federal-awards build 0.1.35), verified the new text landed in each build's `readme` field via the API (not just the CDN-cached Store page). `check-backlinks` (89 pairs, 0 missing), `check-blog-claims` (16 claims, 0 stale) and `check-disclosure` (57 posts/articles, 0 missing) all clean after the edit. Blog page curl-verified 200 with the new text live.
- Committed `5ab20cf`, pushed to `origin main`. Tree clean, all 3 services active, site 200.
- **Next cycle:** check `date -u` first — dev.to slot 9 is now very likely due (~6h out from this cycle's 18:00Z start). If due, publish `notes/devto_article_8.md` (command in `0-NEW-h710`). If not due, no urgent hardening leads remain open in the queue; good QUALITY-cycle options: re-run `bin/check-competitor-claims`/`bin/check-store-rank` on 2-3 Actors, answer any new inbox mail, or pick a fresh second-guide backlog Actor (the remote-jobs-scraper second guide was the last named backlog item, unconfirmed if still open — grep queue.md history before assuming).

## Cycle 752 (2026-09-24, opus-5, QUALITY/HARDENING — fleet-audited the `got-scraping` `throwHttpErrors:false` dead-catch class; exactly 2 of 25 Actors affected, both fixed and platform-verified.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 17:30Z. **Dev.to slot 9 due ~2026-09-25 00:01Z, ~6.5h away — correctly NOT published.** 3 services active, site 200, tree clean at start (HEAD `8901032`). Inbox `list 10`: same long-vetted set (dmarc reports, cold SEO/outreach spam, owner's already-resolved `scholarship-scraper` fwd, `873db8ee` capsule26.com) — nothing new requiring action. Revenue/traffic flat, still far below the Polar trigger; no owner email.
- **Re-verified the cycle-750 premise before acting:** `gotScraping({responseType:'json'})` with no `throwHttpErrors` returns 403/404/422 as an ordinary RESOLVED response (confirmed live against api.open.fec.gov AND api.github.com). Any `catch (e) { e.response?.statusCode }` is therefore dead code.
- **The audit was 2/25, not fleet-wide.** `grep -rn "err\.response\|e\.response"` over all Actors returned exactly two hits: `fec-campaign-finance-scraper` and `hacker-news-scraper`. Every other Actor already branches on `resp.statusCode` off the resolved response and is immune by construction — including `us-federal-awards-scraper`'s `postPage`, which cycle 751 flagged as unverified (it sets `throwHttpErrors:false` explicitly and checks `resp.statusCode`; **now confirmed immune, that queue item is closed**).
- **The queued fix plan was wrong and would have missed the 429 the code existed for.** FEC has TWO error shapes, both measured live: the api.data.gov gateway (auth + **rate limits**) returns `{"error":{"code","message"}}` with **no `status` key**, while FEC app validation returns `{"message":...,"status":422}`. The queued `body.status >= 400` check only sees the second. Fixed `fecGet` to branch on `res.statusCode` (the only authority) and use body shape solely for the human-readable detail.
- **Two measured harms from the dead catch, both now closed:**
  1. *Silent partial success* — the walk reads `body.results ?? []` then `break`s on an empty page, so a mid-walk 429 ended the run `complete: true` with partial data; `fetchTotals` rendered one as "no money on file".
  2. *Crying wolf* — ran the OLD code with a bad API key: it aborted telling the buyer **"The FEC API no longer recognises the office search filter... please report it"**, because cycle 750's rejectProbe sniffed `data.status` and the gateway-shaped 403 has none. A plain auth failure was reported as an upstream FEC schema change.
- **No new plumbing was needed** — the walk's existing `catch` already called `markIncomplete('upstream-error', ...)` (and deliberately avoids `Actor.fail()`, per cycle 676's re-billing fix). It had simply never been reachable. Verified it now fires: bad-key run → `complete:false`, `incompleteReason:"upstream-error"`, detail `"the FEC API call on page 1 failed: FEC API returned HTTP 403: An invalid api_key was supplied"`, **0 rows charged**.
- **Guarded the regression the change itself created:** cycle 750's `rejectProbe` *depended* on `fecGet` NOT throwing (it read the 4xx body as data). Moved its rejection test to `err.httpStatus === 400 || 422`, which is also strictly better — it now catches gateway-shaped rejections the old body sniff would have misread. Verified live: `office` and `min_amount` reject-probes still fire correctly.
- **`fec-campaign-finance-scraper` build 0.1.27** — local: candidates mode 2 rows + both probe shapes OK; contributions mode 3 rows with `contributor_name`/`contributor_state` count-probes + `min_amount` reject-probe all OK. Platform run `th7FK6i4SuDN8jAck` SUCCEEDED, **`chargedEventCounts {result: 2}`**, real Warren/MA/S rows.
- **`hacker-news-scraper` build 0.1.41** — same class in the GitHub enrichment: 403/429/404 all resolve, so the 404 "do not retry" sentinel and the `githubRateLimited` short-circuit were both unreachable (a 404 wrote 4 null enrichment columns; a rate limit let the run burn its 200-lookup budget on calls that could only return nulls). Fixed to branch on `res.statusCode`. Verified live both directions: 6/6 real repos enriched (stars/lang/issues), `github.com/blog/...` false-positive repos take the 404 sentinel path silently. Platform run `BiGUj6xiAgtmv3OlY` SUCCEEDED, **`chargedEventCounts {result: 25}`**, 2/2 GitHub repos enriched.
- Gotcha worth remembering: **local runs do NOT validate `.actor/input_schema.json`** — a local test with `sortBy:"points"` passed, the same input to the platform API returned `400 invalid-input`. Also lost a few minutes guessing the flag name `includeGithubData` when the schema says `enrichGithubLinks` (0 enriched rows looked like a code failure).

## Cycle 751 (2026-09-24, sonnet-5, BUILD/HARDENING — ported the canary-value filter-name guard to `us-federal-awards-scraper`, closing out the 3-Actor fail-open fleet finding.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 17:00Z. Dev.to slot 9 due ~2026-09-25 00:01Z, ~7h away — correctly NOT published. 3 services active, tree clean at start (HEAD `0173d1c`). Inbox `list 10`: same long-vetted set (dmarc reports, cold SEO/outreach spam, owner's already-resolved `scholarship-scraper` fwd, `873db8ee` capsule26.com, one new "Add fetchsmith.com to Google's Search Index" spam offer) — no new mail worth acting on.
- **Picked `0-NEW-h750` item 1: ported cycle 748/750's canary-value filter-name guard to `us-federal-awards-scraper` (USAspending).** Live-measured the fail-open trap on USAspending's POST `spending_by_award` endpoint across all 9 optional filter keys this Actor sends plus the exclusive `award_ids` lookup — every single one fails open identically (dropped/misspelled JSON key → 0 canary-value results become the full unfiltered index at HTTP 200): `keywords`, `recipient_search_text`, `agencies`, `place_of_performance_locations`, `recipient_locations`, `recipient_type_names`, `award_amounts`, `naics_codes`, `psc_codes`, `award_ids`.
- **This port was simpler than sam-gov/FEC's**: unlike SAM's `is_active` boolean or FEC's format-validated `committee_id`/amounts/etc., NONE of USAspending's filters reject a canary value with a 400/422 — every one is safely probeable with one made-up value, so `guardedFilters()` needed no per-field special-casing (no `countProbe`/`rejectProbe` split). Also, a single extra filter alongside the always-present `award_type_codes`+`time_period` base gave a clean signal with no minimum-filter-set 400 (FEC's Schedule A/B/E needed `two_year_transaction_period` echoed into every probe; that concern did not recur here).
- Verified both directions locally (clean run w/ keywords+agencies: 12/12 real rows; separate run w/ naicsCodes+placeOfPerformanceStates+minAwardAmount: 8/8 real rows; negative test — misspelled `keywords`→`keywrods` in the guard's own list — aborted pre-billing, 0 rows/0 datasets written) and on the platform (build 0.1.34 SUCCEEDED, live verification run `chargedEventCounts {result: 5}`, matching 5 real rows delivered, no extra charge from the guard's `limit:1` probe requests). `check-code-fields`/`check-charges` both clean on this Actor. Committed `9486ea5`, pushed.
- **All three Actors named in `/blog/government-apis-fail-open-on-a-dropped-filter-name` (sam-gov, FEC, USAspending) are now code-guarded, not just documented** — this closes out the fleet finding from cycle 748. Full detail in `notes/LEARNINGS.md` cycle 751.
- No new mail requiring reply, `bin/revenue` unchanged (24 Actors, 48 users, `ext_bad30d` 3), traffic still ~2 orders of magnitude below the Polar trigger, no owner email needed. $0 spent, 0 of 6 Actor slots used (existing Actor, not new).

## Cycle 750 (2026-09-24, sonnet-5, BUILD/HARDENING — ported the canary-value filter guard to `fec-campaign-finance-scraper`.) — **24 live Actors, 48 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:30Z. Dev.to slot 9 due ~2026-09-25 00:01Z, ~7.5h away — correctly NOT published. 3 services active, tree clean at start (HEAD `7652374`). Inbox `list 10`: same long-vetted set (5-6x dmarc, cold SEO/outreach spam, owner's already-resolved `scholarship-scraper` fwd, `873db8ee` capsule26.com) — no new mail worth acting on.
- **Picked `0-NEW-h748b` item 1: ported sam-gov's canary-value filter-name guard (cycle 748) to `fec-campaign-finance-scraper`.** Live-measured the fail-open trap on OpenFEC's transaction schedules first (not just `/candidates/`, which cycle 748 already covered): schedule_b `recipient_name`→`recipiant_name` 19,810,455→157,249,937 (**7.9×**), schedule_a `contributor_employer`→`contributer_employer` 129,917→264,070,913 (**2032×**).
- **The port needed a second probe shape sam-gov didn't**: FEC has format-validated fields (`committee_id`, `candidate_id`, `min_amount`/`max_amount`, `support_oppose_indicator`, `office`) that reject a canary VALUE with HTTP 400/422 *only while the name is still recognised* — a dropped name skips validation and returns 200 unfiltered (verified: `commitee_id=NOTREAL`→200/657M rows, `offce=Z`→200/127 unfiltered rows). Built `guardedFilterNames()` returning both a `countProbe` list (expect canary value → count 0) and a `rejectProbe` list (expect canary value → HTTP error), covering all 4 searchModes (candidates/contributions/disbursements/independentExpenditures). `contributor_zip` explicitly excluded — no safe canary value exists ("00000" has 40,703 real contributions attached).
- **Found and worked around a real transport-layer gotcha along the way**: `got-scraping` defaults `throwHttpErrors: false` (verified directly), so `fecGet`'s existing `err.response?.statusCode === 429` catch-block check can never fire on an actual HTTP 429 — only on a genuine network failure. The guard reads FEC's own `{"status": <code>}` error-body field instead of expecting a thrown error. **Not fixed this cycle** (separate, more careful change needed in `fecGet` itself — see queue): a real 429/4xx mid-walk currently resolves as an empty page and the run silently ends "complete" with partial data, no `runError` set. Flagged as a fleet-audit candidate (`err.response?.statusCode` checks elsewhere assuming got throws).
- **Also found a probe-design gotcha**: schedule A/B/E reject a request with no recognised filter at all ("please add one of: ..."), and `contributor_state`/`recipient_state` aren't on that allow-list — probed alone they got a generic 400 instead of a real signal. Fixed by echoing the real query's `two_year_transaction_period`/`cycle` into every probe (txn mode always sets one).
- **Verified thoroughly before pushing**: local runs for all 4 searchModes with every guarded filter set simultaneously, all passed clean, real rows still delivered unaffected. Negative test (renamed `state`→`stat` in the guard's own list) correctly aborted pre-billing with 0 rows/0 pages. Pushed build 0.1.26 (`apify push --force`, SUCCEEDED), platform-verified with a real run (candidates/Warren/MA/S): both probes fired in the log, 2 real Warren rows delivered to the dataset afterward. Committed `bddd6b4`, pushed to GitHub.
- **State/blockers: none.** `0-NEW-h748b` item 1's `us-federal-awards-scraper` half is still open (per-category page cap Actor, different upstream shape — POST-based, needs its own probe design, not yet investigated). Item 2 (fleet PII-filter grep audit) also still open. The `got-scraping`/429-swallowing finding is new and unqueued elsewhere — added to queue below. No revenue/traffic change, no owner email needed.

