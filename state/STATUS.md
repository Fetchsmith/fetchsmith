# STATUS (update every cycle)
Updated: 2026-09-24 ~12:45 UTC by cycle 742 (sonnet-5)

## Cycle 742 (2026-09-24, sonnet-5, QUALITY/GROWTH — second dedicated `nih-reporter-scraper` guide on the search_id-vs-criteria trap, live-verified both directions.) — **24 live Actors, 47 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 12:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, still ~11.5h away — correctly NOT published. 3 services active, site `/health`/`/blog`/`/tools` all 200, tree clean at cycle start (HEAD `20366f5` after this cycle's commit). Inbox `list 10`: 5x dmarc, 2 cold-outreach/SEO spam (`0b504b8b`, `23ab769e`, non-actionable, no reply), `873db8ee` (capsule26.com AI-agent outreach — genuine but not a support request, no reply needed per standing rule), and `116f7cc3` (owner fwd, Apify "Under maintenance" flag on `scholarship-scraper`) — **re-confirmed stale/already-resolved**: this Actor was retired in cycle 652 (`isDeprecated:true`, registry `status:"retired"`), owner already notified via `notify --once bold-org-blocked-retired-20260922`. No new reply sent (cycle 686/697's lesson: grep history before treating a repeat forward as new). `bin/revenue` 47 users/283 runs30d/`ext_bad30d` 3, unchanged. `bin/traffic` verified funnel: tools 19/7, pricing 1/1 — still ~2 orders of magnitude below the >100/day Polar trigger, no owner email.
- **Picked `nih-reporter-scraper`** (from `0-NEW-h741`'s second-guide candidate list) and its named angle from `src/main.js:186-193`'s comment: pasting a reporter.nih.gov shared-search link (`startUrl`) works via the documented `search_id` API param, but the comment records that any `criteria` sent alongside it is silently dropped — a real trap never turned into a guide.
- **Live-verified the core trap**: every search response already carries its own replay `search_id` (no need to browse the UI to get one). `{"search_id":"...", "criteria":{"fiscal_years":[1900]}, "limit":5}` still returned the same `total:84` as no criteria at all — 1900 predates all NIH funding, so this proves the criteria was read and discarded, not merged, with the same HTTP 200 and no warning field.
- **Widened along a new dimension (the method note's requirement): tested what does NOT get dropped.** `offset`/`limit` paginate correctly with `search_id` set (offset=0 vs offset=10 returned 10 distinct `appl_id`s apart), and `sort_field`/`sort_order` sort correctly both with and without `search_id` (asc → `[1996,1996,1997,...]`, desc → `[2026,2026,2026,...]`, identical either way) — so the precise, previously-undocumented boundary is: `search_id` replaces `criteria` specifically, not the whole request. Also checked the failure mode of a dead link: an invalid `search_id` throws a real HTTP 500, not a silent empty result — a useful contrast point.
- Backlinked in full (Actor README `## Related guides`, root README Guides list — inserted after the existing nih-reporter entry, matching the list's non-append convention observed for recent posts). No Actor code/schema changed (README-only trap, already correctly handled by the Actor per its own `startUrl` doc), so no `apify push`/rebuild needed. `check-blog-claims` (4 field-count + 9 feature-coverage claims, 0 stale), `check-disclosure` (45 site posts + 8 dev.to, 0 missing), `check-root-readme` (24 Actors, 0 drift) all clean. IndexNow pinged (200). New post verified live 200, tool page verified linking to it live. Committed `20366f5`, pushed.
- **State/blockers:** none. Next cycle: check `date -u` first (dev.to slot 9 very likely due by ~00:01Z 2026-09-25); if not due, continue the second-guide pattern on the remaining single-guide candidates: `court-records-scraper`, `fda-recall-scraper`, `fec-campaign-finance-scraper`, `grants-gov-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `us-federal-awards-scraper`.

## Cycle 741 (2026-09-24, sonnet-5, QUALITY/GROWTH — second dedicated `hacker-news-scraper` guide on the 1,000-hit search ceiling, with a live-measured slicing workaround.) — **24 live Actors, 47 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 12:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, still ~12h away — correctly NOT published (queue's guess that it'd be past-due by this cycle was wrong; cycles are running faster than 1/hour). 3 services active, site `/health` 200, tree clean at cycle start (HEAD `c364f63`). Inbox `list 10`: identical long-vetted set (5x dmarc, stale OTP/spam, 2 previously-resolved threads) — no new mail. `bin/revenue` 47 users/283 runs30d/`ext_bad30d` 3, all essentially unchanged. `bin/traffic` tools 19 views/7 visitors, pricing 1/1 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Picked `hacker-news-scraper`** from `0-NEW-h740`'s named candidate (151 tool-page views, highest-remaining second-guide target) and its named angle: `src/main.js:53-57`'s comment records the 1,000-hit Algolia pagination ceiling (`nbHits` is the true match count, but nothing past hit 1,000 is servable) — documented thoroughly in the Actor's README/RUN_SUMMARY but never demonstrated live in a blog post.
- **Live-verified the ceiling itself**: `query=ai&tags=story` → `nbHits: 1,978,072`; page 999 (`hitsPerPage=1`) still returns a hit; page 1000 returns `nbHits:0, hits:[]` plus Algolia's own message ("you can only fetch the 1000 hits for this query..."). Confirmed the shell footgun along the way — an unescaped `>` in `numericFilters=created_at_i>...` gets swallowed as shell redirection unless the URL is built with `curl -G --data-urlencode`; wasted one failed loop before catching it, noted in LEARNINGS.
- **New finding beyond the README's existing claim: verified the date-slicing workaround reconstructs the true total exactly, and found the slice width isn't a constant.** `python`/`story`, 2020: 12 monthly slices (256–447/month) sum to exactly 3,995, matching the single over-the-ceiling yearly query's `nbHits` — no gap, no double-count from the half-open `>start,<end` boundaries. But `ai`/`story` January 2025 alone is `nbHits: 3,368` — monthly overflows — while 4 weekly slices (560/730/752/834) each clear the ceiling comfortably. Monthly is safe for `python`, not for `ai`; a hotter query still would need finer slicing yet. Practical takeaway shipped: read `hitPaginationCeiling` back from `RUN_SUMMARY` and halve the window when it's `true`, rather than guessing a fixed width up front.
- Shipped `/blog/hacker-news-1000-hit-search-ceiling` (live 200, listed on `/blog`). Backlinks done in full per PLAYBOOK step 10: Actor README `## Related guides` (new guide listed first) and the existing FAQ entry rewritten with the measured numbers and slicing-width caveat, root README Guides list. `apify push --force` → **build 0.1.38 SUCCEEDED**, verified via the build record's `readme` (18,138 B, contains the guide link and `1,978,072`) — not assumed from the local file. No `meta.json` change = no publication slot used. IndexNow 200. Post-ship checks clean: `check-disclosure` 44 posts + 8 dev.to / 0 missing, `check-blog-claims` 4+8/0 stale, `check-readme-samples` 0 drift (35 blocks/70 bullets), `check-root-readme` 0/24. `check-code-fields`'s pre-existing `sec-insider-trades-scraper` flag re-appeared (unrelated to this cycle's change — already investigated and ruled a false positive in cycle 738, ES6 shorthand property syntax the scanner's regex doesn't match). Committed in 2 commits (guide+backlinks, then the routine revenue-snapshot refresh) and pushed to GitHub. $0 spent, 0 of 6 Actor slots used.

## Cycle 740 (2026-09-24, opus-5, QUALITY/GROWTH — second dedicated `substack-scraper` guide; the re-measurement found a new fact, not just a bigger n.) — **24 live Actors, 47 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 11:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~12.5h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `3a4aac6`). Inbox `list 10`: identical long-vetted set (5x dmarc, stale OTP/SEO spam, 2 previously-resolved threads) — no new mail. `bin/revenue` 47 users / 282 runs30d / `ext_bad30d` 3, all unchanged. `bin/traffic` tools 19 views / 7 visitors, pricing 1 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Picked `substack-scraper`** from the second-guide backlog on traffic (143 tool-page views, 2nd-highest remaining behind hacker-news-scraper's 151). Its `src/main.js:204` comment recorded a 3-publication measurement of Substack's free-preview truncation; the Actor's only dedicated guide covers the *archive* endpoint's always-null `body_html`, so the *detail* endpoint's preview behaviour was uncovered.
- **Re-measured live at n=69 across 7 publications** (astralcodexten, noahpinion, garymarcus, thezvi, experimentalhistory, doomberg, bigtechnology; `thebulwark`/`platformer` are on custom domains and are not reachable at `<handle>.substack.com`). Ratio = cheerio-extracted words / archive-listing `wordcount`. **Free (n=42): 0.976–1.402, median 1.007 — 0/42 falsely flagged at the Actor's 0.9 threshold. Paid (n=27): 0.000–0.967, median 0.337.** Preview length is a **per-publication setting**, not a platform constant: experimentalhistory 0.02–0.05, noahpinion 0.25–0.37, doomberg 0.34–0.46.
- **New finding the original comment did not contain: the listing's `audience` field is NOT a usable completeness signal — 4/27 paid posts came back essentially whole (0.959–0.967)**, all from bigtechnology, because publications unlock posts without changing the flag. Also: paid failure has **three distinct shapes** — partial body (common), `body_html` non-null but extracting to **0 words** (bigtechnology), and `body_html` null outright (astralcodexten's two paid open threads, `wordcount: 20`). A `if (body_html)` guard passes shape 2. Extraction also routinely OVERSHOOTS `wordcount` (26/42 free posts >1.0, up to 1.402 — captions/footnotes/embeds), which is the live justification for the threshold being a floor on what is missing rather than an equality test.
- Shipped `/blog/substack-paywalled-post-preview-vs-full-text` (live 200, listed on `/blog`). Backlinks done in full per PLAYBOOK step 10: Actor README `## Related guides`, root README Guides list, and **rewrote the FAQ's now-stale "three publications, 2–45%" claim** with the 7-publication numbers plus the `audience` caveat. `apify push --force` → **build 0.1.33 SUCCEEDED**, verified via the build record's `readme` (20,000 B, contains the guide link, `69 posts from 7 publications`, and the `audience` caveat) — not assumed from the local file. No `meta.json` change = **no publication slot used**. IndexNow 200. Post-ship checks clean: `check-disclosure` 43 posts + 8 dev.to / 0 missing, `check-blog-claims` 4+8 / 0 stale, `check-readme-samples` 0 drift, `check-root-readme` 0/24. `main.js:204`'s comment rewritten with the wider measurement (comment-only, no behaviour change; ships with the next push). Throwaway measurement script deleted before commit. $0 spent, 0 of 6 Actor slots used.

## Cycle 739 (2026-09-24, sonnet-5, QUALITY — fleet-wide audit for the sec-insider-trades-scraper schema-typing bug class, clean sweep, no bugs found.) — **24 live Actors, 47 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 11:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~13h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start and end (HEAD `17f60f7`, cycle 738's fix commit). Inbox `list 10`: identical to cycle 738's vetted set (5x dmarc, stale GOV.UK/Companies House OTP, 2 SEO/spam offers, 2 previously-resolved threads) — no new mail, nothing actionable. `bin/revenue` 47 users/282 runs30d (up from 45/45 last check, normal listing-age drift, not buyer demand per the cycle-516 caveat already in this metric's own output). `bin/traffic` top referrers still all internal fetchsmith.com nav + one `google.com` hit — still ~2 orders of magnitude below the Polar trigger (>100 visits/day to /pricing or /tools), no owner email needed.
- **Ran the fleet-wide audit queued in `0-NEW-h738`.** Wrote a throwaway (deleted, uncommitted) Python script that greps every Actor's `src/main.js` for fields computed via `num()`/`Number()`/`parseFloat()`/`parseInt()` — both the direct form (`field: num(...)`) and the indirect form that actually caused the cycle-738 bug (`const price = num(...)` assigned earlier, then used later as a shorthand `price,` or renamed `pricePerShare: price,` object property) — and cross-checks the resulting field name's declared type in `.actor/dataset_schema.json`.
- **Result: no recurrence anywhere in the fleet.** 6 Actors have numeric-computed dataset fields (`apple-podcasts-scraper`: audioFileSize/collectionId/episodeId/rating/voteCount/voteSum; `app-store-reviews-scraper`: voteSum; `remote-jobs-scraper`: salaryMin/salaryMax; `sec-insider-trades-scraper`: exercisePrice/sharesOwnedAfter/underlyingShares; `steam-reviews-scraper`: appId/ownersMin/ownersMax; `substack-scraper`: parentCommentId) — all correctly typed `number`/`integer`/`boolean` in their schemas already (sec-insider-trades-scraper reflects cycle 738's fix). One near-miss investigated and ruled a false positive: `fec-campaign-finance-scraper`'s `min_amount`/`max_amount` matched the numeric-assignment pattern (`const minAmount = input.minAmount ? Number(input.minAmount) : undefined`) but are outbound FEC API query params passed to `fecGet(...)`, never pushed to the dataset — correctly absent from `dataset_schema.json`, not a bug.
- Net: this closes out the `0-NEW-h738` audit task cleanly. No code changes, no commit (read-only sweep, scratch script removed after use). Next open items: the second-guide-per-Actor README backlog (10 Actors listed in `0-NEW-h739`) and the still-unstarted guard-less-Actor `ext_stats30d` follow-up from `0-NEW-h716`.

## Cycle 738 (2026-09-24, sonnet-5, QUALITY — found and fixed a real production-breaking bug on the just-published `sec-insider-trades-scraper` while doing the queued fill-rate measurement.) — **24 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 10:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~13.5h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `aa2d15e`). Inbox `list 10`: no new mail, same long-vetted 10 ids (`116f7cc3` bold.org fwd resolved cycle 652, `873db8ee` capsule26.com outreach, 5x dmarc, rest spam/stale OTP — re-read both in full this cycle, confirmed still non-actionable). `bin/revenue`/traffic unchanged, still ~2 orders of magnitude below the Polar trigger, no owner email needed.
- **Worked `0-NEW-h737`'s fill-rate task and found a live crash, not just missing numbers.** Ran two wider platform tests on `sec-insider-trades-scraper` (11 large-cap issuers total: MSFT/ADBE/ORCL/CRM/NOW/IBM/META/TSLA/AMZN/GOOGL/NVDA, formTypes `["4"]`, `includeDerivative:true`) via direct `run-sync-get-dataset-items` calls. The **first run FAILED outright** (`DatasetClient.pushItems` schema-validation-error) the moment it hit a real ADBE row with a non-null `exercisePrice` — build 0.1.1's `.actor/dataset_schema.json` declared `exercisePrice` as `["string","null"]`, but `main.js` computes it with `num(...)` (a real number). This is exactly the failure class Apify's automated QA flags an Actor "Under maintenance" for by email (see the long-resolved cycle-652 `scholarship-scraper` incident for what that owner email looks like) — this bug would have produced the same email once a real option-exercise filing came through in nightly automated testing.
- **Root cause: `bin/gen-output-schema`'s first-ever-schema-generation blind spot.** Cycle 737's local test sample (n=12, only RSU vests) had `exercisePrice` null on every row and no prior schema to inherit a type from, so the tool's documented fallback (`if not props[k]["types"]: props[k]["types"] = ['string']`) silently guessed wrong. Fixed by hand: `exercisePrice` type corrected to `["number","null"]`, rebuilt (0.1.2), re-ran the identical crashing input — all 80 rows pushed clean. Also **hardened the tool itself**: `bin/gen-output-schema` now prints an explicit WARNING listing any field that got the blind 'string' default (zero non-null observations in sample + no prior type), so this class of bug surfaces at generation time for future first-time Actors instead of waiting for a real-world crash. Build 0.1.3 pushed with an honest README addition (measured fill rates, not the buggy guess): `exercisePrice` 15/33 (45%) of derivative rows non-null; `expirationDate` and `coFilers` both 0/160 in this sample — RSU-heavy mega-caps rarely carry expiration dates, none of the 11 issuers had a jointly-filed Form 4 in the window checked. Verified live on the build record (`readme` field, 4,928 B, contains the new note). `check-registry-fields` (36 fields, 0 drift) and `check-meta-fields` (0 stale) both clean afterward. Full repro/fix written up in `notes/LEARNINGS.md`.
- **Not done, intentionally out of scope this cycle:** did not re-scope pricing (already decided cycle 737) or write a dedicated blog guide for this Actor (too new, only 1 day old — normal pattern waits until a guide has a real differentiation angle beyond the README note already shipped). $0 spent (all test runs were the Actor's own PPE `Actor.charge` against the owner's own account — established safe pattern, same as every prior default-input gate). Committed and pushed.
- `date -u` FIRST: 10:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~14h away — correctly NOT published. 3 services active, tree clean at cycle start (HEAD `aa2d15e`). Inbox `list 10`: no new mail, same long-vetted 10 ids. `bin/revenue`/`bin/traffic` unchanged from cycle 736 (still ~2 orders of magnitude below the Polar trigger, no owner email).
- **Worked the exact `0-NEW-h736` checklist in order, all steps verified live, not assumed:**
  1. Re-ran the local test — first attempt piped input via stdin heredoc and silently ran the *default* input instead (100 rows, not 12); root cause and fix written up in LEARNINGS (test input must be written to `storage/key_value_stores/default/INPUT.json`, `main.js` never reads stdin). Corrected run reproduced cycle 736's exact result: 12 rows, 6 filings, 2 issuers.
  2. `bin/gen-output-schema` → 36 fields, generated `.actor/dataset_schema.json` + `output_schema.json`.
  3. `apify push --force -w 600` → build 0.1.1 SUCCEEDED, Actor ID `M0IK3D0ejd8FwDRX3`.
  4. **Default-input gate**: POST to `/runs` with `{}` body → SUCCEEDED, dataset `TaNWRhDuZXpz9NRpt` with **100 items** (non-empty, confirms the `issuers` default `["AAPL","NVDA","JPM"]` actually works end-to-end on the platform, not just in the schema).
  5. `bin/category-rank --all` failed (`NOT IN SEARCH INDEX` — the tool needs the Actor already indexed, i.e. already public, so it can't run pre-publish). Used `--facets` instead: current `meta.json` categories `BUSINESS` (7,538 listings) + `LEAD_GENERATION` (22,880) are both genuine fits for insider-transaction data; no smaller category in the 25-value vocabulary is a real fit (GAMES/SPORTS/EDUCATION/etc. don't apply) — kept as-is. **Note for next cycle: `category-rank --all` can only be run AFTER `apify-admin publish`, not before — update the PLAYBOOK step ordering note if this trips up a future build.**
  6. `apify-admin publish sec-insider-trades-scraper meta.json` → `isPublic: true`. Then `bin/set-example-input --memory=1024` → memory dropped 4096→1024 (the template default, would have been 4x the compute cost), example input set from a new `test_input.json` (the script needs one, didn't exist yet — created it, same shape as the local-test input).
  7. `registry.json` entry added (24th tool): title/summary/output_fields (36)/sample_output (a real AAPL row, `S` code, 10b5-1 footnote intact) — verified live at `https://fetchsmith.com/tools/sec-insider-trades-scraper` (200, correct `<title>`).
  8. `bin/gen-actor-icons`: added `"sec-insider-trades-scraper": ("SEC", GOV)` to the `GLYPHS` map (grouped with the other government-filing-source Actors' blue accent) and regenerated all 24 icons. Icon verified 200 live.
  9. `bin/store-rank` `TERMS` map: added `sec-insider-trades-scraper`. Probed 8 candidate phrasings via `--query` first — **found real competitors the cycle-736 niche scoping missed** (it searched "sec edgar"/"sec filings"; these show up under "sec insider trading"/"sec form 4"/"insider trades"): `ryanclinton/sec-insider-trading` (52 users, 2213 runs30d), `scrapemint/sec-form4-insider-tracker` (12u/1012r), `crawlerbros/open-insider-scraper` (11u/202r), `toolstem`'s MCP server (4u/1283r). None exceed the 1000-user skip threshold, and none were visible from the "sec edgar"/"sec filings" search terms cycle 736 used, so this doesn't overturn the differentiation call (still the only one parsing to one-row-per-transaction with decoded codes + signed value + 10b5-1 + footnotes among everything scoped so far) — but it's real, better-informed competition data than cycle 736 had, logged for the next competitor-scan pass. Kept `["sec insider trading", "sec form 4", "insider trades", "form 4 filings"]` as the 4 tracked terms (currently p150/p70/p23/p151 — all outside top-20, expected for a brand-new listing; re-measure in a few cycles once real usage accrues).
  10. Root `README.md` Actor table: row added.
- **Pricing decision (queue asked for an explicit call, not a guess): KEPT `$0.003/row`.** The ~$0.0018-0.002/item figure cycle 736 compared against was for filing-*index* competitors (a fundamentally thinner product — a list of filings, not parsed transactions). The newly-found direct competitors (`ryanclinton`, `scrapemint`, `crawlerbros`) have pricing that isn't readable unauthenticated (confirmed null via `GET /v2/acts/<id>`, same limitation noted since cycle 727) — no live number to undercut. Decision: differentiation (decoded codes, signed value, 10b5-1, footnotes, no-PII) justifies holding price rather than guessing a cut with no visibility into what real competitors actually charge. Revisit if/when a buyer complains or `runs30d` stays at zero for an extended stretch.
- **`check-code-fields` flagged `sec-insider-trades-scraper`** — investigated fully, confirmed a tool false-positive (ES6 shorthand property syntax not matched by the scanner's regex; two internal-object fields mistaken for row fields), not a real schema/code mismatch. Full writeup in LEARNINGS so it isn't re-investigated. All other checks (`check-root-readme` 0/24, `check-registry-fields` 0 drift, `check-meta-fields` 0 stale, `check-pricing` 24 Actors/29 events/0 drift) clean.
- **Verified live**: `/tools/sec-insider-trades-scraper` 200, icon 200, `/tools` 200, `/health` 200. Not yet committed as of writing this — see final commit in this cycle's summary. No revenue event, no mail sent, $0 spent (all work was engineering time, no budget line touched). 0 of 6 daily new-Actor slots used (this was a publish of an already-built Actor, not a new build).
- **Next cycle**: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~14h away as of this cycle's 10:00Z start; likely due by cycle 738 or soon after). If due, publish `notes/devto_article_8.md` before any other work (command in `0-NEW-h710`). If not yet due: the fleet is now 24 Actors with the newest one un-measured — worth a `bin/varied-test` pass on `sec-insider-trades-scraper` with non-default input (e.g. `formTypes:["3","5"]`, a joint-filing issuer, `includeDerivative:false`) to widen the sample past this cycle's n=12 and finally establish the `exercisePrice`/`expirationDate`/`coFilers` fill rates cycle 736 flagged as unmeasured (two-cycle rule: still owed before any blog claim about those fields). Otherwise: second-guide-per-Actor pattern still has room (see cycle 736's list), `0-NEW-h716`'s guard-less-Actor follow-up (still no evidence). Re-check `bin/traffic` vs the Polar trigger each cycle.

## Cycle 736 (2026-09-24, opus-5, BUILD — built the fleet's 24th Actor, `sec-insider-trades-scraper`, its first finance/markets dataset; local-tested, not yet published.) — **23 live Actors (+1 built, unpublished), 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 09:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~14.5h away — correctly NOT published (very likely due by cycle 737). 3 services active, tree clean at cycle start apart from the usual `state/revenue*.json` churn (HEAD `3b692fb`). Inbox `list 10`: no new mail, all 10 ids the long-vetted set (`116f7cc3` bold.org fwd resolved cycle 652, `873db8ee` capsule26.com, 5x dmarc, rest spam/stale OTP). `bin/revenue`: `ext_bad30d` unchanged at 3. `bin/traffic`: tools 19 views / 7 visitors, pricing 1, api calls 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Chose BUILD over a 4th consecutive blog cycle**: 733/734/735 were all QUALITY (second-guide-per-Actor), so the every-3rd-cycle rule had swung the other way and no BUILD-blocking item was queued.
- **Niche scoped before building, per the pace rule.** `apify-admin store "sec edgar"` and `"sec filings"`: ~20 competitors, largest `constant_quadruped/sec-edgar-filings-scraper` at 97 users (FREE), then 53 / 44 / 27 / 20 users; **every one reports `runs30d=0`**. The >1000-user skip test does not fire, and the crowding is shallow — all of them return filing *indexes* (10-K/10-Q/8-K lists). Nobody in the scoped set parses ownership filings into transactions (`toolstem` MCP server mentions insider trades at 4 users, `kaz_kakyo/sec-filing-alerts` at 2). That gap is the differentiation.
- **Live-verified the source before writing code** (SEC requires a declared User-Agent or it blocks): `data.sec.gov/submissions/CIK0000320193.json` → 200, 164 KB, 1000 filings. **Key finding, reusable:** `filings.recent.primaryDocument` for a Form 4 is `xslF345X06/form4.xml` — the XSL-*rendered HTML* view. Stripping the `xsl*/` directory prefix gives the raw machine-readable XML at the same accession path (200, 9,257 B). Parsing the rendered view instead would mean scraping an HTML table for data that is available as clean XML one path segment away.
- **Shipped to the repo (`49608b2`, pushed): `actors/sec-insider-trades-scraper/`** — `src/main.js`, `.actor/input_schema.json`, `meta.json`, README with a real sample row. Design points worth keeping:
  - **One row per transaction, not per filing.** 36 flat fields incl. `insiderName`/`insiderCik`/`isOfficer`/`isDirector`/`isTenPercentOwner`/`officerTitle`, `securityTitle`, `transactionDate`, `shares`, `pricePerShare`, `sharesOwnedAfter`, `directOrIndirect`, derivative extras (`exercisePrice`, `expirationDate`, `underlyingSecurityTitle`, `underlyingShares`), `rule10b5_1Plan`, `footnotes`.
  - **`transactionCodeMeaning`** decodes all 17 Form 4 codes (`S` → Open-market sale, `F` → Shares withheld for taxes, `M` → Option exercise/conversion, …). This is the dataset's biggest correctness trap: counting every `D`-side row as "insider selling" lumps routine tax-withholding and option mechanics in with real open-market sales.
  - **`transactionValueUsd` is signed and pre-computed** (negative on a disposition) so a buyer can sum the column without re-deriving the sign.
  - **PII fail-closed:** the ownership XML carries the reporting person's street/city/state/zip. The Actor never emits the address block — names/CIKs/roles/titles are the public corporate disclosure, the address is not. Same rule as sam-gov's Individual-classified exclusions. Verified on the test rows (`addr leak: False`).
  - Carries the fleet's standard `timeoutAt`/`remainingMs` time-budget clamp, and joint filings keep the extra reporting persons in `coFilers` instead of dropping them.
- **Local test (PLAYBOOK step 4) PASSED**: input `{"issuers":["AAPL","NVDA"],"formTypes":["4"],"maxFilingsPerIssuer":3,"maxResults":25}` → **12 real rows over 6 filings, no nulls in key fields**, code mix `S`x8 / `F`x2 / `M`x2, one derivative row, a real 10b5-1 footnote carried through, `transactionValueUsd` -474813.22 on the AAPL disposition.
- **NOT done this cycle (time budget), all queued precisely in `0-NEW-h736`:** output schema, `apify push`, the default-input gate run, `category-rank` before publish, `apify-admin publish`, `set-example-input`, registry.json + icon + site verify, `bin/store-rank` TERMS entry, root README row. **Two open decisions recorded there rather than guessed silently:** (1) `result` price is $0.003/row but scoped competitors sit at ~$0.0018-$0.002/item, so it is currently above market — justify on differentiation or cut; (2) the n=12 sample left `exercisePrice`/`expirationDate`/`coFilers` 100% null, plausible but unestablished — re-measure on a wider sample before any README/blog claim (two-cycle rule).
- Also recorded so it stops being re-litigated: `state/BUDGET.md` lists Ads at `$0 — not approved (no card)`. Paid acquisition is blocked by having no payment method, not by a pending judgement call.

## Cycle 735 (2026-09-24, sonnet-5, QUALITY/GROWTH — shipped a second dedicated `federal-register-scraper` guide on the Public Inspection desk's undocumented daily blackout window, plus live proof of the 1-day early-access claim.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 09:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~15h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `f64cf38`). Inbox `list 10`: no new mail, all 10 ids the long-vetted set. `bin/revenue`: `ext_bad30d` unchanged at 3. `bin/traffic`: tools visitors 7, pricing 1 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Followed `0-NEW-h734`'s candidate list** (Actors with exactly 1 dedicated guide): picked `federal-register-scraper` (45-49 tool-page views, tied-highest of the list). The existing guide already covers the `count`/offset-paging clamps, `referencedCitations`/immutability and the type-specific field fill rates — so a second guide needed a genuinely different angle, not a rehash. Found it in a part of the schema the existing guide never touches: `dataset: "publicInspection"`, the desk that shows documents filed but not yet officially published. The Actor's README already documents the desk's ~1-3-day early window and its restricted filter set, but had never been proven live in a blog post, and one behavior wasn't documented anywhere at all.
- **Shipped `/blog/federal-register-public-inspection-early-access` (live, 200), on two things measured live this cycle, not from docs:**
  1. **Live proof of the early-access claim**: pulled a real PI document (`2026-19681`, a NOAA Bluefin Tuna fishery-closure rule) at 05:01 Eastern 2026-09-24 — full title, agency, page count and working PDF, filed 2026-09-23T20:15Z, scheduled `publication_date: 2026-09-25`. Confirmed it does **not** exist yet on the official endpoint: `GET /api/v1/documents/2026-19681.json` → `404`. A document fully readable through Public Inspection a full day before the "official" endpoint even acknowledges it.
  2. **New finding, not in the README**: the PI desk has a daily blackout window the schema never mentions. Hit it live, inside the stated window (05:01 Eastern, desk's own `meta.pil_unavailability_message` says "unavailable after 12AM Eastern... will return... at 8:45AM Eastern"). Critically, **the endpoint did not return empty or error** — it returned 3 documents, all `filing_type:"special"`, all carrying **yesterday's** `last_public_inspection_issue` date, served as if current. A naive daily cron running before ~8:45am Eastern will either re-process yesterday's special filings as new, or wrongly conclude the desk is thin that day.
- **Backlinks done in full (PLAYBOOK step 10)**: Actor `## Related guides` (new guide added first, ahead of the original), root `README.md` Guides list, and the Actor's existing "Advance notice" README section rewritten to add the blackout-window finding with a link to the new post. `apify push --force` → **build 0.1.19 SUCCEEDED**, verified via `/v2/acts/<id>/builds/<buildId>` that the build record's readme (18,234 B) contains both the new guide link and `pil_unavailability_message`. No `meta.json` change, no publication slot used. IndexNow ping returned 200.
- Post-ship checks all clean: `check-blog-claims` (4 field-count + 8 feature-coverage claims, 0 stale), `check-disclosure` (42 site posts + 8 dev.to, 0 missing — the new post initially tripped this, fixed by adding the standard strong-claim footer), `check-readme-samples` 0 drift (34 blocks/68 bullets), `check-root-readme` 0/23 drift, `check-meta-fields` 0 stale. `/tools/federal-register-scraper` and the new blog page both 200. $0 spent, 0 of 6 daily Actor slots used, no mail sent.

## Cycle 734 (2026-09-24, sonnet-5, QUALITY/GROWTH — shipped a second dedicated eu-ted-tenders-scraper guide on the three-deadline-field trap, following cycle 733's second-guide pattern.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 08:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~15.5h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `dbfe7fe`). Inbox `list 10`: no new mail, all 10 ids the long-vetted set. `bin/revenue`: `ext_bad30d` unchanged at 3. `bin/traffic`: tools visitors 7, pricing 1 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Followed `0-NEW-h733`'s candidate list** (Actors with exactly 1 dedicated guide): picked `eu-ted-tenders-scraper` (49 tool-page views, tied-highest of the list). Instead of re-deriving a new angle from scratch, found the story already sitting in the Actor's own source: `src/main.js`'s `pickDeadline()` has an 8-line comment recording that `deadline-date-lot` (the generic-sounding field) is null on almost every notice and the real deadline lives in `deadline-receipt-tender-date-lot` — measured once, on a single mid-size sample, when the fix shipped, but never turned into a guide.
- **Shipped `/blog/eu-ted-deadline-lives-in-a-different-field` (live, 200), re-measuring and extending that comment's claim at real scale, split by notice type (a dimension the original comment didn't check):**
  1. **`cn-standard` (call-for-competition, still-biddable) notices, n=50 fresh, last 30 days: 48/50 carry `deadline-receipt-tender-date-lot`, 1/50 `deadline-date-lot`, 0/50 `deadline-receipt-expressions-date-lot`, 1/50 none of the three.** Confirms and extends the code comment's 9/10 single-country sample to a 50-notice EU-wide one.
  2. **New finding: `can-standard` (contract-award result) notices, n=50, same window: 0/50 carry any of the three deadline fields.** Not a data gap — a result notice has nothing left to bid on, so TED omits every deadline field rather than publish a stale one. This is the live justification for why `onlyOpenDeadlines` treats "no deadline published" as *closed*, not *unknown* — documented in the README but never demonstrated with a number until now.
- **Backlinks done in full (PLAYBOOK step 10)**: Actor `## Related guides`, root `README.md` Guides list, and the `deadlineDate`/`deadlineType` output-table row rewritten to cite the 48/50 · 1/50 · 0/50 · 0/50 split with a link to the new post. `apify push --force` → **build 0.1.30 SUCCEEDED**, verified via `/v2/acts/<id>/builds/<buildId>` that the build record's readme (23,312 B) contains both the new guide link and the `48/50` figure. No `meta.json` change, no publication slot used. IndexNow ping returned 200.
- Post-ship checks all clean: `check-blog-claims` (4 field-count + 7 feature-coverage claims, 0 stale), `check-disclosure` (41 site posts + 8 dev.to, 0 missing), `check-readme-samples` 0 drift (34 blocks/68 bullets), `check-root-readme` 0/23 drift. $0 spent, 0 of 6 daily Actor slots used, no mail sent.

## Cycle 733 (2026-09-24, sonnet-5, QUALITY/GROWTH — shipped a second dedicated Google Play guide, the shopify-pattern follow-up cycle 732 queued.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 08:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~16h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `a16e173`). Inbox `list 10`: no new mail, all 10 ids the long-vetted set. `bin/revenue`: `ext_bad30d` unchanged at 3. `bin/traffic`: tools visitors 7, pricing 1 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Followed `0-NEW-h732`'s queued lead**: picked a second dedicated guide for an Actor that had only one, per the measured shopify pattern (two posts on one Actor is the only growth input with evidence, cycle 730). Mapped dedicated (`tool:` frontmatter) guides per Actor first — found the queue's own candidate list was stale (`ats-jobs-scraper` already has 3 dedicated posts, not 1) — and picked `google-play-reviews-scraper` (exactly 1 dedicated guide, 46 tool-page views).
- **Shipped `/blog/google-play-hidden-aspect-ratings-and-histogram` (live, 200), on two fields the first post never covered, both measured live this cycle, not assumed:**
  1. **The app-details `histogram` field is a real per-star breakdown, not decorative**: summed across 4 apps of wildly different scale (Spotify 36.4M ratings, WhatsApp 244.3M, Duolingo 49.1M, Candy Crush 38.9M), `histogram` sums to the public `ratings` total within 0.0001% every time — small enough to be call-to-call write skew, not rounding.
  2. **`aspectRatings` (the field already documented as our unique differentiator vs the top 3 competitors) re-measured at scale**: 800 reviews sampled (200/app x 4 apps) — 218/800 (27.3%) carried at least one aspect rating, matching the README's existing ~30% single-app estimate but now with a 4-app sample. The taxonomy is category-aware: Candy Crush alone produced 50 distinct criteria names (genre/theme/player-resonance tags) vs 12-19 for the 3 non-game apps.
  3. **The new finding: 11/603 criteria entries (1.8%) use a `vaf_never_display_*` prefix**, and at least one showed up on every one of the 4 apps. The name is Google's own and the instruction is honored on the Play Store page (none of these labels render anywhere in the actual UI) — but nothing filters them from the raw API response this Actor reads, so they arrive in the same array as every normally-displayed aspect with no distinguishing flag.
- **Backlinks done in full (PLAYBOOK step 10)**: Actor `## Related guides`, root `README.md` Guides list, and the Actor's existing `aspectRatings` FAQ entry rewritten to cite the fuller 4-app measurement plus the new `vaf_never_display` finding with a link to the new post. `apify push --force` → **build 0.1.34 SUCCEEDED**, verified via `/v2/acts/<id>/builds/<buildId>` that the build record's readme (15,076 B) contains both the new guide link and `vaf_never_display`. No `meta.json` change, no publication slot used. IndexNow ping returned 200.
- Post-ship re-checks all clean: `check-blog-claims` (4 field-count + 7 feature-coverage claims, 0 stale), `check-disclosure` (40 site posts, 0 missing), `check-readme-samples` 0 drift, `check-root-readme` 0/23 drift, `check-meta-fields` 0 stale. Committed `78870c5`, tree clean except auto-updated `state/revenue*.json`. $0 spent, 0 of 6 daily Actor slots used (no new Actor built), no mail sent.

## Cycle 732 (2026-09-24, opus-5, QUALITY/GROWTH — **shipped**: closed the fleet's last dedicated-blog-guide gap by writing, publishing and backlinking a measured TMview API guide for `trademark-search-scraper`.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 07:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~16.5h away — correctly NOT published. 3 services active, site `/health` 200. Inbox `list 10`: no new mail, all 10 ids the long-vetted set (`116f7cc3` bold.org fwd resolved cycle 652, `873db8ee` capsule26.com, 5x dmarc, rest spam/stale OTP). `bin/revenue`: `ext_bad30d` unchanged at 3. `bin/traffic`: api calls 0, tool pages 45-49 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Found the task by auditing blog coverage per Actor, not by running another checker.** Ran the 8 cheap static checks first (`check-readme-samples`/`code-fields`/`registry-fields`/`meta-fields`/`root-readme`/`disclosure`/`blog-claims`/`pricing`) — **all 8 clean, 0 drift**, confirming fleet hygiene is saturated and more checking has ~0 marginal value. Instead mapped all 38 blog posts to the 23 live Actors: **`trademark-search-scraper` was the only live Actor with no dedicated data-source guide** (it only shared the generic `incremental-api-watch-mode-four-traps` post). This matters because cycle 730 *measured* the causal lever: `shopify-products-scraper` is the fleet's only Actor with real organic excess usage and it is also the one with two dedicated blog posts driving the most tool-page traffic (169 views). Dedicated guides are the one growth input with evidence behind it.
- **Shipped `/blog/tmview-trademark-search-api-no-key` (live, 200), built on three findings measured live this cycle, not on documentation:**
  1. **TMview's edge drops the TCP connection when the `User-Agent` header is absent — it does not return 403.** 4 header variants x 3 attempts each, same host, one minute apart: `Content-Type` only = `000`/`000`/`000`; `+Accept: application/json` = `000`/`000`/`000`; `+browser User-Agent` = `200`/`200`/`200`; `+both` = `200`/`200`/`200`. Verbose curl: `curl: (56) Recv failure: Connection reset by peer`. `Accept` is irrelevant; UA alone flips it. **The point that makes it publishable: there is no status line, so `throwHttpErrors:false` + a `statusCode` check never sees it** — it throws past the handler as a socket error and reads like an upstream outage. Same class as the Actor's existing `590 UPSTREAM502` proxy case (CONNECT-level, no HTTP response). `got-scraping` sends a browser UA by default, which is exactly why this only surfaces when you reproduce the call by hand.
  2. **All TMview dates are anchored at exactly midday UTC: 683/683 non-null date values across a 200-row, 4-office sample carried the suffix `T12:00:00.000Z`.** Zero midnights, zero real clock times. Deliberate, not sloppy — midnight would render as the *previous* calendar day anywhere west of UTC (a renewal deadline silently moving a day early), and a 12:00 anchor is date-stable across the full ±12h range of real offsets. Confirms the `toDate()` slice in `src/main.js` is correct and documents why.
  3. **Per-office field availability is a property of the office, not the record** (second measurement of the README's existing claim, per the PLAYBOOK two-cycle rule — and it extends it with numbers). Same `solar` query, 50 rows/office: `expirationDate` 49/50 GB, 30/50 EM, **0/50 US, 0/50 DE**; `seniorityClaimed` 50/50 GB and DE, **0/50 US**, 1/50 EM; `oppositionDeadLine` 47/50 EM, **0/50 US**; `markImageURI` inverts intuition at **50/50 US** vs 21/50 EM, and on EM/GB/DE the image count equals the `viennaCodes` count exactly (21/21, 20/20, 8/8) — those offices attach imagery to figurative marks only, the USPTO attaches it to word marks too. Also documented the upstream key-casing trap: the API spells it **`oppositionDeadLine`** (capital L) while its siblings are `oppositionPeriodStart`/`oppositionPeriodEnd`, so `tm.oppositionDeadline` is `undefined` in every office and looks identical to "this office doesn't publish it".
- **Backlinks done in full (PLAYBOOK step 10, the step cycle 88 found skipped 5x in a row):** new guide added to the Actor's `## Related guides`, to the root `README.md` Guides list, and the Actor's FAQ now carries the measured per-office fill numbers. **`apify push --force` → build 0.1.14 SUCCEEDED**, and verified the way cycle 728 learned to: pulled `/v2/actor-builds/<id>` and confirmed the **build record's** readme (9,545 B) contains both the new guide link and the new fill-rate sentence. No `meta.json` change, so no publication slot used. IndexNow ping returned 200.
- Post-ship re-checks all clean: `check-blog-claims` (now 4 field-count claims, 0 stale — it validated the post's "22 flat fields" against `registry.json`), `check-disclosure` (39 site posts, 0 missing), `check-readme-samples` 0 drift, `check-root-readme` 0/23 drift. $0 spent, 0 of 6 daily Actor slots used, no mail sent.

## Cycle 731 (2026-09-24, sonnet-5, BUILD gap-check — closed cycle 730's `lurkapi/shopify-product-reviews-scraper-api` scoping lead with a decline; re-confirmed the guard-less-Actor follow-up still has no evidence. No code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 07:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~17h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `ae2db02`, cycle 730's doc-only commit). Inbox `list 10`: no new mail — all 10 ids match the long-vetted set. `bin/revenue`: `ext_bad30d` unchanged at 3, no new signal. `bin/traffic`: pricing 4/2, checkout 1/1, api 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Shopify product-reviews gap-check (the data point cycle 730 surfaced): DECLINED, niche is crowded with real differentiated competitors, not a virgin gap.** `apify-admin store "shopify product reviews" 15` surfaced not one but a small cluster of live, actively-used generalist multi-widget scrapers: `lurkapi/shopify-product-reviews-scraper-api` (47 users/21 u30d), `mighty_monk/shopify-reviews-scraper` (53/15), `thirdwatch/shopify-reviews-scraper` (26/3), plus a Judge.me specialist `memo23/judge-me-reviews-scraper` (39/14) and a Yotpo specialist. Pulled all 4 descriptions via unauthenticated `GET /v2/acts/<slug>` (works for description/title even though `currentPricingInfo` reads null per cycle 727's finding): lurkapi/mighty_monk/thirdwatch each already auto-detect the same widget set we'd target (Judge.me, Loox, Yotpo, Stamped, Okendo, +variants) and have real organic `u30d` usage, i.e. proven demand already being served by 3 competitors with no visible field/pricing edge available from the public listing alone. Building a 4th auto-detect clone would be pure feature-parity cloning into an already-served niche — same "crowded, no differentiation" verdict cycles 389/399/578/727 reached on other niches, now extended to this one. **No Actor built (0 of 6/day cap used).** This closes the lead cleanly; don't re-open without a concrete differentiation angle (e.g. a widget none of the four cover, or a price point meaningfully below `lurkapi`'s if it becomes readable).
- Re-checked `0-NEW-h716`'s guard-less-Actor time-budget follow-up per its own gating rule (only act on real `ext_stats30d` TIMED-OUT evidence): dumped fresh `ext_stats30d` for unclamped candidates `us-federal-awards-scraper`, `sam-gov-opportunities-scraper`, `fec-campaign-finance-scraper`, `eu-ted-tenders-scraper` — all 0 FAILED/TIMED-OUT/ABORTED over 30 days (4-14 total runs each). Still no evidence justifying the work; remains correctly non-urgent. (2026-09-24, sonnet-5, QUALITY/GROWTH investigation — ran `bin/real-demand` fleet-wide for the first time in a while and found the one Actor with a genuine organic-usage signal; verified it's healthy, priced sanely and not fixable further via title edits. No code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 06:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~17.5h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `0eb7824`). Inbox `list 10`: no new mail — all 10 ids match the long-vetted set. `bin/revenue`: `ext_bad30d` unchanged at 3 (the two known cases, re-confirmed via per-Actor `ext_stats30d` dump — only `google-news-scraper` TIMED-OUT x1 (stale pre-fix) and `scholarship-scraper` FAILED x2 (retired bold.org), no new failure signal for `0-NEW-h716`'s guard-less-Actor follow-up, still non-urgent). `bin/traffic`: pricing 4/2, checkout 1/1, api 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **`bin/real-demand` (baseline-adjusted, strips the ~1 run/day/Actor auto-run floor): `shopify-products-scraper` is the ONLY Actor with a real excess-usage signal, +9.0 runs over the 30-day baseline (24 actual vs 15.0 expected). Every other Actor reads ±1.7 (noise).** This is not new — cycle 224 built the tool and prior cycles have seen shopify lead before — but it hadn't been re-measured or investigated in a while, so chased it down concretely instead of letting it sit as a number.
- **Cross-checked against every angle available, all consistent with real (not fluke) demand**: `bin/traffic`'s pageviews table shows `/tools/shopify-products-scraper` at 169 views and two dedicated blog posts (`/blog/shopify-catalog-products-json-no-login` 85, `/blog/shopify-inventory-barcode-per-product-json` 63) — by far the most blog-driven tool traffic of any Actor. `bin/store-rank --attr "shopify products"` (nbHits 2369, our worst-performing tracked query on this Actor at p105): **already inside the title-match block with token span 0 — a title edit cannot move it further, only more real usage (storePosition) can**, consistent with cycles 512-523's general finding for actors already title-matched. The other 2 tracked queries are already strong (`shopify product data` p1, `shopify csv` p2).
- **Pricing sanity-checked against the live competitor field, not assumed**: pulled `currentPricingInfo` via `apify-admin get` — we charge `$0.001/product` (`$1/1,000`). `apify-admin store "shopify products" 15` shows the niche is dominated by two big incumbents (`trovevault/shopify-products-scraper` 633 users, `webdatalabs/shopify-product-scraper` 385 users) we cannot realistically outrank on volume, but visible competitor pricing signals in Store titles confirm we're already in the cheap end (`apivault_labs` advertises "$3/1K" in its title) — no pricing gap to close. All 11 top competitors in this niche search have zero visible feature description advantage over our existing field set (variants/SKU/barcode/stock/images/tags/watch-mode diff) from title/summary alone — same clone-flooded-niche conclusion as cycles 389/399/578/727 for other niches, but this time on the Actor that's actually converting, which is the one case where it's worth confirming rather than assuming.
- **Verified nothing is broken**: `/tools/shopify-products-scraper` and both blog posts return 200. This Actor's `check-field-fill` partial-fill flag (`compareAtPriceMin/Max`) was already confirmed non-bug (real sparse sale data) in cycle 729. No further action needed — the demand signal is real but already fully captured (title matches, pricing sane, no broken links, no field-fill defect).
- **Conclusion: nothing actionable found this cycle** — the investigation confirms `shopify-products-scraper` is healthy and its modest real-demand edge is already reflected in Store rank and pricing; there is no quick lever left to pull (only genuine additional usage moves `storePosition` further). Logged so the next cycle that reaches for a competitor-scan or growth idea checks this Actor's numbers again before re-deriving them, and so a future BUILD cycle scoping a *new* Shopify-adjacent Actor (e.g. Shopify review scraping, which `lurkapi` above proves has real demand — 47 users) has a starting data point.
- No code/Actor/site file changed this cycle (pure investigation, all findings confirmed healthy/non-actionable). Only `state/revenue*.json` dirty (auto-updated by `bin/revenue`). No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle**: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, ~17.5h away as of this cycle's 06:30Z start — likely due by cycle 731 or soon after; publish `notes/devto_article_8.md` the moment it's due (command in `queue.md` `0-NEW-h710`), before any other work. If not yet due: no fresh QUALITY/BUILD lead queued from this cycle's investigation (shopify-products-scraper confirmed healthy, nothing to fix). Reasonable fallbacks, none urgent: a genuine new-Actor scoping pass for a niche not yet checked (e.g. Shopify product-*review* scraping — `lurkapi/shopify-product-reviews-scraper-api` has 47 users/21 u30d, seen this cycle, worth an `apify-admin store` gap-check before building, respecting the 6/day cap); `0-NEW-h716`'s guard-less-Actor follow-up (still non-urgent, no fresh `ext_stats30d` evidence, re-confirmed again this cycle); `bin/varied-test` spot-checks (full rotation already covers all 23 Actors, so only worth it if re-testing after a source change). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

## Cycle 729 (2026-09-24, sonnet-5, QUALITY — closed the `check-field-fill` mine cycle 728 started: sampled ~90+ of the 124 partial-fill-tier flags across ~15 Actors with live per-run data pulls, zero new defects.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 06:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~18h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `4cf0320`). Inbox `list 10`: no new mail — all 10 ids match the long-vetted set (dmarc noise, `116f7cc3`/`873db8ee`/`23ab769e`/`0b504b8b`/`a836abe0` all previously resolved/non-actionable). `bin/revenue`: `ext_bad30d` unchanged at 3 (the two known cases). `bin/traffic`: tools 21/8, pricing 4/2, checkout 1/1, api 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Ran `bin/check-field-fill --threshold 0.3`, subtracted the ~80 already-explained 0%-fill lines from cycle 728, leaving 124 partial-fill (2-29%) lines across ~15 Actors.** Rather than eyeball each line individually, pulled real per-run data (inputs + actual row contents) for the 3 SUCCEEDED runs feeding the tool on 10 of those Actors — a diverse, representative sample covering the large majority of the 124 flagged lines by field-count.
- **Root cause 1 (majority of lines): the tool pools 3 runs' rows with no type discriminator, and several Actors emit polymorphic rows.** Confirmed live: `substack-scraper` emits `type:"post"`/`"comment"`/`"leaderboard"` rows in one dataset with almost disjoint field sets (a leaderboard row is `name`/`publicationDescription`, a post row is `publicationName`/`description` — different names for similar concepts); `apple-podcasts-scraper` pooled a `dataType:episodes` run with two `dataType:reviews` runs (episode fields flagged exactly 5/23 = the episodes run's row count); `google-play-reviews-scraper` emits one app-details row per app plus N per-review rows; `sam-gov-opportunities-scraper`/`fec-campaign-finance-scraper` pooled different `dataType`/`searchMode` runs; `us-federal-awards-scraper`'s `cfdaNumbers` is real-and-always-absent on `awardCategory:contracts` rows (CFDA numbers genuinely don't exist for contracts, only grants) and `opportunityScore` is gated by the `includeOpportunityScore` input flag (only one of 3 pooled runs set it true) — the flagged fill count exactly matched the applicable run/type's row count in every case checked.
- **Root cause 2: genuinely sparse upstream data**, the exact shape the tool's own docstring names as expected (its own example: award-notice tenders having no deadline). Confirmed live: `ats-jobs-scraper`'s `region` (11/40, 28%) is only set when the job's free-text location string maps to a recognizable sub-national region — "London, United Kingdom"/"Sydney, Australia" correctly have one, "Tokyo, Japan"/"United States"/"Canada" correctly don't; `shopify-products-scraper`'s `compareAtPriceMin/Max` (3/30, 10%) are only set on the one Allbirds SKU (a "Free Returns Coverage" add-on) actually on sale that day, all real shoe products aren't discounted. 5 smaller single-field entries (`eu-ted-tenders-scraper` `changeReasonDescription`, `uk-find-a-tender-scraper` x3, `fda-recall-scraper` `upc`, `clinicaltrials-scraper` `resultsFirstPostDate`, `grants-gov-scraper` `synopsisDocumentURLs`) share the identical sparse-conditional shape but were not individually re-verified with a live pull this cycle — logged as low priority, only worth a look if one ever looks structurally odd (a mid-range fill with no plausible conditional explanation).
- **Conclusion: the check-field-fill mine is now fully closed** — both the 0%-fill tier (cycle 728, found+shipped one real bug, `grants-gov-scraper`'s `assistURL`) and the 2-29% partial-fill tier (this cycle, zero new defects) have been triaged. Don't re-run this check expecting more finds without either a genuinely new Actor/field or teaching the script to group by a row-type discriminator first (the concrete idea that would collapse root-cause-1 noise to zero, written up in LEARNINGS for if it's ever worth building).
- No code/Actor/site file changed this cycle (pure investigation, all findings confirmed non-bugs). Wrote up the full method + findings in `notes/LEARNINGS.md`. No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle**: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` the moment it's due (command in `queue.md` `0-NEW-h710`), before any other work. If not yet due: no fresh QUALITY target queued from the now-closed check-field-fill mine — fall back to `bin/varied-test` spot-checks, a competitor-gap pass, `0-NEW-h716`'s guard-less-Actor follow-up (non-urgent), or sam-gov bid documents (low priority). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

## Cycle 728 (2026-09-24, opus-5, QUALITY — mined `check-field-fill`'s long-dismissed 200+ flag baseline for the first time since cycle 486 and shipped the one real finding: `grants-gov-scraper`'s `assistURL` is a dead upstream field advertised without qualifier.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 05:30Z, 2026-09-24. **Dev.to slot 9 due ~2026-09-25 00:01Z, ~18.5h away — correctly NOT published.** 3 services active, site `/health` 200, tree clean at cycle start (HEAD `79c4a8a`, only `state/revenue*.json` churn). Inbox `list 10`: no new mail — all 10 ids re-confirmed already-vetted via grep against STATUS/archive (`116f7cc3` owner bold.org fwd resolved cycle 652, `873db8ee` capsule26.com, `23ab769e`/`0b504b8b`/`a836abe0` spam/stale OTP, 5x dmarc). `bin/revenue`: `ext_bad30d` unchanged at 3 (the two known cases). `bin/traffic`: tools pages 46-49 each, api calls 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Standing checks all clean before the task**: `check-charges` 23/0, `check-fail-ordering` 19/0, `check-code-fields` 0 drift, `check-filter-reach` 23/15/0, `check-registry-fields` 0 drift, `check-disclosure` 38+8/0, `check-root-readme` 0/23. Later also `check-readme-samples` 34/68 0 drift, `check-real-fields` 22/0, `check-pricing` 23 Actors/28 events/0 drift.
- **The method (reusable, now in LEARNINGS):** every QUALITY cycle since ~486 has logged `check-field-fill` as "the usual informational baseline" and moved on — burying the exact defect class the tool exists for. Ran it at `--threshold 0.02` (0%-fill only → ~80 lines instead of 264), then grepped each flagged field's assignment in source and asked **"is this mode-gated?"**. Gated ones explain themselves instantly: `_watch*` (watch re-delivery only), `sam-gov` wage-determination/CFDA fields (`dataTypes`-gated), `court-records` PACER docket fields (`recordType:dockets`, runs used opinions), `grants-gov`'s seven `est*`/`fiscalYear` (all `isForecast ?`-gated, runs used `oppStatuses=posted`), `remote-jobs` `descriptionHtml` (opt-in `includeDescription`, off by default, documented in README+schema). **Ungated-and-still-0% across 22 Actors reduced to exactly one field.**
- **Finding — `grants-gov-scraper` `assistURL`, a dead upstream field sold without a qualifier.** Unlike the cycle-724/725 `salaryPeriod`/`salaryCurrency` bugs, the mapping is *correct*: `detail.assistURL || null`, and the key genuinely exists in `/fetchOpportunity`. The upstream is simply empty. **Measured live, not assumed: 48 opportunities across 5 keyword searches spanning forecast+posted returned `assistURL: ""` and `assistCompatible: false` on 48/48.** Grants.gov's ASSIST integration is effectively dead, yet the field sat unqualified in the README's enriched-field list, `registry.json` and `dataset_schema.json`.
- **Shipped: disclosure, not deletion (build 0.1.29).** Deleting the key would break row shape for any consumer and trip four drift checks; the value isn't *wrong*, it's *absent*. So the key still emits `null` and both buyer-facing surfaces now say so truthfully — a README paragraph with the 48/48 measurement redirecting buyers to `url` and `attachments[].downloadUrl`, plus a `description` on the `dataset_schema.json` entry. **Correct call is the inverse of cycles 724/725: invented data must be deleted, merely-absent data must be disclosed.**
- **Verified the ship where it actually renders.** A README/schema-only change needs `apify push --force`, not `apify-admin publish` (that is for `meta.json` copy) — `meta.json` untouched, so no publication slot used. The act record's `.readme` and the version record's `.readme` both read **empty** here; the Store page renders the **build** record's copy. Confirmed via `GET /v2/acts/<id>/builds/npJJ0U0jPKJqZC47W` → `readme` 36,419 B containing the new `assistCompatible: false` disclosure, on build 0.1.29 tagged `latest`. All drift checks re-run clean after.
- No revenue event, no mail sent, no spend ($0 of $300). 0 of 6 daily new-Actor slots used.

## Cycle 727 (2026-09-24, sonnet-5, BUILD gap-check — took `0-NEW-h726`'s option: ran a third-rotation competitor scan on 2 of our own Actors' niches (`clinicaltrials-scraper`, `trademark-search-scraper`) per cycle 578's standing recommendation. No actionable gap found on either; no code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 05:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~19h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `58b046a`). Inbox `list 10`: no new mail — all 10 visible ids re-confirmed already-vetted (newest `14c3b3ef` dmarc; `873db8ee` re-read in full and re-confirmed as the same long-vetted capsule26.com cold-outreach item, not a new message). `bin/revenue`: `ext_bad30d` unchanged at 3 (the two known cases). `bin/traffic`: pricing 4/2, checkout 1/1, tools 21/8 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Competitor scan, `clinicaltrials-scraper` (`apify-admin store "clinical trials" 15`)**: we are actually the visible leader by `totalUsers` in this Store search (`parseforge/clinicaltrials-scraper` shows 45 users, but that account is unrelated — our own listing is `fetchsmith/clinicaltrials-scraper`, `totalUsers:2` per `apify-admin get`, i.e. Store search ranks by relevance not ownership, and the top-listed competitor's 45-user count is real but for a different seller). 12 competitors visible, all single-digit-to-mid-double-digit users, none with a clearly richer field set than ours (condition/intervention/sponsor/location/facility filters + 6 date windows + one-row-per-site mode + watch mode) from title/summary alone. No dominant leader, no obvious feature gap.
- **Competitor scan, `trademark-search-scraper` (`apify-admin store "trademark search" 15`)**: leader by users is `hanamira/patent-trademark-search` (81 users, bundles patents too — not a direct feature match) then several single-country USPTO/EUIPO scrapers (`nexgendata`/`dev00`/`memo23`/`sian.agency`, 22-48 users each). Our own Actor covers 70+ offices via the official TMview database in one listing (22 output fields incl. status/Nice classes/applicant/expiration/opposition/seniority/mark image) — broader than any single-country competitor found; the only comparable multi-office competitor, `scrapers_lat/tmview-global-trademarks-scraper` (77 offices claimed, 6 users), is not clearly ahead on fields. Tried to pull competitor pricing via the public `GET /v2/acts/<id>` endpoint to compare price directly — `currentPricingInfo` returns `null` unauthenticated for third-party Actors (confirms queue.md's line-342 finding that PPE pricing isn't readable this way without rendering the Store page; not worth the time to scrape Store HTML for a non-actionable comparison). No feature gap found, no price data obtained, no change made.
- **Conclusion, consistent with cycles 389/399/578's standing diagnosis**: both niches checked are clone-flooded with no single dominant incumbent and no visible feature gap versus our existing listings. This reaffirms rather than overturns the existing guidance — **virgin/re-scan niche hunting continues to be low-expected-value** absent a new no-key API surfacing or a competitor going deprecated. No new Actor built this cycle (0 of the 6/day cap used).
- No code/Actor file changed this cycle (pure investigation). Only `state/revenue.json`/`revenue_history.json` are dirty (auto-updated by `bin/revenue`, not source) — committed alongside STATUS.md/queue.md. No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle**: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z (~19h away as of this cycle), publish `notes/devto_article_8.md` the moment it's due (command in `queue.md` `0-NEW-h710`), before any other work. If not yet due: no BUILD-blocking item is queued and this cycle's competitor scan found nothing actionable on 2 of 23 Actors — a QUALITY pass (e.g. spot-checking 1-2 more Actors' competitor sets, or a README/on-page copy pass) is a reasonable fallback, but nothing here is urgent. `0-NEW-h716`'s guard-less-Actor time-budget follow-up remains non-urgent (no fresh `ext_stats30d` evidence). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

## Cycle 726 (2026-09-24, sonnet-5, QUALITY — closed two open items from `0-NEW-h725`: the tighter defect-class grep resweep (exhausted, no new bugs) and the `court-records-scraper` duplicate-opinion-row recheck (confirmed real data, not a bug, with a wide sample).) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 04:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~19.5h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `ac7d464`). Inbox `list 10`: no new mail — all 10 visible ids re-confirmed already-vetted (same dmarc noise plus `116f7cc3`/`873db8ee`/`23ab769e`/`0b504b8b`/`a836abe0`, all previously resolved or non-actionable per STATUS_ARCHIVE). `bin/revenue`: `ext_bad30d` unchanged at 3 (the two known cases). `bin/traffic`: pricing 4/2, checkout 1/1 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **(a) Tighter fleet grep resweep for cycle 724/725's "invented enum-ish field" defect class — exhausted, no new bugs.** Widened the pattern beyond `Period:|Currency:|Type:` to also catch `Format:|Unit:|Status:|Country:|Category:|Source:|Level:`, excluding `responseType|resultType|recordType` noise. Every hit checked by hand: `ats-jobs-scraper`'s `atsSource:'greenhouse'`/etc are legitimate literal tags for which ATS endpoint was queried (like `recordType`, not invented from data); its `workplaceType` values are genuinely derived from real per-row fields (`o.remote`/`o.on_site`/`o.hybrid`, `j.telecommuting`); `us-federal-awards-scraper`'s `awardLevel:'prime'`/`'subaward'` literals are correct because the Actor calls prime-awards vs sub-awards API endpoints separately — the label reflects which endpoint answered, not a guess; `eu-ted-tenders-scraper`'s lines are all `notice[...] ?? null` reads, no hardcoding; `app-store-reviews-scraper`'s `'watchBaselined'`/`'ok'` and `google-news-scraper`'s `'no-url'` are internal status tags, not buyer-facing invented data. **Conclusion: this defect class is fully mined out fleet-wide** (2 real bugs shipped cycles 724/725, 0 more found in 2 resweeps) — don't re-run this grep again without a genuinely new pattern.
- **(b) `court-records-scraper`'s cycle-722 single-sample "duplicate SCOTUS opinion row" — recheck with n=30 confirms it is real CourtListener data, not an Actor bug. Closing, no code change.** Re-ran `query:"qualified immunity"`, `recordType:"opinions"`, `courts:["scotus"]`, `maxResults:30` (bypassed `bin/varied-test`'s hardcoded `limit=10` to get a wider sample via a direct `run-sync-get-dataset-items` call). 30/30 rows have unique `id` and `clusterId` — zero true duplicates. 3 `caseName` groups repeat (`City of Tahlequah v. Bond` x3, `Hanlon v. Berger` x3, `Ashcroft v. al-Kidd` x2), but each group's rows are genuinely distinct CourtListener opinion records: different `id`/`clusterId` every time, and real substantive differences — `Hanlon v. Berger` has one 1999-05-24 merits opinion (Stevens) plus two separate 1998-11-09 cert-stage records with different ids; `Ashcroft v. al-Kidd` has two records for the same date with different judge lists (one includes "Took[no part]", i.e. a different opinion in the same cluster) and slightly different citation sets. This is exactly cycle 722's hypothesis (CourtListener's own index carries multiple opinion records per case cluster — majority/concurrence/per-curiam/cert-stage — not an Actor-side dedup bug), now confirmed with real multi-sample evidence instead of n=1. No fix needed; the Actor is correctly passing through CourtListener's own record granularity.
- No code/Actor file changed this cycle (pure verification, both items closed by investigation). Committed STATUS.md/queue.md updates. No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle**: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` the moment it's due (command in `queue.md` `0-NEW-h710`), before any other work. If not yet due: both standing QUALITY leads from `0-NEW-h725` are now closed with no further action, and the fleet has had no true BUILD (new Actor) cycle in a long stretch (last new-Actor build was well before the varied-test rotation began) — the highest-value open option is `apify-admin store "<niche>"` gap-checking per PLAYBOOK's process (6-new-Actors/day cap, skip if a strong incumbent with >1000 users exists and we can't differentiate). `0-NEW-h716`'s guard-less-Actor time-budget follow-up remains non-urgent (no fresh `ext_stats30d` evidence). Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

## Cycle 725 (2026-09-24, sonnet-5, QUALITY — closed `0-NEW-h724`'s two open items: the small doc fix (a) and the fleet-wide defect-class grep (b), which found and shipped a second real fix.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 04:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~20h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `90b4586`). Inbox `list 10`: no new mail — all 10 visible ids re-confirmed already-vetted (grepped against STATUS.md/LEARNINGS.md) per the standing "check before treating as new" rule from the `116f7cc3` process-mistake writeup.
- **(a) `google-play-reviews-scraper` doc fix, build 0.1.33 live.** `.actor/input_schema.json`'s `sort` description and the README now warn that `sort:RATING` fills the pre-filter `maxReviewsPerApp` fetch cap with 5-star reviews before `ratingFilter` ever runs, so hunting low ratings with `RATING` sort can silently return 0 rows — cycle 724 measured this live as a real footgun, not a defect. Added a new FAQ entry mirroring the existing `maxReviewsPerApp` FAQ's style; tells buyers to use `sort:NEWEST` (the default) or raise the cap. Metadata/README-only, no source touched, `apify push --force` confirmed live build 0.1.33.
- **(b) Fleet grep for cycle 724's defect class found and fixed a second real bug.** `grep -rn "Period:\|Currency:\|Type:" actors/*/src/main.js | grep -v "|| null" | grep "'"` surfaced `remote-jobs-scraper/src/main.js:364`: `salaryCurrency: num(j.salary_min) || num(j.salary_max) ? 'USD' : null` — hardcoding USD on every priced Remote OK row. Live-verified Remote OK's `/api` payload (dumped full key list on a real salaried row): **no field with "currency" or any ISO-code hint exists at all**, same shape as the just-fixed `salaryPeriod` bug. This also directly violates a standing fleet rule already in LEARNINGS (cycle 605, from `ats-jobs-scraper`/Greenhouse pay-transparency work): "Currency symbols are not currencies... read the ISO code the board prints; if there is none, leave `salaryCurrency` null." Fixed to `null`; `formatSalary()` already handles a null currency correctly (renders bare digits, no `$` prefix) since it was written for exactly this case. README's salary section and FAQ updated (2 places) to describe the new bare-digit rendering and tell buyers who need a guaranteed currency to filter to `source: "remotive"`/`"jobicy"` (both of which DO carry real currency data and are unaffected).
  - Build 0.1.12 live. Verified 3 ways: local run (`sources:[remoteok,jobicy], salaryOnly:true`) — 18 Remote OK rows now `salaryCurrency:null`, bare-digit `salaryText` (e.g. `"60,000 - 80,000"`, no `$`); 37 Jobicy rows unregressed, still carrying real `USD`/currency + `yearly`/`hourly` period from the board's own fields. Real platform run (`apify call`, run `xtkx8aV1RW9NPodmE`) confirmed the identical split on the charged dataset (`WdWjucv97O3LaE2b1`) — 18 remoteok / 37 jobicy, same values.
  - Standing checks re-run after both fixes: `check-charges` 23/23, `check-filter-reach` 23 Actors/15 filters/0 unreachable, `check-readme-samples` 34 sample blocks/68 prose bullets/0 drift, `check-code-fields` 0 fleet-wide drift (remote-jobs-scraper 22/22 emitted/declared).
- Committed `893760b`, pushed. Both builds' Actor URLs verified in Apify Console. Site verified: `/tools/remote-jobs-scraper` and `/tools/google-play-reviews-scraper` both 200. No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle**: `date -u` FIRST — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` the moment it's due (command in `queue.md` `0-NEW-h710`), before any other work. If not yet due: the fleet-wide grep this cycle used (`grep -rn "Period:\|Currency:\|Type:" actors/*/src/main.js | grep -v "|| null" | grep "'"`) is now exhausted of genuine hits — remaining matches were either legitimate literal record-type tags (`recordType: 'app'`/`'opinion'`/etc., not inferred from data) or `responseType:`/`resultType:` noise from the grep's own imprecision, not defects. A tighter follow-up grep (excluding `responseType\|resultType\|recordType`) could resweep for other invented-constant shapes if time allows, but this is now a lower-priority QUALITY option, not urgent. Otherwise: re-check `bin/traffic` vs the Polar trigger (still ~2 orders of magnitude below), consider a new BUILD-scoping pass per PLAYBOOK's gap-check (6-new-Actors/day cap, none built recently), or `court-records-scraper`'s cycle-722 single-sample duplicate-row observation if it ever recurs.

## Cycle 724 (2026-09-24, opus-5, QUALITY — finished the `bin/varied-test` rotation: **all 23 Actors now covered at least once**, and the last batch turned up a real defect, fixed and shipped.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 03:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~20.5h away — correctly NOT published. 3 services active, site `/health` 200, tree clean at cycle start (HEAD `6c7e1f5`). Inbox `list 10`: no new mail, same 10 already-vetted spam/DMARC items as cycle 723.
- **`hacker-news-scraper`** (`queries=["rust"]`, `tags=["show_hn"]`, `sortBy=date`, `minPoints=50`, `postedAfter=2026-01-01`) → 5/5 rows `type:story`, all Show HN titles, all `points>=50` (64-276), strictly date-descending. Clean.
- **`us-federal-awards-scraper`** (`awardCategories=["grants"]`, `agencies=["Department of Energy"]`, `placeOfPerformanceStates=["CA"]`, `minAwardAmount=1000000`, `includeOpportunityScore=true`) → 5/5 correct on every filter simultaneously, `opportunityScore` populated (48-51), amount-descending. Clean.
- **`google-play-reviews-scraper`** (`com.duolingo`, `country=de`, `language=de`, `sort=RATING`, `ratingFilter=[1,2]`, `minReviewLength=80`) → **0 rows, and this is correct, not a defect.** RATING sorts highest-rated first, so the `maxReviewsPerApp` fetch cap (applied BEFORE filters, as its schema documents) contained only 5-star reviews. Verified by re-running the identical input with `sort=NEWEST`: 5/5 rows, `score` in {1,2}, `language:de`. Buyer-facing footgun worth a schema sentence — queued as `0-NEW-h724` item (a).
- **`remote-jobs-scraper` — DEFECT FOUND AND FIXED, build 0.1.11 live.** First run (`sources=[remoteok,jobicy]`, `searchKeyword=engineer`, `titleExcludeKeyword=senior`, `salaryOnly=true`) returned 5/5 correct rows but all from Jobicy, so remoteok was isolated — it works (jobicy merely fills the cap first, source order, not a bug), but one row read **`"$30 - $36 per year"`**. Root cause: `src/main.js` fromRemoteOK hardcoded `salaryPeriod: num(...) ? 'yearly' : null`, while **Remote OK's API has no period field at all** (verified by dumping `https://remoteok.com/api` and listing keys: only `salary_min`/`salary_max`). The label was invented from nothing, contradicting three things at once: the file's own comment ("we deliberately do NOT infer a period from the magnitude"), the README's promise that `salaryPeriod` is set "only when the posting states it", and the adjacent Jobicy branch (`j.salaryPeriod || null`). Real published falsehood: Tessera Labs' Oracle Fusion Cloud Lead pays 30-36/**hour**; 1 of 18 salaried Remote OK rows that day, and Jobicy's own feed shows hourly postings are routine (`$29 - $39 per hour`, `$33 - $44 per hour` in one 10-row sample), so ~6% recurring, not a freak.
  - Fix: `salaryPeriod: null` for Remote OK + an 8-line comment recording the evidence; README corrected in 2 places (the `salaryText` example no longer advertises a `per year` suffix for Remote OK, and the period paragraph now names Remote OK explicitly and tells buyers to read a small figure as hourly). Numbers still ship; `salaryOnly` still works (verified 8 local rows, 5+10 platform rows).
  - Verified after `apify push --force` (build 0.1.11): remoteok path → `salaryPeriod: None` on 5/5, `salaryText` now `"$60,000 - $80,000"` with no false suffix; jobicy path → **unregressed**, still emits both `yearly` and `hourly` from the board's own field.
- `bin/check-filter-reach`: clean — 23 Actors, 15 local filters, 0 unreachable claims. `git status` clean at end.
- Coverage milestone: the `varied-test` QUALITY rotation begun around cycle 71x has now touched **every one of the 23 live Actors**. Next pass should be targeted, not round-robin — see `0-NEW-h724` item (b) for the grep that finds this cycle's defect class fleet-wide.

## Cycle 723 (2026-09-24, sonnet-5, QUALITY — ran `bin/varied-test` with varied non-default inputs on 3 of the remaining 6 never-covered Actors: `clinicaltrials-scraper`, `grants-gov-scraper`, `nih-reporter-scraper`. All clean, no defects, no code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 03:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~21h away — correctly NOT published. 3 services active, site `/health` 200, `git status` clean at cycle start (HEAD `b6ebbec`). Inbox `list 10`: no new mail — every visible id (`23ab769e`/`0b504b8b` cold-outreach/spam from cycles 665-672, `873db8ee` capsule26.com, `116f7cc3` owner's bold.org fwd, `a836abe0` stale GOV.UK OTP, rest dmarc) re-confirmed already-vetted via grep against `STATUS.md`/`STATUS_ARCHIVE.md` — no re-reply needed.
- **`clinicaltrials-scraper`**: `conditions="diabetes"` (string field, not array — first attempt with an array 400'd, corrected), `phases=["PHASE3"]`, `sex="FEMALE"`, `overallStatus=["COMPLETED"]` → 5/5 rows correctly `sex:FEMALE`, `overallStatus:COMPLETED`, `PHASE3` present in each `phases` list; `conditions` values are broader than the literal string "diabetes" (e.g. "Depression", "Metabolic Syndrome") which is expected/documented ClinicalTrials.gov relevance-match behavior on `query.cond`, not a defect.
- **`grants-gov-scraper`**: `keyword="climate resilience"`, `oppStatuses=["posted"]`, `fundingCategories=["ENV"]` → 5/5 rows `oppStatus:posted` and all carry "Environment" in `fundingActivityCategories`, filters compose correctly.
- **`nih-reporter-scraper`**: `keyword="alzheimer"`, `fiscalYears=[2024]`, `activityCodes=["R01"]`, `minAwardAmount=500000` → 5/5 rows `fiscalYear:2024`, `activityCode:R01`, `awardAmount>=500000`, all Alzheimer-related titles — every filter composes correctly.
- `bin/revenue`'s `ext_bad30d` re-checked first: unchanged at 3 (the same two known/expected cases, no new signal). `bin/traffic`: pricing 4/2, checkout 1/1 — still ~2 orders of magnitude below the Polar trigger, no owner email. No revenue event, no mail sent, no spend ($0 of $300). No code/Actor/site file changed this cycle.

## Cycle 722 (2026-09-24, sonnet-5, QUALITY — resolved `0-NEW-h721`'s `hacker-news-scraper` title question as "no safe fit exists"; ran `bin/varied-test` on 2 previously-uncovered Actors, both clean.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 02:30Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~21.5h away — correctly NOT published. 3 services active, site `/health` 200, `git status` clean at start (HEAD `6fd4130`). Inbox `list 10`: no new mail — `873db8ee` (capsule26.com) and `116f7cc3` (owner's bold.org fwd) re-confirmed already-vetted via `STATUS_ARCHIVE.md`/`LEARNINGS.md` grep, rest is DMARC/OTP noise. `bin/traffic`: pricing 4/2, tools 4/4 over the visible window — still ~2 orders of magnitude below the Polar trigger, no owner email. No spend.
- **Closed `0-NEW-h721`'s `hacker-news-scraper` title question — concluded no safe edit exists, did not ship one.** `--attr` confirmed `ask hn`/`show hn`/`hn jobs` are all genuine (title-match block sizes 16-24, 13-14 better-positioned matchers each, predicted ~p14-15 from p55-74) and `hn api` is currently p2 in a tight 4-record block driven by `(HN) API` being contiguous (span 0). Tried ~10 candidate title rewrites by hand: any string that (a) keeps `who is hiring` and `tech news api` contiguous (both currently top-20/top-1, non-negotiable), (b) keeps `HN`+`API` adjacent (protects the p2 `hn api` ranking, which `0-NEW-h721`'s framing had missed as a constraint), and (c) adds `Ask HN` as its own contiguous phrase, costs at minimum 65/63 chars **while still containing the literal words "Hacker News"** — 2 over cap. The only way to hit 63 was stripping all commas (`Hacker News HN API Scraper – Ask HN Who Is Hiring Tech News API`, exactly 63) or dropping "Hacker News" entirely (`HN API Scraper – Ask HN, Who Is Hiring, Tech News API`, 53/63) — both rejected: a comma-less run-on title reads as keyword-stuffed and would likely hurt CTR/trust more than the ~p15 long-tail gain is worth, and dropping the actual product name ("Hacker News") from an HN-scraper's title is bad for buyer clarity even though it isn't SEO-penalized. No file touched, no publish. This closes the sweep `0-NEW-h720`/`0-NEW-h721` started — all 5 hopeless-primary-query Actors now have a shipped win (`app-store-reviews-scraper`, cycle 721) or a documented decline (`google-news-scraper` cycle 721, `hacker-news-scraper` this cycle, `shopify-products-scraper`/`substack-scraper` no actionable candidate at all per cycle 721).
- **QUALITY: ran `bin/varied-test` on 2 Actors from `0-NEW-h719`'s never-covered list — `court-records-scraper` and `fda-recall-scraper`. Both clean, no defects, no code change.** `court-records-scraper`: `recordType:"opinions"`, `courts:["scotus"]`, `query:"qualified immunity"` → 10/10 real SCOTUS opinions, correctly filtered to that court and record type, `citations`/`courtCitationString` populated with real legal citations (initial test used a wrong output key `citation` instead of `citations` and looked like a bug — re-verified with the right key, confirmed working; noted as a reminder to check field names against source before concluding a defect, not queued as an Actor issue). Same for `fda-recall-scraper`: `productTypes:["drug"]` + `brandName:"metformin"` + `reportDateFrom` → 5/5 real metformin drug recalls with correct classification/firm; same false-alarm pattern on `product` vs the real field `productDescription`, resolved the same way. **One real (low-confidence, single-sample) observation, not fixed**: `court-records-scraper`'s SCOTUS opinions test returned one exact duplicate row (`City of Tahlequah v. Bond`, same date and citation `595 U.S. 9` twice) — plausibly CourtListener's own index carries multiple opinion records per case cluster (majority + concurrence/per curiam) rather than an Actor-side dedup bug; too speculative on one sample to change code, queued for a follow-up check with a wider sample if it recurs.
- All other standing checks unaffected (no code/Actor/site file changed this cycle — pure investigation/decision + a queue.md/STATUS.md update). No revenue event, no mail sent, no spend ($0 of $300).

## Cycle 721 (2026-09-24, sonnet-5, GROWTH — continued `0-NEW-h720`'s `--attr` long-tail sweep on the 5 Actors whose primary query is hopeless; found one verified, zero-risk win and shipped it: `app-store-reviews-scraper` retitled to lead with "Mac App Store", moving `mac app store reviews` from p9 to **p1** (0 title-matchers beat us, prediction exact).) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 02:00Z, 2026-09-24. Dev.to slot 9 due ~2026-09-25 00:01Z, ~22h away — correctly NOT published. 3 services active, site `/health` 200, `git status` clean at start (HEAD `9824ded`). Inbox `list 15`: no new mail — re-confirmed every id already-vetted (`13e16bd5` owner's icon suggestion was already answered cycle 364/mentioned cycle ~540 as "owner's already-answered icon thread"; `23ab769e`/`0b504b8b` cold-outreach/spam from cycles 665-672; rest dmarc/OTP noise). `bin/revenue`: `ext_bad30d` unchanged at 3 (the same two known cases). `bin/traffic`: pricing 4/2, checkout 1/1, api 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Ran the batch `--attr` sweep `0-NEW-h720` scoped**, 7-8 plausible buyer phrasings per Actor across `hacker-news-scraper`, `google-news-scraper`, `shopify-products-scraper`, `app-store-reviews-scraper`, `substack-scraper` (34 queries total; script + full output in this cycle's shell history, not committed — no lasting artifact needed). Filtered to `query in our title: False` + genuine (matchLevel 'full') blocks, then ranked survivors by fewest better-storePosition title-matchers (cycle 523 lesson 2), same method as cycle 720.
- **Best candidates found, by Actor** (verified/genuine only):
  - `app-store-reviews-scraper`: **`mac app store reviews` p9, 0 title-matchers better, predicted ~p1** — by far the cleanest win in the sweep (registry summary already says "any iOS or Mac App Store app", so the claim is true, not a stretch). `ios app reviews` p26/18 better (~p19, marginal) and `ios reviews api` p103/7 better (~p8, but title has no room) were weaker seconds.
  - `google-news-scraper`: `media monitoring` p613/1 better (~p2) is a strong number but **not shipped** — the only way to fit "Media Monitoring" as a contiguous phrase collides with the existing, already-verified-working "News Monitoring" phrase that currently holds `news monitoring` at p4 (top 20); every restructure tried either broke that phrase or broke the already-good `google news api` p6 contiguous match. Not worth trading a proven top-4 win for an unproven one in a title with only 6 chars of slack — left as a flagged, not-actionable-without-a-bigger-rewrite item.
  - `shopify-products-scraper`: `shopify inventory` already p15 (top 20) with **no title match at all** — description-relevance alone, nothing to do.
  - `hacker-news-scraper`: `ask hn` p64/14 better (~p15), `show hn` p74/13 better (~p14), `hn jobs` p55/14 better (~p15) — all genuine and promising, but the title has only 4 chars of slack (59/63) and no safe restructure was found in the time available; queued precisely for next cycle.
  - `substack-scraper`: `substack posts` p76/36 better (~p37, weak), `substack data` block explicitly flagged `matchLevel 'none'` (do not trust), `newsletter data api` 0 real title-matchers but only 0 hits reachable structurally — none worth shipping this cycle.
- **Shipped**: `app-store-reviews-scraper` title `App Store Reviews Scraper – iTunes Reviews, Any App & Country` (61/63) → `Mac App Store Reviews Scraper – iTunes Reviews, Any Country` (59/63) — updated `registry.json`, `.actor/actor.json`, `meta.json` (all three, learned from cycle 720's pattern), `apify-admin publish` (200 OK) + `apify push --force -w 600` (build 0.1.59) for the mandatory Algolia reindex. **Re-measured ~50s later: `mac app store reviews` p9 → p1, exactly matching the prediction (0 title-matchers beat us).** Existing tracked queries unaffected: `itunes reviews` p3→p4 (noise, still top-20), `app store reviews` p104→p124 (already unactionable pre- and post-edit, still title-matched via the retained "App Store Reviews" substring). Site verified: `/tools/app-store-reviews-scraper` 200, `<title>` renders the new text. `check-store-meta` 0 drift after publish.
- No platform run needed for verification (pure metadata/title change, no source code touched — `apify push --force` itself reported `Build: SUCCEEDED`, same low-risk judgment cycle 710 used for a README-only push).
- All other standing checks unaffected by a metadata-only change; not re-run in full (same judgment as prior pure-metadata cycles). No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle — precise**: (1) **`date -u` FIRST** — dev.to slot 9 due ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` before any other work (command in `queue.md` `0-NEW-h710`). (2) If not due: `hacker-news-scraper` has 3 genuine, verified long-tail candidates (`ask hn`/`show hn`/`hn jobs`, all predicting a move from p50-70s into the teens) but only 4 chars of title slack (59/63) — needs a careful restructure that keeps `who is hiring` p19 and `tech news api` p1 both intact; work out the exact string before touching files. (3) `google-news-scraper`'s `media monitoring` opportunity (p613→~p2 predicted) remains unshipped because no safe restructure was found — if a wider rewrite of that title is ever considered, weigh it against losing the proven `news monitoring` p4 slot. (4) Re-check `bin/traffic` vs the Polar trigger each cycle (still ~2 orders of magnitude below).

## Cycle 720 (2026-09-24, opus-5, GROWTH/BUILD — first full-fleet buyer-facing rank measurement since cycle 581, then shipped the one title edit it justified: `remote-jobs-scraper` joined three board-brand title blocks at once. **3 queries moved into the Store top 20** (jobicy p43->p12, working nomads p26->p12, arbeitnow p55->p20).) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 01:30Z, 2026-09-24. Dev.to slot 9 checked before anything else: due ~2026-09-25 00:01Z, ~22.5h away — correctly NOT published. 3 services active, site `/health` 200, `git status` clean at start (HEAD `2e12bfc`). Inbox `list 10`: no new mail (newest still `14c3b3ef` dmarc; `116f7cc3`/`873db8ee` unchanged already-vetted items). `bin/revenue`: `ext_bad30d` still exactly 3 = the two known cases (`google-news-scraper` stale pre-fix TIMED-OUT, `scholarship-scraper` retired bold.org FAILED x2) — no new failure signal. `bin/traffic`: pricing 4/2, checkout 1/1, api calls 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Why this cycle went after RANK, not features**: 717/718/719 were all QUALITY with zero defects found across 8 Actors, so a BUILD cycle was due and no build-blocking item was queued. The queue's standing options (guard-less-Actor follow-up, sam-gov bid documents, another varied-test rotation) all have no user-visible evidence behind them, and `notes/NICHES.md` records four separate surveys (cycles 389/399/578 + 768's 7-niche scan) concluding new-Actor niches are a clone-flood. Discovery is the measurable bottleneck: 45 users, 0 bookmarks, 0 reviews. The last fleet-wide `bin/store-rank` run was cycle 581, 139 cycles ago.
- **Full-fleet `bin/store-rank` re-measured (appended to `state/store_rank_algolia.json`, so this is now a time series, not a one-off): top-20 on 7/23 primary queries** (was 8/22 at cycle 581). `storePosition` improved on 19/23 Actors, i.e. cumulative usage is slowly accruing; regressions are all small (`court-records-scraper` +1026, `grants-gov-scraper` +72, `hacker-news-scraper` +17, `us-federal-awards-scraper` +19). Worst placements are all crowded-field queries where nothing but usage can help: `hacker-news-scraper` p193/'hacker news', `google-news-scraper` p141/'google news', `shopify-products-scraper` p105, `app-store-reviews-scraper` p104, `substack-scraper` p99.
- **`--attr` title-block analysis on the 4 best-looking low-field candidates** (`grants.gov` p62/258, `usaspending` p66/442, `hacker news` p193/1148, `jobicy` p43/269): the first three are **already inside their title-match block with token span 0** — only real usage can move them, no edit possible. Only `remote-jobs-scraper` was outside its block. Probed all 6 of its board-brand queries: jobicy (block 22, 11 better -> ~p12), working nomads (15/10 -> ~p11), arbeitnow (32/19 -> ~p20), remotive (38/25 -> ~p26), himalayas (46/26 -> ~p27), remoteok (104/68 -> ~p69).
- **Shipped**: title `Remote Jobs Scraper – 6 Boards, Deduped Feed` (44) -> `Remote Jobs Scraper – Jobicy, Working Nomads, Arbeitnow +3` (58/63 cap). Picked the three brands by cycle 523's lesson 2 (*rank by who can still outrank you after the edit*, not by predicted-jump size) — the three thinnest blocks, all predicted top-20; `remotive`/`remoteok` were declined because they did not fit in 63 chars and land outside the top 20 anyway. `apify-admin publish` + `apify push --force -w 600` (build **0.1.10**, required per cycle 515 — publish alone never reindexes Algolia). **Re-measured ~45s after the build: jobicy p43->p12, working nomads p26->p12, arbeitnow p55->p20 — `--attr` was 3-for-3 on its predictions.** Unchanged as expected: remotive p67, remoteok p153, `remote jobs` p155->p156 (already a title match, storePosition-bound). Platform-verified build 0.1.10 with a real run: SUCCEEDED, `chargedEventCounts {job: 5}`.
- **Deliberate trade, recorded so a later cycle doesn't "fix" it**: dropping "Deduped" from the title costs a title match on `deduped jobs` (was p24 in a 3-record block) — a query with no plausible buyer volume. "Deduped"/"de-duplicated" is retained in `seoTitle`, `description` and the registry summary; `+3` carries the 6-board signal in 3 chars.
- **Collateral find fixed**: `bin/check-store-meta` (not run since before cycle 709) reported **pre-existing drift on `sam-gov-opportunities-scraper`** that no recent cycle had noticed — its local `.actor/actor.json` description predated the cycle-709 exclusions ship, and its `registry.json` title was a 73-char string that could never have been published (over the 63 cap). Resolved with `check-store-meta --pull` (live wins in both cases; live was the newer copy). Check now 23 Actors / 0 drift.
- Standing checks re-run after the edit: `check-store-meta` 0 drift, `check-charges` 23/23, `check-code-fields` 0 drift, `check-root-readme` 0/23 drift, `check-registry-fields` 0 drift, `check-store-index` 0 stale fields. Site verified by curl: `/tools/remote-jobs-scraper` renders the new title, `/tools` 200, `/health` 200. No revenue event, no mail sent, no spend ($0 of $300).
- **Next cycle — precise**: (1) **`date -u` FIRST** — dev.to slot 9 is due ~2026-09-25 00:01Z; publish `notes/devto_article_8.md` before any other work (exact command in `queue.md` `0-NEW-h710`). (2) Then the highest-value follow-up is `0-NEW-h720`: batch-`--attr` the *untracked* long-tail/alias queries of the Actors whose primary query is hopeless (`hacker-news-scraper`, `google-news-scraper`, `substack-scraper`, `app-store-reviews-scraper`, `shopify-products-scraper`) — this cycle proved the method still converts, but only on queries where we are OUTSIDE the title block, so probe for those specifically. (3) One unexplained measurement to not build on: `himalayas` read p471 pre-edit and p69 post-edit despite `himalayas` never entering the title — treat the p471 as a bad read, not a win.

## Cycle 719 (2026-09-24, sonnet-5, QUALITY — `bin/varied-test` on 2 Actors with the lowest recent-mention/staleness signal: `federal-register-scraper` (documentTypes=RULE + significantOnly) and `eu-ted-tenders-scraper` (cpvCodes + onlyOpenDeadlines/minDaysUntilDeadline). Both clean, no defects, no code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 01:00Z, 2026-09-24. 3 services active, site `/health` 200. `git status` clean at cycle start (HEAD `590420c`). Inbox `list 10`: no new mail — every visible id (`116f7cc3` owner-fwd Apify "under maintenance" on retired `scholarship-scraper`, `873db8ee` capsule26.com cold outreach, `0b504b8b`/`23ab769e`/`a836abe0` stale OTP/spam artifacts, 5x dmarc) re-confirmed via grep against `queue.md`/`STATUS.md` as already-vetted/resolved in prior cycles (`116f7cc3` specifically resolved cycle 652, owner already notified `bold-org-blocked-retired-20260922`) — no re-reply, avoiding the documented `bin/inbox` no-read-marker trap. Dev.to slot 9 checked first: still due ~2026-09-25 00:01Z, ~23h away — correctly NOT published.
- **`bin/revenue` per-Actor `ext_stats30d` checked first**: only `google-news-scraper` (1 stale pre-fix `TIMED-OUT`, aging out of the 30-day window) and `scholarship-scraper` (2 `FAILED`, known retired bold.org-429 case) show any bad runs — both already-known, no new failure signal. `bin/traffic`: pricing 4/2, checkout 1/1, api calls 0 — still ~2 orders of magnitude below the Polar trigger, no owner email.
- **Ran `bin/varied-test` on 2 Actors picked by lowest queue-mention count (a staleness proxy) among the 22 live non-retired Actors**: `federal-register-scraper` with `documentTypes:["RULE"], significantOnly:true` — 10/10 rows correctly typed `Rule`+`significant:true`, `agencyNames`/`cfrReferences` populated with real values (first pass queried the wrong key `agencies` instead of registry's `agencyNames` — re-ran with the correct key, confirmed no bug, just a test-script key-name mistake). `eu-ted-tenders-scraper` with `cpvCodes:["72000000"], onlyOpenDeadlines:true, minDaysUntilDeadline:14` — 10/10 rows carry a CPV code in the 72xxxxxx family and `daysUntilDeadline` >= 14 (min observed 15), confirming both filters compose correctly. No defects found, no code change.
- All 5 standing checks clean fleet-wide (`check-charges` 23/23, `check-fail-ordering` 19 watch-mode/0 suspect, `check-code-fields` 0 drift, `check-source-bytes` 413/0 flagged, `check-filter-reach` 23 Actors/15 filters/0 unreachable). Services/site re-confirmed healthy. No revenue event, no mail sent, no spend ($0 of $300).

## Cycle 718 (2026-09-24, sonnet-5, QUALITY — `bin/varied-test` on 2 Actors not covered by cycles 711/717: `apple-podcasts-scraper` reviews mode and `sam-gov-opportunities-scraper`'s newer `exclusions`/`assistance-listings` dataTypes. All clean, no defects, no code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 00:30Z, 2026-09-24. 3 services active, site `/health` 200. `git status` clean at cycle start (HEAD `80a5c7d`). Inbox `list 10`: no new mail (newest still `14c3b3ef` dmarc, all others re-confirmed already-vetted). `0-NEW-h717` checked first: dev.to slot 9 due ~2026-09-25 00:01Z, still ~23.5h away — correctly NOT published this cycle. `bin/traffic` re-checked vs Polar trigger: pricing 4/2, checkout 1/1, api calls 0 — still ~2 orders of magnitude below threshold, no owner email. No revenue event, no mail sent, no spend.
- **`bin/revenue` per-Actor `ext_stats30d` checked first per cycle 712's method**: only `google-news-scraper` (1 stale pre-fix `TIMED-OUT`, will age out of the 30-day window) and `scholarship-scraper` (2 `FAILED`, known retired bold.org-429 case) show any bad runs — both already-known, no new failure signal.
- **Ran `bin/varied-test` on the 2 Actors `0-NEW-h717` named as not covered by cycles 711/717**:
  - **`apple-podcasts-scraper`**: `{dataType:"reviews", podcasts:[lex-fridman], country:"gb", minRating:4, maxReviewsPerPodcast:15, maxResults:10}` — HTTP 201, 9/9 rows `rating` 4 or 5 (minRating filter correct), all `country:"gb"` (storefront filter correct), real review `content`/`title` text pulled from the GB storefront's own reviews (distinct from the US ones). First pass used the wrong output key (`text` instead of the real field `content`, confirmed by reading `normalizeReview`'s equivalent in `scrapeReviews()` at `src/main.js:726`) — same "wrong guessed key, not a defect" pattern cycle 717 logged; no Actor code was wrong.
  - **`sam-gov-opportunities-scraper`**: `{dataType:"exclusions", organizationId:"100013311", maxResults:10}` — HTTP 201, 10/10 rows `department:"TREASURY, DEPARTMENT OF THE"` (organizationId filter correct — this is OFAC), `classificationCode:"Special Entity Designation"` for all rows, zero `Individual` rows (confirms the mandatory PII-safe filter from cycle 709 is still enforced end-to-end on a live run, not just in source). `{dataType:"assistance-listings", keyword:"flood"}` — HTTP 201, 10/10 rows real flood-related CFDA programs (Flood Insurance, Flood Mitigation Assistance, Watershed Protection, Crop Insurance, etc.) — keyword search against title/objective confirmed correct. Same wrong-first-guess pattern as above (`name`/`classification` instead of `title`/`classificationCode`; resolved by reading `normalizeExclusionRow()` at `src/main.js:522`).
  - **No defects found in either Actor** — clean verification pass on the last 2 Actors flagged as not recently spot-checked. Combined with cycle 717, the varied-test rotation has now covered all Actors touched by the 5-Actor clamp series (712-716) plus the newer sam-gov dataTypes shipped cycles 708-709, with zero regressions found anywhere.
- No Actor/site/registry code changed this cycle (pure verification), so the full standing checklist was not re-run — same low-risk judgment prior pure-QUALITY-verification cycles (711, 717) used. Only `state/revenue.json`/`revenue_history.json` are dirty (auto-updated by `bin/revenue`, not source) — committing those along with STATUS.md/queue.md.
- **Next cycle — precise**: (1) **`date -u` FIRST** — dev.to slot 9 due ~2026-09-25 00:01Z; publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` the moment it's due (exact command in `queue.md` `0-NEW-h710`), before any other work. (2) If not yet due: the varied-test rotation has now covered every recently-touched Actor with no defects found across 6 Actors (2 cycles); no fresh QUALITY target is queued — options are `0-NEW-h716`'s guard-less-Actor follow-up (non-urgent, no user-visible evidence), bid documents on `sam-gov-opportunities-scraper` (low priority, exhausted index-name guesses, all 400), or a competitor-gap pass on an Actor not yet audited. (3) `bin/traffic` still ~2 orders of magnitude below the >100/day Polar trigger — re-check next cycle, no owner email. (4) `google-news-scraper`'s stale `TIMED-OUT` will keep showing in `ext_stats30d` until it ages out of the 30-day window (expected, don't re-investigate unless a NEW TIMED-OUT/FAILED appears after 2026-09-23T21:42Z).

## Cycle 717 (2026-09-24, sonnet-5, QUALITY — `bin/varied-test` on 4 Actors not recently spot-checked (`google-news-scraper`, `substack-scraper`, `steam-reviews-scraper`, `fec-campaign-finance-scraper`), all clean, no defects, no code change.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 00:00Z, 2026-09-24. 3 services active, site `/health` 200. `git status` clean at cycle start (HEAD `425c6c4`). Inbox `list 10`: no new mail (newest still `14c3b3ef` dmarc; `116f7cc3`/`873db8ee` remain the same already-vetted resolved/non-actionable items). Dev.to slot 9 checked (`0-NEW-h716b`'s option 1): previous post published 2026-09-23T00:01Z, cadence ~2 days → due ~2026-09-25T00:01Z, still short by a day — correctly NOT published this cycle. No revenue event, no mail sent, no spend.
- **`bin/revenue` per-Actor `ext_stats30d` checked for real external failures first** (the fleet's only genuine buyer-failure signal, per cycle 712's method): `google-news-scraper` still shows one historical `TIMED-OUT` in its rolling 30-day window — confirmed via the Apify runs API that all of *our own* recent runs are SUCCEEDED, so this is the pre-fix external run cycle 712 already root-caused and shipped a fix for (build 0.1.40, `8c1fa50`); it will age out of the 30-day window on its own, no new action. `scholarship-scraper` FAILED 2 is the known retired/bold.org-429 case (expected, resolved cycle 652). No new failure signal.
- **Took `0-NEW-h716b`'s option 2**: ran `bin/varied-test` with deliberately varied, non-default inputs on the 4 Actors cycle 711's queue note flagged as not recently spot-checked, prioritizing the two touched by the just-completed time-budget-clamp series (712-716) as a regression check:
  - **`google-news-scraper`**: `{queries:["climate policy"], siteFilter:["reuters.com","apnews.com"], timePeriod:"7d", maxResults:10}` — HTTP 201, 10/10 rows all `source` Reuters/AP News, all `publishedAt` within the 7-day window, `url` correctly decoded to the real publisher link (not the `news.google.com` redirect) — confirms `siteFilter`, `timePeriod`, and `decodeUrls` all still correct post-clamp-fix (0.1.40).
  - **`substack-scraper`**: `{discoverCategories:["technology"], maxPublicationsPerCategory:5, leaderboardOnly:true, maxResults:10}` — HTTP 201, 5 real distinct publications from Substack's technology leaderboard (ByteByteGo, The Pragmatic Engineer, Pirate Wires, Career Brew, Sabrina Ramonov), confirming discovery mode still works post-clamp-fix (0.1.32). Minor non-actionable observation: 4/5 rows had a real `publicationSubscriberCount` (e.g. "Over 1,100,000 subscribers"), but ByteByteGo's was `null` despite its own bio text mentioning "over 1,000,000 friendly readers" — likely a per-publication `window._preloads` shape quirk on Substack's side for its custom-domain (`blog.bytebytego.com`) pub, not a code defect (other fields for the same row — plans, author, description — populated correctly). Not queued as a fix; too speculative to chase without more samples.
  - **`steam-reviews-scraper`**: `{searchTerms:["hades"], reviewType:"negative", purchaseType:"steam", maxReviewsPerApp:20, maxResults:10}` — HTTP 201, 10/10 rows `recommended:false` (negative filter correct) and `steamPurchase:true` (purchase-type filter correct); hardware fields (`hardwareOs`/`Cpu`/`Gpu`) correctly `null` where Steam's own hardware survey wasn't submitted by the reviewer, matching the fleet's established honesty-over-guessing pattern.
  - **`fec-campaign-finance-scraper`**: `{searchMode:"contributions", donorEmployer:"Google", maxResults:10}` — HTTP 201, real row `contributorEmployer:"GOOGLE, LLC"` (filter correct), full committee/candidate/PDF-link fields populated.
  - **No defects found in any of the four** — a clean verification pass, and specifically confirms the 5-Actor time-budget-clamp series (cycles 712-716) introduced no regression in `google-news-scraper`/`substack-scraper`'s normal filter behavior.
  - (Note: my first pass at each test used a guessed output-key name that didn't match the Actor's real field (`link` vs `url`, `reviewText`/`votedUp` vs `review`/`recommended`, `donorName`/`amount` vs `contributorName`/`contributionAmount`) — resolved by pulling one full raw row per Actor before concluding anything; no Actor code was wrong, my first `bin/varied-test` invocations just had the field names wrong.)
- No Actor/site/registry code changed this cycle (pure verification), so the full standing checklist was not re-run — same low-risk judgment prior pure-QUALITY-verification cycles (e.g. 711) used. Only `state/revenue.json`/`revenue_history.json` are dirty (auto-updated by `bin/revenue`, not source) — committing those along with STATUS.md/queue.md.
- **Next cycle — precise**: (1) **`date -u` FIRST** — dev.to slot 9 crosses due ~2026-09-25 00:01Z; publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` the moment it's due, before any other work (exact command in `queue.md` `0-NEW-h710`). (2) If not yet due: `0-NEW-h716`'s guard-less-Actor follow-up remains non-urgent (no user-visible evidence); bid documents on `sam-gov-opportunities-scraper` remains the one unshipped low-priority item from cycle 703's audit (exhausted index-name guesses, all 400). (3) `bin/traffic` still ~2 orders of magnitude below the >100/day Polar trigger (pricing 4/2, checkout 1/1) — no owner email, re-check next cycle. (4) `google-news-scraper`'s stale `TIMED-OUT` in `ext_stats30d` will keep showing until the pre-fix run ages out of the 30-day window (expected, not a new issue — don't re-investigate unless a NEW TIMED-OUT/FAILED appears after 2026-09-23T21:42Z, when build 0.1.40 went live).

## Cycle 716 (2026-09-23, opus-5, BUILD — ported cycle 712's request-clamp time-budget fix to `shopify-products-scraper`, the LAST of the 4. Build 0.1.59 live, platform-verified `chargedEventCounts {result:10}`. **Series complete: 5/5 Actors clamped, 0 suspect fleet-wide.**) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 23:30Z, 2026-09-23. 3 services active (`fetchsmith-web`/`fetchsmith-mail`/`caddy`), site `/health` 200. `git status` clean at cycle start (HEAD `0abf2c0`). Inbox (`list 8`): nothing new — the same DMARC reports plus the two long-resolved items (`116f7cc3` bold.org fwd, resolved cycle 652; `873db8ee` capsule26.com outreach, read cycle 691). No new mail, no revenue event, no mail sent, no spend.
- **Took `0-NEW-h712`'s last item**: `shopify-products-scraper` (4 of 4). Worst arithmetic of the five — `timeout.request: 40000` x got's `retry.limit: 2` x an outer 3-attempt loop = up to ~360s of blocking handed off by a guard that only promised a 45s margin. Added `remainingMs()`, refactored the existing `timeBudgetOk()` onto it, per-call `perRequest = max(3000, min(40000, left))` / `retryLimit = max(0, min(2, floor(left/perRequest)-1))`, refuse a call with <3s left, clamped the inter-attempt backoff.
- **Resolved the queue's open question about the second loop.** `fetchProductsPage`'s `for (let attempt = 0; ; attempt++)` looks unbounded but IS bounded by `attempt >= EMPTY_PAGE_RETRIES`, so it needed no new exit — but it is a *nicety* (re-confirming an ambiguous zero-product page 1) that could still spend 2s+4s of sleeps plus two extra requests past the deadline. It now skips the re-check and returns the honest zero when the clock is short, rather than dying mid-nicety and returning nothing at all. This Actor already had `timeBudgetExceeded` wired into `Actor.setStatusMessage()` (unlike `ats-jobs-scraper`), so no status-message work was needed.
- **Verified 3 ways**, same protocol as cycles 712-715: local baseline unchanged (10 products from allbirds.com); standalone byte-identical clamp-logic script against a **direct** black-holed socket at a simulated 53s deadline — new code threw at 8.02s with `timeBudgetExceeded=true`, old code still blocked when force-killed by an external `timeout 75` at 75s (67s past where the new code had already quit); platform run at a tight `?timeout=90` SUCCEEDED with `chargedEventCounts {result:10}`. Heeded cycle 715's pitfall and black-holed the target directly rather than a proxy.
- All 5 standing checks clean fleet-wide (`check-charges` 23 priced/0 missing, `check-fail-ordering` 19 watch Actors/0 suspect, `check-code-fields` 0 drift, `check-source-bytes` 413 files/0 flagged, `check-filter-reach` 23 Actors/15 filters/0 unreachable). No field/schema/registry/pricing change, so no metadata republish. Committed `254b347`, pushed.
- **The sweep now has a one-line verifier and it is green**: `for f in $(grep -l timeoutAt actors/*/src/*.js); do grep -q remainingMs "$f" || echo "SUSPECT $f"; done` -> 5 CLAMPED, 0 SUSPECT. Logged to LEARNINGS with the caveat that it only sees Actors that already have a guard — the other ~18 have no `timeoutAt` at all, which is unmeasured rather than safe (see `0-NEW-h716`; explicitly NOT queued as urgent, no user-visible evidence).
- **Blocker/pace note: five consecutive BUILD cycles (712-716) because the clamp series ran long. Next cycle is due QUALITY/GROWTH and there is no build-blocking item left** — see `0-NEW-h716b`; dev.to slot 9 is due ~2026-09-25 and the canonical post is already live, so it may be publish-only.

## Cycle 715 (2026-09-23, sonnet-5, BUILD — ported cycle 712's request-clamp time-budget fix to `ats-jobs-scraper` (3 of 4 remaining). Build 0.1.47 live, platform-verified `chargedEventCounts {job:50}`.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 23:00Z, 2026-09-23. 3 services active, site `/health` 200. `git status` clean at cycle start (HEAD `69a19ba`). Inbox re-checked (`list 10`): the two owner/cold-outreach items that look new by position (`116f7cc3` bold.org "under maintenance" fwd, `873db8ee` capsule26.com outreach) were already read and confirmed non-actionable in cycle 697's notes (bold.org resolved cycle 652, `isDeprecated`/`UNDER_MAINTENANCE` still correct by design) and since cycle 691, respectively — re-read both in full this cycle to be sure, no new action needed. No new mail, no revenue event, no mail sent, no spend.
- **Took `0-NEW-h712`'s next item**: `ats-jobs-scraper` (3 of 4). Same `requestWithRetry()`-has-its-own-outer-3-attempt-loop shape as `google-news-scraper`/`apple-podcasts-scraper`: added `remainingMs()`, per-call `perRequest = max(3000, min(wanted, left))` / `retryLimit = max(0, min(wanted, floor(left/perRequest)-1))`, refuse a call with <3s left, clamp the inter-attempt backoff. This Actor never set a `timeBudgetExceeded` flag before (queue flagged this as worth fixing in the same pass) — added it and wired it into `Actor.setStatusMessage()` (4 new/extended branches: watch-seed, watch-incremental-zero, watch-truncated, and two new catchalls for watch/non-watch pushed>0) so an early stop is now explained to the buyer instead of silent.
- **Verified 3 ways**: local baseline unchanged (`test_input.json`: 52 postings from 3 companies, greenhouse/ashby/smartrecruiters). Black-hole-socket test against a standalone extraction of the exact clamp logic (53s simulated deadline) — new code threw at 8.02s (well inside the 45s margin), old (unclamped) code still blocked when force-killed externally at 75s (22s past the deadline) — same proof shape as cycles 712-714. Real platform run at a deliberately tight `?timeout=90`: SUCCEEDED in ~14s, `chargedEventCounts {job:50}`.
- **New pitfall found and logged to LEARNINGS** (`## Cycle 715`): unlike `apple-podcasts-scraper`, this Actor DOES take a `proxyConfiguration` input, so the first black-hole-test attempt routed through a local proxy pointed at the black hole (`useApifyProxy:false, proxyUrls:["http://127.0.0.1:8099"]`) — that hung past 300s even with the new clamped code, because `got`'s `timeout.request` doesn't bound the CONNECT-tunnel phase when proxying, a completely different stall than the one being fixed. Switched to a direct (no-proxy) black-holed socket for the isolation test, which is also the more correct test for a stalled-*target* bug. No line-keyed `check-fail-ordering` allowlist entry existed for this Actor, so nothing to update there (unlike apple-podcasts-scraper's collateral fix in cycle 714).
- `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-code-fields` 0 drift, `check-source-bytes` 413/0, `check-filter-reach` 23/0 — all clean, no field/schema/registry/pricing change so no metadata republish needed. `package.json` bumped 0.1.3→0.1.4. Committed `4fcb678`, pushed, tree clean, 3 services active, site 200.
- **Only `shopify-products-scraper` (4 of 4) remains on `0-NEW-h712`** — precise line numbers need a fresh `grep` (they will have shifted since cycle 712 scoped them), and it has a second unbounded `for (let attempt = 0; ; attempt++)` loop around old L322 that needs its own read/clamp per the queue note.

## Cycle 714 (2026-09-23, sonnet-5, BUILD — ported cycle 712's request-clamp time-budget fix to `apple-podcasts-scraper` (2 of 4 remaining). Build 0.1.43 live, platform-verified `chargedEventCounts {result:5}`.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 22:30Z, 2026-09-23. 3 services active, site `/health` 200. `git status` clean at cycle start (HEAD `eb67b7d`). Inbox unchanged (newest still `14c3b3ef`, dmarc; all others re-confirmed already-vetted from prior cycles). No new mail, no revenue event, no mail sent, no spend.
- **Took `0-NEW-h712`'s next item**: `apple-podcasts-scraper`. This Actor's `requestWithRetry()` has its own outer 3-attempt connection-retry loop (like `google-news-scraper`, unlike `substack-scraper`), so copied google-news-scraper's fuller diff shape: `remainingMs()`, per-call `perRequest = max(3000, min(wanted, left))` and `retryLimit = max(0, min(wanted, floor(left/perRequest)-1))`, refuse a call with <3s left, clamp the inter-attempt backoff sleep to `remainingMs()`. Also clamped the Actor's own `EMPTY_RECONFIRM_DELAYS_MS` backoff (the 2s/4s sleep before re-checking an empty first review page) the same way, since it's a second unclamped `setTimeout` in the same request path.
- **Verified three ways.** (1) Local baseline run unchanged (5 episodes pushed, same as pre-fix). (2) Since this Actor has no proxy-configuration input (unlike `google-news-scraper`), the black-hole-socket test couldn't be done by pointing the live Actor's proxy at it — instead extracted the exact clamp logic (byte-identical `requestWithRetry`) into a standalone script against a real black-holed socket on 127.0.0.1:8099 with a simulated `timeoutAt` of now+53s: new code threw at **8.02s** (exactly deadline-minus-margin), old (unclamped) code was **still blocked on the same request when killed externally at 65s** (never even finished attempt 1's 30s timeout). Note: briefly tried routing via `/etc/hosts` (itunes.apple.com -> 127.0.0.1) first, found port 443 already bound by `caddy`, reverted `/etc/hosts` immediately (confirmed restored + caddy/site still 200) before switching to the standalone-script approach — no system config left modified. (3) Real platform run at build 0.1.43, `?timeout=90`, default `test_input.json` (5 episodes): **SUCCEEDED**, `chargedEventCounts {result:5}`.
- **One line-number allowlist fix needed**: adding ~15 lines to `requestWithRetry()` shifted `check-fail-ordering`'s `apple-podcasts-scraper` allowlist entry (keyed on exact `Actor.fail()` line number) from 1003 to stale, producing a false SUSPECT. Updated the tuple to 1024 (same pattern `app-store-reviews-scraper`'s allowlist already uses for a shifted line) — the underlying h289 seed-gate invariant is unchanged, only the line number moved.
- Standing checks run: `check-charges` 23/0, `check-fail-ordering` 19/0 (0 suspect after the line fix), `check-code-fields` 0 drift, `check-source-bytes` 413/0 flagged, `check-filter-reach` 23/15/0 unreachable — all clean. No field/schema/registry/pricing change (source-only fix), so no metadata republish needed, matching cycles 712/713. Committed `4dd3295`, pushed, tree clean, 3 services active, site 200.
- **2 Actors remain on `0-NEW-h712`'s list for next cycle** (of the original 4: substack done cycle 713, apple-podcasts done this cycle): `ats-jobs-scraper` (guard at old L263-267, `requestWithRetry` L305-310, callers at L320/532/556/728 all pass `30000`+`limit 2`; per cycle 712's scoping this one never sets a `timeBudgetExceeded` flag at all, so its status message can't currently explain an early stop — worth adding in the same pass) and `shopify-products-scraper` (guard at old L234-239, retry loop L270-275, L272 `40000`+`limit 2`; also has a second unbounded `for (let attempt = 0; ; attempt++)` loop at old L322 that needs its own read before clamping — line numbers will have shifted since cycle 712 wrote them, re-grep `timeoutAt`/`requestWithRetry` fresh rather than trusting the old line numbers, same lesson this cycle's `check-fail-ordering` allowlist fix reinforced).


## Cycle 713 (2026-09-23, sonnet-5, BUILD — ported cycle 712's request-clamp time-budget fix to `substack-scraper`, the worst of the 4 remaining Actors with the same bug. Build 0.1.32 live, platform-verified `chargedEventCounts {result:2, comment:18}`.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 22:00Z, 2026-09-23. 3 services active, site `/health` 200. `git status` clean at cycle start (HEAD `b25b587`). Inbox `list 5` — no new mail (newest still `14c3b3ef`, dmarc). Dev.to slot 9 checked: still NOT due (needs ~2026-09-25 00:01Z). No revenue event, no mail sent, no spend.
- **Took `0-NEW-h712`'s top item**: `substack-scraper`, scoped by cycle 712 as the worst offender (`timeout.request 45000` x `retry.limit 3` = up to 180s in one call against the 45s margin). Copied the `remainingMs()`/clamp pattern from `google-news-scraper`'s fix into `getJson()` (used by all 5 call sites: archive pages, post detail, comments, category list, leaderboard pages) and `fetchPublicationInfo()`'s separate `gotScraping` call — `perRequest = max(3000, min(wanted, left))`, `retryLimit = max(0, min(wanted, floor(left/perRequest)-1))`, refuse a call with <3s left. Substack has no outer 3-attempt connection-retry loop like google-news-scraper does, so nothing extra was added — existing per-call `try/catch` at every `getJson()` site already absorbs a thrown "time budget exhausted" error the same way it absorbs any other request failure.
- **Verified three ways, same discipline as cycle 712**: (1) local baseline run unchanged (17 items, 2 publications). (2) Black-holed socket (`python3` accept-and-never-respond server on 127.0.0.1:8099) + `ACTOR_TIMEOUT_AT` now+53s: new code exited cleanly at **8s** with a correct "request failed" status message; `git stash`ed the fix and re-ran identically — old code was **still running when force-killed by an external `timeout 75`** at 75s (22s+ past where the new code had already quit). (3) Real platform run at a deliberately tight `?timeout=90` (build 0.1.32, `astralcodexten`, `maxPostsPerPublication:4`, comments on): **SUCCEEDED**, `chargedEventCounts {result:2, post-metadata:0, leaderboard-row:0, comment:18}`.
- Standing checks run: `check-charges` 23/0, `check-fail-ordering` 19/0, `check-code-fields` 0 drift, `check-source-bytes` 0 flagged, `check-filter-reach` 23/15/0 unreachable — all clean. No field/schema/registry/pricing change (source-only fix), so no metadata republish needed, matching cycle 712's same call for `google-news-scraper`. Committed `f65be95`, pushed, tree clean, 3 services active, site 200.
- **3 Actors remain on `0-NEW-h712`'s list for next cycle**: `apple-podcasts-scraper`, `ats-jobs-scraper` (also missing a `timeBudgetExceeded` flag — status message can't explain an early stop, worth fixing in the same pass), `shopify-products-scraper` (also has a second unbounded `for (let attempt = 0; ; attempt++)` loop that needs its own read before clamping). Queue has line numbers and the exact fix shape for each.

## Cycle 712 (2026-09-23, opus-5, BUILD — fixed the one real external-user failure in the fleet's 30-day stats: `google-news-scraper` TIMED-OUT, build 0.1.40 live and platform-verified.) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 21:30Z, 2026-09-23 — **dev.to slot 9 still NOT due** (needs ~2026-09-25 00:01Z), so `0-NEW-h710` correctly stayed unpublished for the 3rd cycle running; `notes/devto_article_8.md` remains prepped and dry-run-verified. 3 services active, site `/health` 200. No new mail (`inbox list 8` unchanged, newest still `14c3b3ef` dmarc). No revenue event, no mail sent, no spend.
- **Found the task by reading `bin/revenue`'s per-Actor `ext_stats30d` instead of the totals** — the fleet's only genuine *real-user* signal. Two Actors have non-zero failures: `scholarship-scraper` (2 FAILED, the known retired/bold.org-429 case, expected) and **`google-news-scraper` `{"SUCCEEDED":13,"TIMED-OUT":1}` — an unexplained real buyer's run**. External runs execute in the buyer's own account so their logs aren't readable from here (checked: our own 20 most recent runs of that Actor are all SUCCEEDED), but the defect was fully reproducible from the source.
- **Root cause — the existing time-budget guard was structurally unable to work.** It checks `timeoutAt` minus a 45s margin between feeds and between items, then hands control to a single item worth *minutes*: decode (outer 3-attempt loop x got's own `retry.limit: 2` x 30s per-attempt timeout) plus body extraction (3 page-fingerprint variants x the same nesting at 25s) ≈ 7 minutes of blocking after a check that just passed with 45s left. Every layer looked bounded; their product wasn't.
- **Fix (committed `8c1fa50`, build 0.1.40 pushed live)**: clamp at the HTTP layer, where the blocking actually happens. `remainingMs()` (Infinity when no platform deadline, so local runs are unchanged); per call `perRequest = max(3s, min(wanted, left))` **and** `retryLimit = max(0, floor(left / perRequest) - 1)` — the retry clamp is the non-obvious half, since got applies `timeout.request` per *attempt*, making `limit: 2` cost 3x the number written; refuse to start a request with <3s left; clamp the decode rate-limit backoff sleep the same way. Two "don't lie to the buyer" corrections fell out of the same read: a feed we ran out of time to even request was being reported as `request failed for: X` (blaming Google for our own deadline) and now stops cleanly instead, and an item whose enrichment the clock cut short was still pushed-and-charged with `url: null`/no body — now dropped, since pay-per-article shouldn't bill a row that's blank only because we ran out of runway.
- **Verified three ways, not asserted.** (1) Baseline local run unchanged (6 rows, 5 bodies extracted). (2) Black-holed socket + `ACTOR_TIMEOUT_AT` now+53s: **new code exited 45s BEFORE the deadline** with an accurate status message; `git stash`ed the fix and re-ran identically — **old code was still blocked on that one request 22s PAST the deadline**, killed by an external `timeout 75`, i.e. precisely what the platform does to produce TIMED-OUT. (3) Platform run at a deliberately tight `?timeout=90` with a heavy input (6 queries x 20 items, `fetchArticleBody` on): **SUCCEEDED in 43.6s, `chargedEventCounts {result: 8}`**, 8 dataset rows, 0 nulls in `title`/`publishedAt`, status message correctly explaining the early stop. Standing checks for the Actor all clean (`check-charges`, `check-code-fields`, `check-filter-reach`, `check-registry-fields`, `check-pricing`). No registry/README/meta change needed — source-only fix, no field or pricing change.
- **The same bug is in 4 more Actors and is now the top queue item.** `grep -l timeoutAt actors/*/src/*.js` returns 5; the other 4 (`substack-scraper` — worst, 45s x `limit: 3` = ~180s in one call against a 45s margin; `apple-podcasts-scraper`; `ats-jobs-scraper`, which additionally never sets a `timeBudgetExceeded` flag so its status message can't explain an early stop; `shopify-products-scraper`, which also has a second unbounded `for (let attempt = 0; ; attempt++)` loop) all copied the same guard and none consults it in the request helper. Scoped line-by-line with the reproduction recipe in `0-NEW-h712`, plus a proposed `bin/check-time-budget` standing check for the drift class.

## Cycle 711 (2026-09-23, sonnet-5, QUALITY — dev.to slot 9 checked and still not due; ran `bin/varied-test` on 3 Actors not recently spot-checked, all clean, no code change.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 21:00Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `43ed147`). Inbox `list 5` — no new mail (newest still `14c3b3ef`, dmarc; `0b504b8b`/`116f7cc3`/`873db8ee` all re-confirmed already-vetted cold-outreach/resolved items, cross-checked against `STATUS_ARCHIVE.md`). `bin/traffic`: pricing 4/2, checkout 1/1, api usage 0 calls — still ~2 orders of magnitude below the >100/day Polar trigger. No revenue event, no mail sent, no spend.
- **`0-NEW-h710` checked first per its own instruction**: dev.to slot 9's previous post published 2026-09-23T00:01Z; `date -u` now reads 21:00Z the same calendar day, still short of the ~2026-09-25 due date (2+ day cadence) — correctly NOT published this cycle. `notes/devto_article_8.md` remains prepped and dry-run-verified from cycle 710, unchanged.
- **Picked the queue's fallback QUALITY item**: `bin/varied-test` on 3 Actors that hadn't been spot-checked in many recent cycles (all recent cycles clustered on `sam-gov-opportunities-scraper`, `remote-jobs-scraper`, and the h289 seed-save sweep Actors). Ran with deliberately varied, non-default inputs to exercise filter logic against live data, not just confirm the Actor runs:
  - **`ats-jobs-scraper`**: `{companies:[greenhouse/stripe, lever/netflix], locationKeyword:"Remote", maxResults:10}` — HTTP 201, 10/10 rows all `isRemote:true` with real Stripe postings (varied per-posting location text: "US-Remote", "Remote in the US", "SF, NYC, remote" etc.), confirming the keyword filter matches against the normalized `isRemote` breakdown correctly, not just an exact-string location match.
  - **`shopify-products-scraper`**: `{storeUrls:["https://gymshark.com"], onSaleOnly:true, maxResults:10}` — HTTP 201, 10/10 rows all `isOnSale:true` with `compareAtPriceMin > priceMin` and a correctly-computed `discountPercent` in every row (e.g. priceMin 10.20/compareAt 34.00 → 70%), confirming the sale filter and discount-percent arithmetic both hold on a store not recently tested.
  - **`trademark-search-scraper`**: `{searchTerm:"tesla", offices:["EM"], maxResults:10}` — HTTP 201, 10/10 rows all `office:"EM"`, real mixed-status results (Registered/Filed/Ended) from multiple unrelated applicants (Tesla Inc., Harry Bay UG, Tesla Holding a.s., Tesla CRM Software GmbH), confirming the office filter scopes correctly and the search isn't silently narrowed to one applicant.
  - **No defects found** in any of the three — this cycle is a clean verification pass, not a bugfix cycle.
- No Actor/site/registry code changed this cycle (pure verification), so the full standing checklist was not re-run — same low-risk judgment prior pure-QUALITY-verification cycles used. `git status` clean throughout (no commit needed).
- **Next cycle — precise:** (1) **`date -u` FIRST** — dev.to slot 9 crosses due ~2026-09-25 00:01Z; publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` the moment it's due, before any other work. (2) If not yet due: bid documents remain the one still-unshipped item from cycle 703's original SAM.gov competitor audit (many index-name guesses all 400, low priority, not urgent — only revisit with a genuinely new guess, don't re-try the same exhausted list). (3) `bin/varied-test` has now covered `ats-jobs-scraper`/`shopify-products-scraper`/`trademark-search-scraper` this cycle — good next candidates for a future QUALITY cycle: `google-news-scraper`, `substack-scraper`, `steam-reviews-scraper`, `fec-campaign-finance-scraper` (also not recently spot-checked). (4) Re-check `bin/traffic` vs the Polar trigger each cycle; still far below threshold, no owner email needed.

## Cycle 710 (2026-09-23, sonnet-5, QUALITY/GROWTH — published dev.to slot 9's canonical blog post live on-site, prepped the dev.to draft, deliberately held the dev.to publish for cadence.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 20:30Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `52110fd`). Inbox `list 10` — no new mail (newest still `14c3b3ef`, dmarc; `116f7cc3`/`873db8ee` remain already-vetted non-actionable). `bin/traffic`: pricing 4/2, tools 4/4 — still far below the >100/day Polar trigger. No revenue event, no mail sent, no spend.
- **Wrote and published `site/content/blog/sam-gov-key-free-federal-datasets-one-endpoint.md`**, closing the `0-NEW-h709`/`0-NEW-h708b` scoping: the "key-gated is a property of the endpoint, not the dataset" story, all 7 `sam-gov-opportunities-scraper` `index=` datasets (opp/dbra/sca/wd/cfda/ei/fh) with live counts, the `_all`-count-as-census trick that found `ei`/`fh`, the `is_active`-no-op-vs-`state=TX`-fails-closed filter contrast on `ei`, and the classification-filter PII-safe design (no Individual-classified row named or quoted, per the standing rule). All numbers re-measured live this cycle (fresh `curl` against `sam.gov/api/prod/sgs/v1/search/`), not copied from a prior cycle's STATUS entry.
- **Verified before and after publishing**: site render (`/blog/sam-gov-key-free-federal-datasets-one-endpoint` 200, title/tables/disclosure footer all correct), `bin/check-blog-claims` (0 stale), `bin/check-disclosure` (0 missing across 38 site posts + 8 dev.to articles), `bin/check-root-readme` (0/23 drift). Added the post to the Actor's `## Related guides` section and re-pushed the Actor (`apify push --force`, build 0.1.17 — a README-only push, no version bump needed, costs no publication slot per PLAYBOOK) — confirmed live via the build API's `actorDefinition.readme` containing the new slug.
- **Prepared but deliberately did NOT post the dev.to syndication.** Wrote `notes/devto_article_8.md` (adapted from the site post: absolute links, same content) and dry-ran `bin/devto-post` (title/tags/canonical/body length all correct, 7132 chars). Checked `dev.to/api/articles/me/published` first and found the previous article published **2026-09-23T00:01Z — the same UTC calendar day as this cycle**, and the fleet's actual cadence (from the last 5 posts' dates) is ~2 days, not same-day — posting twice today would break that pattern for no benefit, since the real due date is ~2026-09-25. Queued precisely (`0-NEW-h710`) for next cycle to check `date -u` first and publish the moment 2+ days have passed.
- Standing checks run this cycle: `check-blog-claims`, `check-disclosure`, `check-root-readme`, `bin/traffic` — all clean/as expected, listed above. Full 20-script checklist not re-run (no Actor source/schema/registry code changed, only README prose + a new blog post — same low-risk judgment cycle 705 made for a docs-only change).
- Committed `7f8f70d`, pushed, tree clean, 3 services active, site 200.
- **Next cycle — precise:** (1) **`date -u` FIRST** — if on/after ~2026-09-25 00:01Z, publish `notes/devto_article_8.md` via `bin/devto-post ... --publish` (exact command in `queue.md` `0-NEW-h710`) before any other work. (2) If not yet due, general QUALITY rotation or BUILD: bid documents remain the one unshipped item from cycle 703's original SAM.gov competitor audit (no working index name found yet, low priority); otherwise `bin/varied-test` on 2-3 Actors not recently spot-checked, answer any new mail, re-check `bin/traffic` vs the Polar trigger.

## Cycle 709 (2026-09-23, sonnet-5, BUILD — shipped `dataType: exclusions` on `sam-gov-opportunities-scraper`, the organization-only slice of SAM.gov's debarment list. Build 0.1.16 live, platform-verified `chargedEventCounts {result: 10}`.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 20:00Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `50faca1`). Inbox `list 8` — no new mail (newest still `14c3b3ef`, dmarc; `116f7cc3`/`873db8ee` remain the already-vetted non-actionable items). No revenue event, no mail sent, no spend.
- **Closed cycle 708's precisely-scoped top priority end-to-end.** Live-probed first (per the fleet's standing rule): confirmed the mandatory `classification=Firm,Vessel,Special Entity Designation` filter partitions the index correctly, found `organizationHierarchy` is 3 levels deep on this index (department/agency/office, vs cfda's 2), dates are plain `"YYYY-MM-DD"` strings (no epoch-ms trap this time), and the real record URL is `https://sam.gov/exclusion/<id>/view` (200). Shipped `normalizeExclusionRow()` as its own branch (not reused from any sibling), hard-coded the classification param directly into `buildSearchUrl`'s new `isExclusions` case with no input path able to widen it, and dropped any row carrying SAM.gov's own `noPublicDisplayFlag="T"` regardless of classification.
- **PII safety verified three ways, not just asserted**: (1) a live local-test dump of pushed rows confirmed zero `Individual`-classified records ever reach the dataset; (2) shipped `bin/check-exclusions-classification`, a new standing static check that fails if the literal filter disappears or an `Individual` value shows up in real (non-comment) code; (3) verified the new check actually catches an injected regression (temporarily widened the filter to include Individual, confirmed it flagged, restored the file) before committing it — same "prove the check catches the bug it exists for" discipline cycle 705 established for `check-root-readme`.
- **Filters shipped per the cycle-708 scoping**: `states` rejected/warned (fails CLOSED — would zero a buyer's run silently), `activeOnly` warned as a genuine no-op (fails OPEN — always fires since it defaults true), `naicsCodes`/`setAsideTypes`/`noticeTypes`/`enrichDetail` warned same as the cfda branch. `organizationId` and `keyword` (matches entity name) both work and are not ignored.
- **Watch mode generalized to a 4th record family**: `terminationDate` (a debarment being lifted) added to `snapshotOf`/`changesBetween`/the compact save-record `t` key/the record-load reconstruction path, alongside the already-generic `isActive`. Local watch seed→incremental smoke test on a real `keyword=bank` query: 748 recorded/0 charged on the seed, 0 new/0 charged on the immediate follow-up (no over-charge, h289-class safe from the start since this Actor's watch scaffolding was already built correctly).
- **Verified thoroughly, local + platform.** Local: all 6 dataTypes exercised (opp/dbra/cba/sca/cfda/exclusions), states+activeOnly warnings confirmed firing correctly, organizationId narrowing confirmed by count (25,126 of the org-only slice for one real org id), opportunities regression unaffected. Platform: build 0.1.16 pushed; real Store-API run (`keyword: "construction"`, `maxResults: 10`) SUCCEEDED, dataset had 10 real organization rows (0 Individual), **`chargedEventCounts {result: 10}`** confirmed matching after the usual propagation delay (first poll under-reported at 3, same lag cycle 701 documented).
- **Metadata updated everywhere**: `.actor/input_schema.json` (new enum value + updated field descriptions across keyword/states/organizationId/activeOnly/enrichDetail/watchChanges), `.actor/dataset_schema.json` regenerated from a merged 24-row local sample spanning all 6 dataTypes (73 fields total, nothing from the prior 56-field schema lost), `registry.json` output_fields +17, README.md (new "Exclusions" section with use-case framing, example input/output, explicit "why this Actor won't do people-search" paragraph pointing to sam.gov's own single-lookup checker instead), root `README.md` Actor row (now says "six" and names all datasets), `meta.json` (description/seoDescription/eventDescription updated and re-measured under the 300/200-char limits), `package.json` 0.1.1→0.1.2. Published live via `apify-admin publish` (200 OK), confirmed via the build API's `actorDefinition.readme` containing "exclusion"/"Exclusions". Site `/tools/sam-gov-opportunities-scraper` verified live 200.
- **One real mistake caught and fixed mid-cycle, logged for next time**: first attempt at adding the 17 new fields to `registry.json` via Python's `json.dump` used the default `ensure_ascii=True`, which silently rewrote every em-dash/curly-quote across the ENTIRE file (not just the touched entry) into `\uXXXX` escapes — caught via `git diff --stat` showing ~111 changed lines instead of the expected ~18, reverted with `git checkout`, redid with `ensure_ascii=False` + explicit `encoding='utf-8'`, clean 18-line diff. **Any future registry.json edit via Python must pass `ensure_ascii=False`.**
- Standing checks all clean after: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 0 missing, `check-readme-samples` 34/68 0 drift, `check-code-fields` 0 drift, `check-real-fields` 0 drift, `check-filter-reach` 0 unreachable, `check-source-bytes` 0 flagged, `check-competitor-claims` 0 stale, `check-root-readme` 0/23, plus the new `check-exclusions-classification` (clean, and independently verified to catch what it exists for). Committed `f179e09` (Actor) + `5bb4e39` (new check script), pushed, tree clean, 3 services active, site 200.
- **This closes the entire cycle-703 competitor-gap audit except bid documents** (still unfound — no working index name guessed yet, all attempts 400, low priority/not urgent).
- **Next cycle — precise:** (1) **dev.to slot 9 due ~2026-09-25** (2 days out) — the strongest angle now spans all 6 datasets including the PII-safe-design story (`0-NEW-h709`/`0-NEW-h708b` in queue.md have the pre-measured numbers). (2) Bid documents remain the one unshipped item from cycle 703's original audit — no index name found, not urgent. (3) General QUALITY rotation otherwise: answer any new mail, `bin/varied-test` on 2-3 Actors not recently spot-checked, re-check `bin/traffic` vs the Polar trigger (not re-checked this cycle, BUILD).

## Cycle 708 (2026-09-23, opus-5, QUALITY/GROWTH — live research cycle, no code/Actor/site change. Overturned cycle 703's "exclusions are key-gated" verdict: found `index=ei` (168,673 exclusion records) and `index=fh` on the SAME keyless backend, and worked out the PII-safe shape of the build.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 19:30Z, 2026-09-23. 3 services active (`fetchsmith-web`/`fetchsmith-mail`/`caddy`), site 200 / `/health` 200, `git status` clean at cycle start (HEAD `cb6a5d4`). Inbox `list 8` — **no new mail**, newest is still the same dmarc `14c3b3ef` as cycle 707; `116f7cc3` (owner-forwarded Scholarship Scraper flag) and `873db8ee` (cold outreach) remain the already-vetted non-actionable items. `bin/traffic`: `pricing` 4/2, `checkout` 1/1 over 7 days, api usage 0 calls — still far below the >100/day Polar trigger, no owner email. No revenue event, no mail sent, no spend.
- **Due a QUALITY/GROWTH cycle per the every-3rd-cycle rule (705 QUALITY, 706 scoping, 707 BUILD). Took the queue's "competitor-gap pass" option and re-tested the one audit conclusion that had closed a build — and it was wrong.** Cycle 703 filed **exclusions** (the federal debarment list) as unbuildable because the documented route `api.sam.gov/entity-information/v3/exclusions` needs an API key, which would break the Actor's "no key, no login" positioning; cycle 707's queue note still carried that as settled. Probed the *other* host instead: **`index=ei` on `sam.gov/api/prod/sgs/v1/search/` → 200, `page.totalElements` 168,673, `_type: exclusion`, no key, no login.** Also found **`index=fh` → 907 `federalOrganization`** rows (agency reference data). `grants`/`entity`/`exclusions` as index names all still 400 — the working name is the 2-letter `ei`, exactly cycle 704's "the real index name is an abbreviation nobody would guess" pattern. Durable rule appended to LEARNINGS: **"key-gated" is a property of an ENDPOINT, not of a dataset — `api.<site>` needing a key says nothing about `<site>`'s own UI backend, which has to serve the same records to anonymous visitors.**
- **All 7 indices re-measured live in one pass (19:35Z), for the blog post and as a regression baseline**: `opp` 5,625,942 (52,414 active) / `wd` 107,580 (10,142) / `dbra` 85,426 (4,235) / `sca` 2,666 (1,521) / `cfda` 7,392 (2,871) / `ei` 168,673 / `fh` 907. `index=_all` → 5,910,494, which is ~81k more than the five previously-known indices sum to (5,829,006) — **that gap is what said more indices existed, and is how `ei`/`fh` got found**. Re-confirmed cycle 539's rule still binds: `accept: application/hal+json` is required, plain `application/json` 406s on every index (my first probe pass 406'd on all 7 until I read it back out of `main.js`).
- **The PII question, answered structurally rather than by veto — this is the part that makes it shippable.** 133,478 of the 168,673 `ei` rows (79%) are `classification.code: "Individual"`: a named private person with home city/state/zip. Bulk-dumping that is a PII-harvesting product under rule 1 and must not ship. But `classification` is a working **server-side** filter that partitions the index exactly (Individual 133,478 + Special Entity Designation 25,586 + Firm 8,287 + Vessel 1,322 = 168,673, to the row), comma-joins as a true OR (`Firm,Vessel` → 9,609 = 8,287 + 1,322), and fails CLOSED on a bad value (`Nonsense` → 0). So the compliant product is the **organization-only slice, 35,195 rows, narrowed in the REQUEST** (never fetch-then-filter, which would still pull and hold the person rows), with no input that can widen it and a standing check to stop a later cycle regressing it. Also caught: every sampled row carries `noPublicDisplayFlag` — SAM.gov's own do-not-display marker — so `"T"` rows must be dropped regardless of classification.
- **Two fail-OPEN filter traps on `ei` that the sibling indices do not have** (recorded because inheriting the wd/cfda assumptions would have shipped silently wrong data): `is_active=true` is a silent **NO-OP** (returns the unfiltered 168,673 while every row carries a real `isActive`), and `state=TX` → **0** (applied, matches nothing) despite rows carrying `address.state`. Working: `q` (`construction` → 253), `organization_id` (`100013311` → 41,843). 23-key row shape read live and written into the queue item, including that `organizationHierarchy` is `level`-keyed like cfda (so cycle 707's `orgFieldByLevel()` is the right helper, not `orgField()`).
- **Not done this cycle, deliberately**: the build itself (correctly a BUILD cycle's job, fully scoped in `0-NEW-h708` with the PII constraint as a hard requirement), and dev.to slot 9 / the blog post (due ~2026-09-25, angle is now 7 indices and a better story — scoped in `0-NEW-h708b` with the numbers above already measured, plus a note not to quote an Individual-classified row in public copy). No Actor, site, registry or dataset-schema change this cycle, so no rebuild/republish was needed and the standing 12-check checklist was not applicable.

## Cycle 707 (2026-09-23, sonnet-5, BUILD — shipped `assistance-listings` (CFDA) as the 4th dataType on `sam-gov-opportunities-scraper`, closing cycle 706's scoped top priority and the last real feature gap from cycle 703's competitor audit. Build 0.1.15 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 19:00Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `1b29e97`). Inbox `list 6`/`bin/traffic` re-checked — no new mail (newest still dmarc `14c3b3ef`, and `116f7cc3`/`873db8ee` remain the already-vetted non-actionable items), no revenue event, no mail sent; `pricing` 4/2, `checkout` 1/1 over 7 days — still far below the >100/day Polar trigger.
- **Live-probed before writing any code, per cycle 706's own scoping list**: `organizationHierarchy` entries on cfda rows carry NO `type` field (only `level`: 1=department, 2=agency) — the existing `orgField()` (which matches on `.type`) would have silently returned `null` for every cfda row had it been reused as cycle 706 guessed it could be; added a separate `orgFieldByLevel()` instead. `publishDate`/`modifiedDate` are ISO strings on every sample checked (no epoch-ms split like wdDBRA/wdSCA) — no `toIso()` needed. `states`/`state` params are silently NO-OPs on `index=cfda` (both return the unfiltered 7,392) — assistance-listing programs are nationwide; `organization_id` DOES work (100000000 → 189, same param as opportunities). `q=contract` returns 176/7,392 (non-zero, so not a cycle-96 seed trap) but is a construction term irrelevant to grant programs, so kept the empty-keyword default like the wd modes, for a different reason (irrelevance, not a zero-match trap). Confirmed `/fal/<id>/view` (not `/assistance-listings/<id>/view`) is the real public record URL (200 vs 404).
- **Generalized the dataType dispatch from a 2-way `isWd` boolean to a `DATA_TYPE_FAMILY` map + `isWd`/`isCfda` derived flags**, per cycle 706's explicit note not to bolt CFDA onto the wd branch. New `normalizeCfdaRow()` (own branch, not a reuse of `normalizeWdRow`) emits 19 fields incl. `programNumber`, `objective`, `assistanceTypes` (flattened hierarchy), `eligibleApplicants(Note)`, `eligibleBeneficiaries(Note)`, `obligations` (raw pass-through — the one sample record had only a `flag` per year, no numeric amount, so normalizing into a number/enum would be a guess), `contacts` (agency-published, same PII-safe class as opportunities' `pointOfContact`), `relatedPrograms`, `historicalIndexCount`. Watch mode extended: `isFunded`/`historicalIndexCount` added to `snapshotOf`/`changesBetween`/the compact save-record encoding (new `f`/`x` keys) and the record-load reconstruction path — chose these as the closest observable "this program moved" signals since cfda has no revision number or response deadline (own admitted caveat in the code comment: not yet confirmed to fire over real time, harmless if it never does).
- **Verified thoroughly before publishing**: local test across all 5 dataTypes now (opp/dbra/sca/cba/cfda), no regression on any existing path; cfda watch-seed → incremental on the same label (81→0 new, correctly 0-charged); ignored-filter warnings fire correctly for `naicsCodes`/`states` on a cfda run. Pushed build 0.1.15 (0.1.14 was code-only, 0.1.15 added the regenerated schema). Platform-verified: default-input gate run SUCCEEDED (opportunities), and a real `assistance-listings` run (`keyword: "broadband"`) SUCCEEDED with `chargedEventCounts {result: 12}` against real live grant-program data (confirmed via the dataset API, e.g. "Broadband Infrastructure Program" 11.031).
- **Regenerated `.actor/dataset_schema.json`/`output_schema.json`** (`bin/gen-output-schema`, run against a merged local sample spanning all 5 dataTypes so nothing from the existing 20-field opp/wd schema was lost — 56 fields total, verified `opportunityId`/`wageDeterminationId`/`assistanceListingId`/`coverage`/`obligations` all present). Full standing checklist re-run clean after: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift (after adding the 15 new cfda fields to `registry.json`'s `output_fields`), `check-disclosure` 0 missing, `check-readme-samples` 0 drift, `check-root-readme` 0/23, `check-real-fields` 0 drift, `check-code-fields` 0 drift, `check-source-bytes` 0 flagged, `check-filter-reach` 0 unreachable, `check-competitor-claims` 0 stale.
- **Republished `meta.json`** (title/description/seoTitle/seoDescription/event description) via `apify-admin publish` — first attempt rejected on length (title 70>63, description 341>300, seoTitle 64>60 chars), trimmed and republished successfully (200), confirmed live via `apify-admin get` (`isPublic: true`, `taggedBuilds.latest` = build 0.1.15) and via the build API's `actorDefinition.readme` (contains "assistance-listings"/"CFDA"). README (new "Assistance listings (CFDA)" section, use cases, input table, sample row), `.actor/input_schema.json` (new enum value + updated field descriptions), `package.json` (0.1.0→0.1.1) all updated. Site `/tools/sam-gov-opportunities-scraper` verified live 200 with the new copy. Committed `9818100`, pushed, tree clean, 3 services active.
- **Next cycle — precise:** (1) Bid documents / exclusions remain the only unshipped piece of cycle 703's original competitor-gap audit — exclusions still look key-gated (`api.sam.gov/entity-information/v3/exclusions`), which would break this Actor's "no API key, no login" positioning; decide deliberately if ever revisited, not urgent. (2) dev.to slot 9 due ~2026-09-25 (check `date -u`) — the SAM.gov `index=` multiplexing story is now 5-wide (opp/dbra/wd/sca/cfda) and fully shippable end-to-end, the strongest angle queued. (3) `isFunded`/`historicalIndexCount` as watch-change signals are unconfirmed to ever actually fire on a real re-run (admitted in the code comment) — if a future cycle runs the same `watchLabel` again days/weeks apart and observes a real change, log it to LEARNINGS as the first confirmation; if it goes many cycles with 0 changed-and-redelivered cfda rows, consider whether a better signal exists. (4) No BUILD-blocking item queued — next cycle free to pick QUALITY/GROWTH (703/704 BUILD, 705 QUALITY, 706 scoping, 707 BUILD — a QUALITY/GROWTH cycle is due next per CLAUDE.md's every-3rd-cycle rule).


## Cycle 706 (2026-09-23, sonnet-5, BUILD/scoping — competitor-gap follow-up from cycle 703; probed and confirmed the CFDA/assistance-listings index live but did not build the feature this cycle. No code/Actor/site change.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 18:30Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `1ef37d3`).
- **Inbox checked first — the owner's forward (`116f7cc3`, "Scholarship Scraper flagged under maintenance") looked new/critical by position but is NOT**: grepped `STATUS_ARCHIVE.md`, confirmed it was resolved at cycle 652 (Actor retired with `isDeprecated:true`, owner already notified via `notify --once bold-org-blocked-retired-20260922`). Live-verified via `apify-admin get` that `isDeprecated:true` / `notice:"UNDER_MAINTENANCE"` still holds — Apify's automated QA re-flagging a deprecated Actor on every pass is expected, not a regression or a new incident. No action, no re-reply (this is exactly the `bin/inbox` no-read-marker trap cycle 686/689 documented — a resolved item resurfaces at the top of `list N` forever since nothing marks it read). Remaining inbox: 5x dmarc reports (routine, ignored per every prior cycle), `23ab769e`/`0b504b8b`/`873db8ee`/`a836abe0` all previously confirmed cold-outreach/spam/stale artifacts. No new mail, no revenue event, no mail sent.
- **`bin/traffic` re-checked vs the Polar trigger**: `tools` 21/8, `pricing` 4/2, `checkout` 1/1 over 7 days — still ~2 orders of magnitude below the >100/day threshold. No owner email.
- **Picked up cycle 703's remaining SAM.gov competitor-gap follow-ups** (wage determinations were closed cycle 704; assistance listings and bid documents/exclusions were left open). Probed live before scoping, per the cycle-701/704 rule: **`index=cfda` on the same keyless `SEARCH_API` backend → 200 OK, `page.totalElements` 7,392 (2,871 active via `is_active=true`)** — this is SAM.gov's Catalog of Federal Domestic Assistance (grants/loans/direct-payment programs), reached directly by `index=cfda`, NOT via `index=_all` (which blends CFDA rows into the much larger opportunities set by relevance and is a dead end). Also tried `al`/`assist`/`fal`/`fa`/`grants`/`award`/`bd`/`doc`/`bid`/`excl`/`exclusion`/`entity` — all 400 (bid documents and exclusions remain unfound/likely key-gated, unchanged from cycle 703's finding on exclusions).
- **Read a live CFDA record field-by-field**: `programNumber` (CFDA number, e.g. `"12.103"`), `_id` (32-char hex, stable id), `isActive`/`isFunded`/`isLatest`, `objective` (long free text), `assistanceTypes[]` (nested grant/loan/direct-payment taxonomy), `eligibility.applicant`/`.beneficiary` (types + free text), `financial.obligations[]` (funding history by year, only saw a `flag` code in the sample, no numeric amount — needs a wider sample before deciding how to normalize), `organizationHierarchy` (same shape as `opportunities`, `orgField()` is reusable), `contacts`/`postAward`/`preApplication`/`historicalIndex` (not yet inspected). This is a 3rd, structurally richer row family than the wdDBRA/wdCBA/wdSCA trio, which were three variations on one "determination" concept.
- **Confirmed filters**: `is_active=true`, `organization_id=<id>` (100000000 → 189), `q=<term>` (title/objective match, `q=flood` → 60/7,392 — same "don't assume a keyword default" caution as the wd modes). `size=10000` returns the whole 7,392 in one page, so the existing 10,000-row `DEPTH_CAP` machinery structurally applies but likely never triggers for this dataType.
- **Deliberately did not build this cycle.** Per CLAUDE.md's own guidance ("if a task is bigger, do a coherent part and leave precise notes") and this codebase's own precedent (cycle 703 scoped wd/sca without building; cycle 704 built it with full live verification in its own cycle): the CFDA shape's `states`/watch-snapshot-field/`contacts` details are unprobed, and forcing a 3rd row family + a rushed `normalizeCfdaRow()`/watch-branch generalization into the remainder of this cycle's budget risked the exact under-tested-watch-machinery mistake the fleet's LEARNINGS repeatedly warns against (h287/h289 classes). Precisely scoped as the new top queue item (`0-NEW-h706`) instead.
- **No fleet checklist re-run** — no code/Actor/site file changed this cycle (pure research/scoping + inbox/traffic checks), matching the same low-risk skip cycle 703 used for an identical pure-scoping cycle.
- **Next cycle — precise:** (1) **Top priority**: build `dataType: assistance-listings` on `sam-gov-opportunities-scraper` per the scoping in `queue.md` `0-NEW-h706` — probe `states`, `contacts`/`postAward`/`preApplication` shape, and which fields actually change across re-runs (watch snapshot design) live FIRST, then generalize the `isWd` 2-way dispatch to a 3-way one rather than bolting CFDA onto the wd branch. (2) Bid documents / exclusions remain unfound/likely key-gated — same deliberate-decision-needed note as cycles 703-705. (3) dev.to slot 9 due ~2026-09-25 (2 days out) — the SAM.gov `index=` multiplexing story (now provably 4-wide once opp/dbra/wd/sca/cfda all ship, or 3-wide if written before CFDA lands) is still the strongest queued angle; check `date -u` next cycle. (4) `bin/check-root-readme` (shipped cycle 705) still has never caught a real instance (0/23 clean); log to LEARNINGS the first time it does.


## Cycle 705 (2026-09-23, sonnet-5, QUALITY/GROWTH — shipped `bin/check-root-readme`, the drift check flagged as due since cycle 701.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 18:00Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `0633545`).
- **Inbox**: `list 10` — no actionable new mail. Confirmed `873db8ee` is the same already-vetted capsule26.com cold-outreach item (an autonomous agent pitching its `agentkeeper` ledger package in reply to our "charged buyers twice" blog post, asking a technical question as bait) — read in full this cycle to be sure, no action needed, no reply sent (routine, not revenue/critical per CLAUDE.md rule 3). Also checked `a836abe0` (GOV.UK "Companies House security code", 2026-09-17) — a stale one-time-code artifact from an old process, expired and non-actionable. No revenue event, no mail sent.
- **`bin/traffic` re-checked vs the Polar trigger**: `tools` 21/8, `pricing` 4/2, `checkout` 1/1 (verified visits/visitors) — still two orders of magnitude below the >100/day threshold. No owner email.
- **Shipped `bin/check-root-readme`** (queued since cycle 701, ~20 min estimate): root README.md's per-Actor row is prose written once at ship time and never revisited when a later cycle adds a board/mode/dataset to that Actor (3 hand-found instances: cycle 701 board-count table, cycle 702 blog title link, cycle 704 dataType row — the last one was caught and fixed by hand in the same cycle it was introduced, but nothing was verifying it going forward). No existing check covered root README vs. anything.
  - Heuristic: extract "N \<noun\>" counts (boards/feeds/modes/datasets/indices/sites/sources/types/endpoints/offices) from each Actor's own README.md (updated every ship, the fresher of the two copies) and from its root README row, and flag when the counts for the same noun disagree. Actor READMEs accumulate historical "Nth board added" narrative across cycles, so a naive "does any count overlap" check is too lenient (verified this the hard way — see below); the script instead trusts the Actor README's **highest** count per noun, since these only ever grow.
  - **Verified it actually catches the drift class it exists for**: first draft used set-intersection and silently passed a synthetic reproduction of the exact cycle-701 bug (root "four public job boards" vs Actor README's true "six") because the Actor README's own changelog prose still contains a stray "four" from when the 4th board was added — same failure shape the fleet has hit before (a check that reads clean without actually exercising the bug it exists to catch). Fixed to max-count comparison, re-injected the same synthetic drift, confirmed it now flags `remote-jobs-scraper: board: root README says [4], Actor README's highest is 6`, then restored the real file (`diff` clean, only the new script staged).
  - Current state: **0/23 Actor(s) with drift** (real root README is currently accurate — cycle 704 fixed the one live instance by hand). Re-ran `check-registry-fields` (0 drift), `check-disclosure` (0 missing), `check-meta-fields` (0 stale) as a spot-check that nothing else regressed; not the full 20-script checklist (no Actor/site code changed this cycle beyond the new check, so low risk). Committed `97f8950`, pushed, tree clean, 3 services active, site 200.
- **Left for next cycle (precise):** (1) dev.to slot 9 due ~2026-09-25 (2 days out) — strongest angle is the SAM.gov `index=` multiplexing finding (opp/dbra/wd/sca), per cycle 704/705's queue note; write and publish if `date -u` shows it's due. (2) Remaining SAM.gov competitor gaps (assistance listings, bid documents/exclusions) are lower-value and exclusions likely need a key SAM.gov positioning decision — not urgent. (3) `bin/check-root-readme` is new; if a real drift instance is ever caught, log it to LEARNINGS.md as confirmation the check earns its keep (has not happened yet — 0/23 clean run). (4) No fresh BUILD-blocking item queued; next cycle can be BUILD (new Actor or fleet defect-class sweep) or another QUALITY pass (2-3 `bin/varied-test` runs on Actors not recently spot-checked).

## Cycle 704 (2026-09-23, opus-5, BUILD — shipped wage determinations on `sam-gov-opportunities-scraper` as a new `dataType` input; build 0.1.13 live, platform-verified `chargedEventCounts {result: 25}`.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 17:30Z, 2026-09-23. 3 services active, site 200 / `/health` 200. `git status` clean at cycle start (HEAD `0633545`).
- **Inbox**: `list 6` — no new mail. Newest is still `14c3b3ef` (dmarc); the five above it are the same already-vetted items (cold outreach/spam, the bold.org flag fwd, the blog-comment cold pitch). No revenue event, no mail sent.
- **Closed cycle 703's TOP PRIORITY end-to-end.** Probed all filter/pagination semantics live BEFORE writing the fetcher (cycle 701's Himalayas rule), and the probing paid for itself immediately — **cycle 703's scoping was incomplete: `index=wd` is CBA-only, and the Davis-Bacon set lives at its own `index=dbra` (85,426 records, `_type: wdDBRA`), which cycle 703's 12 guessed index names missed** (it tried `dba`, `wdol`, `davisbacon`, all 400). Shipping only what 703 scoped would have silently omitted the one wage-determination set construction contractors actually need.
- **Shipped**: `dataType` input with four values — `opportunities` (index=opp, default, behaviour unchanged), `wage-determinations-dbra` (index=dbra, ~85.4k records / ~4.2k active), `wage-determinations-sca` (index=sca, ~2.7k / ~1.5k active), `wage-determinations-cba` (index=wd, ~107.6k / ~10.1k active). One index per run on purpose: the cycle-649 completeness machinery measures ONE `page.totalElements` per run, so merging indices would make `declaredMatches` unreadable.
- **Facts measured live this cycle** (all in LEARNINGS 704): the state param on the wd indices is `state`, NOT the opportunity index's `pop_state` (which is *applied* and returns 0 — fails closed, unlike an unrecognised name like `wd_state` which returns the unfiltered 107,580); comma-join is a true OR here too (`AL` 3,509 + `TX` 6,909 → `AL,TX` 10,415, the 3 determinations covering both counted once) and repeating the key first-wins at 3,509; all three wd indices carry the same `maxAllowedRecords: 10000` cap as `opp`; `q` matches the determination's *reference number*, not trades (`q=roofing` → 0 against 85k live DBRA rows), so wd modes seed with an EMPTY keyword — the schema's `"default": "contract"` was removed so an Apify-UI prefill can't silently pre-filter a wd run to nothing (the code keeps the cycle-96 seed default for opportunities); `publishDate` is an ISO string on CBA rows but epoch MILLISECONDS on DBRA/SCA, normalized to ISO 8601 for every row; the three indices return covered geography in three different shapes (DBRA `location.state` singular, CBA `location.states[]`, SCA `location.states[]` with `counties.include`/`.exclude`), flattened into one `coverage` array with SCA's EXCLUDED counties kept in their own field rather than merged into the covered list.
- **Honest scope limit, stated in the README and as an always-null `wageRates` field**: SAM.gov does not publish per-occupation hourly rate tables on any of the three search indices, and only CBA has a keyless per-record detail endpoint (`sam.gov/api/prod/wdol/v1/cba/<id>`, 200 verified; the dbra/sca equivalents 404 on every guessed path). This Actor delivers the determination *index* — which determination applies where, its revision, scope and whether it is current — not the rate schedules.
- **Opportunity-only filters fail LOUD in wd modes**: `naicsCodes`/`setAsideTypes`/`noticeTypes`/`organizationId`/`enrichDetail` are ignored with an explicit warning naming them, instead of silently returning a full unfiltered (and fully billed) set that looks filtered.
- **Watch mode generalized, not duplicated**: `dataType` is now part of the baseline fingerprint (opportunities and wage determinations have disjoint id spaces and must never share a baseline), `revisionNumber` joined the change snapshot (`v` in the compact KV record, backward-compatible — an older record missing it reads as null and cannot produce a false change), and one `idOf()` accessor now serves the dedupe set, the baseline and the push loop so they cannot disagree about a row's identity.
- **Verified**: local runs on all four dataTypes (DBRA/SCA/CBA row shapes inspected field by field) + watch seed→incremental on two different wd indices (baseline saved, 0 re-charged on the second run) + an opportunities regression run; platform build 0.1.12→0.1.13, run `s5vJF1u6Rm9vAmbzi` SUCCEEDED, **`chargedEventCounts {result: 25}`** (the cycle-542 defect class re-checked live on the new code path, not just statically); Store metadata published (title/description now name wage determinations, re-pushed after publish to force the Algolia reindex per the cycle-515 rule); `registry.json` +16 output fields; `/tools/sam-gov-opportunities-scraper` 200 and rendering the new copy; root README Actor row updated in the same cycle (the exact drift class cycles 701/702 each found a cycle late).
- Standing checks all clean after: `check-registry-fields` 0 drift, `check-code-fields` 0, `check-charges` 23/0, `check-fail-ordering` 19/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-disclosure` 0, `check-readme-samples` 32 blocks/49 bullets 0 drift. Committed `b059bae`, pushed, tree clean.
- **Next cycle — precise:** (1) Cycle 705 is due a QUALITY/GROWTH cycle (704 was BUILD). (2) Remaining SAM.gov competitor gaps, both lower value than what shipped: assistance listings (`index=_all` blends `_type: assistanceListing` rows — needs its own type-filter probing to isolate; try `index=al`-style guesses again now that `dbra` proved the index names are short and non-obvious) and bid documents / exclusions (exclusions still look key-gated, which would break this Actor's "no API key, no login" positioning — decide deliberately). (3) dev.to slot 9 due ~2026-09-25 — the four-datasets-one-endpoint / `index=` multiplexing story is a strong syndication angle now that it is shipped and provable. (4) `bin/traffic` not re-checked this cycle (BUILD) — re-verify next QUALITY cycle against the Polar trigger. (5) A `bin/check-root-readme` for README-vs-registry drift is still unshipped (3rd instance would have been caught by hand here; now 2 hand-found + 1 avoided).

## Cycle 703 (2026-09-23, sonnet-5, BUILD/scoping — competitor-gap audit found a real gap on `sam-gov-opportunities-scraper`; probed and confirmed a keyless path to close part of it, but did not build the feature this cycle. No code/Actor/site change.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 17:00Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `10c49b0`).
- **Inbox**: `list 8` — two ids (`23ab769e`, `0b504b8b`) that looked new by position confirmed via grep against `STATUS_ARCHIVE.md` as already-vetted cold-outreach/spam from cycles 665-672, not new. Newest is still `14c3b3ef` (dmarc), unchanged for many cycles. No new mail, no revenue event, no mail sent.
- **Per cycle 702's note ("Next cycle can be BUILD"), ran a competitor-gap audit** on an Actor not recently audited: `apify-admin store "SAM.gov contract opportunities" 15`. Leader `jungle_synthesizer/samgov-scraper` has 168 users (next closest competitor: 10) and its live `description` field (read via a direct public-API GET, not the Store page) lists four data types beyond opportunities: bid documents, exclusion records (debarment list), wage determinations, and assistance listings. Our `sam-gov-opportunities-scraper` covers only opportunities — a real, unaddressed gap, first time this Actor has had a formal competitor-gap audit since it shipped (cycle 539/540) already built with h289-safe seed gating and watch mode (verified: `watchLabel`/`watchChanges`/`seedFailure` gate already present in `src/main.js`, so no defect-class work needed there).
- **Probed live before scoping (per cycle 701's rule) rather than assuming a new integration is required.** Our Actor already calls an undocumented, keyless SAM.gov backend (`https://sam.gov/api/prod/sgs/v1/search/?index=opp...`, cycle 538/539 finding). Tried swapping the `index=` param to see if the same backend serves the competitor's extra data types: **`index=wd` → 200 OK, 107,580 results** (`_type: wdCBA`, Davis-Bacon/CBA wage determinations — sample row has `title`, `type.code`, `location.states[].counties[]`, `isActive`, `revisionNumber`, `modifiedDate`). **`index=sca` → 200 OK, 2,666 results** (`_type: wdSCA`, Service Contract Act wage determinations, has a `services[]` code/description array). `index=_all` blends in `_type: assistanceListing` rows alongside opportunities (CFDA/financial-assistance program data) but needs its own type-isolation work. 12 other index-name guesses for exclusions (`excl`, `exclusion`, `exc`, `el`, `wagedetermination`, `al`, `assistance`, `fpds`, `entity`, `biz-ops`, `financialassistance`, `award`) all 400 — exclusions likely needs SAM.gov's separately-registered, key-gated Exclusions API, not this keyless backend.
- **Did not build this cycle** — the wage-determination record shape probed so far looks like a search-result summary (same as `opp` rows before the existing `DETAIL_API` enrichment call), so a real build needs a further probe of detail/full-rate-schedule endpoints and filter-param semantics (state/county/wdNumber/isActive) before writing a fetcher, matching the rigor cycle 701 used for Himalayas. Precisely scoped as the new top queue item (`0-NEW-h703`) rather than rushed half-verified in the remaining time budget.
- **No fleet checklist re-run** — no code, Actor, or site file changed this cycle (pure research/scoping), so nothing to re-verify beyond the services/site health check above (all healthy) and confirming git tree stays clean pre/post.
- **Left for next cycle:** (1) **Top priority**: build the wage-determinations feature on `sam-gov-opportunities-scraper` per the scoping in `queue.md` `0-NEW-h703` — probe record/detail shape and filters live first, decide new-`dataType`-on-existing-Actor vs. new-Actor (existing one already has the auth-free positioning + h289-safe scaffolding, likely cheaper to extend). (2) Assistance listings and exclusions are lower-priority follow-ups from the same audit — exclusions likely requires a registered API key, which would change this Actor's "no API key, no login" README claim; decide deliberately. (3) `bin/check-root-readme`-style drift check still not shipped (2 hand-found instances, cycles 701/702); low priority unless a 3rd instance turns up. (4) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (5) EDUCATION category slot still empty (option, not commitment).

## Cycle 702 (2026-09-23, sonnet-5, QUALITY/GROWTH — re-measured `remote-job-board-json-apis-four-feeds` across all 6 boards now that Himalayas shipped (cycle 701); closed cycle 700's own follow-up. No code/Actor change.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:30Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `551b0e2`).
- **Inbox**: `list 8` unchanged for many cycles running — newest still `14c3b3ef` (dmarc); no new mail, no revenue event, no mail sent.
- **Full standing checklist run first, all clean**: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 37+8/0 missing, `check-readme-samples` 31+46/0 drift, `check-blog-claims` 0 stale, `check-code-fields` 0 drift, `check-real-fields` 0 drift, `check-filter-reach` 0 unreachable, `check-source-bytes` 0 flagged, `check-competitor-claims` 0 stale, `check-entities`/`check-parser-regression` are per-Actor tools, not fleet sweeps (skipped, as usual). `bin/traffic`: pricing 4/2, checkout 1/1 over 7 days — unchanged, still nowhere near the >100/day Polar trigger (deferred per owner decision). File sizes fine: `STATUS.md` 77KB / `queue.md` 85KB, well under the 150KB trim line.
- **Did the follow-up cycle 700 itself flagged** ("when Himalayas ships, re-run this post's overlap measurement — cross-source conclusions do not survive an added source," LEARNINGS cycle 700): `site/content/blog/remote-job-board-json-apis-four-feeds.md` still said "five" and had no Himalayas section, 2 cycles after cycle 701 shipped Himalayas as the 6th board.
- **Live-measured all 6 boards today** (Remotive 19, Remote OK 99, Jobicy 50, Arbeitnow 26 across 2 pages, Working Nomads 58, Himalayas 100 across 5 pages at its fixed 20/page — 352 rows total), same normalize-company-and-title union method as cycles 700/699. **Result: the cross-board duplicate count did not move — still the same 5 pairs found in the 5-board measurement (all Working Nomads↔Remotive or Remote OK↔Remotive).** Himalayas' 100 sampled postings matched none of them — written up honestly as "sample too small to tell," not "Himalayas doesn't syndicate," since 100 rows out of Himalayas' own reported 101,778-posting inventory is not a meaningful sample against 20-58-posting curated boards.
- **Found a second, new fact while re-running the measurement, not previously written up**: two boards have same-board internal duplicates — Arbeitnow repeated 2 listings across its own 2-page pull, Working Nomads repeated one listing 2x and another (Peroptyx "Data Analyst") **4x** within its single 58-row feed. Verified against the Actor's actual dedup code (`src/main.js:549-563`) before claiming it's handled: the `byKey` Map keys purely on normalized company+title across *all* collected rows regardless of source, so same-board dupes are folded exactly like cross-board ones — the claim added to the post ("de-duplicates across and within boards") is accurate, not just plausible-sounding copy.
- **Shipped**: new `## Update, 2026-09-23: a sixth feed — Himalayas...` section (endpoint-discovery story: `/api/jobs` 404s, `/api/v1/jobs` returns the HTML shell, real endpoint `/jobs/api` only found via the site's own `comments` field; cursor pagination; `?limit=` ignored, always 20/page; `totalCount` 101,778; `guid` as the only stable id) plus a re-measured "overlap across all six" subsection and the same-board-duplicate finding. Title/description bumped "Five…" → "Six…" (slug deliberately left as `…-four-feeds`, same reasoning cycle 700 documented — it's indexed and linked three places). Intro paragraph and the top note-box updated to name Himalayas alongside Working Nomads. Old "partly superseded by the fifth board" pointer left in place (still accurate), not rewritten.
- **Verified live**: restarted `fetchsmith-web`, page re-rendered 200, `<title>` correct, "Himalayas" appears 14x in rendered HTML, 0 raw markdown-table pipes leaking (renderer only has `fenced_code`+`tables`), disclosure footer intact. `/blog` listing and `sitemap.xml` both carry the new title/entry. `bin/indexnow` 200 for the post + `/blog` + `/tools/remote-jobs-scraper`. Root README (`README.md:75`) and the Actor README (`actors/remote-jobs-scraper/README.md:131`) link-text both updated "Five…"→"Six…" to match — this is exactly the drift class cycle 701 found in the root README's Actor table, just on a different line; still no `bin/check-*` catches root-README-vs-content drift generally (see queue.md item, unchanged from cycle 701).
- **Re-ran affected checks after editing**: `check-disclosure` 0 missing, `check-blog-claims` 0 stale, `check-readme-samples` 0 drift. Committed `5ccaa22`, pushed, tree clean.
- **Left for next cycle:** (1) No fresh competitor-gap or defect-class sweep currently queued. (2) `bin/check-root-readme`-style check for README-vs-content drift still not shipped — two hand-found instances now (cycle 701's board-count table, this cycle's blog-post title link) — worth ~20 min next QUALITY cycle if a third instance turns up, otherwise low priority. (3) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle; the Himalayas endpoint-discovery story or the six-board milestone are still open syndication angles. (4) EDUCATION category slot still empty (option, not commitment). (5) Next cycle can be BUILD — no BUILD-specific item is blocking; consider a competitor-gap pass on an Actor not recently audited, or resume any dormant fleet-wide grep.

## Cycle 701 (2026-09-23, sonnet-5, BUILD — closed the last competitor-gap on `remote-jobs-scraper`: added Himalayas as a 6th board, build 0.1.9 live, platform-verified. Also fixed a stale root README claim no standing check covers.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:00Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `bf04170`).
- **Inbox**: `list 8` unchanged from cycle 700 — newest still `14c3b3ef` (dmarc); no new mail, no revenue event, no mail sent.
- **Standing checklist run first, all clean**: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift.
- **Shipped Himalayas as the 6th `remote-jobs-scraper` source**, closing the last gap against the category leader (`benthepythondev/remote-jobs-aggregator`, 805 Store users, 6 boards) scoped-but-not-started since cycle 699/700. Probed the live API before writing any code (per standing rule): the documented-looking `/api/jobs` and `/api/v1/jobs` paths both 404/return the HTML app shell; the real endpoint is `https://himalayas.app/jobs/api`, found only by reading its own JSON `comments` field. Confirmed live: cursor pagination (`nextCursor`) works cleanly (0 overlap across 2 pages), **`?limit=` is silently ignored — tested 50/100/200, always returns exactly 20** (so `maxPagesPerSource`, not a page-size param, governs depth — different shape from Arbeitnow's 250/page), `totalCount` 101,773 confirms the "~102k" figure, jobs sorted newest-first, `guid` (job's own permalink) is the only stable per-posting key (same gap-filling shape as Working Nomads' `url`). Full detail + the "probe before scoping" rule in LEARNINGS cycle 701.
- **`fromHimalayas()` added** (`src/main.js`): loops `maxPagesPerSource` times over the cursor, maps `minSalary`/`maxSalary`/`currency`/`salaryPeriod` the same numeric-source shape as `remoteok`/`jobicy`, stops on an empty page (not a missing `nextCursor` — the true last page was never reached live at ~5,088 pages deep, so that assumption is left unverified rather than claimed). Wired into `ALL_SOURCES`/`SOURCE_SITE`/`FETCHERS`.
- **Verified thoroughly, local + platform.** Local: all-6-source run (314 rows, 302 unique, 12 cross-board dupes folded — consistent with the pre-Himalayas baseline since this cycle's small `maxPagesPerSource` sample of Himalayas' ~102k inventory didn't happen to overlap any other board, expected at that sample size, not a bug); unknown-source error message correctly lists `himalayas`; isolated Himalayas-only pull (100 rows over 5 pages) spot-checked field-by-field (salary normalization, null location on a job with no `locationRestrictions`, full description HTML). Platform: build **0.1.9** pushed and live; real Store-API run `2mh48OjcyK4cQlHxI` SUCCEEDED, `chargedEventCounts {job:25}` matching 25 pushed rows (confirmed on re-poll after the usual propagation delay).
- **Metadata updated everywhere and re-verified**: `.actor/input_schema.json` (enum/default/description for `sources`, `maxPagesPerSource` description), `.actor/actor.json`, `meta.json` (title/seoTitle/description/seoDescription, all re-measured under the 63/70/300/200-char limits), `README.md` (board table + a new row explaining why the 5-board dedup-rate measurement does NOT extend to Himalayas — its ~102k inventory dwarfs any single capped pull, so no fleet-wide overlap % is claimed for it), `actors/registry.json` (title/summary/example_input), `package.json`. Published live via `apify-admin publish` (200 OK) — confirmed the Store build's `actorDefinition.readme` contains "Himalayas" and "Six public..." over the API, not the CDN-cached page. `bin/set-example-input` re-run (memory 1024 confirmed). `check-pricing` 23/0, `check-store-meta` 23/0, `check-registry-fields`/`check-meta-fields`/`check-readme-samples`/`check-disclosure`/`check-source-bytes` all re-run clean.
- **Found and fixed real drift no standing check covers**: root `README.md`'s Actor table still said "four public job boards" — never updated when cycle 699 added Working Nomads (5th board). Fixed to "six... Working Nomads and Himalayas". No `bin/check-*` diffs the root README against `registry.json`'s per-Actor board/source count — worth a check if this recurs (see queue.md).
- **Left for next cycle:** (1) No fresh competitor-gap or defect-class sweep currently queued for `remote-jobs-scraper` — it now matches the category leader's board coverage exactly. (2) Consider a `bin/check-root-readme` (or extend an existing check) to catch root-README-vs-registry drift like the one found this cycle — low priority, one instance found so far. (3) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle; Himalayas or the 6-board milestone could be a syndication angle. (4) EDUCATION category slot still empty (option, not commitment). (5) `bin/traffic` not re-checked this cycle (BUILD, not QUALITY) — re-verify next QUALITY cycle, still expected far below the Polar trigger.

## Cycle 700 (2026-09-23, opus-5, QUALITY/GROWTH — closed cycle 699's follow-up (1): the remote-jobs guide now covers five boards, with a live-measured Working Nomads section. Found the added board also **flipped a published conclusion**, not just a count.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 15:30Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `5f27f93`).
- **Inbox**: `list 8` unchanged from cycles 698/699 — newest still `14c3b3ef` (dmarc); the 3 older cold-outreach/OTP ids below it are the already-vetted non-actionable set. No new mail, no revenue event, no mail sent.
- **Standing checklist run first, all clean**: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 37 site + 8 dev.to / 0 missing, `check-readme-samples` 31 samples + 46 prose bullets / 0 drift. `bin/traffic` verified funnel: `/pricing` 4 visits / 2 visitors, `/tools` 4/4 over 7 days — two orders of magnitude below the >100/day Polar trigger, unchanged, still deferred per owner decision.
- **Did cycle 699's follow-up (1)** — `site/content/blog/remote-job-board-json-apis-four-feeds.md`. **The staleness was bigger than cycle 699 scoped it.** That post's load-bearing claim was a measured conclusion ("2 duplicate postings out of 193, so these boards mostly do not syndicate"), and it was computed *across the source set*. Re-measured live today over all 5 boards with the same normalize-company-and-title union: **250 rows, 240 unique keys, 5 keys on >1 board — and 4 of those 5 are Working Nomads <-> Remotive** (3 of them one advertiser, Lemon.io; the 5th is the Remote OK <-> Remotive pair the original pass found). The old conclusion was a fact about the four boards measured, not about public remote-job feeds; adding a fifth flipped its direction.
- **Measured Working Nomads live before writing** (playbook line 18's two-measurement rule satisfied: this cycle's pull, plus cycle 699's independent integration run, which found the same Lemon.io "Senior QA Engineer" WN<->Remotive collapse). Facts now in the post, all re-confirmed on a second independent pull the same day (identical 58 rows / 265 KB / all `-04:00` offsets): 58 postings in a bare array, **every query param ignored** (`?page=2`/`?limit=5`/`?count=5`/`?offset=50` all return 200 + the same 58), ~1-month rolling window (oldest 2026-08-25), **92% of the 265 KB payload is job-description HTML** (243 KB, median 4,256 chars, 58/58 contain HTML tags), and 4 shape gaps: no id field of any kind (the `url` is the only stable key, unique across all 58), no salary field at all, `tags` is a comma-joined string (empty on 1 of 58), and `pub_date` carries a `-04:00` offset rather than `Z` (naive UTC parsing or a 10-char date slice shifts late-evening US postings a day).
- **Shipped**: title/description -> "Five public remote-job APIs…", 5-endpoint block, new `## Update, 2026-09-23` section (~90 lines) with the above, and an explicit in-place **"Partly superseded by the fifth board"** pointer on the old overlap conclusion rather than a rewrite — the 2026-09-21 numbers stay honest and dated. **Slug deliberately unchanged** (`…-four-feeds`) because it is indexed and linked from the root README, the Actor README and the Store listing; the post says why up front so the four/five mismatch does not read as an error. Cross-refs updated in `README.md` + `actors/remote-jobs-scraper/README.md`.
- **Verified**: post live 200, "Working Nomads" x17 in rendered HTML, new `<title>` correct, both new tables and the blockquote render as real HTML (no raw pipes — the renderer only has `fenced_code`+`tables`), listing page + sitemap carry it, IndexNow 200. Actor README reached the Store via `apify push --force` -> **build 0.1.8 SUCCEEDED**, confirmed by reading the build's `actorDefinition.readme` over the API (11,689 chars, contains "Five public…" + "Working Nomads"), not the CDN-cached page. `check-disclosure`/`check-readme-samples`/`check-registry-fields` re-run clean after the edits. Committed `b436540`, pushed, tree clean.
- **Left for next cycle:** (1) **Himalayas** is the remaining board the 805-user leader covers and we do not — cursor-paginated (`nextCursor`), ~102k jobs per its own `totalCount`, a real integration rather than a one-source add; scope rate limits and how deep a `maxResults` cap should walk BEFORE building. When it ships, re-run this post's 5-board overlap measurement again (see LEARNINGS cycle 700 — cross-source conclusions do not survive an added source). (2) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle; this post's update is a natural syndication candidate. (3) EDUCATION category slot still empty (option, not commitment). (4) No fresh defect-class sweep queued (h289/h263 both closed); next BUILD cycle can take Himalayas or another competitor-gap pass. (5) STATUS.md/queue.md well clear of the 150KB trim line.

## Cycle 699 (2026-09-23, sonnet-5, BUILD/GROWTH — `remote-jobs-scraper`: added Working Nomads as a 5th board, closing a real competitor-gap. Build 0.1.7 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 15:00Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `89d6333`).
- **Inbox re-checked**: `list 10` unchanged from cycle 698 — newest still `14c3b3ef` (dmarc). No new mail, no revenue event, no mail sent.
- **Standing checklist run first** (h289/h263 both closed, no fresh defect class queued per cycle 698): `check-fail-ordering` 19/0, `check-charges` 23/0, `check-seed-save` 18/0/0, `check-meta-fields` 8/0, `check-registry-fields` 0 drift, `check-disclosure` 37 site + 8 dev.to, 0 missing. `bin/traffic`: pricing 4, checkout 1 — still nowhere near the >100/day Polar threshold (item deferred per owner decision, unchanged).
- **Competitor-gap audit on `remote-jobs-scraper`** (per cycle 698's suggested next step, and the growth-backlog note that this rotation is "currently dry"): `apify-admin store "remote jobs"` surfaced `benthepythondev/remote-jobs-aggregator` as the clear leader (805 users) — it covers 6 boards (Remotive, Remote OK, Jobicy, Arbeitnow, Himalayas, Working Nomads) where we covered 4 (missing Himalayas, Working Nomads). **Price already fine**: their `currentPricingInfo` (fetched via `/v2/store` search, `apify-admin get` doesn't work for other users' Actors — no `data` key returned) shows FREE-tier $0.015/result vs our $0.0015 — we are already 10x cheaper at every tier, no price change needed (standing rule: always re-check price on a competitor-gap pass, this pass did).
- **Shipped Working Nomads as a 5th source** (`src/main.js`): its feed (`workingnomads.com/api/exposed_jobs/`, public, no auth, flat JSON array) has no stable job-id field, so the job's own permalink `url` doubles as `sourceJobId` (same class of gap-filling as `arbeitnow`'s sparse fields — no salary, no logo, both already handled by the existing null-safe shape). No watch mode on this Actor, no seed-baseline risk (confirmed out of scope for h289/h263). Himalayas (cursor-paginated, larger integration) left for a future cycle — scoped, not started, see queue.md.
- **Verified thoroughly**: local run with all 5 sources (240 rows fetched, 230 unique, 10 cross-board dupes folded — **confirmed Working Nomads dedupes against Remotive on real live postings**, e.g. "Senior QA Engineer" appeared on both). Edge cases: unknown source name still fails the run with the updated valid-list message; a no-match keyword returns clean `0` rows, no charge. `check-registry-fields`/`check-charges`/`check-meta-fields`/`check-source-bytes` all re-run clean after the registry/meta edits. Build 0.1.7 pushed and platform-verified: real run `pknYXGea0kgMcShDU` SUCCEEDED, `chargedEventCounts {job: 20}` matching 20 real dataset items (had to read `chargedEventCounts` at the run's top level, not under `stats` — `stats` doesn't carry it).
- **Metadata updated everywhere**: `.actor/input_schema.json` (enum + default + descriptions), `.actor/actor.json`, `meta.json` (title/seoTitle/description/seoDescription, all re-measured under the 63/70/300/200-char publish limits — description needed trimming twice to fit 300), `README.md` (board table, input table, FAQ, sources/attribution, a re-measured dedup-rate claim dated today rather than reusing yesterday's stale number), `actors/registry.json` (title/summary/example_input). `meta.json` published live via `apify-admin publish` (200 OK), site tool page re-curled and confirmed 200 with "Working Nomads" now present in the rendered copy. Committed `39399e0`, pushed, tree clean.
- **Left for next cycle:** (1) The pillar-page blog post `remote-job-board-json-apis-four-feeds` (linked from this README) is now stale — it says "four feeds" and doesn't cover Working Nomads. Worth a follow-up pass: either update the post to five feeds with a re-measured Working Nomads feed-size figure, or leave as historical (the README doesn't claim the post covers all current sources). Low priority, not billing-correctness. (2) Himalayas is the other gap the leader covers that we don't — cursor-paginated (`nextCursor`), ~102k total jobs per the feed's own `totalCount`, meaningfully bigger integration than Working Nomads was; scope it properly (rate limits, how many pages a `maxResults` cap should walk) before building, don't rush it into a single cycle. (3) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (4) EDUCATION category slot still empty (option, not commitment). (5) No fresh defect-class sweep currently queued (h289/h263 both closed) — next QUALITY cycle can pick another competitor-gap pass (candidates: any Actor not audited recently) or resume the standing checklist rotation.

## Cycle 698 (2026-09-23, sonnet-5, QUALITY/GROWTH — h263 git-repack fix shipped (175M→35M, 10537→16 loose objects); STATUS.md/queue.md trimmed (last trim cycle 685); fleet checklist re-verified clean.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 14:30Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `2ea447f`).
- **Inbox re-checked**: `list 10` surfaced 3 unfamiliar-looking ids (`a836abe0` Companies House security code, `23ab769e` "Add to Google Search Index", `0b504b8b` "Can I Send Error Report of Your Site?") alongside the usual DMARC noise — grepped `STATUS.md`/`STATUS_ARCHIVE.md`/`queue.md` first per cycle 686's rule before treating them as new: all 3 are known cold-outreach/OTP artifacts already vetted non-actionable back around cycles 665-672, just older than the last few cycles' truncated view. Newest item is still `14c3b3ef` (dmarc), unchanged. No new mail, no revenue event, no mail sent.
- **Shipped h263 (git-repack fix), the cosmetic item flagged unaddressed for ~60 cycles.** Executed the exact recipe cycle 636 had scoped: `git pack-objects --stdout --all --reflog --indexed-objects --delta-base-offset --non-empty </dev/null >/tmp/fs.pack`. **This cycle's run confirms the failure really is flaky, not tied to the flag combination**: attempt 1 of the identical command hit the same `fatal: bad revision 'zsh:unalias:1: no such hash table element: unsetenv'`; attempts 2 and 3 (no changes) succeeded — consistent with the "worker shell/sandbox layer" hypothesis, not a git or repo defect. `git index-pack -v` on the resulting pack, installed as canonical `pack-<sha>.{pack,idx}` in `.git/objects/pack/`, verified with `git verify-pack -v` (ok) and `git fsck --no-progress --full` (0 corruption, only pre-existing dangling garbage) BEFORE pruning. `git prune-packed` then dropped the now-redundant loose copies. Re-verified after: `git fsck` clean, `git log --oneline -1` unchanged, `git push --dry-run` "Everything up-to-date", 3 services still active, site still 200. **`.git` 175M → 35M, loose objects 10537 → 16** (the 16 are pre-existing unreachable dangling objects, correctly excluded from an `--all` pack — not new garbage). Re-ran `check-seed-save`/`check-fail-ordering`/`check-charges` afterward to confirm the surgery touched nothing tracked: all identical to pre-surgery (18/18 ok, 19/0, 23/0). No code commit needed — packing is `.git`-internal only. Full detail in `queue.md`'s h263 entry.
- **Executed the STATUS.md/queue.md archive trim** (due since cycle 685, both files had climbed to 97KB/130KB, queue.md closing in on the 150KB line). Verified byte-exact via `diff` against `git show HEAD:<file>` before moving anything (both ranges matched the committed content exactly — my only other edit this cycle, marking h263 DONE, sat entirely below the archived range in queue.md). `state/STATUS.md`: cycles 682-669 (14 entries) moved into `STATUS_ARCHIVE.md`, 97,405 → 57,959 bytes. `tasks/queue.md`: the same cycle range (669-682, 13 entries) moved into `queue_archive.md`, 130,418 → 76,262 bytes. Kept the most recent ~15 cycle entries live in each file, plus queue.md's Growth-cycle-backlog/Standing-rules sections untouched.
- **Left for next cycle:** (1) Next cycle can be BUILD or another QUALITY pass — no freshly-scoped defect class is currently queued (h289 fully closed cycle 697). Candidates: a competitor-gap/README pass on an under-reviewed Actor, or grep the fleet for a new shape. (2) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (3) EDUCATION category slot still empty (option, not commitment). (4) **h263 is now CLOSED** — do not re-run the pack recipe; if `.git/gc.log` noise ever resurfaces before loose objects naturally climb back toward ~6700 (the default `gc.auto` threshold, currently 16), that would indicate something new is invoking `git gc`/`repack` more eagerly and deserves a fresh look, not a repeat of this fix. (5) Files now well clear of the trim line (58KB/76KB) — no trim needed again soon.

## Cycle 697 (2026-09-23, sonnet-5, BUILD/investigation — h289 sweep CLOSED: `google-play-reviews-scraper` hand-read, confirmed no real bug, allowlisted.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 14:00Z, 2026-09-23. 3 services active, site 200/`/health` 200. `git status` clean at cycle start (HEAD `6136d63`).
- **Inbox: two items looked new but both are already-resolved, per the `bin/inbox` no-read-marker trap flagged in cycle 686's LEARNINGS.** (1) `116f7cc3` (owner's forward of Apify's "Under maintenance" flag on `scholarship-scraper`) — grepped `queue.md`/`STATUS.md`, confirmed resolved cycle 652 (retired `isDeprecated:true`, owner already notified via `notify --once bold-org-blocked-retired-20260922`); live-verified via `apify-admin get` that `isDeprecated:true`/`notice:"UNDER_MAINTENANCE"` still holds — the flag re-appearing on every automated QA pass is expected, not a regression. No action, no re-reply. (2) `873db8ee` (capsule26.com AI-agent cold outreach re: our double-charge blog post) — already noted cycles 691+ as needing no action; still true, not a customer, not revenue-relevant. No mail sent this cycle.
- **h289 sweep closed for real: hand-read `google-play-reviews-scraper`, the last `check-seed-save` REVIEW entry, and found NO bug** — this Actor already prevents the classic seed-failure over-charge structurally, via a mechanism `check-seed-save`'s textual heuristic can't see (same class as cycle 696's app-store-reviews-scraper date-floor precedent, but a different shape): completeness is tracked **per app** via `seededApps` membership (main.js:403), not a single flag gating the save. An app whose `reviews()` call throws mid-walk lands in the `catch` (line 420) and is simply never added to `seededApps`, so `appSeeding` re-evaluates true for that exact app on every future run (line 324) until a walk actually succeeds — self-healing, no incomplete baseline is ever read as complete.
- **Checked for a secondary hole (the two things that made apple-podcasts real last cycle) and found none.** (1) Confirmed the underlying `google-play-scraper` library (`node_modules/google-play-scraper/lib/reviews.js`) rejects the whole promise chain on ANY page failure during pagination — it never returns a partial page silently, so our outer `catch` genuinely catches every failure mode, not just page-1 ones. (2) No date-floor to have edge cases in: `watchSeen` keys off `reviewId` directly, not a date heuristic. (3) `sinceDate`/`untilDate`/score/keyword filters are static input values baked into the watch fingerprint (not relative-to-now), so a review filtered out during seeding stays filtered out identically on every incremental run under the same label — it can never later read as newly-in-scope without also changing the fingerprint (= a fresh baseline).
- **Extended `bin/check-seed-save`'s ALLOWLIST mechanism to cover REVIEW (previously SUSPECT-only)** and added a full allowlist entry naming the invariant, matching the app-store-reviews-scraper precedent's bar for evidence. Re-ran: `check-seed-save` **18/18 ok, 0 SUSPECT, 0 REVIEW** (was 1 REVIEW). `check-fail-ordering` 19/0, `check-charges` 23/0, all clean. Committed `d7b67e7`, pushed, tree clean. No code change to the Actor itself — no fix was needed, this closes the investigation with a verified negative.
- **Left for next cycle:** (1) Next cycle should be QUALITY/GROWTH (696 and 697 both spent their budget on the h289 investigation track). Standing checklist: full static check suite, live pricing/JSON-LD check, `bin/traffic` vs Polar threshold, 2-3 varied live test runs, README/competitor gap pass, answer any new mail. (2) `queue.md` ~120KB / `STATUS.md` ~86KB — due for an archive trim (last trim cycle 685). (3) dev.to slot 9 due ~2026-09-25 — check `date -u`. (4) EDUCATION category slot still empty (option, not commitment). (5) h263 cosmetic git-gc warning (`.git/gc.log`) still reproduces on every commit, benign, has not been cleaned up in many cycles — worth a 1-line fix (remove the log file / run `git gc`) next QUALITY cycle just to stop the noise.


## Cycle 696 (2026-09-23, opus-5, BUILD — `apple-podcasts-scraper` h289 seed gate shipped, build 0.1.42 live; corrected cycle 695's over-stated scoping of the same bug.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 13:30Z, 2026-09-23. 3 services active, site 200. `git status` clean at cycle start (HEAD `6136d63`). Inbox `list 5`: newest still `14c3b3ef` (dmarc, non-actionable) — no new mail, no revenue event, no mail sent.
- **Correction to cycle 695's finding on this Actor (matters for the last REVIEW entry).** (a) It described the bug via `scrapeReviews()`/"review history charged as new", but `watchLabel` is forced empty for every dataType except `episodes` (line 113) — the reviews path can never reach `saveWatchRecord()`. (b) The classic over-charge was ALREADY blocked here by the per-podcast **date floor** (ported from `app-store-reviews-scraper`, cycle 637): a podcast the baseline never reached has no `pairFloors` entry, so `floorFor()` falls back to `seedFloor` (the seed run's own timestamp) and all its pre-existing episodes are suppressed as older-than-baseline. **First Actor in the h289 sweep whose over-charge was mitigated by a mechanism other than the save gate — a `check-seed-save` REVIEW proves only that the save is unreadable by grep, not that an over-charge is live.**
- **Two real holes the floor does NOT cover, both now closed.** (1) An episode Apple returns with no parseable `releaseDate` skips the floor test (`!Number.isNaN(releasedMs)`) and falls through to the seen-id test, so a pre-existing dated-less episode of an unread podcast would be pushed and charged as new. (2) A seed run that failed on part of its input still exited 0 and told the buyer "N existing episode(s) recorded … run it again later to get only what's new" for podcasts it never saw.
- **Fix**: `const seedErrors = []`, pushed at three sites each wrapped in `if (seeding)` (search-term `catch` — a failed term resolves to no ids at all; the `got === 0 && failed` podcast site; the raw-RSS-feed site), and the save moved to the fleet-standard flat shape `if (watchMode && seedErrors.length) { Actor.fail(...) } else if (watchMode) { saveWatchRecord(...) }`. Time-budget truncation deliberately excluded (that is what the floor exists for; failing there would make large inputs impossible to baseline).
- **Verified live, old-vs-new DNS injection** (unreachable feed host as seed input; old code restored via `git show HEAD:` into the same dir): OLD exit 0 + record `watch-c696-inject-…` committed with `lastRunStatus: seeded`, `seenIds: []`; NEW exit 1, explicit fail message naming the unread source, **no record written**. Unregressed: local seed (5 recorded/0 charged) -> incremental same label (0 new, "all 5 already delivered") -> non-watch run (5 pushed/charged). Platform build **0.1.42**: default-input run SUCCEEDED `{result: 39}`, watch-seed run SUCCEEDED `{result: 0}`.
- **`bin/check-fail-ordering` allowlist entry added** (line 1003): the new `Actor.fail()` sits between the first `Actor.charge(` and the last save textually, but every `seedErrors.push()` is inside `if (seeding)` and a seeding `pushEpisodeRows()` never calls `pushResult()`, so a seed run's charge count is provably 0. Fleet checks after: `check-seed-save` 18 ok / 0 SUSPECT / **1 REVIEW**, `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `f7f22fc`, pushed, tree clean.
- **Open**: `google-play-reviews-scraper` is the last `check-seed-save` REVIEW — hand-read it next, and check for a floor/per-item mechanism BEFORE scoping a fix (see queue.md item 1). `queue.md` ~118KB / `STATUS.md` ~86KB, trim next QUALITY cycle. dev.to slot 9 due ~2026-09-25.


## Cycle 695 (2026-09-23, sonnet-5, QUALITY/GROWTH — hand-read the 3 `check-seed-save` REVIEW Actors; found and fixed a REAL h289 bug in `us-federal-awards-scraper`, build 0.1.31 live; found a second real one in `apple-podcasts-scraper`, scoped precisely for next cycle.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 13:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `ba3888d`). Inbox `list 10`: newest still `14c3b3ef` (dmarc, non-actionable) — nothing new, no revenue event, no mail sent.
- **Ran the full standing checklist (20 `bin/check-*` scripts): 0 drift everywhere** except the 3 known `check-seed-save` REVIEW entries (the cycle's target) and the informational `check-field-fill`/`check-store-rank` baselines (unchanged). `queue.md` 115KB / `STATUS.md` 84KB — approaching but not past the 150KB trim line; **not trimmed this cycle** (time went to the REVIEW hand-read + fix instead — trim next QUALITY cycle if it keeps climbing).
- **`us-federal-awards-scraper` (line 813, pre-fix) — REAL bug, fixed.** `check-seed-save` had this Actor as REVIEW ("no incompleteness signal recorded at all") only because it tracks nothing at all on a page-request failure: `postPage()` (line 559) retries 4x then returns `null`; the seed walk's `if (!body) break;` (line 749) treated that identically to "no more results" and the unconditional `saveWatchRecord()` right after (old line 813-814) persisted the truncated set as a complete baseline — the next incremental run would have delivered and charged every award past the failure point as "new". This Actor had **zero** `Actor.fail()` calls anywhere, unlike the rest of the fleet.
  - **Fix**: added a `seedError` flag (named to match `check-seed-save`'s recognized walk-signal tokens, not just for style) set when `postPage()` returns null during seeding; gated the save on `seedFailure = seeding && seedError` in the fleet's flat `if (watchMode && seedFailure) {...} else if (watchMode) {...}` shape; added this Actor's first-ever `Actor.fail()` before `Actor.exit()`.
  - **Verified**: 4/4 unit-test gate cases (seed+error→gated, seed+no-error→saved, incremental+error→saved [not a double-charge risk since incremental doesn't claim completeness], incremental+no-error→saved). Live: watch-seed run (`cycle695-smoketest`, grants/broadband) SUCCEEDED, 308 recorded/0 charged; follow-up incremental run on the same label SUCCEEDED, 0 new/0 charged (confirms the baseline was consumed correctly); non-watch run pushed/charged 5/5. Did NOT do a live DNS-failure injection test (time budget) — judged the unit test + the now 9th-time-proven fix shape sufficient, same call cycles 686/687 made.
  - **Deployed**: build 0.1.31 live. All fleet checks re-run clean after the fix: `check-seed-save` 18 ok/0 SUSPECT/2 REVIEW, `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `3695aa3`, pushed, tree clean.
- **`apple-podcasts-scraper` — hand-read, found a REAL bug, NOT yet fixed (next cycle).** `scrapeReviews()` (main.js ~line 682-737) already tracks failures carefully: a thrown request sets `failed`/`pageError`, and the per-podcast caller (~line 917-919) pushes the id onto `failedIds` with a "re-run it, this is NOT evidence Apple has no reviews" warning. But the seed save at line 981 (`if (watchMode) await saveWatchRecord(seeding ? 'seeded' : 'incremental');`) is **unconditional** — it never checks `failedIds`. A podcast whose review fetch genuinely failed mid-seed still gets folded into the baseline as "fully seeded, 0 reviews for that show," so its entire real review history would be delivered and charged as "new" on the next incremental run. Same shape as `app-store-reviews-scraper`'s cycle-693 fix (a per-item/per-pair failure that the single blanket save doesn't consult), not the single-flag shape used elsewhere. **Scoped precisely for next cycle** — see queue.md.
- **`google-play-reviews-scraper` — NOT YET hand-read** (ran out of time budget this cycle). Grep shows no `markIncomplete`/`incompleteReason`-style signal in this file either (same REVIEW reason as the other two) — needs the same hand-read treatment before assuming either shape applies.
- **Left for next cycle (precise):**
  1. **`apple-podcasts-scraper`**: gate the seed save on `failedIds.length > 0` (or track a `seedHadFailure` boolean set alongside each `failedIds.push()` inside the main per-id loop, since `failedIds` also accumulates non-seed-run failures and the gate must be `seeding`-scoped like every other fix). Add this Actor's first-ever `Actor.fail()`. Watch for the same restructure trap cycle 694 hit: `check-seed-save` needs the flat `if (watchMode && seedFailure) {...} else if (watchMode) {...}` shape to recognize the guard, not a nested one.
  2. **`google-play-reviews-scraper`**: hand-read the fetch/error-handling path (main.js, no `fetchEntries`-style helper — check `gotScraping` call sites directly) before picking a gate shape.
  3. `check-seed-save` REVIEW count will be down to 0 once both are read/fixed — that closes h289 fleet-wide for real this time (694's "closes the sweep" claim covered only the SUSPECT list, not REVIEW).
  4. `queue.md` (115KB) — trim into `queue_archive.md` on the next QUALITY cycle if it's still climbing (last trimmed cycle 685).
  5. dev.to slot 9 due ~2026-09-25 (check `date -u` each cycle). EDUCATION category slot still empty (option, not commitment). h263 cosmetic (git gc log) benign, reproduces on every commit.

## Cycle 694 (2026-09-23, sonnet-5, BUILD — h289 sweep closed: `court-records-scraper` fixed, builds 0.1.25/0.1.26 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 12:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `4144443`). Inbox `list 10`: one new dmarc report (`14c3b3ef`, non-actionable, same pattern as always) — no other new mail, no revenue event, no mail sent.
- **Fixed the last named h289 SUSPECT, `court-records-scraper`** (save line 844, per cycle 693's scoping): `markIncomplete()` classifies a mid-walk stop as `charge-limit`/`seed-cap`/`max-results`/`stopped-early`, but the seed save was unconditional. Confirmed `charge-limit`/`max-results` cannot fire during seeding (structurally guarded/unreachable from the seeding code path); gated on the two that can — `source-error` (a walker's `apiGet()` genuinely failed) and `stopped-early` (the code's own "should be unreachable" fallback, treated conservatively as a real failure). `seed-cap` stays exempt (buyer's-query-too-broad, capped-but-real, same fleet policy). Added this Actor's first-ever `Actor.fail()`.
- **First draft used a nested if/else and `check-seed-save` still read SUSPECT despite correct logic** — the heuristic only recognizes the flat `if (watchMode && seedFailure) {...} else if (watchMode) {...}` shape every other fixed Actor uses. Restructured to match (same behavior, now recognized); re-verified live after the restructure too. Full writeup in LEARNINGS.md.
- **Verified live old-vs-new against the real CourtListener API** (injected apiGet failure after page 1 during a watch-seed, 1854-match query): OLD saved 20/1854 ids as `lastRunStatus:"seeded"` exit 0 — 1834 dockets would have been delivered and charged as "new" next run. NEW: no watch-store write, exit 1. Happy path unregressed: narrow real seed (5 matches) saved cleanly `complete:true`; non-watch search pushed 5/5 real charged rows. Deployed and platform-verified twice (builds 0.1.25, 0.1.26): both default-input Store-test runs SUCCEEDED, `chargedEventCounts {result:100}` against real CourtListener data.
- **This closes the h289 sweep's SUSPECT list entirely.** `check-seed-save`: 18 watch-mode Actors ok / 0 SUSPECT / 3 REVIEW (`apple-podcasts-scraper`, `google-play-reviews-scraper`, `us-federal-awards-scraper` — no incompleteness signal recorded at all, same state pre-fix `eu-ted-tenders-scraper` was in before it turned out to be the worst case; each needs a one-time manual read). `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `2c3ddc4`, pushed, tree clean.
- **Next cycle should be QUALITY/GROWTH** (693 and 694 were both BUILD). Priorities: (1) hand-read the 3 REVIEW Actors to confirm a cut-short seed is structurally impossible for each — this is the last piece of the h289 investigation. (2) `queue.md` is 112KB, approaching (not yet past) the 150KB trim line — worth trimming this QUALITY cycle to stay ahead of it. (3) dev.to slot 9 due ~2026-09-25 (check `date -u`). (4) EDUCATION category slot still empty (option). h263 cosmetic (git gc log) benign, still reproduces on every commit.

## Cycle 693 (2026-09-23, sonnet-5, BUILD — h289 sweep item 8: `app-store-reviews-scraper`, a NEW variant (silent-empty on a total transient fetch failure, not an explicit incompleteReason). Commits `8fdc7b2`, `d927d75`, `ce82637`.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 12:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `bb5c6f5`). Inbox `list 10`: newest still non-actionable (dmarc reports, already-answered items); no new mail, no revenue event, no mail sent.
- **`check-seed-save`'s SUSPECT on `app-store-reviews-scraper` was a false positive on the shape it checks for (save-call gating) — but hand-reading it to confirm that turned up a real, different h289 variant.** `fetchPage()` tries a request under 2 client classes; on a *transient* failure (5xx/429/network) it falls through to try the other, and if BOTH classes failed transiently it silently returned `{entries: []}` — textually identical to a genuine Apple-confirmed empty page. During a seed walk this reads as "pair fully baselined, 0 reviews" (reported COMPLETE), so a pair hit by a one-off outage would have its entire real review history delivered and charged as "new" on the next incremental run. The fleet's own `reviewFeedIsDown()` outage probe only fires when the WHOLE run's `feedServed===0`, so one bad pair among several healthy ones slipped past it.
- **Fix:** `fetchPage()` now distinguishes "≥1 client class answered (even empty)" from "every class threw" — only the latter now throws, which the Actor's existing per-pair catch already handles correctly (excludes the pair from `seededPairs`, charges nothing). `tolerateAll` (the outage probe) unaffected. Verified with an isolated 8/8 unit test (incl. the negative control reproducing the silent-empty bug against the old code) plus a live platform run: build 0.1.58, `chargedEventCounts {result:10}` matching 10 pushed rows, no regression.
- **Allowlisted `app-store-reviews-scraper` in `check-seed-save`** with the per-pair-gating invariant documented (it will keep reading SUSPECT otherwise — different, correct safety shape than the single-gate-before-save pattern the heuristic looks for).
- **Caught and fixed a self-inflicted knock-on:** the edit shifted line numbers below it, which turned `check-fail-ordering`'s 3 existing `app-store-reviews-scraper` allowlist entries stale (775→787, 993→1006, 1004→1019), briefly showing a false SUSPECT there too — found and fixed by re-running the full standing checklist before finishing, not anticipated in advance.
- **All fleet checks clean after the fixes**: `check-seed-save` 18 ok / 1 SUSPECT (`court-records-scraper`, next) / 3 REVIEW, `check-fail-ordering` 19/0, `check-charges` 23/0. Full LEARNINGS.md writeup.
- **Next cycle should be BUILD — 1 named Actor left in the h289 sweep: `court-records-scraper`** (save line 844; `markIncomplete()` reasons include `charge-limit`/`seed-cap`/`max-results`/`stopped-early` — read what triggers `stopped-early` specifically before picking a gate shape, per cycle 691's scoping). **Also worth a repeat of this cycle's method** on the 3 REVIEW Actors (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) when reached — they record no incompleteness signal at all, and this cycle showed a clean heuristic verdict doesn't rule out a bug the heuristic isn't shaped to see. dev.to slot 9 due ~2026-09-25 (check `date -u`). EDUCATION slot still empty (option). h263 cosmetic (git gc log) benign, still reproduces on every commit.

## Cycle 692 (2026-09-23, opus-5, QUALITY/GROWTH — shipped `bin/check-disclosure` after finding a live blog post with no AI-disclosure footer. Commit `6950cab`.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 11:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 567/1967 MB. `git status` clean at cycle start (HEAD `c71a196`). Inbox `list 10`: newest still `873db8ee` (unchanged since cycle 688) — nothing new, no revenue event, no mail sent.
- **Cycle-type note:** 689/690/691 were all BUILD, so the every-3rd-cycle QUALITY slot was already deferred once. Took it this cycle per cycle 684's precedent ("do not defer it a second time"). The h289 sweep's last 2 SUSPECT Actors are untouched and still queued — see queue.md.
- **All 9 static checks 0 drift** (`check-filter-reach` 23/15/0, `check-source-bytes` 408/0, `check-code-fields` 0, `check-charges` 23/0, `check-fail-ordering` 19/0, `check-blog-claims` 0, `check-meta-fields` 8/0, `check-readme-samples` 31+46/0, `check-seed-save` 18 checked / 2 SUSPECT / 3 REVIEW — unchanged, a known backlog not new drift). JSON-LD price check 22 tools 0 mismatch. `bin/traffic`: no API usage, funnel nowhere near the >100/day Polar threshold. File sizes `STATUS.md` 73KB / `queue.md` 104KB — well under the 150KB trim line. dev.to slot 9 not due until ~2026-09-25.
- **Real drift found — the one surface with no `bin/` check.** `site/content/blog/watch-baseline-eviction-rebilling.md` (published cycle 669) has been **live for 23 cycles with no AI-disclosure footer at all**. The rule requiring one dates to cycle 160 — which found the identical gap on 6 of 19 posts and wrote the fix as a shell one-liner inside a `queue.md` backlog bullet. Nothing ever executed it, so it rotted the same way twice. Added the **neutral** footer variant (this post has 0 live-captured JSON payloads, so the strong "every endpoint and field name came from a live request" variant would be an unverifiable claim — cycle 160's standing rule is explicit about not upgrading). Verified rendered live: page 200, footer present.
- **Shipped `bin/check-disclosure`** (10th standing check) covering **both** the site blog and dev.to — the old one-liner only ever read the site, so half the published surface was never checked by anything. Baseline: 37 site posts + 8 dev.to articles, 0 missing. dev.to is skipped with a notice (never an error) when `DEVTO_API_KEY` is absent or the API is unreachable, so it stays useful offline. **Fault-injection negative control** per the `check-fail-ordering`/`check-seed-save` play: flags 1/1 against `git show HEAD:` of the broken post, reads 0 on the fixed tree.
- **Near-miss, recorded because it was wrong in the dangerous direction:** my first cut grepped the single phrase `autonomous AI worker` and reported 2 `MISSING` on **live** dev.to articles (4627420, 4610780). Both disclose — in the neutral variant and in dev.to's longer "built with AI assistance (Claude) … only public data" wording. Trusting that output would have meant editing two published third-party articles to fix a non-problem. The shipped check matches on intent across all three legitimate wordings. **All 8 dev.to articles verified as disclosing.**
- **dev.to comment poll: 1 new comment since cycle 681** — `nikhil_patel_10` (2026-09-23 08:51Z) on the Shopify 999,999 article (id 4689167), plus `launchgatecheck` (09-22) which prior cycles had not logged. Neither asks a question; nikhil's restates the post's own conclusion, and launchgatecheck's second suggestion (preserve the raw number alongside an explicit `inventory_tracked: false`) is **already shipped** — that's exactly the `inventoryTracked` flag added in response to `dododata`. Their first suggestion (split `source_status`/`product_count`/`filtered_count` into separate stored fields rather than encoding all three as an empty array) is a real, unaddressed design point — queued, not answered this cycle for time.

## Cycle 691 (2026-09-23, sonnet-5, BUILD — h289 sweep item 7: `hacker-news-scraper` fixed, build 0.1.37 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 11:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `66dc050`). Inbox re-checked (`list 10`): newest still `873db8ee` (unchanged since cycle 688/689, non-actionable) — nothing new, no revenue event, no mail sent.
- **Hand-read `hacker-news-scraper` (line 517, per cycle 690's precise scoping) and confirmed it is a real h289 instance**: its seed walk loops over `queries[]`, retrying each query's Algolia request up to 3 times before giving up and recording it in `erroredQueries` (`incompleteReason:'request-failed'` for that query) — but `if (watchMode) { await saveWatchRecord(...) }` was unconditional, so a truncated baseline would prime the first incremental run to charge for every hit past the failure point as "new". Structural truncation (`seed-cap`, `algolia-pagination-ceiling`, `max-items-per-query`, `scan-short-of-declared`) deliberately left ungated, same fleet policy as the rest of the sweep.
- **Used a third gate shape** (documented in LEARNINGS.md): this Actor's seed walk is a loop over independent per-query sub-fetches, so no single `incompleteReason` string represents "did any query's request fail" — gated on `erroredQueries.length` (already-existing array) instead of a boolean flag or a reason string. Added the Actor's first-ever `Actor.fail()`.
- **Verified live old-vs-new**: `git show HEAD:` copy vs fixed copy, both pointed at an invalid Algolia host to force a real DNS-failure upstream error during a watch-mode seed. OLD: saved `seenIds: []` under `lastRunStatus:"seeded"` (exit 0) — every real "apify"/story hit would have been over-charged as "new" next run. NEW: no watch-store value written at all, `Actor.fail()` fires (exit 1). Happy path unregressed against the real Algolia API: seed run recorded 286 real ids (exit 0, 0 charged), a second run on the same label correctly skipped all 286 as already-seen (0 new, 0 charged), and a non-watch run pushed and charged 10/10 real rows.
- **Deployed and platform-verified**: build 0.1.37 live. Real default-input platform run `XnXU7IU4hsQzeGeyR` SUCCEEDED, `chargedEventCounts {result:10}` for 10 pushed rows (first poll read 0, same propagation-delay artifact noted in cycle 690, confirmed 10 on re-poll).
- **All fleet static checks re-run and clean**: `check-seed-save` now reads `ok` for this Actor (17 watch-mode Actors `ok`, 2 SUSPECT left: `app-store-reviews-scraper`, `court-records-scraper`; 3 REVIEW unchanged: `apple-podcasts`, `google-play-reviews`, `us-federal-awards`). `check-fail-ordering` 19/0, `check-charges` 23/0.
- **Next cycle (BUILD) — precise:** hand-read `app-store-reviews-scraper` (save line 1012, per-pair `incompleteReason` computed ~line 809-825 off `res.storefrontError`/`truncatedByRun`/`chargeLimitHit` — check whether a genuine per-pair upstream failure (`res.storefrontError`) is distinguishable from structural caps before picking the gate shape) and `court-records-scraper` (save line 844, `markIncomplete()` reasons include `charge-limit`/`seed-cap`/`max-results`/`stopped-early` — read what triggers `stopped-early` specifically, it reads suspiciously like an unclassified genuine failure). This closes the h289 SUSPECT list entirely. Then hand-check the 3 REVIEW Actors (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) — no incompleteness signal recorded at all, confirm a cut-short seed is structurally impossible for each. dev.to slot 9 due ~2026-09-25 (check `date -u`). EDUCATION category slot still empty (option). h263 cosmetic (git gc log) benign.

## Cycle 690 (2026-09-23, sonnet-5, BUILD — h289 sweep item 5+6: `sam-gov-opportunities-scraper` and `nih-reporter-scraper` fixed, the status-only pair. Builds 0.1.11/0.1.18 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 10:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `b3e737f`). Inbox unchanged since cycle 688/689 (newest still `873db8ee`, non-actionable) — nothing new, no revenue event, no mail sent.
- **Fixed both status-only h289 Actors from cycle 688's scoping.** Both saved a cut-short seed baseline under a distinct status label (`'seeded-incomplete'`) instead of skipping the save, and neither Actor's load path (`Array.isArray(existing.seenIds)` only) ever reads `.status` back — the tag was cosmetic, the over-charge still happened.
  - `sam-gov-opportunities-scraper` (line 575): classified its incompleteness reasons and gated the save on only the two that mean SAM.gov genuinely didn't answer — `upstream-error` (page fetch failed after 4 attempts) and `short-page` (backend quit before its own declared total, unexplained by duplicates). Left `duplicate-rows` (SAM served everything, just repeated — explained shortfall, no real gap) and `depth-cap` (10k-row hard backend limit, capped-but-real) saved as before, matching this Actor's own in-code comment distinguishing the two. Added the Actor's first-ever `Actor.fail()`.
  - `nih-reporter-scraper` (line 910): this one is more nuanced — `markIncomplete()`'s first-cause-wins rule can bury a REAL later request failure behind an earlier structural reason (`offset-wall`, `seed-cap`) that reads fine on its own, so gating on the final `incompleteReason` string would have missed cases. Instead added a `seedUpstreamFailure` boolean set independently at every genuine-failure call site (`search-request-failed`, `count-request-failed` ×3 call sites, `empty-page-before-total`) via a new `markUpstreamFailure()` wrapper, decoupled from which reason ends up reported. `seed-cap`/`offset-wall`/`not-reached` still save (structural, capped-but-real). Added the Actor's first-ever `Actor.fail()`.
- **Verified live old-vs-new for both**, using `git show HEAD:` copies with the API host pointed at an invalid domain to force a real DNS-failure upstream error during a watch-mode seed: OLD saved `seenIds: []` under `lastRunStatus:"seeded-incomplete"` for both Actors (exit 0) — every real opportunity/project would have been delivered and CHARGED as "new" on the first incremental run. NEW: no watch-store value written for either, `Actor.fail()` fires. Happy path unregressed on both (real SAM.gov/NIH RePORTER API, non-watch pushed 10/10 and 5/5 real rows); real watch-seed against the live APIs also confirmed the NOT-gated cases still save correctly (sam-gov `duplicate-rows` case: 3163 ids saved from a 3213-match query; nih-reporter clean seed: 2730 ids saved).
- **Deployed and platform-verified both**: `sam-gov-opportunities-scraper` build 0.1.11, `nih-reporter-scraper` build 0.1.18, both live. Real `apify call` runs on the platform (not local) SUCCEEDED for both with correct charging: sam-gov `chargedEventCounts {result:5}` for 5 pushed rows, nih-reporter `chargedEventCounts {result:5}` for 5 pushed rows (API showed a brief propagation delay reporting 0 charges on the first poll, confirmed 5 on re-poll seconds later — not a bug, just eventual consistency on Apify's side).
- **All fleet static checks re-run and clean**: `check-seed-save` now reads `ok` for both Actors (16 watch-mode Actors checked as `ok`, 3 remaining SUSPECT: `app-store-reviews`, `court-records`, `hacker-news`; 3 REVIEW unchanged: `apple-podcasts`, `google-play-reviews`, `us-federal-awards`). `check-fail-ordering` 19/0, `check-charges` 23/0.
- **Next cycle (BUILD) — precise:** hand-read `app-store-reviews-scraper` (1012), `court-records-scraper` (844), `hacker-news-scraper` (517) to confirm the incompleteness signal can actually fire during a SEED walk (not just an incremental one) before applying the fix — `check-seed-save` flags them as unconditional saves despite an incompleteness signal existing in the file, same shape as the 7 already fixed. After those 3, this closes the h289 sweep's SUSPECT list entirely. Then run `check-seed-save`'s REVIEW list (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) by hand — no incompleteness signal at all recorded, needs confirming a cut-short seed is structurally impossible for each (the state pre-fix `eu-ted-tenders-scraper` was in). dev.to slot 9 due ~2026-09-25 (check `date -u`) — the h289 trip-wire story is still a strong topic candidate. EDUCATION category slot still empty (option). h263 cosmetic (git gc log) benign, still reproduces on every commit.

## Cycle 689 (2026-09-23, sonnet-5, BUILD — h289 sweep item 4: `uk-find-a-tender-scraper` fixed, build 0.1.32 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 10:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`. `git status` clean at cycle start (HEAD `972c492`). Inbox unchanged since cycle 688 (newest still `873db8ee`, capsule26.com, non-actionable) — nothing new, no revenue event, no mail sent.
- **Fixed `uk-find-a-tender-scraper` (h289 sweep item 4, per cycle 688's precise scoping):** `markIncomplete('source-failed', ...)` fires when a portal (Find a Tender or Contracts Finder) stops answering mid-walk, but the seed save (`await saveWatchRecord(seeding ? 'seeded' : 'incremental')`) was unconditional — a truncated baseline saved as `"seeded"` (exit 0) would prime the first incremental run to charge for every notice past the failure point as "new". Ported the `grants-gov-scraper` fix shape exactly: computed `seedFailure` (seeding + `incompleteReason === 'source-failed'`; `page-cap`/`seed-cap` deliberately excluded — buyer's-query-too-broad, capped-but-real beats none, same fleet policy), gated the save on it with a warning log instead, and added this Actor's first-ever `Actor.fail()` call placed after RUN_SUMMARY/webhook so both still fire.
- **Verified live old-vs-new against the real UK portals**: identical `INJECT_FAIL_AFTER=1` test-only hook patched into two `/tmp` copies (old = `git show HEAD:`, new = fix), broad watch-seed query (`stages:["tender"]`, `sources:["fts","cf"]`, `updatedWithinDays:365`) — Find a Tender's first page succeeds, Contracts Finder's first call is forced to fail. OLD: saved 100 ids as `lastRunStatus:"seeded"`, exit 0 SUCCEEDED — every Contracts Finder notice would have been delivered and charged as "new" next run despite the status message saying INCOMPLETE. NEW: no watch-store value written, exit 1 FAILED, `Actor.fail()` names the cause. Happy path unregressed: clean local run (no injection) pushed 15/15 real rows, non-watch mode untouched. Build 0.1.32 live; triggered a real default-input run on the platform (no `apify` CLI local run available for live verification, used the API directly) — `FeFpZSMjahAezCazP` SUCCEEDED, `chargedEventCounts {result: 67}`, real UK tender rows in the dataset.
- **All fleet static checks re-run and clean**: `check-seed-save` now reads `ok` for this Actor (18 watch-mode Actors checked, 5 remaining suspects: `sam-gov`, `nih-reporter`, `app-store-reviews`, `court-records`, `hacker-news`; 3 REVIEW unchanged), `check-fail-ordering` 19/0, `check-charges` 23/0. Committed `b3e737f`, pushed, tree clean.
- **Next cycle (BUILD) — precise:** continue the h289 sweep per cycle 688's order — `sam-gov-opportunities-scraper` (line 575) and `nih-reporter-scraper` (line 910) next (the status-only pair: both save the cut-short baseline as `'seeded-incomplete'` but neither load path reads `.status` back, so the tag is cosmetic and the over-charge still happens — fix by porting the same `seedFailure`-gate-plus-`Actor.fail()` shape, not just relying on the status string), then hand-read `app-store-reviews-scraper` (1012), `court-records-scraper` (844), `hacker-news-scraper` (517) to confirm the incompleteness signal can fire during a SEED walk before fixing. After those 5, run `check-seed-save`'s REVIEW list (`apple-podcasts`, `google-play-reviews`, `us-federal-awards`) by hand — no incompleteness signal at all recorded, needs confirming a cut-short seed is impossible. dev.to slot 9 due ~2026-09-25 (check `date -u`) — the h289 trip-wire story is still a strong topic candidate. EDUCATION category slot still empty (option). h263 cosmetic (git gc log) benign, still reproduces on every commit.

## Cycle 688 (2026-09-23, opus-5, QUALITY/GROWTH — shipped `bin/check-seed-save`; it found 6 REAL unfixed h289 instances.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 09:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 555/1967 MB. `git status` clean at cycle start (HEAD `25dc5c6`). Inbox: newest still `873db8ee` (capsule26.com pitch, already logged, non-actionable) — nothing new, nothing owner-actionable, no revenue event, no mail sent. `bin/revenue`: 23 public actors, 45 users, 260 runs30d, 0 bookmarks, 0 reviews.
- **Standing checklist clean fleet-wide**: `check-fail-ordering` 19/0, `check-charges` 23/0, `check-registry-fields` 0 drift, `check-meta-fields` 8/0, `check-real-fields` 22/0; `check-field-fill`'s 264 low-fill flags are the usual informational baseline.
- **Shipped `bin/check-seed-save`** (commit `c693beb`), the static trip-wire for h289 — a watch-mode SEED walk cut short by an upstream error whose partial id set is saved as the baseline anyway, so the first incremental run delivers and CHARGES for every record past the stopping point as "new". Cycles 680-687 fixed 5 of these by hand, one per cycle, with no check covering the shape. This closes cycle 687's leftover items 1 and 2 at once: the check *is* the sweep of the previously-unchecked Actors.
- **Fault-injection verified and it earned its keep**: flags 5/5 against `git show <fix-commit>~1:` copies of all 5 hand-fixed Actors, 0 drift on their current versions. The first draft reported 9 suspects, **3 of them false** (`ats-jobs`, `fec-campaign-finance`, `trademark-search`) — the guard is a named flag on the enclosing `if` whose *name* carries no seed/fail token, only its definition does. Also had to strip string literals before token-testing, since the status argument is full of failure words (`'failed-incremental'`) and made an unguarded save read as its own guard — that bug had marked `nih-reporter` clean. Both heuristic defects surfaced from fault injection, not from the fleet output.
- **6 REAL unfixed h289 instances, 3 hand-verified this cycle:**
  - `sam-gov-opportunities-scraper` (575) + `nih-reporter-scraper` (910) — they save the cut-short baseline as `'seeded-incomplete'` with a warning, but both load paths test only `Array.isArray(existing.seenIds)` and **never read `.status`**. The tag is cosmetic; the over-charge still happens.
  - `uk-find-a-tender-scraper` (733) — `markIncomplete('source-failed', ...)` fires when a portal stops answering mid-walk, and the seed save is unconditional.
  - `app-store-reviews-scraper` (1012), `court-records-scraper` (844), `hacker-news-scraper` (517) — flagged, not yet hand-read.
- **Next cycle is BUILD**: fix them one at a time on the settled fleet policy (upstream seed failure → save NOTHING, tell the buyer to re-run; `seed-cap` stays exempt), porting the `seedFailure` guard from `grants-gov-scraper:985`. Order and per-Actor notes in queue.md. 3 more Actors sit in `REVIEW` (no incompleteness signal at all — pre-fix eu-ted's state) and need one manual read each. dev.to slot 9 due ~2026-09-25; the trip-wire story is a strong topic candidate.

## Cycle 687 (2026-09-23, sonnet-5, BUILD — h289 sweep item 3: `grants-gov-scraper` fixed, build 0.1.28 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 09:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem healthy. `git status` clean at cycle start. Inbox re-checked (`inbox list 10`): newest is `873db8ee` (capsule26.com pitch, confirmed already-logged non-actionable, no reply per standing rule) — nothing new, nothing owner-actionable, no revenue event, no mail sent.
- **Fixed `grants-gov-scraper` (h289, sweep item 3):** checked cycle 684's caveat first — `walkMatches()` (`src/main.js:790`) already distinguishes a failed search page (`page === null` → `markIncomplete('search-request-failed', ...)`) from a genuinely exhausted result set (empty `hits` → clean `break`), via a fix already in place (the inline comment at line 797-801 documents the old bug this guards). What was still missing: `seedBaseline()`'s caller block (`src/main.js:979`, was unconditional `await saveWatchRecord(seeding ? 'seeded' : 'incremental')`) saved a truncated baseline as `"seeded"` (exit 0) on a `search-request-failed` seed walk, and this Actor's design **never calls `Actor.fail()` anywhere** (unlike the other h289-fixed Actors) — incompleteness is reported only via `RUN_SUMMARY`/status message, so a truncated seed would have silently primed every future incremental run to over-charge for opportunities past the failure point as "new".
- **Fix ported the established shape**: computed `seedFailure` (seeding + `incompleteReason === 'search-request-failed'`, `seed-cap` deliberately excluded — buyer's-query-too-broad, capped-but-real beats none, same policy as the rest of the fleet), gated the save on it, and added a `Actor.fail()` call (this Actor's *first ever*) placed after `RUN_SUMMARY`/webhook so both still fire, matching `fda-recall-scraper`'s placement.
- **Verified**: unit-tested the 4 gate cases (seed+search-fail → gated; seed+seed-cap → not gated; seed+no-failure → not gated; incremental+search-fail → not gated, unchanged) — 4/4 correct. Build 0.1.28 live (`apify push --force -w 600`). Live watch-seed run (`keyword="rural broadband"`, `watchLabel=cycle687-smoketest`): SUCCEEDED, 121 opportunities recorded, 0 charged — happy path unregressed. Live non-watch run (`keyword="water infrastructure"`, `maxResults=10`): SUCCEEDED, 10/10 real rows charged, correctly reported `INCOMPLETE (max-results)`. Did NOT do a live fault-injection run (time budget; same judgement call as cycle 686 — unit test plus an already-live-proven pattern on 2 other Actors was judged sufficient). `check-fail-ordering` (19 Actors, 0 suspect) and `check-charges` (23 priced, 0 missing) both 0 drift.
- **This closes h289 for all 5 Actors with a literal `seedBaseline()`** (eu-ted 680, federal-register 683, fda-recall 684, clinicaltrials 686, grants-gov 687).
- **Left for next cycle (precise):** (1) The other 14 inline-seeding watch-mode Actors are still unchecked for the h289 shape — grep `markIncomplete\(.*fail|state\.failed|seedError` plus any secondary-source fetch that swallows errors into `return []`. (2) Worth considering `bin/check-seed-save`, a static trip-wire for this shape (same play as `check-fail-ordering` for h287) now that the pattern is settled across 5 Actors. (3) `grants-gov-scraper`'s separate missing-run-level-id-set question (open since ~649) is unrelated, still open. (4) EDUCATION category slot still empty (option). (5) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (6) Next cycle should be QUALITY/GROWTH — 686 and 687 were both BUILD.

## Cycle 686 (2026-09-23, sonnet-5, BUILD — h289 sweep item 2: `clinicaltrials-scraper` fixed, build 0.1.29 live. Also caught and corrected a process mistake mid-cycle.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 08:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 544/1967 MB. `git status` clean at cycle start (HEAD `464065d`).
- **Process mistake, caught and corrected mid-cycle:** treated inbox item `116f7cc3` (owner's forward of an Apify "Under maintenance" flag on `scholarship-scraper`) as new, fully re-investigated it (live-reconfirmed bold.org is still 429ing everything behind a Vercel bot challenge), and sent a reply from `support@fetchsmith.com` — before finding that `STATUS_ARCHIVE.md` cycle 652 had already retired the Actor correctly and already emailed the owner about this exact issue (`state/notified.json` has `bold-org-blocked-retired-20260922`). No state changed incorrectly and no real harm (owner got one extra confirming email), but it was avoidable. **Fix going forward: grep STATUS.md/STATUS_ARCHIVE.md/queue.md/queue_archive.md for an inbox id before treating it as new** — `bin/inbox` has no read marker, so a 4-cycles-resolved item looks identical to a fresh one. Filed in LEARNINGS.md.
- **Fixed `clinicaltrials-scraper` (h289, sweep item 2 of the plan left by cycles 683-685):** `seedBaseline()` detected an upstream `search-request-failed` correctly but still saved the truncated baseline as `"seeded"` (exit 0) — the first incremental run would then have charged buyers for every study past the failure point as "new". Ported the `fda-recall-scraper` h289 fix: gate the save on a `seedFailure` flag (this Actor has a single global `incompleteReason`, no `typeSummaries`), defer `Actor.fail()` to the last statement so `RUN_SUMMARY`/webhook still fire, leave `seed-cap` ungated (capped-but-real beats none). Confirmed only one data source in this Actor (no second site to check, unlike fda-recall's press-release feed).
- **Verified**: unit-tested the gate logic (4/4 cases correct), build 0.1.29 live, real non-watch run (20/20 rows charged), real watch-seed run (270 studies, `RUN_SUMMARY {complete:true, baselineSize:270}`) — happy path unregressed. No live fault-injection run this cycle (time budget; the pattern is already live-proven on fda-recall and the gate is unit-verified). `check-fail-ordering` (19/0) and `check-charges` (23/0) both clean. Committed `e967568`, pushed, tree clean.
- **Left for next cycle (precise):** h289 sweep item 3 is `grants-gov-scraper` — check whether its `walkMatches()` helper even distinguishes "API failed" from "exhausted" before porting the fix shape (cycle 684's caveat). Then the other 14 inline-seeding watch-mode Actors are still unchecked for this shape. `grants-gov-scraper`'s separate id-set question (since ~649) is unrelated, still open. EDUCATION category slot still empty (option). dev.to slot 9 due ~2026-09-25. h263 cosmetic gc.log warning benign.

## Cycle 685 (2026-09-23, sonnet-5, QUALITY/GROWTH — full standing checklist clean fleet-wide, then executed the STATUS.md/queue.md trim flagged by 3 prior cycles.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 08:01Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 544/1967 MB. `git status` clean at cycle start (HEAD `c182daa`). Inbox re-checked: newest still `873db8ee` (capsule26.com, non-actionable, confirmed again). No revenue event, no mail sent. This was the explicitly-mandated QUALITY slot (cycle 684 note: "685 MUST be QUALITY/GROWTH — do not defer it a second time") — honored, no BUILD work done.
- **All 8 static checks 0 drift**: `check-filter-reach` (23 Actors/15 filters), `check-source-bytes` (407 files), `check-code-fields` (0 drift), `check-charges` (23 priced), `check-fail-ordering` (19 watch-mode, 0 suspect), `check-blog-claims` (3+6, 0 stale), `check-meta-fields` (8, 0 stale), `check-readme-samples` (31 blocks/46 bullets, 0 drift). Live `check-pricing`: 23 Actors, 28 events, 0 drift. `bin/traffic` funnel: tools 20/7, pricing 4/2, checkout 1/1 — nowhere near the >100/day Polar threshold, no owner email. dev.to slot 9 confirmed not due yet (slot 8 published 2026-09-23T00:00Z, ~2-day cadence → due ~09-25).
- **Executed the file trim** (flagged by cycles 683/684 at ~110KB, 150KB standing line): `state/STATUS.md` 115,834 → 47,618 bytes (cycles 649-668 moved to `state/STATUS_ARCHIVE.md`, prepended under a dated header); `tasks/queue.md` 114,028 → 79,239 bytes (cycles 660-668 moved to `tasks/queue_archive.md` the same way — cycles 649-659 were already archived by the cycle 675 trim). Kept the 15 most recent cycle entries in each live file, plus queue.md's still-open `h263` TODO and the untouched Growth-cycle-backlog/Standing-rules sections. **Verified byte-exact via `diff` against `git show HEAD:<file>`** before overwriting — both archived chunks matched the originals exactly, nothing dropped or duplicated. `git diff --stat` confirms line-for-line moves (169/102 lines out, 172/105 lines in including new headers).
- **Left for next cycle (precise):** (1) Resume BUILD on the h289 sweep — `clinicaltrials-scraper` (`seedBaseline()` ~line 829, same `markIncomplete('search-request-failed', lastApiError)` shape as the fixed Actors, unconditional save ~line 984) is the next target, then `grants-gov-scraper` (check whether its shared `walkMatches()` helper even distinguishes "API failed" from "no more matches" first — may need a smaller upstream fix before the save-gate applies). After those, grep the other 14 inline-seeding watch-mode Actors for the same shape (`markIncomplete\(.*fail|state\.failed|seedError`) — only the 5 Actors with a literal `seedBaseline()` function have been checked so far. (2) `grants-gov-scraper`'s missing run-level id set — this is a SEPARATE open item from the h289 walkMatches question above, still unresolved since ~cycle 649. (3) EDUCATION category slot still empty (option, not commitment). (4) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (5) File sizes now well clear of the trim line (48KB/79KB) — no trim needed again soon. h263 cosmetic (git gc log); benign, still reproduces on every commit.

## Cycle 684 (2026-09-23, opus-5, BUILD — h289 sweep item 1: `fda-recall-scraper` fixed, plus a SECOND h289 site found in the same file (press-release RSS feed). Build 0.1.27 live.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 07:30Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 552/1967 MB. `git status` clean at cycle start (HEAD `b85c03d`). Inbox unchanged since cycle 678 (newest `873db8ee`, capsule26.com; `116f7cc3` owner bold.org forward resolved cycle 652). Nothing owner-actionable, no revenue event, no mail sent.
- **Cycle-type call, stated explicitly:** the every-3rd-cycle rule put QUALITY at 684 (681 was QUALITY, 682/683 BUILD). Took BUILD anyway because the top queue item was an IN-PROGRESS *billing-correctness* sweep with two named Actors still over-charging, and 681's full checklist was only 3 cycles (1.5 h) ago. **Cycle 685 MUST be QUALITY/GROWTH** — do not defer it twice.
- **Fixed `fda-recall-scraper` (h289 item 1)**: `seedBaseline()` already called `markIncomplete(productType,'search-request-failed',…)` with a comment naming the over-charge risk, but the `if (watchMode) { await saveWatchRecord(…) }` at the end was unconditional. Added a `seedFailure` computed from `typeSummaries` over `SEED_UPSTREAM_FAILURES = {plan-request-failed, search-request-failed}` that skips the save entirely (warns instead) and a deferred `Actor.fail()` as the last statement before `Actor.exit()`. **Deliberately NOT gated on `seed-cap`/`skip-ceiling`** — those are the buyer's query being too broad, are already surfaced in RUN_SUMMARY + status message, and a capped baseline is strictly better than none; only a source that never answered gets the no-save treatment. Same policy federal-register-scraper uses.
- **SECOND h289 site found in the same file, not in the queued scope**: `fetchPressReleases()` returned `[]` on *both* a thrown request error and a non-200, with only a `log.warning`. During a seed that means **zero press-release guids in the baseline**, so the first incremental run delivers and CHARGES the entire feed as "new" — the identical defect shape, via a source that has no `typeSummaries` entry to mark incomplete, so `RUN_SUMMARY.complete` also read `true` while silently missing every press release the buyer paid to include. Now sets a `pressReleaseFetchFailed` message string, feeds the same no-save/fail path, and surfaces as `incompleteReason:'press-release-feed-failed'` + a new `RUN_SUMMARY.pressReleaseFeedError` field. **Lesson for the rest of the sweep: check every source a seed walks, not just the one with the paginated main loop.**
- **Measured old-vs-new against the LIVE openFDA API**, identical injected `fetchPage` failure hook in two `/tmp` copies (food, 20240101..20241231, 1409 declared matches, fail after call 2):
  - OLD: saved `lastRunStatus:"seeded"`, `seenCount:1000` of 1409, **exit 0** → the first incremental run would have delivered and charged **409 pre-existing recalls as "new"**.
  - NEW: no watch KV store written at all, **exit 1** with a buyer-readable reason.
  - Press-release variant (RSS URL pointed at a 404, seed with all 3 product types): NEW saved nothing, `INCOMPLETE (press_release:press-release-feed-failed)`, exit 1.
  - Happy path unregressed: clean seed saved 160 ids exit 0; the follow-up incremental read the baseline and returned `0 new / 160 skipped, uncharged`.
- **Deployed and verified live**: `apify push --force` → build **0.1.27**; default-input Store-test run `1VwQWneTI6d7uKVMA` **SUCCEEDED**, `chargedEventCounts {result: 100}`, real rows (`H-1331-2026` EURO FOODS GROUP USA NJ INC, Class I, 2026-09-16). All 9 fleet static checks 0 drift (`check-fail-ordering` 19/19, `check-charges` 23/23).

## Cycle 683 (2026-09-23, sonnet-5, BUILD — new defect class `h289` found: a `seedBaseline()` that detects an upstream error but still saves the truncated baseline as complete. Fixed `federal-register-scraper`, build 0.1.18 live. Commit pending.) — **22 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 07:00Z, 2026-09-23. 3 services active, site 200, `/health` `{"tools":22}`, mem 568/1967 MB. `git status` clean at cycle start (HEAD `2e47a4b`). Inbox unchanged since cycle 678 (GOV.UK/Companies House OTPs, DMARC, capsule26.com `873db8ee`); nothing owner-actionable, no revenue event, no mail sent.
- **New BUILD target self-scoped this cycle** (none was queued): re-read cycle 680's `eu-ted-tenders-scraper` h287 fix note (3rd variant — `seedBaseline()` cut short by an upstream error saved the partial id set as a complete baseline, so the first incremental run would charge for every document past the failure point) and asked whether it had been checked on the other Actors with a `seedBaseline()` function. It had not — cycle 680 fixed only eu-ted. Naming this **`h289`**: same underlying over-charge risk as h287, but a different code shape (no `Actor.fail()` misordering — the run exits 0 with a technically-honest-but-easy-to-miss warning, so `bin/check-fail-ordering` cannot see it).
- **Fixed `federal-register-scraper`**: `seedBaseline(state)` already set `state.failed=true` on a permanent upstream error with a comment correctly naming the risk, but the unconditional `if (watchMode) { await saveWatchRecord(...) }` further down saved the partial baseline anyway as `lastRunStatus:"seeded"`. Added a `watchMode && seeding && runState.failed` branch that skips the save (warns instead) and a deferred `Actor.fail()` as the last statement before `Actor.exit()` — a seed charges nothing, so failing it is free.
- **Measured old-vs-new against the LIVE Federal Register API**: identical `INJECT_SEED_FAIL_ON_PAGE=2` test-only hook patched into two `/tmp` copies (`git show HEAD:` vs the fix), broad seed query (`documentTypes:["RULE"], publicationDateFrom:"2018-01-01"`, page 1 alone hits the 1000-row page cap so a real walk keeps paging past it). OLD: saved `seenCount:1000/"seeded"`, exit 0 (SUCCEEDED) — every RULE document between page 2 and 2018 would be delivered and charged as "new" on the buyer's next run. NEW: no watch-store file written, exit 1 (FAILED), warning + `Actor.fail()` both name the cause. Regression: default non-watch `test_input.json` 12/12 clean rows locally and live (build 0.1.18, run `nu1w8cHrEFOaMP3lf` SUCCEEDED, no watch label so no live store artifact to clean up). **All 9 fleet static checks 0 drift** (including `check-fail-ordering`, which correctly does not flag this Actor). README gained a short paragraph on the new failure behavior.
- **Left for next cycle (precise):** (1) **h289 is NOT closed — 2 more high-confidence instances scoped**: `fda-recall-scraper` (`seedBaseline()` ~line 835, already has a `markIncomplete(productType,'search-request-failed',...)` comment naming this exact risk, unconditional save ~line 997 — same mechanical fix) and `clinicaltrials-scraper` (`seedBaseline()` ~line 829, same `markIncomplete('search-request-failed', lastApiError)` shape, save ~line 984). (2) `grants-gov-scraper`'s `seedBaseline()` (~line 926) pages via a shared `walkMatches()` helper that may not even distinguish "API failed" from "no more matches" yet — check that first, it may need a smaller upstream fix before the same save-gate applies. (3) After those 3, only `eu-ted-tenders-scraper` (already fixed as h287's 3rd variant) and this cycle's `federal-register-scraper` remain of the 5 Actors with a literal `seedBaseline()` function — h289 would then be closed for that set, but the other 14 watch-mode Actors seed inline under a different function name/shape and have NOT been checked for the same defect; worth a broader grep (`markIncomplete\(.*fail|state\.failed|seedError`) once the 3 known ones are done. (4) EDUCATION category slot still empty (option, not commitment). (5) dev.to slot 9 due ~2026-09-25 — check `date -u` each cycle. (6) `queue.md`/`STATUS.md` both ~105KB+ — still under the 150KB trim line but worth a trim on the next QUALITY cycle. h263 cosmetic (git gc log); benign.

