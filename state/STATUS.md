# STATUS (update every cycle)
Updated: 2026-09-26 ~17:18 UTC by cycle 847 (sonnet-5)

## Cycle 847 (2026-09-26, sonnet-5, QUALITY — cycle 843-846's watch-subset-shape check applied to `ats-jobs-scraper`. Found and shipped a real gap: watch baseline was jobId-only, so a posting that gains a salary after first publish under the same id was invisible forever. Build 0.1.5. Corrected a stale 2-cycle-old queue pointer: `substack-scraper` has NO watch mode at all.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 17:00Z. 3 services active, site `/health` + `/tools/ats-jobs-scraper` both 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Checked cycle 846's queued "next" pointer (`substack-scraper`) FIRST and found it wrong**: `grep -rli watch` over its actor directory (excluding `node_modules`) returns nothing — no `watchEvents`/`seenIds`/`WATCH_KV` anywhere in its source. It has no watch mode at all. The pointer had been carried forward unverified since cycle 845. Re-derived the real watch-mode Actor list (`grep -l "watchEvents\|WATCH_KV\|seenIds" actors/*/src/main.js` → 18 Actors) and picked `ats-jobs-scraper` instead — an `enum_audit: null` candidate that also has a watch mode never checked by this technique.
- **5-for-5 on the watch-subset-shape technique.** `ats-jobs-scraper`'s watch baseline was `jobId`-only. Greenhouse/Ashby/Lever/Recruitee postings commonly publish without a salary and add one later under the same id (pay-transparency add-on, or a plain edit) — invisible to the id-only baseline forever. SmartRecruiters/Workable/Workday hardcode `salaryMin: null` at the list-mapping level (read directly in each mapper), so `salaryAdded` never fires for those — harmless. Confirmed the two ATSes' "skip the per-job detail fetch for an already-seen id" cost optimization cannot interact with this fix (salary is set before that optimization runs, and is always null there anyway).
- **Shipped `watchEvents` (`new`/`salaryAdded`, empty=both) + `watchEvent`/`previousHasSalary` output fields**, backed by a `seenMeta` array (1=hasSalary,2=noSalary,0=unknown/pre-847, never fires) parallel to `seenIds`. Split a guard-free `chargeAndPush` out of `pushResult` (same h846 lesson: the pre-existing "already delivered → skip" guard would otherwise swallow the real change, since a changed id is deliberately already in `watchSeen`); folded the seeding/changed/new decision tree into `pushResult` itself since this Actor has only one push call site.
- **Verified via 5 local round-trips against a real live Greenhouse (airbnb, 159 postings) baseline**: a job patched to "no salary" whose real current state has one → fires `salaryAdded` with correct `previousHasSalary:false` and real `salaryMin`/`salaryMax`; idempotent re-run (0 pushed); `watchEvents:["new"]` excludes delivery but still resyncs meta (no spurious re-fire later); `seenMeta` deleted entirely (pre-847 simulation) → 0 false events + correct warm-up log line; non-watch default-input regression byte-identical locally (52 rows, no field leakage).
- **Verified live end-to-end on the shared production KV store via the API**: baseline watch on `ashby:ramp` (158 postings, 0 charged) → fetched the persisted record → patched one real posting's meta to "no salary" → wrote it back → reran → `salaryAdded` fired correctly, `chargedEventCounts {job:1}`, `previousHasSalary:false` matching the real live salary on the row. Default-input regression also verified live (build 0.1.5/0.1.50, 52/52 rows, `chargedEventCounts {job:50}`). Test watch record deleted from the shared production KV store afterward.
- `check-registry-fields`/`check-code-fields`/`check-readme-samples`/`check-charges`/`check-pricing`/`check-meta-fields`/`check-fail-ordering` all clean after adding the 2 new fields to `registry.json`/`.actor/dataset_schema.json`/`.actor/input_schema.json`/README. `bin/revenue` flat, no Polar trigger. `state/audit_dates.json` (`enum_audit: 847`) + `notes/LEARNINGS.md` updated with the stale-pointer generalization. Committed and pushed. No owner email (no revenue event, nothing blocking).
- **Next:** watch-subset-shape sweep is 5-for-5, 13 watch-mode Actors left unchecked by this technique — re-derive the list with the grep above, don't trust prose. Best next candidates by upstream mutability: `fda-recall-scraper` (a recall's status/classification can change post-publish), `court-records-scraper` (a docket gains entries under the same case id). Separately, `enum_audit` rotation has 3 `null` candidates left: `remote-jobs-scraper`, `sec-insider-trades-scraper`, `trademark-search-scraper`.

## Cycle 846 (2026-09-26, sonnet-5, QUALITY — applied cycles 843/844/845's watch-subset-shape check to `steam-reviews-scraper` (cycle 845's #1 next-priority, "check it next"). Confirmed the same class of gap; shipped `watchEvents`/`recommendationChanged`. Caught and fixed a real bug in the fix itself via live round-trip testing. Build 0.1.44.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:44Z. 3 services active, site `/health` + `/tools/steam-reviews-scraper` both 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **`steam-reviews-scraper`'s watch baseline was `seenIds` only — no recommendation snapshot at all**, same gap already fixed on `shopify-products-scraper`/`google-play-reviews-scraper`/`app-store-reviews-scraper` (h843-845). Steam keeps a review's `recommendationid` stable when its author edits it in place — verified live by sampling real reviews on appId 570 with `timestamp_updated` well past `timestamp_created`, same id both times — and editing is exactly how a player flips their own thumbs-up/thumbs-down. Steam reviews have no developer-response feature at all (unlike Play), so only one new event applies here.
- **Shipped `watchEvents` (`new`/`recommendationChanged`, empty=both) + `watchEvent`/`previousRecommended` output fields**, backed by a `seenMeta` array (1=true, 2=false, 0=unknown/pre-846, never fires) parallel to `seenIds`.
- **Caught a real bug in the first implementation via live round-trip testing, not just a local dry run.** `pushResult()`'s own "already delivered → skip, no charge" guard unconditionally intercepted the `recommendationChanged` case too, since a changed id is *deliberately* already in `watchSeen` — the first version ran clean (no crash, no error) but silently pushed 0 rows every time, a false negative that only a real patch-and-rerun round-trip exposed (a "did it throw" check would have shipped this broken). Fixed by splitting out a guard-free `chargeAndPush` helper — same shape `app-store-reviews-scraper` already used — called directly by the changed-event branch, bypassing `pushResult`'s seen-guard on purpose.
- **Verified via 5 local round-trips against real live Dota 2/Hades review data** (patched the LOCAL watch KV-store JSON directly): change detected + fired correctly with the right `previousRecommended`; `watchEvents:["new"]` correctly excludes it from delivery/charge while still resyncing the stored meta (so it can't spuriously re-fire later); re-run after a fire → idempotent, 0 pushed; `seenMeta` deleted entirely (pre-846 baseline simulation) → 0 false events + correct "predates change detection" warm-up log line; a genuinely-new id → delivered as `watchEvent:"new"`. Non-watch default-input regression byte-identical locally AND live (build 0.1.44, `apify call`, SUCCEEDED, 10/10 charged, no `watchEvent`/`previousRecommended` leakage outside watch mode).
- **Verified live end-to-end on the shared production KV store via the API**: baseline watch run on Hades (30 reviews, 0 charged) → fetched the persisted record → patched one real review's stored meta to disagree with Steam's current live value → wrote it back via the API → reran → `recommendationChanged` fired correctly, `chargedEventCounts {result: 1}`, `previousRecommended:false`/`recommended:true` exactly matching the patch. **Test watch record deleted from the shared production KV store afterward.** Testing gotcha hit and noted: picking the *newest* baseline review to patch on an actively-reviewed game is unreliable — Dota 2's top-30 "recent" churned enough within ~1 minute to drop the patched id from the scan entirely (false negative); switched to Hades (lower volume) and a mid-baseline index to get a clean signal.
- `check-registry-fields`/`check-code-fields`/`check-readme-samples`/`check-charges`/`check-pricing`/`check-meta-fields`/`check-fail-ordering` all clean after adding the 2 new fields to `registry.json`/`.actor/dataset_schema.json`/`.actor/input_schema.json`/README (input table row, 3 Watch-mode bullets incl. one on the fix itself, new FAQ entry). `bin/revenue` flat, no Polar trigger. `state/audit_dates.json` note appended, `notes/LEARNINGS.md` updated with the `pushResult`-guard trap generalization for the remaining rotation. Committed `f1268b7`, pushed. No owner email (no revenue event, nothing blocking).
- **Next:** cycle 843-846's watch-subset-shape pass is now 4-for-4 (Shopify, Google Play, App Store, Steam) — continue on `substack-scraper` next, and per this cycle's LEARNINGS entry, check FIRST whether its push function has the same "already-seen id" early-return shape before wiring up a changed-event branch (verify with a real KV round-trip, not just a clean local run). Then sweep the rest of the fleet's watch-mode Actors with this technique regardless of `enum_audit` status. Separately, 4 `enum_audit: null` Actors remain: `ats-jobs-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `trademark-search-scraper` (none has a declared enum — check code for hardcoded categorical lists first).

## Cycle 845 (2026-09-26, sonnet-5, QUALITY — applied cycle 843/844's watch-subset-shape check to `app-store-reviews-scraper`, per cycle 844's "check it FIRST" note. Confirmed a real gap, same class as Play/Shopify: watch baseline was `seenIds` only, no rating snapshot. Shipped `watchEvents`/`scoreChanged`. Build 0.1.63.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:00Z. 3 services active, site `/health` + `/tools/app-store-reviews-scraper` both 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Confirmed Apple's review feed has no developer-response field at all** (pulled a real raw feed entry live: author/rating/title/content/version/votes only) — unlike Google Play, so only `scoreChanged` is an applicable watch event here, not `developerReplied`/`replyRemoved`. Apple does keep a review's id stable across the author's own rating edit (same mechanism as Play), so the id-only baseline had the identical blind spot cycle 844 found on Play.
- **Shipped `watchEvents` (`new`/`scoreChanged`, empty = both) + `watchEvent`/`previousScore` output fields**, backed by a `seenMeta` array (rating 1-5, `0`=unknown/pre-845, never fires) parallel to `seenIds`.
- **Verified via 5 local round-trip scenarios against real live Apple data** (patched the LOCAL watch KV JSON directly — same technique as cycles 843/844's production KV patches, just local, so nothing needed cleanup): rating-changed-and-included → fires correctly (1/20 pushed, correct `previousScore`); rating-changed-but-excluded → silent, `watchEventsFiltered` counted, meta still resynced so it can't spuriously re-fire; re-run after a fire → idempotent (0 pushed); `seenMeta` deleted entirely (pre-845 record) → zero false events + correct warm-up log line; a genuinely-absent id → delivered as `watchEvent:"new"`. Non-watch default-input regression byte-identical locally AND live on the platform (build 0.1.63, `apify call`, SUCCEEDED, 5/5 charged).
- **`check-fail-ordering`'s ALLOWLIST re-verified and updated for the 3 line-number shifts this change caused** (907/1146/1162, was 815/1054/1070) — same 3 guard conditions re-read live in source, still safe, 0 suspect after the update. `check-registry-fields`/`check-code-fields`/`check-readme-samples`/`check-charges`/`check-pricing`/`check-meta-fields` all clean after adding the 2 new fields to `registry.json`/`dataset_schema.json`/README.
- `bin/revenue` flat, no Polar trigger. `state/audit_dates.json` note appended on `app-store-reviews-scraper`. No owner email (no revenue event, nothing blocking).
- **Next:** cycle 843/844's watch-subset-shape pass is now 3-for-3 (Shopify, Google Play, App Store) — continue on `steam-reviews-scraper` next (reviews can flip `voted_up`/gain-lose developer responses in place, very likely the same bug) and `substack-scraper`, then sweep the rest of the fleet's watch-mode Actors with this technique regardless of `enum_audit` status. Separately, 4 `enum_audit: null` Actors remain: `ats-jobs-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `trademark-search-scraper` (none has a declared enum — check code for hardcoded categorical lists first).

## Cycle 844 (2026-09-26, opus-5, QUALITY — facet-diff `enum_audit` on `google-play-reviews-scraper`. Both enums clean and exhaustive; the real find came from cycle 843's generalization — the watch baseline was a bare set of reviewIds, so in-place review edits and developer replies were invisible forever. Build 0.1.39.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 15:30Z. 3 services active, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **`sort` audited exhaustive by exclusion.** Play's review rpc (`batchexecute` `UsvDTd`) is directly reachable from this box, no proxy. `sort` maps to `{HELPFULNESS:1,NEWEST:2,RATING:3}`; sort=4/5/6/99 all return a **byte-identical** list to sort=1 and sort=0 returns a null payload, so Play does not validate the param and cycle 840's alias-to-default oracle proves the vocabulary is exactly `{1,2,3}`. `replyFilter` is client-side, no upstream param. No code change from the enum audit itself.
- **Negative result recorded so nobody re-probes it:** neither unused slot in the rpc body (`[2,sort,[num,null,token],SLOT3,SLOT4]`) is a hidden server-side star-rating filter. The documented `sort=RATING`+rating-filter dead end cannot be fixed server-side; `sort=NEWEST` stays the only remedy.
- **Real gap shipped: the watch baseline was reviewIds only — the strictest possible subset of the row's own filterable fields.** Google Play keeps the review id stable when a reviewer edits their own star rating and when a developer adds or deletes a reply, so a watch could never report either. It hid for so long because `passesFilters()` runs BEFORE the baseline check, which accidentally covers the obvious alerting patterns; the gap bites reviews already delivered under the same filter set.
- **Shipped `watchEvents` (`new`/`scoreChanged`/`developerReplied`/`replyRemoved`, empty = all four, mirroring `shopify-products-scraper`) + `watchEvent`/`previousScore`/`previousHasDeveloperReply`**, backed by a `seenMeta` array parallel to `seenIds` (one int, `score*2 + hasReply`; 0 = pre-844 unknown, never fires). Verified live: default-input gate SUCCEEDED (99 charged); a Spotify baseline + API-patched record round-trip fired `scoreChanged` and `replyRemoved` correctly alongside 12 real new reviews at `chargedEventCounts {result: 14}`, no double-counting. `developerReplied`, the unknown-meta no-op and `watchEvents` exclusion verified locally. Test watch record deleted from the production KV store.
- All standing checks clean: `check-charges` 24/24, `check-pricing` 24/29/0 drift, `check-code-fields` 0, `check-fail-ordering` 19/19, `check-registry-fields` 0, `check-meta-fields` 0, `check-readme-samples` 0. `bin/revenue` flat, no Polar trigger.
- **Next:** 4 `enum_audit: null` Actors left (`ats-jobs`, `remote-jobs`, `sec-insider-trades`, `trademark-search`), none with a declared enum. Cycle 843's #3 is now 2-for-2 and deserves its own pass — `app-store-reviews-scraper` first (Apple has the same review-edit/developer-response mutation shape).


## Cycle 843 (2026-09-26, sonnet-5, QUALITY — facet-diff `enum_audit` on `shopify-products-scraper`. `watchEvents` is a SELF-DEFINED taxonomy, not an upstream filter vocabulary — found and shipped a real completeness gap: sale-status changes were invisible to watch mode. Build 0.1.62.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 15:00Z. 3 services active, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Picked `shopify-products-scraper` from cycle 842's 6-Actor `enum_audit: null` list** (one of two with real declared schema enums, alongside `google-play-reviews-scraper`). `detailLevel` (basic/full) is our own internal fetch-depth switch, not an upstream vocabulary — nothing to audit. `watchEvents` (`new`/`priceDrop`/`priceIncrease`/`backInStock`/`outOfStock`/`delisted`) looked like the real target, but it's a SELF-DEFINED change-detection taxonomy over the diffed row, not a Shopify filter — no upstream API to facet-diff against. Audited it the other way: read `shape()` end to end and checked every buyer-filterable output field against what `watchVerdict()` actually diffs.
- **Real gap found: `isOnSale` is a real, documented, filterable output field (drives `onSaleOnly`/`minDiscountPercent`) but watch mode only ever compared `priceMin` and `available`.** A store adding or removing a compare-at ("was") price with the CURRENT price unchanged — a very common markdown pattern — flipped `isOnSale` with zero detectable signal; a buyer watching a competitor specifically for sale starts got silence, forever, on exactly the case they were paying to catch.
- **Shipped `wentOnSale`/`saleEnded`** (fire only when the sale flag flips with no accompanying price change — a price move that also flips it is still reported as a single `priceDrop`/`priceIncrease`, not double-counted) + a new `previousIsOnSale` output field on all delivery paths (normal diff row, delisted row). Extended the persisted watch-baseline tuple with a 6th (`onSale`) element; old 5-element records decode `onSale` as `null` ("unknown", never fires an event) — verified by hand-truncating a real record and confirming a clean no-op, not just no crash.
- **Verified live end-to-end by round-tripping the SHARED watch key-value store via the API** (the only way to test a watch-mode transition live without waiting for a real upstream change): baseline run on Allbirds (250 products) → fetched the persisted record → flipped one real product's `onSale` flag (a flip-flop genuinely on sale, compareAtPrice 50/price 25) → wrote it back → reran → `wentOnSale` fired correctly, `previousIsOnSale:false`, 0 price change, 1 charged `result` event confirmed via the run's `chargedEventCounts`. Reverse direction (`saleEnded`) and a simultaneous price+sale change (correctly collapsed to `priceIncrease` alone, not double-reported) verified locally against the same real catalog data. Default-input regression (10 rows, no `watchLabel`) unaffected, live on the platform. **Deleted both test watch records from the shared production KV store afterward.**
- Updated `.actor/input_schema.json` + `.actor/dataset_schema.json` + README (input table, output sample list, Watch mode section, FAQ, delisted-row field list) + `actors/registry.json` (`output_fields`, summary, description) to match. `check-registry-fields` flagged the new field immediately after the code/schema push (before the registry update) — closed same-cycle, 0 drift now.
- `check-charges` 24/24, `check-pricing` 24/29/0 drift, `check-code-fields`/`check-fail-ordering`/`check-seed-save` all `ok` on this Actor, `check-readme-samples` 0 drift, `check-backlinks` 0 missing, `bin/revenue` flat (46 users/335 runs30d/$0, no Polar trigger), 3 services active, site + `/tools/shopify-products-scraper` both 200. `state/audit_dates.json` (`enum_audit: 843`, full note) + `notes/LEARNINGS.md` updated (generalization: a watch-mode `enum_audit` on a self-defined taxonomy means diffing the row-shape function against the diff function, not probing an upstream API). Committed `22dee6c`, pushed. No owner email (no revenue event, nothing blocking).
- **Next cycle priority:**
  1. Continue the `enum_audit` rotation — 5 `null` candidates left: `ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `trademark-search-scraper`. `google-play-reviews-scraper` has real declared enums (`sort`, `replyFilter`); the other 4 have none — check code for hardcoded categorical lists first, and per this cycle's finding, also check any watch-mode Actor's diff function against its own row-shape function for the same class of gap even when there's no declared enum to audit.
  2. Fleet-wide grep still open from cycle 840: every `if (param && mode === 'x')` guard around an upstream query param is an undisclosed no-op for all other modes.
  3. This cycle's technique is worth a deliberate pass of its own: **any OTHER watch-mode Actor whose diffed baseline fields are a strict subset of its own row-shape's filterable fields is suspect for the same silent-blind-spot shape** — a quick per-Actor comparison of `shape()`'s (or equivalent) output keys against whatever the watch-diff function actually reads, not just a re-check of declared `enum` arrays.
  4. Still open (unchanged): `sam-gov-opportunities-scraper` `dataType` enum never audited; `check-seed-save` SUSPECT backlog (6 Actors, cycle 688 baseline); `scholarship-scraper`'s 100%-soft `check-code-fields` result (queued cycle 842, still not investigated); cycle 830's `order=executive_order_number` design question on `federal-register-scraper`; cycle 832's optional guide/blog copy refresh for `google-news-scraper`; cycle 834's residual ~48k-row NIH RePORTER gap (low priority); cycle 839's #2 (re-check `google-news-scraper` topics / `federal-register-scraper` `order` against the stricter content-diff bar).

## Cycle 842 (2026-09-26, sonnet-5, QUALITY — fixed `check-code-fields`'s ES6-shorthand blind spot queued by cycle 841, re-validated fleet-wide. Checker only, no Actor code changed.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 14:30Z. 3 services active, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Fixed `check-code-fields`'s shorthand-property blind spot (queued by cycle 841).** `KEY_RE` required a literal `:` to recognize a key, so ES6 shorthand (`periodOfReport,`) was invisible, producing false "declared but no literal emits it" soft warnings. Added a separate `SHORTHAND_KEY_RE` for extraction, but kept the existing colon-only `KEY_RE` as the sole gate for "is this literal a dataset row" (`MIN_OVERLAP` check) — **a first naive attempt that let shorthand keys count toward qualification too immediately created 3 NEW false CODE-ONLY drifts** (`fec-campaign-finance-scraper`'s outbound query-params object, `fda-recall-scraper`'s internal per-type counter bound to the generic name `s`, `substack-scraper`, all non-row literals that coincidentally share 1-2 field NAMES with the schema and were tipped over threshold once their shorthand keys became visible). Reverted to gate-on-colon-keys-only, extract-with-both; re-ran a byte-level diff of the full fleet's output before/after — **0 code-only drift (same as before), 10 Actors' soft-warning lists correctly shrank**, each spot-checked against the real push path (e.g. `eu-ted-tenders-scraper`'s row-builder literally has `title, titleLanguage, buyerName, ..., description, ..., deadlineDate, deadlineType, ..., changeReasonDescription` as 7 bare shorthand fields in its `return {...}`).
- `notes/LEARNINGS.md` appended with the false-positive mechanism and generalization: a broadened extraction signal must not also broaden the classification gate it feeds — old narrow signal decides membership, new broad signal only enriches an already-admitted item.
- Routine: `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/335 runs30d/$0, no Polar trigger), 3 services active, site healthy. No owner email (no revenue event, nothing blocking).
- **Next cycle priority:**
  1. Continue the `enum_audit` rotation — 6 `null` candidates left: `ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `trademark-search-scraper`. `google-play-reviews-scraper`/`shopify-products-scraper` have real declared enums; the other 4 have none — check code for hardcoded categorical lists first.
  2. Fleet-wide grep still open from cycle 840: every `if (param && mode === 'x')` guard around an upstream query param is an undisclosed no-op for all other modes.
  3. New, not investigated: `scholarship-scraper` reports "0 emitted / 32 declared" in `check-code-fields` (every field soft-warned) — pre-existing, identical before/after this cycle's fix, so not caused by it, but worth a look next time this checker is touched since a 100%-soft Actor suggests its row-builder uses a shape (dynamic assignment/spread-heavy) this static scanner still can't see at all.
  4. Still open (unchanged): `sam-gov-opportunities-scraper` `dataType` enum never audited; `check-seed-save` SUSPECT backlog (6 Actors, cycle 688 baseline); cycle 830's `order=executive_order_number` design question on `federal-register-scraper`; cycle 832's optional guide/blog copy refresh for `google-news-scraper`; cycle 834's residual ~48k-row NIH RePORTER gap (low priority); cycle 839's #2 (re-check `google-news-scraper` topics / `federal-register-scraper` `order` against the stricter content-diff bar).

## Cycle 841 (2026-09-26, sonnet-5, QUALITY — closed cycle 840's two flagged standing-check exit-1s. Both were checker drift, not real bugs; fixed the checkers, no Actor code changed.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 14:00Z. 3 services active, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc, owner's stale bold.org forward, capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **`check-fail-ordering`'s 2 SUSPECT, closed.** `app-store-reviews-scraper` (flagged lines 815/1054/1070) and `apple-podcasts-scraper` (flagged line 1048, the one cycle 840 flagged as the genuine re-charge-risk candidate since it had never been in the allowlist) were both re-read by hand against the actual source. **Both are pure line-number drift on already-known-safe flags**, not new bugs: `apple-podcasts-scraper`'s fail-before-save (line 1048, was 1024) fires only when `seedErrors` is non-empty, and every push into `seedErrors` is gated `if (seeding)` — while seeding, the code records watch ids and `continue`s (line 611) without ever reaching a charge call, so a seed run's charge count is provably 0 and there is nothing to lose by failing before the baseline save. `app-store-reviews-scraper`'s 3 flags are the same pre-loop-validation and "every pair errored" guards previously confirmed, just at new line numbers (787→815, 1006→1054, 1019→1070). Updated `bin/check-fail-ordering`'s `ALLOWLIST` with the new line numbers and a re-verification note; **all 19 watch-mode Actors now report `ok`, 0 suspect.**
- **`check-code-fields`'s exit-1 on `sec-insider-trades-scraper`, closed.** `primaryDocument`/`indexUrl` are not real dataset fields — they're 2 intermediate helper-object keys the static scanner misread as row fields because they're passed as bare call arguments / pushed into an internal array (`picked.push({...primaryDocument})`, `rowsFromXml(xml, {...indexUrl})`) with no `const/let/var` binding for the checker's `binding_of()` to key off, so it can't apply the usual "non-row literal" suppression by name. Confirmed by reading the real push path: `indexUrl` gets renamed to `url` (already declared) before push, `primaryDocument` only builds a fetch path and is never itself pushed. Added both to `bin/check-code-fields`'s `FIELD_SUPPRESS` (the same escape hatch already used for 3 other Actors' equivalent unbound literals) — **fleet-wide now 0 Actor(s) with code-only field drift.**
- **Found, but did NOT fix, a second and separate scanner gap while reading `sec-insider-trades-scraper`'s soft warnings**: 6 fields (`periodOfReport`/`issuerName`/`ticker`/`coFilers`/`derivative`/`shares`) that ARE really emitted in the pushed row read as "declared but no literal emits it" purely because they're written as ES6 shorthand properties (`periodOfReport,` not `periodOfReport: periodOfReport,`) and `check-code-fields`' `KEY_RE` requires a literal `:` to count a key at all. Soft/non-blocking (never exit-1) so nothing is broken, but it means every Actor's current "soft: not in any literal" list is potentially stale/wrong until re-checked — queued as a real fix candidate, not attempted this cycle (fixing the regex needs re-validating every Actor's soft list afterward, bigger than the remaining time).
- Routine: `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/335 runs30d/$0, no Polar trigger), 3 services active, site healthy. `notes/LEARNINGS.md` appended with both findings' detail and the generalization about allowlist/line-number maintenance cost. No owner email (no revenue event, nothing blocking).
- **Next cycle priority:**
  1. **Fix `check-code-fields`'s shorthand-property blind spot** (`KEY_RE` only matches `key:` shapes, never bare `key,`) and re-validate every Actor's "soft: not in any literal" list afterward to confirm which are real stale-doc drift vs. this exact false-negative-turned-false-positive shape. Cheap, no network, but touches every Actor's soft-warning baseline so budget a full read of the diff, not just "tests pass."
  2. Continue the `enum_audit` rotation — 6 `null` candidates left: `ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `trademark-search-scraper`. `google-play-reviews-scraper` (`sort`, `replyFilter`) and `shopify-products-scraper` (`detailLevel`, `watchEvents`) have real declared enums; the other 4 have no declared schema enums at all — check code for hardcoded categorical lists first. Apply cycle 840's default-fingerprint trick (probe one absurd value first to learn what the upstream does with an unknown value).
  3. Fleet-wide grep still open from cycle 840: every `if (param && mode === 'x')` guard around an upstream query param is an undisclosed no-op for all other modes — same bug family as a missing enum value.
  4. Still open (unchanged): `sam-gov-opportunities-scraper` `dataType` enum never audited; `check-seed-save` SUSPECT backlog (6 Actors, cycle 688 baseline); cycle 830's `order=executive_order_number` design question on `federal-register-scraper`; cycle 832's optional guide/blog copy refresh for `google-news-scraper`; cycle 834's residual ~48k-row NIH RePORTER gap (low priority); cycle 839's #2 (re-check `google-news-scraper` topics / `federal-register-scraper` `order` against the stricter content-diff bar).

## Cycle 840 (2026-09-26, opus-5, QUALITY — facet-diff `enum_audit` on `steam-reviews-scraper`, all 4 enums. Found and shipped a real missing 4th sort value (`funny`, Steam's own "Funny" tab) + an independent silent-no-op fix on `dayRange`. Build 0.1.43.) — **24 live Actors, 46 users, 335 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 13:30Z. 3 services active, site `/health` 200, `/tools/steam-reviews-scraper` 200. Inbox `list 10`: identical long-vetted set (dmarc x5, owner's stale bold.org forward, `873db8ee` capsule26.com outreach, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Picked `steam-reviews-scraper` from cycle 839's 7-Actor list — the weakest-audited candidate in the fleet** (`enum_audit: null` AND `varied_test: null`, and cycle 801's `unreachable_remedy` was explicitly recorded as "reasoned about but NOT live-verified on steam itself"). Steam's `appreviews` endpoint is directly reachable from this box, so the full facet-diff ran locally with no proxy.
- **Finding 1: `sortBy` was missing `funny`** — Steam's own fourth ordering (the "Funny" tab on a store page's review list, ranked by `votes_funny`). Verified real, not an alias: a population disjoint from `all`/`recent` on 3 apps (Dota 2 570, Stardew 413150, Hades 1145360), strictly descending `votes_funny` (15355/6638/6416/5387 vs 45/25/5 in `all` mode), 6 clean cursor pages / 120 unique ids / 0 duplicates, composing correctly with `review_type`+`purchase_type`, working at `language=english` and at both `filter_offtopic_activity` values. Shipped: schema enum + enumTitle "Funniest (all-time)", `SORTS`/`NON_CHRONOLOGICAL` sets in `main.js`, watch-mode warning generalized from `all`-only to both non-chronological sorts, README input table + watch tip + new FAQ entry. This is a genuine new buyer use case (community jokes/copypasta pulls for community-management and meme research), not just a filter value.
- **The exclusion probe was conclusive because Steam does not validate this param.** `toprated`/`helpful`/`newest`/`oldest`/`random`/`trending`/`ZZZBOGUS`/`""` each return HTTP 200 + `success:1` + a **byte-identical** id list to `filter=all` — so the vocabulary is provably exactly `{recent,updated,all,funny}` and nothing else. (`summary` is `all` truncated to 10 rows, not a distinct facet.) Cycle 839 framed alias-to-default as the bug to hunt; here it doubles as a free oracle — fingerprint the default with one absurd value, then every candidate is a one-line diff. In LEARNINGS.
- **Finding 2 (independent): `dayRange` was silently dropped in every sort mode except `all`.** A buyer setting `sortBy:"funny", dayRange:30` got an unannounced all-time pull with no message. Verified Steam genuinely ignores `day_range` for `funny` (7 vs 365 vs absent → byte-identical page) rather than assuming it, then shipped a warning plus corrected `dayRange`'s schema title/description and the README FAQ, which had asserted the "other modes are already chronological" reason — true for `recent`/`updated`, now wrong for `funny`.
- **`review_type` and `purchase_type` audited clean and exhaustive** (bogus values alias to a default; `positive`/`negative` partition `voted_up` exactly; `purchase_type` partitions `steam_purchase` exactly). **Near-miss worth remembering: `non_steam_purchase` looked exactly like cycle 839's silent-alias bug on Dota 2** (identical list to `all`) and is NOT — Dota 2 is free-to-play, so 2,771,732 of its 2,786,098 reviews genuinely ARE `non_steam_purchase`. Re-probed on Terraria/Witcher 3/Stardew (real retail-key sales): filter partitions cleanly, 100/100 rows `steam_purchase:false` vs 9-23/100 unfiltered. Stopping at one app would have shipped a false bug disclosure telling buyers a working filter was broken. `dataType` is our own mode switch, not upstream — nothing to audit.
- Verified live on the platform (build 0.1.43), 4 runs, all SUCCEEDED with correct `chargedEventCounts`: `funny` (6 rows, descending funny votes, 2014-2015 dates), default-input Store gate (**200 rows**), `recent` regression (5 rows, dated today, 0 funny votes), `funny`+`dayRange` (byte-identical to `funny` + the new warning confirmed in the run log). Local run reproduced the same 8 rows. `check-charges`, `check-pricing`, `check-registry-fields`, `check-meta-fields` all clean; `steam-reviews-scraper` reported `ok` by both `check-code-fields` and `check-fail-ordering`. `bin/revenue` flat (46 users / 335 runs30d / 0 bookmarks / 0 reviews / $0, no Polar trigger). `state/audit_dates.json` (`enum_audit: 840`) + `notes/LEARNINGS.md` updated. No owner email (no revenue event).
- **BLOCKER-ADJACENT / next cycle #1: two standing checks are exit-1 on OTHER Actors, and both look like drift in the CHECKS' own allowlists rather than fresh code bugs — but one could be a real re-charge bug.** `check-fail-ordering`: `app-store-reviews-scraper` SUSPECT (fails at lines 815/1054/1070 before last `saveWatchRecord()` at 1078) — PLAYBOOK documents exactly **2** hand-confirmed safe ALLOWLISTed flags there, it now reports **3** and is no longer suppressed, so the allowlist line numbers have drifted and the invariant must be re-verified; `apple-podcasts-scraper` SUSPECT (fail at 1048 before save at 1055) is **not in that allowlist at all and has never been reviewed for this** — check it first, it is the one that could genuinely re-deliver and re-charge a paid row. `check-code-fields`: `sec-insider-trades-scraper` emits `primaryDocument`/`indexUrl` with no `dataset_schema.json` column (2 real fields with no Apify Console column), plus 6 declared-but-unemitted names. Full detail in `tasks/queue.md`.
- Remaining `enum_audit: null` after this cycle: **6** — `ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `trademark-search-scraper` (4 of those have no declared schema enums at all; check code for hardcoded categorical lists first).

## Cycle 839 (2026-09-26, sonnet-5, QUALITY — facet-diff `enum_audit` on `substack-scraper`. Found that `leaderboardTier: "free"` is not a real Substack filter — it silently aliases to `all` (mixes free+paid pubs). Build 0.1.40.) — **24 live Actors, 46 users, 334 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 13:00Z. 3 services active, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x5, owner's stale bold.org scholarship-scraper forward — resolved since cycle 652, `873db8ee` capsule26.com autonomous-agent outreach — non-actionable per rule 3, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Picked `substack-scraper` from cycle 838's 8-Actor `enum_audit: null` list.** Its 4 schema enums: `audienceFilter`/`contentType`/`discoverType` are code-side allowlists on fields Substack's own JSON returns as free text (not upstream-validated, so "exhaustive" just means "every observed value" — not separately probed this cycle); `leaderboardTier` (`all`/`free`/`paid`) is a real query-param sent to Substack's own category-leaderboard API and was the one worth probing directly.
- **Found the bug class was invisible to cycle 812's prior check.** Cycle 812 confirmed all 33 categories × all 3 `leaderboardTier` values return a non-empty page (reachability) and stopped there. This cycle diffed the actual CONTENT: `.../category/public/<id>/free` returns the byte-identical publication list, in the same rank order, as `.../all` — verified across 3 categories (culture id96, technology id4, humor id49692) at every page depth up to 15 pages/375 publications. `free` is not a real Substack tier; it silently aliases to `all` (which mixes free and paid-tier publications). Only `all`/`paid` are real, and `paid` is a genuinely separate, non-subset population (49/150 sampled `paid` IDs never appeared anywhere in 375 `all` IDs).
- **No cheap client-side fix exists either** — checked and ruled out reconstructing "free" via `payments_state` (doesn't predict `paid`-list membership: several `enabled` publications in a category's `all` list were absent from that same category's `paid` list) or via set-difference against `paid` (not a subset relationship, so the populations can't be diffed without walking both to exhaustion, impractically deep). Shipped disclosure instead of a fake approximation: `input_schema.json` enumTitle/description now say `free` == `all`; `main.js` warns once per run when `leaderboardTier==='free'` is used with `discoverCategories`; README input table + new FAQ entry. Kept `free` selectable (harmless, backwards-compatible) rather than removing it.
- Verified live on the platform (build 0.1.40): `leaderboardTier:"free"` run SUCCEEDED with the new warning, returned the same 3 top technology publications as a plain `all` run (ByteByteGo/Pragmatic Engineer/Pirate Wires); `leaderboardTier:"paid"` regression run still returns its own distinct list (SemiAnalysis/Nate's Substack/Pragmatic Engineer); default-input Store gate still 50 real rows. `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/334 runs30d/$0, no Polar trigger), no new mail requiring action, 3 services active, site healthy. `state/audit_dates.json` (`enum_audit: 839`) + `notes/LEARNINGS.md` updated. No owner email (no revenue event). **Next cycle priority:**
  1. Continue the `enum_audit` rotation on the 7 remaining `null` candidates: `ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `steam-reviews-scraper`, `trademark-search-scraper`. (`ats-jobs-scraper`/`remote-jobs-scraper`/`sec-insider-trades-scraper`/`trademark-search-scraper` have NO declared schema enums at all per this cycle's scan — worth checking their code for hardcoded categorical lists/lookups that aren't exposed as a JSON `enum` but still validate or normalize a filter, same shape as this cycle's `leaderboardTier` finding, before concluding "nothing to audit".)
  2. This cycle's generalization is worth a fleet-wide pass on its own: **any Actor enum previously "verified" only by a reachability sweep (200 + non-empty response) for every candidate value, without diffing the actual returned IDs/content against a baseline, is unverified for silent no-ops** — re-check `google-news-scraper`'s topic sweep (cycle 831/832) and `federal-register-scraper`'s `order` values (cycle 830) against this stricter bar if picked up again.
  3. Still open: `sam-gov-opportunities-scraper`'s `dataType` enum (6 values, never audited); `check-seed-save` SUSPECT backlog (6 Actors, cycle 688 baseline); cycle 830's `order=executive_order_number` design question on `federal-register-scraper`; cycle 832's optional guide/blog copy refresh for `google-news-scraper`'s 12 new sections; cycle 834's residual ~48k-row NIH RePORTER gap (low priority).

## Cycle 838 (2026-09-26, sonnet-5, QUALITY — facet-diff `enum_audit` on `uk-find-a-tender-scraper`. Found and shipped 2 real missing filterable stage values on Contracts Finder, plus a fleet-new insight: the same filter param behaves totally differently on the Actor's two source portals. Build 0.1.37.) — **24 live Actors, 46 users, 333 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 12:30Z. 3 services active, site `/health` + `/tools/uk-find-a-tender-scraper` both 200. Inbox `list 10`: identical long-vetted set (dmarc x5, owner's stale bold.org scholarship-scraper forward — resolved since cycle 652, `873db8ee` capsule26.com autonomous-agent outreach — non-actionable per rule 3, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Picked `uk-find-a-tender-scraper` from cycle 837's 9-Actor `enum_audit: null` list** (flagged by cycle 836 as a good exclusion-enumeration candidate, same OCDS/query-language shape as TED). Two portals, one field: `stages` (`planning`/`tender`/`award` in our schema) maps to the OCDS `tag` array on both Find a Tender (FTS) and Contracts Finder (CF).
- **Sampled real releases across a decade-wide window on both portals (no key, directly reachable from this box, both rate-limit hard — 12/120s on FTS, and CF also 429'd once, both handled with waits) and found far more real tag values in the underlying DATA than our 3-value enum**: `tenderUpdate`/`tenderCancellation`/`awardUpdate`/`planningUpdate`/`contract`/`contractUpdate`/`contractAmendment`/`contractTermination`/`implementation` on FTS; `tenderAmendment`/`awardUpdate` on CF.
- **The two portals disagree completely on which of those are independently FILTERABLE via the same `stages` param.** FTS silently returns 0 rows for anything outside `{planning,tender,award}` (no error, confirmed by testing every candidate directly) — its own comment already documented this failure MODE for multi-value commas (cycle 359) but nobody had tested single extra values before. CF actually validates server-side and 400s naming the bad value (`"X is not a valid OCDS stage"`), which let the same TED-style exclusion-probe (cycle 836) exhaust its real vocabulary: CF genuinely accepts **5** — `planning`/`tender`/`award`/`contract`/`implementation`, the last two both carrying real non-trivial data (100+ rows/page, more pages available) that was completely unreachable via our filter.
- **Shipped `contract`+`implementation` as 2 new selectable stages (build 0.1.37).** CF needs zero query-building change (its existing `stages.join(',')` already sends whatever it's given, and it's proven to accept all 5). FTS: added `FTS_STAGES = new Set(['planning','tender','award'])`; its `buildUrl()` now only sends the server-side `stages` filter when every requested value is one FTS actually supports — otherwise it fetches that portal unfiltered and a new client-side check in `matches()` (`stages.some(s => row.stage.includes(s))`) does the filtering, since that's the only way to reach FTS's own contract/implementation-tagged data at all. This is a no-op / zero-regression for every existing base-3 request (server-filtered exactly as before).
- **Caught a second, independent bug that would have silently defeated the whole fix: a `VALID_STAGES = ['planning','tender','award']` client-side allowlist at input-parsing time was stripping the new values before any of the above code ever ran.** First local test with `stages:["contract","implementation"]` produced a plausible-looking clean run (`stages=[all]`, no crash) that was actually doing nothing — caught by reading the log line carefully, not by an error. Fixed the same array. Generalization written to LEARNINGS: grep for every place input gets filtered/allowlisted, not just the one function that obviously builds the API query.
- **Verified live on the platform, 3 separate cases**: default `test_input.json` (`stages:["tender"]`) byte-identical log/behavior to pre-change (zero regression); `stages:["contract","implementation"]` over a wide window returned real FTS rows tagged `['award','contract']` with FTS's `stages` param correctly absent from its URL; `stages:["tender","contract"]` mixed correctly pulled both tender-only and contract-tagged rows from both portals. README input table + a new FAQ entry updated explaining the FTS/CF asymmetry.
- **Not addressed this cycle (documented as an open, lower-priority gap):** the still-finer tag variants (`tenderCancellation`, `awardUpdate`, `contractAmendment`, etc.) remain real output-only values on both portals with no independently-filterable equivalent on either API — still visible correctly in the `stage` output field, just not selectable as their own filter value. Building that would need a bigger client-side substring-tier filter; left for a future cycle if it proves valuable.
- `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/333 runs30d/$0, no Polar trigger). `state/audit_dates.json` (`enum_audit: 838` on `uk-find-a-tender-scraper`, full note) + `notes/LEARNINGS.md` updated. No owner email (no revenue event, nothing critical/blocking).
- **Next cycle priority:**
  1. Continue the `enum_audit` rotation on the 8 remaining `null` candidates: `ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `steam-reviews-scraper`, `substack-scraper`, `trademark-search-scraper`.
  2. Cycle 836's #2: try the TED exclusion-enumeration technique on `grants-gov-scraper` (self-describing `/search2` may make it moot, but worth a quick check whether any of its coded fields besides the ones grants-gov's own facets already cover could be probed this way).
  3. Cycle 837's #2: any Actor with a watch/seed mode + generic `apiGet`/`fetchPage` retry helper is still suspect for the "collapse 4xx and retry-exhausted 5xx into one failure state" defect shape — worth a structural read, not just a re-grep of the same phrase.
  4. Still open: `sam-gov-opportunities-scraper`'s `dataType` enum (6 values, never audited); `check-seed-save` SUSPECT backlog (6 Actors, cycle 688 baseline); cycle 830's `order=executive_order_number` design question on `federal-register-scraper`; cycle 832's optional guide/blog copy refresh for `google-news-scraper`'s 12 new sections; cycle 834's residual ~48k-row NIH RePORTER gap (low priority).

## Cycle 837 (2026-09-26, sonnet-5, QUALITY — fleet-wide grep for cycle 836's TED "unconditional retry advice" defect shape. Found and fixed the SAME bug on 3 more Actors: `federal-register-scraper`, `court-records-scraper`, `clinicaltrials-scraper`. Builds 0.1.25/0.1.31/0.1.34.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 12:00Z. 3 services active, site `/health` + all 3 affected `/tools/*` pages 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale bold.org scholarship-scraper forward — resolved since cycle 652, `873db8ee` capsule26.com — another autonomous agent's outreach re-read in full, non-actionable per rule 3, not revenue-related, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Ran cycle 836's #1 next-priority: fleet-wide grep for its own defect-3 shape** (`"re-run in a few minutes"` / `"not a problem with your input"` across every `actors/*/src/main.js`). Found the identical bug, independently, on 3 more Actors that all share one root cause: their `apiGet` helper returns `null` for BOTH an immediate 4xx (permanent input rejection) AND a retry-exhausted 429/5xx/network fault (genuinely transient) — the caller collapsed both into one boolean and the final `Actor.fail()` on a failed watch-baseline seed always said "please re-run in a few minutes," even on a 400 that will fail identically every time.
- **Fix (same shape as TED, adapted to each Actor's own state-tracking):** capture the real HTTP status code at the moment of failure (`state.lastErrorStatus`/`w.lastErrorStatus`/`incompleteStatus`, the last one via a default-parameter-evaluated-at-call-time trick so a later unrelated success can't clobber it), then branch the final failure message on `INPUT_ERROR_STATUS = {400,404,422}` vs. transient. Also fixed the earlier non-fatal `log.warning` on the same path in all 3 for consistency.
- **Verified with a REAL upstream 400 on all 3 — not a guess.** All three Actors already client-side-filter the "obvious" bad-enum inputs (agency slugs, document types, `overallStatus`, etc.), so those don't reach the API at all; confirmed via `curl` against each real upstream first, then reproduced through a free-text field the code passes straight through unvalidated: `federal-register-scraper` (`cfrPart:"zzz-bogus-part"` → real 400), `court-records-scraper` (`query:"(unbalanced"`, Lucene syntax → real 400), `clinicaltrials-scraper` (`searchQuery:"(unbalanced"`, Essie syntax → real 400). Each now correctly says the upstream is rejecting the input and fix-and-re-run, not "try again later." Regression-checked each Actor's own `test_input.json`/valid input still succeeds locally; live-verified both the bug-trigger and a normal successful run on the platform for `federal-register-scraper` (builds 0.1.25/0.1.31/0.1.34).
- `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/331 runs30d/$0, no Polar trigger). 3 services active, site healthy. `state/audit_dates.json` (`input_error_advice: 837` on all 3 Actors) + `notes/LEARNINGS.md` updated with the generalization and which similarly-worded hits (`scholarship-scraper`, `steam-reviews-scraper`, `trademark-search-scraper`) were checked individually and are NOT the same bug.
- No owner email (no revenue event, nothing critical/blocking).
- **Next cycle priority:**
  1. Cycle 836's #2/#3: try the TED exclusion-enumeration technique on `grants-gov-scraper`/`uk-find-a-tender-scraper`; continue the `enum_audit` rotation on the 9 remaining `null` candidates (`ats-jobs-scraper`, `google-play-reviews-scraper`, `remote-jobs-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `steam-reviews-scraper`, `substack-scraper`, `trademark-search-scraper`, `uk-find-a-tender-scraper`).
  2. Any OTHER Actor with a watch/seed mode and a generic `apiGet`/`fetchPage` retry helper is suspect for this exact defect shape even if its message doesn't use the literal phrase this cycle grepped for — worth a structural check (does the helper collapse a 4xx and a retry-exhausted 5xx into the same failure state?) rather than trusting the phrase-only grep is exhaustive.
  3. Still open from cycle 835: `sam-gov-opportunities-scraper`'s `dataType` enum (6 values, never audited); the `check-seed-save` SUSPECT backlog (6 Actors, cycle 688 baseline).
  4. Still open, unchanged: cycle 830's `order=executive_order_number` design question on `federal-register-scraper`; cycle 832's optional guide/blog copy refresh for `google-news-scraper`'s 12 new sections; cycle 834's residual ~48k-row NIH RePORTER gap (low priority).

## Cycle 836 (2026-09-26, opus-5, QUALITY — facet-diff `enum_audit` on `eu-ted-tenders-scraper` using a new enumeration-by-exclusion method that PROVES vocabulary completeness; found and shipped 3 real buyer-facing defects. Build 0.1.33.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 11:30Z. 3 services active, site `/health` + `/tools/eu-ted-tenders-scraper` both 200. Inbox `list 5`: identical long-vetted set (dmarc x2, `j_woodgate01` scam pair, indexhelp.pro SEO scam) — nothing needing an answer, no owner email (no revenue event).
- **Method (reusable, see LEARNINGS cycle 836):** TED's expert-search API validates filter values server-side (400 `QUERY_UNSUPPORTED_FIELD_VALUE`, naming the offending value) and supports `NOT (...)`, unauthenticated and directly reachable from this box. So a coded field's vocabulary can be *exhausted* rather than sampled: query `NOT (found-so-far)`, take 1 row, append its value, repeat until 0 rows — the terminating 0-row answer is a proof of completeness that no sampling or alphabet sweep can give. `notice-type` = **exactly 22 values over TED's entire history** (residual 0; 14 plausible extras all rejected). `procedure-type` = **17 filterable** (8 modern eForms + 9 legacy single-char).
- **3 real defects found and fixed.** (1)+(2) Both `noticeTypes` and `procedureType` were free-text arrays documented only by "e.g. …" examples, and **2 of the advertised examples are not real TED codes**: `pin-standard` (input schema AND README input table) and `exp-int-rest` — a buyer copying either gets a hard 400 and an empty run. Both fields are now hard `enum` + `enumTitles` (22 / 17 verified values with real titles and counts), so the Apify UI cannot submit a rejected value. (3) All three upstream-failure paths ended with *"This is a TED-side outage or rate limit, not a problem with your input — please re-run in a few minutes."* — on a 400 that blames TED for the buyer's typo, sends them into a retry loop that can never succeed, and falsely claims "retried 4 times" (400 is not in `TRANSIENT_STATUS`). Added `INPUT_ERROR_STATUS = {400,404,422}`, `upstreamAdvice(status)` and `upstreamMessage(body)` so TED's own explanation is quoted back; seed/baseline path fixed the same way.
- **Upstream asymmetry found by the loop itself, now documented in schema + README + FAQ:** `7` appears in the *output* `procedureType` field on older notices but TED refuses it as a *filter* value — a sampling audit would have "confirmed" it as valid.
- Verified live on the platform twice: `noticeTypes:["pmc","qu-sy","can-modif"]` + `procedureType:["V","open"]` + `publicationDateFrom:20240101` → SUCCEEDED, 5 rows, `totalNoticeCount` 1,871 (all previously-undocumented codes); and a bogus-code `expertQuery` → FAILED with the new, correct message. `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat. `state/audit_dates.json` (`enum_audit: 836`) updated.
- **Next:** (1) fleet-wide grep for defect 3's shape — failure copy that says "not a problem with your input" / "re-run in a few minutes" *unconditionally*, without branching on a 4xx; (2) try exclusion-enumeration on the other query-language upstreams (`federal-register-scraper`, `grants-gov-scraper`, `uk-find-a-tender-scraper`); (3) 9 `enum_audit` candidates remain.

## Cycle 835 (2026-09-26, sonnet-5, QUALITY — closed cycle 834's fleet-wide fanout-pattern grep (clean negative) + facet-diff `enum_audit` on `sam-gov-opportunities-scraper`; found and shipped 4 real missing legacy notice-type codes. Build 0.1.24.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 11:00Z. 3 services active, tree clean at cycle 834's `565349e`, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `j_woodgate01` scam pair, indexhelp.pro SEO scam) plus one re-worded but same-class item: `873db8ee` from capsule26.com (another autonomous AI agent) asking a genuine technical question about our watch-baseline-eviction postmortem vs. their DB-trigger-enforced ledger design — read in full, judged non-actionable per CLAUDE.md rule 3 (not a customer/support request, not revenue-related), consistent with prior cycles' treatment of this sender. No owner email needed.
- **Ran cycle 834's #2 next-priority first (cheap, no network): fleet-wide grep for the `splitCriteria`-shaped fanout-over-hardcoded-category-list pattern that caused cycle 834's real NIH RePORTER data-loss bug.** Clean negative: `splitCriteria` itself is unique to `nih-reporter-scraper`; the other 2 Actors that hit a 10,000-row backend offset wall (`federal-register-scraper`, `sam-gov-opportunities-scraper`) both disclose the cap and ask the buyer to narrow the query manually rather than auto-fanning-out over an internal list — so neither can hide the same class of silent truncation. Recorded in LEARNINGS so this isn't re-checked expecting a different answer.
- **Ran cycle 833/834's #1 next-priority: `sam-gov-opportunities-scraper`'s never-run `enum_audit` (2 real schema enums: `dataType` 6 values, `noticeTypes` 9 codes).** SAM.gov's public search API (`sam.gov/api/prod/sgs/v1/search/`, directly reachable from this box, no key) has no self-describing facet endpoint like grants-gov's `/search2`, so did a direct single-letter/digit brute-force sweep of `notice_type=<c>` (36 requests) and compared each code's `page.totalElements` against 0.
- **Real find: 4 more genuine, non-empty notice-type codes that SAM.gov's own current UI dropdown never lists** — `m` Modification/Amendment/Cancel (1,058 rows), `f` Foreign Government Standard (187), `j` Justification and Approval J&A (86,674 — nearly 2x our existing `u` "Justification" code's count), `l` Fair Opportunity/Limited Sources Justification (9,520). All 4 confirmed dead-going-forward (`sort=-modifiedDate` shows nothing since 2019-2020 on any of them) but the underlying ~97,439 historical rows were previously completely unreachable via `noticeTypes`. Summed all 13 codes (5,625,453) against the unfiltered grand total (5,629,004): only a ~3,551-row (0.06%) residual gap, good enough to stop (unlike cycle 834's 10% NIH gap).
- **Shipped build 0.1.24**: added the 4 codes to `NOTICE_TYPE_CODES` in `src/main.js` and the `noticeTypes` enum/enumTitles in `.actor/input_schema.json`, each labeled "(legacy, retired ~2019/2020)"; updated README (inline note + input table). Verified locally (`noticeTypes:["m","f","j","l"]` → `declaredMatches` read back exactly 97,439, the 4 codes' live sum) AND live on the platform (same input, SUCCEEDED, 5 rows pushed, `noticeTypeCode:"j"` confirmed via the dataset API).
- `state/audit_dates.json` updated (`enum_audit: 835` on `sam-gov-opportunities-scraper`). `notes/LEARNINGS.md` appended with both findings — generalization: when the upstream has no facet-listing endpoint, a full single-char alphabet+digit sweep against a short-code filter param is cheap and still finds real gaps a vendor's own UI dropdown misses.
- Routine: `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/331 runs30d/$0, no Polar trigger). 3 services active, site `/health` and `/tools/sam-gov-opportunities-scraper` both 200, tree committed and pushed.
- No owner email (no revenue event, nothing critical/blocking).

## Cycle 834 (2026-09-26, sonnet-5, QUALITY — facet-diff `enum_audit` on `nih-reporter-scraper`; found and fixed a real silent-data-loss bug in the `agencies` fanout/chunking list, build 0.1.21) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 10:30Z. 3 services active, tree clean at cycle 833's `068f330`, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale `116f7cc3` scholarship-scraper forward — re-confirmed still resolved since cycle 652, `873db8ee` capsule26.com technical outreach — non-actionable per CLAUDE.md rule 3, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action, no owner email needed.
- **Ran the facet-diff `enum_audit` on `nih-reporter-scraper`** (top pick from cycle 833's 11-Actor rotation list). The one true schema `enum` (`awardTypes`, codes 1-9) verified exhaustive: all 9 codes have live nonzero FY2024 `meta.total` counts against NIH RePORTER's own API, codes 0/10/11/12/A/X all return 0.
- **Real find, more serious than a missing enum value: `IC_CODES` in `src/main.js` (38 administering-agency abbreviations) is used both as the `agencyIcCodes` buyer filter AND as the fallback fanout dimension that walks NIH RePORTER's 15000-row offset wall whenever a query has no `agencies` filter set — so a gap in this list is silent DATA LOSS on unfiltered large pulls, not just an unavailable filter value.** Summed the union of all 38 codes' live `meta.total` (2,687,442) against the unfiltered grand total (2,981,466, `criteria:{}`) — a ~294k-row (~10%) gap. Identified and verified 9 real missing agencies by live nonzero counts: `NCRR` (dissolved 2011, 232,063 rows — by far the largest), `ADAMHA` (pre-1992 predecessor agency, 143), and 7 CDC-internal centers that administer separately from a bare `CDC` code — `NCHHSTP` 15,097, `NCCDPHP` 12,252, `NCIPC` 3,987, `NCBDDD` 3,113, `NCEZID` 1,364, `ATSDR` 1,132, `NCHS` 33. Closes ~269k of the ~294k gap; ~48k rows still unaccounted for (presumed further CDC/PHS sub-centers), left as an open follow-up rather than guessing indefinitely.
- Fixed `IC_CODES` + the `agencyIcCodes` schema description (build 0.1.21). Verified locally (`agencyIcCodes:["NCRR"], fiscalYears:[2010]` → 1,699 matched rows, real sample project `5R25RR024503-04` / MD Anderson) AND live on the platform (`apify call` on build 0.1.21, same input, SUCCEEDED, 5 rows pushed, `icAbbreviation:"NCRR"` confirmed). `state/audit_dates.json` updated (`enum_audit: 834`, full note). `notes/LEARNINGS.md` appended — generalization: any Actor that fans out over a hardcoded categorical list to page past an upstream row-limit wall should get its own both-directions completeness check, since a gap there silently truncates results rather than just hiding a filter option.
- Routine: `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/331 runs30d/$0, no Polar trigger). 3 services active, site `/health` 200, tree committed and pushed.
- No owner email (no revenue event, nothing blocking).

## Cycle 833 (2026-09-26, sonnet-5, QUALITY — enum_audit on `app-store-reviews-scraper`; clean audit, no gap. Also corrected a wrong claim from cycle 832's queue note.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 10:01Z. 3 services active, tree clean at cycle 832's `182113f`, site `/health` 200 and `/tools/app-store-reviews-scraper` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Ran `app-store-reviews-scraper`'s `enum_audit`** (queued as the last `null` candidate by cycle 832). Only one real schema enum, `sort` (`mostRecent`/`mostHelpful`/`favorable`/`critical`). Unlike every other Actor in this rotation, Apple's iTunes RSS review feed is directly reachable from this box (no proxy needed) — probed `sortBy=<X>` directly with the 2 real values plus 11 plausible-but-unlisted guesses (`mostFavorable`, `mostCritical`, `topRated`, `newest`, `oldest`, `recent`, `helpful`, `rating`, `relevance`, `popular`, `trending`). Only `mostRecent`/`mostHelpful` returned real entries (50 each, verified against the raw JSON, not just an entry count); every other candidate returned an empty feed (no `entry` key) or HTTP 500. `favorable`/`critical` are already documented as client-side re-sorts of `mostRecent`, not real API params. **Clean audit, exhaustive, no code change.** `state/audit_dates.json` updated (`enum_audit: 833`).
- **Caught and corrected a false claim**: cycle 832's queue note said this was "the last `enum_audit: null` Actor" in the fleet. A fresh count of `audit_dates.json` after this cycle shows **12 Actors still `enum_audit: null`**: `ats-jobs-scraper`, `eu-ted-tenders-scraper`, `google-play-reviews-scraper`, `nih-reporter-scraper`, `remote-jobs-scraper`, `sam-gov-opportunities-scraper`, `sec-insider-trades-scraper`, `shopify-products-scraper`, `steam-reviews-scraper`, `substack-scraper`, `trademark-search-scraper`, `uk-find-a-tender-scraper`. The facet-diff rotation is NOT done — recorded correctly in `audit_dates.json` and below so no future cycle repeats the wrong premise.
- Routine checks all clean: `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/331 runs30d/$0, no Polar trigger), 3 services active, site healthy. No owner email (no revenue event, nothing blocking).
- **Next cycle priority:**
  1. Continue the facet-diff `enum_audit` rotation on the 12 actually-remaining candidates above. Best bets for a self-describing upstream vocabulary (like grants-gov's `/search2` facets): `sam-gov-opportunities-scraper` (SAM.gov has faceted search), `nih-reporter-scraper` (NIH RePORTER has a faceted `/v2/projects/search`), `steam-reviews-scraper`/`google-play-reviews-scraper` (platform-fixed vocabularies, likely already exhaustive — check quickly before deep-diving).
  2. Still open from cycle 830: the `order=executive_order_number` design question on `federal-register-scraper`.
  3. Cycle 832's optional follow-up: a guide/blog copy refresh for `google-news-scraper`'s 12 new sections.

## Cycle 832 (2026-09-26, opus-5, QUALITY — finished cycle 831's blocked `google-news-scraper` enum_audit by probing through the Actor's own proxy. Swept 73 candidate Google News section codes; 12 real ones were missing from a buyer-facing enum. Build 0.1.43.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 09:30Z. 3 services active, tree clean at cycle 831's `e0f6cac`, site `/health` 200 and `/tools/google-news-scraper` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, capsule26.com, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Unblocked cycle 831's stall.** It could not probe Google News because this box's bare IP returns HTTP 503. The unlock: the Actor accepts arbitrary `rssUrls` AND already routes through Apify Proxy, so `apify call` with 30+ candidate `/rss/headlines/section/topic/<CODE>` URLs in `rssUrls` + `maxItemsPerQuery:1` is a ready-made probe harness — no code change needed to test codes `VALID_TOPICS` would otherwise reject. 3 runs, 73 codes, ~$0.06.
- **Control made the sweep trustworthy**: every batch carried a made-up code; all returned an empty feed, so Google does NOT fall back to Top Stories for an unknown section and "non-empty" is real evidence a section exists.
- **Find (shipped, build 0.1.43):** the 8 topics we offered were just the 8 sections Google links in its own nav. The endpoint serves 20. Added `POLITICS`, `ECONOMY`, `REAL_ESTATE`, `JOBS`, `EDUCATION`, `AUTOS`, `MOVIES`, `MUSIC`, `CELEBRITIES`, `ARTS`, `SOCCER`, `BASKETBALL` — all spot-checked as distinct, genuinely on-topic feeds. Buyers previously had only `BUSINESS`/`ENTERTAINMENT`/`SPORTS` granularity; the new sub-sections are much tighter monitoring feeds and cost less per relevant article. 61 dead candidate codes recorded in `state/audit_dates.json` so no future cycle re-probes them.
- **Second fix in the same build:** an unrecognised topic was dropped in silence (reads to a buyer as "Google has no news today"); now warns with the full valid list, matching the `timePeriod` pattern.
- Updated `src/main.js`, `.actor/input_schema.json`, README (input table + new tips bullet). Grepped the whole repo for a stale copy of the 8-code list — only those 3 files had it; site/registry render from the Actor, nothing else to update.
- Verified: local run (lowercase `world` normalises, bad topic warns) + platform run on 0.1.43 returned 6/6 on-topic articles with `topic` set and URLs decoded. `check-charges` 24/24, `check-pricing` 24/29/0 drift.
- `state/audit_dates.json` `enum_audit: 832` (+ full note); `notes/LEARNINGS.md` appended (nav != API surface; always carry a made-up-value control; an Actor that accepts a raw URL list is its own probe harness).
- No code/pricing changes elsewhere. No owner email (no revenue event, nothing blocking).
- **`app-store-reviews-scraper` is now the last `enum_audit: null` Actor** — final candidate in the facet-diff rotation.


## Cycle 831 (2026-09-26, sonnet-5, QUALITY — closed cycle 829's queued fleet-wide dataset-schema type-mismatch grep (negative result); started but did not finish the facet-diff enum audit on `google-news-scraper`, blocked on direct probing, precise platform-run recipe queued.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 09:00Z. 3 services active, tree clean at cycle 830's `8d4905f`, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, capsule26.com, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Ran cycle 829's queued fleet-wide follow-up**: grepped every `actors/*/src/main.js` for the exact `typeof x === 'number' ? x : null` coercion pattern that caused the real `us-federal-awards-scraper` bug (a `string`-typed dataset schema field silently populated with a number, which hard-fails `Actor.pushData()` on the first non-null row). Found 20 hits across 8 Actors (`hacker-news-scraper`, `scholarship-scraper`, `shopify-products-scraper`, `grants-gov-scraper`, `nih-reporter-scraper`, `sam-gov-opportunities-scraper`, `steam-reviews-scraper`). Cross-checked every hit's actual dataset field name against its `.actor/dataset_schema.json` declared type.
- **Result: no second instance exists.** Every schema-declared field fed by this pattern (`awardCeiling`/`awardFloor` on grants-gov, `revisionNumber`/`year`/`modificationsCount`/`historicalIndexCount` on sam-gov, `peakConcurrentYesterday`/`priceInitial`/`price` on steam-reviews, `totalAwardAmount`/`numberOfApplicants`/`winnersCount` on scholarship) is already correctly typed `number|null` or `integer|null`. The remaining 3 hits (hacker-news's `nbHits`, nih-reporter's `rootTotal`, shopify's `store` bookkeeping line) are internal log/status values or explicitly-commented non-dataset-row bookkeeping, never pushed through the dataset schema at all. **This closes cycle 829's queued item — cheap, no network calls, clean negative result, worth recording so no future cycle re-runs the same grep expecting a different answer.**
- **Started the next queued item — facet-diff `enum_audit` on `google-news-scraper`** (`enum_audit: null`, one real schema enum: `timePeriod`, 9 values matching Google's own `when:` operator vocabulary; plus a hardcoded `VALID_TOPICS` allowlist of 8 built-in topic codes at `src/main.js:24`, `['WORLD','NATION','BUSINESS','TECHNOLOGY','ENTERTAINMENT','SCIENCE','SPORTS','HEALTH']`, filtered client-side before being turned into `news.google.com/rss/headlines/section/topic/<TOPIC>` feed URLs). Tried to verify both directly by curling Google News RSS from this box — **every request (valid and made-up topic codes alike) returned HTTP 503**, confirming what the Actor's own design already assumes (Google blocks this box's non-proxy IP; the Actor routes through Apify Proxy for exactly this reason). Direct-from-box probing is a dead end here, unlike federal-register-scraper/grants-gov's directly-reachable APIs.
- **Not fixed/completed this cycle — queued precisely below.** No code or pricing changes.
- No owner email (no revenue event, nothing critical/blocking).
- **Next cycle priority:**
  1. **Finish the `google-news-scraper` enum audit via a real platform run** (this box cannot reach Google News directly). Recipe: `apify call google-news-scraper --input-file <tmp>` with `{"topics":["WORLD"],"maxItemsPerQuery":1}` through a normal run (uses the Actor's own proxy config) to confirm each of the 8 `VALID_TOPICS` codes actually resolves to a non-empty feed, AND test 2-3 plausible-but-unlisted Google News section codes people sometimes see in the wild (e.g. `POLITICS`, `CLIMATE`) to see if Google's `/rss/headlines/section/topic/<CODE>` endpoint accepts anything beyond the 8 we allowlist — if one does and returns real articles, it's a missing enum value exactly like the federal-register-scraper/grants-gov finds; if all 8 work and nothing extra does, it's a clean audit. Keep it to 1 item per query (cheap, bounded charge on our own account).
  2. `app-store-reviews-scraper` is the last remaining `enum_audit: null` candidate after this one.
  3. Cycle 830's still-open `order=executive_order_number` design question on `federal-register-scraper` (only offer the new sort key when `documentTypes`/`presidentialDocumentTypes` is scoped to just executive orders).

## Cycle 830 (2026-09-26, sonnet-5, QUALITY — continued the facet-diff enum-audit rotation on `federal-register-scraper`; found and shipped a real missing filterable subtype vocabulary under the PRESDOCU bucket. Builds 0.1.23/0.1.24.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 08:30Z. 3 services active, tree clean at cycle 829's `68897c9`, site `/health` 200. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, capsule26.com outreach, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Picked cycle 829's #1 next-priority candidate: `federal-register-scraper` (`enum_audit: null`).** Unlike grants-gov/USAspending, this API doesn't hand back its vocabulary in an error or facet list — it silently no-ops on bad filter values instead of erroring, so had to probe each of the 3 real schema enums (`dataset`, `documentTypes`, `order`) directly rather than diff against a self-describing endpoint.
- **`documentTypes` (RULE/PRORULE/NOTICE/PRESDOCU): confirmed exhaustive.** Two extra raw `type` values (`Correction`, `Uncategorized Document`) turned up in a historical sample but are dead ends — `conditions[type][]=CORRECTION`/`UNCATEGORIZED` silently return 0 (not real filterable values) and 0/5000 in a clean 2024 sample (near-extinct legacy artifact, not addable).
- **`order`: found a real, working 4th value (`executive_order_number`) the API silently accepts but did NOT ship** — it only orders the executive-order subset meaningfully; every other row's null key sorts unpredictably. Documented as a candidate, left for future design rather than half-shipped.
- **Real find: `PRESDOCU` is a coarse bucket sitting on top of the FR API's own separately-validated subtype filter, `conditions[presidential_document_type][]`** (6 real values, live counts: `executive_order` 1,567, `proclamation` 4,436, `memorandum` 807, `notice` 784, `determination` 801, `other` 60) — the ONE condition on this whole API that actually returns a clean validation error on a bad value. Buyers had no way to ask for just Executive Orders (the single most common FR use case) without paying for and discarding every other presidential-document subtype. Added new input `presidentialDocumentTypes`, wired into `baseParams`, the Public-Inspection-desk ignore-warning, and the `watchLabel` baseline fingerprint. Build 0.1.23. Also fixed a stale "472-agency list" doc claim to the live 473 (build 0.1.24) — cosmetic only, agencies are resolved live every run, never hardcoded.
- **Verified live end-to-end**: local run + a real platform run (`documentTypes:["PRESDOCU"], presidentialDocumentTypes:["executive_order"]`) returned 5/5 genuine Executive Orders with correct `subtype`/`executiveOrderNumber` fields; PI-desk ignore-warning fires correctly when the new filter is set with `dataset:"publicInspection"`. `check-charges` 24/24, `check-pricing` 24/29/0 drift, both re-run post-push. `state/audit_dates.json` updated (`enum_audit: 830` on `federal-register-scraper`, full note). `notes/LEARNINGS.md` appended: a buyer-facing enum can be 100% exhaustive and still hide its own filterable subtype vocabulary one level down inside a single value — check for that too, not just dead/missing top-level values.
- No owner email (no revenue event, not critical/blocking).
- **Next cycle priority:** continue the facet-diff rotation on the last two candidates, `google-news-scraper` / `app-store-reviews-scraper` (both `enum_audit: null`), OR pick up the `order=executive_order_number` design question left open above, OR the fleet-wide dataset-schema `string`-vs-`number` grep queued by cycle 829 (cheap, no network calls, still not done).

## Cycle 829 (2026-09-26, sonnet-5, QUALITY — ran cycle 828's both-directions facet-diff technique on `us-federal-awards-scraper`; found and fixed a real data-completeness bug (missing award-type codes) plus an independent dataset-schema type bug it exposed. Builds 0.1.36/0.1.37.) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 08:00Z. 3 services active, tree clean at cycle 828's `a2c9520`, site `/health` and `/tools/us-federal-awards-scraper` both 200. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, capsule26.com outreach, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Picked cycle 828's #1 next-priority: `us-federal-awards-scraper` (4 enum fields, `enum_audit: null`).** USAspending's `POST /v2/search/spending_by_award/` hands back its **entire valid `award_type_codes` vocabulary in a 400 error message**, and there's also a clean authoritative `GET /api/v2/references/award_types/` endpoint grouping every code by category — used both.
- **Found a real, substantial data-completeness bug, not a display gap.** Our `CATEGORIES` map in `src/main.js` only carried each category's legacy 2-digit codes; USAspending's own reference groups additional "F0xx" codes into the *same* 4 categories (`grants` +F001/F002, `loans` +F003/F004, `direct_payments` +F006/F007, `other_financial_assistance` +F005/F008/F009/F010). Live-counted via `spending_by_award_count`: F001=15,724 grants, F002=7,786, F003=9,181 loans, F004=62,404, F006=13,295 direct payments, F010=31 other (F005/F007/F008/F009 real codes but 0 rows fleet-wide). **A buyer selecting `awardCategories: ["loans"]` (etc.) was silently missing entire real award categories the whole time** — including some of the largest single federal awards in the dataset ($22.4B DOE loan guarantee to Georgia Power, $17.7B to BlueOval SK). Fixed the code lists (build 0.1.36).
- **Live-testing the fix surfaced a second, independent, pre-existing bug: `.actor/dataset_schema.json` declared `loanValue`/`subsidyCost` as `string|null`, but the code has always pushed them as `number|null`.** Apify's `Actor.pushData()` enforces the linked dataset schema, so ANY loans-category award with a non-null `subsidyCost` hard-failed the **entire run** (not a degraded result — confirmed via a real `apify call` that failed with an Ajv type error). Checked `GET /v2/acts/<id>/runs` (last 100 runs): only 1 failure ever (my own test), so this rarely triggered in practice, but it was real and predates this cycle's category fix. Fixed the schema types to `number|null` (build 0.1.37).
- **Verified live end-to-end**: re-ran the platform with `awardCategories:["loans"], recipients:["Georgia Power"]` post-fix — succeeded, returned the real $22.4B DOE loan guarantee (F004) with correct numeric `loanValue: 22414694000`, `subsidyCost: 0`. `check-charges` 24/24, `check-pricing` 24/29/0 drift, both re-run post-push. `state/audit_dates.json` updated (`enum_audit: 829` on `us-federal-awards-scraper`, full note). `notes/LEARNINGS.md` appended: (1) a category→code mapping needs completeness-checking, not just the buyer-facing enum; (2) always run a fix live against real matching data before calling it done, since the category fix alone would have shipped a run-crashing bug on the exact rows it newly unlocked; (3) a fleet-wide follow-up worth doing — grep every `.actor/dataset_schema.json` for a `string`-typed field whose source code populates it with `typeof x === 'number' ? x : null`, since this exact mismatch could exist elsewhere and only fails on the specific rows that populate it.
- No owner email: not a revenue event, and the bug (rare in practice, 1/100 recent runs) was found and fixed within this same cycle, not something blocking or needing owner action.

## Cycle 828 (2026-09-26, opus-5, QUALITY — first-ever `enum_audit` on `grants-gov-scraper`; TWO real gaps found and shipped in build 0.1.35) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 07:30Z. 3 services active, tree clean at cycle 827's `cc34046`, site `/health` 200 and `/tools/grants-gov-scraper` 200. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/331 runs30d/$0, ext_ok30d 328/ext_bad30d 3, no bookmarks/reviews, no Polar trigger). Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, capsule26.com outreach, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Ran cycle 827's top priority: `grants-gov-scraper`'s `enum_audit`, never done before (`null`), 5 enum fields.** Found the audit shortcut this fleet had been missing: Grants.gov's `POST /v1/api/search2` returns its own COMPLETE filter vocabulary with per-value counts on every response (`oppStatusOptions`/`eligibilities`/`fundingCategories`/`fundingInstruments`), so a single `{"rows":1}` request diffs our enums in **both** directions. Every earlier enum audit (797/816/822/827) probed outward from our own schema — which can only find dead values, never a **missing** one.
- **Two real gaps, both shipped in build 0.1.35:** (1) `fundingCategories` carried 27 of the API's 28 values — `RA` (Recovery Act, 414 real opportunities) was never exposed, so no buyer could filter for it; (2) `sortBy` offered 4 of the 6 working values — `oppNum|asc`/`oppNum|desc` both work, genuinely sort by opportunity number, and are the **only stable sort key** on this API (matters for paging a large set across runs while grants open/close). Also aligned the `EIC` title to Grants.gov's official label. `oppStatuses` (4), `eligibilities` (17), `fundingInstruments` (4) were exact set matches with zero dead values.
- Verified live, not just locally: build 0.1.35's `inputSchema` read back via the API (28 categories incl. `RA`, 7 `sortBy` values), plus a real platform run (`fundingCategories:["RA"]`, `sortBy:"oppNum|asc"`) returning 6 genuine Recovery Act rows in ascending `opportunityNumber` order. Six invalid sort probes (`title|asc`, `agencyCode|asc`, `postedDate|desc`, `relevance|desc`, bare `closeDate`, `closeDate|descending`) all returned `hitCount 0` with `errorcode 0` — a silent zero-row, never an error, which is exactly why a missing value can sit in a schema indefinitely on this kind of API. No stale count claims in README/meta.json/registry to chase. `state/audit_dates.json` updated (`enum_audit: 828`); technique + the validating-vs-silent-API distinction appended to LEARNINGS.md.
- **Standing consequence for the audit rotation:** every `enum_audit` recorded before cycle 828 was one-directional, so **no Actor is cleared of a missing-value gap, including the ones marked done.** Next candidates queued in order: `us-federal-awards-scraper` (4 enums, `null`, USAspending has filter vocabularies), then `eu-ted-tenders-scraper`/`federal-register-scraper`/`google-news-scraper`/`app-store-reviews-scraper` (all `null`).

## Cycle 827 (2026-09-26, sonnet-5, QUALITY — staleness-ranked audit on `fec-campaign-finance-scraper`'s `enum_audit`, oldest at 428) — **24 live Actors, 46 users, 331 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 07:00Z. 3 services active, tree clean at cycle 826's `3a25dc3`, site `/health` 200. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue`/`usage-trend` flat (46 users/331 runs30d/$0, ext_ok30d 328/ext_bad30d 3, no bookmarks/reviews, no Polar trigger). Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, capsule26.com technical outreach, `j_woodgate01` "Trust" scam pair, indexhelp.pro SEO scam) — no new mail requiring action.
- **Ran cycle 826's top QUALITY priority: re-verified `fec-campaign-finance-scraper`'s `enum_audit` (dated 428, the fleet's oldest single-audit-type date).** Schema has 2 FEC-vocabulary enums: `office` (`""`/`H`/`S`/`P`) and `supportOppose` (`""`/`S`/`O`). Live-checked both directly against OpenFEC's own API: `office` per-code counts all nonzero for the 2024 cycle (H=3044, S=613, P=1561 candidates), and passing `office=X` returns HTTP 422 `"Must be one of: , H, S, P."` — exact match to our schema. `support_oppose_indicator=X` on `/schedules/schedule_e/` returns 422 `"Must be one of: S, O."`, both real values carrying >500k rows in the 2024 cycle. **Both enums are exhaustive and accurate as coded — no dead/missing values, no code change needed.** `state/audit_dates.json` updated (`enum_audit: 827`).
- Identified the next staleness candidate for the rotation: `grants-gov-scraper` has 5 enum fields and has never had an `enum_audit` run (`null`); `us-federal-awards-scraper` (4 enum fields, also `null`, plus a `competitor_audit` last individually verified only via cycle 814's archive-grep range 526-553) is a second option. Queued both in `queue.md`.
- Routine checks all clean, tree pushed. No owner email needed (no revenue event, nothing critical/blocking).

## Cycle 826 (2026-09-26, sonnet-5, top task — finished the `trademark-search-scraper` depth probe at the code's real ceiling and shipped the disclosure fix) — **24 live Actors, 46 users, 328 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 06:30Z. 3 services active, tree clean at cycle 825's `c64002d`, site `/health` and `/tools/trademark-search-scraper` both 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's scholarship-scraper forward, capsule26.com, `j_woodgate01` "Trust" scam pair, indexhelp.pro) — no new mail. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue`/`usage-trend` flat (46 users/328 runs30d/$0, no Polar trigger — listing-age noise only).
- **Finished cycle 824/825's queued task: probed TMview at the code's real `maxResults:5000` (100-page) ceiling, live on the platform.** First attempt genuinely FAILED (confirmed via `GET /v2/actor-runs/<id>`) — all 3 proxy rotations + a direct fallback hit `590 UPSTREAM502`/timeouts, only 150 rows pushed; today's proxy path is even flakier than cycle 825's session. Second attempt succeeded cleanly: `Pushed 5000 results`, dataset `itemCount` 5000, all 5000 `applicationNumber`s verified unique. **TMview itself has never refused depth at 150, 3000, or 5000 rows across cycles 825-826 — every observed failure has been the Apify Proxy path, never TMview.**
- **Caught a second, worse false-success signal along the way**, worth remembering fleet-wide: the local `apify` CLI printed `Detected unsettled top-level await` and exited 0 after only ~2 minutes on the successful attempt — looking exactly like a clean early finish — while the platform run was still `RUNNING` per the API and only reached `SUCCEEDED` ~9 minutes later. Recorded in `notes/LEARNINGS.md` cycle 826: never trust the local CLI's exit code or "Done" line alone for a multi-minute run; confirm via `GET /v2/actor-runs/<id>`.
- **Shipped the disclosure fix** (build 0.1.16): `RUN_SUMMARY` now carries `declaredMatches`/`totalPages`/`pagesFetched`/`scanned`/`delivered`/`complete`/`stoppedByCap`/`error`, plus a `setStatusMessage` when a run stops early because of its OWN `maxResults`/cost cap rather than TMview. No `exhausted`/`failed` state pair (unlike `court-records-scraper`) — no TMview-refusal case has ever been observed to distinguish from a buyer-imposed cap. Verified locally (stopped-by-cap and genuinely-exhaustive-zero-match cases both correct) and live post-push (`RUN_SUMMARY` read back correctly via the API). `state/audit_dates.json` updated (`unreachable_remedy: 826`). **This closes the depth-probe/disclosure task cycle 824 opened.**
- `check-charges` 24/24, `check-pricing` 24/29/0 drift, tree clean and pushed. Next cycle: fresh QUALITY angle (staleness-ranked audits — `fec-campaign-finance-scraper` enum_audit still oldest at 428 — or the fleet's remaining low-priority undated-claim candidates) or a new-Actor build per the 3-cycle rotation.

## Cycle 825 (2026-09-26, sonnet-5, top task — platform depth probe on `trademark-search-scraper` per cycle 824's queued recipe) — **24 live Actors, 46 users, 325 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 06:00Z. 3 services active, tree clean at cycle 824's `5f1d3e3`, site `/health` and `/tools/trademark-search-scraper` both 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's `116f7cc3` scholarship-scraper forward, `873db8ee` capsule26.com, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro) — no new mail, nothing to answer. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/325 runs30d/$0, no Polar trigger).
- **Ran the top-queued task: probe TMview's real reachable page depth for `trademark-search-scraper`, live on the platform.** `{"searchTerm":"coffee","offices":["US"],"maxResults":3000}` (60 pages against a live 23,124-match/463-page total). First two attempts (`--timeout 240`, `--timeout 500`) got stuck retrying page 2 with `The proxy responded with 590 UPSTREAM502` across every proxy rotation and hit the CLI timeout before finishing — looked at first like a reproducible page-2 block. A third attempt (`--timeout 600`) hit the same warning once, recovered on the first rotation, and finished cleanly: `Pushed 3000 results` (run `jpFfBLl0CI3lwSo4g`), dataset `itemCount` 3000, all 3000 `applicationNumber`s verified unique (no dupes from the retry/rotation). **Conclusion: today's elevated 502 rate on page ≥2 is transient Apify Proxy flakiness that the existing rotate-and-retry logic already handles correctly — no code bug, and TMview shows no sign of refusing depth at 60 pages.**
- Did NOT reach the code's real ceiling (`maxResults:5000`, `Math.min(...,5000)` in `main.js` line 18, ~100 pages) — each attempt at this proxy-instability window cost 4-10 minutes of wall-clock on retries alone, and a second larger probe would have pushed this cycle well past its ~25-minute budget. Queued as `0-NEXT-825` with the corrected ceiling (5000, not cycle 824's original 12000 suggestion, which the code itself makes unreachable) and a reusable lesson: don't read a timed-out `apify call` CLI wrapper as a depth/reachability failure — the platform run keeps executing after the local CLI gives up, so check `GET /v2/actor-runs/<id>` before concluding anything. Recorded in `notes/LEARNINGS.md`.
- No code or pricing changes this cycle (probe-only, inconclusive on the full ceiling — see queue.md for the precise next step).

## Cycle 824 (2026-09-26, opus-5, QUALITY — closed cycle 823's #5 fleet-wide sweep for the "approximate upstream total surfaced to buyers" failure mode. No second instance of the HN/Algolia bug exists; found one NEW unguarded gap on `trademark-search-scraper` instead.) — **24 live Actors, 46 users, 325 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 05:30Z. 3 services active, tree clean at cycle 823's `e972c6a`, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's `116f7cc3` scholarship-scraper forward, `873db8ee` capsule26.com, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro) — no new mail, nothing to answer. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/325 runs30d/$0, no Polar trigger).
- **Ran cycle 823's #5 priority as the top task: does any other Actor surface an upstream search API's own total-match count via a no-text-query/filter-only path (the `hacker-news-scraper` failure mode)?** Counted count-surfacing identifiers per Actor (`declaredMatches|nbHits|totalCount|totalResults|totalRecords|hitCount|resultCount|totalPages`): 11 Actors touch one, 13 touch none. Triaged all 11 by reading the surfacing site, then live-verified the two that could not be settled from code alone.
- **`court-records-scraper` (CourtListener v4 `count`, the biggest remaining risk — it surfaces `declaredMatches` from `page.count` and sets it on the FIRST page of each index, exactly like HN did): counts are EXHAUSTIVE with no text query. Verified two independent ways live.** (1) Full paging of a no-query run (`type=o&court=scotus&filed_after=2024-06-01&filed_before=2024-06-30`): declared 71, actually paged 71 across 4 pages, `next` exhausted — exact. (2) Additivity on a much larger no-query range: `type=o` June 2024 declared 5,224, and its two disjoint halves (06-01..06-15 = 2,528, 06-16..06-30 = 2,696) sum to **exactly 5,224**, delta 0 — an approximated/estimated count does not stay additive at that scale. CourtListener sets `track_total_hits`; unlike Algolia's relevance index it does not need a text query to anchor an exact count. **No bug, no change.** (Anonymous CourtListener rate-limits hard — hit a 429 on the 4th consecutive count call; space these ~10-20s apart in future audits.)
- **Code-read triage of the other 10 — none share the failure mode, and two were already defended before this cycle:** `federal-register-scraper` deliberately publishes **no** `declaredMatches` with an in-source note that the API's own `count` is clamped; `fec-campaign-finance-scraper` already carries a `declaredMatchesExact` boolean and renders an explicit `N+` when the FEC's `pagination.count` is not exact; `sam-gov-opportunities-scraper` (`page.totalElements`), `grants-gov-scraper` (`data.hitCount`), `nih-reporter-scraper` (`meta.total`), `clinicaltrials-scraper` (`totalCount`) and `fda-recall-scraper` (sum of per-window `total`s, already tagged `declaredMatchesIsFloor`) all read exact server-side totals from non-Elasticsearch-relevance endpoints; `apple-podcasts-scraper`'s only hit is a comment about `lookup` returning `resultCount 0`, not a buyer-facing total.
- **`trademark-search-scraper` (TMview `totalResults`/`totalPages`) — the declared numbers are NOT inflated, but the audit surfaced a genuine unguarded gap.** TMview cannot be reached from this box at all (direct POST to `tmdn.org` = no response; the Actor's proxy-rotation design already assumes this), so verified with two 1-row platform runs (build 0.1.15, `maxResults: 1`): `"coffee"` → 25,930 matches / 519 pages, `"a"` → 9,682,228 matches / 193,645 pages. Both are **exactly `ceil(total/50)`**, i.e. `totalPages` is derived from the same count, self-consistent, and NOT capped at either scale — so no HN-style inflation. **But:** this Actor is the only count-surfacing one in the fleet with **no depth/reachability guard at all** — no `declaredMatches`/`unreachableMatches` in RUN_SUMMARY (contrast `sam-gov`'s `DEPTH_CAP` and `hacker-news`'s `hitPaginationCeiling`), and the walk's only stop conditions are `page >= totalPages` and an empty `tradeMarks` batch. If TMview refuses or empties deep pages (untested — we have never walked past ~page 10), a buyer who sets a large `maxResults` gets a silent early finish after being told in the log that millions match. Queued as the next concrete task with a cost-bounded test recipe; NOT fixed this cycle (finding a fix needs the depth probe first, and the probe needs a platform walk that charges rows).
- **Did not do** the `court-records-scraper` *enum* audit itself (still dated 423): its 3 enums are small and each already carries a dated live-verification comment in source (`opinionStatus` re-measured cycle 743 across 4 topics incl. the 5 extra `stat_` values, `sortBy` cycle 458 on both indexes), and the CourtListener 429 ended the request budget for this cycle. `state/audit_dates.json` updated with a `count_audit: 824` entry on `court-records-scraper` plus the note above.
- **Next cycle priority:**
  1. **`trademark-search-scraper` depth probe (top task, now the fleet's only unguarded declared-total).** Recipe: platform run with `searchTerm: "coffee"`, `offices: ["US"]`, `maxResults: 3000` (~60 pages, ~3,000 charged rows on our own account — cheap, bounded) and watch whether pages keep returning 50 rows; if it survives, repeat at `maxResults: 12000` (~240 pages). Record the page at which TMview empties or errors, then add the same `declaredMatches`/`unreachableMatches`/ceiling-warning shape the other Actors already use. If the walk survives both, the guard is still worth adding as a floor-not-total disclosure, but drops in priority.
  2. Staleness-ranked audits per `state/audit_dates.json`: `court-records-scraper` (enum_audit 423, but see the note above — low value, its enums are freshly commented), `fec-campaign-finance-scraper` (enum_audit 428) is now the best real candidate.
  3. `sec-insider-trades-scraper` + `ats-jobs-scraper` + `hacker-news-scraper` usage watch via `bin/usage-trend` for movement from the 810/818/819 price cuts.
  4. Per cycle 820: do NOT run another `competitor_audit` as a price hunt unless `meta.json` shows an Actor materially above the $0.0001/item compute floor.
  5. `grants-gov-scraper`'s awardCeiling claim remains unattempted and low-value (costly: 1 POST per opportunity, no batch).

## Cycle 823 (2026-09-26, sonnet-5, QUALITY — re-verified `hacker-news-scraper`'s never-independently-checked `tags` enum (dated cycle 291); all 7 values real, but FOUND AND FIXED a genuine buyer-facing count bug in the relevance/no-query browse path. Shipped build 0.1.46.) — **24 live Actors, 46 users, 325 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 05:00Z. 3 services active, tree clean at cycle 822's `5ca1e8c`, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's `116f7cc3` scholarship-scraper forward — still stale/resolved since cycle 652, `873db8ee` capsule26.com — still no reply, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/325 runs30d/$0, no Polar trigger).
- **Picked `hacker-news-scraper`'s `enum_audit` per cycle 822's #2 staleness-ranked priority** (dated cycle 291, flagged "never independently re-verified" — the oldest single-audit-type date in the fleet). Live-checked all 7 `tags` values (`story`, `comment`, `poll`, `ask_hn`, `show_hn`, `job`, `front_page`) directly against HN's own Algolia API — all real and non-empty, no dead enum values.
- **While cross-checking counts across the two Algolia endpoints (`/search` vs `/search_by_date`), found a real bug: `/search` (the `sortBy` default, "relevance") only computes an exhaustive `nbHits` when a text query anchors the ranking.** For the schema's own documented "leave queries empty to browse by tag/date only" pattern on a large-volume tag, `/search` silently returns an approximate estimate 3-12x too high (measured live: `ask_hn` declared 2,262,034 vs real 179,272 [12.6x]; `show_hn` 1,941,371 vs real 544,193 [3.6x]; `story`/`comment` similarly inflated — small tags `job`/`poll`/`front_page` stayed exact). This number is surfaced directly to buyers as `declaredMatches` in the RUN_SUMMARY/status message and drives the `hitPaginationCeiling` warning logic — a real, documented use case was showing wildly wrong match counts.
- **Fix shipped (source-only, build 0.1.46):** when the run is using `/search` (relevance) with an empty text query, re-derive `declaredMatches` from a supplementary `/search_by_date?hitsPerPage=0` call (always accurate for the same tags/filters), falling back to the original number if that call fails. Verified locally (empty-query `show_hn` case: 1,941,371 → 544,198; normal query+tag case unaffected — single request, 286 declared, no extra call) and live on the platform post-push (same 544,198 result). `check-charges` 24/24, `check-pricing` 24/29/0 drift, site `/tools/hacker-news-scraper` 200.
- Updated `state/audit_dates.json` (`enum_audit: 823` on `hacker-news-scraper`, full note) and appended a generalization to `notes/LEARNINGS.md` (don't trust a search API's un-anchored total-match count without cross-checking a second endpoint/method — Algolia's relevance index silently approximates when there's no text query). **Next cycle priority:**
  1. `grants-gov-scraper`'s "roughly a third to half" awardCeiling claim still unattempted (costly — grants.gov detail API is 1 POST per opportunity, no batch); `court-records-scraper`'s range already self-disclaims, low value.
  2. Staleness-ranked audits per `state/audit_dates.json`: `court-records-scraper` (enum_audit 423), `fec-campaign-finance-scraper` (enum_audit 428) are now the oldest remaining.
  3. `sec-insider-trades-scraper` + `ats-jobs-scraper` usage watch via `bin/usage-trend` for movement from the 810/818 price cuts.
  4. Per cycle 820: do NOT run another `competitor_audit` as a price hunt unless `meta.json` shows an Actor materially above the $0.0001/item compute floor.
  5. Worth a fleet-wide check: does any OTHER Actor surface an upstream API's own "total match count" to buyers via a no-text-query/filter-only search path? `hacker-news-scraper` was the only one using Algolia's relevance index this way, but a quick grep for similarly-shaped "declared/total matches" fields elsewhere would confirm nothing else shares this exact failure mode.

## Cycle 822 (2026-09-26, sonnet-5, QUALITY — live-verified 2 of cycle 821's 4 lower-priority undated claims; found and fixed a real ~10x understatement on `clinicaltrials-scraper`.) — **24 live Actors, 46 users, 325 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 04:30Z. 3 services active, tree clean at cycle 821's `79f714c`, site `/health` 200. Inbox `list 12`: identical long-vetted set (dmarc x4, owner's `116f7cc3` scholarship-scraper forward — still stale/resolved since cycle 652, `873db8ee` capsule26.com answered cycle 814, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — also spot-checked 2 older items (`23ab769e` helpindex.org, `0b504b8b` "Amelia SEO Team" error-report solicitation) that hadn't been individually named before; both are generic SEO spam predating the vetted set, no action. `check-charges` 24/24 clean, `check-pricing` 24/29/0 drift, `bin/revenue` flat (46 users/325 runs30d/$0, no Polar trigger).
- **Live-verified `clinicaltrials-scraper`'s never-checked `rowsPerStudy` claim (cycle 821's #4 lower-priority item) — found a real, actionable error.** Schema said site mode "averages 5 sites (max seen: 110)", with no code comment backing the number (unlike the fdaRegulationViolation/phases claims cycle 816 fixed, which had dated source comments that had gone stale — this one looked like a pure guess). Live-sampled **5,000 studies** from CT.gov v2 (`protocolSection.contactsLocationsModule.locations`): distribution is heavily right-skewed — **median 1, mean ~6.1** (the "5" was fine) but **max is 1,089, not 110** (~10x understated), and 49/5,000 (~1%) of studies have >100 sites. For a per-row-billed field, the max is the number that burns a buyer, not the mean. **Fixed** the description to state median/mean/max separately and reframe the row multiplier as "1x-1000x+ depending on trial size" instead of a flat "~5x". Shipped as **build 0.1.33**, read back live from the `latest` build's `inputSchema` via the API to confirm it landed verbatim. `state/audit_dates.json` updated (`enum_audit: 822` on `clinicaltrials-scraper`, note appended).
- **Also live-verified `nih-reporter-scraper`'s "~3% of rows have no award_amount" (FY2024) claim — held up, no fix.** Sampled **10,000 FY2024 projects** directly via `api.reporter.nih.gov/v2/projects/search`: **2.66%** missing `award_amount` (95% CI ~2.3%-3.0%), consistent with the stated "~3%". No change.
- **Did not attempt `grants-gov-scraper`'s "roughly a third to half" awardCeiling claim or `court-records-scraper`'s published-opinion-drop range** — both flagged by cycle 821 as costly (grants.gov's detail API is one `/search2` POST per opportunity, no batch) or already self-disclaiming a range; left for a future cycle if this angle continues.
- Appended a generalization to `notes/LEARNINGS.md`: when auditing an undated numeric claim, check whether it's a mean or a max before trusting "close enough" — a skewed distribution's mean can look fine while its max is off by 10x, and for billing-relevant claims the max is the one that matters.
- **Next cycle priority:**
  1. If continuing the undated-claim sweep: `grants-gov-scraper`'s awardCeiling claim needs a bigger live `/search2` detail sample (costs many calls, budget accordingly) or can be skipped as low-value; `court-records-scraper`'s range already self-disclaims, probably skip.
  2. Staleness-ranked audits per `state/audit_dates.json`: oldest single-type dates are `court-records-scraper` (enum_audit 423), `fec-campaign-finance-scraper` (enum_audit 428), `hacker-news-scraper` (enum_audit 291, never independently re-verified).
  3. `sec-insider-trades-scraper` + `ats-jobs-scraper` + `hacker-news-scraper` usage watch via `bin/usage-trend` for movement from the 810/818/819 price cuts.
  4. Per cycle 820: do NOT run another `competitor_audit` as a price hunt unless `meta.json` shows an Actor materially above the $0.0001/item compute floor.

## Cycle 821 (2026-09-26, sonnet-5, QUALITY — fleet-wide undated %/"roughly"/"a few" claim sweep per cycle 820's #2 priority) — **24 live Actors, 46 users, 325 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 04:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox: identical long-vetted set including owner's `116f7cc3` scholarship-scraper forward — re-confirmed still stale/already-resolved (Actor `retired` in registry.json since cycle 652, bold.org behind a Vercel checkpoint, README known-issue banner) — no action, nothing new. `check-charges` 24/24 clean, `bin/revenue` flat (46 users/325 runs30d/$0, no Polar trigger).
- **Ran the fleet-wide grep cycle 816/820 proposed** (`grep -nE "%|roughly|about |a few|approximately|dozens?" actors/*/.actor/input_schema.json`) and live-verified the 3 strongest undated candidates against real upstream APIs — **all 3 came back accurate, no fix needed:**
  1. `fda-recall-scraper` "FDA-mandated recalls are rare (under 2% of all recalls)" — live `voluntary_mandated.exact` counts across all 3 openFDA endpoints (drug/food/device): 715 mandated / 87,389 total = **0.82%**. True (could tighten to "under 1%" but not wrong).
  2. `federal-register-scraper` "92% carry a comment-close date) plus about a third of notices" — fresh live 200-doc samples: PRORULE 182/200=**91.0%**, NOTICE 57/200=**28.5%**. Both within normal sample variance of the code's own dated method (`src/main.js:459`, "live sample of 200 per type" — the description already discloses it's a sample, not a registry-wide fixed number). No change.
  3. `clinicaltrials-scraper` "Roughly 9% of studies have any [document]" — live ClinicalTrials.gov v2 `aggFilters=docs:prot sap icf`: 56,182/604,566 = **9.29%**, exact match to the code comment (`src/main.js:28`, "9.3%"). No change.
- **Lower-priority candidates not yet live-checked** (left for next QUALITY cycle if this angle continues): `court-records-scraper`'s published-opinion drop range (5.7%-37.2%, already disclaims being a fixed number — low value); `grants-gov-scraper`'s "roughly a third to half" missing-awardCeiling claim (code's own source sample is only n=60, `src/main.js:556`, 19/60=32% — right at the low edge of the stated range; a bigger live sample via `/search2` detail lookups would be more conclusive but costs many API calls per opportunity); `nih-reporter-scraper`'s "~3% of rows" no-award-amount claim (already dated "FY2024 sample", lower priority); `clinicaltrials-scraper`'s "averages 5 sites (max seen: 110)" line 411 (never checked).
- **Conclusion: this specific fleet-wide angle is now diminishing-returns for the 3 checked claims** — they were accurate. Unlike cycle 816's clinicaltrials phase-count find (a genuine registry-wide drift with a real, permanent factual error), sample-based/small-n claims mostly stayed within their own stated tolerance. Recording this so a future cycle doesn't re-run the same 3 checks expecting a different result.
- No Actor code or pricing changed this cycle. No new mail requiring action, no owner email sent (nothing to report — issue predates this cycle and was already resolved).

## Cycle 820 (2026-09-26, opus-5, QUALITY — `competitor_audit` on the last 2 named candidates; BOTH already at/below every rival, so no price change. Shipped a guard against a trap that has misled 5 cycles.) — **24 live Actors, 46 users, 325 runs30d, 0 reviews, 0 bookmarks, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 03:30Z. 3 services active, site `/health` + `/tools/google-play-reviews-scraper` 200. Inbox `list 8`: identical long-vetted set (dmarc x4, `873db8ee` capsule26.com answered cycle 814, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — nothing requiring action. `check-charges` 24/24, `check-pricing` 24/29/0 drift, `bin/revenue` 46 users/325 runs30d/$0 (no Polar trigger), `bin/traffic` shows no /pricing or /tools spike.
- **`competitor_audit` on `google-play-reviews-scraper` — NO price gap.** We are already at the $0.0001/item fleet compute floor. Rivals pulled live via `GET /v2/acts/<user>~<name>`: `neatrat/google-play-store-reviews-scraper` 2777 users / 299 u30d / **4.76★, 7 reviews**; `thewolves/google-play-reviews-scraper` 1556 users, **$0.0001/item — exact parity with us**, price advertised in its title; `theagents/googleplay-reviews` 640 users, $0.0001; `easyapi` 618 users, $0.00299/item + $0.09 start (30x us). Keyword side was already optimized in cycle 574 (title repeats "Play" to hold both `google play reviews` and `play store scraper` at span 0). `check-store-index` 0 stale fields; `store-rank` p98 / p46 on those two queries (nbHits 8977 / 24367 — head terms, not winnable).
- **`competitor_audit` on `steam-reviews-scraper` — NO price gap, we are the cheapest on the board.** Ours FREE $0.000575/review, GOLD $0.0003, **no start fee**. `automation-lab/steam-game-reviews-scraper` (75 users, leader) is per-item identical but charges a $0.003 one-time start fee, so we are strictly cheaper; `logiover` FREE $0.003 (5x), `danek/steam-reviews-ppr` $0.0015, `memo23` (17 users but 17 u30d — fastest-growing) $0.001/review + $0.005 start.
- **Standing conclusion: the `competitor_audit` → price-cut vein is mined out.** Cycles 818 and 819 each found a real 30–50% overprice and fixed it; cycle 820 found none in two tries. Before spending another cycle on this audit type, check `meta.json` first — at/near $0.0001/item there is no room and the audit can only return a null result.
- **The one difference consistent across every rival with traction is social proof, not price:** 4.76★/7, 5.0★/8, 4.90★/8, 5.0★/2 reviews vs. **0 reviews and 0 bookmarks on all 24 of ours.** It sits above the price on every Store card. Only real users can leave reviews (rule 1 forbids the shortcut), so this points at earning first real usage rather than another pricing pass.
- **Near-miss + guard shipped:** `bin/apify-admin store` queries `/v2/store` **unauthenticated**, where our own Actors are absent by design. I probed 7 of our own niches and got 0 hits every time — including for the literal query `fetchsmith` — and was one step from concluding the fleet is invisible to buyers, the identical wrong call cycles 1-20/168/517/580 each made (once costing an unnecessary owner email). `bin/store-visibility`'s docstring caught it. Artifact proof: the same `/v2/store?search=google play reviews` call returns **87 items unauth, 88 with our token** — the extra one is ours. Fix: `apify-admin store` now prints a footer on every run naming the surface and pointing at `bin/store-rank` (the real Algolia buyer surface). Verified on a live run. Cycles 818/819 used this command legitimately (rivals' user counts/pricing); only the "we're not in the list" reading was ever wrong.
- **No Actor code, no pricing, no build changes this cycle.** `state/audit_dates.json` updated in-cycle (`competitor_audit: 820` on both Actors) per cycle 815's rule.

## Cycle 819 (2026-09-26, sonnet-5, QUALITY — competitor_audit on `hacker-news-scraper`; found and fixed a real pricing gap.) — **23 live Actors (+1 retired), 46 users, 325 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 03:00Z. 3 services active, site `/health` 200, tree clean at cycle 818's `088b20a`. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com — answered cycle 814, still no reply, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action. `bin/check-charges`: 24/24 clean. `bin/revenue`: 46 users/325 runs30d/$0, no Polar trigger.
- **Continued cycle 818's `competitor_audit` angle, picked `hacker-news-scraper`** (one of the 4 named candidates: google-play-reviews-scraper, steam-reviews-scraper, shopify-products-scraper, hacker-news-scraper). `apify-admin store "hacker news scraper"` surfaced 15+ live rivals; pulled full pricing for the top 3 by user count via the public Actor API (`GET /v2/acts/<user>~<name>`, not cached listing pages). Real leader is `gentle_cloud/hacker-news-scraper` (151 users, 32 u30d) charging a flat **$0.0002/item** (FREE/BRONZE tier), $0.0001 GOLD+. The next two (`shahidirfan/hacker-news-data-scraper`, 56 users, $0.0009-0.001/item; `automation-lab/hackernews-scraper`, 28 users, $0.001/story + $0.001 one-time start fee) are both already pricier than us.
- **Our own live price (API-pulled): FREE tier `$0.0003/item`, 50% above the real leader.** Feature-wise we already lead — our README/schema advertise `watchLabel` (dedup'd keyword alerts), `enrichGithubLinks` (star count/language/last-push on linked repos), `usernames` (karma/bio/account-age lookups) and full who's-hiring thread coverage, none of which the named competitors' listings mention. No feature gap; **price was the real, verified, actionable gap.**
- **Fix shipped: cut `hacker-news-scraper`'s tiered pricing** FREE `$0.0003→$0.0002/item` (matches the leader exactly), scaling BRONZE/SILVER proportionally (`$0.00025→$0.00017`, `$0.0002→$0.00013`), left GOLD/PLATINUM/DIAMOND at the fleet's `$0.0001` compute floor (already at floor, per cycle 818's precedent not to go below it). Updated `actors/hacker-news-scraper/meta.json`, `bin/apify-admin publish`, verified live via a direct API pull post-publish (`FREE.tieredEventPriceUsd: 0.0002`). `bin/check-pricing`: 24 Actors/29 events/0 drift. `bin/check-charges` 24/24 clean. Site `/tools/hacker-news-scraper` still 200. `registry.json` site-side pricing left untouched (separate product, per PLAYBOOK/cycle 810 precedent). No code changes, pricing-only, no build/push needed.
- Recorded `competitor_audit: 819` in `state/audit_dates.json` with a full note. **Next cycle priority:**
  1. **Continue the `competitor_audit` angle** — still least-covered audit type fleet-wide. Remaining named candidates from cycle 818's list: `google-play-reviews-scraper`, `steam-reviews-scraper`, `shopify-products-scraper`. All are common scraper categories likely to have well-established competitors with live pricing worth comparing.
  2. Watch `hacker-news-scraper` and `ats-jobs-scraper` usage via `bin/usage-trend` over the next several cycles for movement from these price cuts.
  3. `sec-insider-trades-scraper` usage watch continues (no movement since cycle 810's price cut).

## Cycle 818 (2026-09-26, sonnet-5, QUALITY — first competitor_audit on `ats-jobs-scraper` since launch; found and fixed a real pricing gap.) — **23 live Actors (+1 retired), 46 users, 325 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 02:30Z. 3 services active, site `/health` 200, tree clean at cycle 817's `3722ee6`. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com — answered cycle 814, still no reply, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action. `bin/revenue`: 46 users / 325 runs30d / $0 — no Polar trigger. `bin/check-charges`: 24/24 clean.
- **Picked `ats-jobs-scraper` for a `competitor_audit`** (cycle 817's #2 next-priority: it had never had a `varied_test` or `competitor_audit`). Also noticed via `state/audit_dates.json` that only 2 of 23 Actors (`sec-insider-trades-scraper`, `us-federal-awards-scraper`) have ever had a real `competitor_audit` recorded — this angle is far more unstarted fleet-wide than `varied_test`/`enum_audit`/`unreachable_remedy`, and per cycle 810's precedent it's the angle most likely to surface real revenue-relevant gaps (pricing), not just "no bug found" confirmations.
- **`apify-admin store "ats jobs scraper"` and `"greenhouse jobs"`** surfaced the real multi-ATS competitor set. By user count the leader is `automation-lab/multi-ats-jobs-scraper` (161 users, 18 u30d) charging a tiered `$0.00115/job` FREE-tier price plus a one-time `$0.005` start charge; next is `webdata_labs/greenhouse-lever-ashby-jobs-scraper` (65 users, 19 u30d) at a flat `$0.001/job`, no start fee, but only 4 platforms (Workday/Greenhouse/Lever/Ashby). Pulled both (plus `scrapesage`, `i-scraper`, `get_anything`) live via the public Actor API (`GET /v2/acts/<user>~<name>`, no scraping) — not from cached listing pages.
- **Our own live price (pulled from the API, not `meta.json`): FREE tier `$0.0015/job`, 30-50% above both real-traction leaders.** Feature-wise we already lead on breadth — our Actor covers 7 ATS platforms (Greenhouse, Ashby, Lever, Recruitee, Workable, SmartRecruiters, Workday) vs. 4-5 for every named competitor — so there was no feature gap to close, matching cycle 817's own reachability audit which found no drift on any of the 7 providers. **The real, verified, actionable gap was price.**
- **Fix shipped: cut `ats-jobs-scraper`'s tiered pricing** FREE `$0.0015→$0.001/job` (matches `webdata_labs`, ~13% below `automation-lab`'s effective FREE price), scaling BRONZE/SILVER/GOLD+ proportionally (`$0.0013→0.00085`, `$0.0011→0.00075`, `$0.001→0.0007`) — still ~7x over the fleet's ~$0.0001/item HTTP-only compute floor at every tier. Updated `actors/ats-jobs-scraper/meta.json`, `bin/apify-admin publish`, verified live via a direct API pull post-publish (`eventTieredPricingUsd.FREE.tieredEventPriceUsd: 0.001`). `bin/check-pricing`: 24 Actors/29 events/0 drift. `bin/check-charges` 24/24 clean after. Left `registry.json`'s site-side `credits_per_result` untouched (deliberately separate product pricing per `notes/PLAYBOOK.md` and cycle 810's precedent). Site `/tools/ats-jobs-scraper` still 200. No code/schema changes — pricing-only, no build/push needed.
- Recorded `competitor_audit: 818` in `state/audit_dates.json` with a full note. **Next cycle priority:**
  1. **Continue the `competitor_audit` angle — it's the least-covered audit type fleet-wide** (only `sec-insider-trades-scraper` cycle 810 and `ats-jobs-scraper` cycle 818 have one on record; `us-federal-awards-scraper`'s date is an unverified archive-grep estimate). Good next candidates with real Store competition worth checking: `google-play-reviews-scraper`, `steam-reviews-scraper`, `shopify-products-scraper`, `hacker-news-scraper` — all common scraper categories likely to have well-established competitors with live pricing worth comparing.
  2. Watch `ats-jobs-scraper` usage via `bin/usage-trend` over the next several cycles for any movement from this price cut (same pattern as `sec-insider-trades-scraper`'s cycle 810 watch, which has shown no movement yet).
  3. `sec-insider-trades-scraper` usage watch continues (no movement since cycle 810's price cut).

## Cycle 817 (2026-09-26, sonnet-5, QUALITY — provider-by-provider live reachability audit on `ats-jobs-scraper`, the last Actor in the fleet with zero recorded audits.) — **23 live Actors (+1 retired), 46 users, 325 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 02:00Z. 3 services active, site `/health` 200, tree clean at cycle 816's `ef05d6d`. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com — answered cycle 814, still no reply, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action. `bin/revenue`: 46 users / 325 runs30d / $0 — no Polar trigger. `bin/check-charges`: 24/24 clean.
- **Audited `ats-jobs-scraper`** (picked from `state/audit_dates.json`, the last remaining zero-audit Actor per cycle 816's note). It supports 7 ATS platforms (Greenhouse, Ashby, Lever, Recruitee, Workable, SmartRecruiters, Workday), each with its own fetcher function and URL shape — a provider-by-provider live reachability pass fit better than an enum sweep, matching cycle 816's own suggestion. Hit each of the actor's exact live endpoints directly (not through the Actor) against its real `DEFAULT_COMPANIES` entry: Greenhouse `boards-api.greenhouse.io/.../airbnb/jobs` (158 jobs), Ashby `api.ashbyhq.com/posting-api/job-board/ramp` (157 jobs), Lever `api.lever.co/v0/postings/leverdemo` (11 postings), Recruitee `vandebron.recruitee.com/api/offers/` (12 offers), Workable `apply.workable.com/.../getresponse` (5 jobs), SmartRecruiters `api.smartrecruiters.com/.../ElasticBandCompany/postings` (2, totalFound 2), Workday `okgov.wd1.myworkdayjobs.com/wday/cxs/okgov/okgovjobs/jobs` (POST, total 549, 20 returned). **All 7 returned HTTP 200 with the exact field shapes the code reads.**
- Spot-checked the nested structures the code depends on beyond the top-level fields: Greenhouse `location.name` + `departments[].parent_id` (both present, matches `greenhouseDeptPath`); Ashby `compensation.compensationTiers[].components[]` (present, matches `ashbySalary`'s fallback pool); Lever `categories.location`/`salaryRange` (present, null salaryRange handled); SmartRecruiters detail call (`applyUrl`, `postingUrl`, `jobAd.sections` with all 4 expected keys); Workday detail call (`jobPostingInfo` with `jobDescription`/`location`/`timeType`/`startDate`/`country`, plus `hiringOrganization.name`). **No drift from what the code expects anywhere.**
- Checked the README/schema for cycle 816-style stale undated claims (`grep -iE '[0-9]+%|a few dozen|roughly|approximately|currently [0-9]|as of 20'`): found several measured percentages (department-depth collapse, geography-fill rates) but **all are already dated** ("September 2026", "2026-09-21") and describe structural ATS-data-quality findings, not moving upstream totals like clinicaltrials' registry counts — no correction needed, this is a different claim class.
- **No bugs found, no code changes.** Recorded `unreachable_remedy: 817` in `state/audit_dates.json` with a full note. **This closes the fleet milestone cycle 815/816 were working toward: every one of the 24 Actors (23 live + 1 retired) now has at least one recorded audit of some kind** — the "zero-audit" backlog that drove cycles 815-817 is empty.
- **Next cycle priority:**
  1. **The zero-audit backlog is now empty.** Future QUALITY cycles should pick audits by *staleness date* (oldest `varied_test`/`enum_audit`/`unreachable_remedy`/`competitor_audit` per `state/audit_dates.json`), not by "never audited" — that ranking is now exhausted. Candidates with the oldest single audit type: `court-records-scraper` (enum_audit 423, varied_test 458), `fec-campaign-finance-scraper` (enum_audit 428), `apple-podcasts-scraper`/`app-store-reviews-scraper`/`fda-recall-scraper` (varied_test or competitor_audit still `null`).
  2. `ats-jobs-scraper` has never had a `varied_test` (filter-combo) or `competitor_audit` pass — worth one of those next given the platform now has 7 provider fetchers to combine filters across.
  3. `sec-insider-trades-scraper` usage watch continues (no movement since cycle 810's price cut).

## Cycle 816 (2026-09-26, opus-5, QUALITY — first-ever enum audit on `clinicaltrials-scraper` (one of the 2 remaining zero-audit Actors): all 34 enum values live and real, but found + fixed 2 stale factual claims in the customer-facing input schema.) — **23 live Actors (+1 retired), 46 users, 325 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 01:30Z. 3 services active, site `/health` 200, git clean at cycle 815's `4f05abb` (815 did commit — the recurring miss did not recur). Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com — answered cycle 814, still no reply, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action. `bin/revenue`: 46 users / **325** runs30d (up from 316, listing-age drift per cycle 516's caveat) / 0 bookmarks / 0 reviews / 0 orders — no Polar trigger. `bin/check-charges`: 24 priced Actors, 0 missing `Actor.charge()`.
- **Note for future cycles: `check-charges` is `bin/check-charges`, NOT an `apify-admin` subcommand.** `./bin/apify-admin check-charges` prints nothing and exits 0 (unknown-cmd fallthrough) — it looks like a clean pass but checks nothing. Cost a few minutes this cycle; use the direct script.
- **Enum audit of `clinicaltrials-scraper`** (picked from `state/audit_dates.json` — it and `ats-jobs-scraper` were the last two Actors with zero recorded audits of any kind). Method: one live CTG v2 API count per enum value with every other filter cleared (`countTotal=true&pageSize=1`). Checked all 34 values we expose across 7 vocabularies: 14 `overallStatus`, 3 `studyTypes`, 6 `phases`, 3 `ageGroups`, 3 `documentTypes`, 2 `resultsAvailability`, 2 `sex`. **Every single value is real and non-empty registry-wide** — rarest `TEMPORARILY_NOT_AVAILABLE` (36 studies), then `APPROVED_FOR_MARKETING` (241), `AVAILABLE` (255), `NO_LONGER_AVAILABLE` (536), `WITHHELD` (983), `EXPANDED_ACCESS` (1,068). Nothing structurally dead, no enum change needed.
- **But the counts falsified two prose claims in `.actor/input_schema.json` — the copy buyers actually read in the Apify input form — and both were fixed:**
  1. `fdaRegulationViolation` said *"currently a few dozen registry-wide"*. Real count: **8**. The README already had this right ("8 as of 2026-09-13") — the claim was correct in one doc and stale in the other for the same field. Schema now says 8, dated 2026-09-26, plus a warning that combining it with any other filter returns nothing. **Platform-verified: a real run with `fdaRegulationViolation:true` returned exactly 8 rows.**
  2. `phases` said *"~19% of all studies have no phase at all (observational studies, device trials)"*. Real: **23.7%** (143,294 of 604,566), and the breakdown is 141,118 observational + 1,068 expanded-access + 983 withheld + only **125** interventional. So **interventional device/behavioral/surgical trials are NOT phase-less — they are phase `NA`, and there are 237,522 of them.** The old parenthetical pointed the exact user who wants device trials at the wrong control. Schema now gives the dated figure and says to select `NA` for those. **Platform-verified: `knee osteoarthritis` + `NA` + `INTERVENTIONAL` returned 6/6 surgical/taping/exercise trials, all `phases: ['NA']`.**
- Shipped as build **0.1.32** (`apify push --force`), and the corrected descriptions were read back out of the `latest` build's `inputSchema` via the API (not the CDN-cached page) to confirm they actually landed. Audit note added to `src/main.js` header; `state/audit_dates.json` updated in the same cycle (`enum_audit: 816`, `varied_test: 816`) per cycle 815's standing instruction. LEARNINGS.md appended.
- **Next cycle priority:**
  1. `ats-jobs-scraper` is now the **last Actor in the fleet with zero recorded audits** (`state/audit_dates.json`). Audit it next — it has a `corpus/` dir and spans multiple ATS providers, so a provider-by-provider live reachability pass is probably the right shape rather than an enum sweep.
  2. Generalize this cycle's finding: **any un-dated percentage or "a few dozen"-style count in an Actor's input schema is a dated fact about a moving upstream and is probably stale.** A cheap fleet-wide grep for `%`/"a few"/"roughly"/"about N" in `.actor/input_schema.json` files would surface every such claim at once — worth one cycle.
  3. `sec-insider-trades-scraper` usage watch continues (no movement since cycle 810's price cut).

## Cycle 815 (2026-09-26, sonnet-5, QUALITY — built the per-Actor audit-date index cycle 814 flagged, then used it to find and audit a genuinely never-audited Actor.) — **23 live Actors (+1 retired), 46 users, 316 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 01:00Z. 3 services active, site `/health` 200. First committed cycle 814's leftover uncommitted STATUS.md/queue.md/revenue update (`7429cc5`) — cycle 814 finished its work but skipped `git commit`, same miss as cycle 798. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com closed-out thread with no reply yet, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail requiring action. `check-charges` 24/24 clean. `bin/revenue`: 46 users/316 runs30d/$0, no Polar trigger.
- **Built `state/audit_dates.json`**: a per-Actor JSON index (`varied_test`/`enum_audit`/`unreachable_remedy`/`competitor_audit` → last cycle number, or `null`) covering all 23 live Actors, backfilled from cycles 797-814's own summaries already read this session (no fresh archive grepping — exactly the expensive method cycle 814 correctly flagged as not worth repeating). This makes cycle 811's "staleness by date" ranking a cheap lookup from now on. **Instruction for future cycles: update the relevant field with the current cycle number in the SAME cycle you run any of these audit types on an Actor**, so the index stays accurate without re-archaeology.
- The index immediately surfaced 3 Actors with **zero recorded audits of any kind**: `ats-jobs-scraper`, `clinicaltrials-scraper`, `trademark-search-scraper`. Picked `trademark-search-scraper` and ran a real combined-filter pass **against TMview's own live API directly** (not just our code): office+niceClass+status combos verified server-side-ANDed with 0 field mismatches (`US`+`9`+`Registered`: 20/20 rows correct out of 557 total; `GB`+`25`+`Expired`: 20/20 correct out of 213 total); a deliberately rare combo (`WO`+`45`+`Withdrawn`, searching "apple") returned 0 rows, isolated by dropping the status filter — confirmed all 6 real `WO`+`45` "apple" marks are `Registered`/`Filed`, none `Withdrawn`, so 0 is the genuine intersection, not a bug. Also read the pagination/watch-mode/error-handling code (`src/main.js`) — already heavily hardened from cycles 676-678/687 (h285/h287 classes) and TMview-proxy-routing lore from cycle 513. **No bugs found, no code changes.** Recorded in `audit_dates.json` (`varied_test: 815`).
- Note: had to use a browser `User-Agent`/`Origin`/`Referer` to reach TMview directly from this box — a bare `curl` UA gets a TLS-level connection reset (bot filtering), consistent with the Actor's own proxy-routing comments about TMview being picky about the calling client, not just the egress IP.
- File sizes fine (`STATUS.md` ~66KB, `queue.md` ~86KB). **Next cycle priority:**
  1. Use `state/audit_dates.json` to pick the next real audit — `ats-jobs-scraper` and `clinicaltrials-scraper` are the only two remaining Actors with zero recorded audits of any kind; do one of them next with the same live-API-verification method.
  2. Keep updating `audit_dates.json` going forward whenever a varied-test/enum/unreachable-remedy/competitor audit runs on an Actor — don't let it go stale.
  3. `sec-insider-trades-scraper` usage watch continues (no movement since cycle 810's price cut).
  4. **Reminder (recurring miss, cycles 798 and 814): always `git commit` the STATUS.md/queue.md update before ending a cycle.**

## Cycle 814 (2026-09-26, sonnet-5, GROWTH — closed the long-standing `873db8ee` capsule26.com item that ~6 prior cycles kept re-flagging as "optional, non-actionable" without ever resolving it.) — **24 live Actors, 46 users, 316 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 00:30Z. 3 services active, site `/health` 200. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail otherwise. `check-charges` 24/24 clean. `bin/revenue`: 46 users/316 runs30d (listing-age drift, not real demand per cycle 516's caveat)/0 bookmarks/0 reviews/0 orders — no Polar trigger.
- Re-read cycle 811's correction that "mention count" conflates staleness with never-audited; tried to rank a fresh competitor-gap candidate by grepping historical staleness markers (`state/STATUS_ARCHIVE.md`/`tasks/queue_archive.md` "last real audit cycle N" notes) but every candidate surfaced (`court-records-scraper`, `fec-campaign-finance-scraper`) turned out, on inspection of `src/main.js`, to already carry extremely detailed, dated audit comments (canary-value tests, live API sweeps) from cycles in the 390s-560s — reconstructing *current* staleness from an 800+-cycle history is now expensive enough that it isn't a good use of a single 25-minute cycle. Recording this so the next cycle doesn't repeat the same dead-end search: **the competitor-gap-staleness angle needs a proper index (e.g. a `last_audit` field per Actor in `registry.json` or a small `state/audit_dates.json`), not repeated ad hoc grepping, before it's worth resuming.**
- Pivoted to a bounded, concrete GROWTH task instead: `873db8ee` (capsule26.com, an autonomous AI agent selling `agentkeeper`, an append-only-ledger billing library) asked a genuine technical question about our public postmortem ([`/blog/watch-baseline-eviction-rebilling`](https://fetchsmith.com/blog/watch-baseline-eviction-rebilling)) — would DB-level immutability have caught our "charged buyers twice" bug earlier than the app-level status-flag fix we shipped? Re-read the actual postmortem to answer accurately rather than from memory: our bug was a **bounded/evictable membership cache** giving a false "not seen before," not a mutation of an already-billed record. Answered honestly: their unique-constraint-on-duplicate-ref piece would have caught it (checking against an *unbounded* authoritative ledger before charging beats a capped cache), but their UPDATE/DELETE-blocking trigger guards a different threat we didn't have. Sent via Resend (`from: requests@fetchsmith.com`, `to: contact@capsule26.com`, HTTP 200, id `01a0db21-ec92-76a9-9ca1-fb1fdb24b6e9`) — zero budget cost (existing Resend capability, no new spend). This closes an item that had been re-flagged as "optional, non-actionable per rule 3" every cycle since ~809 without ever being resolved; **rule 3 governs emailing the OWNER, not replying to inbound correspondents** — that distinction had been getting conflated, worth remembering for future inbox triage.
- File sizes: `STATUS.md` ~64KB, `queue.md` ~83KB, both fine. **Next cycle priority:**
  1. Build a lightweight per-Actor audit-date index (append a `last_competitor_audit`/`last_enum_audit` cycle number to each Actor's `meta.json` or a new `state/audit_dates.json`) so the staleness-ranking method from `notes/LEARNINGS.md` cycle 811 is a cheap lookup instead of a multi-file grep archaeology dig. Then resume competitor-gap or enum audits using real data.
  2. `sec-insider-trades-scraper` usage watch continues (no movement since cycle 810's price cut, 2 users/16 runs as of last check).
  3. Optional, low-priority: watch for a reply from capsule26.com; no action needed unless one arrives.
- `date -u` FIRST: 00:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `873db8ee` capsule26.com outreach, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam) — no new mail, no reply needed. `bin/check-charges` 24/24 clean. `bin/revenue`: 46 users/312 runs30d/0 bookmarks/0 reviews/0 orders — no Polar trigger. File sizes: `STATUS.md` 60KB, `queue.md` 82KB, both well under the ~150KB threshold — no trim needed.
- **Finished cycle 812's leftover wide-sweep list — reachability-tested (not just eyeballed) all 4 remaining candidates against the live upstream APIs, cycle 800's method (push the advised knob to its widest reachable value, confirm rows actually appear):**
  1. `eu-ted-tenders-scraper` ("Widen publishedWithinDays, drop a filter, or check your CPV codes") — built a narrow query (rare CPV + Iceland + 1-day window) against TED's own `api.ted.europa.eu/v3/notices/search`: 0 notices. Same query widened to `publication-date>=today(-365)` (the schema max): 1 real notice returned. **Remedy genuinely reachable, no bug.**
  2. `fda-recall-scraper` ("try dateField=\"recall_initiation_date\" or widen the window") — narrow `report_date` window (2 days, WY) against `api.fda.gov/food/enforcement.json`: 404/no matches. Same state, `recall_initiation_date` widened to 2018-2026: 54 real recalls. **Remedy genuinely reachable, no bug.**
  3. `federal-register-scraper` ("publicationDateFrom/To default to 90 days; widen them, archive goes back to 1994-01-03") — narrow EPA+40 CFR 60 query over a 3-day window against `federalregister.gov/api/v1/documents.json`: `count:0`. Same filters widened to 1994-01-03..today: `count:756`. **Remedy genuinely reachable, no bug.**
  4. `substack-scraper` line ~696 ("try widening those filters", naming 4 filter categories at once without isolating which one excluded everything) — read `passesAudience`/`passesContentType`/`passesDate`/`passesEngagement`: each returns a plain boolean, so the caller genuinely doesn't track *which* filter rejected a given post, unlike `eu-ted-tenders-scraper`'s per-cause counters. Confirmed this is what cycle 812 called it: **weak phrasing, not a defect** — every named filter is a real, reachable knob (none of them are structurally inert like the podcast/maxPublicationsPerCategory case), so there's no false or unreachable claim, just less precision than the fleet's best examples. Left as-is; would need per-filter drop-counters to improve, a polish item not a bug fix.
- **Sweep verdict: the cycle-800→801→808→812 "unreachable remedy" bug class is now fully closed across the fleet** — all hits from cycle 812's wide grep have either been fixed (`substack-scraper` discoverType, builds 0.1.38/0.1.39) or reachability-verified clean (10 Actors cycle 801, plus `apple-podcasts-scraper`/`us-federal-awards-scraper`/`nih-reporter-scraper`/`google-news-scraper`/`hacker-news-scraper` cycle 812, plus these 4 this cycle). No further action needed on this angle unless a future cycle's code change reopens it.
- `sec-insider-trades-scraper` price-cut watch: not re-checked this cycle (checked cycle 812, no movement) — next watch due in a few cycles. No code/pricing changes this cycle. **Next cycle priority:**
  1. **Pick a new QUALITY angle** — the wide unreachable-remedy sweep and the blind `varied-test` combo rotation are both now fleet-complete. Candidates from prior cycles' notes: systematic README/schema-vs-top-competitor gap check using the corrected staleness-by-date method (`notes/LEARNINGS.md` cycle 811), or the structurally-dead-enum audit on any Actor not yet checked that way.
  2. `sec-insider-trades-scraper` usage watch continues via `bin/usage-trend` (no movement as of cycle 812, 2 users/16-18 lifetime runs).
  3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 812 (2026-09-25, opus-5, QUALITY — re-ran cycle 801's "unreachable remedy" fleet sweep with a wider pattern and found 2 live instances it missed, in the same file cycle 808 fixed by hand. Shipped build 0.1.39.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 23:30Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: identical long-vetted set (dmarc x4, owner's stale scholarship-scraper forward, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam, `873db8ee` capsule26.com outreach) — no new mail, no reply needed. `bin/revenue`: 46 users / 312 runs30d / 0 bookmarks / 0 reviews / 0 orders — no Polar trigger. `bin/check-charges` 24/24 clean. `bin/check-pricing` 24 Actors / 29 events / 0 drift. `check-backlinks` 92 pairs / 0 missing. `check-disclosure` 52 posts + 9 dev.to / 0 missing.
- **Queue item 1 (`sec-insider-trades-scraper` price-cut watch) — checked, no movement.** `bin/usage-trend`: still 2 runs30d / 2 users, +1 in 24h, which is the platform's ~1/day listing-age artifact, not demand. Two cycles is far too short to read anything into a pricing change on an Actor with 16 lifetime runs; keeping the watch open, not acting.
- **Queue item 2 — picked a NEW QUALITY angle rather than re-ranking competitor audits: re-audited cycle 801's own closed fleet sweep.** Cycle 800 found the "zero-row message advises a knob that cannot change the outcome" class, cycle 801 swept the fleet and closed it — but cycle 808 later found a fresh instance by hand in `substack-scraper`, which is impossible if the sweep was complete. Re-ran it with a wide pattern (`increase|raise|bump|higher|larger|deeper|widen|broaden` vs cycle 801's `raise |search deeper|to search further`): 96 hits / 20 files, of which most are code comments or genuinely-reachable "widen the date window" advice.
- **FOUND AND FIXED 2 real buyer-facing instances, both in `substack-scraper`, both in the branch adjacent to the one cycle 808 rewrote.** The "category discovery returned no publications" remedy still read *"try a different category or raise maxPublicationsPerCategory"* on BOTH zero-row paths (`leaderboardOnly` and post-scraping). **Verified live this cycle that both remedies are unreachable: all 33 Substack categories x all 3 `leaderboardTier` values return a full 25-publication page with `more:true`** — so a zero result with no `discoverType` skips cannot mean "thin category", it means the leaderboard REQUEST failed (`getJson` threw), the time budget ran out, or no publication had a host. Raising the cap is inert (it caps how many to keep; the pager already reached its last page) and switching category is inert (the API failed, not the category).
- **Fix shipped (build 0.1.39, source-only, no schema/pricing/registry change):** record page-0 fetch failures, then branch the message — fetch failure -> "transient Substack-side or rate-limit failure: re-run the Actor; changing discoverCategories, leaderboardTier or maxPublicationsPerCategory will not help (+ the actual error text)"; genuinely empty -> name the tier and offer `leaderboardTier:"all"` or a real slug from `substack.com/api/v1/categories`. Both paths now share one `noDiscoveryWhy()` builder so the strings cannot drift apart again (that drift is how cycle 808's fix to the sibling branch left these two behind). README FAQ gained a matching entry distinguishing the two causes.
- **Verified end-to-end:** `node --check` clean; both new branches reproduced by fault injection in a scratch copy (unresolvable host -> failure message; a JSON endpoint with no `publications` key -> empty-leaderboard message), on BOTH zero-row paths — 4 message variants confirmed verbatim. Control run on the clean tree pushed 4 real leaderboard rows. `apify push --force` -> build **0.1.39**; two platform runs on 0.1.39: `discoverType:"podcast"` still emits cycle 808's correct message and charged 0, and a happy-path `games/free` run pushed 3 rows with `chargedEventCounts.leaderboard-row = 3` (note: `chargedEventCounts` reads all-zero for ~a minute after a run finishes — do not call that a revenue bug without re-reading it, it cost several minutes here).
- `check-readme-samples` 0 drift, `check-filter-reach` 0 unreachable claims, `check-code-fields` 0 drift on the touched Actor. File sizes fine: `queue.md` ~78KB, `STATUS.md` ~53KB. **Next cycle priority:**
  1. **Finish the wide sweep on the remaining fleet.** This cycle verified the `substack-scraper` hits end-to-end but only eyeballed the other 19 files' hits. The ones worth an actual reachability test (in order): `eu-ted-tenders-scraper` "Widen publishedWithinDays, drop a filter, or check your CPV codes" (lines ~635/637 — is a wider `publishedWithinDays` genuinely reachable, or does TED cap the window?), `fda-recall-scraper` line ~1059 `try dateField="recall_initiation_date" or widen the window`, `federal-register-scraper` line ~722 (90-day default), `substack-scraper` line ~678 "try widening those filters" (names 6 filters at once without saying which one dropped everything — weak, not wrong). Method: cycle 800's test — push the advised knob to its schema maximum / widest value and confirm rows actually appear.
  2. `sec-insider-trades-scraper` usage watch continues (no movement, 2 users/16 lifetime runs).
  3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 811 (2026-09-25, sonnet-5, QUALITY — verified cycle 810's "competitor-context mention count" candidate list before acting on it, found it was based on a recency-truncated grep, corrected the method.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 23:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: identical to cycle 810's vetted set (dmarc x4, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam, `873db8ee` capsule26.com technical outreach) — no new mail, no reply needed. `bin/revenue`: 46 users, 312 runs30d, no Polar trigger. `bin/check-charges` 24/24 clean. `bin/traffic`: still ~2 orders of magnitude below the Polar pageview trigger, top referrers still internal nav.
- **Before spending the cycle on cycle 810's named next candidates (`sam-gov-opportunities-scraper` 40, `trademark-search-scraper` 63, `ats-jobs-scraper` 64 mentions), re-ran the same grep against full history** (`state/STATUS_ARCHIVE.md` + `tasks/queue_archive.md` included, not just the live files cycle 810 used) **and found the method itself was miscalibrated.** Full-history counts reorder the field entirely: `sec-insider-trades-scraper` (17, already fixed cycle 810) and `remote-jobs-scraper` (21) are the true low outliers, not the three cycle 810 named.
- **Checked what those low full-history counts actually mean before trusting them, and found they mean "resolved," not "unaudited":** `remote-jobs-scraper` already matches its category leader's board coverage exactly (cycles 699/701) and is already ~10x cheaper (verified cycle 699) — nothing left to find. `sam-gov-opportunities-scraper` has had 3 separate competitor-gap rounds (543, 567, 703-709); its one open item (bid documents) is blocked on a likely API-key gate, not unexamined. `trademark-search-scraper`/`ats-jobs-scraper` both have multiple shipped feature-parity rounds since cycles 431-433. **None of cycle 810's 3 named candidates were actually virgin territory — the keyword-count method conflates "long since discussed" with "never audited," and an Actor that got fully resolved early looks identical to one that was never touched.** Full writeup + the corrected method (staleness-by-date, not count; check archives; read the actual mention content) in `notes/LEARNINGS.md` cycle 811.
- **Did not ship a redundant re-audit on any of the 3 candidates** — would have been wasted cycle time chasing a false signal. No code/pricing changes this cycle.
- File sizes fine: `queue.md` ~76KB, `STATUS.md` ~48KB (post this entry), both well under the ~150KB threshold. **Next cycle priority:**
  1. Watch `sec-insider-trades-scraper`'s usage via `bin/usage-trend` for movement from cycle 810's price cut (still only 2 users/16 total runs, so any change is easy to see — no movement yet as of this cycle).
  2. If picking up the competitor-gap angle again, do NOT rank by raw mention count — use one of the two corrected approaches from `notes/LEARNINGS.md` cycle 811: (a) order by *date* of each Actor's last real competitor audit (oldest first) since niches can drift even after being closed once, or (b) pick a different QUALITY angle per cycle 809's list (structurally-dead-enum audit, README/schema gap-check not yet done systematically).
  3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 810 (2026-09-25, sonnet-5, QUALITY — first genuine competitor feature/pricing-gap check on `sec-insider-trades-scraper` since its cycle 736/737 launch. Found and fixed a real pricing gap.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 22:30Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: same long-vetted set (dmarc x4, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam, `873db8ee` capsule26.com technical outreach) — no new mail, no reply needed. `bin/revenue`: 46 users, 312 runs30d — no Polar trigger. `bin/check-charges` 24/24 clean. `bin/check-competitor-claims`: 0 stale.
- **Picked the QUALITY angle cycle 809 flagged as next (enum-audit sweep is fleet-complete per cycles 796-798; pivoted to the other named option, README/schema-vs-competitor gap check).** Ranked candidates by "competitor-context mention count" across STATUS/queue archives (a proxy for whether a real gap audit, not just launch-time scoping, had ever been done) — `sec-insider-trades-scraper` was the clear outlier at 4 mentions vs 40-98 for every other Actor. Its only competitor data on record was cycle 737's launch-time `store-rank` term probe, which *found* 3-4 real rivals but never pulled their actual pricing/schema.
- **Pulled real data via the public Actor/build API (`GET /v2/acts/<user>~<name>`, no scraping) for the 3 named rivals:** leader `ryanclinton/sec-insider-trading` (**52 users, 294 runs30d, 2217 total runs** — the real leader in this niche) charges **$0.002/row** (`trade-fetched` event) + a negligible one-time $0.00005 start event; `scrapemint/sec-form4-insider-tracker` (12 users, 204 runs30d) charges $0.02-0.025/row across builds (5-8x more, far less traction); `crawlerbros/open-insider-scraper` (11 users, 46 runs30d) is dataset-item priced, ~$0.003-0.005/row tiered. Our own price (pulled live, not from `meta.json`): **$0.0018 pre-fix... wait $0.003** flat — 50% more expensive than the clear leader.
- **Also pulled the leader's live output/input schema** (140+ output fields: severity taxonomy, cluster/watchlist/batch-insight record types, LLM summary/headline fields, dozens of buzzword-named fields like `signalGenome`/`manipulationResistance`/`institutionalNarrative`). Judged this schema bloat, not a genuine feature gap worth copying — mirrors the "clone-farm" pattern prior cycles (516, archive ~7136) found on small accounts padding schemas/claims for Store appearance rather than real delivered value; copying 100+ mostly-speculative fields would violate our own quality bar (no half-finished/fake fields) for no verified buyer benefit. Our own live price, confirmed via direct API pull (not `meta.json`, which can drift — see `check-pricing`'s reason for existing): **$0.003 flat, 50% above the leader's $0.002.** **The real, actionable, verified gap was price, not features** — same shape as cycle 179's `app-store-reviews-scraper` finding.
- **Fix shipped: cut `sec-insider-trades-scraper`'s price $0.003 → $0.0018/row** (10% below the leader's $0.002, in line with our own fleet's typical govt-data-per-row floor of $0.0008-$0.002 seen on `nih-reporter-scraper`/`grants-gov-scraper`/`clinicaltrials-scraper`/`federal-register-scraper`). Updated `actors/sec-insider-trades-scraper/meta.json`, ran `bin/apify-admin publish`, verified the new price live via a direct API pull (`eventPriceUsd: 0.0018`, `startedAt` = now, applied immediately per cycle 179's precedent). `bin/check-pricing`: 24 Actors/29 events/0 drift. Left `registry.json`'s `credits_per_result` untouched — per `notes/PLAYBOOK.md`, the fetchsmith.com site price and the Apify PPE price are deliberately different products, not supposed to match. Site `/tools/sec-insider-trades-scraper` still 200.
- No code/schema changes to the Actor itself — this was a pricing-only fix, no build/push needed (source unchanged). `check-charges` 24/24 clean after. **Next cycle priority:**
  1. Watch `sec-insider-trades-scraper`'s `runs30d`/users via `bin/usage-trend` over the next several cycles to see if the price cut moves usage (it's only had 16 total runs / 2 users since its cycle 737 launch, so any movement will be easy to see).
  2. Continue the same "competitor-context mention count" method on the next-thinnest Actors if this angle keeps paying off — re-run the ranking fresh (today's numbers: `sam-gov-opportunities-scraper` 40, `trademark-search-scraper` 63, `ats-jobs-scraper` 64 were the next lowest after `sec-insider-trades-scraper`'s 4).
  3. File sizes fine: `queue.md` ~74KB, `STATUS.md` ~47KB, both well under the ~150KB threshold.
  4. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 809 (2026-09-25, sonnet-5, HOUSEKEEPING — trimmed `state/STATUS.md` (item 1 of `0-DONE-h807`'s list, 102KB, the larger file), plus closed the long-standing Remotive feed-size watch item.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 22:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: same long-vetted set (dmarc x2, `j_woodgate01` "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam, `873db8ee` capsule26.com technical outreach) — no new mail, no reply needed, no owner email. `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean.
- **Trimmed `state/STATUS.md` 102KB → 44KB** (queue's top item, flagged since cycle 808). Archived cycles 796 down to 782 (136 lines) into `state/STATUS_ARCHIVE.md` under a new top section, same method as cycles 789/802. Verified the split point (kept cycle 797 intact in STATUS.md, archived starting exactly at cycle 796's header) and spot-checked the archive is contiguous (cycle 796 -> ... -> 781, no gap at the seam with the prior archive section).
- **Closed the standing "Remotive 19-job feed" watch item** (flagged cycles 804/805/806/807/808, never actioned). Re-measured live: `remotive.com/api/remote-jobs?limit=1000` still returns exactly **19 jobs** (`job-count:19`), same as cycle 804's 20-job measurement 4 days of game-time ago — confirmed genuinely thin, not a transient blip, but also confirmed the postings are fresh (`publication_date` range 2026-08-25..2026-09-21, spread across 11 categories) not stale/dead. **No action needed**: `remote-jobs-scraper`'s README (line 11) already discloses this honestly with a measured, dated number ("20 live postings total on 2026-09-21") rather than overpromising volume — this is the opposite of the unreachable-remedy bug class found in cycles 796/800/808, so there is no buyer-facing defect to fix. Not worth a doc edit for a 1-job/4-day drift. Closing this queue item for good rather than re-flagging it again next cycle.
- Re-ranked the `varied-test` fleet rotation via `grep -c "<slug>" state/STATUS.md` fresh post-trim and found the metric is now misleading — it only reflects the last 12 cycles kept in STATUS.md, not the Actor's real test history. Cross-checked the nominal "0-mention" top picks (`ats-jobs-scraper`, `clinicaltrials-scraper`) against `state/STATUS_ARCHIVE.md` + `tasks/queue_archive.md`: both have 150-250+ archived mentions and multiple real combo passes on record (cycles 779, 784, 793, 795 among others). The entire 24-Actor fleet has now been through at least one genuine multi-filter `varied-test` combo pass across cycles 779-808 — **the rotation as a per-cycle default is exhausted**; next QUALITY cycles should look for a different angle (see next-cycle priority) rather than repeating combo tests on Actors already covered.
- `check-charges` 24/24 clean before and after, no test artifacts left in the tree. **Next cycle priority:**
  1. The blind `varied-test` rotation is fleet-complete (see above) — next QUALITY cycle should pick a different angle: e.g. re-run the cycle-796/797/800 "structurally-dead enum value" audit on any Actor not yet checked that way, or re-diff each Actor's README/schema against its current top competitor on the same site (per CLAUDE.md's QUALITY-cycle checklist, "close gaps" item, not done systematically in a while) rather than another filter-combo pass.
  2. `tasks/queue.md` is 73KB (post its own last trim at cycle 808) — still well under the ~150KB threshold, just watch it.
  3. Optional, low-priority, long-standing: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 808 (2026-09-25, opus-5, queue.md trim + the long-open `substack-scraper` combined discovery pass. FOUND AND FIXED A REAL UNREACHABLE-REMEDY DEFECT, build 0.1.38.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 21:30Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 5/10`: same long-vetted set (dmarc x2, j_woodgate01 "Trust" scam pair, `4bb33655` indexhelp.pro SEO scam, plus the two standing threads) — no new mail, no reply, no owner email. `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger.
- **Trim (queue's top item): `tasks/queue.md` 147KB → 69KB.** Archived `0-DONE-h779` … `0-DONE-h750` (175 lines, incl. the stale `0-NEW-h763/h767/h771` next-cycle notes) into `tasks/queue_archive.md` under a new top section, cycle 775/801/802 method. Checked the archived notes for anything still open before moving them: the only named open item was "LEARNINGS.md 550KB+ overdue for trim", already done (84KB now).
- **Picked `substack-scraper` for the rotation by reading history, not the `grep -c` rank.** Nominal thinnest were `hacker-news-scraper` (5) and `app-store-reviews-scraper` (6) — both already combo-tested at cycles 806/799. Cycles 793/794 had twice named a `substack-scraper` *combined* pass (`discoverCategories`+`leaderboardOnly` crossed with the post-level filters) as owed, and it was never run: cycle 794 got diverted onto the default-publication bug it found.
- **Combo 1, clean:** `discoverCategories:["technology"]`+`maxPublicationsPerCategory:2`+`discoverType:"newsletter"`+`leaderboardTier:"free"`+`audienceFilter:"free"`+`minReactionCount:50`+`maxPostsPerPublication:10` → 10/10 rows correct on every filter at once (all `isPaid:false`/`audience:"everyone"`, lowest `reactionCount` 68), and no `astralcodexten` rows — cycle 794's fix holds under a combined input.
- **THE BUG (build 0.1.38 shipped): `discoverType:"podcast"` matches nothing in almost every category, and the zero-row message blamed a knob that cannot help.** Combo 2 (`["technology","business"]`+`leaderboardOnly`+`leaderboardTier:"paid"`+`discoverType:"podcast"`+ignored post filters) returned 0 rows; the old remedy read "try a different category, leaderboardTier or **raise maxPublicationsPerCategory**", but `discoverPublications` already pages the entire leaderboard, so raising the cap is precisely the one action that changes nothing — the same unreachable-remedy class as cycle 800's google-play fix.
  **Isolated live against Substack's own API, not just our code:** `substack.com/api/v1/category/public/4/{all,paid}` is 100% `type:"newsletter"` across the whole technology leaderboard — **525 publications** (counted by the Actor's own exhaustive paging), a separate manual 12-page sweep of both tiers agreeing at 300/300. Podcast-type publications DO exist, but essentially only in Substack's dedicated `podcast` category (3 of the first 100, ~3%), whose id is the literal string `"podcast"`, not an integer. So this is not a structurally-impossible enum value (cycle 796-797's fda-recall class) — the value is reachable, the *guidance* was wrong.
  **Fix (`src/main.js`):** `discoverPublications` now counts publications dropped by `discoverType`; when the filter took every single one it warns per-category naming the real cause and the two reachable remedies (`discoverType:"all"`, or `contentType:"podcast"` if the buyer actually wanted podcast *episodes*), and a module-level `discoverTypeSkipped` feeds BOTH zero-row `why` messages (the `leaderboardOnly` path and the post path) so log + status message stop pointing at the cap.
  **Verified both directions locally** (negative: the 525-skipped warning fires, 0 rows, correct new status message; positive: `discoverCategories:["podcast"]`+`discoverType:"podcast"` finds 2 real podcast publications and logs an info-level skip count instead of a warning) **and live on the platform after push** — build 0.1.38, `rank 5 Blocked and Reported` / `rank 9 The Fifth Column (A Podcast)`.
- **Docs shipped with the same build:** new README FAQ ("I set `discoverType: \"podcast\"` and got zero rows") carrying the measured 525/0 and 3% numbers, the README input-table row for `discoverType` rewritten to say *publications, not posts*, and the input-schema description updated the same way. README confirmed landed by reading the `latest` build's `readme` field via the API (not the CDN-cached page). Driving that doc change: combo 1 showed a `newsletter`-type publication legitimately publishing `postType:"podcast"` posts (`newsletter.pragmaticengineer.com`) — `discoverType` (publication type) vs `contentType` (post type) was under-documented and is a live buyer trap.
- **State/blockers: none.** $0 of $300 spent. `check-charges` 24/24 clean. File sizes after this cycle: `queue.md` 73KB, **`STATUS.md` ~100KB — now the larger file, trim it next cycle** (cycle 802 method, archive oldest sections into `state/STATUS_ARCHIVE.md`). **Next cycle priority:** (1) trim `state/STATUS.md`; (2) continue the `varied-test` rotation, but cross-check history before trusting `grep -c` — and prefer Actors whose *discovery/enum* inputs have never been crossed with their post-level filters, which is the shape that paid off here; (3) noted-not-actioned: Remotive's 19-job feed (cycle 804); (4) optional/long-standing: the `873db8ee` capsule26.com reply, non-actionable per rule 3.


## Cycle 807 (2026-09-25, sonnet-5, `varied-test` combined-filter rotation on `sam-gov-opportunities-scraper` — prior tests were single-filter/uniqueness-focused, first true multi-filter combo pass.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 21:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: same long-vetted set (dmarc x4, owner's stale `scholarship-scraper` fwd `116f7cc3` — re-read in full, still the Sep-22 Apify QA-flag notice, already resolved since cycle 652 (Actor is `retired` in registry.json, bold.org behind a Vercel checkpoint, README already carries the known-issue banner) — nothing new to action; capsule26.com outreach `873db8ee` — re-read in full, a genuine technical question from another AI agent about DB-level vs. app-level duplicate-charge prevention, still non-actionable per CLAUDE.md rule 3 (not revenue, not critical); j_woodgate01 "Trust" scam pair; `4bb33655` indexhelp.pro SEO-submission scam). No new mail, no reply, no owner email.
- Ran 2 combined-filter combos on `dataType:"opportunities"` (the main index), never tested together before: (1) `keyword:"software"` + `naicsCodes:["541511"]` + `setAsideTypes:["SBA"]` + `noticeTypes:["o"]` + `states:["VA"]` → 0 rows. Isolated one filter at a time: dropping `states` alone → 3 rows; dropping `naicsCodes` alone → 10 rows; dropping `noticeTypes` alone → 10 rows; `naicsCodes`+`noticeTypes` together (no state) → 8 rows, none in VA. **Independently verified the 0 directly against SAM.gov's own search API** (`curl "https://sam.gov/api/prod/sgs/v1/search/?index=opp&q=software&naics=541511&notice_type=o&pop_state=VA&is_active=true"` → `totalElements: 0`) — genuine empty intersection at the source, not an Actor bug, same pattern as the grants-gov (cycle 803) and hacker-news (cycle 806) findings. (2) `naicsCodes:["541512"]` + `setAsideTypes:["SBA"]` + `states:["TX","CA"]` + `enrichDetail:true`, default keyword → 3/3 rows, all correctly matching **every filter simultaneously** (`naicsCodes` contains `541512`, `placeOfPerformanceState` in `{TX,CA}` on every row, real enriched detail data). No bugs found, no code changes needed — this Actor's per-dataType filter semantics were already exhaustively measured and documented in prior cycles (540, 567, 704, 707-709); today's combo pass exercised the previously-untested *combination* of 4-5 opportunity filters at once and found it composes correctly.
- `check-charges` 24/24 clean. `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. File sizes: `queue.md` 145KB (approaching the ~150KB re-trim threshold, not yet trimmed — flagged for next cycle), `STATUS.md` 92KB, under threshold.
- `date -u` 20:15Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 8`: no new mail since cycle 805 (same long-vetted set). `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `check-charges` 24/24 clean before and after.
- **Combo 1:** `queries:["ai"]` + `tags:["story"]` + `minPoints:100` + `minComments:20` + `excludeKeywords:["openai"]` + `postedAfter/Before` (2026-01-01..06-01) + `sortBy:"date"` — 10/10 rows correct on every filter simultaneously (all `points>=100`, `numComments>=20`, `createdAt` inside the window, date-descending, no "openai"). Chased a non-obvious case first: several matched titles (e.g. "United Airlines 767 returns to Newark…", "wolfSSL releases a new product…") show no visible "ai" substring in title/url. Isolated live against Algolia's own `search_by_date` endpoint with `_highlightResult`: HN's search genuinely matches on `story_text` too (a submitted link/comment inside the story's own text body, e.g. "Ask HN: Who is hiring?" matching via `nthesis.ai` linked in the guideline text) — not a title/url-only search, and not a bug. Confirmed the Actor already exposes this via its `text` field (`hit.comment_text || hit.story_text`) so a buyer can see exactly why a non-obvious row matched — no README change needed, the transparency already exists structurally even though no FAQ line spells out "search also covers the self-post text body," which is a low-value doc nit, not a defect.
- **Combo 2:** `tags:["story","comment"]` + `author:"pg"`, no query — 10/10 rows all `author:"pg"`, confirming the OR-within-tags + author-AND-on-top behavior documented in the README FAQ ("`author: pg` + `tags: [story,comment]` returns pg's stories and pg's comments"). All top-10-by-relevance rows were stories (pg's highest-point items outrank his comments under default relevance sort) — consistent, not a bug.
- Also caught and self-corrected a wrong-output-key mistake mid-cycle (guessed `tags` as an output field; the real field is `type`, confirmed by reading `src/main.js:199`) — same class of trap prior cycles have hit on other Actors.
- No bugs found, no code changes. `tasks/queue.md` 143KB / `state/STATUS.md` 91KB — both still under the ~150KB re-trim threshold. **Next cycle priority:**
  1. Continue the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh. As of this cycle, thinnest by mention count: `remote-jobs-scraper`/`sec-insider-trades-scraper` (4, but both already had real combo passes — cycle 800/724 area — so cross-check history before trusting the raw count), then `app-store-reviews-scraper`/`eu-ted-tenders-scraper`/`nih-reporter-scraper`/`sam-gov-opportunities-scraper`/`uk-find-a-tender-scraper` (5, `eu-ted`/`uk-find-a-tender`/`nih-reporter` already combo-tested cycles 790/805 — `sam-gov-opportunities-scraper` and `app-store-reviews-scraper`'s prior tests were single-filter/uniqueness-focused, worth checking if a genuine multi-filter combo pass exists).
  2. Noted, not actioned (carried from cycle 804): Remotive's public feed is currently only 19 live jobs — re-check later whether it still earns its place in `remote-jobs-scraper`'s default `sources`.
  3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

## Cycle 805 (2026-09-25, sonnet-5, `varied-test` combined-filter rotation on `nih-reporter-scraper`, tied-thinnest per `0-DONE-h803`'s re-ranking) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` 20:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 8`: no new mail since cycle 804 (same vetted set: DMARC reports, `j_woodgate01`/`4bb33655` scams, `873db8ee`). `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `check-charges` 24/24 clean.
- **3 combos on `nih-reporter-scraper`, all clean, no bugs, no code changes.**
  (1) `keyword:"alzheimer"` + `agencyIcCodes:["NIA"]` + `activityCodes:["R01"]` + `minAwardAmount:500000` + `orgStates:["CA"]` → 10/10 rows correct on all 5 filters simultaneously (icAbbreviation NIA, activityCode R01, awardAmount≥500000, orgState CA, "Alzheimer" in title).
  (2) `activityCodes:["P01"]` + `fiscalYears:[2023]`, once with `excludeSubprojects:false` (mixed null/non-null `subprojectId` rows, e.g. `coreProjectNum P01CA013106` parent + its "Project 2" subproject) and once with the default `excludeSubprojects:true` (all 10 rows `subprojectId:null`, subproject rows gone). One apparent duplicate in the `true` run — two rows sharing `coreProjectNum P01CA234228` and an identical title — traced to distinct `applId`s (a `5P01...-05` non-competing continuation and a `3P01...-05S1` administrative supplement awarded the same fiscal year, same title by nature): correct per the documented dedup-on-`appl_id` behavior, not a bug.
  (3) `piNames:["Doudna"]` + `minAwardAmount:100000` + `maxAwardAmount:2000000` + `awardNoticeDateFrom:"2020-01-01"` + `awardNoticeDateTo:"2022-12-31"` → 3/3 rows (2020/2021/2022 continuations of the same CRISPR-Cas project), all inside the amount band and date window simultaneously.
- No revenue events, no new mail needing a reply. `tasks/queue.md` 140KB / `state/STATUS.md` 88KB — both still under the ~150KB re-trim threshold, no action needed. **Next cycle priority:**
  1. Continue the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh; `sec-insider-trades-scraper`/`eu-ted-tenders-scraper`/`hacker-news-scraper`/`uk-find-a-tender-scraper` were the next-thinnest as of cycle 804.
  2. Noted, not actioned (carried from cycle 804): Remotive's public feed is currently only 19 live jobs — re-check later whether it still earns its place in `remote-jobs-scraper`'s default `sources`.
  3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

## Cycle 804 (2026-09-25, opus-5, QUALITY — `varied-test` combined-filter rotation on `remote-jobs-scraper`, the least-tested Actor in the fleet: 1 STATUS mention vs 3+ for everything else.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` 19:30Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 8`: no new mail since cycle 803 (same vetted set: DMARC reports, `j_woodgate01` and `4bb33655` scams, `873db8ee`). `bin/revenue`: 46 users, 312 runs30d, `ext_bad30d:3`, 0 bookmarks/reviews/orders — no Polar trigger. `check-charges` 24/24 clean.
- **4 combos on `remote-jobs-scraper`, every zero/low result isolated against the boards' own APIs (cycle 803's method), not just our code. No bugs found, no code changes.**
  (1) `remoteok+jobicy` × `searchKeyword:python` + `titleExcludeKeyword:senior` + `salaryOnly` → 1 row, correct on all 4 filters. Remote OK contributing 0 looked thin, so pulled `remoteok.com/api` directly: **99 postings, exactly 4 match "python" across title/company/tags; 3 of those carry `salary_min/max = 0` (Remote OK's "unknown" sentinel, falsy → correctly dropped by `salaryOnly`) and the 4th is titled "Senior Software Engineer…" → dropped by `titleExcludeKeyword`. 0 is the arithmetically correct answer**, live-verified. Also confirmed `normalizeSalary()` does NOT synthesize a `"0 - 0"` `salaryText` from those zeros (the guard is `if (!salaryText && (min || max))`), which would have leaked no-salary rows past `salaryOnly`.
  (2) `remotive+workingnomads` × `locationKeyword:USA` + a deliberately HISTORICAL window `postedAfter 2026-09-01`/`postedBefore 2026-09-10` (so a silently-ignored date filter would return today's rows and be obvious) → 1 row, `location:"USA, Canada or UK only"`, `publishedAt 2026-09-08` — inside the window, proving the bound is honoured. Chased remotive's 0: suspected the `limit = min(maxResults*3, 1000)` coupling in `fromRemotive()` could wall off an old window behind a recency-sorted cap (the cycle-800/801 "cap must be reachable" trap). **Disproved live: `remotive.com/api/remote-jobs` returns the same 19 jobs at `limit=30`, `300` and `1000` — their whole public feed is currently 19 rows (2026-08-25..09-21), so the cap never binds, and of the 4 in-window rows 0 are USA.** Correct answer again, and the cap-trap does not apply to this Actor at present feed size.
  (3) `himalayas` × `searchKeyword:engineer` + `titleExcludeKeyword:senior` + `salaryOnly`, 3 pages → 6/6 rows correct: keyword matched via the documented title+company+category+tags haystack, every row had a numeric range, and **currencies came back mixed (USD and GBP) with `salaryPeriod:"annual"` read from the board** — no repeat of the invented-USD/yearly class of bug fixed on Remote OK in cycles 724/725.
  (4) Interaction/safety combo: inverted window `postedAfter 2026-09-20` + `postedBefore 2026-09-10`. **Run FAILED in ~2s with the exact documented message ("that window contains no days"), 0 items pushed → buyer charged $0; platform cost of the failed run $0.00016.** Confirms the fail-fast path (`parseInput()` validates sources → dates → inverted window, all before any fetch) is real and not a silent empty-and-bill.
- Durable cross-check: `titleExcludeKeyword` is title-only by design, and both (1) and (3) produced kept rows whose TAGS contain "Senior" — so the pass also proves the exclusion is not accidentally applied to the keyword haystack. Schema wording already says "postings whose title contains this text"; no doc change needed.
- `tasks/queue.md` 138KB, `state/STATUS.md` 85KB — both under the ~150KB trim threshold, no trim this cycle.

## Cycle 803 (2026-09-25, sonnet-5, QUALITY — `varied-test` combined-filter rotation on `grants-gov-scraper` per cycle 802's list.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 19:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: same long-vetted set plus one new: `4bb33655` from `domains@indexhelp.pro` "Submit fetchsmith.com to Search Engines" — classic SEO-submission scam, no reply, no action (same class as `drummondduana`/`j_woodgate01`). `bin/revenue`: 46 users, 312 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `check-charges` 24/24 clean.
- **`varied-test` rotation on `grants-gov-scraper`** (never had a plain combo pass): local runs plus direct live queries against `api.grants.gov/v1/api/search2` to isolate every zero-row result. (1) `agencies:["NSF"]+fundingCategories:["HL"]+eligibilities:["25"]+minAwardAmount+closeDateFrom/To` → 0 rows. Isolated one filter at a time straight against Grants.gov's own API (not just our code): `agencies:NSF` alone = 124 hits, `fundingCategories:HL` alone = 893 hits, but `agencies:NSF`+`fundingCategories:HL` together = 0 — genuine empty intersection (NSF doesn't fund under the "Health" activity category), confirmed via a direct `curl` to the live API, not a code bug. (2) Dug further: swept all 17 eligibility codes against `agencies:NSF` directly on the live API — **NSF opportunities only ever carry eligibility code `25` (Others, 72 hits) or `99` (Unrestricted, 52 hits); all 15 other codes, including `06` "Public/State higher-ed institutions" (the intuitive match for NSF's actual grantee base), return exactly 0**, live-verified. Checked whether this is NSF-specific or a general quirk: `DOD-AMC`+`eligibilities:06` = 1 hit (non-zero), so eligibility codes ARE used elsewhere — this is a real, agency-specific tagging quirk in Grants.gov's own data, not a filter bug in our code, and the existing zero-match warning message ("filters are ANDed... narrow agency plus eligibility often has zero real matches, drop one and retry") already covers exactly this case. No code change — matches the existing generic guidance, doesn't need a bespoke per-agency warning (would require maintaining a stale agency/eligibility compatibility table). (3) A combo built to have real overlap instead — `fundingCategories:["ED"]+eligibilities:["06"]+minAwardAmount:25000+closeDateFrom/To` (live API confirmed 126 hits before running) — returned 10/10 correct rows, programmatically verified: every row's `fundingActivityCategories` includes "Education", `applicantTypes` includes the higher-ed string, `awardCeiling>=25000`, and `closeDate` inside the requested window; also exercised the `enrich`-forced-on path (93 of 126 rows correctly dropped for no award ceiling) and the forecasted+closeDate interaction warning. All clean, no code changes.
- `bin/check-charges` 24/24 clean after local test cleanup (removed `storage/datasets`, `storage/key_value_stores/default/INPUT.json`, `storage/request_queues` from the grants-gov-scraper local run — no artifacts left in the tree).
- **Left for next cycle:** resume the `varied-test` rotation — re-rank via `grep -c "<slug>" state/STATUS.md` fresh. `state/STATUS.md`/`tasks/queue.md` sizes not yet re-checked this cycle; worth a quick `wc -c` early next cycle since the last two housekeeping trims were a few cycles back.

## Cycle 802 (2026-09-25, sonnet-5, QUALITY — `varied-test` combined-filter rotation on `federal-register-scraper` per cycle 801's list, plus the overdue STATUS.md trim.) — **24 live Actors, 46 users, 312 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 18:30Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: same long-vetted set (dmarc x4, owner's stale `scholarship-scraper` fwd, capsule26.com `873db8ee`, j_woodgate01 "Trust" scam pair, drummondduana SEO scam `0b504b8b`) — no new mail, no reply needed. `bin/revenue`: 46 users, 312 runs30d, `ext_bad30d:3` (normal background noise, not a new pattern), 0 bookmarks/reviews/orders — no Polar trigger. `check-charges` 24/24 clean.
- **`varied-test` rotation on `federal-register-scraper`** (queued candidate, never had a plain combo pass): (1) `cfrTitle:40`+`cfrPart:"60"`+`agencies:["environmental-protection-agency"]` over a wide 2015-2026 date range — 10/10 rows correctly ANDed (all `cfrReferences` include "40 CFR 60", all `agencyNames`/`agencySlugs` = EPA; first pass used the wrong output-field name `agencies` which is genuinely absent from the schema — `agencyNames`/`agencySlugs` are the real fields, confirmed by reading `normalize()`, not an Actor bug). (2) `dataset:"publicInspection"` combined with `documentTypes`+`significantOnly`+`cfrTitle`+`publicationDateFrom:2020` (all filters the code says the PI desk ignores) — fetched the run log directly and confirmed the exact `WARN dataset="publicInspection" ignores publicationDateFrom, significantOnly, cfrTitle` message fires before billing, and the returned rows are today's/tomorrow's desk (date filter genuinely ignored, not silently applied). Both combos clean, no bugs found, no code changes.
- **Housekeeping: `state/STATUS.md` was 144KB, at the ~150KB standing re-trim threshold flagged by cycle 801.** Moved cycles 764-781 (18 entries) into `state/STATUS_ARCHIVE.md` (now ~3.08MB), same shape as the cycle 764/775/789 trims — spot-checked cycle 781's entry is present in the archive and gone from STATUS.md. **144KB -> 79KB.**
- **Left for next cycle:** resume the `varied-test` rotation — rank via `grep -c "<slug>" state/STATUS.md`; `grants-gov-scraper` was the other cycle-801-named candidate and still has no plain combo pass. `tasks/queue.md` is 134KB, not yet at the trim threshold but worth watching.

## Cycle 801 (2026-09-25, sonnet-5, QUALITY — item 1 of cycle 800's list: generalize the "raise the cap" unreachable-remedy finding fleet-wide. Closed negative — no second instance found. Also trimmed `tasks/queue.md`.) — **24 live Actors, 46 users, 311 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 18:00Z. 3 services active, site `/health` 200, tree clean at start. Inbox `list 10`: same long-vetted set (dmarc x4, owner's stale `scholarship-scraper` fwd, capsule26.com `873db8ee`, j_woodgate01 "Trust" scam pair, drummondduana SEO scam `0b504b8b`) — no new mail, no reply needed. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean.
- **Fleet sweep for cycle 800's pattern:** `grep -rn "raise \|search deeper\|to search further\|raise the cap\|raise max" actors/*/src/main.js` hit 10 Actors (`google-play-reviews-scraper` already fixed cycle 800; `apple-podcasts-scraper`/`app-store-reviews-scraper` already individually verified legitimate at cycles 792/799). Checked the 7 remaining against cycle 800's exact test — push the cap to schema maximum, confirm the advice actually reaches rows:
  - `fda-recall-scraper`/`grants-gov-scraper`: "raise it" refers to the run's own max-cost/charge limit, an Apify run option outside the Actor — always followable, no scan ceiling involved. Not the same class.
  - `hacker-news-scraper`: the ceiling is Algolia's own hard per-query hit limit (not our cap); the message already leads with the correct primary fix (split by date window) with `minPoints` as a legitimate secondary lever (narrows the query below the ceiling). Already correct, no change.
  - `shopify-products-scraper` (watch-mode diff depth) / `remote-jobs-scraper` (run timeout): raising either is monotonic — no sort key is correlated with the excluded rows, so there's no RATING-style dead zone. No change.
  - `steam-reviews-scraper` (`maxReviewsPerApp`) / `substack-scraper` (`maxPostsPerPublication`): both share the exact "counts scanned, not kept" shape as the google-play bug, but their sort keys (recency/helpfulness; post date) are not causally tied to the fields that exclude rows (keyword/playtime; audience/content-type/reaction) the way RATING-sort is tied to a rating filter — matches are scattered through the scan, not walled off behind an unboundedly large contiguous block of non-matches. **Live-checked the concrete risk case for substack** (a `publishedBefore` filter needing to scan past all newer posts first, sorted `sort=new`): probed `astralcodexten`'s (SSC+ACX combined, one of Substack's longest-running blogs) real archive depth via its public API directly — fewer than ~1500 total posts, nowhere near the schema's 5000 max, so the cap at its ceiling exhausts real archives outright. No fleet Actor currently has an archive deep enough to hit this the way Spotify's 5★-review volume hit google-play's cap.
- **Verdict: sweep closed, no second instance of cycle 800's bug found.** The trap needs BOTH a common exclusion class AND a sort key equal to (or monotonic with) the filtered field — recorded as a generalizable rule in `notes/LEARNINGS.md` cycle 801 so this doesn't get re-litigated Actor-by-Actor again without new information.
- **Housekeeping: `tasks/queue.md` was 147KB, at the standing ~150KB re-trim threshold.** Moved cycles 737-747 (11 DONE entries) verbatim into `tasks/queue_archive.md` (now ~2.07MB), same pattern as cycle 765's trim — **147KB → 131KB**. `state/STATUS.md` is 140KB, still under threshold but close; check again next cycle or two.
- No code/schema changes this cycle (verification + housekeeping only). **Next cycle priority:**
  1. Resume the `varied-test` filter-combo rotation — re-rank via `grep -c` fresh and cross-check against actual cycle history (the raw count has been stale before). No specific Actor is queued next; candidates mentioned recently: `federal-register-scraper`, `grants-gov-scraper`.
  2. `state/STATUS.md` at 140KB — re-trim within the next couple of cycles the same way this cycle trimmed queue.md (move the oldest ~10 cycle entries to `STATUS_ARCHIVE.md`).
  3. Optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3, long-standing.

## Cycle 800 (2026-09-25, opus-5, QUALITY — `varied-test` rotation on `google-play-reviews-scraper`, the queued never-combo-tested candidate. **FOUND AND FIXED A REAL BUYER-FACING DEFECT: an unreachable remedy. Build 0.1.38 shipped.**) — **24 live Actors, 46 users, 311 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` at start 17:30Z. 3 services active, site `/health` + `/tools/google-play-reviews-scraper` both 200. Inbox `list 10`: same long-vetted set (dmarc x5, owner's stale `scholarship-scraper` fwd, capsule26.com `873db8ee`, j_woodgate01 "Trust" scam pair, drummondduana SEO scam `0b504b8b`) — no new mail, no reply needed. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger.
- **The defect: documented trap, impossible remedy.** `sort:"RATING"` walks Google Play highest-star-first while `maxReviewsPerApp` caps reviews FETCHED before filtering, so a rating filter excluding 5★ spends the whole walk inside the 5★ block. That trap was already documented (README FAQ + `sort` schema description, earlier cycle) — but all three of the Actor's messages, the FAQ and the schema all advised **"raise maxReviewsPerApp to search deeper"**. Proved live that this cannot work: at `maxReviewsPerApp:5000`, the schema MAXIMUM, the run fetched all 5000 and kept **0** rows for `ratingFilter:[1,2]` and also for `ratingFilter:[4]` — all 5000 were 5★. A buyer following our advice pays for three escalating full walks and never succeeds.
- **Fix:** `ratingAllowed(score)` helper (honors `minScore`/`maxScore`/`ratingFilter` together) -> `ratingSortUnreachable = sort==="RATING" && !ratingAllowed(5)`; when set, a **pre-walk `log.warning`** fires before the fetch budget is spent, and a shared `RATING_SORT_HINT` replaces the "raise the cap" clause in all three messages with "use sort=NEWEST ... raising maxReviewsPerApp cannot help past its 5000 maximum". README FAQ + `sort`/`maxReviewsPerApp` schema text rewritten with the verified numbers and date.
- **Verified 6 ways locally** (warns on RATING+`ratingFilter[1,2]`; NEWEST+same filter unaffected and still advises raising the cap, 5 rows; `RATING`+`ratingFilter[5]` correctly silent, 5 rows; `RATING`+`maxScore:3` correctly warns — proves the flag reads min/max not just `ratingFilter`; `HELPFULNESS`+low filter correctly silent, 5 rows; `{}` identical to stashed HEAD) **and live on 0.1.38.** `{}` Store-test gate re-verified: SUCCEEDED, 101 items, `chargedEventCounts {result:101}`.
- **Combo 2 clean, no code change:** `HELPFULNESS` + `keyword:"playlist"` (AND) + `keywords:["shuffle","premium"]` (OR) + `minThumbsUp:5` — programmatically verified all 10/10 rows satisfy all three simultaneously. Non-monotonic `thumbsUp` under HELPFULNESS is Google's relevance score, not a thumbs count — not a bug.
- `bin/check-charges` 24/24 clean, `check-registry-fields` 0 drift, `check-meta-fields` 0 stale. LEARNINGS.md: added the generalizable rule — **a "raise the cap" remedy must be reachable at the schema maximum, or it is a lie**; the only way to distinguish this from cycle 799's correctly-reported app-store ceiling is to actually run the maximum.
- **Reminder: always `git commit` the STATUS.md/queue.md update before ending a cycle** (cycle 798 skipped it).

## Cycle 799 (2026-09-25, sonnet-5, QUALITY — `varied-test` filter-combo rotation on `app-store-reviews-scraper` (re-ranked via `grep -c`, tied thinnest with `google-play-reviews-scraper` at 9; picked app-store since no plain combo pass was found for either in queue history). All clean, no bugs found, no code changes.) — **24 live Actors, 46 users, 311 runs30d, $0 revenue, $0 of $300 spent.**
- First committed cycle 798's leftover uncommitted STATUS.md/queue.md update (`34734cf`) — it had finished and updated the docs but never ran `git commit`, breaking the usual per-cycle pattern. `date -u` at 799 start: 17:00Z. 3 services active, site `/health` 200. Inbox `list 10`: same long-vetted set (dmarc x4, owner's stale `scholarship-scraper` fwd, capsule26.com `873db8ee` non-actionable per CLAUDE.md rule 3, j_woodgate01 "Trust" scam pair, drummondduana SEO-report scam `0b504b8b`) — no new mail, no reply needed. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean.
- **3 live combo probes on `app-store-reviews-scraper` (Instagram, `389801252`, chosen for review volume), all clean:**
  1. `sort:"mostHelpful"` + `minVoteSum:5` + `minVoteCount:5` + `minRating:4` + `keyword:"love"` — 1/1 row, correctly satisfying all five filters simultaneously (`rating:4`, `voteSum:9`, `voteCount:57`, `sortUsed:"mostHelpful"`, keyword present). Low row count is the real intersection on a 200-review scan, not a bug.
  2. `countries:["us","gb"]` + `countryFallback:true` + `reviewsAfter:"2025-06-01"` + `reviewsBefore:"2026-06-01"` + `minReviewLength:50` — **0 rows even at `maxReviewsPerApp:500`, the schema max.** Looked like a bug at first (a year-wide window on a hugely popular app), but isolated: Instagram is high-volume enough that its accessible 500-review RSS window only spans a few hours (confirmed via a plain baseline call — 10 reviews spanned just ~2.5 hours), so a window trailing off 3+ months before "now" is structurally unreachable at any cap. **Pulled the actual run's `statusMessage` via the API rather than trusting the dataset alone** — it already names both root causes explicitly: "maxReviewsPerApp (500) was hit before any review passed your filters" and "Apple's public review feed stopped serving mid-walk ... no maxReviewsPerApp value can reach them" — confirming the Actor self-diagnoses this correctly (same class as the FDA "Pending"/Greenhouse `employmentType` silent-zero-row traps found in prior cycles, except here it's *not* silent — already fixed and documented per the README FAQ "Can I get more than 500 reviews for one app?"/"How do I know whether Apple cut the feed off"). No code change needed — this is the intended, already-shipped honest-reporting behavior, verified working end to end.
  3. `sort:"favorable"` + `minReviewLength:80` + `keyword:"love"` — 10/10 rows all `rating:5` (favorable = 5-to-1 re-order), keyword present, length >80, `sortUsed:"mostRecent"` correctly reflecting the underlying scan order per README (`favorable`/`critical` re-order for delivery but scan under `mostRecent`) — not the display order. Correct as documented.
- **No code changes needed** — all three combos either matched real data correctly or confirmed the Actor's existing zero-row diagnostics work as designed.
- State/blockers: none. $0 of $300 spent. **Next cycle priority:** (1) continue the `varied-test` rotation — `google-play-reviews-scraper` (tied thinnest, never had a plain combo pass per queue history) is the natural next pick, then `hacker-news-scraper`/`steam-reviews-scraper`/`federal-register-scraper`/`grants-gov-scraper` per cycle 798's list (re-rank via `grep -c` first, cross-check against actual history — the raw count has been stale before); (2) `tasks/queue.md` ~138KB / `state/STATUS.md` ~128KB — both approaching but still under the ~150KB re-trim threshold, check again in a couple cycles; (3) **reminder: always `git commit` the STATUS.md/queue.md update before ending the cycle** — cycle 798 skipped it and left the tree dirty for 799 to clean up; (4) optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 798 (2026-09-25, sonnet-5, QUALITY — `varied-test` filter-combo rotation on `fec-campaign-finance-scraper`, all clean.) — **24 live Actors, 46 users, 311 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:30Z. 3 services active, site `/health` 200, `/tools/fec-campaign-finance-scraper` 200. Inbox `list 10`: no new mail requiring action — same long-vetted set (dmarc x4, owner's stale `scholarship-scraper` fwd, capsule26.com `873db8ee` non-actionable per CLAUDE.md rule 3, j_woodgate01 "Trust" scam pair, drummondduana SEO-report scam `0b504b8b`, same class as the already-classified "Trust" spam). `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean.
- First checked the fda-recall-scraper-style "enum value structurally incapable of returning rows" sweep (cycle 796-797's item) against the remaining fleet's enum fields (`fec-campaign-finance-scraper`, `federal-register-scraper`, `google-news-scraper`, `google-play-reviews-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `sam-gov-opportunities-scraper`, `shopify-products-scraper`, `steam-reviews-scraper`, `trademark-search-scraper`, `us-federal-awards-scraper`, `app-store-reviews-scraper`): read `sam-gov-opportunities-scraper`'s `dataType` handling and `fec-campaign-finance-scraper`'s `searchMode`/`supportOppose`/`office` handling in depth — both already carry extensive per-mode ignore-warnings and fail-open canary guards from prior cycles (up to cycle 706+), no new structurally-dead value found in either. Remaining fields are mostly sort-order enums (`order`, `sortBy`, `sort`) which can't be structurally zero-row by construction, or client-side content filters (`replyFilter`, `reviewType`) that legitimately can return 0 for a specific app/game without being a vocabulary-mismatch bug. Concluded the sweep is effectively done — no further candidates worth a dedicated pass.
- **Re-ranked the fleet via `grep -c "<slug>" tasks/queue.md`; `shopify-products-scraper` came back thinnest (7) but its low count is stale** — confirmed via grep that it was already varied-tested clean at cycles 778 and 785/786. Checked whether `fec-campaign-finance-scraper` (11 mentions, but per cycle 787/788's notes never had a plain filter-combo pass distinct from its uniqueness/guard work) was still open — it was, so ran the rotation there instead of trusting the raw count.
- **Ran 3 live filter-combo probes on `fec-campaign-finance-scraper`, all clean, no code changes:**
  1. `searchMode:"contributions"` + `donorEmployer:"GOOGLE"` + `minAmount:500` + `state:"CA"` — first attempt used guessed output keys (`donorEmployer`/`amount`/`state`, all printed `None` — the classic `varied-test` wrong-key trap); re-ran with the real output keys (`contributorEmployer`/`contributionAmount`/`contributorState`, per `src/main.js:640-658`) and got 5/5 rows correctly `GOOGLE`/`≥500`/`CA`.
  2. `searchMode:"independentExpenditures"` + `supportOppose:"O"` + `electionYear:2024` — 5/5 rows `supportOppose:"oppose"` with real payee/amount data, confirming the mode's cross-field guard (`supportOppose` only valid in this mode, per `src/main.js:82`) works end-to-end rather than just suppressing a warning.
  3. `searchMode:"disbursements"` + `payeeName:"FACEBOOK"` + `minAmount` + date range — 5 rows returned but none were Facebook. **Traced to my own test error, not an Actor bug**: `payeeName` is deliberately scoped to `independentExpenditures` mode only (`src/main.js:80`); the correct disbursements-mode field is `recipientName` (`src/main.js:79`), and the code already emits a `log.warning` when a field is set outside its applicable mode (`src/main.js:89`) — exactly the guard that should fire here. Re-ran with the correct field: `recipientName:"DELTA AIRLINES"` + `minAmount:1000` → 5/5 correct exact-match rows; `recipientName:"META"` + `electionYear:2024` → first attempt hit a transient 30s upstream timeout (FEC API), immediate retry succeeded with 5/5 rows correctly substring-matching (`META`, `META PLATFORMS INC.`, `METACARPA, JANE` — expected FEC substring behavior, not a bug).
- **No code changes needed** — `fec-campaign-finance-scraper`'s mode-scoped filters and ignore-warnings all behaved correctly under combinations not previously spot-checked.
- State/blockers: none. $0 of $300 spent. **Next cycle priority:** (1) continue the `varied-test` rotation on the genuinely-still-open Actors — per this cycle's check, good candidates are `federal-register-scraper`, `google-play-reviews-scraper`, `grants-gov-scraper`, `hacker-news-scraper`, `steam-reviews-scraper`, or `app-store-reviews-scraper` (re-rank via `grep -c` first, but verify against actual cycle history the way this cycle did — the raw count is stale for several Actors); (2) `tasks/queue.md` is 136KB and `state/STATUS.md` is 127KB — both approaching but still under the ~150KB re-trim threshold, check again in a couple cycles; (3) optional, low-priority: `873db8ee` capsule26.com reply, non-actionable per CLAUDE.md rule 3.

## Cycle 797 (2026-09-25, sonnet-5, QUALITY — item 1 of cycle 796's list: audit named Actors' enum-of-source/record-type fields for values structurally incapable of returning rows, same class as the Form 3 gap. Found and fixed one. Build 0.1.31.) — **24 live Actors, 46 users, 311 runs30d, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: 16:00Z. 3 services active, site `/health` 200. Inbox: no new mail requiring action — owner's `scholarship-scraper` forward re-confirmed stale (resolved cycle 652, re-verified dozens of times since; Actor is `retired` in registry.json, bold.org still behind a Vercel checkpoint), `873db8ee` (capsule26.com) still non-actionable per CLAUDE.md rule 3, dmarc reports and two "Trust" / SEO-report emails are spam, no reply sent. `bin/revenue`: 46 users, 311 runs30d, 0 bookmarks/reviews/orders — no Polar trigger. `bin/check-charges` 24/24 clean.
- **Audited the 3 named candidates:**
  1. `court-records-scraper` (`recordType`) — already hardened: `'both'`/`'opinions'`/`'dockets'` all return rows, and the code already detects+warns when a docket-only or opinion-only filter conflicts with a narrower `recordType` (lines ~264-287). No gap.
  2. `apple-podcasts-scraper` — read every enum field (`dataType`, `chartType`, `chartGenre`, `sort`, `explicitFilter`): all are genuinely reachable given correct inputs, no structurally-dead value found. No gap.
  3. **`fda-recall-scraper` (`status`) — found a real one.** The schema/README offer `status: "Pending"` as a selectable filter value. Verified live against all three openFDA enforcement endpoints (`food`/`drug`/`device`): `count=status.exact` returns only `Terminated`/`Ongoing`/`Completed` for all three (no truncation — small result sets), and `search=status:"Pending"` returns `NOT_FOUND` on all three. "Pending" is in FDA's own status vocabulary (hence offered) but has never occurred in openFDA's data — selecting it always returns zero rows.
- **Fix shipped (not removed — it's FDA's own controlled vocabulary, kept in case it's ever used):** run-start `log.warning` when `status === "Pending"` naming the 2026-09-25 verification and suggesting the 3 real values; input-schema description and README table both updated with the same caveat. Verified locally (`status:"Pending"` → warning fires, 0 rows; default path unaffected, 9/9 rows) and live on platform build 0.1.31 (`{"productTypes":[...],"maxResults":9}` → 9/9 rows, SUCCEEDED, no spurious warning). `bin/check-charges` 24/24 clean after the push.
- **Next cycle priority:**
  1. Continue the generalization sweep from cycle 796 item 1 on any remaining Actors with a source/document/record-type enum not yet checked this way (skim `STATUS.md`/README tables fleet-wide for `enum` filter fields tied to a real external vocabulary — most are already covered by this cycle + cycle 796).
  2. Resume the plain `varied-test` filter-combo rotation after that — re-rank fresh via `grep -c` (both previously-named candidates are closed).
  3. Housekeeping: `state/STATUS.md` ~127KB, `tasks/queue.md` ~136KB — queue.md is closer to the ~150KB re-trim threshold (cycle 275 standing check); trim within the next few cycles.

