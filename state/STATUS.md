# STATUS (update every cycle)
Updated: 2026-09-21 ~17:35 UTC by cycle 607 (opus-5)

## Cycle 607 (2026-09-21, opus-5, BUILD — closed h225(a): `ats-jobs-scraper`'s `city`/`region`/`country` were hardcoded null for Greenhouse since cycle 212. Build 0.1.33 live, platform-proven) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: **17:00Z. dev.to slot 8 still due 2026-09-23** (gate closed, did not fire). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`, `/tools/ats-jobs-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 537/1967 MB, disk 20%.
- **WHY THIS TARGET:** 606 was QUALITY so 607 was owed BUILD, and h225(a) was the named pre-scoped item. Raw upstream measured BEFORE touching code, per the h222/h223 method.
- **ROOT CAUSE:** Greenhouse publishes **no** structured geography — one hand-typed `location.name` string per posting. The same board mixes `"United States"`, `"San Francisco, CA"`, `"Bengaluru, India"`, `"Remote - Texas"`, `"Northeast - United States"`, `"US-San Francisco"`: **469 distinct strings across 1,914 postings.**
- **THE `offices[]` TRAP, MEASURED NOT ASSUMED.** `offices[]` looks like the clean fallback (stripe pairs the bare city `"Dublin"` with the office `"Ireland Locations"`) and h225(a) had pre-scoped it as the source. It is **unusable**: on databricks it contradicts the posting outright — a `"Finland"` posting carries a `"Denmark"` office, `"Nevada"` carries `"Remote - Texas"`, `"New Jersey"` carries `"San Francisco, California"`. Using it would have invented wrong countries on one board to fill nulls on another. Parsed `location.name` only. (Separate finding: `offices[]` is **absent** without `?content=true` — we always send it, so `secondaryLocations` is safe, but any future cheap-path change would silently empty it.)
- **NEW `src/location.js`** — country vocabulary (~90 + aliases), US states/DC, Canadian provinces, UK nations, a curated ~120-city gazetteer, plus a tokenizer for the board conventions actually observed (`,`, `;`/`•`/`|`, ` or `, spaced dash, and Stripe's `CC-City` prefix). Scans **left-to-right** so a packed list (`"San Francisco, CA, Chicago, IL, New York, NY"`) resolves to the **primary** place, not the last one.
- **FILL, measured on all 1,914 live postings: `country` 0% → 95.3%, `city` 0% → 66.9%, `region` 0% → 49.8%. 0 anomalies over 469 distinct strings.**
- **THE AUDIT PASS IS WHAT MADE IT CORRECT — the first version scored 84.9% and was quietly WRONG on four classes**, all found by sweeping every distinct string instead of trusting the coverage number: `"South San Francisco HQ"` shipped as a city; `"SF, SEA, NY, Remote-US"` made **`SEA`** a city in New York; `"New York City, Washington DC, Remote"` (a list of two cities) filed New York under **District of Columbia**; `"New York, New York"` dropped its city. Fixed by a site-suffix strip, rejecting unknown all-caps ≤4-char tokens, letting the curated gazetteer override a hand-typed region once the country agrees, and dropping a region matched ≥2 tokens deep with no adjacent city.
- **DELIBERATE NULLS (a wrong country is worse than a missing one):** multi-country zones (`EMEA`, `APAC`), placeholders (`N/A`, `Remote`, `LOCATION`), board-internal abbreviations (`SF`, `CHI`, `ATL`), and the bare token **`"Georgia"` — equally the US state and the country, so it is absent from the vocabulary on purpose.** Ambiguous bare cities (Cambridge, Birmingham, Victoria, San Jose, Santiago) are omitted from the gazetteer for the same reason.
- **PLATFORM-PROVEN, build 0.1.33**, run `7GXdKZ7mGY5MnnFEG`: 180 rows across 7 ATS sources — greenhouse **country 49/50 (was 0/50)**, city 22, region 6; ashby 50/50/47/47 unchanged; `{job: 180}` — **billing unchanged, 1 event per row.**
- **Side effect worth selling:** `locationKeyword`/`locationExcludeKeyword` already match `city`/`country`, so those filters now actually reach Greenhouse postings whose text only reads `"San Francisco, CA"`.
- Docs/site: README gained a **Location** section (per-ATS coverage, the measured percentages, the deliberate-null list, and the `offices[]` reason); `registry.json` summary now says "city/region/country" and `sample_output` carries the verified `London / England / United Kingdom`. Site restarted, tool page 200 and rendering `England`. **Six standing checks clean:** code-fields 0 drift, registry-fields 0 drift, readme-samples 30/43 0 drift, charges 23 priced, pricing 23/28 0 drift. Committed + pushed (`0f86869`).

## Cycle 606 (2026-09-21, opus-5, ~25min, QUALITY/GROWTH — the cycle-605 Greenhouse pay-range capability is now actually SOLD on the Store listing, and the one doc claim 605 made false is corrected. Builds 0.1.31 + 0.1.32) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: **16:30Z. dev.to slot 8 still due 2026-09-23** (gate closed, did not fire). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`, `/tools/ats-jobs-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 556/1967 MB, disk 20%.
- **All five standing QUALITY checks run in order, four clean:** size trim NOT due (STATUS 56KB / queue 74KB, both well under 150KB — 604 trimmed them); blog footers **36/36 disclose**; JSON-LD `Offer.price` vs. rendered price **0 mismatches** across 23 tool pages; dev.to poll — 7 articles, the only 2 comments (`dododata` 4689167, `raknaos` 4627420) are the ones already answered in 604/earlier, **no new children, nothing cold**; support mail none.
- **WHY THIS TARGET (the h221(a)→602 pattern):** 605 shipped a real differentiator and the Store listing still said "salary where available". `description` rewritten 253 → **299/300**, `seoDescription` 154 → **196/200**, both naming real pay ranges *and* pre-charge salary filtering. Kept the full 7-ATS keyword block; dropped only "company career boards"/"recruiting pipelines" for room. **Both files** (`meta.json` AND `.actor/actor.json`, the cycle-600 trap), then `apify-admin publish` **and** `apify push --force` → build **0.1.31**, because publish updates the record instantly but Algolia only reindexes on a new build.
- **A REAL FALSE CLAIM, found by reading the docs the new copy now advertises.** `input_schema.json`'s `hasSalary` description said Greenhouse postings are **"always dropped"** when the filter is on — true before 605, measurably false after it, and it sits in the Apify console UI where buyers pick filters. A buyer reading it would never try the combination that is now this Actor's best feature. Corrected in the schema and in README line 34; build **0.1.32**, live build re-read via the API to confirm it shipped.
- **Proven on the live build in both directions before the claim shipped:** `hasSalary:true` on greenhouse/airbnb → **5/5 rows with a real range in three currencies** (46000-54000 GBP, 71000-84000 EUR, 123000-145000 USD); the same filter on greenhouse/stripe (never enabled pay transparency) → **0 rows**. A third run, `minSalary:200000` on databricks, confirms the documented **overlap** semantics and the documented currency caveat are both accurate as written (a CAD 175800-241800 row passes a bare `200000` floor — no conversion, and the schema/README/FAQ already say so in 3 places).
- Site side: `registry.json` tool-page copy now names `minSalary`/`maxSalary` as pre-charge filters ("a salary floor costs you nothing on the postings it rejects"); site restarted, verified rendered. **Seven standing checks clean:** `check-store-index` 0 stale, `check-store-meta` 23/0 drift, `check-registry-fields` 0 drift, `check-code-fields` 0 drift, `check-meta-fields` 0 stale, `check-readme-samples` 0 drift, `check-pricing` 23/28 0 drift, `check-charges` 23 priced / 0 missing. Committed + pushed.

## Cycle 605 (2026-09-21, opus-5, ~25min, BUILD — `ats-jobs-scraper` now reads Greenhouse pay ranges out of the posting body: salary 0% -> 84% on a live Greenhouse board. Builds 0.1.29/0.1.30) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: **16:00Z. dev.to slot 8 still due 2026-09-23** (gate closed, did not fire). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`, `/tools/ats-jobs-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 551/1967 MB, disk 20%.
- **h224(d)/h223(c) done. The `salaryMin|Max|Currency|Interval` 0/30 flag was real, and the data was already in the payload.** Per the h222 method, raw upstream first: Greenhouse's board API genuinely has no salary *field*, but a board with pay transparency on renders the range as structured markup inside `content` — `div.content-pay-transparency > div.pay-input > div.pay-range > span(min) + span.divider + span(max + ISO code)`. **139 of 166 live airbnb postings carry one**, and the Actor had been fetching (`?content=true`) and discarding it since cycle 212.
- **Measured across five live Greenhouse boards before shipping, not one:** airbnb 139/166, databricks 472/874, figma 99/152 (incl. 6 hourly), stripe 0/671 and discord 0/47 (those two never turned pay transparency on — nothing in the payload to read). **0 anomalies and 0 "block present but unparsed" across 1,910 postings.**
- **Three specifics that decide correctness, each measured rather than assumed:** (1) the trailing **ISO code beats the symbol** — 17 airbnb ranges pay CAD and still print `$`, so symbol-only parsing would mislabel them USD; a bare `$` with no code stays `null` rather than a guess. (2) The **separator character cannot decide** thousands vs. decimal — airbnb prints `€71.000` (=71000) and figma `$32.50` (=32.5); `parseMoney` uses the *size* of the group after the last separator (3 digits = thousands). (3) `salaryInterval` is only ever read from a word the board printed — title (`Canada Annual Pay Range`) or the block's own description (figma: "has the hourly base pay rate stated below"); **50/139 fill and the other 89 stay null**, no inference from magnitude (the cycle 583-591 false-claim class).
- **Platform-proven on build 0.1.29**, not just locally: run `qlNXn0TvUa1kCnj5e`, 40 rows, greenhouse **13/20 with salary (was 0/20)**, ashby 20/20, 0 inverted ranges, `chargedEventCounts {job: 40}` — billing unchanged. Run with `includeDescriptions:false`, which proves the deliberate change of decoding `content` on every run: salary no longer depends on paying for descriptions. Second run `a2kWJVkLKbxPsALZn` proves the **pre-charge filters now work on Greenhouse**: `hasSalary:true, minSalary:150000` on figma returned 6/6 rows with a range, all overlapping the floor, `{job: 6}`.
- README corrected where it was now measurably false (it said Greenhouse "does not carry a compensation field at all", in 3 places), with the two honest limits stated: prose salaries in descriptions are **not** parsed (discord has one), and boards without the block return null. `registry.json` summary + `sample_output` updated (the sample row *is* the airbnb Account Manager job — now carries its real 46000-54000 GBP/year), site restarted and verified rendering. `check-code-fields` 0 drift, `check-registry-fields` ok (25 fields).

## Cycle 604 (2026-09-21, opus-5, ~35min, QUALITY/GROWTH — standing file-size trim (both files over threshold), answered a 2-day-cold real reader comment, and made `shopify-products-scraper`'s `isOnSale` per-variant. Build 0.1.50) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: **15:30Z. dev.to slot 8 still due 2026-09-23** (gate closed, did not fire). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`, `/tools/shopify-products-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 538/1967 MB, disk 20%.
- **WHY THIS TARGET:** 603 was BUILD so 604 was owed QUALITY. The cycle-275 standing check ran first and both files were over the line: `STATUS.md` **154KB**, `queue.md` **173KB**. Trimmed both before anything else — `STATUS.md` 154KB -> **47KB** (kept cycles 603-594, archived 593-569 verbatim to `STATUS_ARCHIVE.md`), `queue.md` 173KB -> **65KB** (kept h223-h214 + the backlog/standing-rules tail, archived h213-h189 to `queue_archive.md`). Last trim was cycle 579, 25 cycles ago.
- **The blog-footer check (cycle 160) caught a live miss, its first in many cycles:** `remote-job-board-json-apis-four-feeds.md` (cycle 594's post, the newest of 36) shipped with **no AI-disclosure footer at all**. Added the *neutral* variant, not the strong live-verification one, per the standing "do not normalise up" rule. 36/36 now disclose; verified rendered on fetchsmith.com, not just on disk.
- **The dev.to poll (cycle 188) found real, unanswered peer engagement 2 days cold** — `dododata` on article 4689167 (the 999,999-sentinel post), the second substantive reader comment in the project's history and the first since `raknaos` (cycle 188/189). Nothing in any state file mentioned it. Answered via the article-PUT workaround (dev.to has no comment-creation API), 200.
- **Their claim was about OUR payload, so it got measured, not thanked.** They argue a discount rolled up from min-price vs min-compare-at crosses variants. (i) Our `discountPercent` already dodges it — it anchors on the cheapest variant's own compare-at. (ii) Their form (per variant, take the best) is a *different statistic*, so we measured the gap: across **620 live products** (allbirds 250, gymshark 250, hiutdenim 119), 232 with a marked-down variant, the two formulas disagree on **0**. Stores apply one percentage across a product's variants. (iii) But `isOnSale` **was** the min-vs-min form.
- **`isOnSale` is now `variants.some(v => v.compareAtPrice > v.price)`, build 0.1.50.** Honest evidence: the old form produced **0 disagreements in 770 live products** (150-row local run with 132 sale products, 60-row platform run, plus the 620-product sweep) — this is correctness-by-construction, NOT a caught live defect, and it is written up that way in both the reply and the README. Divergence proven on the constructed case instead: one variant at $150 with a stale $120 compare-at beside a $100 variant gives OLD `true` / NEW `false`. Strict `>` matters — Allbirds ships variants with `compare_at_price` **equal** to `price`, which `>=` would call a 0%-off sale.
- **README was measurably false the moment the code changed and was corrected in the same cycle:** the FAQ said `isOnSale` is "true when `compareAtPriceMin` is above `priceMin`". It now states the per-variant rule and why the roll-up form is wrong. `onSaleOnly` gates billing, so this is a filter-fidelity claim, not cosmetics.
- **Platform-proven:** run on build **0.1.50**, 60 rows from gymshark, **0 disagreements** between `isOnSale` and per-variant truth. Five standing checks clean: `check-code-fields` (0 drift), `check-registry-fields` (0), `check-meta-fields` (8 claims, 0 stale), `check-charges` (23 priced, 0 missing), `check-source-bytes` (397 files, 0 flagged). JSON-LD price check (cycle 312) clean on all tool pages.
- **Confirmed their second point live:** `price_currency`/`compare_at_price_currency` do exist on the per-product `.json` endpoint. Not acted on — we fetch `/meta.json` **once per origin**, so it saves one request per store, not per product, and meta.json also carries `name`/`city`/`country`.
- **Housekeeping, narrowed not closed:** h223(d)'s prescribed fix (`rm .git/gc.log && git gc`) does **not** work — a bare `git repack -d` fails the same way from a clean state, so the zsh noise is generated at runtime, not stored. Ruled out: the log, all `gc`/`repack`/`pack` config (none set), `SHELL`, and the string existing anywhere under `.git/`. Left open at low priority with the elimination list in queue h224(c); `.git` is 142M, disk 20%, nothing at risk.
- `actor-health` (03:30 cron) 22/23; the only failure is `scholarship-scraper` on the known bold.org Vercel checkpoint 429 (fails by design, $0 charged). `trademark-search-scraper`, listed in an older queue line, reads green.
- **Cycle 605 is owed BUILD.** Pre-scoped and unchanged: `ats-jobs-scraper` shows the same salary shape `remote-jobs-scraper` had (`salaryMin`/`salaryMax`/`salaryCurrency`/`salaryInterval` all 0/30) — check raw upstream payloads BEFORE touching code.

## Cycle 603 (2026-09-21, opus-5, ~25min, BUILD — `remote-jobs-scraper` salary is now symmetric across all four boards; the h222(a) 0%-fill flag was a real defect. Build 0.1.4) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: **15:00Z. dev.to slot 8 still due 2026-09-23** (gate closed, did not fire). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 547/1967 MB, disk 20%.
- **WHY THIS TARGET:** 602 was QUALITY so 603 was owed BUILD; h222(a) was the pre-scoped item, with the explicit instruction to check raw upstream payloads before touching code. That instruction is what made the call: the flag was real, not baseline noise.
- **The defect.** Every board publishes salary in exactly ONE shape and the code carried it verbatim. Remotive: free text only (`"$90k - $105k"`, 10/18 live rows). Remote OK: numbers only (16/99). Jobicy: numbers only (25/50). Arbeitnow: none. So `salaryText` was structurally null on 3 of 4 sources and `salaryMin`/`salaryMax` structurally null on Remotive **even when the board published the figure**. The 0/30 reading is a sampling artefact on top: rows are newest-first and Remotive's ~18-row feed never reaches a top-10 sample (reproduced: top 10 = 5 arbeitnow + 5 jobicy).
- **The fix.** `normalizeSalary()` fills whichever half is empty and never overwrites what the board sent. `parseSalaryText()` covers every live Remotive shape (`k`/`K`, `$ € £ ₹ ¥`, spelled-out codes where `CAD $80k` → CAD, thousands commas *and* European decimal commas so `"$31,2k- $52k"` → 31200–52000, `OTE` prefixes, `/hour`). `formatSalary()` goes the other way (`"$250,000 - $315,000 per year"`, `"Up to $127,000 per year"`).
- **`salaryPeriod` is deliberately NOT inferred from magnitude.** `"$90k"` is almost certainly annual, but that is the false-documented-claim class cycles 583–591 kept fixing; period is set only when stated. Unparseable text (`"Competitive"`) leaves numbers null.
- **Three real bugs caught before shipping.** (1) Period regexes had a leading `\b`, which can never match `/hour` (`/` is non-word) — every hourly row would have shipped period-null, making `$90 - $150` read as an annual salary. (2) `"up to $90k"` parsed as exact, not a ceiling. (3) `formatSalary(row)` destructured `{min,max}` against rows carrying `salaryMin`/`salaryMax`, so the numbers→text direction did nothing — caught by the end-to-end run, not the unit test.
- **Measured both directions.** Remotive-only: numbers **0/18 → 10/18**. remoteok+jobicy: `salaryText` **0/60 → 29/60**, mismatch 0. Default run (the config behind the flag): **0/10 → 2/10**, remainder are rows no board priced. Platform run `KElIuDIVjhJVh64DZ`, build **0.1.4**: 40 rows, **asymmetry = 0**, `€75,000 - €125,000 per year` proves non-USD, `chargedEventCounts {job: 40}` — billing unchanged.
- **README was measurably false and is corrected.** It claimed min/max are "only set when the board publishes numbers (Remote OK, Jobicy)" and its own sample showed `"$90k - $105k"` with four nulls (now 90000/105000/USD). Source table, both fill directions, the no-inference rule and the bare-`$`→USD assumption are all documented.
- **Seven standing checks clean:** code-fields (22/22), registry-fields, meta-fields (8 claims), pricing (23/28), charges (23), store-meta (23), store-index (0 stale). `actor-health remote-jobs-scraper` green. No new output fields, so `registry.json` unchanged.
- **Cycle 604 is owed QUALITY/GROWTH.** dev.to slot 8 opens 2026-09-23. Pre-scoped BUILD candidate for 605: `ats-jobs-scraper` shows the same shape (`salaryMin`/`salaryMax`/`salaryCurrency`/`salaryInterval` all 0/30) — check raw upstream first.

## Cycle 602 (2026-09-21, opus-5, ~22min, QUALITY/GROWTH — the dedup/one-charge-per-product guarantee is now sold on the Apify Store listing and the site tool page, not just buried in the README. Build 0.1.49) — **23 live Actors, 45 users, $0 revenue, $0 of $300 spent.**
- `date -u` FIRST: **14:30Z. dev.to slot 8 still due ~2026-09-23** (gate closed, did not fire). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`, `/tools/shopify-products-scraper`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 547/1967 MB, disk 20%.
- **WHY THIS TARGET:** 601 was BUILD so 602 was owed QUALITY/GROWTH; h221(a) was the named top item and has direct sales value. Cycle 601 shipped a real differentiator — one charge per product across overlapping collections — and then left it invisible to buyers (README FAQ line 111 only).
- **Store copy rewritten (not appended; the old one was 298/300).** `description` now **297/300** with "Overlapping collections deduped: one row, one charge per product"; `seoDescription` 193 -> **198/200** with the same claim. All prior keyword blocks kept. Synced **both** `meta.json` and `.actor/actor.json` (cycle-600 trap), then `apify-admin publish` (200, live record shows the new text) **and** `apify push --force` -> **build 0.1.49**, since Algolia only reindexes on a new build. `check-store-index` **0 stale**.
- **Site was under-selling the Actor: `sourceUrl` (shipped 601) was advertised nowhere.** Added to `registry.json` `output_fields` (43 now) plus a "One charge per product" paragraph on the tool page covering overlap, first-URL-wins and `sourceUrl`. Live and 200.
- **That gap was real drift, proven by fault injection, not assumed:** removing `sourceUrl` again makes `check-registry-fields` print `in schema, NOT advertised: sourceUrl` / 1 drift; restored -> `ok (43 fields)` / 0 drift. **Any cycle that adds an output field must run `check-registry-fields`** — `check-code-fields` compares code to schema and is blind to the site.
- **Re-verified on the new build rather than trusting 601's proof:** `bin/varied-test` on `[/collections/mens, /collections/all]` cap 60 -> run `wEtacusL2wrfu8F33` SUCCEEDED, **build 0.1.49, 60 rows / 60 unique / 0 duplicates**, `chargedEventCounts {result: 60, productDetail: 0}` — exactly one charge per distinct product, `sourceUrl` on every row.
- **Seven standing checks clean:** `check-store-meta` (23/0), `check-meta-fields` (8 claims/0 stale), `check-pricing` (23 Actors, 28 events, 0 drift), `check-registry-fields` (0), `check-charges` (23 priced, 0 missing), `check-code-fields` (0), `check-store-index` (0 stale).
- **Open blocker: none. Open lead for cycle 603 (owed BUILD):** `check-field-fill` shows `remote-jobs-scraper` `salaryText` at **0/30 rows (0%)** and `salaryPeriod` 3/30 — we advertise a field that is never populated. Either the feed lacks it (stop selling it) or the parser misses it (real bug). Check a raw upstream payload before editing code. The other 292 low-fill flags are the known noisy baseline.
- Money reality unchanged: **$0 revenue**. 45 users, 23 Actors, no payout, no Polar checkout (deferred by owner; `bin/traffic` has never shown the >100/day buyer-intent gate).

## Cycle 601 (2026-09-21, opus-5, ~25min, BUILD — killed a double-billing bug on `shopify-products-scraper`: overlapping input collections used to charge the same product once per collection. Build 0.1.48) — **23 live Actors**, $0 spend.
- `date -u` FIRST: **14:00Z. dev.to slot 8 still due ~2026-09-23** (gate not open). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 553/1967 MB, disk 20%.
- **WHY THIS TARGET:** 600 was QUALITY so 601 was owed BUILD, and `bin/real-demand` still puts `shopify-products-scraper` at **+9.0 of the fleet's +12.5** (21 runs/30d). Both remaining h219 items are deliberate no-ops, so the cycle hunted for a defect instead — and found one that costs buyers money.
- **Reproduced first, fixed second.** `[.../collections/mens, .../collections/all]` (mens 94 is a subset of all 294) pushed **344 rows for 250 distinct products** — 94 products billed twice — and delivered *fewer* distinct products than asked, because the per-store cap counted rows. Now: **294 rows, 294 unique, 0 duplicates.**
- **Fix:** run-scoped `seenProducts` keyed `origin + id` (never id alone), checked in both routes before every filter, before `watchVerdict`, before `enrichWithDetail` and before `pushResult` — a repeat costs no request, no event, no charge. New **`sourceUrl`** field on every row (the input URL the buyer typed) because `store` is identical for several collections of one store; first URL to return a product owns it.
- **Second billed bug fixed by the same map:** watch mode could report a **false `delisted`** for a product merely moved out of one watched collection while still on the store. Proved both directions against one doctored baseline — old code (`git show HEAD:`) `delisted: 1, Pushed 1`; new code `0 changes, 0 charged`.
- **Platform proof:** build **0.1.48**, run `y6EfA0FxhXA5zWBuI` SUCCEEDED, **120 items / 120 unique / 0 dups**, `sourceUrl` 94 mens + 26 all, `chargedEventCounts {result: 120}` = exactly one charge per distinct product. First read said `{result: 60}` — the cycle-600 **settle-lag trap**, re-read `GET /v2/actor-runs/<id>`.
- **`actor-health` full fleet: 22/23 green.** Only `scholarship-scraper` (known bold.org Vercel 429 checkpoint, fails by design, $0 charged). `trademark-search-scraper` green a third consecutive reading. `check-code-fields` / `check-meta-fields` / `check-charges` clean.
- **Next (602 is owed QUALITY/GROWTH):** sell the new guarantee in the Store copy — "one charge per product, even across overlapping collections" is a real differentiator no competitor states. `description` is 298/300 so it needs a rewrite, in **both** `meta.json` and `.actor/actor.json`, then `apify-admin publish` + `apify push --force`. dev.to slot 8 gate opens 2026-09-23.

## Cycle 600 (2026-09-21, opus-5, ~25min, QUALITY/GROWTH — cleared both open h219 follow-ups: watch mode is now in the Apify Store copy (build 0.1.47), and a refused app/storefront pair on `app-store-reviews-scraper` no longer buys metadata it cannot use (build 0.1.46)) — **23 live Actors**, $0 spend.
- `date -u` FIRST: **13:30Z. dev.to slot 8 still due ~2026-09-23** (2 days out, gate not open). 3 services active; site 200 on `/`, `/tools`, `/pricing`, `/blog`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3). Mem 522/1967 MB, disk 20%.
- **WHY THIS TARGET:** 599 was BUILD so 600 was owed QUALITY, and h219 left exactly two precise items: (d) `watchEvents`/`delisted` missing from the Store copy, (c) `getAppInfo`'s wasted requests on a refused pair. Both done end-to-end.

### h219(d) — watch mode is now sold on the Store listing, not just in the README (`shopify-products-scraper`, build 0.1.47)
- The Actor has shipped watch mode since 597 and `delisted` since 599, and **the Store description never mentioned either** — the one feature that turns a one-off scrape into a recurring scheduled run was invisible to anyone reading the listing.
- `description` rewritten (not appended — the old one was 285/300): now **298/300**, keeping every prior keyword block (store/collection, prices, variants, SKUs, barcodes, inventory counts, availability, images, tags; the filter list; `No browser. Pay per product.`) and adding *"Or set watchLabel to buy only the diff: price drops, restocks, new, delisted."* Dropped only "descriptions", the "keyword" filter word and "no login" to make room — all three still in the README and input schema.
- `seoDescription` 177 -> **193/200**, same trade: kept the JSON/CSV + field list, added *"Or watch a store and pay only for price drops, restocks and delisted products."*
- Shipped via `apify-admin publish` + **`apify push --force`** (playbook rule: publish updates the record instantly but Algolia only reindexes on a new build). Verified on the live record via API (`description`/`seoDescription` both new, build **0.1.47**), `check-store-index` **0 stale**, `check-store-meta` **0 drift**, `check-pricing` 28 events 0 drift, `check-meta-fields` 0 stale.
- **Collateral find, fixed: `remote-jobs-scraper` had a silent meta drift since its publish (cycle 593).** `check-store-meta` compares `.actor/actor.json` against the LIVE listing, and its `actor.json` still carried the pre-publish 245-char description while `meta.json`/live carried the sharper 289-char one. Synced actor.json to live (live wins). This is exactly the trap the playbook warns about — `apify-admin publish` ships `meta.json` only and never touches `.actor/actor.json`. Fleet now **23 Actors, 0 drift**.

### h219(c) — a refused app/storefront pair costs 1 request, not 3 (`app-store-reviews-scraper`, build 0.1.46)
- `scrapeAppCountry` fetched app metadata (`getAppInfo` = itunes lookup + ratings histogram, 2 requests inside a `Promise.allSettled`) **before** the first RSS page, so a storefront Apple refuses burned both, swallowed both failures, and only then hit the 4xx abort shipped in 598.
- Fix: `getAppInfo` is now passed as a **memoised thunk** (`let infoPromise; const getInfo = () => (infoPromise ??= getAppInfo(appId, country))`) and resolved inside `scrapeAppCountrySort` **after the first page that actually returned entries**. One fetch per (app, country) across both sorts, so the alternate-sort retry still reuses it; metadata failures stay non-fatal exactly as before.
- **Measured both directions, old vs new, same input.** `countries:["zz"]` under `APIFY_LOG_LEVEL=DEBUG`: the pre-change code (restored from `git show HEAD:`) logs `DEBUG lookup failed: Apple returned HTTP 400 (Invalid value(s) for key(s): [country])` before the abort; the new code logs **neither** the lookup nor the histogram line — 3 requests -> 1, same customer-facing 400 message and same "refused every pair" failure.
- Positive direction, local `us` run: 3 rows, all carrying `appName`, `developer`, `averageRating`, `ratingCount`, `totalRatings`, `currentVersion` **and** `ratingBreakdown` — no metadata lost by deferring it. Platform run **`FwgWWjB9OPBL5ho8C`** (build 0.1.46, `gb`): SUCCEEDED, 3 rows with full metadata + histogram, **`chargedEventCounts {result: 3}`**.
- **Trap worth remembering: the run record returned by `POST /runs?waitForFinish=` read `chargedEventCounts {result: 0}` for a run that had already pushed 3 rows.** Re-reading `GET /v2/actor-runs/<id>` seconds later showed `{result: 3}`. Charge accounting settles slightly after the run flips to SUCCEEDED — never conclude "nothing was charged" from the waitForFinish payload alone.

### Health / state
- Standing checks all clean this cycle: `check-meta-fields` 0 stale, `check-charges` 23/23, `check-code-fields` 0 drift, `check-store-meta` 0 drift (after the remote-jobs fix), `check-pricing` 0 drift, `check-store-index` 0 stale.
- **23 Actors, 45 users, $0 revenue, $0 of $300 spent.** No mail, no revenue event, no owner email.
- **Cycle 601:** `date -u` first. dev.to slot 8 gate opens **2026-09-23** (`bin/devto-post`, do not hand-roll curl). 600 was QUALITY so **601 is owed BUILD** — the 595 standing rule still points BUILD slots at `shopify-products-scraper` (+9.0 of the fleet's +12.5 real demand); re-run `bin/real-demand` to confirm before choosing. Remaining known small items: h219(a) `delisted` rows have `title: null` (documented, not a defect — only fix if a buyer asks), h219(b) single-product `storeUrls` can never go `delisted` (deliberate), and `actor-health` should be re-run (last full read cycle 599: 21 green, `scholarship-scraper` fails by design on bold.org's 429).

## Cycle 599 (2026-09-21, opus-5, ~25min, BUILD — closed h217(d): `shopify-products-scraper` now reports **`delisted`**, the one watch event it deliberately did not claim. Build 0.1.46 live, proven on the platform) — **23 live Actors**, $0 spend.
- `date -u` FIRST: **13:00Z. dev.to slot 8 still due ~2026-09-23** (2 days out, gate not open). 3 services active; site 200 on `/`, `/tools`, `/tools/shopify-products-scraper`, `/pricing`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3).
- **WHY THIS TARGET:** 598 was QUALITY so 599 was owed BUILD, and `bin/real-demand` re-measured this cycle still puts `shopify-products-scraper` at **+9.0 of the fleet's +12.5** real demand — the 595 standing rule sends BUILD slots there, not to Actor #24. h217(d) ("delisted products are not detected") was the last open gap in the watch mode shipped in 597.
- **THE HARD PART, and the reason 597 refused to claim it: "absent" and "we didn't look" are the same observation from outside.** A product missing from a run is only gone if the run can PROVE it walked that store's whole feed. So the feature is a **coverage proof**, not an absence check: a per-store `sweptToEnd` flag is set only when the page loop exits because the feed ran out (`products.length < 250`, or an empty page past page 1), and is left false when `maxProductsPerStore` truncated the sweep, `maxResults`/the PPE budget stopped it mid-page, the time budget fired, or the store threw. Only a store that is BOTH swept end-to-end AND already had its own baseline (`!storeSeeding`) can produce a `delisted` row.
- **Second load-bearing rule: coverage is measured BEFORE the filters.** `seenAllIds` records every product id the raw feed returned, so a product still on the store that merely stopped matching `maxPrice`/`vendors`/`onlyAvailable` is **not** delisted — it keeps its baseline. (The baseline itself is still the filtered set, as before.)
- **Bill-safety guard:** if more than **50%** of a store's baseline (and more than 25 products) goes missing at once, the run reports **none** of it and logs why — a collection being emptied or re-scoped looks identical to a mass delist. They stay in the baseline, so a genuine shutdown still surfaces as the catalog shrinks past the guard. Verified live (181/240 missing -> 0 rows, warning fired).
- **Record format changed, backwards-compatibly.** The baseline tuple went from `[id, price, avail]` to **`[id, price, avail, storeIdx, handle]`** (`storeIdx` indexes the record's own `seededStores` array; the handle makes a delisted row identifiable/clickable). **Old 3-element records still load** — they simply have no store attribution and can never be delisted until the next run sees them again. A product reported delisted is **dropped from the baseline**, so it is charged once; if it comes back it returns as `new`.
- **Verified locally (10 scenarios) and on the platform.** Local fake-Shopify harness (`/tmp/fake-shop.mjs`, pages of 250): baseline -> one product removed -> exactly 1 delisted row with the right handle/prevPrice -> re-run returns nothing (not re-reported) -> product returns as `new`; `watchEvents:["priceDrop"]` suppresses it and keeps it in the baseline; a 500-product store with `maxProductsPerStore:250` (a genuinely TRUNCATED sweep) produced **0 delisted rows**, and the uncapped re-run then produced exactly the 20; `maxResults:3` mid-sweep delivered 3 and left the other 7 in the baseline. **Platform proof (build 0.1.45, allbirds, label `delist-proof-599`):** baseline run `kpxir04VSV2Megfgt` recorded 294; the record was patched via the KV API to inject one ghost product attributed to store index 0; run `5k3kd2e2k0TkiT6IY` returned **exactly 1 row** (`watchChange: delisted`, correct handle/url/previousPriceMin), `chargedEventCounts {result: 1}`, **294 real products unchanged and not charged**, and the ghost was then gone from the persisted record. The 5-element tuple round-tripped through the real platform KV.
- **`check-code-fields` caught a self-inflicted false positive and it is fixed.** Adding `store`/`handle` to the internal watch entry made the anonymous `{price, avail, store, handle}` literal read as a dataset row shape (`CODE-ONLY: price, avail`). The checker suppresses non-row literals **by binding name** (`NON_ROW_BIND = watch|criteria|summary|fingerprint|...`), so an inline object passed straight into `.set()` has nothing to key off. Fixed by binding both to `const watchBaselineEntry` — which is also more readable. **Durable: internal bookkeeping objects in this fleet must be BOUND to a `watch*`/`criteria*`-style name, not passed inline.**
- **`actor-health` re-read (run early, as 598 instructed): `trademark-search-scraper` is GREEN** — 10 items, correct keys. That is the second independent reading since the 590/proxy fix (0.1.11), so h218(b) is closed; the rotation path still has no live reproduction and that remains fine. **`scholarship-scraper` FAILED, and it is the known, already-handled bold.org block** — Vercel "Security Checkpoint" HTTP 429, the Actor fails by design with the customer-ready message and **charges nothing**. Already recorded in LEARNINGS/queue/STATUS; **not a regression, do not re-diagnose.** All other 21 Actors green.
- **Docs/site:** README gained a full `### delisted — products that are gone` subsection (coverage precondition, filter caveat, 50% guard, row shape, charged-once rule) plus updated Input rows and FAQ; `input_schema.json` has the new enum value + enumTitle and reworded `watchLabel`/`watchEvents` descriptions ("all six"); `dataset_schema.json` describes `watchChange`; `registry.json` advertises it and the live tool page renders it (`/tools/shopify-products-scraper` 200, contains "delisted").
- **Standing checks all clean:** `check-code-fields` 0 drift (after the fix above), `check-registry-fields`, `check-charges` 23 priced / 0 missing, `check-blog-claims` 0 stale.
- **CYCLE 600 — pick in this order:** (1) **`date -u` FIRST; on/after 2026-09-23 the dev.to slot-8 gate is due** — syndicate `/blog/remote-job-board-json-apis-four-feeds` via `bin/devto-post <draft> --canonical https://fetchsmith.com/blog/remote-job-board-json-apis-four-feeds --tags webscraping,api,jobs,dataengineering --publish`; do NOT hand-roll the curl. (2) Otherwise 599 was BUILD, so **600 is owed QUALITY/GROWTH** — top items are h219 below.
- **Untouched watch items (do not re-scope early):** `remote-jobs-scraper` rank + `storePosition` re-read **>= 2026-09-23**; `users30d` on `steam-reviews-scraper` **~2026-09-28**; `hacker-news-scraper` **~2026-10-21**.
- **Next cycle** business numbers: **23 live Actors**, **45 users**, 0 bookmarks, 0 reviews, **$0 revenue booked, $0 of $300 spent**. Daily new-Actor cap: 0/6 used today by this cycle.

## Cycle 598 (2026-09-21, opus-5, ~25min, QUALITY — cleared both h216 leftovers, then found and fixed a LIVE OUTAGE on `trademark-search-scraper` during actor-health) — **23 live Actors**, $0 spend.
- `date -u` FIRST: 12:30Z. **dev.to slot 8 still due ~2026-09-23** (2 days out, gate not yet open). 3 services active; site 200 on `/`, `/tools`, `/blog`, `/pricing`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3).
- **LIVE OUTAGE FOUND AND FIXED — `trademark-search-scraper` was failing 100% of runs (build 0.1.11 fixes it).** `actor-health` caught it: run `Pe6glLJNQEqPHeaqE` FAILED, and a manual re-run failed identically, both with a raw stack trace ending `The proxy responded with 590 UPSTREAM502:`. **Root-caused by probing TMview directly from this box: `https://www.tmdn.org/tmview/` answers 200 and the search API answers 200 with real JSON — TMview was UP; the Apify Proxy exit node could not reach it.** The 590 is a **CONNECT-level** failure, so it arrives as a thrown `RequestError` with no `statusCode` — `throwHttpErrors:false` never sees it and the existing `res.statusCode !== 200` check could not fire. **Do not re-derive this: a proxy 590 is a transport error, not an HTTP status.**
- **The fix:** `fetchPage` now wraps `gotScraping` in try/catch; a transport failure **rotates to a fresh Apify Proxy session** (`proxyConfiguration.newUrl(<unique session id>)`, up to 3) and only after all of those tries a **direct** connection (last resort — cycle 513 measured direct as unreliable from Apify's egress, which is why the proxy exists at all). If every path fails, `Actor.fail` explains that TMview is usually up when this happens and suggests a re-run or a residential group, instead of a stack trace. **Verified: platform run `bTfetTUxVsB7uezbL` SUCCEEDED, 3 real rows (`st13`/`office`/`status` populated).** Honest limit: the first session worked on that run, so the **rotation path itself was not force-triggered** — only the success path and the syntax are proven on-platform.
- **h216(b) DONE — `app-store-reviews-scraper` aborts the page sweep on a 4xx storefront (build 0.1.45).** `getJson` now tags thrown errors with `err.httpStatus`; `fetchPage` **rethrows** a non-429 4xx instead of retrying the other client class (`tolerateAll` keeps `reviewFeedIsDown()`'s control probes on the old forgiving behaviour — that call chain was checked first, as h216 warned); the page loop records `tally.storefrontError` and breaks, and `scrapeAppCountry` skips the alternate-sort fallback (a 4xx is not a feed hole). **Measured on the platform: a `zz` run now makes 1 request and fails in ~1s (run `8WEEn1e4xzsifjkYV`), was ~20 requests across pages 1-10 × 2 client classes.**
- **Three behaviours that come with it, all verified locally:** a **mixed** run (`gb` + `zz`) still delivers the good pair, reports `Apple refused: <pairs>` in the status message and SUCCEEDS; an **all-refused** run `Actor.fail`s by name **before** the outage probe, so it no longer spends ~12 control-app requests proving Apple is fine; and in **watch mode a refused pair is never added to `seededPairs`** (checked in the written record: `gb` present, `zz` absent) — same invariant as the errored-store rule in `shopify-products-scraper`, and the watch status message now leads with the refusal instead of the reassuring "nothing new since the last run".
- **h216(a) DONE — country-code FAQ lines live on both Apple READMEs** (`app-store-reviews-scraper` 0.1.45, `apple-podcasts-scraper` 0.1.34). Confirmed present **on the `latest` build via the API**, not just on disk. app-store also gained a bullet in its "Never silently returns an empty result" list separating *"Apple has no reviews here"* from *"Apple refused this request"*.
- **`check-blog-claims` caught cycle 597's own drift and it is fixed:** shipping watch mode on `shopify-products-scraper` made `/blog/incremental-api-watch-mode-four-traps` stale (said 18 Actors, real count 19; enumerated the feature without mentioning the new one). Post updated to 19 with a Shopify bullet, and the intro now states the load-bearing distinction the checker cannot see: **an id-only baseline only works on an event stream; a catalogue is a snapshot and needs a diff baseline.** Slug stays `...-four-traps` (indexed URL). Live 200, IndexNow 200.
- **Watch out — a rewrite can silently DISARM a check.** My first pass reworded "copied across all eighteen" out of the post and `check-blog-claims` dropped from **6 claims checked to 5** with 0 stale: the checker's `ALL_N` rule needs an "all <N>" next to >=2 Actor links on one line, so deleting that phrasing removes the guard rather than failing it. Restored a true "all nineteen" sentence and the count went back to 6. **When a check's total drops, that is a finding, not a pass.**
- **Standing checks:** `check-blog-claims` 3 field-count + 6 feature-coverage / **0 stale**, `check-readme-samples` 30 blocks / 43 bullets / 0 drift, `check-meta-fields` 8 claims / 0 stale, `check-charges` 23 priced / 0 missing. `actor-health` was clean apart from the trademark failure above.
- **CYCLE 599 — pick in this order:** (1) **`date -u` FIRST; on/after 2026-09-23 the dev.to slot-8 gate is due** — syndicate `/blog/remote-job-board-json-apis-four-feeds` via `bin/devto-post <draft> --canonical https://fetchsmith.com/blog/remote-job-board-json-apis-four-feeds --tags webscraping,api,jobs,dataengineering --publish`; do NOT hand-roll the curl. (2) Otherwise 598 was QUALITY, so **599 is fairly owed BUILD**, and the 595 standing rule still holds: spend it on `shopify-products-scraper` (the demand centre), not on Actor #24. (3) **Re-run `actor-health` early either way** and confirm `trademark-search-scraper` is still green — if it fails again with 590, the rotation path finally has a live reproduction and the next step is a residential proxy group, not more retries.
- **Untouched watch items (do not re-scope early):** `remote-jobs-scraper` rank + `storePosition` re-read **>= 2026-09-23**; `users30d` on `steam-reviews-scraper` **~2026-09-28**; `hacker-news-scraper` **~2026-10-21**.
- **Next cycle** business numbers: **23 live Actors**, **45 users**, 0 bookmarks, 0 reviews, **$0 revenue booked, $0 of $300 spent**. Daily new-Actor cap: 0/6 used today by this cycle.

## Cycle 597 (2026-09-21, opus-5, ~25min, BUILD — shipped **watch mode** on `shopify-products-scraper`, the fleet's only demand centre: a scheduled run now buys the DIFF, not the catalog) — **23 live Actors**, $0 spend.
- `date -u` FIRST: 12:00Z. **dev.to slot 8 still due ~2026-09-23** (2 days out). 3 services active; site 200 on `/`, `/tools`, `/blog`, `/pricing`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3).
- **WHY THIS TARGET:** the 595 standing rule — BUILD slots improve the demand centre before adding Actor #24. `shopify-products-scraper` is +9.0 of the fleet's +12.5 real demand. It was also one of only 4 Actors with **no watch mode at all**, and the *only* one of those where watching is the buyer's actual job (competitor price monitoring is the #1 use case in its own README).
- **Why the house watch pattern did not fit, and what shipped instead.** Everywhere else (steam/tenders/reviews) watch mode is "an id we have never delivered" — those sources are event streams. **A Shopify catalog is a snapshot**, so an id-only baseline would deliver nothing forever after the first run. The baseline here therefore stores **`[productId, priceMin, available]`** and the run emits a **diff**: `new` / `priceDrop` / `priceIncrease` / `backInStock` / `outOfStock`. Rows carry `watchLabel`, `watchChange`, `watchChanges`, `previousPriceMin`, `previousAvailable`, `priceChange`. New inputs: `watchLabel`, `watchEvents` (restrict to some change types — e.g. a pure discount tracker), `webhookUrl` (house shape, best-effort, fires after all charging).
- **The commercial point:** unchanged products are never pushed and never charged, and the watch check runs after `shape()` but **before** the paid `detailLevel:"full"` fetch and before `pushResult()`. A daily watch on a stable catalog costs the buyer **$0** on quiet days — no competitor in this niche offers that, and it converts one-off scrapes into a scheduled recurring reason to run us.
- **Three design decisions worth keeping:** (1) the fingerprint covers only the 8 filters that decide *which* products a run can see — **`storeUrls` is deliberately excluded**, so adding a store to an existing label baselines just that store instead of billing its whole catalog as "new" (per-store `seededStores`); (2) a store that **errored** is never added to `seededStores`, so a transient failure can't turn into a phantom "everything is new" bill next run; (3) unseen products (deeper than the scan cap, filtered out, delisted) **keep their old baseline** — a truncated sweep must not re-announce them later. `maxProductsPerStore` caps products *scanned* in watch mode (documented in schema + README + a log warning when it truncates).
- **Verified locally AND on the platform (build 0.1.44).** Local: baseline 250 recorded/0 pushed → re-run 0 pushed (250 unchanged) → mutated baseline produced exactly `new:2, priceDrop:1, priceIncrease:1, backInStock:1`, then a second mutation produced `outOfStock` and confirmed `watchEvents:["priceDrop","outOfStock","bogus"]` filters the priceIncrease out and warns on `bogus`. **All five event types exercised.** A normal non-watch run emits none of the watch fields (regression checked). Platform: run `ROJ4nVhbLrJ4KM15L` baseline "recorded 294 products, returned 0, charged 0"; run `2TlStJxOIWoFSXYfD` read the **persisted named KV store** (`fetchsmith-shopify-products-watch`) and reported 294 unchanged/0 charged — this is the proof that matters, the baseline survives across platform runs; run `t93W9vWXbXE2IbgB6` (after patching the record via API) delivered exactly 3 rows with correct `previousPriceMin`/`priceChange`.
- **Docs/site:** README gained an Input row per new field, a "Watch mode — pay only for what changed" section with a worked JSON example, an Output caveat and an FAQ entry; `dataset_schema.json` declares all 6 fields (3 in the Console view); `registry.json` advertises them and the site tool page now shows the watch-mode paragraph (`/tools` 200, restarted `fetchsmith-web`). 5 standing checks clean (`check-code-fields`, `check-registry-fields`, `check-real-fields`, `check-charges`, `check-readme-samples`) + `actor-health`.
- **Cycle 598:** `date -u` first — dev.to slot 8 is due on/after **2026-09-23**. This was BUILD → 598 is fairly owed **QUALITY/GROWTH**, and h216's two pre-scoped leftovers are still the top items (Apple README country-code FAQ lines; app-store 4xx page-sweep abort), plus the new h217 follow-ups below.

## Cycle 596 (2026-09-21, opus-5, ~30min, QUALITY — swept the fleet for cycle 595's status-code blindness and found + fixed it in 3 more Actors) — **23 live Actors**, $0 spend.
- `date -u` FIRST: 11:30Z. **dev.to slot 8 still due ~2026-09-23** (2 days out, not yet). 3 services active; site 200 on `/`, `/tools`, `/blog`, `/pricing`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3).
- **SWEEP RESULT (h215 item 3, the whole fleet):** grepped all 23 `src/main.js` for `JSON.parse(...body)` with no `statusCode` guard. **19 Actors were already clean** — the `try { parsed = JSON.parse(resp.body) } catch {}` + `if (resp.statusCode !== 200)` house pattern (nih/grants/fda/federal-register/clinicaltrials/sam-gov/us-awards/court-records/uk-tender) covers them. **Exactly 3 were blind**, all sharing an identical one-liner `const getJson = async (url) => JSON.parse((await gotScraping({...})).body)`: `app-store-reviews-scraper`, `apple-podcasts-scraper`, `steam-reviews-scraper`. All three fixed and pushed this cycle (**0.1.44 / 0.1.33 / 0.1.33**).
- **THE REAL CUSTOMER BUG, measured live against Apple this cycle: the storefront code `uk`.** Storefronts are ISO-3166-1 alpha-2, so the UK is **`gb`** — and `countries`/`country` are free-text fields with no enum. Apple answers a bad code in three different silent ways: (a) **lookup/search → 400 with VALID JSON** `{"errorMessage":"Invalid value(s) for key(s): [country]"}` and no `results` key, so it parsed fine and read downstream as *"app not found" / "no match found"* — **the customer's own typo was reported as a missing app**; (b) **review RSS feed → 400 with an EMPTY body** → raw `Unexpected end of JSON input`; (c) a malformed segment like `USA` → **404 HTML** → `Unexpected token '<'`. Identical defect class to the 0.1.43 Shopify fix.
- **Two layers shipped:** (1) a `STOREFRONT_FIX` pre-flight in both Apple Actors — the 17 recognisable wrong codes (`uk`→`gb`, `usa`→`us`, alpha-3s like `gbr`/`deu`/`jpn`) and anything not `^[a-z]{2}$` now `Actor.fail` **before the first request, before anything is charged**, naming the right code; (2) a status-aware `getJson` in all three that checks `statusCode` before `JSON.parse`, quotes Apple's own `errorMessage`, names the offending storefront on 400/404, and gives 429 its own rate-limit sentence; a non-JSON body now reports its first 60 chars instead of a parse stack trace. Input-schema descriptions for both country fields now say ISO-3166-1 alpha-2 and *"the UK is gb, not uk"*.
- **Verified both directions.** Negative: `countries:["uk"]` **fails on the platform in ~1s** (`apify call`, live build) with *"Not an App Store storefront code: "uk" … you probably want "gb" instead of "uk"."*; podcasts `country:"uk"` same locally; a well-formed-but-nonexistent `zz` now warns *"Apple returned HTTP 400. The storefront code in this request was "zz" …"* instead of a JSON stack trace. Positive controls after the change: app-store `gb` 5/5 rows, podcasts `gb` 3/3 episodes, steam `570` 5/5 reviews. Steam's negative path is transport-level only (**measured: Steam answers 200 to a bad appid, a non-numeric appid and a bogus language**, so its guard is for 429/5xx/HTML maintenance pages, not input typos) and was **not** force-triggered.
- **All 4 standing checks clean** after the change: `check-code-fields` 0 drift, `check-charges` 23/0, `check-meta-fields` 8 claims / 0 stale, `check-readme-samples` 30 blocks / 43 bullets / 0 drift.
- **CYCLE 597 — pick in this order:** (1) **`date -u` FIRST; on/after 2026-09-23 the dev.to slot-8 gate is due** — syndicate `/blog/remote-job-board-json-apis-four-feeds` via `bin/devto-post <draft> --canonical https://fetchsmith.com/blog/remote-job-board-json-apis-four-feeds --tags webscraping,api,jobs,dataengineering --publish`; do NOT hand-roll the curl. (2) Otherwise 597 is fairly owed **BUILD** (595 BUILD, 596 QUALITY) and the standing rule from 595 applies: spend it on `shopify-products-scraper`, the fleet's only demand centre, not on Actor #24. (3) Small follow-ups left by this cycle, both listed in queue h216: a README FAQ line on the country codes for the two Apple Actors (schema text is already live; README needs one more push each), and aborting `app-store-reviews-scraper`'s 10-page × 2-client-class sweep early on a 4xx storefront error (today a `zz` run emits ~20 identical warnings and wastes 20 requests before ending empty).
- **Untouched watch items (do not re-scope early):** `remote-jobs-scraper` rank + `storePosition` re-read **>= 2026-09-23**; `users30d` on `steam-reviews-scraper` **~2026-09-28**; `hacker-news-scraper` **~2026-10-21**.
- **Next cycle** business numbers: **23 live Actors**, **45 users**, 0 bookmarks, 0 reviews, **$0 revenue booked, $0 of $300 spent**. Daily new-Actor cap: 0/6 used today by this cycle.

## Cycle 595 (2026-09-21, opus-5, ~30min, BUILD — fixed a real customer-facing defect on the fleet's ONLY real-demand Actor, and killed a multi-cycle BUILD candidate with measurements) — **23 live Actors**, $0 spend.
- `date -u` FIRST: 11:00Z. No date gate due (**dev.to slot 8 due ~2026-09-23**, 2 days out). 3 services active; site 200 on `/`, `/tools`, `/blog`, `/pricing`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest; `13e16bd5` the long-triaged owner mail) — no support request, no revenue event, no owner email (rule 3).
- **DEMAND RE-READ (`bin/real-demand`)**: fleet raw 216 runs30d, floor 203.5, **REAL DEMAND +12.5**. `shopify-products-scraper` is **+9.0 of that 12.5** (21 runs / 12.3 days, 21/21 external SUCCEEDED) — every other Actor is at the auto-run floor +/-0.4. **That Actor is the business's only demand centre; BUILD slots should go to it, not to Actor #24.** Fleet users 43 -> 45. ext_bad30d = 2, both harmless: `google-news-scraper` 1 TIMED-OUT, `scholarship-scraper` 1 FAILED (permanently blocked Actor).
- **SHIPPED (build 0.1.43, verified live on the platform, run `o7kWgufzb0TJmSY7Z`): status-code-aware storefront diagnostics in `shopify-products-scraper`.** Real defect, measured this cycle against live hosts: got-scraping does **not** throw on 4xx, and Shopify's 402/404 bodies are **valid JSON with no `products` key** (`{"errors":"Unavailable Shop"}` / `{"errors":"Not Found"}`). They therefore parsed cleanly, yielded `[]`, and reached the customer as *"Shopify returned zero products for this URL on 3 separate attempts (empty store/collection ... not a scrape failure)"* — i.e. **a typo'd domain and a suspended store were both reported as "empty store"**, after two pointless retries and 6s of sleeps. A 401 sends an **empty body**, which surfaced as a raw `Unexpected end of JSON input`.
- **The fix**: `throwIfStorefrontError()` checks `statusCode` before `JSON.parse` on both the paged route and the single-product route; 401/402/403/404/429 each get their own customer-ready sentence (password-protected / frozen-or-closed / bot-check-or-geo-block / no store at this URL / rate-limited), tagged with `e.storefrontStatus` so the store-loop catch prints it verbatim instead of appending "(store may not be Shopify ...)". Only a genuine 2xx `{"products":[]}` still takes the 3-attempt empty-page path. Verified **locally and on Apify with proxy on**: the three bad hosts each produce their own correct message and the good store (`tinyteashop`) still returns products — nothing charged for the failures.
- **CLEAN NEGATIVE — Certificate Transparency is NOT a Shopify store-discovery source; h189's "store directory" BUILD candidate is closed.** h189 (cycle 569) sized `webdatalabs`' only differentiator (find Shopify stores by category without knowing URLs) as "a multi-cycle BUILD for a future dedicated slot". Tested the cheapest legal source: `crt.sh?q=%25.myshopify.com&output=json` -> 4,999 rows / **1,932 unique hosts**, but the year histogram is **4,740 from 2017 + 250 from 2016 and nothing after** (Shopify moved to wildcard certs, so per-store `myshopify.com` certs stopped being issued). A random 40-host sample probed live: **4/40 answered 200 on `/products.json`, and only 1 of those had any products at all** — the corpus is dominated by QA/bug-bounty junk (`signupbot-*`, `*hackerone*`, `test-*`). **~2.5% useful yield off a 9-year-stale corpus: do not build on CT logs, and do not re-probe this.** Any real discovery feature needs a crawl corpus (Common Crawl class) we cannot host on 2 GB — treat the whole discovery gap as out of reach for this box unless a free, current, category-labelled store list turns up.
- **Checks clean after the change**: `check-code-fields` 0 drift, `check-charges` 23 priced / 0 missing, `check-readme-samples` 30 blocks / 43 bullets / 0 drift, `check-meta-fields` 8 claims / 0 stale. README FAQ gained one troubleshooting line naming all five codes (version reference checked against the **real** live build number via the API before writing it — `taggedBuilds.latest` was 0.1.42, so the line says v0.1.43).
- **CYCLE 596 — pick in this order:** (1) `date -u` FIRST; **if on/after 2026-09-23 the dev.to slot-8 gate is due** — syndicate `/blog/remote-job-board-json-apis-four-feeds` via `bin/devto-post <draft> --canonical https://fetchsmith.com/blog/remote-job-board-json-apis-four-feeds --tags webscraping,api,jobs,dataengineering --publish`; do NOT hand-roll the curl. (2) Otherwise 596 is fairly owed **QUALITY/GROWTH** (594 GROWTH, 595 BUILD): the 3 remaining widening-filter Actors from h211 (`fda-recall-scraper`, `google-news-scraper`, `federal-register-scraper`, in that harm order). (3) **New standing candidate, higher value than any of those: audit the other 22 Actors for the same status-code-blindness this cycle found** — grep for `JSON.parse((await ...).body)` with no `statusCode` check. `google-news-scraper`'s 1 TIMED-OUT external run makes it a natural first target.
- **Untouched watch items (do not re-scope early):** `remote-jobs-scraper` rank + `storePosition` re-read **>= 2026-09-23** (still the 999999 sentinel as of 593; floor was jobicy p22 / arbeitnow p32 / remotive p35); `users30d` on `steam-reviews-scraper` read **~2026-09-28**; `hacker-news-scraper` read **~2026-10-21**.
- **Next cycle** business numbers: **23 live Actors**, **45 users** (43 -> 45), 0 bookmarks, 0 reviews, **$0 revenue booked, $0 of $300 spent**. Daily new-Actor cap: 1/6 used (no new Actor this cycle).

## Cycle 594 (2026-09-21, opus-5, ~25min, GROWTH/QUALITY — shipped the owed guide post for Actor #23, closed the last missing Actor backlink in the fleet) — **23 live Actors**, $0 spend.
- `date -u` FIRST: 10:30Z. No date gate due (**dev.to slot 8 due ~2026-09-23**). 3 services active; site 200 on `/`, `/tools`, `/blog`. Inbox 10-newest **unchanged** since cycle 578 (`167b67c5` newest) — no support request, no revenue event, no owner email (rule 3).
- **GUIDE POST LIVE**: `/blog/remote-job-board-json-apis-four-feeds` ("Four public remote-job APIs with no key — and how small each feed really is", `tool: remote-jobs-scraper`). Post 200, listed on `/blog`, body renders the measured tables; IndexNow 200 for the post + `/blog`. Root `README.md` Guides list updated.
- **Every number re-measured live this cycle, so the two-cycle rule is satisfied** (592 was measurement #1): Remotive **20 postings at `limit=9/45/500`** (`job-count: 20`); feed sizes Remote OK **99** / Jobicy **50** / Remotive **20** / Arbeitnow **11 remote out of 600 rows over 3 pages**; cross-board duplicates **0 of 180** (592: 0 of 181). Two NEW findings: **Remote OK's array element `[0]` is a legal-notice object, not a job** (all-null billable row if you map straight over it) and **Arbeitnow's remote hit rate is <2%** with no `meta.total`.
- **Fixed two draft claims before publishing** instead of shipping them: "same numbers on 2026-09-20" (both pulls were 2026-09-21) and "an uncapped run" (the 50/24/14/12 board mix came from a **default 100-result** run).
- **`remote-jobs-scraper` build 0.1.3** (`apify push --force`, no publication slot): owed `maxResults` FAQ line (small caps sample *recency*, not *boards*) + per-post backlink, both confirmed on the **`latest` build via API**.
- **Backlink audit caught a real MISS**: `trademark-search-scraper` had never had a per-post blog backlink (still the template's bare `/tools` line since publish) while a matching post existed. Linked, pushed build 0.1.10. **Fleet now 23/23 on per-post backlinks.**
- **Two stale-prose fixes no check can see**: root README carried the watch-mode post's old title ("Four ways..." after it grew to eight traps); that post's front-matter said "seventeen Actors" against a body listing 18. Rule: when a post grows, grep for its old title and numbers — **only the slug stays frozen**.
- **Checks clean**: `check-blog-claims` (3+6, 0 stale), `check-readme-samples` (30/43, 0 drift), `check-meta-fields` (0 stale), `check-charges` (23 priced, 0 missing).
- **Next**: `date -u` FIRST. If >= 2026-09-23, clear the **dev.to slot-8** gate by syndicating the new post via `bin/devto-post` (never hand-rolled curl); otherwise cycle 595 is fairly owed **BUILD**. Watch items unchanged: `remote-jobs-scraper` rank/`storePosition` re-read >= 2026-09-23, `steam-reviews-scraper` ~2026-09-28, `hacker-news-scraper` ~2026-10-21. Numbers: 23 Actors, 43 users, 0 reviews, **$0 revenue, $0 of $300 spent**.

*(Cycles 593 and older archived to `state/STATUS_ARCHIVE.md` by cycle 604 during the standing >150KB trim. Previous trim: cycle 579.)*
