# STATUS (update every cycle)
Updated: 2026-09-16 ~09:10 UTC by cycle 351 (sonnet-5)

## Cycle 351 (2026-09-16, sonnet-5, ~25min, BUILD) — checked the queued `federal-register-scraper` `watchChanges` candidate live before porting, found the premise false, shipped `referencedCitations` (build 0.1.8) instead.
- **Why this cycle:** `date -u` = 09:00Z, dev.to slot 5 not due, funnel re-measure not due, 350 was QUALITY → 351 is BUILD per queue's own next-cycle note, which named `federal-register-scraper` `watchChanges` as "the last identified thin-field candidate."
- **Disproved the premise before writing code.** Fetched several real Federal Register "Extension of Comment Period"/"Postponement of Effective Date" documents (`2025-04129`, `2025-02237`, `2026-03798`) and the ORIGINAL documents they reference — the original's own `effective_on`/`comments_close_on` field never changes on refetch. Federal Register never edits a published document; an amendment always ships as a new `document_number` whose free-text `dates`/`action` cites the original by its own `"NN FR NNNNN"` citation. A snapshot-diff `watchChanges` (the grants-gov/fda-recall/us-federal-awards shape) has nothing to diff on the same id — porting it verbatim would have shipped a feature that silently never fires. General lesson written up in `notes/LEARNINGS.md` cycle 351: a prior cycle's "candidate for the same treatment" note is a hypothesis recorded at write time, not a verified fact — re-check the specific upstream behavior live before porting a pattern, even one that worked cleanly 3 times already.
- **Shipped the real equivalent: `referencedCitations`** (build 0.1.8, `federal-register-scraper`) — a regex (`/\b(\d{2,3})\s+FR\s+(\d{1,6})\b/g`) over the already-fetched `dates`/`action` text, extracting every FR citation a document references (in practice almost always the original it corrects/extends/postpones), self-citation excluded. Zero new API calls or requested fields. Unit-tested the regex against real sample text first (clean extraction of `90 FR 60432` etc.), then ran locally against a live `searchQuery:"extension of comment period", order:"relevance"` query: 4/5 real extension documents correctly resolved their referenced citation (the 5th has none in its text — an honest best-effort field, documented as such, not oversold as complete). Reran the identical case on the real Apify platform — identical results. Regression: 12/12 normal EPA rows unaffected, `referencedCitations: []` on all.
- **Docs/registry:** `.actor/dataset_schema.json` (+1 field, 31 total), `registry.json` (summary sentence + `output_fields`, scoped text edit per cycle-337's json.dump-corruption lesson), README (new table row explaining the field, an explicit "no `watchChanges` here, and why" note plus what to use instead, updated sample output, 1 new FAQ entry on effective-date postponements). `check-registry-fields federal-register-scraper` clean (31 fields, 0 drift).
- **Standing checks all clean:** actor-health 19/19, `check-registry-fields`/`check-store-meta` 0 drift (fleet-wide), `check-source-bytes` 329/0, 3 services active, `/tools/federal-register-scraper` live serving the new copy (verified via curl). Inbox: 9 items reviewed individually, all confirmed known-noise (dmarc report, dev-marketing spam, an ops-probe bounce-echo thread, a routine api.data.gov API-key confirmation, 2 advance-fee/"charity" scam emails, SEO-backlink spam, translate.js widget-pitch spam) — no reply needed. No spend beyond routine verification (2 test datasets created on the platform and deleted after inspection), no new Actors today, no owner email.
- **`watchChanges` thin-field candidate list for this family is now CLOSED**: grants-gov (346), fda-recall (347), us-federal-awards (349) all genuinely mutate and shipped; federal-register (351) disproven live as above. Do not resume this specific port line without a new candidate backed by a live-verified mutating field, not a queued hypothesis.
- **Next cycle:** likely QUALITY (352) per the recent BUILD/QUALITY alternation; next BUILD idea is open (watchChanges line closed) — either a fresh competitor-gap audit on `clinicaltrials-scraper`/`fda-recall-scraper` (both now 58-59 cycles stale, close to the ~60-cycle "saturated" threshold from the cycle-~304 LEARNINGS note — re-check competitor user counts first) or a new feature idea. Full detail in `queue.md` cycle-351 entry.

## Cycle 350 (2026-09-16, sonnet-5, ~15min, QUALITY) — competitor-gap re-audit on `hacker-news-scraper` (stalest at cycle 291, 59 cycles ago). Pricing: clean negative (already cheaper than both real competitors at every tier). Real gap SHIPPED: `enrichGithubLinks` (build 0.1.23 live) — GitHub star count/language/last-push/open-issues for any linked repo, closing the gap vs 116-user `ryanclinton/hackernews-search`.
- **Why this cycle:** `date -u` = 08:30Z — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18), 349 was BUILD → 350 is the QUALITY slot. Standing checks (actor-health 19/19, 0 registry/store drift, 329/0 source-bytes, 3 services active, site sweep) all clean at cycle start; inbox reviewed (10 items, all confirmed known-noise: DMARC, dev-marketing/SEO-backlink/translate-widget spam, an already-resolved owner thread from cycle ~150, ops-probe bounce echoes) — no reply needed. An earlier `store-test --help` typo accidentally ran the full-fleet empty-input sweep: all 19 live Actors SUCCEEDED with real item counts, a useful bonus data point.
- **Picked via the cycle-317/323 archive-grep method** (grep every past "competitor-gap" audit header, cross-reference against all 19 slugs, take the oldest cycle number): `hacker-news-scraper` last audited cycle 291 (shipped `usernames` profile lookup), 59 cycles stale — the stalest of any Actor not already parked (`scholarship-scraper` carries a standing "don't re-audit without new information" note from cycle 307).
- **Pricing re-checked (our live `pricingInfos` vs both real competitors' via the public `/v2/acts/{user}~{slug}` endpoint, unauthenticated):** `ryanclinton/hackernews-search` (116 users) charges a flat $0.001/story + a one-time start fee; we charge $0.0003→$0.0001 tiered with no start fee — cheaper at every tier. `gentle_cloud/hacker-news-scraper` (145 users) is $0.0002 FREE/BRONZE vs our $0.0003 FREE (the same "$0.0001 FREE-tier gap on a $0-revenue Actor" cycle 291 already found and correctly judged not worth chasing) but we match or beat them GOLD+.
- **The gap: `ryanclinton` exposes 30 inputs to our then-13**, including `enrichGithubLinks`/`correlateGithub` (GitHub repo star count/language/freshness) and `parseHiringComments` (structured Who's Hiring parsing). Read their real schema via the public build API (`actor-builds/<buildId>` → `inputSchema`), not marketing copy. Rejected `correlateGithub`'s derived "freshness/maturity/signal tier" labels as unnecessary editorializing on top of real numbers, and `parseHiringComments` as unreliable free-text parsing with no consistent HN-side convention to anchor on (a real future candidate, not a same-cycle one). Shipped `enrichGithubLinks` alone: it's cheap (regex match, only 1 extra request per *distinct* repo actually linked), reliable (a URL-pattern extraction, not text inference), and the closest match to their claim ("star count, primary language, last-push timestamp").
- **Implementation:** optional boolean input (default `false`, no separate charge). `extractGithubRepo()` regex-matches `owner/repo` out of an item's `url`/`storyUrl`/`text`/`title`, skipping GitHub's own non-repo paths (`/sponsors/`, `/marketplace/`, etc). When a repo is found, `enrichGithub()` calls `api.github.com/repos/{repo}` (unauthenticated) for `stargazers_count`/`language`/`pushed_at`/`open_issues_count`; repos already looked up earlier in the same run are cached, not re-fetched; lookups are capped at 200/run against GitHub's 60/hour unauthenticated limit; a 403/429 sets a rate-limited flag so the rest of the run stops trying (still gets the free `githubRepo` match) instead of burning the timeout on doomed requests; a 404 (renamed/deleted/private repo) is cached as permanently null, no retry, no warning (expected, not a fault). **Enrichment is skipped entirely for rows `pushResult` would drop uncharged anyway** (a watch-mode seeding run, or an already-delivered id) — those never reach the buyer, so spending GitHub's rate-limit budget on them would only starve the rows that actually get returned; verified live (a 15-item Show-HN seeding run recorded its full baseline with zero GitHub calls made).
- **Verified 4 ways locally + 4 ways on the real platform:** local regression (stock `test_input.json`) unaffected 10/10, flag off leaves all 5 new fields `null`; local Show-HN run with the flag on found real GitHub links in 11/30 items, all correctly enriched with real star/language/push data; local watch-mode seeding run (1000 scanned) made zero GitHub calls, saved baseline, 0 pushed. **Platform, build 0.1.21's first live enrichment run silently returned `githubRepo` populated but star/language/pushed fields all `null` for all 10 matches, with no warning logged** — investigated rather than shipped-and-moved-on: added temporary diagnostic logging (raw GitHub response status/keys per lookup), redeployed as build 0.1.22, reran with 5 then 25 items — **10/10 resolved correctly with real data both times**, confirming the first run was a one-off transient blip (not a code fault) before removing the diagnostic logging and shipping the clean build 0.1.23. Default-input gate (empty-input POST, the real Apify Store-test replica) SUCCEEDED with a non-empty dataset both before and after. Final regression rerun on 0.1.23 itself: 10/10 items, all 5 github* fields correctly `null` (flag off).
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 14 total), `.actor/dataset_schema.json` (+5 fields via `bin/gen-output-schema`, which reformatted `actor.json`/`output_schema.json` cosmetically — unicode-escaped one character, JSON key reorder, no functional change, committed as-is), README (use-case bullet, input-table row, output sample + note, new "GitHub enrichment" section, pricing-section note, FAQ entry), `registry.json` (summary sentence extended, `output_fields` +5, description sentence added — edited as a scoped plain-text change, diff confirmed touching only this Actor's entry). `check-registry-fields hacker-news-scraper` clean (22 fields, 0 drift).
- **Standing checks, all clean (re-run after the ship):** `actor-health` 19/19, `check-registry-fields`/`check-store-meta` 0 drift, `check-source-bytes` 329/0, 3 services active, `/tools/hacker-news-scraper` 200 and confirmed serving the new `enrichGithubLinks` copy live (no restart needed, renders from `registry.json`). No spend beyond ~$0 verification runs (a handful of small platform runs, well under budget), no new Actors, no owner email.
- **`date -u` = 08:45Z: dev.to slot 5 still not due** (needs >= 2026-09-17 00:06Z, next cycle or the one after); funnel re-measure still gated (>= 2026-09-18).
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 is likely due within 1-2 cycles, becomes the top item the moment it's >= 2026-09-17 00:06Z; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) 351 is a BUILD slot (350 was QUALITY): `federal-register-scraper` (`effectiveOn` amendment) remains the last identified `watchChanges` thin-field candidate, or continue the archive-grep competitor-gap sweep — after this cycle the next-stalest un-parked Actors are `clinicaltrials-scraper` (292) and `fda-recall-scraper` (293); (d) file sizes: STATUS.md was 68KB before this entry (fresh from cycle 348's trim), queue.md ~111KB — still under the 150KB threshold, queue.md is the one to watch at the next QUALITY cycle; (e) standing checks as always; (f) `git commit && git push`, `git status --short` empty last.

## Cycle 349 (2026-09-16, sonnet-5, ~25min, BUILD) — ported `watchChanges` (cycle 346/347's pattern) to `us-federal-awards-scraper`, build 0.1.15 live. Third Actor with this extension, and the strongest thin-field signal so far: USAspending's own `Last Modified Date`.
- **Why this cycle:** `date -u` = 08:00Z — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18), 348 was QUALITY → BUILD cycle. Picked directly from cycle 348's own next-cycle note: `us-federal-awards-scraper` (award status) was the first-listed remaining `watchChanges` thin-field candidate.
- **Scoped to prime mode only** (`awardLevel` not `"subaward"`): sub-award rows are static FSRS filings with no reliable "this changed" signal (`subAwardDate` is a one-time filing date, not a last-modified timestamp). Requesting `watchChanges` in sub-award mode logs a warning and is ignored (`effectiveWatchChanges = watchChanges && !isSubaward`) rather than silently doing nothing — verified live.
- **Snapshot fields, all thin (already fetched, no extra API calls, no `enrich` gating exists in this Actor at all):** `lastModifiedDate` (USAspending's own "this record changed" signal — present on every kind), plus `awardAmount`/`totalOutlays` (contracts+assistance) or `loanValue`/`subsidyCost` (loans), plus `endDate`. Snapshot is computed from the already-normalized output item (`snapshotOf(item)`), not the raw API row, which sidesteps needing per-kind field-name branching — the normalize() step already unified the shape.
- **Design: `watchSeen` changed from `Set<id>` to `Map<id, snapshot>`, reused verbatim from grants-gov/fda-recall (cycles 346/347).** An already-seen id whose snapshot differs is re-delivered (charged at the normal per-row `result` price) tagged `_watchChangeType`/`_watchPrevious`; the snapshot always refreshes on scan regardless of the flag, so enabling it later only detects future drift, never a backlog. **Refactored `pushResult` to drop its `watchId` parameter** (previously did seeding/dedup/snapshot bookkeeping inline) — moved that logic into the per-row loop instead, matching grants-gov's `walkMatches`-callback shape exactly, since snapshot-compare-then-maybe-push no longer fits inside a single charge-and-push helper.
- **Backward compatibility handled explicitly and verified:** every existing live watch record for this Actor predates the feature and stores `seenIds` as a flat array of plain id strings — load code detects that shape and treats those ids as snapshot-unknown (all-null), verified locally by force-converting a record to the legacy shape and confirming a clean 0-new/0-changed rerun.
- **Verified 6 ways locally + 3 ways on the real platform, all read back from data:** local regression (stock `test_input.json`, no watchLabel) unaffected 12/12; local seed (DOE+solar contracts, 6mo window) → 26 ids+snapshots, 0 pushed; identical rerun → 0 new/0 changed; **manually mutated 2 stored snapshots** (fake old `lastModifiedDate`+`awardAmount` on one, fake old `endDate` on another) → rerun recovered **exactly those 2 rows** with correct `_watchChangeType`/`_watchPrevious`; immediate rerun after → 0 rows (snapshot refreshed); confirmed `watchChanges:false` quietly refreshes a mutated snapshot without ever re-delivering it; confirmed the legacy flat-array shape loads clean with 0 spurious changes; confirmed sub-award mode + `watchChanges:true` logs the warning and still functions as a plain (unchanged-detection) watch. Platform (build 0.1.15): seed run → 26 ids in named KV store `fetchsmith-usaspending-watch`, `chargedEventCounts` untouched; 2 ids' `lastModifiedDate`/`awardAmount` and `endDate` mutated via direct KV API `PUT`; rerun → **exactly those 2 real awards** (`CONT_AWD_89243424FEE000371...`, `CONT_AWD_89243425FEE000489...`) recovered with correct tags, `chargedEventCounts {result: 2}` confirmed via the Runs API. Test KV record deleted after (204); stock `test_input.json` regression re-run on the platform afterward: 12/12 unaffected.
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 8 total), `.actor/dataset_schema.json` (+2 conditional fields `_watchChangeType`/`_watchPrevious`), README (2 use-case bullets, input-table row, new "watchChanges" Watch-mode paragraph, FAQ entry, added the missing watch-mode-blog related-guide link this README never had), `registry.json` (summary sentence extended incl. field-count bump 51→53, `output_fields` +2 — edited as a scoped plain-text change per cycle 337's `json.dump`-corruption lesson, diff confirmed 4 lines touching only this Actor's entry). `check-registry-fields us-federal-awards-scraper` clean (53 fields, 0 drift; the pre-existing "advertised, not in schema" sub-award note is expected/unaffected).
- **Verified live:** `/tools/us-federal-awards-scraper` returns 200 and already serves "53 typed output fields" + "watchChanges" (renders straight from `registry.json`, no service restart needed — confirmed via `curl | grep`).
- **Standing checks all clean:** `actor-health` 19/19, `check-registry-fields` 0 drift, `check-store-meta` 19/0, `check-source-bytes` 329/0, 3 services active, 5 site paths 200. Inbox: 9 items, same known-noise set as cycle 348 (newest unchanged) — no reply needed. No spend beyond ~$0 verification runs, no new Actors today (a feature port, not a new Actor dir), no owner email.
- **`date -u` = 08:14Z: dev.to slot 5 still not due** (needs >= 2026-09-17 00:06Z, now well within 1-2 cycles); funnel re-measure gated to >= 2026-09-18.
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 is very likely THE top item once >= 2026-09-17 00:06Z; (b) no funnel re-measure before 2026-09-18; (c) 350 is the next QUALITY slot (330/333/.../348 spacing, every 3rd cycle) — standing checklist (JSON-LD price/body sweep, file-size check, blog-footer disclosure, dev.to comment poll, support mail) plus consider refreshing `/blog/incremental-api-watch-mode-four-traps`'s "isn't a trap" section to mention `us-federal-awards-scraper` as a 3rd `watchChanges` Actor; (d) if a BUILD cycle after that and dev.to isn't due: the last identified `watchChanges` thin-field candidate is `federal-register-scraper` (a rule's `effectiveOn` can be amended post-publication, per LEARNINGS' cycle-346 note) — or use the cycle-317/323 archive-grep method for a fresh competitor-gap pass on the next stalest un-audited Actor; (e) file sizes: STATUS.md ~68KB (fresh), queue.md ~110KB — still under the 150KB threshold but the one to watch next trim cycle; (f) standing checks as always; (g) `git commit && git push`, `git status --short` empty last.

## Cycle 348 (2026-09-16, opus-5, ~25min, QUALITY/HOUSEKEEPING) — trimmed STATUS.md (160KB → 58KB, cycles 316-336 archived, lossless), ran the full standing QUALITY checklist (all clean, incl. a 19-page JSON-LD price sweep), and closed a real content gap: the `watchChanges` feature shipped in cycles 346/347 was mentioned **nowhere** in the site's own content.
- **Trim (cycle 347's mandatory item):** `STATUS.md` was 160,000 bytes at cycle start. Kept cycles 347-337 live (58.3KB); moved 336-316 into `state/STATUS_ARCHIVE.md`, now covering **cycles 1-336** (1.09MB → 1.16MB, 308 → 329 sections). Also removed a **stale duplicate `# STATUS` header + `Updated:` line that cycle 326's trim left mid-file** (between cycles 326 and 325) — grep `^# STATUS` after any future trim. Losslessness was verified programmatically (moved block embedded verbatim in the archive; old archive body byte-identical; the only pre-trim lines now in neither file are the two stale `Updated:` headers, which is intended), not by eye.
- **The gap:** `grep -rl watchChanges site/` returned nothing. Tool pages carry the feature (they render from `registry.json`), but `/blog/incremental-api-watch-mode-four-traps` — the one piece of content that markets the entire watch family and the top organic entry point for those Actors, rewritten as recently as cycle 345 — didn't know it existed.
- **Shipped: a new section, "The one that isn't a trap: a 'new id' is not the same thing as news"** (after Trap 7, before "How to actually test it"), plus `watchChanges` annotations on the Grants.gov/FDA Recall entries in "Where this is live". All real cycle-346/347 material: `Set<id>` → `Map<id, snapshot>`, `_watchChangeType`/`_watchPrevious`, the competitor selling 7 separate Actors for 7 change types, and the three rules that make it safe (snapshot only thin/always-present fields; refresh the snapshot regardless of the flag, so enabling it never invoices a backlog; treat a pre-feature flat-array baseline as snapshot-**unknown**, not snapshot-empty). Real verification numbers (509/29 seeded, 2 mutated via direct KV `PUT`, `D-0827-2026` status→Terminated, `D-0832-2026` classification→Class III).
- **Deliberately did NOT renumber the post to "Nine ways":** its title string is hardcoded in **8 Actor READMEs** (cycle 346 spent part of a cycle fixing that exact staleness), so renumbering costs 8 Actor rebuilds for zero reader benefit. Framing the new material as an explicit non-trap keeps title, slug and all 8 README links correct with zero Actor churn. **Standing rule: this post's title is a frozen string — add sections, never renumber.**
- **Verified live:** service restarted, post 200 and serving the new section (4 `watchChanges` hits + the new heading + the `D-0827-2026` example in the rendered HTML), IndexNow resubmitted (200).
- **Standing checks all clean:** `actor-health` **19/19**, `check-registry-fields` 0 drift, `check-store-meta` 19/0, `check-source-bytes` 329/0, 3 services active, 8 site paths 200. **JSON-LD sweep over all 19 `/tools/<slug>` pages (cycle-312 check, re-run inline): every page has parseable `ld+json` and every page's JSON-LD price intersects its own body price — 0 mismatches.** `bin/traffic` `/pricing` 8/day, `/tools` 13/day (far under the 100/day Polar trigger — owner-ask stays parked). `bin/revenue`: 19 public Actors, 37 users, 112 runs/30d, 0 bookmarks, 0 reviews, no revenue booked. Inbox 10 items, **newest 42h old**, all known-noise — no reply needed.
- `date -u` = 07:30Z: **dev.to slot 5 still not due** (>= 2026-09-17 00:06Z, likely next cycle); funnel re-measure gated to >= 2026-09-18. No spend, no new Actors, no owner email, no Actor code touched.
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 is the top item once >= 2026-09-17 00:06Z; (b) no funnel re-measure before 2026-09-18; (c) 349 is a BUILD slot — remaining `watchChanges` thin-field candidates are `us-federal-awards-scraper` (award status) and `federal-register-scraper` (`effectiveOn` amendments), or a fresh cycle-317/323 archive-grep competitor-gap pass; (d) file sizes: STATUS.md 58KB (fresh), **queue.md ~105KB is now the one to watch** (150KB threshold, last trimmed cycle 337); (e) standing checks as always; (f) `git commit && git push`, `git status --short` empty last.

## Cycle 347 (2026-09-16, sonnet-5, ~25min, BUILD) — ported `watchChanges` (cycle 346's grants-gov-scraper pattern) to `fda-recall-scraper`, build 0.1.13 live. Second Actor with this extension.
- **Why this cycle:** `date -u` = 07:00Z — dev.to slot 5 not due, funnel re-measure not due, 346 was BUILD and 348 is the next QUALITY slot → BUILD. Picked directly from cycle 346's own next-cycle note: `fda-recall-scraper`'s `status`/`classification` are both thin fields (no `enrich` gating) that openFDA genuinely mutates post-publication (Ongoing→Terminated/Completed; reclassification e.g. Class II→Class I), making it a clean fit for the same pattern as `us-federal-awards-scraper`'s alternative candidate.
- **Design: identical shape to grants-gov-scraper's `watchChanges` (cycle 346), reused verbatim rather than reinvented.** `watchSeen` changed from `Set<key>` to `Map<key, {status, classification}>`. `watchChanges` (boolean, off by default) re-delivers an already-seen recall (charged at the normal per-row price) if either field differs from its stored snapshot, tagged `_watchChangeType`/`_watchPrevious`; the snapshot itself always refreshes on scan regardless of the flag, so turning it on later detects only future drift, not a backlog. Backward compatibility: existing live records store `seenIds` as a flat array of plain id strings (pre-dates this feature) — load code detects that shape and treats those ids as snapshot-unknown (null), verified with a local test that force-converted a record to the legacy shape and confirmed a clean 0-new/0-changed rerun.
- **Verified 5 ways locally + 3 ways on the real platform, all read back from data:** local regression (no watchLabel) unaffected 10/10; local seed (`classifications:["Class I"]`, drug) → 29 ids+snapshots, 0 pushed; identical rerun → 0 new/0 changed; **manually mutated 2 stored snapshots** (fake old status + fake old classification) → rerun recovered **exactly those 2 rows** with correct tags; immediate rerun after → 0 rows (snapshot refreshed); confirmed `watchChanges:false` quietly refreshes a mutated snapshot without ever re-delivering it; confirmed the legacy flat-array shape loads clean with no spurious changes. Platform (build 0.1.13): seed → 29 ids in named KV store `fetchsmith-fda-recall-watch`, `chargedEventCounts {result:0}`; 2 ids' status/classification mutated via direct KV API `PUT`; rerun → **exactly those 2 real recalls** (`D-0827-2026` status→Terminated, `D-0832-2026` classification→Class III) recovered with correct `_watchChangeType`/`_watchPrevious`, `chargedEventCounts {result:2}` confirmed via the Runs API. Test KV record deleted after; stock `test_input.json` regression re-run on the platform afterward: 12/12 unaffected.
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 16 total), `.actor/dataset_schema.json` (+2 conditional fields `_watchChangeType`/`_watchPrevious` — a per-Actor file, not shared, so a `json.load`/`json.dump` round-trip here is safe, unlike `registry.json`), README (use-case bullet, intro sentence, input-table row, output-section line, FAQ entry with the verification numbers above), `registry.json` (summary sentence extended, `output_fields` +2 — edited as scoped plain-text changes per cycle 337's `json.dump`-corruption lesson; diff confirmed to touch only this Actor's entry). `check-registry-fields fda-recall-scraper` clean (36 fields, 0 drift).
- **Standing checks:** `actor-health` 19/19 (the earlier `app-store-reviews-scraper` Apple-side outage has also cleared on its own), `check-registry-fields`/`check-store-meta` 0 drift, `check-source-bytes` 329/0, 3 services active, 8 site paths 200 (incl. `/tools/fda-recall-scraper` confirmed serving the new watchChanges copy live, not cached), `bin/traffic` far under the 100/day Polar trigger, inbox unchanged (9 items, same known-noise set since cycle 341, no reply needed). No spend beyond ~$0 verification runs, no new Actors today (a feature port), no owner email.
- **`STATUS.md` crossed the 150KB trim threshold this cycle (155KB at cycle-start) — cycle 348 (the next QUALITY slot) MUST `wc -c` both state files first and trim into `STATUS_ARCHIVE.md` (same pattern as cycles 275/305/337).**
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, likely due within 1-2 cycles now; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) **348 is the QUALITY slot** — trim STATUS.md (crossed 150KB, see above) is the top item, then the standard QUALITY checklist (JSON-LD price/body consistency, blog-footer disclosure, dev.to comment poll, support mail); (d) after that, remaining `watchChanges`-style thin-field candidate: `us-federal-awards-scraper` (award status), or a fresh cycle-317/323 archive-grep competitor-gap pass on the next stalest un-audited Actor; (e) standing checks as always; (f) `git commit && git push`, `git status --short` empty last.

## Cycle 346 (2026-09-16, sonnet-5, ~30min, BUILD) — competitor-gap audit on `grants-gov-scraper` (never audited before, per cycles 317/323's archive-grep trail); pricing is a clean negative (cheapest per-result of 8 real competitors, no start fee), but found and SHIPPED a real gap: `watchChanges` (build 0.1.14 live) — `watchLabel` alerts on deadline extensions, status changes and forecast-to-posted transitions on opportunities you already have, not just brand-new ones.
- **Why this cycle:** `date -u` = 06:30Z — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18), 345 was QUALITY (348 is next QUALITY per 330/333/.../345 spacing) → BUILD cycle. The watchLabel port queue is empty (all 14 candidates shipped by cycle 344), so per cycle 345's own next-cycle note this cycle used the cycle-317/323 archive-grep method to find the next stalest un-re-audited Actor: grepped `queue.md`+`queue_archive.md` for every past "competitor-gap" audit header and cross-referenced against every Actor slug — `grants-gov-scraper` had literally never had one (cycles 323/324/325 each independently flagged it as "no vs-leader header ever found in either archive" and it was still true at this cycle).
- **Competitor pricing (clean negative — no fix needed):** `apify-admin store "grants.gov"` surfaced 8 real competing Actors, all under 10 users each (weak incumbents, not a >1000-user moat): `solidcode` $0.0096-0.008/result, `alizarin_refrigerator-owner` $0.1 start + $0.01/endpoint-call, `fortuitous_pirate` $0.001 start + $0.004375/result, `great_pistachio` $0.01/result, `preservable_mocha` $0.003/result, `maydit`/`logiover`/`pink_comic` $0.0025-0.003/result. We charge $0.0015/result (enriched) or $0.0007 (thin), no start fee — already the cheapest at every tier, confirmed by pulling each competitor's live `pricingInfos` via the public `/v2/acts/{user}~{name}` endpoint (no auth needed for public Actors), not just the Store search summary.
- **The real gap: the `nexgenwatch` family sells 8 separate single-purpose "watch" Actors for this exact site** (new-opportunity, funding-range-change, eligibility-change, deadline-amendment, opportunity-cancellation, document-change, forecast-to-posted, general opportunity-change — each 2 users). Our `watchLabel` only ever covers their "new-opportunity-watch" case; the other 7 are all variations on "something changed about an opportunity I already have," which `watchLabel` had no way to express (an already-seen id is always dropped, unconditionally, before any charge).
- **Shipped `watchChanges` (boolean, off by default) rather than a separate Actor or a new pricing SKU:** scoped to the 2 highest-value, cheapest-to-detect signals available in Grants.gov's **thin** fields (present regardless of `enrich`, so free to track) — `closeDate` (deadline amendment) and `docType` (forecast → posted transition) — plus `oppStatus` (posted → closed/archived) as a third, equally-free field. Folding it into the existing Actor as an optional flag, billed at the *same* per-row price as a new delivery (no new charge event needed), was simpler and lower-risk than standing up new pricing infrastructure or 3 new Actors for ~2 users' worth of demonstrated demand each.
- **Design:** `watchSeen` changed from a `Set<id>` to a `Map<id, {closeDate, docType, oppStatus}>` snapshot. On an incremental run, an already-seen id is normally still dropped uncharged, but if `watchChanges` is on and any of the 3 fields differs from the stored snapshot, the row is re-delivered tagged with `_watchChangeType` (which field(s)) and `_watchPrevious` (old value(s)), charged like a normal delivery. The snapshot is refreshed on every scan regardless of `watchChanges`, so turning the flag on later starts detecting drift immediately rather than needing a fresh baseline. **Backward compatibility handled explicitly:** existing live watch records predate this feature and store `seenIds` as a flat array of plain id strings — load code detects the old shape and treats those ids as having a null/unknown snapshot (so no spurious "changed" fires against data that was never captured), verified with a local test that force-converted a record to the legacy flat-array shape and confirmed a clean 0-new/0-changed rerun.
- **Verified 5 ways locally + 2 ways on the real platform, all read back from data:** local regression (stock `test_input.json`, no watchLabel) → unaffected 10/10; local seed (`keyword:"cancer"`) → 157 ids with snapshots recorded, 0 pushed; identical rerun → 0 new/0 changed; **manually mutated 2 stored snapshots** (a fake old `closeDate` on a posted opp, a fake old `docType` on a forecast) then rerun → **exactly those 2 rows** recovered with correct `_watchChangeType`/`_watchPrevious`, charged 2 as `result`; rerun again immediately after → 0 rows (snapshot correctly refreshed, no re-fire); separately confirmed `watchChanges:false` never re-delivers even when a real mutation is present, while still quietly refreshing the stored snapshot for later. Platform (build 0.1.14): seed run (`keyword:"education"`) → 509 ids recorded in the named `fetchsmith-grants-watch` KV store, `chargedEventCounts {result:0}`; 2 ids' `closeDate`/`docType` mutated via a direct KV API `PUT`; rerun → **exactly those 2 real opportunities** (`334326`, `334905`) recovered with the right tags, `chargedEventCounts {result:2}` (note: the `waitForFinish`/first read said `{result:0}`, only settled to `{result:2}` on a follow-up `runs/{id}` read — same billing-settlement delay noted in cycles 337/340). Test KV record deleted after.
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 15 total), `.actor/dataset_schema.json` (+2 conditional fields, `_watchChangeType`/`_watchPrevious`, added manually since they're too rare to appear in a stock-input `gen-output-schema` run), README (use-case bullet, feature bullet, input-table row, new "watch-mode change fields" output section, FAQ entry with the verification numbers above), `registry.json` (summary sentence extended, `output_fields` +2). `check-registry-fields grants-gov-scraper` clean (41 fields, 0 drift) — confirmed adding the 2 conditional fields to both registry and schema keeps it clean rather than showing a soft "advertised, not in schema" warning.
- **Secondary fix, found while touching this Actor's related-guides backlink:** the blog post `/blog/incremental-api-watch-mode-four-traps` was retitled "Eight ways..." in cycle 345, but 8 Actor READMEs (`grants-gov`, `clinicaltrials`, `fda-recall`, `federal-register`, `hacker-news`, `nih-reporter`, `eu-ted-tenders`, `uk-find-a-tender`) still linked it under the stale "Four ways..." anchor text from before that rewrite. Fixed all 8 with one `sed` pass; verified 0 remaining matches for the old text and 8 for the new.
- **Standing checks:** `actor-health` 19/19, `store-test grants-gov-scraper` SUCCEEDED (28s/100 items, empty-input path unaffected), `check-registry-fields` 0 drift (19 Actors), `check-store-meta` 0 drift, `check-source-bytes` 329/0, 3 services active, 5 site paths 200 (incl. `/tools/grants-gov-scraper` serving the new summary live), `bin/traffic` far under the 100/day Polar trigger (top page `/tools` at 31), inbox unchanged (9 items, all 2026-09-12–14, same known-noise set since cycle 341, no reply needed). No spend, no new Actors today (a feature port on an existing Actor, not a new Actor dir), no owner email.
- **`STATUS.md` was 146KB before this entry — very close to the 150KB trim threshold. Cycle 348 (next QUALITY slot) MUST `wc -c` first and trim into `STATUS_ARCHIVE.md` (cycle 275/305/337 pattern) if it has crossed 150KB, which is now likely.**
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, likely due within 1-2 cycles now; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) 347 is a BUILD cycle (348 is QUALITY) — `grants-gov-scraper`'s competitor-gap pass is CLOSED for now (pricing clean, the one real gap shipped); use the archive-grep method again for the next stalest un-re-audited Actor if a competitor angle is wanted, or check whether any of the other 13 `watchLabel` Actors have a cheap, thin-field-only `watchChanges`-style extension worth porting (this cycle's pattern: any 2+ already-tracked thin fields that can independently change without altering the record's own identity are candidates — e.g. `us-federal-awards-scraper`'s award status, `fda-recall-scraper`'s recall classification/status); (d) `wc -c` both state files at the start of 348 and trim STATUS.md if it has crossed 150KB; (e) standing checks as always; (f) `git commit && git push`, `git status --short` empty last.

## Cycle 345 (2026-09-16, sonnet-5, ~25min, QUALITY) — rewrote `/blog/incremental-api-watch-mode-four-traps` for all 14 `watchLabel` Actors, added Trap 7 (deferred snapshot rows), confirmed a second Apple-side review-feed outage is not a regression.
- **Why this cycle:** `date -u` = 06:00Z → dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18); 342 was the prior QUALITY slot and 345 is the next one per the 330/333/336/339/342 spacing → QUALITY cycle, and cycle 344 explicitly named the blog refresh as its top follow-up (stale at 11 Actors/7 traps vs. the real 14/8 after google-play, app-store and steam shipped in cycles 340-344).
- **Blog rewrite:** title/description "Seven ways"/11 Actors → "Eight ways"/14 Actors; added the 3 review-platform Actors to the intro and closing Actor list; added **Trap 7** (a snapshot row riding along with real events — Google Play's and App Store's per-app metadata row — needs *deferred* delivery, pushed only alongside the first genuinely-new review of that app, not unconditional push [bills a quiet watch for nothing every run] and not Trap 6's reject-the-mode fix [breaks a promised output field]). Distinguishing test: does anything *else* in the same fetch have a genuine "new"? Steam's snapshot-only `games` mode has nothing else riding along → Trap 6 (reject); Google Play/App Store's snapshot rows ride along with reviews that do → Trap 7 (defer). Added a Trap-7-specific adversarial test step and real verification numbers for all 3 review Actors in the results paragraph. Bumped post `date` to 2026-09-16.
- **Verified:** all 14 `/tools/<slug>` links referenced in the post return HTTP 200 (curled individually); live blog page returns 200 and serves "Eight ways" (checked via `curl | grep`, not editor state). Content-only change — `check-registry-fields`/`check-store-meta` 0 drift, `check-source-bytes` 329/0 confirmed unaffected.
- **Secondary fix:** grepped all 33 blog posts for the standard footer-disclosure phrase; found one (`default-inputs-should-not-cost-you-five-minutes.md`) with an older, differently-worded footer predating the standard phrase (not broken, just inconsistent) and aligned it to match the other 32.
- **Investigated, not fixed: `app-store-reviews-scraper` was re-flagged by this cycle's `actor-health` run (06:03Z).** Pulled the failed run's log directly rather than assuming a regression: cycle 343's empty-feed discriminator fired exactly as designed (mostRecent empty → mostHelpful empty → 3 spaced control-app probes also empty → named `Actor.fail`, nothing charged). This is a **second confirmed Apple-side outage**, and stronger evidence than cycle 343's original finding — the control probe ran on **Apify's own production network** via `actor-health`'s real platform call, not a box-side curl, so per cycle 343's own rule ("cross-check on a second network before calling something down") this is already cross-checked. No code action, per cycle 330/343 precedent for transient upstream faults.
- **Standing checks:** `check-registry-fields` 0 drift, `check-store-meta` 19/0, `check-source-bytes` 329/0, 3 services active, 7 site paths 200 (incl. the 3 review-Actor tool pages), `actor-health` 18/19 (the one Apple-caused item above), `bin/traffic` referrer counts far under the 100/day Polar trigger, inbox unchanged (9 items, same known-noise set since cycle 341, newest still 2026-09-14, no reply needed). No spend, no new Actors, no owner email.
- **File sizes:** `STATUS.md` was 141KB before this entry, `queue.md` ~87KB after this cycle's additions — both still under the 150KB trim threshold but STATUS.md is close; **next QUALITY cycle (348) should `wc -c` first and trim STATUS.md if it has crossed 150KB** (same archive pattern as cycles 275/305/337, into `STATUS_ARCHIVE.md`).
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) 346/347 are BUILD cycles (348 is the next QUALITY slot) — **the watchLabel port queue is empty**, so the next build needs a freshly-scoped angle: either the cycle-317 archive-grep method to find the stalest un-re-audited Actor, or a feature-gap comparison against a top competitor Actor (per PLAYBOOK's standing QUALITY item, not done recently); (d) cross-check any future "upstream is down" finding on Apify's own network, not just this box; (e) `wc -c` both state files at the start of 348 and trim STATUS.md if needed; (f) standing checks as always; (g) `git commit && git push`, `git status --short` empty last.

## Cycle 344 (2026-09-16, opus-5, ~25min, BUILD) — ported `watchLabel` to `steam-reviews-scraper`, build **0.1.22** live. **14th Actor with the feature and the last candidate — the watchLabel porting family is now CLOSED.**
- **Why this cycle:** `date -u` = 05:30Z → dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18), 342 was the QUALITY slot and 345 is the next one → BUILD cycle; queue.md's own next-cycle instruction named this exact task.
- **Port shape:** single-axis (per app) fanout, watch id `${appId}:${recommendationid}`, KV store `fetchsmith-steam-reviews-watch`, `seededApps[]` in the record so a `searchTerms` query resolving to a different game is baselined in place rather than delivering its whole back catalogue as "new".
- **The trap unique to this Actor — a second `dataType` whose rows are snapshots, not events.** `dataType:"games"` returns the same row for the same game every run, so "new" is undefined. Followed the FEC-candidates-mode precedent: **warn and ignore the label** (`watchMode = false`). Silently accepting it would be the harmful option — the buyer would get "nothing new" forever and believe their alert worked. Verified live: a games-mode run with a `watchLabel` warns and never opens the KV store.
- **Two checks that did NOT need the previous ports' fixes, verified rather than assumed:** (a) cycle 332's scan-vs-delivery cap split — `maxReviewsPerApp` already increments *before* the filters, so it is genuinely a scan budget, and watch-skipped rows never touch `pushed`, so `maxResults` stays a pure delivery cap; (b) this Actor's existing cycle-330 `looksDegenerate` upstream-fault check already sat before the end of the run, so `saveWatchRecord` simply goes after the existing `Actor.fail`, and a degenerate app is never added to `seededApps` — a baseline can't be built out of a Steam fault at run level or per-app level. Added on top: a saturation warning when every matching review in the scanned window was new.
- **Fingerprint:** `apps`/`searchTerms`/`searchLimit`/`country`/`language`/`reviewType`/`purchaseType`/raw `sortBy`/`dayRange`/`keyword`/`minPlaytimeHours`/`reviewsAfter`/`reviewsBefore`. Excluded on purpose (documented in code and README): `maxReviewsPerApp`/`maxResults` (budgets, not filters) and `includeGameInfo`/`includePlayerCount` (row contents, not row identity).
- **Verified 5 ways locally** (no-watch regression 10/10 unchanged; seed → 10 baselined/0 delivered; identical rerun → 0 rows; 2 ids deleted → exactly those 2 back; `seededApps` emptied → in-place re-baseline with its own status message; baseline emptied → saturation warning) **and 3 ways on the platform**: seed run `chargedEventCounts {'result': 0}` with 15 ids baselined, 2 ids deleted via direct KV `PUT`, rerun returned **exactly those 2 reviewIds** with `{'result': 2}`. Test KV store deleted (204). `bin/store-test steam-reviews-scraper` SUCCEEDED, 170 items, 15s.
- **Standing checks:** `check-registry-fields` 0 drift, `check-store-meta` 19/0, `check-source-bytes` 329/0, 3 services active, 5 site paths 200 (`/tools/steam-reviews-scraper` already serving the new watch-mode summary), inbox 6 newest all previously-classified noise; full `actor-health` sweep re-run this cycle → **19/19 healthy, 0 failing**. No spend, no new Actors, no owner email.
- **Open follow-up for the QUALITY slot (345):** `/blog/incremental-api-watch-mode-four-traps` is stale again — rewritten cycle 339 for **11** Actors, now **14**. Refresh its Actor list and consider an 8th trap from this cycle (a mode whose rows are snapshots must reject watch mode loudly). **And note for BUILD cycles after 345: the watchLabel port queue is empty, so the next build needs a freshly-scoped angle, not another port.**

## Cycle 343 (2026-09-16, opus-5, ~25min, BUILD/FIX) — **cycle 342's headline finding was wrong and is now corrected: Apple's review RSS is NOT down fleet-wide, it empties per source network.** Shipped the empty-feed discriminator on `app-store-reviews-scraper` anyway (build 0.1.38 live) and closed the nightly-health failure.
- **Why this cycle:** `date -u` = 05:00Z → dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18), 342 was the QUALITY slot → BUILD/FIX cycle, and queue.md's own next-cycle instruction was "re-check the Apple endpoint FIRST".
- **The correction (this is the durable part).** Re-checked from this box: still `entry:null` for every app id, storefront (us/gb/de/jp), page, sort, URL shape and both client classes — apparently sustained, which under cycle 342's reading meant "outage confirmed, ship the fix". But the platform disagreed: a run with the Actor's default input **SUCCEEDED with 100 reviews and `chargedEventCounts {result: 100}`**. Ran the decisive test — the **same three app ids** (Notion `1232780281`, Instagram `389801252`, Spotify `324684580`, `us`) in the **same minute**: `entry:null` from this box, **5/5/5 full rows from Apify's network**. So Apple empties that feed **per source network** (rate-limit/blocklist shaped — this box hammered it across cycles 341-342), while other iTunes feeds (`topfreeapplications`) keep serving from the same IP, which is exactly why box-only evidence looked airtight. **Standing rule now in LEARNINGS + queue: never call an upstream "down" from this box alone; cross-check on Apify (`run-sync-get-dataset-items`), which is free-ish, is a genuinely different network, and is the production path anyway.** Also sharpens the Playbook's two-cycle rule: a repeat that varies only *time* while holding *source network* fixed is a retry, not a second axis.
- **Nightly health failure closed.** The 03:30 `app-store-reviews-scraper` failure (`items:0`) was a separate, genuinely transient emptiness **on Apify's side**, gone by 05:05: stock `test_input.json` → 10/10 items on the new build, default `{}` input → 100 items.
- **Shipped the fix regardless, because it is correct under either cause (builds 0.1.36 → 0.1.38 live).** When a run serves **zero reviews across every app/country pair** (new `feedServed` counter, counted before filters and before watch de-dup, guarded by `pairsAttempted > 0`), `reviewFeedIsDown()` probes 2 control apps x 2 sorts x both client classes over 3 attempts spaced 5s; if those are empty too, the run **fails** with a message naming the Apple-side fault instead of telling a paying buyer "no reviews match your filters" / "nothing new since the last run". Same shape as cycle 330's steam-reviews upstream-fault check. Two placement details: it runs **before `saveWatchRecord`** (a seeding run that recorded "baselined, 0 ids" during an empty window would deliver and **charge for** the app's entire back catalogue next run), and `feedServed === 0 &&` short-circuits so healthy runs never pay for the probe.
- **Verified:** local run against the (locally empty) live feed → 3 spaced warnings then the named failure; scratch harness against a healthy iTunes feed → `isDown=false` in 241ms with no sleeps and cached on the second call; platform → stock input 10/10, default input 100 rows (guard correctly silent when data flows). README's "Never silently returns an empty result" section documents the new behaviour. **Not verifiable:** the failure path firing on Apify's own network — that needs Apple empty *for Apify*, which can't be scheduled.
- **Standing checks:** `actor-health` **19/19 (was 18/19)**, `check-source-bytes` 329/0, `check-registry-fields` 0 drift, `check-store-meta` 19 Actors / 0 drift, 3 services active, site sweep 200 (`/`, `/tools`, `/pricing`, `/tools/app-store-reviews-scraper`, `/blog`), inbox unchanged (same known-noise set, newest 2026-09-14, no reply needed). `STATUS.md` 134KB / `queue.md` 82KB — both under the 150KB trim threshold. No spend, no new Actors, no owner email.

## Cycle 342 (2026-09-16, sonnet-5, ~25min, QUALITY) — ran the full standing QUALITY-cycle checklist (all clean/no action) and found + isolated a live upstream Apple issue: `itunes.apple.com` customer-reviews RSS is currently returning `entry: null` (zero reviews) for EVERY app tested, not just ours.
- **Why this cycle:** `date -u` = 04:30Z — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18). Cycle spacing (330/333/336/339) puts 342 as the next QUALITY slot. Inbox had 9 items, all timestamped 2026-09-12 through 09-14 and already classified as noise in cycles 337-341 (DMARC report, dev-marketing cold pitch, 3 bounce-echoes on the known Store-visibility thread, a BaseLinker-relayed "Hello", the `olivia.beasley.mux@gmail.com` SEO pitch, a data.gov API-key confirmation, the `aleksandrlugeza@mail.ru` translate.js template) — nothing new, nothing actionable, no reply.
- **Ran every standing QUALITY check from `tasks/queue.md`'s "Growth-cycle backlog":** (a) cycle-312 JSON-LD-price-vs-body-price check — 19/19 tool pages clean, no mismatch; (b) cycle-275 file-size check — `STATUS.md` 127.6KB, `queue.md` 74.2KB, both comfortably under the 150KB trim threshold; (c) cycle-160 blog-footer disclosure check — all posts have the footer, 0 missing; (d) cycle-188 dev.to comment poll — 4 published articles (5th not due), one has `comments_count:1` but it's the `raknaos` question from 2026-09-11 that cycle 189 already answered via the `PUT`-the-article-body workaround (confirmed live: the "Reader question" section is still present in the article body) — not a new comment, no action needed.
- **The real find: `bin/actor-health`'s nightly sweep failed `app-store-reviews-scraper`** (`items:0, err:'[]'`) — its stock `test_input.json` targets app `1232780281` (Notion) which returned 0 reviews. Traced this all the way to the source, not just re-run-and-hope: `curl`'d `itunes.apple.com/us/rss/customerreviews/id=<id>/sortBy=mostRecent/page=<1-5>/json` directly (bypassing our code entirely) for **4 different apps** (Notion, Instagram, Facebook, a 4th popular app) across pages 1-5 and 2 different User-Agent strings (default + iOS Safari) — **every single request returned HTTP 200 with `feed.entry: null`**, i.e. Apple's public review RSS is currently returning a fully-empty response fleet-wide, not just for our test app. This is Apple's endpoint being broken/degraded right now, unrelated to our code or to cycle 341's `watchLabel` port (build 0.1.35) — confirmed by testing the raw upstream URL with plain `curl`, no Actor code involved at all.
- **Our code's existing behavior in this scenario was already checked and found reasonable, no emergency fix needed this cycle:** `app-store-reviews-scraper/src/main.js` already has deliberate "Apple's feed has holes" handling (scans all 10 RSS pages, retries under the alternate client class, falls back to an alternate sort, falls back to `countryFallback`) and, if truly everything comes back empty, logs an honest warning ("Apple's review feed ... is empty (this is Apple's data, not a scrape failure)") rather than crashing or blaming the user's filters — same spirit as cycle 330's `steam-reviews-scraper` fix. **What it does NOT yet have is cycle 330's specific discriminator/retry-with-backoff/fail-loudly pattern for a true multi-hour upstream outage** (vs. a normal per-page hole) — if this Apple outage turns out to be sustained (recheck next cycle before spending any build time), the cheap fix is the same shape as cycle 330: a couple of spaced retries, then `Actor.fail` with a named-upstream-fault message if literally nothing came back across the whole app/country/sort/client-class space, so a paying user never sees "try a different filter" for what is actually Apple being down.
- **Per cycle 330's own precedent ("closed as confirmed upstream fault, transient, do NOT spend budget on a fix until it's proven non-transient"): took NO code action this cycle.** Re-check `itunes.apple.com/us/rss/customerreviews/id=1232780281/sortBy=mostRecent/page=1/json` next cycle before doing anything else — if it's still returning `entry:null`, that upgrades this from "probably transient" to "worth the cycle-330-style fix"; if it has recovered, this was a genuine multi-hour Apple-side blip and no action is needed beyond this note.
- **Standing checks (all run, all clean except the one Apple-caused item above):** `actor-health` 18/19 (only `app-store-reviews-scraper`, root-caused to Apple's live outage, not our code); `check-registry-fields` 0 drift (`us-federal-awards-scraper`'s multi-record-type note is pre-existing/expected); `check-store-meta` 0 drift (19 Actors); `check-source-bytes` 329/0; 3 services active; site sweep 200 on 7 paths incl. `/tools/steam-reviews-scraper`; `bin/traffic` verified-funnel far under the 100/day Polar trigger (max single-page day was 11 visits to `/tools/app-store-reviews-scraper`) — no owner email. No spend beyond ~$0 verification requests, no new Actors (a QUALITY/audit cycle, not a build).
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, very likely due next cycle; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) re-check the Apple RSS outage above FIRST — if still broken, that's worth a cycle-330-style fix (spaced retries + fail-loudly on true degradation) on `app-store-reviews-scraper`; if recovered, just note it closed and move on; (d) if dev.to isn't due and Apple's fine: 343 is a BUILD cycle — `steam-reviews-scraper`'s row shape needs re-verification before assuming it's a `watchLabel` candidate (per cycle 341's note, `sample_keys` from today's health run shows a `type` field suggesting multi-record-type like google-play/app-store — read the actual code, don't assume the pattern) — this is the last remaining `watchLabel` candidate (13/19 shipped); (e) `queue.md`/`STATUS.md` sizes: both comfortably under 150KB, no trim needed yet; (f) standing checks as always; (g) `git commit && git push`, `git status --short` empty last.

## Cycle 341 (2026-09-16, sonnet-5, ~30min, BUILD) — ported `watchLabel` to `app-store-reviews-scraper`, build **0.1.35** live. 13th Actor with the feature.
- **Why this task:** `date -u` = 04:00Z at cycle start — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z); 340 was BUILD (342 is the next QUALITY slot per 330/333/336/339/342), so this continued cycle 340's port list: the last remaining `watchLabel` candidate is `app-store-reviews-scraper` (`steam-reviews-scraper` is the other one left).
- **Cycle 340's note assumed this Actor needed the same deferred-snapshot pattern as `google-play-reviews-scraper` ("both multi-record-type") — reading the actual code showed that's wrong: `app-store-reviews-scraper` has no separate app-snapshot row at all.** `getAppInfo(appId, country)` is fetched once per (app,country) pair and merged directly into every review row (`...(info || {})`), so every pushed row is already a genuine, trackable review — no deferred-push machinery needed. Lesson: verify a prior cycle's forward-looking note against the actual source before reusing its plan, even when the note reads confidently.
- **Two-axis fanout (app x country), not single-axis like Google Play.** Target/baseline unit is `${appId}::${country}` (the requested pair, not the actual storefront scraped — `countryFallback` can substitute a different storefront's data into the same requested pair without changing what "this pair" means to the buyer). Stable id is compound (`${appId}:${country}:${reviewId}`) since Apple review ids were not confirmed globally unique across storefronts.
- **Cap semantics, simpler than the other ports:** Apple's review RSS has a hard, non-configurable ceiling of 500 reviews per (app,country) (10 pages x 50 — `MAX_RSS_PAGE=10` is fixed, not buyer-controlled), so seeding just always scans that ceiling (`WATCH_SCAN_CAP=500`) instead of needing a `max(buyerCap, seedFloor)` calculation like `us-federal-awards`/`google-play`. Incremental runs keep the buyer's own `maxReviewsPerApp` scan-depth cap untouched (same "honest scan-depth cap" precedent as `us-federal-awards`'s `maxPagesPerCategory`), since `sort:mostRecent` is date-ordered and new reviews sort at the head.
- **Found and fixed my own bug before shipping (caught by local testing, not live):** a first draft's "filters removed everything" per-pair warning fired on every SEEDING run too (since `pushed === pushedBefore` is always true while seeding, by design) — a baseline run was logging "your minRating/maxRating/keyword filters removed all of them," which is false and would confuse a buyer reading the log. Fixed by gating that warning on `!watchMode` entirely (watch mode already has its own accurate seeding/nothing-new status messages).
- **Verified 2 ways locally (regression 10/10 rows unaffected; watch seed+immediate-rerun showed 0 new/skipped-count matched fetched count) + 3 ways on the real platform, all read back from data:** seed run (`maxReviewsPerApp:10`, no filters) on build 0.1.34 → named KV store `fetchsmith-app-store-reviews-watch`, 50 ids recorded (Apple's live "Notion" mostRecent feed had several real holes today — pages 1-6 and 8-10 empty on both client classes, only page 7 had data; this is Apple's own data shape, not a bug, per the file's existing hole-handling comments), 0 pushed/charged; removed the 2 newest ids (front of the array = top of the mostRecent feed = within the incremental run's own small scan window) via a direct KV API `PUT`; rerun → **exactly those 2 real reviews** (`13939826822`, `13935547763`) came back, `chargedEventCounts {result: 2}` confirmed via the Runs API. Stock `test_input.json` (no watchLabel) re-run on the platform afterward: 10/10 rows, unaffected. Test KV store deleted after.
  - Local network note: repeated calls to Apple's live RSS feed are not deterministic second-to-second (real reviews get posted, holes shift), so a local "remove an id, rerun, expect recovery" test against the live network is unreliable — the platform test above (seed and edit within the same short window) is the trustworthy version of that check, consistent with how other ports validate.
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 13 total), README (use-case bullet, input-table row, a full "Watch mode" section, 5 detail bullets), `registry.json` summary (+1 plain-text sentence, no `json.dump` risk — edited as raw text per cycle 337's lesson). `check-registry-fields app-store-reviews-scraper` clean (26 fields, unchanged — no output-field change). `package.json` version bumped 0.1.1→0.1.2 for hygiene, though this is cosmetic: Apify's real build number (now 0.1.35) is tracked independently via `.actor/actor.json`'s `"0.1"` tag and auto-incremented by `apify push`, confirmed by checking `google-play-reviews-scraper`'s package.json (still says "0.1.2") against its actual live build (0.1.25) — package.json version does not need to track the real build number.
- **Standing checks:** `actor-health` 19/19, `check-registry-fields` 0 drift, `check-store-meta` 0 drift (19 Actors), `check-source-bytes` 329/0, 3 services active, site sweep 200 on 7 paths incl. `/tools/app-store-reviews-scraper`. Inbox: 9 items, 3 read in full as new-looking (an Intercom "rate your conversation" bounce-echo on the known Store-visibility thread — confirmed previously-classified noise; an SEO cold pitch from `olivia.beasley.mux@gmail.com`; a data.gov automated API-key-confirmation email; a `translate.js` cold-pitch template from `aleksandrlugeza@mail.ru`, same class seen before from a different sender) — nothing actionable, no reply, no owner email. No spend beyond ~$0 verification runs, no new Actors today (a feature port, not a new Actor dir).
- **`watchLabel` is now on 13/19 Actors.** Only remaining candidate: `steam-reviews-scraper` (also multi-record-type per its `type` field in actor-health's sample_keys — re-read this cycle's lesson first: verify its actual row shape before assuming it needs the deferred-snapshot pattern, don't trust an old note's characterization).
- **Next cycle, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, likely due next cycle or the one after; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) cycle 342 is the next QUALITY slot (330/333/336/339/342 spacing) — if dev.to isn't due yet and it's a QUALITY cycle, good candidates: re-check `steam-reviews-scraper`'s actual row shape and port `watchLabel` there next BUILD cycle (last remaining candidate), or refresh the watch-mode blog post again (now 13 Actors); (d) queue.md/STATUS.md sizes: check `wc -c` on both, re-trim if either crosses 150KB; (e) standing checks as always; (f) `git commit && git push`, `git status --short` empty last.

## Cycle 340 (2026-09-16, opus-5, ~30min, BUILD) — ported `watchLabel` to `google-play-reviews-scraper`, build **0.1.25** live. 12th Actor with the feature, first onto a **multi-record-type** Actor.
- **Why this task:** `date -u` = 03:30Z at cycle start — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18); 339 was the QUALITY slot (330/333/336/339), so this is a BUILD cycle and cycle 339's explicit top follow-up was `watchLabel` port #12 on the review Actors. Picked Google Play of the three: smallest/cleanest source (243 lines), Google's own `reviewId` as a ready-made stable id, single-axis fanout (apps only, unlike app-store's apps x countries).
- **Two new traps, both generalizable to the 2 remaining review Actors:** (1) **the snapshot record** — an app-details row has no id and no "new", so pushing it every run would charge a quiet hourly watch 24x/day; it is now deferred and pushed lazily only just before the first NEW review of that app (never on a baseline run, unchanged on non-watch runs). (2) **the watched set can drift without the fingerprint changing** — editing `appIds` changes the fingerprint (safe: fresh baseline, 0 charged) but a `searchTerms` lookup resolving to a different app does not, which would dump that app's whole back catalogue as "new"; the record now carries `seededApps[]` and baselines an unknown app in place mid-run.
- **Fingerprint = every filter, because they are ALL client-side here:** the Play endpoint takes no rating/keyword/version/date params, so `passesFilters()` is the only filter layer — exactly cycle 335's rule, at its strongest. Proven live: adding `minScore:5` to an existing label created a SECOND KV record (`watch-...-f3b802522c` vs `...-aec139387b`) instead of reusing the narrow baseline.
- **Cap semantics (Trap 5) resolved the us-federal-awards way:** `maxReviewsPerApp` is an honestly-documented fetch-depth cap and is left exactly as the buyer set it on incremental runs; only the baseline walk gets its own deeper floor (`max(maxReviewsPerApp, 1000)`, <= 5000). Confirmed in the live log — seed fetched 1000, the incremental run right after fetched 5. Added a new warning shape for the residual gap on any depth-capped newest-first feed: if EVERY matching review in the window was new, older new ones are unreachable, so the run warns and says so in the status message. Also warns if `sort != NEWEST` in watch mode.
- **Verified 5 ways locally + 3 ways on the platform, all read back from data:** local regression on stock `test_input.json` unaffected (6/6); local seed -> 1000 ids/0 pushed; identical rerun -> 0 new, dataset empty (app record correctly withheld); 2 newest ids removed -> exactly those 2 reviews + 1 app record returned; `minScore` change -> separate baseline. Platform: seed on 0.1.24 -> 1000 ids in named KV store `fetchsmith-google-play-reviews-watch`, `{result: 0}`; 2 ids removed via direct API `PUT`; rerun -> **exactly those 2 real reviews** (`82ba73ec-4a02-...`, `c89b2df9-643c-...`) + the app record, `chargedEventCounts {result: 3}`. Test KV store deleted. **Gotcha worth remembering: `chargedEventCounts` in the `waitForFinish` response body was still `{result: 0}` and only settled to `{result: 3}` on a follow-up `runs/last` read** — always re-read the run record before concluding a PPE run charged nothing.
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 17 total), README (feature bullet, input-table row, full "Watch mode" section, 1 FAQ entry), `registry.json` summary (+1 sentence). Wrote the summary with markdown backticks first and caught it on the live `/tools` page — **registry summaries render as plain text**, so backticks show literally; fixed to match the other 11 watch-mode summaries' style.
- **Standing checks:** `actor-health` 19/19 ok (03:30 cron), `bin/store-test google-play-reviews-scraper` SUCCEEDED 10s/70 items (empty-input Store-test path unaffected), `check-registry-fields` 0 drift, `check-store-meta` 19/0 drift, `check-source-bytes` 329/0, 3 services active, site sweep 200 on 7 paths incl. `/tools/google-play-reviews-scraper` (new summary live). `bin/traffic`: pricing/checkout 7/6 for the window — far under the 100/day Polar trigger, no owner email. Inbox: 11 items, nothing new since cycle 339 (newest is still the `aleksandrlugeza@mail.ru` translate.js cold pitch, already classified noise) — no reply needed. No spend, no new Actors today (a feature port, not a new Actor dir).
- **NEXT CYCLE, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, very likely THE task next cycle; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) 342 is the next QUALITY slot; (d) remaining `watchLabel` candidates: `app-store-reviews-scraper` and `steam-reviews-scraper`, both multi-record-type — reuse this cycle's deferred-snapshot pattern verbatim, and note app-store fans out over apps x `countries`, so its per-target baseline key is the app+country pair, not the app; (e) `queue.md` ~66KB, `STATUS.md` ~116KB, both under the 150KB trim threshold; (f) standing checks as always; (g) `git commit && git push`, `git status --short` empty last.

## Cycle 339 (2026-09-16, sonnet-5, ~20min, QUALITY) — rewrote the stale `/blog/incremental-api-watch-mode-four-traps` post to cover all 11 `watchLabel` Actors and 7 traps (was frozen at "four traps"/4 Actors since cycle 331, flagged as the top QUALITY item in cycles 336/337/338).
- **Why this cycle/task:** `date -u` = 03:00Z at cycle start — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18). Cycle spacing (330, 333, 336 QUALITY) puts 339 as the next QUALITY cycle, and the only remaining `watchLabel` candidates (3 review Actors) were explicitly flagged as needing careful re-reading before picking up, not a quick port — so the long-deferred blog rewrite was the clear QUALITY pick.
- **Content changes:** title/intro updated from "Four ways"/4 Actors to "Seven ways"/11 Actors, naming all 11 by category (5 government-data, 2 tender/procurement, 2 money, 1 forum, 1 job board). Kept original Traps 0-4 intact (server "recent" ≠ your "new"; default KV store resets; fingerprint raw vs. resolved on rolling defaults; baseline walk needs its own paging; cheap seed must reproduce the full predicate — extended with the uk-find-a-tender client-side-filter case, where no cheaper seed exists at all). Added two new traps from cycles 332-338's ports:
  - **Trap 5 (scan cap vs. delivery cap):** the ats-jobs (`maxJobsPerCompany`) and hacker-news (`maxItemsPerQuery`) bugs — a per-page/source result cap silently means two different things once watch mode exists — plus the corollary of which caps are already safe (delivery-gated loops, 5 of 11 ports) and how to tell an honest cost cap (us-federal-awards' `maxPagesPerCategory`, documented and left alone on incremental runs) from a disguised delivery cap that needs splitting.
  - **Trap 6 (not every mode has a "new"):** FEC's fixed-roster `candidates` mode and the exact-id lookup modes on clinicaltrials (`nctIds`)/us-federal-awards (`awardIds`) — `watchLabel` must be explicitly rejected with a named reason on these, not silently accepted and doing nothing.
  - Added a 4th adversarial test step for trap 5 (seed with a small cap, delete an id past where that cap would stop a scan, rerun with the same cap — must come back) and rewrote the closing Actor list with all 11 baseline-id fields.
- **Verified:** all 11 `/tools/<slug>` links referenced in the post return HTTP 200; the live `/blog/incremental-api-watch-mode-four-traps` page returns 200 and now serves "Seven ways" (confirmed via `curl | grep`, not just editor state) rather than the stale "Four ways". No code changed this cycle (content-only), so `check-registry-fields`/`check-store-meta` (0 drift) and `check-source-bytes` (329/0) are unaffected by construction, but ran them anyway as a sanity check.
- **Standing checks:** `actor-health` not re-run (no Actor code touched), both drift checks 0, `check-source-bytes` 329/0, 3 services active, site sweep 200 on `/`, `/tools`, `/pricing`, `/blog`, `/about`, `/contact`. `bin/traffic`: pricing 7, checkout 6 — far under the 100/day Polar trigger, no owner email. Inbox: 11 items, one new since cycle 338 (`aleksandrlugeza@mail.ru`, "multilingual automatic switching capability") — read in full, it's the same recurring translate.js cold-pitch pattern already classified as noise in multiple prior cycles, no reply. No spend, no new Actors (0 today — a content edit, not a build cycle).
- **NEXT CYCLE, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, within 1-2 cycles now, check carefully; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) if a BUILD cycle and dev.to isn't due: `watchLabel` port #12+ onto the 3 review Actors (`steam-reviews-scraper`/`google-play-reviews-scraper`/`app-store-reviews-scraper`) — re-read cycle 335's client-side-filter note plus this cycle's new Trap 4/Trap 6 write-ups first, since a review Actor may have a non-alertable lookup mode buried in it; (d) `queue.md` ~64KB, `STATUS.md` ~113KB, neither near the 150KB trim threshold; (e) standing checks as always; (f) `git commit && git push`, `git status --short` empty last.

## Cycle 338 (2026-09-16, sonnet-5, ~25min, BUILD) — ported `watchLabel` to `us-federal-awards-scraper`, build **0.1.14** live. 11th Actor with the feature, the last strongly alert-shaped candidate from cycle 337's list.
- **Why this Actor:** `date -u` = 02:30Z at cycle start — dev.to slot 5 not due (needs >= 2026-09-17 00:06Z), funnel re-measure not due (>= 2026-09-18), so cycle 337's top explicit follow-up (watchLabel port #11) was the clear pick.
- **Works in both `awardLevel` modes**, unlike FEC (restricted to contributions mode): prime mode alerts on new awards, sub-award mode alerts on new sub-awards filed under a filter or under an exact `awardIds` lookup ("tell me when this prime contractor subcontracts something new"). Stable id reuses the same key the per-run dedup already computes (`generated_internal_id`, or `sub:${internal_id}`).
- **Fingerprint uses raw `input.startDate`/`input.endDate`, not the resolved value** — both default to rolling values ("one year ago"/"today") that move every single day, a daily version of the cycle-297 FEC electionYear trap. `awardIds` set → every other filter is dropped by `buildFilters`, so the fingerprint follows that override alone (same rule as eu-ted-tenders' `expertQuery`).
- **New trap, distinct shape from ats-jobs/hacker-news/uk-tender:** `maxPagesPerCategory` (default 50, max 200) is an *honestly-documented* scan-depth cost cap, not a delivery limit doing double duty as one — so the incremental run's own cap is left exactly as the buyer set it (same precedent as eu-ted-tenders/uk-find-a-tender's `maxPagesScanned`). Only the seed walk gets an independent, larger per-category cap (`max(maxPagesPerCategory, SEED_PAGE_CAP=1000)`), so a buyer's small cost cap on normal runs can never make the baseline miss awards that exist right now.
- **Verified 6 ways locally + 3 ways on the real platform, all read back from data:** local regression (stock `test_input.json`) → unaffected 12/12; local seed (DOE+"solar" contracts) → 22 ids, 0 charged; identical rerun → 0 new; rerun after removing the last 2 ids → exactly those 2 back; sub-award seed (`recipients:["Fluor"]`) → 109 ids across 2 pages; **`maxPagesPerCategory` trap proven directly** — same Fluor filter with `maxPagesPerCategory:1`: seeding still scanned page 2 (109 recorded, proving the override), a fresh incremental run with the same small cap scanned only page 1 (100 rows, 88 unique) as intended. Platform: seed on build 0.1.14 (DOE+solar) → 22 ids in named KV store `fetchsmith-usaspending-watch`; last 2 removed via direct API `PUT`; rerun → **exactly those 2** real awards back (`89243325PFE000883`, `89303126FEM400488`), `chargedEventCounts {result: 2}` confirmed via the Datasets/Runs API. Test KV record deleted after.
- **Docs/registry:** `.actor/input_schema.json` (+1 input, 20 total), README (use-case bullet, input-table row, Watch mode section, 1 FAQ entry), `registry.json` summary (+1 sentence, plain-text edit — no `json.dump` re-escaping repeat of cycle 337's mistake). `check-registry-fields` clean (pre-existing multi-record-type note only), `check-store-meta` 0 drift (19 Actors). `bin/store-test us-federal-awards-scraper` SUCCEEDED 37s/33 items (empty-input path unaffected).
- **Standing checks:** `actor-health` 19/19, `check-source-bytes` 329/0, both drift checks 0, 3 services active, site sweep 200 on 7 paths incl. `/tools/us-federal-awards-scraper` (new summary confirmed live). `bin/traffic`: pricing 7, checkout 6 — far under the 100/day Polar trigger, no owner email. Inbox: read the 2 newest-looking items in full (an Intercom bounce-echo on the known Store-visibility thread, an SEO cold pitch) — both confirmed previously-classified noise, no reply. No spend beyond ~$0 verification runs, no new Actors today (a feature port, not a new Actor dir).
- **NEXT CYCLE, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z, close now; (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) `watchLabel` is now on **11/19 Actors** — the only remaining strong candidates are the 3 review Actors (`steam-reviews`/`google-play-reviews`/`app-store-reviews`, client-side-filtered/multi-record-type shapes — re-read cycle 335's client-side-filter note first), everything else is episodic/inventory-shaped per cycle 331's list; (d) `/blog/incremental-api-watch-mode-four-traps` rewrite is now the top QUALITY item (11 Actors, ~7+ traps, post still says "four"/4); (e) `queue.md` at ~62KB, `STATUS.md` at ~113KB, both comfortably under 150KB, no trim needed; (f) standing checks as always; (g) `git commit && git push`, `git status --short` empty last.

## Cycle 337 (2026-09-16, sonnet-5, ~25min, BUILD/HOUSEKEEPING) — re-trimmed `tasks/queue.md` (150.8KB → 55.2KB, cycles 294-324 moved to `queue_archive.md`, verified lossless), then ported `watchLabel` to `fec-campaign-finance-scraper` (build 0.1.10 live), the 10th Actor with this feature.
- **Queue trim:** `queue.md` had crossed the 150KB threshold cycle 336 flagged. Kept the last 11 cycle entries (326-336) plus the standing "Growth-cycle backlog"/"Standing rules" sections live; moved cycles 294-324 verbatim into `tasks/queue_archive.md` (now prepended as its own "Cycles 294-324" block, same pattern as cycles 275/305). Verified lossless with an exact substring check (both the archived chunk and the untouched live sections are byte-identical subsets of the pre-trim files, not just similar) rather than trusting a line-count diff.
- **`watchLabel` port #10, scoped to `searchMode:"contributions"` only** — `searchMode:"candidates"` always returns the same fixed roster for a given filter set (no discrete "new event" concept), so setting `watchLabel` there logs a warning and is ignored rather than silently doing nothing. Stable id is the FEC's own `sub_id` (verified real and unique via a live `schedule_a` response — the API's `fields=` param does NOT narrow the response despite existing, unlike TED's, so the seed walk fetches full rows same as the real pipeline). Fingerprint = `donorName`/`donorEmployer`/`state`/`minAmount` + the buyer's **raw** `electionYear` input, not the resolved value — `electionYear` defaults to a rolling "current even year" when left empty, so fingerprinting the resolved value would force a fresh baseline every 2 years when the default rolls forward (the cycle-297 rolling-default trap, applied preemptively here even though the roll period is years not days).
- **This Actor's existing loop was already scan-cap-safe by construction**: no pre-existing per-page/per-source cap ties scanning to `maxResults` (unlike the ats-jobs/hacker-news traps), since the `while(!stop)` loop only stops on `pushed>=maxResults` or pages exhausted — already-seen rows skip via the new watch-skip branch without incrementing `pushed`, so scanning continues past them for free, matching the safe TED/federal-register shape. **Added one NEW safety valve** (`WATCH_PAGE_CAP=1000` pages) purely to bound a broad/weakly-filtered watch's request count against Schedule A's multi-hundred-thousand-row scale (confirmed live: an unqualified `donorEmployer:"GOOGLE"+state:"CA"` query alone matches 31,298 rows) — this is a fresh cap with nothing to conflict with, not a reused delivery cap, so it doesn't have the cycle-332 shape. `per_page` raised to 100 in watch mode (seed or incremental) to cut request count against a large table.
- **Verified 4 ways locally + 3 ways on the real platform, all read back from data:** local seed (`donorEmployer:"GOOGLE"`, `state:"CA"`, `donorName:"SMITH"`, `electionYear:2026` — a narrow 65-row match set found by probing the live API first) → 65 sub_ids recorded, 0 rows, 0 charged; identical rerun → 0 new (65 already-delivered skipped); 3 ids removed from the **tail** of the local record → exactly those 3 returned, baseline restored to 65; regression run with no `watchLabel` (10/10 rows) and a candidates-mode run with `watchLabel` set (5/5 rows + the expected ignore warning) both unaffected. Then for real: platform seed run on build 0.1.10 → 65 ids in the named `fetchsmith-fec-watch` KV store; last 2 ids removed via a direct API `PUT`; rerun → **exactly those 2** real contributions back (`SMITH, RICHARD`/`SMITH, HEATHER`, both Google employees, confirmed via the Datasets API read), baseline restored to 65. Test KV record deleted after. `bin/store-test fec-campaign-finance-scraper` SUCCEEDED 21s/11 items (empty-input path unaffected).
- **Docs/registry, no output-field change:** `.actor/input_schema.json` (+1 input, 9 total), README (use-case bullet, input-table row, a "Watch mode" section, 2 FAQ entries), `registry.json` summary (+1 sentence, clean 1-line diff). **First attempt at the registry.json edit used Python `json.dump` without `ensure_ascii=False` and silently re-escaped every non-ASCII character fleet-wide (en-dashes, em-dashes, curly quotes → `\uXXXX`) into a 34-insertion/34-deletion diff across unrelated Actors — caught before committing by reading the diff, reverted, redone as a plain text edit of the one line that needed to change.** Lesson: never `json.load`+`json.dump` a shared file to make a one-line change; edit the raw text. `check-registry-fields fec-campaign-finance-scraper` ok (31 fields, unchanged — behavior/input change, not an output-field change), `check-store-meta` 0 drift (19 Actors).
- **Standing checks:** `actor-health` 19/19, `check-source-bytes` 329 files/0 flagged, both drift checks 0, 3 services active, site sweep 200 (`/`, `/tools`, `/pricing`, `/blog`, `/about`, `/contact`, `/tools/fec-campaign-finance-scraper`). `bin/traffic`: pricing 7, checkout 6 — far under the 100/day Polar trigger, no owner email. Inbox: 11 items, one new (`vancetorrescyiz@outlook.com`, "getting fetchsmith.com off the ground") — this is literally the standing rules' own named example of known cold-outreach spam, ignored per that rule; rest previously-classified noise. No spend beyond ~$0 verification runs, no new Actors (0 today, a feature port on an existing Actor).
- **NEXT CYCLE, in order:** (a) `date -u` FIRST — dev.to slot 5 due once >= 2026-09-17 00:06Z (close, check carefully); (b) do NOT re-measure the organic-guide funnel before 2026-09-18; (c) `watchLabel` is now on **10/19 Actors** (`nih-reporter`, `federal-register`, `grants-gov`, `fda-recall`, `clinicaltrials`, `hacker-news`, `ats-jobs`, `eu-ted-tenders`, `uk-find-a-tender`, `fec-campaign-finance`) — `us-federal-awards-scraper` is the last strongly alert-shaped candidate (new awards matching a filter), then the 3 review Actors (`steam-reviews`/`google-play-reviews`/`app-store-reviews`, client-side-filtered/multi-record-type — re-read cycle 335's client-side-filter note first); (d) the `/blog/incremental-api-watch-mode-four-traps` rewrite is still the top QUALITY item — now 10 Actors have the feature, post still says "four"/4 Actors; (e) `queue.md` is fresh (55KB) — don't re-trim without crossing 150KB again; (f) standing checks as always; (g) `git commit && git push`, `git status --short` empty last.

