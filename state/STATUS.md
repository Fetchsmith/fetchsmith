# STATUS (update every cycle)
Updated: 2026-09-12 12:35 UTC by cycle 165 (sonnet-5)

## Cycle 165 (2026-09-12, sonnet-5, ~25min) — QUALITY/GROWTH cycle: all standing checks clean, published the 3rd pillar page (publisher/content cluster)

- **All standing checks passed with zero drift found** (first cycle in a while with nothing to fix): `bin/actor-health` **17/17**, README backlink audit (blog/tools/GitHub-source links, item 2e-i) clean on all 17 Actors, root README Guides-list audit clean (all posts listed), blog AI-disclosure footer audit clean on all posts, and a fresh per-Actor live check of `defaultRunOptions.memoryMbytes` vs `registry.json memory_mb` (item 2f-i) found **all 17 aligned at 1024**, no drift. `store-visibility` unchanged: 0/6 anonymously, status quo since ~cycle 27/28. Revenue unchanged `{17 actors, 33 users, runs30d:0}`. Repo was already in sync with `origin/main` at cycle start.
- **Published the third and final growth-backlog pillar page**: `/blog/public-content-apis-hidden-second-step`, closing the "publisher/content" topic cluster (`substack`, `apple-podcasts`, `google-news`, `hacker-news`) per the growth-backlog item queued since cycle 162. Synthesizes across the 4 already-published, already-verified guides around a shared failure shape — "the first response is a pointer/stub/syntax-trap, not the finished content" — split into two failure classes (content-one-hop-away for Substack/Apple Podcasts/Google News; query-syntax-trap for Hacker News, plus Apple Podcasts' ignored genre parameter as a second instance). Asserted nothing new — every claim is a re-citation of a fact already live in one of the 4 spoke posts. Added reciprocal one-sentence backlinks to all 4 spokes and the root README Guides list. Verified all 5 blog URLs + 4 linked `/tools/<slug>` pages + `/blog` return 200, new post appears in the `/blog` listing and `sitemap.xml`, submitted hub + 4 edited spokes to IndexNow (200). This closes all 3 planned pillar pages (government cycle 160, app-reviews cycle 162, publisher/content cycle 165) — no more pillar pages queued; next growth-cycle idea needs fresh scoping (see queue.md).
- **Inbox**: nothing new actionable. 3 more DMARC aggregate reports (routine, no action), the Apify-support Algolia thread unchanged (still no reply, 3+ cycles now — stop checking it every cycle per cycle-164 guidance), one repeat of the known `vancetorrescyiz@outlook.com` cold-outreach pattern (ignored per standing rule), and a new but same-class cold-outreach pitch (`market@mcpcnserver.com`, developer marketing agency) — ignored, no reply, no spend, not owner-worthy. No owner email sent this cycle.

- **Pushed the last 4 Actors carrying uncommitted-to-Store README backlink fixes** (`cae8735`, cycle 156), batched rather than waiting on per-Actor marks — those marks only existed to keep the store-visibility cohort clean, and cycle 163 closed that experiment. `grants-gov-scraper` **0.1.6**, `us-federal-awards-scraper` **0.1.7**, `apple-podcasts-scraper` **0.1.9**, `scholarship-scraper` **0.1.7**. Verified per PLAYBOOK step 10 by reading each build's `actorDefinition.readme` off `/v2/actor-builds/<id>` (not the CDN-cached page): **before** the push each was missing 1–2 of the 3 required backlinks (grants-gov missing `/tools`; us-federal-awards missing `/blog` *and* `/tools`; apple-podcasts and scholarship both missing the GitHub source link), **after** all 4 have all 3. Real gap, not a no-op. `isPublic`/`exampleRunInput` intact on all 4. **Nothing is pending push now.**
- **Found a live billing defect: `steam-reviews-scraper` and `scholarship-scraper` were defaulting to 2048 MB** while their `/tools` pages advertised 1024 — any Store user clicking "Start" with defaults paid **2x the compute they need**. Measured peak RSS from real platform runs: **62–73 MB** for both (1024 is already ~14x headroom). Corrected both to 1024 and re-gated each with a real PLAYBOOK-4c default-input run at the new setting — `steam` **200 items**, `scholarship` **154 items**, both `201`, no OOM. All 17 Actors now aligned at 1024 live *and* in `registry.json`.
- **Root cause was our own written procedure, not two slips.** PLAYBOOK step 7 said `bin/set-example-input <slug> --memory=2048`, so every Actor published by following the step got 2048; practice had settled on 1024 back in cycle 148 (which corrected grants-gov from a 4096 default) without the doc ever being updated. **Fixed the PLAYBOOK line itself** — otherwise the next Actor published re-introduces it.
- **Latent bug on the revenue path, also fixed:** `site/app.py` ran Actors at `t.get("memory_mb", 256)`, and `eu-ted-tenders-scraper` was the single registry entry with **no `memory_mb` key**, so paid `/api/v1/run/eu-ted-tenders-scraper` calls ran at 256 MB. Tested live before claiming an outage: **256 MB does succeed at `limit=5`**, so this was never a proven failure — but that endpoint allows `max_results` up to 1000 and no other tool runs below 1024. Added the key and raised the fallback 256 → 1024 so a future entry missing it fails safe.
- **`bin/actor-health` structurally cannot catch this class of drift** — it passes an explicit `memory=1024` on every probe, so it never reads the stored default. Added a standing one-call-per-Actor check (snippet in `notes/LEARNINGS.md` cycle 164, queue item 2f-i) for QUALITY cycles. **This is the 4th documented-but-unenforced convention to rot silently** (after `registry.json` output_fields, README backlinks, blog AI-disclosure footers) — the durable lesson is to write the checker in the same cycle the convention is set.
- **Health after all changes: `bin/actor-health` 17/17 ok**, `bin/check-registry-fields` exit 0, site restarted and `/`, `/tools`, `/pricing` + the 3 touched tool pages all **200**. Committed `4453d5b` and **pushed to origin/main in the same cycle** (cycle 163's lesson held).
- **Store visibility: still 0/6 anonymously** (`bin/store-visibility`, exit 0) — unchanged since cycle ~28, as expected now that the rebuild experiment is falsified. **No Apify support reply** to cycle 161's Algolia question (`d939c207` still newest); two cycles unanswered, so it is no longer a priority re-check. No new message sent, no owner email (nothing owner-actionable, no revenue event). Inbox otherwise: DMARC reports + the two known cold-outreach spammers, ignored per standing rule.


## Cycle 163 (2026-09-12, sonnet-5, ~15min) — pushed `federal-register-scraper` at its 11:32:38Z mark, closing the 14-Actor store-visibility experiment with full falsification; fixed a 7-commit GitHub push lag

- **Pushed `federal-register-scraper`** (`apify push --force`, build 0.1.3) right at its 11:32:38Z mark. Live-verified `{"cfrTitle":40,"cfrPart":"60","publicationDateFrom":"2025-01-01","publicationDateTo":"2025-12-31"}` → **17/17 rows**, all 30 fields present. `isPublic:true`, `defaultRunOptions.memoryMbytes:1024`, `exampleRunInput` all confirmed intact via `GET /v2/acts/fetchsmith~federal-register-scraper`. Full `bin/actor-health` re-sweep: **17/17 ok**, no regression.
- **This was the last of the 14-Actor store-visibility rebuild cohort — the experiment is now fully closed.** `bin/store-visibility` immediately after the push: still **0/6 anonymously**, unchanged since cycle 28. All 14 rebuilt Actors have now crossed their 24h "recognized" mark from Apify support's original reply with zero anonymous visibility change — Apify support's claim does not hold, full stop. Per cycle 161's more targeted finding (the real apify.com/store search box runs on a separate Algolia index, not the `/v2/store` REST API `bin/store-visibility` probes), the live open question is already in flight in the existing Intercom thread (asked cycle 161, Resend id `55516e26`, no reply yet as of this cycle — `d939c207` is still the newest message and it's the same one cycle 161 already read/replied to). **Did not send a second message this cycle** — the REST-API mechanism is already explained (cycles 116-119, intentional exclusion filter) and restating "14/14 now, still 0/6" would just be noise on top of an unanswered, more precise question. Next cycle: check for a reply before doing anything else on this thread.
- **Incidental fix: `/root/agent` (the git repo) was 7 commits behind `origin/main`** — the last commit actually pushed to GitHub was cycle 156's `a4ea32c`; cycles 157-162's commits (`435eb9d` through `1d0eda8`) were sitting local-only. Pushed all 7 now. This matters because every Actor README's "Source code" backlink points at `github.com/Fetchsmith/fetchsmith/tree/main/actors/<slug>` — for a week of cycles those links would have shown stale code to anyone (or any crawler) who followed them. Nothing currently checks `git status`/ahead-count as a standing rule; added one to queue.md item 3 note. Root cause not investigated (likely a `git push` that silently failed once, e.g. lock contention, and no cycle since happened to check `git status` at the repo root before finishing).
- Standing checks: 3 services active, site 200, `/tools/federal-register-scraper` 200, revenue unchanged `{public_actors:17, users:33, runs30d:0}`, inbox nothing new/actionable (2 cold-outreach threads already known, DMARC noise). No spend, no owner email. Committed and pushed.

## Cycle 162 (2026-09-12, sonnet-5, ~10min) — second pillar page: app/product-reviews cluster (App Store, Google Play, Steam), federal-register push still ~28min out

- **`federal-register-scraper`'s 11:32:38Z push mark (queue item 3, the store-visibility experiment's final checkpoint) was ~28min out at cycle start (11:00Z)** — correctly left untouched rather than waiting out most of the budget for it; next cycle (~11:30Z start) should land right at the mark.
- **Inbox re-checked, nothing new/actionable.** The `d939c207` Apify-support Intercom reply is the same message cycle 161 already read and replied to (Resend id `55516e26`) — no new reply has arrived since. `e105cd59`/`2aa190eb` re-confirmed as the known cold-outreach pattern (AI lead-gen trial pitch, branding/logo pitch) — ignored, no reply, no owner-actionable content. DMARC reports are routine noise.
- **Built the second growth-backlog pillar page (cycle 160 opened this as a follow-up): `/blog/app-store-review-apis-three-silent-empties`**, synthesising the 3 existing review-API guides (`app-store-reviews`, `google-play-reviews`, `steam-reviews`) around one shared failure shape — a successful, well-formed response that means "zero reviews" for more than one unrelated reason. Named each platform's specific mechanism (Apple: paging holes; Google Play: identity ambiguity, no separate ID-validation on the reviews call; Steam: 3 unrelated causes behind one `success:1`) and the pattern that fixes 2 of 3 (cross-check a details/identity endpoint) vs. the one that needs a different fix (Apple: sweep the full page range). Nothing asserted that wasn't already verified live in the 3 source posts.
- **Closed the cluster in both directions**: hub links out to all 3 guides + all 3 `/tools/<slug>` pages; each of the 3 spoke posts got a one-sentence backlink to the hub before its footer (matching cycle 160's pattern). All 6 URLs + `/blog` + `/tools` verified **200**. Root `README.md` Guides list updated. IndexNow submitted (200) for the new post + the 3 edited posts + `/blog`.
- **Standing checks all clean and re-run after the edits**: AI-disclosure footer present on all 19→20 posts, README backlink audit (blog/tools/source links) clean on all 17 Actor READMEs, guides-list audit clean. `bin/actor-health` **17/17 ok**, `bin/revenue` unchanged `{public_actors:17, users:33, runs30d:0}`, `bin/store-visibility` **0/6 anonymously** (status quo since cycle ~27), 3 services active, site/tools/blog 200. No owner email, no spend, no code (product) changes — copy/content only.
- **Remaining growth-backlog pillar (queue item, still open):** the *publisher/content* cluster — `substack`, `apple-podcasts`, `google-news`, `hacker-news` (4 guides, currently only cross-linked ad hoc). Same recipe as this cycle and cycle 160.
- **Next cycle: at/after 11:32:38Z, push `federal-register-scraper`** (queue item 3) — exact steps and verification are already written in `tasks/queue.md`. This is the final data point of the store-visibility experiment.

## Cycle 161 (2026-09-12, sonnet-5, ~10min) — store-visibility thread: found the real Store-search frontend uses a separate Algolia index, sent one scoped follow-up; re-confirmed KYC is a closed question, not an owner item

- **Re-read the `dac5bfcd`/`d939c207` Apify-support thread carefully instead of skimming it as old noise, and initially over-read it as new** — corrected: KYC-done + `LIMITED_PERMISSIONS` (neither named `includeUnrunnableActors` exclusion applies to us) was already established cycles 116-119, per `STATUS_ARCHIVE.md`. Not re-opening that as a finding. **What genuinely is new this cycle:** fetched the live `apify.com/store?search=...` page HTML and found its search results are driven by an **Algolia index** (`algoliaSearchParams` embedded in the page's server-rendered JSON), a separate pipeline from the `/v2/store` REST API every prior cycle's `bin/store-visibility` probe has been measuring. We have never checked whether that Algolia index applies the same (or any) exclusion filter — it's a genuinely open question, not covered by the 116-119 investigation.
- Empirically reconfirmed (not new, but verified live) the REST-API mechanism still holds exactly as support described: anonymous `GET /v2/store?search=fetchsmith` → 0/17 of our Actors; same call + `includeUnrunnableActors=true`, still fully anonymous → all 17 visible. Doesn't change any prior conclusion, just current-state-verified.
- **Sent one scoped follow-up** to the existing Intercom thread (replied to `bounces+215475896932638@outbound.intercom.apify.com`, Resend id `55516e26`) asking specifically whether the Algolia-based search page applies an equivalent filter, since that's what real buyers actually use — the REST-API bypass param (`includeUnrunnableActors=true`) is caller-side and doesn't help anonymous visitors on the actual site. This is a support-thread reply, not an owner ask; no score cost per the standing rule. Watch `bin/inbox` for a reply next cycle before doing anything else with it.
- Standing checks all clean: `bin/actor-health` **17/17 ok**, `bin/revenue` unchanged `{public_actors:17, users:33, runs30d:0}`, `bin/store-visibility` (REST-API probe) still 0/6 anonymously — status quo, expected, doesn't tell us about Algolia. 3 services active, site + /tools 200. `federal-register-scraper`'s 11:32:38Z push mark was still ~1h out at cycle start (10:30Z) — correctly left untouched, still the store-visibility experiment's final REST-API data point. No owner email, no spend, no code changed this cycle.
- **Lesson for next cycle, and written to LEARNINGS.md:** before treating an inbox thread as "already fully processed," check whether the *evidence available now* (e.g. a live page fetch) is actually new, even if the thread's prior conclusions aren't. Don't let "old thread" become a reason to stop looking.

## Cycle 160 (2026-09-12, opus-5, ~22min) — federal-register still locked (11:32Z); spent the cycle on distribution: built the government-data PILLAR PAGE and turned 9 scattered guides into an interlinked topic cluster

- **`federal-register-scraper` (11:32:38Z) was ~92min out at cycle start (10:00Z)** — correctly left untouched; it is still the store-visibility experiment's final data point. All later marks (`grants-gov` 22:42Z, `us-federal-awards` 00:31Z, `apple-podcasts` 02:08Z, `scholarship` 07:02Z) unchanged. Standing checks all clean: `bin/actor-health` **17/17 ok**, `bin/store-visibility` **0/6 anonymously** (exit 0, status quo since cycle ~27), revenue `{public_actors:17, users:33, runs30d:0}`, 3 services active, site + /tools 200. Inbox: nothing new — the owner-address `Error in the shopify actor` thread (`6ba0a151`) is the one already resolved (build 0.1.13, owner replied "All good now"), rest is the known cold-outreach spam + DMARC/Apify-bounce noise. No owner email warranted, no spend.
- **Acted on the standing "distribution, not features, is the binding constraint" rule with the highest-leverage SEO asset we did not have: a pillar/hub page.** Published `/blog/free-government-data-json-apis-no-key` — *"Eight government JSON APIs that need no key — and the specific way each one lies to you"*. It is a genuine synthesis rather than a listicle: it sorts the failure modes of all 8 government APIs we already ship Actors for (Grants.gov, NIH RePORTER, USAspending, Federal Register, openFDA, ClinicalTrials.gov, EU TED, UK Find a Tender) into **three named classes** — silent truncation (you got fewer rows than you asked for), silent zero (a typo'd *value* empties the result set), silent everything (a typo'd *field name* drops the filter and returns the whole database) — plus a 4-assertion checklist. Every claim is lifted from our own already-verified guides; nothing new was asserted about an API we had not already tested live. Targets broad queries ("free government grants API", "public tender data API") that no single per-Actor guide can rank for.
- **Closed the cluster in both directions, which is the part that actually does the SEO work.** The hub links out to all 8 guides + all 8 `/tools/<slug>` pages (17 internal links, **all verified 200**), and each of the 8 guides now carries a tailored one-sentence link *back* to the hub, placed before the footer. Before this, the 8 government guides were near-siblings that barely referenced each other — only `eu-ted` had cross-links. Site needs no rebuild (markdown is read per request, mtime-cached), so all 9 pages were live and 200 within seconds; new post is in `/blog` and `sitemap.xml`; IndexNow 200 for the new URL + `/blog` + `/tools`, and a second 200 submission for the 8 edited guides.
- **Incidental find while editing — an AI-disclosure gap, now fixed.** Audited every post for the standard *"Built and maintained by an autonomous AI worker at FetchSmith. AI-assisted, human-owned"* footer: **6 of 19 were missing it entirely** (`bold-org-nextjs-rsc-scholarship-data`, `clinicaltrials-gov-json-api`, `decode-google-news-rss-redirect-links`, `hacker-news-algolia-tags-and-not-or`, `json-ld-article-extraction-fails-on-real-news-sites`, `shopify-catalog-products-json-no-login`). Appended a **neutral variant** to those 6 + the new hub — it keeps the AI disclosure but deliberately drops the *"every JSON snippet comes from a live request made while writing this post"* clause, because I did not write those posts and cannot retroactively verify that claim. All 19 posts now disclose; 13 carry the stronger live-request claim they earned. New standing check added to queue.md.
- Root `README.md` Guides list updated with the new post (the 2e-i audit one-liner flagged it as the only miss); Actor README backlink audit re-run, clean for all 17.

## Cycle 159 (2026-09-12, sonnet-5, ~15min) — all time-gated pushes still locked; closed out the no-match edge-case sweep (all 17 Actors now covered) and found a real test-methodology gotcha along the way

- **`federal-register-scraper` (11:32:38Z) is the earliest unlockable push and was ~2h out at cycle start (09:30Z)** — not reachable this cycle's budget, correctly left untouched (an early push would burn the store-visibility experiment's final data point). `grants-gov-scraper`/`us-federal-awards-scraper`/`apple-podcasts-scraper`/`scholarship-scraper` pushes are even further out (22:42Z–next-day 07:03Z). Standing checks all clean: `bin/actor-health` **17/17 ok**, `bin/store-visibility` still 0/6 anonymously (status quo since cycle ~27), 3 services active, site 200. Inbox re-checked including the 3 `bounces+...@outbound.intercom.apify.com` threads (`8423a49f`/`dac5bfcd`/`d939c207`) that sort as "recent" by mtime — confirmed via `state/STATUS.md` cycle 149 these are the **same old KYC/`includeUnrunnableActors` thread already fully resolved cycles 116-119** (KYC done, our Actors are `LIMITED_PERMISSIONS`, neither named exclusion applies), not new information. Rest of inbox is known cold-outreach spam and DMARC noise — no owner email.
- **Used the cycle on the queue's growth backlog: no-match edge-case tests on the 3 Actors not yet covered** (`federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`), closing out the sweep started cycles 157-158 across all 17 public Actors. First two were clean `201 []` via `searchQuery`. The third, `apple-podcasts-scraper`, initially looked like a real bug: a `searchTerms` no-match query returned **one real result (Lex Fridman Podcast)** instead of empty.
- **Ran it down rather than filing it as a bug: it was a test-input artifact, not a code defect.** Curled Apple's own `itunes.apple.com/search` directly for the same term and confirmed it genuinely returns `resultCount:0`. The Actor's search loop correctly found nothing — but I had omitted the `podcasts` field from the test call entirely, and that field's `.actor/input_schema.json` entry has a non-empty `default` (the Lex Fridman URL, used to seed the example input). Apify's `run-sync-get-dataset-items` substitutes a field's schema `default` whenever the key is *omitted*, which is not equivalent to sending `[]`. Re-ran with `"podcasts":[]` explicit → clean `[]`, confirming no actual bug. Verified all three Actors use the same safe `Actor.charge({eventName:'result', count:1})` / `chargedCount===0` guard, so no billing risk either way. Full write-up in `notes/LEARNINGS.md` cycle 159 — the reusable lesson: any future no-match test on an Actor whose primary list-input field carries a non-empty `default`/`prefill` must send an explicit empty value for it, not just omit the field.
- Nothing pushed, no code changed, no owner email, no spend. Next cycle: push `federal-register-scraper` at/after 11:32:38Z (queue item 3) if in budget — it's also the store-visibility experiment's final falsification checkpoint; otherwise continue growth backlog (competitor-gap re-audit second pass, or dev.to slot 2 once ~09-13/14).

## Cycle 158 (2026-09-12, sonnet-5, ~10min) — pushed `substack-scraper` right at its 09:03:04Z mark; 2 more clean edge-case verifications

- **Pushed `substack-scraper` at 09:03:51Z (build 0.1.7), landing the README backlink fixes from cycle 156 (`cae8735`) on its Store page.** Waited out the ~3min gap from cycle start (09:00Z) rather than pushing early, per the standing store-visibility-clock HAZARD. Verified live via the build record (not the CDN-cached store page): `actorDefinition.readme` is 7506 chars and contains all 3 required backlinks (`/blog`, `/tools`, GitHub source). `isPublic:true`, memory 1024, `exampleRunInput` intact. Full `bin/actor-health` re-sweep **17/17 ok** — no regression from the push. `substack-scraper` joins the store-visibility cohort at t=0 2026-09-12T09:03:51Z (next read-out ~2026-09-13T09:04Z). 4 Actors still pending this same backlink push (`grants-gov-scraper` 22:42:06Z, `us-federal-awards-scraper` 2026-09-13T00:31:47Z, `apple-podcasts-scraper` 02:08:55Z, `scholarship-scraper` 07:02:56Z) — none reachable this cycle.
- **Used remaining time on 2 more edge-case (no-match input) verifications**, extending cycle 157's pass: `substack-scraper` (`searchQuery: "zzzznonexistentsubstackquery9999xyz"`) and `fda-recall-scraper` (`searchQuery: "zzzznonexistentproductxyz9999notreal"`) both returned clean `201 []`, no crash/timeout. Confirmed both charge strictly per-result (`Actor.charge({eventName:'result', count:1})` with `chargedCount===0` guard), so zero matches means zero charge automatically. No bug found, no code change. Remaining un-checked for this specific test: `federal-register-scraper`, `scholarship-scraper`, `apple-podcasts-scraper`.
- **Standing checks, all status quo:** 3 services active, tree clean before/after (only `state/health.json`/`revenue*.json` changed from the health sweep). `bin/revenue` unchanged `{public_actors:17, users:33, runs30d:0}`. `bin/store-visibility` **0/6 anonymously**, unchanged since cycle ~27. Backlink audit (2e-i one-liner) exits clean across all 17 READMEs. Inbox: no new items since cycle 157 — same resolved owner shopify thread, known cold-outreach spam, DMARC/bounce noise. No revenue event, nothing owner-actionable — no owner email sent, no spend. `federal-register-scraper` (item 3, visibility experiment's final checkpoint) still untouched, unlocks 11:32:38Z.
- Committed queue/STATUS updates.

## Cycle 157 (2026-09-12, sonnet-5, QUALITY/GROWTH, ~20min) — transient `steam-reviews-scraper` 502 confirmed non-regression; clean edge-case (no-match) verification on 3 Actors, no bugs found

- **All 3 time-gated pushes still out of reach at cycle start (08:30Z):** `substack-scraper` unlocks 09:03:04Z (~33min out), `federal-register-scraper` (item 3, also the store-visibility experiment's final checkpoint) 11:32:38Z, the other README-backlink pushes (grants-gov, us-federal-awards, apple-podcasts, scholarship) all 09-12/13 later. None reachable in this cycle's ~25min budget — none attempted early (would reset the store-visibility clock for no benefit, per the standing HAZARD).
- **`bin/actor-health` first sweep this cycle showed 16/17: `steam-reviews-scraper` returned `502 Bad Gateway` (HTML body), and the cron had already auto-appended a "FIX FAILED ACTORS" line to `queue.md`.** Investigated live rather than trusting a single red result: an immediate manual retry with the exact same input (`Hades`, `maxReviewsPerApp: 10`) returned `201` with 10 real review rows (rating, playtime, author fields all present and correct). A full re-sweep of all 17 Actors then came back clean. Confirmed **transient Apify-side edge-proxy hiccup, not a code regression** — same pattern as cycle 154's app-store-reviews scare. Removed the stale auto-flagged queue line and recorded the resolution rather than leaving a false-positive backlog item.
- **Used the remaining budget for real quality work per the CLAUDE.md QUALITY/GROWTH-cycle instructions: edge-case (no-match) input testing on 3 Actors that hadn't had this specific check logged** — `clinicaltrials-scraper` (`conditions: "zzzznonexistentdisease9999"`), `us-federal-awards-scraper` (`agencies: ["ZzzzNonexistentAgency"]`), `google-play-reviews-scraper` (`appIds: ["com.totally.fake.nonexistent.app.xyz123"]`). All three returned clean `201 []` — no crash, no timeout, no partial/garbage output. Also read `src/main.js` for all three to confirm they charge strictly per-result via `Actor.charge({eventName:'result', count:1})`, so a zero-match run is automatically zero-charge with no separate guard needed — good, no fix required. This was a genuine verification pass that found nothing wrong, not a missed bug; logged which Actors still haven't had this specific check (substack, federal-register, fda-recall, scholarship, apple-podcasts) as a cheap opportunistic item for a future cycle.
- **Other standing checks, all status quo:** 3 services active, tree was clean at start, `bin/revenue` unchanged `{public_actors:17, users:33, runs30d:0}`, `bin/store-visibility` **0/6 anonymously**, unchanged since cycle ~27. Inbox: no new items since cycle 156 — same resolved owner shopify thread, `e105cd59`/`2aa190eb` cold-outreach spam (ignored per standing rule), DMARC/bounce noise. No revenue event, nothing owner-actionable — no owner email sent, no spend.
- Committed queue/STATUS updates.
- **Next cycle:** if starting at/after **09:03:04Z**, push `substack-scraper` (item 2e, exact steps there). Otherwise the reachable work is more opportunistic edge-case testing (list above) or other growth backlog items; `federal-register-scraper` (item 3, 11:32:38Z) remains the highest-priority time-gated task once its mark passes.

## Cycle 156 (2026-09-12, opus-5, QUALITY/HOUSEKEEPING, ~25min) — found and closed README backlink drift on 6 of 17 Actors + 5 missing guides in the root README; trimmed STATUS.md

- **Standing checks all clean at cycle start (08:00Z):** 3 services active, `bin/actor-health` **17/17 ok**, `bin/revenue` unchanged `{public_actors:17, users:33, runs30d:0}`, `bin/store-visibility` **0/6 anonymously** (status quo since cycle ~27; `[filtered: index counts N, public sees 0]` on 4 of 6 queries as usual). Inbox: nothing new since cycle 153 — same resolved owner thread, 2 cold-outreach spams, DMARC/bounce noise. Item 3 (federal-register, 11:32:38Z) and item 5 (dev.to, 09-13/14) both out of reach, so this was a growth-backlog cycle.
- **Housekeeping first: `state/STATUS.md` trimmed 69.5KB/204 lines → 33.5KB/96 lines** (cycles 155-147 kept; 146-and-older appended verbatim to `state/STATUS_ARCHIVE.md`, now 454KB). The backlog item claiming 274KB was stale — cycle 149 had already archived cycles 71-138; the archive file is UPPERCASE `STATUS_ARCHIVE.md`, which is why a `ls STATUS_archive.md` check said it didn't exist.
- **The real find, and it is a distribution one: PLAYBOOK step 10 mandates that every Actor README carry a guide backlink, a `/tools` link and a source-code link — nothing enforced it, and 6 of 17 Actors had drifted.** Same silent-drift class as `registry.json` (item 2b) and `meta.json` (cycle 149). `us-federal-awards-scraper` had **no `## Related guides` section at all** even though `blog/usaspending-federal-awards-json-api` has existed since 09-10; `grants-gov-scraper` and `substack-scraper` were missing the `/tools` link; `apple-podcasts-scraper`, `fda-recall-scraper` and `scholarship-scraper` were missing the source-code link. This matters more than a normal copy fix because **the Apify Store renders the README server-side on a domain Google already crawls** — with Store search visibility still 0/6, those backlinks are close to the only distribution lever that is actually working. Also fixed a stale "12 HTTP-only Actors" claim in fda-recall's README (we have 17).
- **Root `README.md` audited the same way and was also stale: its Guides list had 14 of 19 posts.** Added the 5 missing (google-play, substack, clinicaltrials, grants-gov, nih-reporter) with their real titles read out of each post's front matter. The Actors table was complete at 17. All 19 guide URLs and `/tools` verified 200.
- **Pushed only `fda-recall-scraper` (build 0.1.5).** A README reaches the Store page only via `apify push --force`, and a push resets that Actor's 24h store-visibility clock (cycle-132 HAZARD) — fda-recall's own mark (2026-09-12T01:32Z) had already passed, so it was the one Actor that could ship without costing a pending data point. Verified per PLAYBOOK step 10 by reading `actorDefinition.readme` off the build record (8922 chars, source link present, stale "12 HTTP-only" gone) rather than the CDN-cached apify.com page; `isPublic`/memory 1024/`exampleRunInput` intact; post-push health `ok:true, items:10`.
- **The other 5 pushes are queued as item 2e with each Actor's exact mark time** (substack 09:03Z today, grants-gov 22:42Z, us-federal-awards 00:31Z, apple-podcasts 02:08Z, scholarship 07:02Z) — or batch them all once item 3's 11:32:38Z read-out closes the visibility experiment. The audit one-liners are now standing checks in item 2e-i.
- Committed `cae8735`, pushed to GitHub, tree clean. No spend, no owner email (copy fixes are neither revenue nor critical).
- **Next cycle:** if it starts at/after **11:32:38Z**, item 3 (`federal-register-scraper` push + final store-visibility read-out) is the top item. If earlier, item 2e's `substack-scraper` push unlocks at 09:03:04Z.

## Cycle 155 (2026-09-12, sonnet-5, ~15min) — closed queue item 2c-ii: `app-store-reviews-scraper`'s dataset schema was under-declared, not multi-record-type as suspected

- **Standing checks all clean at cycle start (07:30Z):** 3 services active, `bin/actor-health` **17/17 ok**, `bin/revenue` unchanged `{public_actors:17, users:33, runs30d:0}`, `bin/store-visibility` still **0/6 anonymously** (unchanged since cycle ~27). `federal-register-scraper`'s push mark (11:32:38Z, the store-visibility experiment's final checkpoint) was ~4h out, not reachable this cycle. Inbox: no new items since cycle 154 — same already-processed owner thread, cold-outreach spam, and DMARC/bounce noise.
- **Took item 2c-ii, the small follow-up cycle 154 opened.** `bin/check-registry-fields` flagged `app-store-reviews-scraper`'s `author` field as live-but-undeclared, and cycle 154 guessed this was the benign multi-record-type case (review rows vs app-metadata rows). Reading `src/main.js` showed that's wrong: `getAppInfo()`'s result is spread directly into the same review object (`...(info || {})`), so there is only **one flat record shape** — the schema was simply badly under-declared (10 of 22 real fields).
- **Derived every type empirically, per the cycle-154 lesson** — pulled 24 real rows from 5 recent platform runs via `/v2/datasets/<id>/items`, computed the observed type per field, and only then wrote the schema. All new fields (`author`, `authorUrl`, `updatedAt`, `voteSum`, `voteCount`, `sortUsed`, `clientClass`, `developer`, `bundleId`, `currentVersion`, `primaryGenre`, `appUrl`, `scrapedAt`) are `["type","null"]` unions matching the file's existing style — string except the 4 numeric vote/rating fields.
- **`registry.json output_fields` grown 15 → 22** to match (the product page was missing `authorUrl`, `bundleId`, `primaryGenre`, `appUrl`), summary rewritten to name the fuller field set.
- **Pushed (build 0.1.22) and verified live before trusting it**, exactly the sequence cycle 154's outage taught: read `actorDefinition.storages.dataset` back off the build record (23 properties registered, confirming the push actually took), then re-ran `bin/actor-health` — **still 17/17**, so the new type declarations didn't fail the run the way `hacker-news.storyId` did last cycle. `isPublic`/memory/`exampleRunInput` confirmed intact, `/tools/app-store-reviews-scraper` 200 with the new fields rendering, IndexNow 200, `bin/check-registry-fields` exits 0 for all 17 Actors. Committed `a8eadf3`.
- No spend, no owner email (a schema/copy fix on an already-public Actor is neither a revenue nor a critical event).
- **Next cycle:** at/after **2026-09-12T11:32:38Z**, push `federal-register-scraper` (item 3 in queue.md — code done since cycle 143) — this is the store-visibility experiment's final confirm/falsify checkpoint. If the cycle starts well before that mark, the growth-cycle backlog (`queue.md` "Growth-cycle backlog" section) is the next reachable work.


Cycles 71-138 archived to `state/STATUS_ARCHIVE.md` by cycle 149 (2026-09-12, file had regrown to 282KB/805 lines) — this file now holds only cycles 139+. Nothing lost, just not re-read every cycle.
